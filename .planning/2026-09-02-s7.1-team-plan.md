# Team Plan — obstack M6 sprint 1 (S7.1): alerts for real — rules CRUD in Postgres, claim-based evaluation against the real stores, at-least-once notification with egress fences, /app/alerts live-wired; mock mode byte-identical

meta:
- date: 2026-09-02
- repo/branch: `s7-1` (cut from master @ `930bd49`)
- mode: waves, Fable-led (user-directed minimal-model flow: Fable plans/reviews/integrates; executors delegated per task — Opus for complex, Sonnet for exactly-specified; no standing agent team)
- mode amendment (user directive, 2026-09-02, mid-wave-2): **Fable-direct from here** — no new subagent spawns; T4/T5 (already in flight) run to completion and are verified, T6/T7 and any fix work are executed directly by Fable in the main session (the S6.4 T7 precedent, now for the rest of the sprint).
- iteration-limit: 3
- iteration: 0/3
- governing docs (binding, read before executing): `.planning/2026-09-01-s7-m6-ops-suite-phase-plan.md` §5 S7.1 + §7; `.planning/2026-09-02-s7.1-alerts-engine-packet.md` (RULED — implemented VERBATIM incl. amendments D490–D492 at its tail; deviations escalate, never smoothed)

## Kickoff rulings (packet amendments, appended to the packet tail)

- **D490 — delivery decoupled from evaluation (amends D485).** The evaluator only WRITES event rows (`pending`). A second small ticker — the deliverer, 5s cadence (the metering constant) — claims `pending` events with `FOR UPDATE SKIP LOCKED`, attempts delivery (timeout 5s), increments `attempts`, marks `delivered` or (at 3 attempts) `failed`. At-least-once survives any crash point; the pre-registered "redelivery of stuck pending" collapses into the design instead of being deferred; nothing depends on a post-commit callback surviving the process.
- **D491 — event rows carry their delivery target; test notifications ride the real path (amends §4 DDL + D488).** `alert_events` gains `channel_id` (captured at emit time — later rule edits never retro-target old events; FK ON DELETE SET NULL, a null-channel pending event marks `failed`) and `attempts SMALLINT NOT NULL DEFAULT 0`; `rule_id` becomes NULLABLE. `sendTestNotification` = the action inserts a `pending` event with `rule_id NULL`, title "Test notification"; the deliverer treats it identically to a real event — D488's front-door claim is satisfied through the one path, and web never performs egress.
- **D492 — the egress policy is injected config, strict by default (amends D487).** The D487 fences (HTTPS-only, no literal IPs, no private/link-local/loopback/metadata resolution, resolver-pinned) are the DEFAULT policy; compose/CI inject a test policy (`OBSTACK_NOTIFIER_ALLOW_PRIVATE=true` shape) so the drive's local webhook receiver (compose-internal hostname = private range, plain HTTP) is reachable — without this the drive cannot prove delivery at all. A Go test proves the default is strict (env unset ⇒ all three refusals red-proven); production config never sets the escape hatch and the deploy touchpoint verifies its absence.

## Critical items (return to Fable-as-advisor before merge)
- T1 — the DDL migration (0010): with idempotency + pin evidence attached.
- T3 — the notifier: the three D487 refusals proven red under the DEFAULT policy.
- T4 — the evaluator: claim correctness, state machine, fail-open — the packet §5.1 items.
- T7 — the exit evidence: the drive's alerts arm with its proven-red run.

## Tasks

### T1: Postgres DDL 0010_alerts + migration pins
- status: approved (Fable-verified vs packet §4/D491 at `d54bf7e`; ADVISOR-SIGN pending the critical-item return with idempotency evidence — evidence already in the report: double-apply via psql all-skipping, pgmigrate suite green incl. 3x re-apply no-op)
- wave: 1 · depends: — · model: Sonnet (exactly specified)
- owns: services/ingest/pgmigrations/0010_alerts.sql (new), services/ingest/internal/pgmigrate/qa_a6_migrations_test.go (the 0010 pin — the plan's original owns line named `internal/migrate/migrate_test.go`, which is the CLICKHOUSE pin file and has no pg concept; executor STOPPED and reported per A-rule instead of inventing an edit there. Plan defect, corrected here; `internal/migrate` untouched, correctly)
- goal: the packet §4 tables VERBATIM as amended by D491 — `notification_channels`, `alert_rules` (channel_id FK RESTRICT; partial index `(next_eval_at) WHERE enabled`), `alert_events` (rule_id NULLABLE FK CASCADE, channel_id FK SET NULL, attempts, delivery CHECK, index `(workspace_id, created_at DESC)`) — 0009 house style (TEXT pks, ws FK CASCADE, CHECK never enum, UNIQUE (workspace_id, name), `<> ''` name checks), idempotent like 0001–0009.
- done-check: migration applies twice cleanly against compose Postgres; pins updated and green; `go test ./internal/pgmigrate/ -count=1`.
- result: committed `d54bf7e`, scoped. Three tables verbatim per §4+D491 (channel FK RESTRICT commented with the disarm rationale; rule_id NULLABLE; channel_id SET NULL; attempts; the claim-shape partial index; the events feed index). Idempotency proven raw via psql double-apply (all 5 objects skip on pass 2) AND through the runner (fresh-boot + 3x re-apply no-op). Pin: qa_a6 applied-set now ends `…,0009_dashboards,0010_alerts` — the one real pin site. go build clean; migrate + pgmigrate suites green.

### T2: condition schema — TS + Go + the shared parity fixture (D483)
- status: pending
- wave: 1 · depends: — · model: Opus
- owns: apps/web/src/lib/alert-types.ts (new — client-safe, no imports, the metrics-types pattern: AlertCondition union, window vocab D482, the contract row shapes from packet §0, the one condition→string formatter), services/ingest/internal/alerting/condition.go (new — struct + parse/validate), services/ingest/internal/alerting/testdata/condition-fixtures.json (new — the ONE fixture file: valid + invalid conditions with expected verdicts), both sides' tests reading that file
- goal: D481/D482/D483 VERBATIM — two source shapes (`metric` with VALID_AGGS validity re-used from `lib/metrics-types.ts` (one table, no copy — D390 held); `trace` with the two signals); TS validates on write, Go on claim; both sides accept/reject the fixture file identically; Go treats a failed parse as an eval failure signal (typed error), never a panic.
- done-check: `npm test --workspace apps/web -- alert-types` and `go test ./internal/alerting/ -run Condition -count=1` both green off the SAME fixture file; one fixture flipped locally proves both sides go red (parity is discriminating, D385 precedent).
- result: approved (Fable-verified at `548502c`, 5 files, 1166 lines). 50-fixture corpus (16 valid / 34 invalid) incl. the absent-vs-null `service` trap (Go side uses json.RawMessage for that one field — documented, fixture-pinned); red-proof both polarities on both sides; corpus self-guards (min counts, unique names) block vacuous parity; bonus D385-style vocab test parses Go source vs TS exports — KEPT (house precedent). Web 897/0 (+8), Go alerting green, typecheck/lint/vet clean. Stamps: (a) `AlertRuleRow.runbook: string | null` RATIFIED — packet §0 reconciled to §4's `TEXT NULL`; (b) NO Go condition formatter, RATIFIED as the one-formatter rule — T4's event titles compose from structured fields (rule name + observed value), never a render clone; (c) plan phrasing note: `npm test --workspace apps/web -- alert-types` runs the FULL suite (the arg doesn't filter node --test); targeted run is `cd apps/web && npx tsx --conditions react-server --test src/lib/alert-types.test.ts` — later done-checks read accordingly.

### T3: the notifier — deliverer client, egress policy, payloads
- status: pending
- wave: 1 · depends: — · model: Opus (security-bearing)
- owns: services/ingest/internal/notify/ (new package: client, policy, payload shapes, tests)
- goal: D486/D487/D492 VERBATIM — one HTTPS POST per delivery; `webhook` payload = the versioned JSON document (`{version:1, rule, event, workspace}`), `slack_webhook` = the `{text}` shape; resolver-pinned dialer refusing (default policy) non-HTTPS, literal IPs, private/link-local/loopback/metadata resolutions — checked post-DNS at dial time; response bodies capped and discarded; per-workspace delivery rate cap (constant, ops counter on breach); targets never logged, never echoed in errors.
- done-check: `go test ./internal/notify/ -count=1` — the three refusals proven red EACH under the default policy (condition §5.4); the test policy admits a private HTTP target; a payload-shape test per kind; no target string in any error text (asserted).
- result: approved (Fable-verified at `f7f41ea`, 6 files, 1330 lines; critical-item RED evidence on record — all fences proven red against a permissive stub first: the stub run genuinely dialed the metadata endpoint (item 2), and item 6 caught the real `*url.Error` full-URL credential leak, fixed by `stripURL` preserving `errors.Is`). Dial-time binding is structural: strict policy resolves, vets EVERY address (a mixed set refuses whole), dials the vetted IP literal never the name; `Dialer.Control` re-validates the exact connect(2) address; `Proxy: nil` explicit; redirects never followed (a 302 IS a failed delivery — T7 note); TLS SNI/verification unaffected. Deviations ACCEPTED as rulings: (a) `Payload.Rule` is `*RulePayload` — rule-less events emit `"rule":null`, never fabricated empties (T4 passes `&notify.RulePayload{…}`); (b) house metric prefix `obstack_ingest_notify_*` + dedicated rate-limited counter; (c) AllowPrivate also relaxes literal-IP (compose/httptest need it; still http/https only); (d) MaskTarget = `scheme://host[:port]/...`+last-4-of-path, ASCII dots, query/userinfo dropped, `(invalid target)` fallback — T5 corrected mid-flight to mirror byte-for-byte; (e) JSON HTML-escaping off + Slack mrkdwn-escaping — T7's goldens must match. One attempt per Deliver (D490); ctx is the only timeout bound — T4 sets it.

### T4: the evaluator — claim loop, eval SQL, state machine, deliverer loop wiring
- status: pending
- wave: 2 · depends: T1, T2, T3 · model: Opus
- owns: services/ingest/internal/alerting/ (evaluator, claim, metric-leg + trace-leg eval SQL, state machine, deliverer loop per D490), services/ingest/cmd/ingest/main.go (loop wiring + ordered shutdown), their tests
- goal: D477–D480, D484, D490 VERBATIM — 60s eval tick claims due enabled rules (`FOR UPDATE SKIP LOCKED`), evaluates: metric leg over `metric_points_1m` with -Merge (never FINAL, never bare SELECT — the S6.1 rule binds Go), trace leg over `trace_summaries` same combinator rule, both `workspace_id`-first with scoping proven red per leg; `ok→firing` inserts the event (severity = rule's, channel captured per D491) and stamps `last_triggered_at`; `firing→ok` inserts the `info` resolution event; eval failure = state unchanged, `next_eval_at` advanced, `alert_evals_failed_total` incremented, NO event (D480); the 5s deliverer tick (D490) claims pending, calls T3, marks. Shutdown order: stop claiming before the pool closes (main.go:331-347 pattern).
- done-check: `go test ./internal/alerting/ -count=1` against compose — proven red first (S4.3 L4): threshold-cross fires once (not per tick while firing), resolution fires once, two concurrent claimers never double-evaluate one rule (a real two-goroutine claim test), eval failure changes nothing but the counter, a delivered test proves pending→delivered and an unroutable target proves pending→failed at attempts=3.
- result: approved (Fable-verified at `3c35756`, 8 files, 2491 lines; critical-item RED evidence on record — every done-check item red against a genuinely permissive skeleton, plus an UNASKED tenancy red per leg: `workspace_id !=` inversion leaked the 9000-valued foreign seeds on both legs, reverted, cross-workspace seeds kept permanently). Race test is deterministic (two pools, claimer A parked mid-txn on a channel, B runs to completion: B evaluated 0, exactly 1 event). `FOR UPDATE OF e` on the deliverer claim is load-bearing (outer-join lock refusal + never blocking web edits behind a webhook). Escalations RULED: (1) rate-limit BURNS an attempt — T4's reading wins; a not-burned reading retries a capped event forever; T3's notify.go contract comment AMENDED in place (goes into the T7 commit); (2) payload `Condition` = Go `conditionSummary()` sentence, deliberately NOT the TS formatter's string (the one-formatter rule is about UI render; T7 goldens expect the Go sentence); (3) trace `p95_ms` is TRACE wall-clock duration (trace_summaries has no span durations) — systematically ≠ services.ts's span p95; documented in eval_trace.go, RECORDED as a known limit for the ship log + a UI-hint candidate for S7.2+; (4) service-filtered error_rate is trace-scoped — the house caveat, accepted; (5) event `link` NULL on both legs — honest absence (explore's range vocabulary can't express alert windows; ingest holds no public base URL); (6) house counter prefixes; (7) claimBatch=100/deliverBatch=10 bounds documented; (8) `attempts` counts attempts MADE (first-try success reads 1; nothing renders it), loops never touch updated_at; (9) main.go `unwindBoot` closure (vet lostcancel-driven), shutdown receiver→writer→meter→alerting→sweeper→admin, pool last. Full module `go test ./... -count=1`: 15/15 packages ok.

### T5: web query + mutation modules
- status: pending
- wave: 2 · depends: T1, T2 · model: Sonnet (S6.3 dashboards pattern, exactly specified)
- owns: apps/web/src/server/alerts.ts (new — Postgres product-store modules live at server/ root beside dashboards.ts; server/queries/ is the ClickHouse tree; plan corrected), apps/web/src/components/alerts/actions.ts (new — the components/dashboards/actions.ts "use server" shape; T6 owns the REST of components/alerts/), their tests
- goal: packet §0 contract VERBATIM — reads (rules with rendered condition string via T2's formatter + state + lastTriggeredAt; events newest-first with delivery truth; channels with targetMasked = scheme+host+last-4, NEVER the full target); mutations expose-only (S6.3-L2), lock-first (D429 pattern), `$1`-bound: rule CRUD (validate condition via `lib/alert-types.ts` on write — an unevaluatable rule is unwritable), channel CRUD (delete REFUSED while referenced — surface the FK RESTRICT as a typed refusal, not a 500), `sendTestNotification` per D491 (insert pending test event; no egress from web). Every read/write through the workspace-scoped module (D7/D11).
- done-check: `npm test --workspace apps/web -- alerts` green — including: second workspace reads `[]`/refusals (tenancy, the D440 shape), full target absent from EVERY read path (masking test), invalid condition refused with the field named, referenced-channel delete refused.
- result: approved (Fable-verified at `8da6d3d` + Fable rider `d9abb28`). 611-line module on the dashboards discipline ($1-bound, lock-first TxQuery, refuse-never-upsert, D440 absent-indistinguishable); actions expose the 8 mutations only (source-text-guarded); maskTarget re-ported mid-task from the Go twin's OWN doc comment after my correction (byte-parity + no-unicode-ellipsis tests); RED proofs real on both flagged items (defanged predicate caught by the shape check AND a genuine cross-tenant leak on real Postgres; identity maskTarget caught by 5 tests incl. integration). 945/0 full suite, 8/8 integration, typecheck/lint clean. Escalations ruled: (1) D489's rule cap was genuinely missing — landed as the `d9abb28` rider (MAX_ALERT_RULES_PER_WORKSPACE=200, counted under the create lock, at-cap refusal + below-cap pass both tested; test engine gained ruleCount); (2) no name-length cap RATIFIED — the DDL is the authority, nothing invented; (3) update-never-touches-enabled RATIFIED (a full-form submit can't flip it). Ship-log note: the executor rebuilt the ingest image + recreated ingest/clickhouse so compose carries 0010 (ClickHouse recreated EMPTY — T4 seeds its own data; drives seed fresh strangers).

### T6: /app/alerts live wiring
- status: pending
- wave: 3 · depends: T5 · model: Opus (byte-identity move + live UI)
- owns: apps/web/src/app/app/alerts/page.tsx, apps/web/src/components/alerts/ (new: AlertsMock.tsx — the current 124-line client body moved VERBATIM; AlertsLive.tsx + rule/channel form subcomponents, fed exclusively by T5 + typed via lib/alert-types.ts), apps/web/src/lib/live-routes.ts, their tests
- goal: the D367 shape — server component branching on `dataMode`; mock branch = AlertsMock verbatim (byte-identity holds, `mock/intelligence.ts` untouched — ask/evals still import it); live branch = rules table (name, rendered condition, channel, enabled toggle, state, last-triggered), events feed (severity, title, detail, link, delivery truth incl. `failed` shown honestly — D485), channel management + create/edit rule + test-notification button; honest empty states (one sentence each); fenced absences per packet §0 (no `actions`, no anomaly events) ABSENT, not disabled-with-promises; `/app/alerts` joins `liveWiredRoutes`.
- done-check: full web suite green; mock byte-identity holds (two-worktree DOM md5, the house method); live page against compose renders a T4-fired event; the resolved-specifier mock-import guard (S6.3-L1) covers AlertsLive.
- result: done, Fable-direct at `711ebde` (8 files, +993/−123). Page = the dashboards D431 shape (mock branch before any await, zero props); AlertsMock = master's page body sha256-PINNED modulo the export line (f4dc14c9…, stays a server component); AlertsLive server + AlertsEditor client (structured-condition form, agg re-anchored on type change per D390, no filters editor v1 — narrower input, not narrower truth; delivery truth rendered incl. `failed`; test/delete/toggle per channel with refusal sentences verbatim). Fenced absences per packet §0: no `actions` remediations, no anomaly events, no TerraformExport in live (guarded — it narrates fixture rules). Flip RED-proven: the registry test went red on the flip before the two controls moved (live-routes control → /app/slos + a new exact-not-subtree test; OnboardingChecklist second control → /app/oncall). Full suite 932/0, typecheck clean. Deferred to integration: the two-worktree DOM md5 (the sha pin guards source-level identity meanwhile) and the live-render-of-a-T4-event leg (T4 in flight).

### T7: drive/e2e alerts leg
- status: pending
- wave: 4 · depends: T4, T5, T6 · model: Opus
- owns: deploy/compose/e2e-drive.mjs, deploy/compose/exit-seed.mjs, deploy/compose/alert-checks.ts (new, the metrics-checks.ts pattern), compose env for the D492 test policy, README's drive section (in place)
- goal: the drive grows the alerts arm RED-first (packet §5.2): through the product's front door — create a channel targeting a drive-local webhook receiver (compose-internal; reachable under the D492 test policy), create a rule whose threshold the drive's own telemetry will cross, generate that telemetry, settle ≤ one eval cadence + one deliverer cadence (settle against observed state, never elapsed time — the S6.4 flush-race lesson), assert: event in the feed at the rule's severity, receiver got the versioned payload, delivery shows `delivered`; the failed arm: a channel with an unresolvable compose hostname proves `failed` at attempts=3, rendered honestly; the D21 flip (no SAMPLE badge on /app/alerts; `/app/ask` control still badged); no-session probe joins the wired-route sweep; hygiene sweep covers the new artifacts (no channel target in any output).
- done-check: the e2e job green locally via its house command; proven red first by withholding the rule-creating step (downstream assertions fail, never the seeder's own status).
- result: done, Fable-direct. `--leg alerts` seeds channels+rules the dashboards-leg way (UI rows, no front door — stated in the leg header); everything provable stays real: the container's evaluator claims on its own tick, fires on the metrics leg's front-door gauge (42 > 40 avg/15m), the deliverer POSTs to the drive's in-process receiver (host.docker.internal, D492 compose policy + extra_hosts host-gateway for Linux CI) and burns 3 attempts on an RFC-2606 `.invalid` host. Six new checks + the no-session probe; settle is state-observed with a 120s deadline (S6.4 lesson). GREEN 246/0 in 192s first try. RED (RED_WITHHOLD_RULES, hook kept + documented): 243/3 — EXACTLY the event/delivery/receiver trio, never the seeder's status. `alert-checks.ts` NOT created (plan allowed it): page-level assertions + the receiver's own capture cover the contract; a checks module would re-prove what the page already states. README drive section updated in place. T4's ruling-1 amendment to notify.go's rate-limit comment rides this commit.

## Escalations log

## Integration
- verdict: GO (2026-09-02, Fable direct)
- rounds: 1
- gates: drive GREEN 246/0 (192s) with the alerts arm; RED 243/3 exact trio; mock byte-identity of /app/alerts PROVEN two-build (master worktree vs HEAD, script-stripped + chunk/css-normalized DOM md5 48b62511… identical both sides); full web suite 932/0 + typecheck clean (post-T6); Go module 15/15 packages ok (post-T4); T6's deferred live-render leg discharged by the drive's D484 check (a T4-fired event renders in the feed).
- seam findings: one, fixed in-flight — Turbopack refuses an out-of-root node_modules symlink in a worktree (`Symlink … points out of the filesystem root`); the byte-identity method now clones node_modules (APFS `cp -Rc`) instead. Recorded for the next sprint's identity proof.
- fix tasks: none open.

## Goal check (Step 7, one entry per iteration)
- iteration 0 (2026-09-02, integration GO): **GOAL MET** — measured against the sprint title clause by clause. (1) Rules CRUD persists per-workspace in Postgres: 0010 DDL verbatim per packet §4+D491 (T1), the T5 module on the dashboards discipline with the D489 cap, tenancy proven on real rows both ways (T5 integration tests + the drive's bob leg). (2) Claim-based evaluation against the real stores: SKIP LOCKED claims with a deterministic two-claimer race proof, both eval legs -Merge over metric_points_1m / trace_summaries with per-leg red tenancy proofs, once-per-crossing state machine, D480 fail-open and D13 no-data both proven (T4); the drive proves the whole path on a front-door metric. (3) At-least-once notification with egress fences: the D490 deliverer (pending→delivered / failed@3, both truths in the drive), all D487 refusals red-proven dial-time under the strict default, targets masked byte-identically on both sides of the stack (T3/T5). (4) /app/alerts live-wired: flipped in liveWiredRoutes RED-first, no-session probed, no SAMPLE badge with /app/ask the standing control. (5) Mock byte-identical: AlertsMock sha-pinned to master's page body AND the two-build DOM md5 identical. Honest absences per packet §0 (remediation actions, anomaly events, links) absent, not staged.

## Retro (filled at end of run)
