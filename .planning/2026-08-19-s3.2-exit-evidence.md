# S3.2 exit bundle — advisor return VERBATIM (2026-08-19)

- sprint: **S3.2 — DB-backed API keys, invites and a real settings surface** (phase plan `.planning/2026-08-18-s3-m3-phase-plan.md` §3 S3.C2/D94; team plan `.planning/2026-08-19-s3.2-team-plan.md`)
- milestone: **M3 — Product Shell**
- status: **SIGNED by the advisor 2026-08-19, effective at the PR tip on eight green checks.** Any red check or a D137 watch trip returns to the advisor before merge.
- recorded by: manager, verbatim from the advisor's return. The D142(3) wire-evidence command block is embedded below from the integrator's gate report (the scratchpad transcript is ephemeral; this is the durable copy).

---

**STEP 7 VERDICT: MET — every clause on evidence, no clause narrowed, 0/3 iterations used.** The round-1 fix is in-charter and correct (the D139 amendment postdated T1's close; the gate is exactly where that lands). The in-TTL 200 recorded as the D146 cost before the 35s 401 is the honesty posture working — sign that line as-is.

## S3.2 — Exit-evidence bundle (advisor-signed)

**Sprint:** S3.2 — DB-backed API keys, invites and a real settings surface (M3) · branch `s3.2-keys-invites-settings` · integrator FIXED-GO round 1 · iteration 0/3 · 10/10 task-slots approved (T1–T8, F1, F2) · rulings **D138–D160** · goal MET clause-by-clause (Step 7 verdict of record).

**Exit clauses, each on evidence:**

1. **"A signed-up user issues and revokes API keys in settings"** — drive 102/0 from a cycled cold stack: key issued in the real settings surface, shown-once banner asserted, token absent from the full server render post-dismiss (Postgres holds prefix + D139 hash only, proven by the to_jsonb falsification probe), listed by prefix, revoked with a row-specific `revoked_at` poll. Cross-language hash contract held under independent recomputation on BOTH D139 vectors (ASCII + non-ASCII with latin1/utf16le decoys, load-bearing). D98 whole: fail-static keystore re-proven red by the reviewer eight further ways; revocation honored ≤`keyCacheTTL` proven on the wire (in-TTL 200 recorded as the stated D146 cost, 401 at 35s).
2. **"Invites a teammate into the workspace"** — additive-never-re-home delivered at zero opened doors (D143): `ALLOWED_ENDPOINTS` byte-identical `{get-session, sign-out}`, all invite operations via server actions on the session's own org; the drive's red run of record shows exactly the six post-acceptance claims failing under a dropped owner-pin while the "library DID flip activeOrganizationId" precondition stays green — the measured S3.1 tenancy break stays impossible, proven with a guard that shares no assumptions with the tripwire (D135). Members shows the accepted member; copy is present-tense-true (D140), email-binding stated, 48h expiry rendered honestly.
3. **"Telemetry sent with an issued key lands in that workspace and no other"** — D142(3) performed at the gate to both ruled properties: content-aware attribution (the sent trace's content token found only under the issued key's workspace, provably absent from all three others including `ws_demo`), commands verbatim with fixing values riding this bundle (embedded below). The standing wire drive step remains S3.4's pre-registered extension (D115) — stated narrowing, not a gap.
4. **"The same wire format the M1 dev keys used"** — the D98 HTTP contract byte-unchanged: collector + sdk profiles ingest under `ok_dev_local` against the seeded continuity row with zero client-side change and zero 401s; `OBSTACK_API_KEYS` at 4 hits repo-wide, all deletion prose (S2.3 L1 sweep of record, T7); one indistinguishable `ErrUnauthorized` verified at both receive seams.

**Inheritance list discharged:** D29 pair MERGED inside the sprint (T2 signed — cap 100/`_overflow` red-proved, `as_of` envelope refusing bare arrays; the M3-GATE BLOCKING constraint is closed before any second workspace's data arrives). D98 whole (above). D120 held at zero doors with the matrix widened to named stay-closed invitation endpoints under live ammunition. D106: `/signup` primary CTA live (nav/hero/footer), demo secondary, waitlist relationship stated, landing claims all true at tip; settings registered per-section — General/Members/API keys real, SAMPLE on exactly the four ruled tabs (D141). `e2e` grown in place, one drive one definition, +D160 prefix guard armed. Pattern inheritance held: D121/D127/D129 on every new form, D68 corpora, D130 integration class, zero `authConfig()` diff (ddl-drift idle by construction).

**Checks at the signed tip:** *placeholder — eight checks pending CI on the PR; signature applies at the tip once all eight are green, and the real-runner `e2e` composite number returns to the advisor per the D137 watch (three consecutive runs over the ~6-minute K5 line trigger the over-branch decision). Local composite of record: 89s.*

**Gate evidence otherwise:** post-drive Postgres exactly the D138 shape (one `ws_demo`/`org_demo` row, zero member rows referencing it, two strangers, api_keys = seed + revoked drive key); ddl-drift PASS; virgin-kind acceptance PASS 182s with the chart Postgres carrying the seed row; builds mock ○ / live ƒ (D157 documented); commit hygiene 63/63 owned or ratified, lockfiles float-free.

**SIGNATURE: S3.2 exit bundle SIGNED (2026-08-19), effective at the PR tip on eight green checks.** Any red check or a D137 watch trip returns to the advisor before merge.

## D142(3) wire evidence — the integrator's command block, verbatim (fixing values stated)

Performed at the integration gate on the cycled compose stack: own production build on `:3211`, fresh throwaway Chrome profile, fresh signup (self-started processes only, S2.3 L4). Fixing values: workspace `ws_db11d22d30ec8e1c`, key `ok_live_6d5c…fae2` (SHA-256 `8d1e39f9af3cb17c0b64a8e8f709cb90e2600a2528a319fcf48e4d179eb0fa6c`), content token `zzwiremszyuqsb6opr`, `keyCacheTTL = 30 * time.Second` (`services/ingest/internal/keystore/store.go`).

```
$ curl -s -o /dev/null -w '%{http_code}\n' -X POST http://127.0.0.1:4318/v1/traces -H 'Content-Type: application/json' -H 'Authorization: Bearer ok_live_6d5c0a13d223945da4c5b7100e222a191a6196e8a0212806ab3113f9f675fae2' --data-binary @…/trace.json
200
$ docker exec obstack-clickhouse clickhouse-client --user obstack_web --password obstack_web_dev -q "SELECT workspace_id, name, service FROM obstack.spans WHERE name LIKE '%zzwiremszyuqsb6opr%'"
ws_db11d22d30ec8e1c	zzwiremszyuqsb6opr-span	zzwiremszyuqsb6opr-svc          # content-aware: 1 in the key's ws; 0 in ws_1827566cb638c0d5, ws_18bb5c302083ec37, ws_demo
[revoke clicked in the product UI → api_keys.revoked_at stamped]
$ curl … same command …            # 0.3s after the revoke, inside keyCacheTTL
200                                # the stated cost: revocation is honored ≤ keyCacheTTL, not instantly (D146)
$ sleep 35                         # 35.3s since revoked_at — past keyCacheTTL
$ curl … same command …
401
```

*(Integrator's note of record: the scratchpad transcript's one FAIL line was the runner's own baseline arithmetic — it compared against a count taken before the in-TTL accepted send; re-measured after: 2 spans = 2 accepted sends, the 401 send wrote none.)*

## S3.3 kickoff MUST inherit (binding list):

- **The D98 cache widens into the D99 workspace-state cache** — key validity + over-quota flag + pricing overrides, one cache, one TTL, one staleness story; **D155 pre-registered: the memory story is restated for the widened entry size and large-tenant row counts at S3.3 breakdown.**
- **Liveness lands where D138 refused it:** per-API-key health rows (D100) carry last-event-at/accepted/dropped — no `last_used_at` column ever joins `api_keys`.
- **D108 overrides on the D9 row shape through that cache; the Data & ingest tab loses its SampleMark and shows `as_of` in S3.3 (D141) — one touch of that tab.** Billing & usage loses its SampleMark per D106.
- **D110 Polar contract whole:** ledger authoritative, Polar never the quota source, ingest never calls Polar, sandbox for evidence / deterministic fake in CI, the four API-shaped facts MEASURED via the Polar MCP at breakdown and attached.
- **UsageBanner leaves `@/mock/workspace`; the layout.tsx live-mode suppression flips (D99).**
- **Pattern inheritance standing:** D121/D127/D129, D68, D130, D11, D142's content-aware-evidence bar, the D150/D152 mock-mode action guard on every new auth-surface server action.

## Pre-registered residue (NOT S3.3 work):

**S3.4** — issued-key wire drive step (D115), quickstart/onboarding rework (incl. its D144-conformant copy). **S3.5** — switcher affordance + demo-workspace footer (D134), landing "no signup, no setup" sentence + waitlist copy + pricing copy truth (D106 fence), palette/changelog (D60/D63), TopBar residue. **M4** — `BETTER_AUTH_URL` hosted env inventory (D119) + `BLOB_READ_WRITE_TOKEN`, `postgres.password` rotation (D123), NoSessionError log volume (D132), expired-invitation lifecycle (D153), **build-env-equals-serve-env enforcement at its raised stakes (D157: costs signup entirely)**. **Unowned debt recorded at this gate:** 6 pre-existing eslint errors in non-S3.2 files with no CI lint job — absorbed by whichever sprint first adds lint to CI (M4's CI surface is the natural home); recorded so it is chosen, not discovered.
