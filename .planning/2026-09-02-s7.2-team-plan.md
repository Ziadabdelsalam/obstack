# Team Plan — obstack M6 sprint 2 (S7.2): changes for real — POST /v1/changes under api-key auth, change_events in Postgres, /app/changes live-wired, the D362 deploys-panel release, the GitHub Actions recipe executed end-to-end; mock mode byte-identical

meta:
- date: 2026-09-02
- repo/branch: `s7-2` (cut from master @ `19d3e7e`)
- mode: **Fable-direct, session-wide (user directive 2026-09-02, at kickoff)** — no subagent spawns; every task T1–T7 is executed by Fable in the main session, sequentially in wave order; the S7.1 mid-sprint amendment applied from the start.
- iteration-limit: 3
- iteration: 0/3
- governing docs (binding, read before executing): `.planning/2026-09-01-s7-m6-ops-suite-phase-plan.md` §5 S7.2 + §7; `.planning/2026-09-02-s7.2-changes-feed-packet.md` (RULED — implemented VERBATIM, D493–D504; deviations escalate, never smoothed)

## Kickoff notes

- Packet ruled as drafted (2026-09-02); no kickoff amendments. Rulings numbered from D505 if the breakdown surfaces seams.
- Model routing: VOID for this run — Fable-direct throughout (user directive). The per-task `model:` lines below record what the routing WOULD have been, for the retro's calibration only.
- The A-rule still binds Fable-as-executor: a plan defect found mid-task is corrected in the plan and recorded, never smoothed.

## Critical items (return to Fable-as-advisor before merge)
- T1 — the DDL migration (0011): idempotency + pin evidence attached.
- T3 — the endpoint: the strict document's refusals and the tenancy proof (workspace from the credential only), the durable-2xx claim (503 on storage failure, proven), the 429 guard.
- T4 — D501: the Postgres sweep over BOTH tables with the other-workspace-untouched proof — the S7.1 carry-forward, signed here.
- T7 — the exit evidence: the drive's changes arm with its proven-red run, the docs recipe executed verbatim.

## Tasks

### T1: Postgres DDL 0011_change_events + migration pin
- status: done (Fable-direct; see result)
- wave: 1 · depends: — · model: Sonnet (exactly specified)
- owns: services/ingest/pgmigrations/0011_change_events.sql (new), services/ingest/internal/pgmigrate/qa_a6_migrations_test.go (the applied-set pin, `want` string at :178)
- goal: packet D500 VERBATIM — one table `change_events` (TEXT pk `chg_`, workspace FK CASCADE, `key_id` FK api_keys SET NULL, kind CHECK IN six, every length CHECKed in the DDL — the authority, the S7.1 T5 ruling — `link_label`/`link_href` both-or-neither CHECK, `at` + `created_at`), three indexes (feed `(workspace_id, at DESC)`; deploys partial `(workspace_id, service, at DESC) WHERE kind = 'deploy'`; dedupe partial UNIQUE `(workspace_id, external_id) WHERE external_id IS NOT NULL`); 0010 house style incl. the header comment stating the why of key_id/external_id/at; idempotent like 0001–0010.
- done-check: migration applies twice cleanly against compose Postgres (psql double-apply: every object skips on pass 2); pin updated; `go test ./internal/pgmigrate/ -count=1` green with `OBSTACK_TEST_POSTGRES_DSN` set (incl. the 3x re-apply no-op).
- result: committed `6bd8066`, scoped to the two owned files. Table verbatim per D500 (15 columns, every length CHECK in the DDL, the both-or-neither link CHECK) + the three indexes; psql double-apply against compose: pass 1 creates 4 objects, pass 2 skips all 4; pgmigrate suite green off the runner (fresh schema + the 3x re-apply no-op) with the pin now ending `…,0010_alerts,0011_change_events`; go build + vet clean. Note for T3/T4: compose Postgres already holds the table (applied raw); the runner records 0011 on the next ingest boot as a no-op.

### T2: web read modules — `lib/change-types.ts` + `server/changes.ts`
- status: done (Fable-direct; see result)
- wave: 2 · depends: T1 · model: Sonnet (S7.1 T5 / dashboards discipline, exactly specified)
- owns: apps/web/src/lib/change-types.ts (new, ZERO imports — the metrics-types rule), apps/web/src/server/changes.ts (new — Postgres product-store modules live at server/ root beside alerts.ts), apps/web/src/server/changes.test.ts (new; unit + integration against compose Postgres, the alerts.test.ts pattern)
- goal: packet §0 contract VERBATIM — `ChangeKind` (the one TS definition), `ChangeEventRow`, `listChangeEvents(ws, limit, query)` newest-first by `at`, `listServiceDeploys(ws, service, limit, query)` (`kind = 'deploy' AND service = $2`), `$n`-bound, `link` folded from the two columns to `{label, href} | null`, `at` as ISO UTC. NO mutations, NO actions file (nothing to expose; state it in the module doc). Every read through the workspace-scoped module (D7/D11).
- done-check: `cd apps/web && npx tsx --conditions react-server --test src/server/changes.test.ts` green incl.: second workspace reads `[]` for both reads against real rows (the D440 shape, proven red by a defanged predicate first); limit honored; newest-first by `at` not `created_at` (a backfilled row sorts by its own time); link both-or-neither round-trips; full suite + typecheck + lint clean.
- result: committed `aea0711`, 4 files. `lib/change-types.ts` (zero imports; `ChangeKind` + `CHANGE_KINDS` + the two row shapes) and `server/changes.ts` (READS ONLY — two `$1`-bound statements, `ORDER BY at DESC, id DESC`, the link folded from two columns, limit clamped; no lock, no TxQuery, no actions file — asserted by a source-text test). Unit 6/6 hermetic (workspace binding on both reads; orders by `at` never `created_at`; deploys scoped `kind = 'deploy' AND service = $2`; limit clamp; §0 row mapping incl. ISO UTC and link/null). Integration 3/3 against compose Postgres (rows seeded raw — the module has no writer): two workspaces disjoint on BOTH reads + an empty workspace reads `[]`; a backfill received last sorts by its own time; the deploys read excludes other kinds, other services, other workspaces, and honours the limit. RED by sabotage (workspace predicate defanged to `$1 IS NOT NULL`): the shape test + all 3 integration tests red, restored. Full web suite green with the DSN set (960/0 after restoring the local compose `plans` catalog to its 0005/0006 seed — free's event_quota 300→50000 and explain_quota 2→20 were drift from earlier drives' quota legs, not S7.2; 4 usage/explain-quota integration tests read the catalog and were red on the drifted DB); tsc clean; eslint clean.

### T3: the endpoint — `POST /v1/changes` (document, handler, guard, counters)
- status: done (Fable-direct; see result)
- wave: 2 · depends: T1 · model: Opus (the strict validator on the receive path, the durable-2xx contract, the first 429)
- owns: services/ingest/internal/changes/ (new package: document.go — the D499 struct with json tags + validator; handler.go — auth → decode → validate → insert → respond; ratelimit.go — per-workspace token bucket; counters; tests incl. a compose-Postgres suite), services/ingest/internal/receive/http.go (ONE route line `POST /v1/changes` + the handler hook — no other change), services/ingest/cmd/ingest/main.go (wiring only: pool + meter + auth into the changes handler), services/ingest/internal/metering/metering.go (ONE new method `RecordAcceptedChange(workspaceID, keyID)` = health `accepted` +1, never the ledger — the `RecordAcceptedMetrics` body shape; and the existing `RecordDropped(…, DropDecode, 1)` for 400s)
- goal: packet D493–D499 VERBATIM: JSON only (415 shape from `requestEncoding`), 64 KiB `MaxBytesReader` (413), `DisallowUnknownFields` (400 `"<field>: <reason>"` for every refusal incl. unknown/`workspace_id`/`key_id` fields, array bodies, bad kind, empty/over-long title, future `at` > 5 min, non-absolute or non-http(s) href, both-or-neither link), synchronous `$n`-bound INSERT with `workspace_id`/`key_id` from `Identity` (201 `{"id","deduplicated":false}`), `external_id` repeat → 200 with the ORIGINAL id + `deduplicated:true` and NO write (the UNIQUE violation path, first write wins), Postgres failure → 503 `{"error":"storage unavailable"}`, per-workspace bucket 60/min burst 60 → 429 + `Retry-After: 60`; ids `chg_` + 16 hex (the `newEventID` shape); counters `obstack_ingest_changes_{accepted_total{kind},refused_total{reason},deduplicated_total}`; health `accepted`/`dropped_decode` per D497. No quota check, no sampling, no batcher.
- done-check: `go test ./internal/changes/ -count=1` against compose (`OBSTACK_TEST_POSTGRES_DSN`) — every refusal and both 2xx shapes proven RED first against a permissive skeleton (S4.3 L4), plus: the inserted row's `workspace_id` equals the credential's and a body naming another workspace is a 400 (tenancy), the dedupe answer carries the FIRST row's id and the second body's title is nowhere in the table, the 503 path proven with the pool closed, the 61st post in a minute is 429; `go vet ./...` clean; full module `go test ./... -count=1` green.
- result: committed `424b424`, 12 files. PLAN CORRECTION (A-rule, recorded): the HTTP glue lives in `internal/receive/changes.go` beside the Vercel/CloudWatch handlers — auth, `countDrop`, the workspace slot, `writeJSONError` and the panic recovery's JSON dialect are all there already; `internal/changes` holds the pure parts (document validator, store, limiter, counters). RED evidence on record: 27 refusal subtests + trim + UTF-8 + all 3 store tests red against a permissive skeleton (loose decode, allow-all limiter, plain insert), and 8/10 receiver tests red (only the 401 and the panic-dialect tests passed — pre-existing house machinery). GREEN: changes pkg (37 tests incl. 3 compose-Postgres store tests: dedupe returns the FIRST id and the second body is nowhere; same external_id in two workspaces = two rows; FK-gone key → ErrUnknownKey; closed pool → ErrStorage), receiver 10/10 (401/415×5/413/400×6 with the field named/201 shape/200 dedupe/503/401-on-gone-key/500-JSON-on-panic/429 at the 61st with Retry-After 60 and bob untouched), metering D497 test, full module 15/15. Rulings taken in-flight: (a) invalid UTF-8 is a BODY-level decode refusal — encoding/json launders bad bytes to U+FFFD before any field check, so the bytes are checked first (RFC 8259 §8.1); (b) 415s count `dropped_unsupported` (the OTLP path's column for an encoding it does not take), 413/400s count `dropped_decode` per D497; (c) the limiter runs right after auth so a flood costs no parse and no Postgres, and refused posts do not touch the health row (D162: health counts what the path could count); (d) a store-refused-after-validation (CHECK violation) is OUR bug → 500, counted under `storage`; a FK violation (key/workspace gone inside the 30s cache window) → 401. Deferred to T7: the live compose proof (the ingest image is rebuilt once T4 lands).

### T4: D501 — the Postgres retention leg (alert_events + change_events)
- status: done (Fable-direct; see result)
- wave: 2 · depends: T1 · model: Opus (a delete loop inside the house sweeper)
- owns: services/ingest/internal/retention/retention.go, services/ingest/internal/retention/*_test.go (the Postgres leg's tests)
- goal: packet D501 VERBATIM — the per-workspace pass (same `plansSQL`, same `retention_days`, same cadence) gains a Postgres leg: `alert_events` by `created_at`, `change_events` by `at`, `DELETE … WHERE ctid IN (SELECT ctid FROM t WHERE workspace_id = $1 AND <col> < now() - ($2 * interval '1 day') LIMIT 5000)` looped to zero, one statement timeout per statement, counted on the sweeper's existing per-table counters (new label values, same metric). The ClickHouse leg untouched. Ship-log line: the S7.1 defect (D489's retention half unassigned, integrator unchecked) is recorded in this plan's Integration section.
- done-check: `go test ./internal/retention/ -count=1` against compose — proven red first: a row outside the window goes, a row inside survives, the OTHER workspace's out-of-window rows are untouched (tenancy), both tables, the loop terminates on > 5000 rows; ClickHouse suite still green.
- result: committed `f7ad8fc`. PLAN CORRECTION (A-rule, recorded): the sweeper has NO per-table counters (only `sweeps_total` and `sweep_failures_total`, unlabelled) — the packet assumed some; since a Postgres DELETE reports its row count where ClickHouse's lightweight DELETE does not, the honest addition is a NEW `obstack_ingest_retention_postgres_rows_deleted_total{table}`. Design as ruled otherwise: two seams (`plansReader`, `pgDeleter`) factored out of `New` so the Postgres leg is provable WITHOUT ClickHouse; `pgDeletes` = alert_events by `created_at`, change_events by `at`, ctid-batched at 5000 and looped until short; failure semantics identical to the ClickHouse leg (count, log, skip, retry next interval); a sweeper built without the leg sweeps nothing there. Tests: 3 unit (fan-out incl. the 2-full-batches-then-short drain = 3+1+1+1 statements; continue-past-a-failed-table; the statements carry their ruled columns + `make_interval(days => $2)`) + 1 Postgres integration proof in a throwaway migrated schema (free=1d vs pro=30d; alert_events 2 old + 1 fresh; change_events a 5001-row backlog OLD-by-`at` but RECEIVED now + 1 fresh — free drains to exactly the fresh row on both tables, pro's 3-day-old rows untouched = the tenancy half). RED by sabotage, restored: (A) workspace predicate removed → both the SQL-shape unit test and the integration proof red; (B) change_events cut on `created_at` → the 5002 rows survive, proof red on exactly that assertion. Retention 13/13; full module green.

### T5: the flip — `/app/changes` live + the D362 release on `/app/services/[id]`
- status: done (Fable-direct; see result)
- wave: 3 · depends: T2 · model: Opus (byte-identity move on two routes + a fence release)
- owns: apps/web/src/app/app/changes/page.tsx, apps/web/src/components/changes/ChangesMock.tsx (new — master's page body VERBATIM, sha-pinned modulo the export line), apps/web/src/components/changes/ChangesLive.tsx (new, server component), apps/web/src/app/app/changes/page.test.ts (new: the D431 shape guard + the sha pin + the mock-import guard), apps/web/src/lib/live-routes.ts + live-routes.test.ts (`/app/changes` exact-not-subtree, RED-first), apps/web/src/app/app/services/[id]/page.tsx (the ONE mock import DELETED; live branch reads `listServiceDeploys(ws, id, 3)`), apps/web/src/components/services/ServiceDetailLive.tsx (SampleMark + `DeploySample` gone; the §0 row shape; the empty state), apps/web/src/app/app/services/page.test.ts (flips: zero `@/mock/` imports in `[id]/page.tsx`; the SampleMark string ABSENT)
- goal: packet D502 + D503 VERBATIM — mock branch before any await, zero props to ChangesMock; ChangesLive renders `listChangeEvents(ws, 100)` in the timeline chrome with its own six-row style table keyed by `ChangeKind`, absolute UTC times, the `source` label, external links `rel="noopener noreferrer" target="_blank"`, the honest empty state linking `/docs/connectors/github-actions`; absent-not-staged: the INC-42 badge, product-internal links, pagination, `regression`. `ServiceDetailMock`, `mock/changes.ts`, `mock/intelligence.ts` untouched.
- done-check: full web suite green; the two guards went RED before the flips (evidence in the report); sha pin holds; typecheck/lint clean; the two-build DOM md5 for both routes is discharged at integration (Fable, the S7.1 method).
- result: committed `7affab8`, 9 files. RED on record before any flip: the new `changes/page.test.ts` (ENOENT on ChangesMock), `live-routes.test.ts` (the exact-route test) and the two flipped `services/page.test.ts` guards all red. ChangesMock GENERATED from `git show master:…/changes/page.tsx` with only the export line rewritten — sha256 `1d0e828f…` pinned (round-trips); page in the D431 shape (mock branch before any await, zero props; `listChangeEvents(ws, 100)`); ChangesLive a server component with its own six-row style table, the alerts feed's `YYYY-MM-DD HH:MM UTC` clock, `via <source>` label, ref/service chips, EVENT links as external anchors (`target=_blank rel=noopener noreferrer` — guarded), the ONE `next/link` being the empty state's recipe link (eslint's `no-html-link-for-pages` corrected a too-broad first guard); absent-not-staged and guarded: `INC-42`, any incident boolean, product-internal event links. `/app/changes` in `liveWiredRoutes` exact-not-subtree. D503: `services/[id]/page.tsx` names NO mock module (the `deploys` fixture import deleted; live branch `Promise.all` of the scorecard + `listServiceDeploys(ws, id, 3)`); `ServiceDetailLive` drops the SampleMark and `DeploySample`, renders `ref ?? title` · clock · who · external run link, honest empty state with the recipe link; `regression` absent (guarded). `ServiceDetailMock` + both fixtures untouched. Guards 4/4 + 15/15 + 4/4; full web suite green; tsc + eslint clean. Deferred to integration (Fable): the two-build DOM md5 for `/app/changes` and `/app/services/[id]` in mock mode.

### T6: the docs page + the recipe + the D499 three-reader pin
- status: done (Fable-direct; see result)
- wave: 3 · depends: T2, T3 · model: Sonnet (content + one source-parsing test, exactly specified)
- owns: apps/web/src/content/docs/connectors/github-actions/index.mdx (new), apps/web/src/content/docs/manifest.ts (one entry under Connectors, after aws-cloudwatch), apps/web/src/content/docs/what-obstack-does-not-do/index.mdx (the fence line), apps/web/src/lib/change-types.test.ts (new — the D499 pin: Go struct tags in `services/ingest/internal/changes/document.go` ↔ the TS union/row ↔ the MDX field table, one test, three readers)
- goal: packet D504 VERBATIM — the endpoint, the header in the quickstart's exact words, the field table (names, required/optional, limits — from D499), the secret sentence, the ONE `curl`+`jq` step exactly as ruled (the reader edits `service`); the `run:` block is a single fenced `yaml` block the drive can extract by a stable marker (state the marker in the MDX comment). Fence line in the absences page: one endpoint — no connector catalog, no polling, no flag-provider SDKs.
- done-check: docs route renders the page (the manifest test); the pin test goes RED when one field is renamed on any of the three sides (evidence: three flips, three reds); full suite green.
- result: committed `7e94210`, 10 files. `connectors/github-actions/index.mdx` (Connectors, after aws-cloudwatch): status in the Vercel/CloudWatch posture (built, CI-exercised, card stays coming soon, self-hoster today), the route + the quickstart's exact header sentence, the D499 field table (13 rows incl. `link.label`/`link.href`, every limit stated), the secret sentence, the answer list (201/200-dedupe/400/401/413/415/429/503 — never accepted-then-lost), the ONE curl+jq step between `{/* recipe:start */}`/`{/* recipe:end */}` markers the drive extracts by, where-it-shows, the fence. `lib/change-types.test.ts` = the D499 pin, three readers: Go `kinds` ↔ `CHANGE_KINDS` ↔ the docs kind row; Go json tags (top + `link.*`) ↔ the docs table's field names (set-equal both ways); every Go `max*` constant ↔ the number its docs row states; the recipe block's shape (one yaml fence, `run: |`, posts `/v1/changes`, Bearer, `GITHUB_RUN_ATTEMPT`, no host of its own); change-types import-free. Overview: GitHub Actions joins the routes-today paragraph (three now; "the other thirteen"). PLAN CORRECTIONS (A-rule, recorded): (a) `what-obstack-does-not-do` carried two STALE claims — "no alerts … no issues … no change tracking" (alerts real since S7.1, issues since S6.2) and "the services deploys panel renders sample data" — both rewritten to the truth after S7.2; the D264b ingest-routes line now names `/v1/changes`; the fence paragraph added; (b) `Quickstart.test.ts` banned any `${` in a docs page as an unresolved template hole — the recipe must carry GitHub Actions' `${{ }}` verbatim, so the sweep now ignores exactly `${{` (a JS hole is `${` not followed by `{`), and the one bash `${VAR::7}` slice became `printf '%.7s'`; (c) FIVE deliberate pins moved for the fifteenth page: doc-links corpus links 20→21 (both arms), manifest 14→15, mirror corpus 14→15, landing-fence docs surface 14→15, and mirror's "other N cards" derivation now subtracts the THREE coming-soon cards with a route (was two). Gates: docs 12/12, doc-links 6/6 against a fresh mock build (prerender arms included), mirror 18/18, landing-fence 11/11, corpus-honesty 6/6, Quickstart 19/19, change-types 5/5; full web suite green; tsc + eslint clean.

### T7: drive/e2e changes leg
- status: done (Fable-direct; see result)
- wave: 4 · depends: T3, T5, T6 · model: Fable-direct (standing precedent)
- owns: deploy/compose/e2e-drive.mjs (the `--leg changes` arm + the MDX extractor), deploy/compose/README.md (drive section), deploy/compose/exit-seed.mjs only if a seeder leg proves necessary (expected: none — the front door is the seed)
- goal: packet §5.2 VERBATIM — the docs `run:` block extracted and executed under bash with `OBSTACK_INGEST_URL`/`OBSTACK_API_KEY` (alice's UI-issued key) and the `GITHUB_*` set (real on CI, faked locally) for the trace leg's own service name; a `config` event posted directly; the duplicate `external_id` → 200 deduplicated and rendered once; a `javascript:` href → 400 naming `link.href`; keyless → 401; bob's key posts one alice never sees. Assertions on `/app/changes` (both titles, the source label, no SAMPLE, no `INC-42`), on `/app/services/<service>` (ref + who; the SampleMark string ABSENT), on the raw responses. `RED_WITHHOLD_CHANGES` skips the posts.
- done-check: the e2e job green locally via its house command; proven red first by withholding the posts (downstream assertions fail, never the step's own status); the token in no output.
- result: committed `112b2b8`, 2 files (`e2e-drive.mjs` +170/−3, README drive section). The arm lifts the `run:` block from the docs page between the recipe markers, dedents it, makes the ONE documented edit (`service` → `CHAIN_AGENT`, asserted to be exactly one substitution) and runs it under `bash -euo pipefail` with `OBSTACK_INGEST_URL`/`OBSTACK_API_KEY` (the quickstart's key, environment only) and the `GITHUB_*` context (`process.env` first — real on the Actions runner — stood in for locally with a sha1-of-RUN sha). Seven checks: 201 + `chg_` id from the step; the SAME step again → 200 deduplicated with the ORIGINAL id (D496); a flag event 201 + `javascript:` href 400 naming `link.href` + keyless 401; `/app/changes` renders both events newest-first with `via github-actions`/`via drive`, no SAMPLE, no INC-42; the deploy's link as an external anchor to the run URL with the D499 rel; `/app/services/<agent>` names the ref + actor with the mark AND the empty sentence gone (D362 released); bob's feed empty with none of her words. The S6.2 "MARKED rather than hidden" check FLIPPED to "no longer marked; the honest empty sentence before any deploy is posted". No seeder leg (the front door is the seed — as expected). GREEN 253/0 in 188s (246 + 7); RED (`RED_WITHHOLD_CHANGES=1`, hook kept + documented) 247/6 — EXACTLY the six answer/page checks, never the leg's preconditions (bob's empty-state check stays true by construction). Observed once in four local runs, pre-existing and unrelated: the saved-views leg's "after a reload onto a different URL, the view is still listed" raced its own reload (the list read `No saved views` right after the create) — passed in the other three; registered for the S6.4 settle-on-the-sample lesson, not touched here. Tenancy reading: the drive holds no key for bob, so the inverse (bob's key never reaching alice) is proven at the store (Go) and module (T2 integration) layers, the drive proves alice→bob. Token hygiene: the key rides only in the child's environment; the drive's sweep found it in none of its outputs.

## Escalations log

## Integration
- verdict: pending
- rounds: 0
- gates:
- seam findings:
- fix tasks:
- carry-forward defect (S7.1): D489's retention half was ruled, assigned to no task, and not checked at the S7.1 integration gate — `alert_events` had no expiry until T4 here. Recorded on the S7.1 ship log by reference.

## Goal check (Step 7, one entry per iteration)

## Retro (filled at end of run)
