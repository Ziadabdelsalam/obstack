# Team Plan — obstack M6 sprint 3 (S7.3): SLOs for real — definitions CRUD in Postgres, burn computed by a second claim on the S7.1 ticker over trace_summaries, breach/at-risk transitions delivered through the S7.1 pipeline, `/app/slos` live-wired; mock mode byte-identical

meta:
- date: 2026-09-02
- repo/branch: `s7-3` (cut from master @ `56024ac`, the RULED packet commit)
- mode: **Fable-direct, session-wide (user directive, S7.2 precedent carried forward: "go ahead")** — no subagent spawns; every task T1–T7 is executed by Fable in the main session, sequentially in wave order.
- iteration-limit: 3
- iteration: 0/3
- governing docs (binding, read before executing): `.planning/2026-09-01-s7-m6-ops-suite-phase-plan.md` §5 S7.3 + §7; `.planning/2026-09-02-s7.3-slos-packet.md` (RULED — implemented VERBATIM, D505–D517; deviations escalate, never smoothed).

## Kickoff notes

- Packet ruled as drafted (2026-09-02); (b) held loosely by Fable, ratified by the user — route scoping stays pre-registered. Rulings numbered from D518 if the breakdown surfaces seams.
- Model routing: VOID for this run — Fable-direct throughout. The per-task `model:` lines record what the routing WOULD have been, for the retro's calibration only.
- The A-rule binds Fable-as-executor: a plan defect found mid-task is corrected in the plan and recorded, never smoothed.
- SEQUENCE CORRECTION (A-rule, recorded at T1 close): the D517 fixture file is Go `testdata`, so T3 (the Go parser, arithmetic, text — and the fixture) runs BEFORE T2 (the TS contract + store, which reads the fixture and the Go vocabulary declarations); each commit is then green on its own. T2's `owns` loses the fixture file; T3 gains it.
- Lessons carried in from the S7.2 retro, as plan items: the absences page and the tour step are DONE-CHECK items of the flip (T6 is its own task so neither can be "later"); one command, one chain, braces around the tail for every guarded commit; the drive's compose `plans` drift is a known local-only red on four usage tests — restore the seed before the full web suite, and never read it as an S7.3 failure.

## Critical items (return to Fable-as-advisor before merge)
- T1 — the DDL migration (0012, the track's first ALTER): raw double-apply + runner idempotency evidence attached.
- T3/T4 — the arithmetic (D509) and the state machine (D511): the table test and the transition-sequence integration proof, plus the two-claimer proof re-run for `slos`.
- T5 — the flip: the sha pin, the D440 tenancy proof on the store, the unwired controls moved (registry test AND the onboarding checklist test), the D324 status-link guard extended to both new components.
- T7 — the exit evidence: the drive's slos arm with its proven-red run; the independent ClickHouse recomputation matching the card.

## Tasks

### T1: Postgres DDL 0012_slos + migration pin
- status: done (Fable-direct; see result)
- wave: 1 · depends: — · model: Sonnet (DDL in a stated house style with one new idiom — the guarded ALTER)
- owns: services/ingest/pgmigrations/0012_slos.sql (new), services/ingest/internal/pgmigrate/qa_a6_migrations_test.go (the applied-set pin, `want` string at :178)
- goal: packet D513 VERBATIM: `slos` (id `slo_`, workspace FK CASCADE, name UNIQUE per ws `<> ''`, indicator JSONB object CHECK, target NUMERIC(6,3) CHECK (0,100) exclusive, "window" CHECK IN ('7d','30d'), channel_id NULL FK notification_channels ON DELETE RESTRICT, enabled, status CHECK IN four DEFAULT 'no-data', current_pct/budget_burned_pct DOUBLE PRECISION NULL, good_count/total_count BIGINT NULL with the both-or-neither CHECK, evaluated_at/last_transition_at NULL, next_eval_at DEFAULT now(), created_at/updated_at); index `(next_eval_at) WHERE enabled`; `ALTER TABLE alert_events ADD COLUMN IF NOT EXISTS slo_id TEXT NULL REFERENCES slos (id) ON DELETE CASCADE`; partial index `alert_events (slo_id) WHERE slo_id IS NOT NULL`; the exclusivity CHECK `(rule_id IS NULL OR slo_id IS NULL)` added under a `pg_constraint` existence guard (DO block). Pin gains `,0012_slos`.
- done-check: psql double-apply against compose (pass 1 creates every object, pass 2 a no-op — every statement guarded); `go test ./internal/pgmigrate/ -count=1` green (fresh schema + the re-apply no-op + the pin); `go build ./... && go vet ./...` clean. Evidence lines in the result.
- result: committed (see log), scoped to the two owned files. PLAN CORRECTION (A-rule, recorded): the packet's `"window"` column is a RESERVED word in Postgres 17 (`create table t (window text)` is a syntax error) — the column is `eval_window TEXT CHECK (eval_window IN ('7d','30d'))`, the same vocabulary string the TS and Go sides carry (single-sourced across three readers), and the DDL comment says why. Otherwise D513 verbatim: `slos` (19 columns incl. the both-or-neither counts CHECK, target NUMERIC(6,3) strictly inside (0,100), status DEFAULT 'no-data'), the partial claim index, the track's FIRST `ALTER TABLE alert_events ADD COLUMN IF NOT EXISTS slo_id … ON DELETE CASCADE`, the partial `(slo_id)` index, and the exclusivity CHECK `(rule_id IS NULL OR slo_id IS NULL)` under a `pg_constraint`/`regclass` guard (a DO block — the runner executes a file whole in one transaction, no splitter). Evidence: psql double-apply against compose — pass 1 `CREATE TABLE / CREATE INDEX / ALTER TABLE / CREATE INDEX / DO`, pass 2 four `already exists, skipping` NOTICEs + the DO a no-op (pg_constraint holds `alert_events_one_producer_check` exactly once; `alert_events` now has 11 columns ending `slo_id`); `go test ./internal/pgmigrate/` green with the pin ending `…,0011_change_events,0012_slos` (fresh schema + the re-apply no-op both PASS, not skipped); `go build ./... && go vet ./...` clean. Note for T2/T4: compose Postgres already holds the objects (applied raw); the runner records 0012 on the next ingest boot as a no-op.

### T2: web contract + store — `lib/slo-types.ts`, `server/slos.ts`, the events feed learns `slo`
- status: done (Fable-direct; see result)
- wave: 2 · depends: T1, T3 (sequence correction above) · model: Opus (the D517 validator is half a cross-language contract; the store is lock-first with the D440 sentences)
- owns: apps/web/src/lib/slo-types.ts (new, ZERO runtime imports), apps/web/src/lib/slo-types.test.ts (new — the TS half of D517 + the formatter + the arithmetic mirror, reading T3's fixture and vocabularies), apps/web/src/server/slos.ts (new), apps/web/src/server/slos.test.ts (new, hermetic recorder), apps/web/src/server/slos.integration.test.ts (new, compose Postgres), apps/web/src/lib/alert-types.ts (`AlertEventRow` gains `sloId`/`sloName`), apps/web/src/server/alerts.ts (`LIST_EVENTS_SQL`/`GET_EVENT_SQL` gain `LEFT JOIN slos s`; `deleteNotificationChannel` counts SLOs too — the sentence names both), apps/web/src/server/alerts.test.ts (the engine + the delete-refusal cases follow)
- goal: packet §0 + D505–D509 + D513 + D517 VERBATIM: `SloWindow`/`SLO_WINDOWS`, `SloStatus`, `SloIndicator` (strict `validateSloIndicator`: exact key set per kind, `service` string|null non-empty when set, `thresholdMs` finite integer > 0 on latency only), `SloRow`, `formatSloObjective` (the ONE renderer), `sloBudget()` — the TS mirror of D509 for the editor's preview (pinned against the Go table by shared cases in the fixture file's `arithmetic` section, so the two never disagree); `server/slos.ts`: `listSlos` (oldest first, name tie-break, channel name joined), `createSlo`/`updateSlo`/`setSloEnabled`/`deleteSlo` (lock-first, D430 shape refusals before the lock, `MAX_SLOS_PER_WORKSPACE = 50` counted under the lock, channel EXISTS check when set — `NO_SUCH_CHANNEL` for another workspace's id, `NO_SUCH_SLO` = "no SLO with this id in your workspace", refuse-never-upsert on the name, `updateSlo` never names enabled/status/measurements/next_eval_at; a changed indicator/window/target RESETS the measurement columns to the no-data state and `next_eval_at = now()` so a stale number never renders under a new objective — RECORDED as an in-flight ruling if the packet is read otherwise); target stored with 3 decimals, read back as a number.
- done-check: `slo-types.test.ts` — every fixture verdict matches the file (the Go half lands in T3 and reads the same file), the formatter's two sentences exact, the arithmetic mirror's cases exact; `slos.test.ts` — workspace binding on every statement, lock-first on every mutation, zero-statement shape refusals (bad kind, missing thresholdMs on latency, thresholdMs on availability, target 0/100/NaN, window "1d", blank name), the cap, D440 sentences, refuse-never-upsert, app-generated `slo_` ids, `updateSlo`'s SET list pinned; `slos.integration.test.ts` — two workspaces disjoint on the list, cross-workspace id refusals, the channel RESTRICT refused inside Postgres for an SLO reference; `alerts.test.ts` still green with the joined feed; RED by sabotage (the workspace predicate defanged) on record; full web suite green with the DSN; tsc + eslint clean.
- result: committed (see log), 9 files. `lib/slo-types.ts` (ZERO imports; vocabularies as runtime lists + `SLO_WINDOW_DAYS`; strict `validateSloIndicator` — exact key set per kind, absent≠null on `service`, `thresholdMs` a finite positive whole ≤ 2^31−1 on latency only; `sloBudget` = the D509 integer-count arithmetic byte-for-byte with Go; `formatSloObjective` the ONE renderer; `SloRow`). `server/slos.ts` (`SloRefusal`; `listSlos` with the channel name joined; create/update/setEnabled/delete lock-first; shape refusals before the lock incl. target strictly in (0,100) with ≤3 decimals; `MAX_SLOS_PER_WORKSPACE = 50` under the lock; channel EXISTS check only when named, `NO_SUCH_CHANNEL` imported from `alerts.ts` so the two modules refuse in the same words; NUMERIC/BIGINT read back as numbers at the one mapping site). `alerts.ts`: the events feed joins `slos` (`sloId`/`sloName` on `AlertEventRow`), `deleteNotificationChannel` counts SLOs beside rules and names both ("used by 2 alert rules and 3 SLOs — delete or repoint them first"; the 23503 backstop says "an alert rule or an SLO"); the S7.1 sentence's "1 alert rule … repoint them" became "it" (a grammar slip, the integration pin updated). RULING TAKEN IN-FLIGHT — **D518** (recorded in the Escalations log): an objective change (indicator, target or window) RESETS the measurement columns to no-data and sets `next_eval_at = now()` (a number measured against a different objective must not render under the new one, even for a cadence); a rename or channel change keeps the measurement. Gates: `slo-types.test.ts` 9/9 (parity 29/29 against the Go fixture, arithmetic 14/14, the Go vocabularies and `AtRiskBudgetPct` read from source), `slos.test.ts` 14/14, `slos.integration.test.ts` 5/5 against compose Postgres (disjoint lists, D440 cross-workspace refusals, the SLO→channel RESTRICT refused by the module AND by Postgres raw, measurements read back as numbers + the D518 reset lands + `next_eval_at` due, the feed names an SLO's event only in its own workspace, the 0012 one-producer CHECK refuses a row claiming both, events CASCADE with their SLO); alerts unit + integration + alert-types still green — 68/68 across the six files. RED by sabotage (the list predicate defanged to `$1 IS NOT NULL`): 6 red — both unit binding tests and all four integration proofs — restored 19/19. Full web suite 1000/1000 with the DSN (970 → 1000); tsc + eslint clean.

### T3: Go — the indicator parser, the arithmetic, the event text (pure)
- status: done (Fable-direct; see result)
- wave: 2 · depends: T1 (the fixture file lands in T2 — T3 runs after T2 in this Fable-direct sequence) · model: Opus (the arithmetic is the sprint's honesty core)
- owns: services/ingest/internal/alerting/indicator.go (new — `ParseIndicator`, `Indicator` struct, `ErrInvalidIndicator`, vocabularies as `var` slices the TS test can read), services/ingest/internal/alerting/indicator_test.go (new — corpus shape + parity + populated-fields + never-panics, the condition_test.go shape), services/ingest/internal/alerting/slo_math.go (new — `sloBudget(good, total, target) (current, burned float64, status string)` + `AtRiskBudgetPct = 75` + the strict-boundary rule on counts), services/ingest/internal/alerting/slo_math_test.go (new — the packet §5.2 table + the fixture file's `arithmetic` cases), services/ingest/internal/alerting/slo_text.go (new — `sloTransitionText(name, ind, target, window, measurement, from, to)`), the corresponding cases in evaluator_test.go
- goal: packet D505–D509, D512 (text), D517 VERBATIM. Statuses: `breached` iff `bad*100 > total*(100-target)` on counts (float arithmetic, strict), else `at-risk` iff burned ≥ 75, else `healthy`; `total = 0` ⇒ `no-data`, current/burned unset. Text: title `<name>: breached — 98.40% against a 99% target` / `<name>: at risk — …` / `<name>: healthy again`; detail `<objective sentence>: 98.40% over the last 30d (1,234 of 1,254 traces good); 160% of the error budget consumed.` (`formatNumber` for the percentages, thousands separators on the counts).
- done-check: `go test ./internal/alerting/ -run 'Indicator|SloBudget|SloText' -count=1` green; the corpus ≥ 20 cases, ≥ 8 each verdict; the TS half (T2) still green against the same file — one artifact, two readers; `go vet` clean.
- result: committed (see log), 7 files: `testdata/indicator-fixtures.json` (29 indicator cases — 9 valid / 20 invalid — and 14 D509 arithmetic cases, `{indicators, arithmetic}` shape), `indicator.go` (`ParseIndicator` strict: DisallowUnknownFields + trailing-content refusal, kind from `sloIndicatorKinds`, `service` present-string-or-null non-blank, `thresholdMs` required on latency / forbidden on availability / finite positive whole ≤ MaxInt32; `windowDays`; the three vocabularies as `var … = []string{…}` for the TS reader), `slo_math.go` (`sloMeasure` on COUNTS in integer arithmetic: `bad×100000 > total×(100000 − target×1000)` decides breached, burned = that ratio ×100 unbounded, `AtRiskBudgetPct = 75` ≥, no-data on total 0, clamps on impossible counts), `slo_text.go` (`sloObjective`, `sloTransitionText`, `groupThousands`), three test files. PLAN CORRECTION (A-rule, recorded): packet §5.2's example table said 1 bad of 1000 at 99.9 is "healthy" — by D509's own rule 100% of the budget consumed is ≥ 75 and therefore AT-RISK; the ruling governs and the fixture pins `at-risk` (the ruling text is unchanged; the example was the slip). Also recorded: the breach test is integer, not float — 997 of 1000 at a 99.7% target sits EXACTLY on its budget and a float ratio puts it one ulp over (the "float-trap" fixture cases, three decimal depths). Gates: 53 subtests PASS / 0 FAIL first run (parity 29/29, arithmetic 14/14, boundary 6/6, text, never-panics); `gofmt`/`go vet` clean.

### T4: Go — the SLO claim on the S7.1 ticker, transitions as events, the deliverer's `slo`
- status: done (Fable-direct; see result)
- wave: 3 · depends: T1, T3 · model: Opus (a second claim loop inside the proven evaluator; the deliverer join; the additive payload)
- owns: services/ingest/internal/alerting/slo_evaluator.go (new — `EvaluateDueSLOs`, claim SQL, the write-back SQL, the transition + event insert, counters `obstack_ingest_slo_evals_{failed,nodata}_total`, `SLOEvalInterval = 5m`, `sloEvalTimeout = 30s`, `sloClaimBatch`), services/ingest/internal/alerting/eval_slo.go (new — the trace_summaries read: good/total per D505/D506, the S7.1 trace SQL shape widened to day windows), services/ingest/internal/alerting/evaluator.go (`evaluateOnce` runs `EvaluateDue` then `EvaluateDueSLOs`; `eval` seam gains a sibling `evalSLO` func field for the stub), services/ingest/internal/alerting/deliverer.go (`claimPendingSQL` gains `LEFT JOIN slos s`; `pendingEvent` gains the slo fields; `payloadFor` sets `Slo`), services/ingest/internal/notify/payload.go (`Payload.Slo *SloPayload` with `json:"slo"` — additive, version stays 1; `SloPayload{Name, Objective, Status}`), services/ingest/internal/notify/payload_test.go (goldens gain `"slo":null` — the ONE deliberate wire change, additive), services/ingest/internal/alerting/integration_test.go (the SLO proofs), services/ingest/cmd/ingest/main.go (NO change expected — the tick already runs; recorded if one is needed)
- goal: packet D508, D510–D512 VERBATIM: claim `FROM slos WHERE enabled AND next_eval_at <= now() ORDER BY next_eval_at LIMIT $1 FOR UPDATE SKIP LOCKED` in its own transaction; per-SLO: parse (fail-open on error), read (30s timeout, fail-open, `failed` counter), `total = 0` ⇒ no-data (counter; status → `no-data` with NULL numbers; no event either way), else compute (T3), write current/burned/good/total/evaluated_at/status, `last_transition_at = now()` on a status change, and INSERT an `alert_events` row (`slo_id` set, `rule_id` NULL, `channel_id` = the SLO's channel at emit, severity by transition: →breached critical, →at-risk warning, →healthy info; `link` NULL) ONLY when the SLO has a channel and the transition is between healthy/at-risk/breached; reschedule `+5m` always. Deliverer: the joined `slo` name/objective/status ride into `payloadFor`; `rule` stays null on SLO events.
- done-check: RED-first against a stub (`evalSLO` returning fixed counts): the transition sequence healthy → at-risk → breached → healthy emits exactly three events at warning/critical/info with `slo_id` set and `rule_id` NULL; two ticks in one status emit nothing; a channel-less SLO transitions (status + `last_transition_at` move) with ZERO rows in `alert_events`; no-data → healthy is silent; the fail-open path leaves status and numbers unchanged, advances `next_eval_at`, counts; two concurrent claimers never double-evaluate one SLO (the D478 proof re-run). Real half: seeded traces (the S7.1 `seedTrace` helper) → availability and latency good/total exactly as independently counted, the service scope excludes the other service, the other workspace excluded; the deliverer sends an SLO event with `"slo":{…}` and `"rule":null` and marks it delivered. `go test ./internal/alerting/ ./internal/notify/ -count=1` green against compose; full module green; `go vet` clean.
- result: committed (see log), 9 files. `slo_evaluator.go` (`EvaluateDueSLOs`: claim `FOR UPDATE SKIP LOCKED` on `slos`, parse → window → read each fail-open with `obstack_ingest_slo_evals_failed_total`, no-data → status `no-data` + NULL numbers + `obstack_ingest_slo_evals_nodata_total`, else the measurement written with `last_transition_at` moved by a SQL CASE on the OLD status, an `alert_events` row only when the SLO has a channel and `sloTransitionSeverity` says emit; `SLOEvalInterval = 5m`, `sloEvalTimeout = 30s`, batch 50), `eval_slo.go` (the trace leg's shape widened to days; availability `countIf(error_spans = 0)`, latency `countIf(duration_ms <= ?)`; args bound in TEXT order — the outer threshold first — stated in the file), `evaluator.go` (`evalSLO` seam; `evaluateOnce` runs rules then SLOs — NO `main.go` change, as predicted), `deliverer.go` (third LEFT JOIN to `slos`; `payloadFor` sets `Slo`), `notify/payload.go` (`Payload.Slo *SloPayload{Name, Objective, Status}` after `rule`, version stays 1) + `payload_test.go` (the two goldens gain `"slo":null` — the one deliberate wire change, additive — and an SLO golden), `slo_integration_test.go` (8 tests). D518(ii) implemented: `no-data → at-risk/breached` emits, `no-data → healthy`, `→ no-data` and same-status are silent (the 13-row severity table pinned). Gates: the transition sequence no-data→healthy(silent)→at-risk(warning)→breached(critical)→breached(silent, numbers move, stamp does not)→healthy(info)→no-data(silent, NULLs)→breached(critical) = exactly 4 events with `slo_id` set and `rule_id` NULL; a channel-less SLO transitions with zero rows; three fail-open cases (CH unreachable, bad indicator, a window past the CHECK) leave status/numbers, advance the clock, count 1 each; no-data counts on its own counter; two concurrent claimers → B reads 0 times, 1 event; REAL read against seeded ClickHouse: checkout 3/4 and all 3/5 (availability), 4/4, 4/5, `<=` at the boundary 4/4, under-threshold 0/4 (latency), other workspace 0/1 on its own, at both 7d and 30d; a real end-to-end tick over 3/4 at 99.9% → breached 75% with the event title from the measured numbers; empty workspace → no-data, no event; the deliverer sends an SLO event with `"rule":null` and `slo.{name,objective,status}` and marks it delivered. One first-run red: the severity table returned `("info", false)` for no-data→healthy — corrected to `("", false)`. Alerting package 159 subtests PASS; notify green with the moved goldens; full module 15/15 packages green; `gofmt`/`go vet` clean.

### T5: the flip — `/app/slos` live (mock, live, editor, actions, page, guards, registry)
- status: done (Fable-direct; see result)
- wave: 3 · depends: T2 · model: Opus (the D431 shape with a client editor; three unwired-control tests move)
- owns: apps/web/src/components/slos/SlosMock.tsx (new — master's page body VERBATIM, sha-pinned modulo the export line), apps/web/src/components/slos/SlosLive.tsx (new, server), apps/web/src/components/slos/SlosEditor.tsx (new, client), apps/web/src/components/slos/actions.ts (new, mutations only), apps/web/src/app/app/slos/page.tsx (the D431 shape; reads `listSlos` + `getUsage` for `retentionDays`), apps/web/src/app/app/slos/page.test.ts (new), apps/web/src/lib/live-routes.ts + live-routes.test.ts (`/app/slos` exact; the unwired control → `/app/incidents`), apps/web/src/components/dash/OnboardingChecklist.test.tsx (the second control → `/app/incidents`), apps/web/src/app/status/status.test.ts (D324 guard reads page + SlosMock + SlosLive), apps/web/src/components/alerts/AlertsLive.tsx (renders `slo: <name>` beside `rule:`), apps/web/src/server/alerts.test.ts (the D441 action-surface test for slos: a sibling assertion in slos.test.ts)
- goal: packet D514 VERBATIM: `if (dataMode !== "live") return <SlosMock />; await connection(); …`; `SlosLive` renders the four-way status style (`NO DATA` faint), the window label + the D507 clip note (`30d · 7d retained on Free`) when `retentionDays < windowDays`, the objective from the ONE formatter, `current%` or `—`, `target N%`, the budget bar `min(100, burned)` with the ≥75 colour rule, `evaluated YYYY-MM-DD HH:MM UTC` / `not yet evaluated`, `<status> since …` when `lastTransitionAt` set, the channel name or `no channel — computed only`, the `inspect` link built with `tracesSearchString` (`status=error` + `service` for availability; `minMs=<thresholdMs>` + `service` for latency), `data-tour="slos"` on the grid; `SlosEditor`: new/edit form (name, kind, service or all, threshold ms on latency, target, window, channel or none), enable toggle, delete — the `AlertsEditor` shape, the store's sentence verbatim; `actions.ts` exposes exactly `createSlo, updateSlo, setSloEnabled, deleteSlo`; empty state "no SLOs yet — an objective is a target over your own traces". Absent and GUARDED: `note`, `TerraformExport`, any `@/mock/` import in the live graph, a `/status` href or "status page" text (the D324 sweep over the three files).
- done-check: RED first — `slos/page.test.ts` (ENOENT on SlosMock), `live-routes.test.ts` (the exact-route test), `OnboardingChecklist.test.tsx` (control), `status.test.ts` (extended) all red before the flip; `SlosMock` GENERATED from `git show master:apps/web/src/app/app/slos/page.tsx` with only the export line rewritten and its sha256 pinned; guards green; full web suite green; tsc + eslint clean. Deferred to integration: the two-build DOM md5 of `/app/slos` in mock mode.
- result: committed (see log), 12 files. RED on record before the flip: `slos/page.test.ts` (ENOENT on SlosLive), `live-routes.test.ts` (the exact-route test), `status.test.ts` (the extended D324 sweep), `slos.test.ts` (the D441 action-surface guard) — 4 red, 31 green; the onboarding-checklist control moved (`/app/slos` → `/app/incidents`) without going red, as a control move should. `SlosMock` GENERATED from `git show master:…/slos/page.tsx` with only the export line rewritten — sha256 `209e8388…` pinned, round-trips. `page.tsx` in the D431 shape (mock branch before any await, zero props; `Promise.all` of `listSlos`, `listNotificationChannels`, `getUsage`). `SlosLive` (server): the four-way status style (`NO DATA` faint), the window label + the D507 clip note (`30d · 7d retained on Free`), the objective from the ONE formatter, `pct()` (two decimals, trimmed, or `—`), `target N%`, the good/total counts, the budget bar clamped at 100 with the ≥75 colour rule and the UNclamped number, `evaluated … UTC` / `not yet evaluated`, `<status> since …`, `→ channel` / `no channel — computed only`, DISABLED at 60% opacity, the `inspect` link BUILT through `parseTracesUrl({})` + `tracesSearchString` + `tracesHref` (status=error / minMs=threshold, plus service), `data-tour="slos"` on the grid and on the empty state. `SlosEditor` (client): one form for create/edit (name, indicator kind, service or all, good-under ms on latency, target, window, notify channel or none), a `sloBudget` PREVIEW line under the target (decides nothing the card shows), the "changing the objective resets its measurement" note on edit, edit/delete/toggle controls. `actions.ts` exactly four mutations. `liveWiredRoutes` gains `/app/slos` exact; `AlertsLive` renders `slo: <name>` beside `rule:`. Absent and guarded: `31%`, `afternoon`, `kb-reindex`, TerraformExport, any `@/mock/` import in the live graph, a `/status` href or "status page" text in all three files. Gates: guards 57/57 (page 5, registry, checklist, status, slos store incl. D441, alerts page); tsc + eslint clean; full web suite 1007/1007 with the DSN. Deferred to integration: the two-build DOM md5 of `/app/slos` in mock mode.

### T6: the tour step (D515) + the absences page (D516)
- status: done (Fable-direct; see result)
- wave: 3 · depends: T5 · model: Sonnet (copy + two needle tests, in a stated shape)
- owns: apps/web/src/components/shell/TourGuide.tsx (the `/app/slos` step's copy), apps/web/src/components/shell/TourGuide.test.ts (the D404-shape test for `/app/slos`), apps/web/src/content/docs/what-obstack-does-not-do/index.mdx (the M6 paragraph)
- goal: packet D515/D516 VERBATIM: the step's copy is capability-only, true in both modes; the test bans `31%`, `chat latency`, `afternoon` from the step and asserts `data-tour="slos"` in BOTH `slos/SlosLive.tsx` and `slos/SlosMock.tsx`; the absences paragraph as written in D516 (two surfaces render sample data; SLOs computed over the workspace's own traces; the fence sentence). No docs page, no manifest change, no corpus pin moves.
- done-check: TourGuide suite green with the new test proven red first (the old copy fails it); docs 12/12, doc-links, mirror, landing-fence, corpus-honesty, Quickstart suites green (no pin moves expected — recorded if one does); the absences page read by a human once: no sentence about SLOs is false after T5.
- result: committed (see log), 3 files. The D404-shape test for `/app/slos` written FIRST and red against the old copy (`31%`, `chat latency`, `afternoon`, `monthly budget` banned; `error budget` and `your own traces` required; `data-tour="slos"` in both SlosLive and SlosMock) — 1 red / 6 green — then the copy rewritten to capability only, true in both modes: 7/7. The absences page's M6 paragraph now says two surfaces render sample data (on-call, incidents), the status page is still deliberately not fed by SLOs, SLOs are computed over the workspace's own traces with the D516 fence sentence (no custom SLI query, no route scope, no burn-rate alerting), and keeps the S7.1/S7.2/S6.2 sentences. No docs page, no manifest entry: NO corpus pin moved (docs, doc-links, mirror, landing-fence, corpus-honesty, Quickstart all green unchanged). Full web suite 1008/1008 with the DSN; eslint clean.

### T7: drive/e2e slos leg
- status: done (Fable-direct; see result)
- wave: 4 · depends: T4, T5, T6 · model: Opus (a new arm with an independent recomputation and a RED hook)
- owns: deploy/compose/exit-seed.mjs (`--leg slos`: three SLO rows the `seedAlerts` way, `RED_WITHHOLD_SLOS`, the exported names), deploy/compose/e2e-drive.mjs (the slos arm after the changes arm; reuses the alerts leg's receiver pattern), deploy/compose/README.md (drive section: the slos bullet)
- goal: packet §5.3 VERBATIM: alice — availability SLO target 99.99 / 7d on the drive's live webhook channel (seeded by this leg the alerts way; breached by construction: the seed carries error spans), latency SLO target 50 / 600000 ms / 7d with NO channel (healthy); bob — availability SLO on his empty workspace (`NO DATA`). Settle on OBSERVED state (≤60s tick + ≤5s deliverer, 120s deadline). Checks: the three status words on the right cards; alice's availability `current%` equals the drive's OWN `trace_summaries` recomputation (good = merged `error_count = 0` traces; total = merged traces; workspace-scoped) to two decimals; budget > 100 and the bar width capped at 100; the objective sentence exact; no SAMPLE badge, no `31%`, no `afternoon`; `/app/alerts` carries the breach event `delivered` with `slo: <name>`; the receiver's payload has `slo.name` and `rule: null`; bob's page lists only his no-data card with none of alice's names; the `inspect` link parses (`status=error`). RED hook withholds the SLO rows: the page/feed/receiver checks fail, never the seeder.
- done-check: drive GREEN (all prior checks + the new ones) and RED (`RED_WITHHOLD_SLOS=1`) exactly the slos-arm checks red; the README bullet describes what runs; token hygiene sweep unchanged (no new secret).
- result: committed (see log), 3 files (`exit-seed.mjs` +90, `e2e-drive.mjs` +190, README drive section). `--leg slos`: with `--target`, a `slo webhook` channel + the availability objective (99.99% / 7d, on the channel) + the latency objective (50% under 600,000 ms / 30d, NO channel); without, one availability objective scoped to `exit-nothing`. The arm: its own receiver, both seeds, the drive's OWN recomputation over `trace_summaries` (merged per trace, `sum(error_count) = 0`, the 7d HAVING) and D509's integer arithmetic restated as a third reader, a settle loop whose condition IS the assertion, eight checks. GREEN **261/0 in 210s** (253 + 8): the card's `98.23%` and `612 of 623 traces good` equal the recomputation; the budget `17656.5%` stated with the bar at `width:100%`; the breach in `/app/alerts` by the evaluator's title, `delivered`, `slo:` not `rule:`; the receiver's POST `version 1`, `rule null`, `slo.{name, objective, status: breached}`; the latency card `no channel — computed only` with `30d · 7d retained on Free` and no event; the inspect links `?status=error` / `?minMs=600000`; bob's `exit-nothing` card `NO DATA` with a dash and none of her words. RED (`RED_WITHHOLD_SLOS=1`) **253/8 in 299s** — EXACTLY the eight slos-arm checks, the seeder answering `slos: []` with the channel landed. Three drive defects found and fixed on the way, all in the arm, none in the product: (1) a constant not imported aborted the arm after the receiver check (green1: 216/2); (2) the feed check's fixed-length slice ran into the alerts leg's next card and read its `rule:` label (green1); (3) bob's workspace is SEEDED with the same dataset as alice's, so an all-services objective measured instead of reading no-data — his objective is now scoped to a service his traces never name, the sharper D508 proof (green2: 260/1); (4) the S6.4 settle-race class, met once (green3: 260/1): the receiver holds the POST a beat before the deliverer's transaction commits the `delivered` mark — the loop now settles on the feed card SAYING delivered, the exact state the check asserts. One background run was killed by the harness at the build step (the S7.2 retro's pattern) — the foreground run passed. Token hygiene sweep unchanged. Local-only: the drive lowers the compose `plans` catalog each run; restored by hand after each (the pre-registered teardown fix is still open).

## Escalations log
- **D518 (in-flight, T2 → binds T4; amends D508/D511 at their seam)** — (i) an objective edit (indicator, target, window) resets the row to no-data with NULL numbers and `next_eval_at = now()`; a rename or channel change does not. (ii) Because (i) routes every re-defined objective through no-data, the evaluator treats a measurement that lands in `at-risk` or `breached` FROM `no-data` as a notifiable transition (warning/critical) — a first measurement that is already bad is news, and a reset must never swallow a breach; `no-data → healthy` and `any → no-data` stay silent (nothing measured, or nothing to recover from). D508's "no event entering or leaving no-data" is read as "entering, and leaving into healthy".

## Integration
- verdict: GO (2026-09-02, Fable direct)
- rounds: 1
- gates: drive GREEN **261/0** (210s, foreground) with the slos arm (253 + 8); RED (`RED_WITHHOLD_SLOS=1`) **253/8** — EXACTLY the eight slos-arm checks, the seeder green with `slos: []`; mock byte-identity of the flipped route PROVEN two-build (master worktree @ `56024ac` vs HEAD, APFS-cloned `node_modules`, mock builds served side by side, script-stripped + asset-normalized DOM md5: `/app/slos` f42fd836… identical, 33,800 bytes; `/app/alerts` as the control 4638a9de… identical); full web suite **1008/1008** with the DSN set and the fresh HEAD mock build (the doc-links prerender arms included); Go module **15/15** packages (alerting 159 subtests incl. the real ClickHouse reads); `npm run lint` + `tsc --noEmit` clean; the compose ingest rebuilt with the evaluator and the runner recording `0012_slos` on boot as a no-op over the raw-applied objects; PR #35 all ten checks green at `20e2980` on the FIRST CI attempt — the exit gate (e2e 8m6s, stack 10m38s, web 5m15s, images 4m57s, sdk-e2e 4m38s, go 2m39s, kind 1m41s, sdk-js 1m5s, lint 49s, sdk-py 33s).
- seam findings: four, none structural. (1) `window` is a reserved word in Postgres — the column is `eval_window`, recorded at T1; the vocabulary string is unchanged across the three readers. (2) The packet §5.2 example table contradicted D509 on the 1-bad-of-1000 case (100% consumed is at-risk, not healthy) — the ruling governs, the fixture pins it, recorded at T3. (3) D518, taken in flight at T2/T4: objective edits reset the measurement to no-data, and a first measurement that is already at-risk/breached notifies — the two halves are one ruling (a reset must never swallow a breach); recorded in the Escalations log. (4) The drive arm's own four defects (an import, a slice boundary, a fixture assumption about bob's traces, and a settle race of the S6.4 class) — all in the arm, none in the product, all recorded at T7; the drive's compose `plans` drift (event AND explain quota) still needs restoring by hand after every local run — the pre-registered teardown fix stays open, now with the second column named.
- fix tasks: none open.

## Goal check (Step 7, one entry per iteration)
- iteration 0 (2026-09-02, integration GO): **GOAL MET** — measured against the run-goal clause by clause. (1) SLO definitions CRUD per-workspace: 0012 (T1), `server/slos.ts` lock-first with the D440 sentences and the cap (T2), the editor + four actions (T5), proven disjoint across two workspaces at the store (integration) and at the surface (the drive's bob). (2) Burn computed by S7.1's scheduler over summaries: a second claim on the same 60s ticker, the `trace_summaries` read in the trace leg's shape, the D509 arithmetic on counts (T3/T4), the drive's card equal to an independent recomputation (612 of 623 = 98.23%). (3) Breach/at-risk raises events through the S7.1 pipeline: `alert_events.slo_id`, the transition machine with D511/D518 severities, the deliverer's additive `slo` payload — the drive's receiver holds `rule: null` + `slo.{name,objective,status}` and the feed says `delivered` (T4/T7). (4) `/app/slos` flipped: exact-not-subtree, RED-first, `SlosMock` sha-pinned, the evaluator's numbers rendered with the D507 clip note and the honest no-data state (T5), the tour and the absences page true in both modes (T6). (5) Mock byte-identical: DOM md5 identical two-build. Honest absences per packet §0 (the analyst note, the evals/pipelines objectives, TerraformExport, the status-page link) absent, not staged — guarded by tests.

## Retro (filled at end of run)

**Shipped**: all 7 tasks in one session, Fable-direct throughout (the S7.2 directive carried forward on "go ahead"); PR #35 ten checks green on the FIRST CI attempt (e2e 8m6s, stack 10m38s — the exit gate); merged `cce0525`. Drive 253/0 → 261/0 (the slos arm, 210s); web suite 970/0 → 1008/0; Go module 15/15 with four new files in `alerting` (159 subtests). The third M6 surface (`/app/slos`) leaves mock; `/app/ask` remains the drive's SAMPLE control and `/app/incidents` takes the registry's. Production promotion: a user-confirmed critical item (Railway README §4), recorded below when it happens.

**What earned its keep**:
- Sequencing the Go parser and the shared fixture BEFORE the TS contract (the T2/T3 swap, recorded at T1 close): every commit green on its own, and the one artifact two languages read caught the packet's own worked example (1 bad of 1000 at 99.9% is at-risk, not healthy) before a line of store code existed. Lesson: a worked example in a packet is a claim to be computed, not typed.
- Deciding the breach on COUNTS in integer arithmetic: 997 of 1000 at a 99.7% target sits exactly on its budget, and a float ratio puts it one ulp over. The three "float-trap" fixture cases are the proof that a customer's objective never breaches on a rounding error — on either side.
- D518, found by writing the reset first and asking what the evaluator does on the next tick: an objective edit that reset to no-data would have swallowed a breach under D508's letter; the ruling's two halves (reset, and no-data → bad notifies) are one thought, and the 13-row severity table pins it.
- The drive's second reader: the card's `98.23%` and `612 of 623` are checked against the drive's OWN merge over `trace_summaries` and D509 restated a third time — never against a number the app produced (D71(b)), and the same read makes bob's `exit-nothing` objective a sharper no-data proof than an empty workspace would have been.
- Byte identity two-build with `/app/alerts` as a control: the identity script and normalizer were recovered from the previous session's scratchpad and generalized in two `sed`s; a control route makes an IDENTICAL verdict mean something.
- Calibration note for the routing policy: the tiers would have sent T1/T6 to Sonnet and the rest to Opus; the four corrections that mattered (the reserved `window`, the packet's example slip, D518, bob's seeded workspace) were all found by reading the tree or running the thing — none by reading the plan.

**What went wrong**:
- The drive arm cost three full runs (≈15 min) on four defects of its own, none in the product: an unimported constant (`node --check` is syntax-only — the drive is not under eslint's `no-undef`, and CI's lint job passed with the reference unresolved; pre-registered: bring `deploy/compose/*.mjs` under the lint job), a fixed-length HTML slice that read the next card's label, a fixture assumption (bob's workspace is SEEDED, not empty — the packet's §5.3 said "no traces"), and the S6.4 settle race met again (the receiver holds the POST a beat before the `delivered` mark commits). Lesson, now a rule for the next arm: the settle condition IS the assertion, and an arm's first green run is not evidence its fixture reads the world right.
- One background drive run was killed by the harness at the build step — the S7.2 retro's pattern, third occurrence. Rule: the drive runs in the foreground, full stop.
- The compose `plans` drift has TWO columns (event_quota AND explain_quota); restoring only the first left three explain-quota tests red on the final web run until the second was put back. The pre-registered teardown fix (the drive restores the rows it changed) is now a T0-class item for S7.4, with both columns named.
- A first-run red in T4: the severity table returned `("info", false)` for no-data → healthy where the table wanted `("", false)` — a return-value convention, caught by the table, fixed in one line.
