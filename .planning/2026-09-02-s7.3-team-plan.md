# Team Plan — obstack M6 sprint 3 (S7.3): SLOs for real — definitions CRUD in Postgres, burn computed by a second claim on the S7.1 ticker over trace_summaries, breach/at-risk transitions delivered through the S7.1 pipeline, `/app/slos` live-wired; mock mode byte-identical

meta:
- date: 2026-09-02
- repo/branch: `s7-3` (cut from master @ `56024ac`, the RULED packet commit)
- mode: **Fable-direct, session-wide (user directive, S7.2 precedent carried forward: "go ahead")** — no subagent spawns; every task T1–T7 is executed by Fable in the main session, sequentially in wave order.
- iteration-limit: 3
- iteration: 0/3
- governing docs (binding, read before executing): `.planning/2026-09-01-s7-m6-ops-suite-phase-plan.md` §5 S7.3 + §7; `.planning/2026-09-02-s7.3-slos-packet.md` (RULED — implemented VERBATIM, D505–D517; deviations escalate, never smoothed).

## Kickoff notes

- Packet ruled as drafted (2026-09-02); (b) held loosely by Fable, ratified by the user — route scoping stays pre-registered. Rulings numbered from D518 if the breakdown surfaces seams.
- Model routing: VOID for this run — Fable-direct throughout. The per-task `model:` lines record what the routing WOULD have been, for the retro's calibration only.
- The A-rule binds Fable-as-executor: a plan defect found mid-task is corrected in the plan and recorded, never smoothed.
- Lessons carried in from the S7.2 retro, as plan items: the absences page and the tour step are DONE-CHECK items of the flip (T6 is its own task so neither can be "later"); one command, one chain, braces around the tail for every guarded commit; the drive's compose `plans` drift is a known local-only red on four usage tests — restore the seed before the full web suite, and never read it as an S7.3 failure.

## Critical items (return to Fable-as-advisor before merge)
- T1 — the DDL migration (0012, the track's first ALTER): raw double-apply + runner idempotency evidence attached.
- T3/T4 — the arithmetic (D509) and the state machine (D511): the table test and the transition-sequence integration proof, plus the two-claimer proof re-run for `slos`.
- T5 — the flip: the sha pin, the D440 tenancy proof on the store, the unwired controls moved (registry test AND the onboarding checklist test), the D324 status-link guard extended to both new components.
- T7 — the exit evidence: the drive's slos arm with its proven-red run; the independent ClickHouse recomputation matching the card.

## Tasks

### T1: Postgres DDL 0012_slos + migration pin
- status: pending
- wave: 1 · depends: — · model: Sonnet (DDL in a stated house style with one new idiom — the guarded ALTER)
- owns: services/ingest/pgmigrations/0012_slos.sql (new), services/ingest/internal/pgmigrate/qa_a6_migrations_test.go (the applied-set pin, `want` string at :178)
- goal: packet D513 VERBATIM: `slos` (id `slo_`, workspace FK CASCADE, name UNIQUE per ws `<> ''`, indicator JSONB object CHECK, target NUMERIC(6,3) CHECK (0,100) exclusive, "window" CHECK IN ('7d','30d'), channel_id NULL FK notification_channels ON DELETE RESTRICT, enabled, status CHECK IN four DEFAULT 'no-data', current_pct/budget_burned_pct DOUBLE PRECISION NULL, good_count/total_count BIGINT NULL with the both-or-neither CHECK, evaluated_at/last_transition_at NULL, next_eval_at DEFAULT now(), created_at/updated_at); index `(next_eval_at) WHERE enabled`; `ALTER TABLE alert_events ADD COLUMN IF NOT EXISTS slo_id TEXT NULL REFERENCES slos (id) ON DELETE CASCADE`; partial index `alert_events (slo_id) WHERE slo_id IS NOT NULL`; the exclusivity CHECK `(rule_id IS NULL OR slo_id IS NULL)` added under a `pg_constraint` existence guard (DO block). Pin gains `,0012_slos`.
- done-check: psql double-apply against compose (pass 1 creates every object, pass 2 a no-op — every statement guarded); `go test ./internal/pgmigrate/ -count=1` green (fresh schema + the re-apply no-op + the pin); `go build ./... && go vet ./...` clean. Evidence lines in the result.

### T2: web contract + store — `lib/slo-types.ts`, `server/slos.ts`, the events feed learns `slo`
- status: pending
- wave: 2 · depends: T1 · model: Opus (the D517 validator is half a cross-language contract; the store is lock-first with the D440 sentences)
- owns: apps/web/src/lib/slo-types.ts (new, ZERO runtime imports), apps/web/src/lib/slo-types.test.ts (new — the TS half of D517 + the formatter + the arithmetic mirror), services/ingest/internal/alerting/testdata/indicator-fixtures.json (new — the ONE two-consumer artifact; T3 adds the Go reader), apps/web/src/server/slos.ts (new), apps/web/src/server/slos.test.ts (new, hermetic recorder), apps/web/src/server/slos.integration.test.ts (new, compose Postgres), apps/web/src/lib/alert-types.ts (`AlertEventRow` gains `sloId`/`sloName`), apps/web/src/server/alerts.ts (`LIST_EVENTS_SQL`/`GET_EVENT_SQL` gain `LEFT JOIN slos s`; `deleteNotificationChannel` counts SLOs too — the sentence names both), apps/web/src/server/alerts.test.ts (the engine + the delete-refusal cases follow)
- goal: packet §0 + D505–D509 + D513 + D517 VERBATIM: `SloWindow`/`SLO_WINDOWS`, `SloStatus`, `SloIndicator` (strict `validateSloIndicator`: exact key set per kind, `service` string|null non-empty when set, `thresholdMs` finite integer > 0 on latency only), `SloRow`, `formatSloObjective` (the ONE renderer), `sloBudget()` — the TS mirror of D509 for the editor's preview (pinned against the Go table by shared cases in the fixture file's `arithmetic` section, so the two never disagree); `server/slos.ts`: `listSlos` (oldest first, name tie-break, channel name joined), `createSlo`/`updateSlo`/`setSloEnabled`/`deleteSlo` (lock-first, D430 shape refusals before the lock, `MAX_SLOS_PER_WORKSPACE = 50` counted under the lock, channel EXISTS check when set — `NO_SUCH_CHANNEL` for another workspace's id, `NO_SUCH_SLO` = "no SLO with this id in your workspace", refuse-never-upsert on the name, `updateSlo` never names enabled/status/measurements/next_eval_at; a changed indicator/window/target RESETS the measurement columns to the no-data state and `next_eval_at = now()` so a stale number never renders under a new objective — RECORDED as an in-flight ruling if the packet is read otherwise); target stored with 3 decimals, read back as a number.
- done-check: `slo-types.test.ts` — every fixture verdict matches the file (the Go half lands in T3 and reads the same file), the formatter's two sentences exact, the arithmetic mirror's cases exact; `slos.test.ts` — workspace binding on every statement, lock-first on every mutation, zero-statement shape refusals (bad kind, missing thresholdMs on latency, thresholdMs on availability, target 0/100/NaN, window "1d", blank name), the cap, D440 sentences, refuse-never-upsert, app-generated `slo_` ids, `updateSlo`'s SET list pinned; `slos.integration.test.ts` — two workspaces disjoint on the list, cross-workspace id refusals, the channel RESTRICT refused inside Postgres for an SLO reference; `alerts.test.ts` still green with the joined feed; RED by sabotage (the workspace predicate defanged) on record; full web suite green with the DSN; tsc + eslint clean.

### T3: Go — the indicator parser, the arithmetic, the event text (pure)
- status: pending
- wave: 2 · depends: T1 (the fixture file lands in T2 — T3 runs after T2 in this Fable-direct sequence) · model: Opus (the arithmetic is the sprint's honesty core)
- owns: services/ingest/internal/alerting/indicator.go (new — `ParseIndicator`, `Indicator` struct, `ErrInvalidIndicator`, vocabularies as `var` slices the TS test can read), services/ingest/internal/alerting/indicator_test.go (new — corpus shape + parity + populated-fields + never-panics, the condition_test.go shape), services/ingest/internal/alerting/slo_math.go (new — `sloBudget(good, total, target) (current, burned float64, status string)` + `AtRiskBudgetPct = 75` + the strict-boundary rule on counts), services/ingest/internal/alerting/slo_math_test.go (new — the packet §5.2 table + the fixture file's `arithmetic` cases), services/ingest/internal/alerting/slo_text.go (new — `sloTransitionText(name, ind, target, window, measurement, from, to)`), the corresponding cases in evaluator_test.go
- goal: packet D505–D509, D512 (text), D517 VERBATIM. Statuses: `breached` iff `bad*100 > total*(100-target)` on counts (float arithmetic, strict), else `at-risk` iff burned ≥ 75, else `healthy`; `total = 0` ⇒ `no-data`, current/burned unset. Text: title `<name>: breached — 98.40% against a 99% target` / `<name>: at risk — …` / `<name>: healthy again`; detail `<objective sentence>: 98.40% over the last 30d (1,234 of 1,254 traces good); 160% of the error budget consumed.` (`formatNumber` for the percentages, thousands separators on the counts).
- done-check: `go test ./internal/alerting/ -run 'Indicator|SloBudget|SloText' -count=1` green; the corpus ≥ 20 cases, ≥ 8 each verdict; the TS half (T2) still green against the same file — one artifact, two readers; `go vet` clean.

### T4: Go — the SLO claim on the S7.1 ticker, transitions as events, the deliverer's `slo`
- status: pending
- wave: 3 · depends: T1, T3 · model: Opus (a second claim loop inside the proven evaluator; the deliverer join; the additive payload)
- owns: services/ingest/internal/alerting/slo_evaluator.go (new — `EvaluateDueSLOs`, claim SQL, the write-back SQL, the transition + event insert, counters `obstack_ingest_slo_evals_{failed,nodata}_total`, `SLOEvalInterval = 5m`, `sloEvalTimeout = 30s`, `sloClaimBatch`), services/ingest/internal/alerting/eval_slo.go (new — the trace_summaries read: good/total per D505/D506, the S7.1 trace SQL shape widened to day windows), services/ingest/internal/alerting/evaluator.go (`evaluateOnce` runs `EvaluateDue` then `EvaluateDueSLOs`; `eval` seam gains a sibling `evalSLO` func field for the stub), services/ingest/internal/alerting/deliverer.go (`claimPendingSQL` gains `LEFT JOIN slos s`; `pendingEvent` gains the slo fields; `payloadFor` sets `Slo`), services/ingest/internal/notify/payload.go (`Payload.Slo *SloPayload` with `json:"slo"` — additive, version stays 1; `SloPayload{Name, Objective, Status}`), services/ingest/internal/notify/payload_test.go (goldens gain `"slo":null` — the ONE deliberate wire change, additive), services/ingest/internal/alerting/integration_test.go (the SLO proofs), services/ingest/cmd/ingest/main.go (NO change expected — the tick already runs; recorded if one is needed)
- goal: packet D508, D510–D512 VERBATIM: claim `FROM slos WHERE enabled AND next_eval_at <= now() ORDER BY next_eval_at LIMIT $1 FOR UPDATE SKIP LOCKED` in its own transaction; per-SLO: parse (fail-open on error), read (30s timeout, fail-open, `failed` counter), `total = 0` ⇒ no-data (counter; status → `no-data` with NULL numbers; no event either way), else compute (T3), write current/burned/good/total/evaluated_at/status, `last_transition_at = now()` on a status change, and INSERT an `alert_events` row (`slo_id` set, `rule_id` NULL, `channel_id` = the SLO's channel at emit, severity by transition: →breached critical, →at-risk warning, →healthy info; `link` NULL) ONLY when the SLO has a channel and the transition is between healthy/at-risk/breached; reschedule `+5m` always. Deliverer: the joined `slo` name/objective/status ride into `payloadFor`; `rule` stays null on SLO events.
- done-check: RED-first against a stub (`evalSLO` returning fixed counts): the transition sequence healthy → at-risk → breached → healthy emits exactly three events at warning/critical/info with `slo_id` set and `rule_id` NULL; two ticks in one status emit nothing; a channel-less SLO transitions (status + `last_transition_at` move) with ZERO rows in `alert_events`; no-data → healthy is silent; the fail-open path leaves status and numbers unchanged, advances `next_eval_at`, counts; two concurrent claimers never double-evaluate one SLO (the D478 proof re-run). Real half: seeded traces (the S7.1 `seedTrace` helper) → availability and latency good/total exactly as independently counted, the service scope excludes the other service, the other workspace excluded; the deliverer sends an SLO event with `"slo":{…}` and `"rule":null` and marks it delivered. `go test ./internal/alerting/ ./internal/notify/ -count=1` green against compose; full module green; `go vet` clean.

### T5: the flip — `/app/slos` live (mock, live, editor, actions, page, guards, registry)
- status: pending
- wave: 3 · depends: T2 · model: Opus (the D431 shape with a client editor; three unwired-control tests move)
- owns: apps/web/src/components/slos/SlosMock.tsx (new — master's page body VERBATIM, sha-pinned modulo the export line), apps/web/src/components/slos/SlosLive.tsx (new, server), apps/web/src/components/slos/SlosEditor.tsx (new, client), apps/web/src/components/slos/actions.ts (new, mutations only), apps/web/src/app/app/slos/page.tsx (the D431 shape; reads `listSlos` + `getUsage` for `retentionDays`), apps/web/src/app/app/slos/page.test.ts (new), apps/web/src/lib/live-routes.ts + live-routes.test.ts (`/app/slos` exact; the unwired control → `/app/incidents`), apps/web/src/components/dash/OnboardingChecklist.test.tsx (the second control → `/app/incidents`), apps/web/src/app/status/status.test.ts (D324 guard reads page + SlosMock + SlosLive), apps/web/src/components/alerts/AlertsLive.tsx (renders `slo: <name>` beside `rule:`), apps/web/src/server/alerts.test.ts (the D441 action-surface test for slos: a sibling assertion in slos.test.ts)
- goal: packet D514 VERBATIM: `if (dataMode !== "live") return <SlosMock />; await connection(); …`; `SlosLive` renders the four-way status style (`NO DATA` faint), the window label + the D507 clip note (`30d · 7d retained on Free`) when `retentionDays < windowDays`, the objective from the ONE formatter, `current%` or `—`, `target N%`, the budget bar `min(100, burned)` with the ≥75 colour rule, `evaluated YYYY-MM-DD HH:MM UTC` / `not yet evaluated`, `<status> since …` when `lastTransitionAt` set, the channel name or `no channel — computed only`, the `inspect` link built with `tracesSearchString` (`status=error` + `service` for availability; `minMs=<thresholdMs>` + `service` for latency), `data-tour="slos"` on the grid; `SlosEditor`: new/edit form (name, kind, service or all, threshold ms on latency, target, window, channel or none), enable toggle, delete — the `AlertsEditor` shape, the store's sentence verbatim; `actions.ts` exposes exactly `createSlo, updateSlo, setSloEnabled, deleteSlo`; empty state "no SLOs yet — an objective is a target over your own traces". Absent and GUARDED: `note`, `TerraformExport`, any `@/mock/` import in the live graph, a `/status` href or "status page" text (the D324 sweep over the three files).
- done-check: RED first — `slos/page.test.ts` (ENOENT on SlosMock), `live-routes.test.ts` (the exact-route test), `OnboardingChecklist.test.tsx` (control), `status.test.ts` (extended) all red before the flip; `SlosMock` GENERATED from `git show master:apps/web/src/app/app/slos/page.tsx` with only the export line rewritten and its sha256 pinned; guards green; full web suite green; tsc + eslint clean. Deferred to integration: the two-build DOM md5 of `/app/slos` in mock mode.

### T6: the tour step (D515) + the absences page (D516)
- status: pending
- wave: 3 · depends: T5 · model: Sonnet (copy + two needle tests, in a stated shape)
- owns: apps/web/src/components/shell/TourGuide.tsx (the `/app/slos` step's copy), apps/web/src/components/shell/TourGuide.test.ts (the D404-shape test for `/app/slos`), apps/web/src/content/docs/what-obstack-does-not-do/index.mdx (the M6 paragraph)
- goal: packet D515/D516 VERBATIM: the step's copy is capability-only, true in both modes; the test bans `31%`, `chat latency`, `afternoon` from the step and asserts `data-tour="slos"` in BOTH `slos/SlosLive.tsx` and `slos/SlosMock.tsx`; the absences paragraph as written in D516 (two surfaces render sample data; SLOs computed over the workspace's own traces; the fence sentence). No docs page, no manifest change, no corpus pin moves.
- done-check: TourGuide suite green with the new test proven red first (the old copy fails it); docs 12/12, doc-links, mirror, landing-fence, corpus-honesty, Quickstart suites green (no pin moves expected — recorded if one does); the absences page read by a human once: no sentence about SLOs is false after T5.

### T7: drive/e2e slos leg
- status: pending
- wave: 4 · depends: T4, T5, T6 · model: Opus (a new arm with an independent recomputation and a RED hook)
- owns: deploy/compose/exit-seed.mjs (`--leg slos`: three SLO rows the `seedAlerts` way, `RED_WITHHOLD_SLOS`, the exported names), deploy/compose/e2e-drive.mjs (the slos arm after the changes arm; reuses the alerts leg's receiver pattern), deploy/compose/README.md (drive section: the slos bullet)
- goal: packet §5.3 VERBATIM: alice — availability SLO target 99.99 / 7d on the drive's live webhook channel (seeded by this leg the alerts way; breached by construction: the seed carries error spans), latency SLO target 50 / 600000 ms / 7d with NO channel (healthy); bob — availability SLO on his empty workspace (`NO DATA`). Settle on OBSERVED state (≤60s tick + ≤5s deliverer, 120s deadline). Checks: the three status words on the right cards; alice's availability `current%` equals the drive's OWN `trace_summaries` recomputation (good = merged `error_count = 0` traces; total = merged traces; workspace-scoped) to two decimals; budget > 100 and the bar width capped at 100; the objective sentence exact; no SAMPLE badge, no `31%`, no `afternoon`; `/app/alerts` carries the breach event `delivered` with `slo: <name>`; the receiver's payload has `slo.name` and `rule: null`; bob's page lists only his no-data card with none of alice's names; the `inspect` link parses (`status=error`). RED hook withholds the SLO rows: the page/feed/receiver checks fail, never the seeder.
- done-check: drive GREEN (all prior checks + the new ones) and RED (`RED_WITHHOLD_SLOS=1`) exactly the slos-arm checks red; the README bullet describes what runs; token hygiene sweep unchanged (no new secret).

## Escalations log

## Integration
- verdict: pending
- gates: drive GREEN + RED; two-build byte identity of `/app/slos`; full web suite (DSN set, fresh mock build); Go module; tsc + eslint; PR CI all green; K0 staging + production promotion in the ship log.

## Goal check (Step 7, one entry per iteration)

## Retro (filled at end of run)
