# Deploy-team Plan — the client-pilot chart bump (0.7.0): the pilot packet's §12 as one additive bump (`demo.enabled`, `web.explainMode`, the `/mcp` address from the web Ingress, the collector's key through a Secret, an opt-in ClickHouse `backups` disk), the self-hosting docs page's pilot sections, and the riders that prove them; calls (a)–(g) proceeding as drafted

meta:
- date: 2026-09-15
- repo/branch: `claude/continuing-work-6qef96` (cut from master @ `bfb31c1`, the S8.1 close merge — PR #39)
- mode: **Fable-direct, session-wide** — every task executed in the main session, sequentially in wave order. A deploy-team run, not a dev sprint (M6 phase plan §7: deploy-shaped work goes to deploy touchpoints); no product surface moves.
- iteration-limit: 3
- iteration: 0/3
- governing docs (binding, read before executing): `.planning/2026-09-14-client-pilot-cluster-packet.md` (PROCEEDING AS DRAFTED on the user's "continue", D680–D700 — §12 implemented VERBATIM; deviations escalate, never smoothed); the chart README's scope boundary (D35/D253 — what stays OUT stays out); D268 (one bump per run, additive); D275 (no credential literal in a rendered pod spec); D283 (requests only on the stores); D14 (exact pins); D119 (the browser origin); D276 (the upgrade guard, untouched); the S8.1 packet's condition 10 and D653 (the `/mcp` address is an env line on the web Deployment, rendered from `web.ingress.host`).

## Kickoff notes

- **Ruling posture, stated so nobody mistakes it for a signature:** the packet's calls (a)–(g) were not put individually. The user answered the packet as a whole with "continue", with S8.1 merged and this packet the only next gate on the table — the S8.1 convention ("as drafted ratifies all", the reading stated in the session) applies, and this plan proceeds AS DRAFTED. Consequences, per call: **(a)** public packages is a GitHub package-visibility setting, not a repository change, so it is OWED TO THE USER and the chart gains no `global.imagePullSecrets` (the packet's own "only if (a) rules for pull secrets"); **(b)** and **(c)** are the two product calls the packet declined to take — "as drafted" for both is the DOCUMENTED manual recipe (the operator's `workspace_plans` row, the seeded key's revocation), so nothing built here forecloses either alternative and a later ruling re-opens exactly the docs section it names; **(d)**–**(g)** are the pilot's runbook choices (cert-manager + Let's Encrypt, VM snapshot + nightly `pg_dump`, the demo pod kept for the four-layer proof then disabled, the 4 vCPU / 8 GiB / 60 GiB floor) and land as docs, with the alternatives named beside them. Any call the user flips re-opens exactly its task; nothing else moves.
- **Tree-lens review of the packet before executing (the S8.1 precedent), citations checked against `bfb31c1`:** `values.yaml:141` (the Explain-mode comment), `templates/web/deployment.yaml:112` (the ingest-Ingress branch), `daemonset.yaml:53-54` / `cluster-deployment.yaml:71-72` (the key literal), `0005_metering.sql:128-129` (the plan seed), `0004_api_keys.sql:53-56` (the continuity row), `billing/client.ts:29` (`fake` default), chart `README.md:430` and `docs/self-hosting/docker-compose:89-94` (the seed statements), "no `imagePullSecrets` in the nine pod templates" (grep: 0 hits, 9 templates) — all as cited. **Corrections, folded in:** (1) the web image is tagged `live-sha-<sha>` / `mock-sha-<sha>`, not `sha-<sha>` (`images.yml:247-266`); ingest and clickhouse carry `sha-<sha>` — the docs print the right form; (2) `images.yml` publishes on EVERY master push (`:216-219`), so the newest images are at the S8.1 merge, not PR #37's — the pilot pins the digest of whatever sha it deploys, read off that run's step summary; (3) the demo pod is two files (`templates/demo/deployment.yaml` + `service.yaml`), so the gate goes on both. **Measured today:** anonymous GHCR token requests for `obstack-web`, `obstack-ingest`, `obstack-clickhouse` answer 401 and the manifest GETs at `bfb31c1` 403 — (a) stands exactly as the packet measured it; `obstack-demo-agent` has no package at all (token 403).
- **The box:** no Docker daemon, no kind, no kubectl. helm v3.19.0 installed to the scratchpad (the chart README's measurements are under helm v4.0.1; `lookup` is a no-op in `helm template` either way). Consequence, stated once: every render-time gate runs here (`helm lint`, `helm template`, the render riders, the web unit suite's docs pins, `tsc`, lint); the live rider (T8's backups round-trip) runs on the runner's `stack` job, dispatched on this branch by `workflow_dispatch` (`stack.yml:98`). The one store-level claim this run makes — the `backups` disk XML at the pinned ClickHouse version — is run here against the pinned server's own static build (`clickhouse-common-static-26.3.17.110`, 222 MB, fetched), not believed from the docs.
- **The image's entrypoint, read at the pinned tag before T5 exists:** `docker/server/entrypoint.sh` at `v26.3.17.110-lts` extracts every `storage_configuration.disks.*.path` from the merged config (`:42`) and `mkdir -p` + `chown`s each one alongside the data dir (`:88-91`), so a disk declared in `config.d` gets its directory created and owned by `clickhouse` (uid/gid 101, `Dockerfile.ubuntu:19-20`) at boot — no init container, no `fsGroup`.
- Seams surfaced by the breakdown are numbered from **D701**. D680–D700 belong to the packet.
- **No product surface changes** (§12's last sentence): `apps/web` moves only in the docs corpus, plus ONE test pin — the chart's `/mcp` literal asserted equal to `MCP_PATH`, the `mirror.test.ts` rule for a literal with a shared definition.
- Lessons carried in: a default render that changes is stated as such, never discovered (S6.4's 0.6.0 note); a rider on the `stack` job carries its wall-clock cost in the plan (S4.4/S6.4 precedent); a pin that must move moves deliberately and says so.

## Critical items (advisor-return before merge)
- T1 — the demo gate: the default render is 0.6.0's plus exactly the lines this bump adds; `demo.enabled=false` removes exactly the two demo objects and nothing else.
- T4 — the collector's key: NO key literal in any rendered pod spec on either path (the D275 line, extended); the default path still authenticates on the runner (every SOLID/NEARBY row in the stack run is proof); a `--set collector.apiKey` change rolls both collector workloads through a checksum annotation.
- T5 — the backups disk: `BACKUP` → `RESTORE` → row count on the pinned server locally AND on the runner's kind (upgrade with the flag on, last in the run).
- T7 — the docs page: every chart value it names resolves in `values.yaml`, every `OBSTACK_*` name it prints is defined in the repo, the install line stays verbatim, no fabricated host or credential (the corpus tests are the pins).

## Tasks

### T1: `demo.enabled` (D688)
- status: pending
- wave: 1 · depends: —
- owns: `deploy/helm/obstack/values.yaml` (`demo.enabled: true` with its reason), `templates/demo/deployment.yaml` + `templates/demo/service.yaml` (gated whole), the README's demo bullet.
- goal: the demo pod renders exactly as today by default (kind and CI unchanged); `false` removes the Deployment and the Service and nothing else; the collector's `excludeContainer` stays as it is (excluding a container name that does not exist is a no-op).
- done-check: `helm template` default vs the 0.6.0 baseline render — identical (this task adds no line); `--set demo.enabled=false` → 21 objects, the two `-demo` objects gone, `budget` 960m.
- result: **done.** `demo.enabled: true` in `values.yaml` with its reason; both demo templates gated whole (`{{- if .Values.demo.enabled }}` … `{{- end }}`, the cluster-deployment idiom). Measured: the default render is OBJECT-IDENTICAL to the 0.6.0 baseline (the only textual diff is the two gate comments helm carries through — no field moved); `--set demo.enabled=false` renders 21 objects (23 − Deployment/`obstack-demo` − Service/`obstack-demo`), zero `obstack-demo` references anywhere in the render; `acceptance.ts budget` 990m default / 960m with the demo off (25m + 5m, the two containers' requests). `helm lint` clean.

### T2: `web.explainMode` (D692)
- status: pending
- wave: 1 · depends: —
- owns: `values.yaml` (`web.explainMode: fake`), `templates/web/deployment.yaml` (`OBSTACK_EXPLAIN_MODE` rendered ALWAYS, the D267 "always set the runtime half" posture; a value outside `fake`/`anthropic` refused at render with the web image's own sentence).
- goal: the pilot's operator sets `anthropic` and puts the key in the web Secret's `explain-api-key`; until then the manifest SAYS `fake`, so the fake panel is a stated posture, not a discovered one.
- done-check: default render carries `OBSTACK_EXPLAIN_MODE=fake` (one added env line — the ONE default-render delta of this bump, stated in the README's upgrade note); `--set web.explainMode=anthropic` renders it; `--set web.explainMode=gpt` fails `helm template` naming the two admitted values.
- result: **done.** `web.explainMode: fake` beside `explainApiKey` (the key's comment re-pointed at the mode above it); the Deployment renders `OBSTACK_EXPLAIN_MODE` unconditionally, after a `has`-guard that `fail`s the render with the web image's own sentence. Measured: the default render's non-comment diff against the 0.6.0 baseline is exactly the two lines `- name: OBSTACK_EXPLAIN_MODE` / `value: "fake"` (T1 and T3 add none by default); `--set web.explainMode=anthropic` → `"anthropic"`; `--set web.explainMode=gpt` → `Error: … web.explainMode must be "fake" or "anthropic", got "gpt"`. `helm lint` clean.

### T3: `OBSTACK_PUBLIC_MCP_ENDPOINT` from the web Ingress (D691 / S8.1 D653) and the `MCP_PATH` pin
- status: pending
- wave: 1 · depends: —
- owns: `templates/web/deployment.yaml` (under the existing `web.ingress.enabled` branch: `<scheme>://<web.ingress.host>/mcp`, scheme from `web.ingress.tls.enabled` — the OTLP line's exact ternary), `values.yaml` (the `web.ingress` comment names it), `apps/web/src/server/mcp-endpoint.test.ts` (the chart's literal path suffix asserted equal to `MCP_PATH`).
- goal: a self-hosted install behind a hostname prints that hostname on `/app/mcp` and in the docs' client setups; nothing is exposed the web Service does not already serve (`pathType: Prefix` on `/` carries `/mcp`).
- done-check: `--set web.ingress.enabled=true,web.ingress.host=obstack.pilot.test,web.ingress.tls.enabled=true` renders `https://obstack.pilot.test/mcp`; tls off renders `http://…/mcp`; ingress off renders no such line; the web unit test reads the template as text and fails if the suffix and `MCP_PATH` disagree.
- result: **done.** The env line sits in a new `web.ingress.enabled` branch of the web Deployment, directly above the ingest-Ingress branch it mirrors (`printf "%s://%s/mcp" (ternary "https" "http" .Values.web.ingress.tls.enabled) .Values.web.ingress.host`); the `web.ingress` values comment names it. Measured: tls on → `"https://obstack.pilot.test/mcp"`, tls off → `"http://obstack.pilot.test/mcp"`, ingress off → zero occurrences in the render. The pin: `mcp-endpoint.test.ts` gains a third test that reads the template as text and asserts the printf's path suffix equals `MCP_PATH` — proven RED first by editing the template's suffix to `/mcpx` (the test fails with "the chart's MCP address suffix and MCP_PATH disagree"), then restored; 3/3 green.

### T4: the collector's key through the chart's Secret — `collector.existingSecret` (D687)
- status: pending
- wave: 2 · depends: T1
- owns: `templates/secret.yaml` (`obstack.collector.secretName`; the render condition and a `collector-api-key` entry), `templates/collector/daemonset.yaml` + `cluster-deployment.yaml` (`OBSTACK_COLLECTOR_API_KEY` via `secretKeyRef`, NOT optional — a collector without a key must fail to start, not send `Bearer `; a `checksum/secret` annotation on the default path so `--set collector.apiKey=…` rolls both), `values.yaml` (`collector.apiKey` re-described, `collector.existingSecret: ""`), the README ("Credentials and rotation": five groups, seven fixed key names; the contract section's env bullet), `deploy/collector/README.md` (the "Bearer key" sentence).
- goal: the pilot's values file never carries the issued key — it lives in a brought Secret under the fixed key name — and the default `ok_dev_local` still flows on kind through the chart's own Secret, so the acceptance run is unchanged.
- done-check: default render — no rendered pod spec contains the key string; the only carrier is `Secret/<release>-credentials` under `collector-api-key`; `--set collector.existingSecret=x` — the chart's Secret drops the entry, both `secretKeyRef.name` read `x`; `--set collector.apiKey=other` changes the two `checksum/secret` annotations.
- result: **done.** `obstack.collector.secretName` beside the other four helpers; the Secret's render condition and `stringData` gain the collector entry on the default path; both collector pod templates read `OBSTACK_COLLECTOR_API_KEY` through a `secretKeyRef` (not optional — a keyless collector must hold in `CreateContainerConfigError`, not send `Bearer `) and carry `checksum/secret` over the value on the default path (the ClickHouse StatefulSet's mechanism). Measured on the default render: `ok_dev_local` occurs ONCE outside comments — `collector-api-key: "ok_dev_local"` in `Secret/obstack-credentials` — and in no pod spec; two `secretKeyRef` readers, both naming `obstack-credentials`; the two collector annotations share one hash. `--set collector.existingSecret=x`: the Secret has no `collector-api-key`, both references name `x`, the two collector annotations are gone (the two remaining `checksum/secret` lines are ClickHouse's and Postgres's, untouched). `--set collector.apiKey=other`: both collector hashes change together, the stores' do not. The non-comment diff against the T2/T3 render is exactly the Secret entry, the two annotations and the two `value:` → `secretKeyRef` replacements. README: the scope-boundary Secret bullet, "Credentials and rotation" (five groups, seven fixed key names, the collector's rotation bullet), the contract section's env bullet; `deploy/collector/README.md`'s "Bearer key" sentence now names the Secret. `helm lint` clean.

### T5: the opt-in ClickHouse `backups` disk (D696) — `clickhouse.backups.enabled`
- status: pending
- wave: 2 · depends: —
- owns: `values.yaml` (`clickhouse.backups.{enabled,existingClaim}`), `templates/clickhouse/configmap-backups.yaml` (new, the `config.d` file), `templates/clickhouse/statefulset.yaml` (the config.d mount, the disk's volume — a `subPath` of the data PVC by default or the operator's `existingClaim`; a `checksum/backups` annotation so toggling rolls the pod), the README's new section.
- goal: with the flag on, `BACKUP DATABASE obstack TO Disk('backups', '<name>')` has somewhere to write and `RESTORE … FROM Disk('backups', …)` reads it back; what runs those statements, when, and what copies the result off the node stays the operator's (backup automation is OUT, D253) — the docs state the smallest honest floor beside it.
- done-check: default render — no ConfigMap, no extra mount, ClickHouse pod template unchanged; flag on — the ConfigMap, the mount at `/etc/clickhouse-server/config.d/obstack-backups.xml`, the volume; `existingClaim=pvc-x` — a `persistentVolumeClaim` volume instead of the subPath; NEVER a second `volumeClaimTemplate` (immutable on a live StatefulSet). **Local proof on the pinned server:** the static `clickhouse server` with `files/obstack-users.xml` + the rendered `config.d` file: BACKUP a table, RESTORE it under another name, row counts equal.
- result: **done.** The XML lives in `files/clickhouse-backups.xml` (the chart's first `config.d` entry; no outside source, so not a contract-test row — the cluster-config precedent, stated in the README's files section) and `templates/clickhouse/configmap-backups.yaml` renders it through `.Files.Get`; the StatefulSet gains, behind the flag, a `checksum/backups` annotation over the file, the file-by-file `config.d` mount, and the disk's mount — a `backups` subPath of the `data` PVC by default, or a `persistentVolumeClaim` volume named by `clickhouse.backups.existingClaim`, never a second claim template. Measured: the default render is BYTE-IDENTICAL to T4's; the flag adds exactly one ConfigMap (24 objects), the annotation, the two mounts and the volume; `existingClaim=pvc-x` renders a `persistentVolumeClaim: claimName: pvc-x` volume and the plain mount, and the render still carries the one ClickHouse `volumeClaimTemplates` block. **Local proof, the pinned build (`clickhouse-common-static-26.3.17.110`, 757 MB static binary, `ClickHouse server version 26.3.17.110 (official build)`), run with a minimal `config.xml`, this chart's `users.d/obstack-users.xml` and the new `config.d/obstack-backups.xml`, the disk directory created at the XML's own absolute path:** `system.disks` → `backups /var/lib/clickhouse-backups/ Local`; as `default`: `BACKUP DATABASE obstack TO Disk('backups','proof.zip')` → `BACKUP_CREATED`, `RESTORE DATABASE obstack AS obstack_restored FROM …` → `RESTORED`, counts 1000/1000; as `obstack_ingest`: `BACKUP TABLE` → `BACKUP_CREATED`, `RESTORE TABLE … AS obstack.spans_proof_restored` → `RESTORED`, count 1000; as `obstack_web`: `Code: 497 … Not enough privileges … BACKUP ON obstack.spans_proof`; two zip files on the disk. The image entrypoint's own directory creation is the one leg not run here (no daemon) — read at the tag instead (kickoff notes) and covered by T8's kind rider. README "Backups" section written with the measurement and the pilot's floor (`pg_dump` nightly, the ClickHouse loop, the VM snapshot under both; the CronJob, the target and the drill named as the operator's).

### T6: `Chart.yaml` 0.7.0, the README's "Upgrading to 0.7.0", scope and credentials, the values comments
- status: pending
- wave: 3 · depends: T1–T5
- owns: `Chart.yaml` (`0.7.0` with the comment in the 0.3.0–0.6.0 idiom), `README.md` (a new "Upgrading to 0.7.0" — additive; which pods roll once on the upgrade and why: web (the env line), both collector workloads (the `secretKeyRef` + annotation), ClickHouse only if backups is turned on; the scope boundary's Secret and demo bullets; "Credentials and rotation" for the collector group; the backups section), `values.yaml` header.
- goal: an operator reading the README at 0.7.0 finds every new value, its default, and what changes on upgrade, in the sections that already exist for that purpose.
- done-check: every value name the README prints resolves in `values.yaml` (the same walk `mirror.test.ts` does for the docs page, run by hand here); the 0.6.0 section untouched.
- result: **done.** `Chart.yaml` → `0.7.0` with the bump comment in the 0.3.0–0.6.0 idiom (five items, what an operator observes, what rolls once); README gains "Upgrading to 0.7.0" above the 0.6.0 note (additive; the web pod and both collector pods roll once and why; the Ingress branch's one new line; the two switches; the untouched rest; budget 990m/960m; D276 armed), the scope boundary's demo, web and Ingress bullets name the 0.7.0 values, the OUT list gains the pilot packet's line (an ingress controller, certificates, a pull secret, external-secrets stay the cluster's) and the backups-disk-is-not-automation parenthesis; the values header names the four new blocks. Measured: `helm lint` clean, `helm show chart` → `version: 0.7.0`, the default render byte-identical to T5's; the `mirror.test.ts` value walk run by hand over the chart README resolves 22 of 24 dotted names — the two unresolved are `collector.k8sEvents`/`.enabled`, the OLD key the 0.6.0 note names on purpose (the S6.4 T1 commit records the same finding), untouched.

### T7: the self-hosting docs page's pilot sections (§12's docs item)
- status: pending
- wave: 3 · depends: T1–T5
- owns: `apps/web/src/content/docs/self-hosting/helm-chart/index.mdx` — "Since 0.7.0"; images from the registry (names, the `live-sha-`/`sha-` tags, the digest pin, `pullPolicy: IfNotPresent`, the package-visibility fact (a)); the two-step key install (install → sign up → issue an `ingest` key → `helm upgrade` with the brought Secret); the plan a self-hosted workspace lands on and the operator's row (b, as drafted, the alternative named); the seeded dev key and its revocation (c, as drafted, the alternative named); TLS with Traefik (d); the backup floor and the opt-in ClickHouse disk (e); Explain on Kubernetes (`web.explainMode`); MCP behind the Ingress (the env the chart sets, `/docs/mcp` for the client side).
- goal: the pilot's operator follows one page from a fresh cluster to a developer's Claude Code reading a trace, with the packet's runbook order (§11) as the page's order.
- done-check: `npm test` (the corpus pins: every chart value resolves, every `OBSTACK_*` defined, the install line verbatim, no fabricated host/credential, the corpus count unchanged at 16); the page names no host that resolves anywhere (placeholders in `<…>`); `/docs/mcp` linked with an absolute `/docs/…` href.
- result: **done.** The page gains a "Since chart 0.7.0" paragraph, a corrected scope section (the old text said requests and limits were unset on the stores, the collectors and the demo — false since D283; it now states values.yaml: requests everywhere, limits everywhere but the two stores — and the OUT list gains the controller, certificates and the pull secret), the five-group credentials section with `collector-api-key` and the collector's rotation bullet, and "A real install, in order" — eleven steps in the packet's §11 order: the cluster and the floor with the `budget` line, the registry images with the `live-sha-`/`sha-` tags and the digest pin and the private-package fact, the brought Secrets and the values shape (placeholders in `<…>` only, inside code), TLS (cert-manager or a held certificate), install → sign up → the `workspace_plans` row (b, with the alternative named), the seeded key (issue → Secret + `collector.existingSecret` + `demo.enabled: false` → revoke; c, with the alternatives named), the application's two paths (links only — no exporter variable is printed, the Quickstart test's ban), Explain, MCP behind the Ingress (`/docs/mcp`), the backup floor, upgrades and the first measurements. Links added: four (`/docs/billing-and-plans`, `/docs/quickstart`, `/docs/sdks/bring-your-own-otel`, `/docs/mcp`), so `doc-links.test.ts`'s corpus link count moves 24 → 28 deliberately, its comment saying why; the anchored count (5) and the corpus count (16) do not move. **One RED taken and paid:** `corpus-honesty.test.ts`'s helm-command scan parsed the prose "holds `helm install --wait` until" as a chart named `until` — the sentence now reads "holds the install's `--wait`"; a good catch by the pin, recorded. Gates: `mirror.test`, `corpus-honesty.test`, `docs.test`, `Quickstart.test`, `doc-links.test` — 65 tests, 63 pass, 0 fail, the two prerender arms SKIP until the mock build exists (run at T9).

### T8: the riders — render-time in `acceptance.ts`, the live backups round-trip in `acceptance.sh`, and the `stack` dispatch
- status: pending
- wave: 4 · depends: T1–T7
- owns: `acceptance.ts` (a new `render` command, run by `acceptance.sh` right after `budget`: the demo gate, the collector-key hygiene, the `/mcp` line, the Explain mode, the backups objects — every assertion above, as code against `helm template`), `acceptance.sh` (the `render` step; the backups rider LAST: `helm upgrade --set clickhouse.backups.enabled=true`, BACKUP → RESTORE → count through `kubectl exec`, then the re-run's `--reset-values` returns the release to defaults), `stack.yml` untouched (the script is the job).
- goal: the chart's own signed proof covers every 0.7.0 item — the render half on every run in a second, the live half on kind.
- done-check: `render` green here against helm v3.19.0 and RED-first (each assertion falsified once by a deliberate `--set` before it is trusted); `stack` dispatched on this branch and green, its wall-clock recorded against the S4.3 band (5m58–7m17 PR prior; the backups rider's cost stated).
- result: **written and proven at render time; the live half dispatched to the runner (see Integration).** `acceptance.ts render` — sixteen arms over `helm template` (`renderChart` throws with helm's stderr so a refusal is assertable; typed walkers over the rendered docs): the demo pair removed and NOTHING else moved (every other object JSON-identical); the collector key absent from every pod template under a random `--set`, present in the Secret, read by both collectors through the same non-optional `secretKeyRef`, the two checksums moving together, a brought Secret the only carrier with no checksum; `OBSTACK_EXPLAIN_MODE` `fake` by default, `anthropic` when set, `gpt` refused with the sentence; no `/mcp` line without the Ingress, `https://…/mcp` with TLS, `http://…/mcp` without; backups off renders nothing, on renders the ConfigMap with the disk XML, the config.d mount and the data-PVC subPath, an `existingClaim` its own `persistentVolumeClaim` volume — one `volumeClaimTemplates` block in every case. GREEN locally in ~4 s. **RED-first, five copies of the chart broken on purpose (`OBSTACK_CHART_DIR`, the `OBSTACK_CPU_BUDGET_M` precedent):** the demo gate deleted → "removed nothing (23 → 23 objects)"; the DaemonSet's key back to a `value:` literal → "literal in DaemonSet/obstack-collector"; the explainMode guard deleted → "helm template ACCEPTED web.explainMode=gpt"; the printf suffix `/mcpx` → the address arm; `<allowed_disk>other</allowed_disk>` → the backups-on arm — each exit 1 naming its arm. `acceptance.sh`: `harness render` right after `budget`; the backups rider LAST (a `--reset-values` upgrade with the flag, `rollout status`, `system.disks` must say `/var/lib/clickhouse-backups/`, `BACKUP TABLE obstack.spans` → `BACKUP_CREATED`, `RESTORE … AS obstack.spans_acceptance_restore` → `RESTORED`, equal counts, the copy dropped) — `bash -n` clean; README "Running the acceptance" names both. **Observation, not fixed:** `acceptance.ts` sits outside `apps/web`, so the app's `tsc -p apps/web` and its eslint base path never covered it (nor `deploy/compose/*.ts`); type-checked here through a scratch tsconfig extending the app's — clean — and eslint reports "outside of base path". Pre-registered as a harness-coverage item for whoever next touches `web.yml`. The doc-links prerender arm's second link pin (`checkedBodyLinks`, 24 → 28) moved with the same comment as the first — 46/46 across the docs tests with the mock build present. **The live half, on the runner (PR #41, `stack` job 104501618003, head `27c9b1d`):** `budget` 990m; the render step **16/16** on the runner's helm; the backups rider — `helm upgrade` with the flag on, `Release "obstack" has been upgraded`, the ClickHouse roll ("partitioned roll out complete: 1 new pods have been updated"), `system.disks` at `/var/lib/clickhouse-backups/`, **`BACKUP + RESTORE of obstack.spans round-tripped 15 row(s)`** — the whole rider **28 s** (18:06:42 → 18:07:11), the script's own total 187 s, the job **6m50s** wall-clock (18:00:26 → 18:07:16): INSIDE the 5m58–7m17 PR-run band, so the rider's cost is absorbed by the existing watch, not a structural breach of it. The image entrypoint creating and owning the disk directory on a real PVC subPath — the one leg the local proof could not take — is proven by that line.

### T9: integration gate, goal check, retro
- status: pending
- wave: 5 · depends: T8
- owns: this file (Integration, Goal check, Retro), the packet's status line.
- goal: GO/NO-GO on the packet's §12 clause by clause; what is owed to the user (a's flip, the §11 runbook, the rulings still open on b/c) named once.
- done-check: `helm lint` clean; the render riders green; web unit suite green (the S8.1 count 1077 + this run's additions, reconciled); `tsc --noEmit` + lint clean; the branch pushed.
- result: **done — see Integration, Goal check and Retro below.** Counts reconciled: S8.1's 1077 → **1078** (+1, the `MCP_PATH` pin in `mcp-endpoint.test.ts`); pass 955 → 955 and skipped 122 → 123 because this box's clone is SHALLOW and one existing arm skips on "fd3ab6a is not in this checkout's object store" — an environment fact, not this run's; the 122 DSN/ClickHouse skips are S8.1's own. Two `# SKIP` docs arms of the first local run went green once the mock build existed.

## Escalations log
- **D701 (T5, a decision inside D696's shape, recorded so it can be cited)** — the `backups` disk's volume is a pod-template `volumes:` entry, never a second `volumeClaimTemplate`: a StatefulSet's claim templates are immutable once it exists, so a template-claimed disk would make "turn backups on" a reinstall of the store. Default: a `backups/` subPath of the data PVC (a staging area, stated as such in values, README and the docs page); `clickhouse.backups.existingClaim` names the operator's PVC for a volume of its own. The path is a separate mount (`/var/lib/clickhouse-backups`), not a directory under the data dir, so the two choices differ only in which volume backs it. The directory's creation and ownership are the image entrypoint's (`storage_configuration.disks.*.path`, read at the tag), not an init container's.
- **D702 (T4, a decision inside D687's shape)** — the collector's `secretKeyRef` is deliberately NOT `optional: true` (a keyless collector must hold in `CreateContainerConfigError`, not send `Bearer ` and be 401'd quietly on every batch — the opposite of the web workload's Explain key, which IS optional because `fake` mode reads none), and the default path keeps the literal in the chart's own Secret plus a `checksum/secret` annotation on both collector pod templates so `--set collector.apiKey=…` still rolls them — the ClickHouse StatefulSet's mechanism, copied rather than a new one.
- **Observation (T8, pre-registered, not taken):** `deploy/helm/obstack/acceptance.ts` (and `deploy/compose/*.ts`) sit outside `apps/web`, so the app's `tsc -p apps/web` and its eslint base path have never covered them; only `tsx` runs them, which strips types without checking. Type-checked here through a scratch tsconfig extending the app's (clean). A `web.yml` step that includes the harness files in the typecheck is the honest fix, owed to whoever next touches that workflow.
- **CI observation (T8):** `workflow_dispatch` of `stack.yml` from this session was refused — `403 Resource not accessible by integration` (the GitHub App token carries no `actions: write`). No pull request was opened (none was asked for), so the live half of T8 — the kind run with the render step and the backups rider — is owed to the `stack` job of the PR the user opens (`pull_request` on `deploy/**`), or to a dispatch from a credential that can (`gh workflow run stack.yml --ref claude/continuing-work-6qef96`).
- **Commit-message note:** this environment's permission classifier refused two commits whose messages were in the house's long single-paragraph style; the messages were shortened and every measurement lives in this plan and the chart README instead.

## Integration
- verdict: **GO — both halves** (2026-09-15, Fable direct; branch `claude/continuing-work-6qef96`, PR #41 at `27c9b1d`: **eleven of eleven** on the runner — `kind` 1m24, `go` 1m49, `lint` 38 s, `sdk-py` 31 s, `sdk-js` 50 s, `web` 3m15, `sdk-e2e` 3m21, `images` 3m24, `e2e` 6m45, `stack` 6m50, `publish` skipped by design on a PR; mergeable state `clean`, zero review threads).
- gates, all on this box: **`helm lint`** clean at the defaults and `--strict` with a pilot-shaped values file (every group's Secret brought, both Ingresses with TLS, registry images by digest, `explainMode: anthropic`, `demo.enabled: false`, backups on the operator's claim) — that render: 23 objects, ZERO `Secret` objects, zero credential literals, `OBSTACK_EXPLAIN_MODE=anthropic`, `OBSTACK_PUBLIC_MCP_ENDPOINT=https://obstack.pilot.test/mcp`, `OBSTACK_PUBLIC_OTLP_HTTP_ENDPOINT=https://otlp.pilot.test`, `imagePullPolicy: IfNotPresent` on all four app containers, the `secretKeyRef` names the five brought Secrets, the backups volume `claimName: obstack-clickhouse-backups`, `budget` 960m — the packet's §11 step 1 taken in the pilot's shape (with placeholder digests; the real ones come off the publish run's summary). **`acceptance.ts render`** 16/16 green in ~4 s, five arms proven red against broken chart copies (T8). **`acceptance.ts budget`** 990m at the defaults, 960m with the demo off. **The pinned ClickHouse server, locally:** BACKUP → RESTORE → equal counts as `default` and as `obstack_ingest`, `obstack_web` refused (T5). **Web:** unit suite **1078 tests, 955 pass, 0 fail, 123 skipped** (122 DSN/ClickHouse legs + 1 shallow-clone arm) with the mock build present; `tsc --noEmit -p apps/web` clean; `eslint` clean; the docs tests 46/46 including both prerender arms; the harness type-checked through a scratch tsconfig — clean. **Go:** untouched (0 files under `services/`); the two `files/` copies the contract test pins are byte-identical to master. **Mock/live:** no product surface moved — `apps/web` changes are the docs page and two test files; the corpus tests pin the docs to no mode branch.
- **not run here, run on the runner:** the kind stack (no Docker, no kind, no kubectl on this box; the dispatch refused, see the escalations log) — the render step and the backups rider's first live run happened on PR #41's `stack` job: 16/16 and the 15-row round trip in 28 s (T8's result carries the lines). What only kind could add — the image entrypoint creating the directory and the `subPath` mount on a real PVC — is now measured, not read.
- diff: 20 files, +1,197/−59 — `deploy/helm/obstack` 14 files, `deploy/collector/README.md`, the docs page, two test pins, the two planning files. Nine commits.
- seam findings: D701, D702 recorded (decisions inside the ruled shapes, none smoothed); two observations pre-registered.

## Goal check (Step 7, one entry per iteration)
- iteration 0 (2026-09-15): **GOAL MET on §12's dev-shaped items, clause by clause; the operator actions named.** (1) `global.imagePullSecrets` — NOT built, exactly as §12 conditions it ("only if (a) rules for pull secrets"): (a) proceeds as drafted (public packages), which is a GitHub package setting and is OWED TO THE USER — measured today the three packages still answer 401/403 anonymously. (2) `demo.enabled` — T1, default `true`, the pair removed and nothing else moved. (3) `web.explainMode` — T2, always rendered, refused outside the two values. (4) `OBSTACK_PUBLIC_MCP_ENDPOINT` rendered from `web.ingress.host` — T3, with the scheme from the TLS flag and the `/mcp` literal pinned to `MCP_PATH`. (5) `collector.existingSecret` with a `collector-api-key` key replacing the rendered literal — T4, the literal gone from both pod specs on both paths. (6) the optional ClickHouse `config.d` with a `backups` disk — T5, proven on the pinned server; D701 states the volume shape. (7) the self-hosting docs page gains every listed item — registry images and digests, the two-step key install, the plan row (b, as drafted, alternative named), the dev-key revocation (c, as drafted, alternative named), the TLS pairing, the backup floor — T7, in the §11 runbook's order, the corpus pins green. (8) "`acceptance.sh` riders" — T8: the render half proven here and again on the runner, the live half proven on the runner (PR #41: 15 rows round-tripped through the disk in 28 s). (9) "none changes a product surface" — held: `apps/web` moves only in the docs corpus and two tests. **Narrowings, stated:** none against §12. **Owed forward, not absorbed:** (a)'s visibility flip; the merge of PR #41 (the user's); the §11 runbook against the client's VM (the user's, with the real digests); the (b)/(c) product calls; the S7.5 renumbering to `0015`; the harness-coverage observation.

## Retro (filled at end of run)

**Shipped**: chart 0.6.0 → **0.7.0**, additive, in nine commits on the designated branch: the five §12 chart items, the chart README (an "Upgrading to 0.7.0" note, a "Backups" section, five credential groups), the self-hosting docs page's runbook, sixteen render-time assertions and a live backups rider in the acceptance, and one cross-source pin in the web suite. The default render differs from 0.6.0 by exactly one env line (`OBSTACK_EXPLAIN_MODE`), one Secret entry and the two collectors' `secretKeyRef` + checksum; the demo pod, the stores, the Jobs and the ClusterRole render byte-for-byte as before.

**What went right**:
- **Running the pinned server instead of believing the docs** — the 757 MB static build of `26.3.17.110` ran with the chart's own `users.xml` and the new `config.d` file in under a minute of setup, and turned the backups disk from a plausible XML into a measured round trip, including the read-only user's refusal.
- **Reading the image's entrypoint at the tag before writing the template** — the `storage_configuration.disks.*.path` loop answered the "who creates the directory" question with a citation, and saved an init container that would have been a guess.
- **The render checks as code with `OBSTACK_CHART_DIR`** — sixteen arms, four seconds, and each proven red against a broken copy in the same minute it was written; the `OBSTACK_CPU_BUDGET_M` precedent made the hook obvious.
- **The corpus pins caught a real lie** — "holds `helm install --wait` until the timeout" parses as a chart named `until`; the honesty scan refused the page and the sentence is better for it.
- **The tree-lens review of the packet paid before a line was written** — the web image's `live-sha-` tag form would have gone into the docs wrong.

**What went wrong**:
- **The live half of T8 could not be proven here.** No Docker, no kind, and the workflow dispatch refused with 403 — the backups rider's first kind run had to wait for the PR. Everything it asserts was run locally against the same server version, and the runner then said the rest: green on the first run, 15 rows round-tripped in 28 s, the job inside its band. The cost was one round trip through a PR instead of a local run; the rider was right first time because the local server run had already taken the same statements.
- **The permission classifier refused the house's commit-message style twice**, so the commit log carries short messages and the plan carries the measurements — a legibility cost, stated.
- **The harness has never been type-checked in CI** — found while adding 270 lines to it. A scratch tsconfig covered it here; the `web.yml` fix is pre-registered, not taken (not this run's file).
- **The docs page carried a stale scope claim** (limits unset on the collectors and the demo — false since D283); corrected because the page was open, but it had sat there through three chart versions.

**Lessons carried forward**:
- A `config.d` file the chart mounts needs the same `checksum/` annotation the users file has, or an edit never reaches a running server.
- A StatefulSet feature that needs a volume goes in the pod template, never in `volumeClaimTemplates`, if it must be switchable on a live release.
- A credential that must exist is a non-optional `secretKeyRef` — the failure mode should be a pod that will not start, not a request that is quietly refused.
- Prose in the docs corpus is parsed as commands by the honesty scan: write `helm install` only where the next two tokens are a release and a chart.
