# M5 Milestone Bundle — exit met

meta: milestone M5 (Full Data Platform, S6 arc) · exit wording **phase plan §4 "Common exit (M5)"** (`2026-08-31-s6-m5-full-platform-phase-plan.md`) · split **D360 Option C, BINDING** — S6.1 → S6.2 → S6.3 → S6.4, sequential per D364 · effective tip **`1c957ef`** (PR #32 merge, all checks green; = production since the S6.4 promotion) · the S6.5 contingency seam (costs split-out, phase plan §8) was never triggered — costs shipped inside S6.4.

## The common exit, verbatim, met clause by clause

> **Common exit (M5, proposed):** explore, dashboards, infra, map, services, costs, users, traces/diff and WatchWidgets render the signed-in workspace's real data in live mode (each flipped in `live-routes.ts`), the OTLP metrics signal is ingested/stored/queryable end-to-end, and the public demo is byte-unchanged in mock mode (D114/D21).

1. **explore** — S6.1 (PR #29, ship log in `2026-09-01-s6.1-team-plan.md`): ExploreLive on T6's frozen contract, `/app/explore` in `liveWiredRoutes`, D21 flip asserted in CI against the badged control; honest cap statement from `count(DISTINCT series_hash)`.
2. **map, services, users, traces/diff** — S6.2 (PR #30): five surfaces (issues rode along per D361 pull-forward) live-wired over EXISTING tables only, ingest diff EMPTY; the traces/diff carve-out removed (D420) — the exclusion list is gone with its last entry.
3. **dashboards + WatchWidgets** — S6.3 (PR #31): CRUD persists per-workspace in Postgres (`0009_dashboards`, D424), widget charts read only `queryMetricSeries`/`listMetricCatalog`, `/app` overview renders pinned widgets from Postgres (D425), flag-sabotage drive proves the view is over the flag.
4. **infra + costs** — S6.4 (PR #32): the collector distro ships k8s metrics into the S6.1 store (kind gate in `acceptance.sh` proves the leg), infra renders real node/pod views, costs renders trace-derived LLM economics under the D362 fence (fenced-out figures absent, not zeroed); both flipped — the final flip.
5. **"each flipped in `live-routes.ts`"** — verified at the tip: `liveWiredRoutes` carries `/app/explore`, `/app/dashboards` (+detail subtree), `/app/map`, `/app/services` (+detail), `/app/users`, `/app/issues`, `/app/infra`, `/app/costs`, `/app` (overview/WatchWidgets), and the `/app/traces/` subtree covering traces/diff; the carve-out list is deleted. `/app/ask` remains the one un-wired badged control (M7's), exactly as the S6.4 exit intended.
6. **"OTLP metrics ingested/stored/queryable end-to-end"** — S6.1: gRPC+HTTP accepted in the one ingest binary with auth parity, `0005_metrics` DDL per the D363 packet (engine-probed, 18 in-run amendments), rollups to 1m/1h + `metric_series`, queryable only through the frozen contract (never FINAL, never bare SELECT); the drive reads exact bucket values for all three temperaments through that contract. S6.4 extended the same store with the collector's k8s leg (whitelist unforgeable via the condition-13 single literal source, `lib/infra-types.ts`).
7. **"public demo byte-unchanged in mock mode (D114/D21)"** — proven per sprint, not once: S6.1 two-worktree prerender DOM md5 identical + `mock/` diff EMPTY; S6.2 ten routes md5-identical after chunk-name/Fizz-separator normalisation; S6.3 ten routes md5-identical, fenced diffs EMPTY; S6.4 both new mock routes DOM-md5 identical (two-worktree builds).

**Method of record:** each clause stands on its sprint's advisor-signed GOAL MET entry and ship log (S6.1 §Goal-check iteration 0; S6.2 iteration 1 after the r1 NO-GO; S6.3 iteration 0 FIXED-GO; S6.4 GO round 1, Fable-direct) — plus the D107 e2e drive grown across the arc: 166 checks at M3 → **239/0** at S6.4 close, RED-first proofs on every flip. Full suite at the tip: 889/0. CI: PRs #29–#32 each merged all-green; post-merge master runs green (the one failure since — stack on the docs-only retro commit `96084bd` — is a kubelet stats-window timing flake on the D466 `memWorkingSetBytes > 0` assertion; same code green on `1c957ef` 20 minutes prior; rerun of the same run green — flake confirmed).

## Deploy state (production of record)

Production branch = `1c957ef` (promoted at S6.4 close, user-confirmed). Every S6 sprint closed with its K0 staging gate and production promotion in the ship log; migrations landed in order (`0005_metrics` + `0008_metrics_health` at S6.1, `0009_dashboards` at S6.3), each boot log proving ONLY the new versions applied — both volumes intact throughout (the S5 wipe precedent never recurred). Public checks at each promotion: app routes gate to `/login`, `/v1/metrics` 401 behind auth, obstack.dev + status.obstack.dev 200.

## Consolidated carry-forward inventory (nothing absorbed silently)

- **M6/S7-GATE (from phase plan §6/§8):** alert engine + notifier + alerts (the XL foundation) → slos → incidents → changes → oncall; consider pulling `changes` to second (three consumers block on it). Standing rulings due at M6 planning: oncall build-vs-integrate (audit leans pager-provider integration); security pull-forward from M7 or not (vision reserves the call).
- **Fence releases owed to M6:** services' deploys panel drops its `SampleMark` when the changes feed lands (D362); costs' infra-$/revenue split waits on M6's connector machinery; the `/app` top-failing table still `sample={live}`.
- **Open observations:** D410 (edge query unbounded, 30s backstop; fix pre-registered), D413, the drive's URL-not-click path to `/app/traces/diff`, `e2e-drive.mjs:2911` near-tautological, D440 silent-no-op delete, the Railway watch-path observation (all four services rebuild on every production push — verify before relying on watch paths for volume-backed services), local harness posture S6.1-L1, D382/D385/D389 pre-registrations.
- **Not run (operator credentials, offers stand):** authed production walks — a real metrics POST + `/app/explore` render (S6.1), the five S6.2 surfaces, a dashboard create/pin/reload (S6.3).
- **New flake class (this bundle):** the D466 kind acceptance assertion `memWorkingSetBytes > 0` can race a freshly-started pod's kubelet stats window. One occurrence on record (`96084bd`; rerun of the identical run green). If it recurs, the fix is a settle/retry in `acceptance.sh`, not a weakened assertion.

> **Bundle signature.** M5 is CLOSED. The phase-plan common exit is met in full at `1c957ef`, each clause standing on its sprint's advisor-signed bundle inside the four team-plan files: S6.1, S6.2, S6.3, S6.4 — all four exit-blocking sprints closed, sequential per D364, zero fix waves escalated to the user. The whole-arc "zero SAMPLE badges" wording remains M5+M6+M7's shared exit, not claimed here; M5's contribution is done — `/app/ask` is the last badge standing, and it is M7's. — 2026-09-01
