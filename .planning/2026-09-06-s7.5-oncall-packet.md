# S7.5 PACKET — on-call for real: rotations, escalation policies, channel routing, the S7.1 notifier consulting them, the escalation walker, in-product acknowledgement, and the `/app/oncall` flip (pre-breakdown, D363 precedent)

meta:
- date: 2026-09-06 · repo `master` @ `95ce584` (S7.4 T1–T6 merged); S7.4 T7–T8 and the integration gate sit on `s7-4` @ `5a7c8f6` (PR #37, CI deferred on GitHub billing — the local gates are the sprint's evidence). This packet is drafted against THAT tree, which is what S7.5 will be cut from once #37 merges.
- status: **DRAFT, REVIEWED — for ruling.** Rulings numbered from **D598** (S7.4's log ended at D597 in flight at T8); a seam surfaced by the breakdown is numbered from **D627**. S7.5 tasks implement the RULED packet verbatim; deviations escalate, never smoothed.
- governing: phase plan §5 S7.5 (run-goal: "rotations + escalation policies + channel routing CRUD per-workspace, the S7.1 notifier consults routing/rotation to pick delivery targets, escalation steps walk on unacknowledged critical events, `/app/oncall` flipped; mock byte-identical" · scope fence: "no calendar sync, no mobile push, no ack-via-channel round-trip (ack is in-product), schedules v1 = the mocked weekly-rotation shape only") and §8 **R1** ("oncall = FENCED IN-HOUSE BUILD … prices the build at M … **if breakdown re-prices the fenced build above M, escalate — do not silently widen**"); §7 standing constraints; D13/D21/D114 (honesty, byte-identity); D7/D11 (tenancy); **D112** (`0001_workspaces.sql:5-9`, restated at `0002:13-15` and `0004:17-19`: no FK of ours binds a better-auth table — "DDL of ours that bound to them would make an upstream rename our outage"); the S7.1 packet D477–D492 (the engine and notifier this consults, amended by D490–D492); the S7.3 packet D505–D518 and the S7.4 packet D519–D566 (the flip template this copies, and the store/DDL idioms it inherits).
- inputs verified against the tree (file:line, every one opened; the two lenses re-opened them and corrected nineteen — the corrections are applied below): `app/app/oncall/page.tsx` (133 lines, sha256 `17f0cbf5e5c2e923c4f4a3fc7adbc5469e8c71bcc4697723f6fbe0f65e1f1cce`, byte-identical on `master` and `s7-4`), `mock/oncall.ts` (104 lines; consumers exactly `page.tsx:3`, `state/workspace-store.tsx:5`, `components/oncall/ChannelToggles.tsx:5`), `workspace-store.tsx:13-14,21,62-63,68`, `ChannelToggles.tsx` (49 lines, `"use client"`), `SideNav.tsx:52`, `CommandPalette.tsx:27`, `TourGuide.tsx` (fifteen `path:` entries, NONE is `/app/oncall`; fifteen at `6655b15` too — the M6 steps that exist pre-dated their flips), `lib/live-routes.ts:31-32,38` (22 entries, the incidents pair mid-list, `/app/settings` last), `live-routes.test.ts:9-18`, `OnboardingChecklist.test.tsx:152-154`, `lib/docs/mirror.test.ts:618-635`, `content/docs/what-obstack-does-not-do/index.mdx:110-126`, `content/docs/retention/index.mdx:17`, `pgmigrations/0001_workspaces.sql:5-13`, `0002_saved_views.sql:10-15`, `0004_api_keys.sql:17-19`, `0010_alerts.sql:15,43,58-59,74-75`, `0011_change_events.sql:42`, `0012_slos.sql:47,55-84` (the ALTER idiom: the FK INLINE under `ADD COLUMN IF NOT EXISTS`, the `DO $$ … pg_constraint … regclass $$` guard for a table-level CHECK on an existing table), `0013_incidents.sql:8-9,47-58,79-82,99-100`, `0003_auth.sql:36,44,46`, `server/invites.ts:214-247`, `server/auth.ts:129-141`, `server/postgres.ts:56-86` (`pg_advisory_xact_lock`, transaction-scoped), `server/alerts.ts:55,86,189-191,258,441,488,662-690,703`, `server/alerts.test.ts:663-674` (the eight-name export guard over `components/alerts/actions.ts`), `server/incidents.ts:279,312-313` and `server/slos.ts:109,116,161` (reads of `alert_events`/`alert_rules`/`notification_channels` outside `alerts.ts`), `components/alerts/actions.ts:77-139`, `components/alerts/AlertsLive.tsx:33-37,140`, `components/alerts/AlertsEditor.tsx:239-296`, `lib/incident-types.ts:447-455` (`formatIncidentClock(iso)` — ONE argument, renders `YYYY-MM-DD HH:MM:SS UTC`, absent as `—`), `internal/alerting/evaluator.go:46,55,209-215,219-230,360-365,386-387`, `internal/alerting/slo_evaluator.go:115-117`, `internal/alerting/deliverer.go:49-67,125-153,184-190,195-214,242-278`, `internal/alerting/testdata/condition-fixtures.json` (the D483 artifact; read by `condition_test.go:27` and `apps/web/src/lib/alert-types.test.ts:30`), `internal/notify/notify.go:1-65,142`, `internal/notify/payload.go:39-95`, `cmd/ingest/main.go:302-341` (`OBSTACK_NOTIFIER_ALLOW_PRIVATE` at `:310-312`), `internal/pgmigrate/qa_a6_migrations_test.go:178`, `internal/retention/retention.go:158-168`, `retention_test.go:415-417`, `pg_integration_test.go:148`, `deploy/compose/e2e-drive.mjs:3274-3324,3548-3550,3629,4464-4541`, `deploy/compose/exit-seed.mjs:1141-1215`.
- method, and the evidence standard: drafted against the tree by one author, then **adversarially reviewed on two read-only lenses the same day** (the S7.4 method): correctness-against-the-tree (19 findings: 3 refutations, 10 citation/number corrections, 6 notes) and house-doctrine/honesty (25 findings: 6 refutations, 9 amendments, 3 re-flaggings, 7 notes). **Three refutations were found by BOTH lenses independently** — the FK binding into better-auth's tables against D112 (D602), the drive arm's step-1 target closed by another arm (D623), and the acknowledge race stated backwards (D611) — and the sharpest disputes were re-verified by hand before acceptance (`0001:5-9`, `e2e-drive.mjs:3324`, `AlertsLive.tsx:140`, `alerts.ts:662-690`, `incident-types.ts:447`, `postgres.ts:76`). Every accepted finding is folded in and marked ⟨rev⟩ where it changes a ruling; two findings became new §7 calls ((n), (o)) and one widened an existing one ((c)). Nothing was refuted and left standing.
- **the price, stated first because R1 makes it the first call (§7(a)):** the fenced build as this packet reads it is **L, not M** — five new tables and two guarded ALTERs (§2), the deliverer re-pointed at a deliveries table (§3, D609), a third ticker in the ingest binary (§3, D610), a six-section surface with three editors and an acknowledgement control (§4), and a drive arm with two receivers of its own (§5). The M reading exists and is written down in §7(a) — routing to the current primary with NO escalation walk — but it drops a run-goal clause ("escalation steps walk on unacknowledged critical events"), so it is not a fence, it is a narrowing, and R1 says that is the user's to take. §7(c) now carries a THIRD shape the review surfaced — escalation steps as child `alert_events` rows — priced honestly beside the other two.

---

## 0. Frozen product contract (stated first)

**What master renders today.** `app/app/oncall/page.tsx` is a 133-line SERVER component (no `"use client"`, no async, no `connection()`), sha256 `17f0cbf5…`, rendering five sections from `@/mock/oncall` in this order: **Now on call** (three team cards — `Platform`/`AI`/`Infra` — each a primary, a secondary and an `until Mon Aug 11, 09:00` handoff), **Rotation schedule** (seven weekday cells per team, `TODAY = "Sun"` hard-coded at `:7` with the comment "never derived from the real clock"), **Escalation policies** (three, each an arrow chain of free-text steps: `Page primary on-call → 5 min no-ack → page secondary → 15 min no-ack → notify #incidents + team lead`), **Notification channels** (`<ChannelToggles />`, a `"use client"` component reading `channels`/`toggleChannel` from `state/workspace-store.tsx` — four seeded channels of kinds `slack | pagerduty | email | webhook`, two enabled, toggled in CLIENT state that survives nothing), and **Alert routing** (eight rows mapping rule NAME → policy id → a LIST of channel ids, cross-referenced by string, with a `Manage rules` link to `/app/alerts`). The header prints `{rotations.length} rotations · {escalationPolicies.length} policies` — derived from the fixture's counts, not typed. There is no `data-tour` anchor on this page and no tour step names it. There is no `server/oncall.ts`, no `page.test.ts`, no e2e coverage, and the registry has no `/app/oncall` entry (`live-routes.ts` holds 22 entries; the incidents pair is at `:31-32`, `/app/settings` closes the list at `:38`). The phase plan's inventory (§2): "`oncall/page.tsx` (133 ln, `mock/oncall.ts` — rotations/policies/channels/routing cross-referenced by name/id)"; its sizing (§3): "rotations/escalations riding the notifier — OR pager integration · XL if built / M if fenced · §8 ruling R1".

**What the alerting pipeline holds today, which this sprint CONSULTS rather than rebuilds.** A rule points at ONE channel (`alert_rules.channel_id … ON DELETE RESTRICT`, `0010`); the evaluator claims due rules every 60 s (`EvalInterval`, `evaluator.go:46`; `claimSQL:209-215` selects `channel_id` with the rule) and on `ok→firing` inserts ONE `alert_events` row carrying that channel, captured at emit (`insertEventSQL:228-230`, the D491 comment at `:219-222`: "a later rule edit never retargets an alert that already happened"); `firing→ok` inserts the `info` resolution event through the same helper (`:360-365`); the deliverer claims `pending` events every 5 s (`DeliverInterval`, `deliverer.go:49`), joins the captured channel and the rule or SLO (`claimPendingSQL:137-151`, `FOR UPDATE OF e`), POSTs through `notify.Deliverer` with the three egress fences (scheme/address bound to the connection, secrets masked, a per-workspace per-minute cap — `notify.go`'s header), and marks the row `delivered` or, at `maxAttempts = 3`, `failed` (`markSQL:153`, `deliverOne:242-278`, the retry branch `:273-277`). A NULL or disabled channel is `failed` at once (`deliverable():187-190`). SLO transitions ride the same table with `slo_id` instead of `rule_id` (`slo_evaluator.go:115-117`). The versioned payload evolves by addition only (`payload.go:39-57`: `slo` was added at version 1 with that sentence). The feed renders `pending`/`delivered`/`failed` as pills (`AlertsLive.tsx:33-37`) and each rule as `→ {channelName}` (`:140`). `server/alerts.ts` owns every WRITE of the three tables; reads exist elsewhere already (`incidents.ts:279,312-313` select `alert_events` joined to `alert_rules` for the promotion path; `slos.ts:109,116,161` read `notification_channels`), so D530's "one module per table's read shapes" describes the writes and the feed, not every read ⟨rev: tree⟩. `components/alerts/actions.ts` exports exactly eight Server Functions (`:77-139`), pinned by name in `server/alerts.test.ts:663-674`.

**Who a person is.** better-auth's organization plugin: `"user"` (`0003:36` — `id`, `name`, `email`), `"organization"`, `"member"` (`0003:46` — `organizationId`, `userId`, `role`); a workspace belongs to an organization through a SOFT `org_id` (`0001:5-13`, D112 — deliberately no FK; "integrity is the signup transaction's property"); the roster is one read, `listOrgMembers(orgId, query)` over `MEMBERS_SQL` (`invites.ts:214-247` — `"member"` joined to `"user"` on `organizationId = $1`; it has no `workspaces` join), bound to the session's org. Signup provisions one org with one owner member and one workspace (`auth.ts:129-141`). **There is no per-member contact channel anywhere in the product** — a member is a login, not a pager. No migration in the set references `"user"`, `"member"` or `"organization"`, and nothing under `services/ingest` reads them ⟨rev: tree⟩.

**What v1 KEEPS.** The five sections, as real per-workspace objects: a rotation is a named weekly schedule of workspace members with one primary per weekday and one optional secondary; an escalation policy is an ordered list of steps against one rotation — page the primary, page the secondary, notify a channel — each after a stated delay; routing is a rule pointing at a policy; the notifier consults the policy and the rotation when a routed rule fires, and walks the policy's later steps while a critical event stays unacknowledged; acknowledgement is a click on `/app/oncall` by a signed-in member. Channels stay the S7.1 store's rows with the S7.1 kinds and actions. **The run-goal's "the S7.1 notifier consults routing/rotation" is read as: the consult happens where the target is CAPTURED — at emit, in the evaluator's transaction (D491's rule) — and the deliverer, which is the notifier's edge, delivers what was captured** ⟨rev: doctrine — the re-reading named, the D533 habit⟩.

**What v1 FENCES OUT, absent-not-faked (D13).** Channel kinds beyond `webhook` and `slack_webhook` — the mock's `pagerduty` and `email` are ABSENCES (the phase-plan fence: no pager provider, no mobile push; D603 says how a person is reached instead); **simultaneous fan-out** — the mock's routing rows name a LIST of channels per rule (`alertRouting[].channelIds`), and the live model has ONE target per step, so a rule fires to one place at a time and nothing fans out ⟨rev: doctrine — a narrowing the first draft dropped silently; recorded here, on the absences page (D622) and pre-registered as N `channel` steps sharing a position⟩; calendar sync; ack-via-channel (a receiver cannot acknowledge; only a signed-in member can); schedules beyond the weekly shape (no overrides, swaps, layers, or handoff on any day but every day at one hour — pre-registered, not built); condition-based waits (`30 min unresolved`) and business-hours or "next morning digest" steps (the mock's second and third policies — a step that waits for a state or a calendar is a scheduler this sprint does not build); an "open incident" step (`mock/oncall.ts:63` — creation v1 is manual + promotion, S7.4's fence, and a policy minting incidents is an auto-incident heuristic by another name); re-notify while firing (D484 stands); an ack link in the delivered payload (D613 says why); time zones (D605, §7(b)); SLO transitions routed through policies (D608's narrowing, §7(l)).

**Query/mutation contract (freezes at kickoff, `lib/oncall-types.ts`, client-safe, ZERO runtime imports — the `incident-types.ts` rule, machine-enforced):**
- `EscalationTargetKind = "primary" | "secondary" | "channel"`; `EscalationDelay = "1m" | "5m" | "15m" | "30m"` (D606); `DeliveryTargetKind = "own" | "primary" | "secondary" | "channel"` (`own` = the producer's own channel: a rule's, an SLO's, a test notification's, or a step that fell back — D615) ⟨rev: doctrine⟩; `DeliveryState = "pending" | "delivered" | "failed" | "cancelled"` (the NEW table's four; `alert_events.delivery` keeps S7.1's three — D611); `DeliveryReasonKind = "" | "no_slot" | "no_secondary" | "channel_disabled"` (D615); `WalkEnded = "acknowledged" | "resolved" | "exhausted" | "policy_deleted"` (D612); `IsoWeekday = 1 | 2 | 3 | 4 | 5 | 6 | 7` (Monday = 1, the DDL's and both resolvers' one convention — D607); each with its runtime list, pinned to `0014`'s CHECKs (D616).
- `RotationRow { id, name, handoffHourUtc, participants: RotationParticipant[], week: Record<IsoWeekday, participantId | null>, secondaryParticipantId: string | null, createdAt }`; `RotationParticipant { id, userId, name: string | null, channelId, channelName, channelEnabled }` — `name` is the `"user"` row's at read time through a LEFT JOIN, `null` when that row is gone, never stored twice (D603).
- `OnCallNow { rotationId, weekday, primary: RotationParticipant | null, secondary: RotationParticipant | null, since: string, until: string }` — `null` is a slot no row covers, rendered as such (D604).
- `EscalationPolicyRow { id, name, rotationId, rotationName, steps: EscalationStep[] }`; `EscalationStep { position, target: EscalationTargetKind, channelId: string | null, channelName: string | null, after: EscalationDelay | null }` (`after` is `null` at position 0 — the DDL's `0`, D616 states the mapping).
- `AlertRouteRow { ruleId, ruleName, severity, channelName, policyId: string | null, policyName: string | null }`.
- `PagingRow { eventId, title, severity, at, acknowledgedAt: string | null, acknowledgedByName: string | null, walkEnded: WalkEnded | null, nextStepAt: string | null, deliveries: DeliveryRow[] }`; `DeliveryRow { step, target: DeliveryTargetKind, channelName: string | null, pagedName: string | null, reason: DeliveryReasonKind, weekday: IsoWeekday | null, delivery: DeliveryState, attempts, at, decidedAt: string | null }`.
- Formatters, ONE definition each: `formatEscalationStep(step)` (the arrow-chain sentence — `page the primary`, `after 5m unacknowledged, page the secondary`, `after 15m unacknowledged, notify #ops`), `formatDeliveryReason(reason, weekday, pagedName)` (D615 — the sentence is built at render, the row holds a KIND), `formatHandoff(handoffHourUtc)` (`09:00 UTC`), `formatWeekday(IsoWeekday)`; absolute instants through the EXISTING `formatIncidentClock(iso)` from `@/lib/incident-types` (one argument, `YYYY-MM-DD HH:MM:SS UTC`, absent as `—` — importable because that file has zero imports) ⟨rev: tree — the first draft gave it a `nowMs` it does not take and a `13:04` shape it does not render⟩; no new clock formatter.
- Mutations (server actions, mutations-only per S6.3-L2/D441, lock-first per D429 except where D611 says why not): in `components/oncall/actions.ts` → `createRotation`, `updateRotation`, `deleteRotation`, `addParticipant`, `removeParticipant`, `setParticipantChannel`, `setSecondary`, `setRotationWeek` (all seven at once — D604), `createPolicy`, `updatePolicy`, `setPolicySteps` (wholesale — D617), `deletePolicy`; in `components/alerts/actions.ts` (the table's own module, D618) → `setAlertRulePolicy`, `acknowledgeAlertEvent`. Mock fixtures untouched; mock byte-identity per D114.

---

## 1. Q1 — The route shape, the flip, and the pins that move

- **D598 — `/app/oncall` flips in the D431/D519 shape, one route, no `[id]`.** `components/oncall/OncallMock.tsx` is `app/app/oncall/page.tsx` moved verbatim with ONE line changed (`export default function OncallPage() {` → `export function OncallMock() {`), sha-pinned at `17f0cbf5…` with the regeneration recipe naming the pre-flip COMMIT (`95ce584`; the `incidents/page.test.ts:135-159` idiom — the S7.3 recipe went stale the day its flip landed, and D519 fixed the idiom). `page.tsx` becomes `if (dataMode !== "live") return <OncallMock />;` before `await connection()`, then the live reads. Every byte the demo serves at that URL today it serves after — including `ChannelToggles` and its `workspace-store` client state, which stay exactly where they are: the mock's toggles are demo interaction over demo channels and are rendered by the pinned body alone (D601). There is no detail route: the mock has none, the run-goal names none, and every object here is small enough to edit in place on the one page (the `/app/alerts` and `/app/slos` shape, not the incidents pair). The registry takes ONE exact entry after `live-routes.ts:32`: `"/app/oncall", // the workspace's own rotations, policies and routing; the S7.1 channels; who is paging now (S7.5)` — 23 entries, and the drive's D565 coverage check reads them.
- **D599 — the three pins that were written to go red at this flip go red, and the packet names each so the breakdown moves them rather than meets them.** (i) `mirror.test.ts:629-630` — the premise line `isLiveWiredRoute("/app/oncall") === false` with its own message "when it wires, narrow the paragraph again", and the needle `"There are no on-call rotations"`: the paragraph narrows (D622) and the premise flips to `true`; the registry-derived half of that test (T7's coupling, `:618-635`) then requires that `/app/oncall` NOT be listed as an absence — the test was built to fail exactly here. (ii) `OnboardingChecklist.test.tsx:152-154` — the checklist's second unwired control is `/app/oncall` and its comment says "S7.5 wires oncall and will move that one"; it moves to **`/app/mcp`** (M7's third surface, unwired past every M6 sprint), NOT to `/app/ask`, which is the drive's SAMPLE positive control and phrases five drive checks (D522's reason, restated). (iii) `live-routes.test.ts:9-18` does NOT move — its control sits on `/app/pipelines` precisely so that S7.5 would not have to move it (D522: "a control that expires in one sprint has to be moved every sprint, and a move is a chance to forget"); the packet records that the S7.4 choice paid off here. **The tour does not move because there is nothing to move:** `TourGuide.tsx` carries fifteen steps and none has `path: "/app/oncall"` — a tour needle test for this surface would pin an absence, and the sprint adds NO step (the M6 tour steps that exist — incidents, slos — pre-dated their flips at `6655b15` and were rewritten, not invented). `SideNav.tsx:52` and `CommandPalette.tsx:27` point at the index and stay.
- **D600 — the mock's four channel kinds do not survive, and the live channels section is the S7.1 store's, read and toggled through S7.1's own module.** `notification_channels.kind` is `CHECK (kind IN ('webhook','slack_webhook'))` (`0010`); the mock's `slack | pagerduty | email | webhook` union is the FIXTURE's. The live section renders `listNotificationChannels(ws)` (`alerts.ts:488`) — id, name, kind, `targetMasked` (`maskTarget`, `:86` — the target is never whole on any read path, D487), enabled — with an enable/disable toggle calling the EXISTING `setNotificationChannelEnabled` action (`actions.ts:105`) and a create form calling the existing `createNotificationChannel` (`:99`), so `/app/oncall` gains no second writer of that table and the D487 masking has one implementation. `pagerduty` and `email` appear nowhere on the live surface, not even as disabled options: a kind the deliverer cannot POST to is a promise (D13). **`deleteNotificationChannel` (`alerts.ts:662-690`) DOES change ⟨rev: both⟩:** it counts rules and SLOs today and refuses with them named, and its 23503 backstop (`:684`) says "used by an alert rule or an SLO"; after D602 a channel can also be held by a rotation participant or an escalation step, so the action gains two counts, the sentence names all four kinds (`used by 2 alert rules, 1 rotation participant and 1 escalation step — delete or repoint them first`), and the backstop sentence widens to match — the S7.3 precedent ("counts SLOs beside rules"), recorded as a moved S7.1 sentence with its test.
- **D601 — `mock/oncall.ts`, `workspace-store.tsx` and `ChannelToggles.tsx` are untouched, and `git diff --stat master -- apps/web/src/mock apps/web/src/state` is EMPTY in the evidence line.** `workspace-store.tsx:5` seeds its `channels` from `seedChannels` and `:68` provides them to the shell; the store is mounted for both modes, so editing it — or the fixture it imports — would change bytes on a different surface to fix demo state that was never broken (the D522 five-links reasoning, applied to state). `ChannelToggles` is rendered ONLY by the pinned `OncallMock` body and is banned from the live import graph by name, as `IncidentRca.tsx` was (D546).

---

## 2. Q2 — The objects: DDL, vocabularies, the person, the schedule, the bound

- **D602 — `services/ingest/pgmigrations/0014_oncall.sql`: five tables, two guarded ALTERs, 0012/0013 house style, and NO foreign key into a better-auth table ⟨rev: both — the first draft bound `"user"` three times; D112 forbids it, and the set has never done it⟩.** Every table `workspace_id TEXT NOT NULL REFERENCES workspaces (id) ON DELETE CASCADE` — carried on the CHILD tables too (`rotation_participants`, `rotation_slots`, `escalation_steps`, `alert_deliveries`), denormalised deliberately, so every read on every table is `workspace_id = $1`-first and no read has to prove tenancy through a join (the `alert_events.workspace_id` precedent). TEXT pks in the house idiom: `rot_`, `rp_`, `pol_`, `step_`, `dlv_` + 16 hex. CHECK never enum; `created_at`/`updated_at` set by hand (no trigger exists in the set). **Every pointer at a person is a SOFT TEXT column in the `workspaces.org_id` shape (`0001:5-9`): `user_id`, `acknowledged_by`, `paged_user_id` carry no FK, the D603 member proof at write time is their only integrity, and every read of a name is a `LEFT JOIN "user"` whose miss renders as an absent name — never a cascade, never a refusal in a flow the operator cannot see.** Pin: `qa_a6_migrations_test.go:178`'s `const want` gains `,0014_oncall` — the only assertion the migration moves; the web integration test's DSN guard names `0014_oncall` (the `0013_incidents` idiom carried from S7.4 T3).
  - **`rotations`**: `id`, `workspace_id`, `name TEXT NOT NULL CHECK (char_length(name) BETWEEN 1 AND 80)`, `handoff_hour_utc SMALLINT NOT NULL DEFAULT 9 CHECK (handoff_hour_utc BETWEEN 0 AND 23)` (D605), `created_at`, `updated_at`, `UNIQUE (workspace_id, name)`.
  - **`rotation_participants`**: `id`, `rotation_id TEXT NOT NULL REFERENCES rotations (id) ON DELETE CASCADE`, `workspace_id`, `user_id TEXT NOT NULL` (soft — D112), `channel_id TEXT NOT NULL REFERENCES notification_channels (id) ON DELETE RESTRICT`, `is_secondary BOOLEAN NOT NULL DEFAULT false`, `created_at`, `UNIQUE (rotation_id, user_id)`; `CREATE UNIQUE INDEX … ON rotation_participants (rotation_id) WHERE is_secondary` — ONE secondary per rotation with no circular FK (a `rotations.secondary_participant_id` would reference a table that references `rotations`, and the ALTER-after-CREATE it forces is a second guarded statement for a fact a partial UNIQUE states in one); `CREATE INDEX … ON rotation_participants (channel_id)` — **the RESTRICT's own lookup** (the D524/D567 lesson: a FK action's lookup without an index is a seq scan on every channel delete) ⟨rev: doctrine⟩.
  - **`rotation_slots`**: `rotation_id … ON DELETE CASCADE`, `workspace_id`, `weekday SMALLINT NOT NULL CHECK (weekday BETWEEN 1 AND 7)` (ISO — D607), `participant_id TEXT NOT NULL REFERENCES rotation_participants (id) ON DELETE CASCADE`, `PRIMARY KEY (rotation_id, weekday)`. Seven rows per rotation through every product path (D604); the DDL's permissiveness exists only inside `setRotationWeek`'s own transaction.
  - **`escalation_policies`**: `id`, `workspace_id`, `name … 1..80`, `rotation_id TEXT NOT NULL REFERENCES rotations (id) ON DELETE RESTRICT`, `created_at`, `updated_at`, `UNIQUE (workspace_id, name)`; index `(rotation_id)` — the RESTRICT's lookup. RESTRICT: a rotation under a policy cannot vanish — the action refuses with the policies named (the S7.1 channel-under-a-rule doctrine: refuse-never-cascade for cross-references, because a cascade silently disarms).
  - **`escalation_steps`**: `id`, `policy_id … ON DELETE CASCADE`, `workspace_id`, `position SMALLINT NOT NULL CHECK (position BETWEEN 0 AND 4)`, `target_kind TEXT NOT NULL CHECK (target_kind IN ('primary','secondary','channel'))`, `channel_id TEXT NULL REFERENCES notification_channels (id) ON DELETE RESTRICT`, `after_seconds INTEGER NOT NULL CHECK (after_seconds IN (0, 60, 300, 900, 1800))` (D606), `CHECK ((target_kind = 'channel') = (channel_id IS NOT NULL))`, `CHECK ((position = 0) = (after_seconds = 0))` — the both-or-neither idiom (`0011:42`, `0012:47`, `0013`) makes "a first step that waits" and "a later step that does not" unrepresentable; `UNIQUE (policy_id, position)`; index `(channel_id) WHERE channel_id IS NOT NULL` — the RESTRICT's lookup.
  - **`alert_rules` ALTER — the `0012:61-62` form, the FK INLINE under `ADD COLUMN IF NOT EXISTS` ⟨rev: tree — the first draft put it under a `DO $$` guard as well, which double-adds; the `pg_constraint`/`regclass` guard (`0012:73-84`) is for a table-level CHECK on an existing table and this ALTER has none⟩**: `policy_id TEXT NULL REFERENCES escalation_policies (id) ON DELETE RESTRICT`; index `(policy_id) WHERE policy_id IS NOT NULL`. This IS the mock's routing table — one pointer, not a row per (rule, policy, channels): the channels belong to the policy's steps, and a rule with a NULL policy delivers to its own `channel_id` exactly as S7.1 shipped it. RESTRICT rather than SET NULL (D609 says why the incidents pointer chose SET NULL and why this one must not): a policy delete that silently sent a critical page back to a rule's plain webhook is the S7.1 "cascade disarms rules" failure in a new coat, so the action refuses with the rules named and the operator re-routes first.
  - **`alert_events` ALTER (same inline form, six columns, two partial indexes)**: `policy_id TEXT NULL REFERENCES escalation_policies (id) ON DELETE SET NULL` (captured at emit — the D491 rule extended; SET NULL because an event that happened under a policy since deleted still happened, and its deliveries rows are the fact), `escalation_step SMALLINT NOT NULL DEFAULT 0`, `next_escalation_at TIMESTAMPTZ NULL`, `walk_ended TEXT NULL CHECK (walk_ended IN ('acknowledged','resolved','exhausted','policy_deleted'))` ⟨rev: doctrine — D612's sentence needed a column that records WHY the walk ended, or it names a cause the product did not observe (the D587 class)⟩, `acknowledged_at TIMESTAMPTZ NULL`, `acknowledged_by TEXT NULL` (soft — D112): `acknowledged_at` is the FACT; the name is a LEFT JOIN at read, absent when the row is gone, and the surface names NO cause for the absence (D619). Indexes: `(next_escalation_at) WHERE next_escalation_at IS NOT NULL` — the walker's claim shape, the `alert_rules_next_eval_at_idx … WHERE enabled` precedent (`0010`); `(policy_id) WHERE policy_id IS NOT NULL` — the SET NULL's own lookup, the `alert_events_slo_id_idx … WHERE slo_id IS NOT NULL` precedent (`0012`) ⟨rev: doctrine⟩. The `alert_events_one_producer_check` CHECK is untouched.
  - **`alert_deliveries`**: `id`, `event_id TEXT NOT NULL REFERENCES alert_events (id) ON DELETE CASCADE`, `workspace_id`, `step SMALLINT NOT NULL CHECK (step BETWEEN 0 AND 4)`, `target_kind TEXT NOT NULL CHECK (target_kind IN ('own','primary','secondary','channel'))`, `channel_id TEXT NULL REFERENCES notification_channels (id) ON DELETE SET NULL`, `paged_user_id TEXT NULL` (soft — D112), `reason_kind TEXT NOT NULL DEFAULT '' CHECK (reason_kind IN ('', 'no_slot', 'no_secondary', 'channel_disabled'))` and `reason_weekday SMALLINT NULL CHECK (reason_weekday BETWEEN 1 AND 7)` ⟨rev: doctrine — a KIND, never a sentence with a name baked in (D603's one-copy rule)⟩, `delivery TEXT NOT NULL CHECK (delivery IN ('pending','delivered','failed','cancelled')) DEFAULT 'pending'` (D611 — `cancelled` is this table's fourth state; `alert_events.delivery` keeps three), `attempts SMALLINT NOT NULL DEFAULT 0`, `created_at`, `decided_at TIMESTAMPTZ NULL`, `UNIQUE (event_id, step)`; indexes `(workspace_id, created_at DESC)`, the claim's `(created_at) WHERE delivery = 'pending'`, and `(channel_id) WHERE channel_id IS NOT NULL` — the SET NULL's lookup ⟨rev: doctrine⟩. **Retention: deliveries ride the `alert_events` sweep by the CASCADE and nothing else.** `retention.go:158-168`'s `pgDeletes` stays at two entries and `retention_test.go:415-417`'s `len(pgDeletes) != 2` pin does NOT move; the S7.4 §7.2 proof shape is repeated in `pg_integration_test.go` — GREEN: an `alert_events` row 30 days old with three deliveries attached, the sweeper's own ctid-batch DELETE, no error, event and deliveries gone; and the assertion lives in its OWN function (`TestDeliveriesRideTheEventCascade`), because `retention_test.go:415` is a `t.Fatalf` and an assertion after it never runs (the S7.4 D528(i) lesson). The five definition tables are never swept — they are definitions (the `alert_rules`/`slos` precedent). The retention docs page gains half a sentence (D622) ⟨rev: doctrine⟩.
- **D603 — a participant is a workspace MEMBER with a CHANNEL, and both halves are proven scoped at write time.** The mock's rotation names people as strings; the product has no strings for people, it has `"member"` rows and no pager. So the person is `user_id` — a member of the workspace's organization, proven by ONE scoped read at every write that binds one: a NEW join, `SELECT 1 FROM "member" m JOIN workspaces w ON w.org_id = m."organizationId" WHERE w.id = $1 AND m."userId" = $2` — `MEMBERS_SQL`'s `"member"`→`"user"` join does not reach `workspaces`, so this is its own statement in the same two-quoted-tables idiom ⟨rev: tree⟩ — refusing `NO_SUCH_MEMBER = "no member with this id in your workspace's organization"` on a miss (D440's one sentence for a never-existed id and another org's). The reach is `channel_id` — a `notification_channels` row of THIS workspace, proven by the S7.4 D544 scoped SELECT (`WHERE workspace_id = $1 AND id = $2`) and never by the FK, whose `id` is a global TEXT pk that proves existence and says nothing about whose. The member's NAME is read from `"user"` at render (`RotationParticipant.name`, a LEFT JOIN), never copied into the rotation — a renamed user renames everywhere, and the schedule holds no second copy that can disagree. Rejected: free-text names (D13 — a rotation that "pages Nour" and can reach no one is a promise); a per-user pager field on the profile (a settings surface change on a table better-auth owns, and it would make the ONE person's channel cross every workspace their org has — the channel belongs to the workspace, D487's secret rule applies to it, and the participant row is where the two meet).
- **D604 — the schedule is the mock's shape exactly — seven weekday slots and one optional secondary — written in ONE mutation, refused with a gap, and complete through every product path ⟨rev: both — the "a `"user"` cascade can create a gap" clause is gone with the FK⟩.** `setRotationWeek(rotationId, week: Record<IsoWeekday, participantId>)` writes all seven under `lockWorkspace` (delete-then-insert of the seven rows inside the transaction, the `setPolicySteps` shape — D617) and refuses `INCOMPLETE_WEEK = "every weekday needs a primary; {weekdays} unassigned"` if any of the seven is missing or names a participant not of this rotation (a scoped read — D621 Path I). `removeParticipant` refuses `PARTICIPANT_HOLDS_SLOTS = "this member is the primary on {n} weekday(s); assign those first"` while a slot names them, and `HOLDS_SECONDARY` while they are the secondary — so through the product's own controls a rotation is always covered, and a `user_id` whose `"user"` row has gone still holds its slot and its CHANNEL (the page goes to the channel; the card reads the channel's name with no person's name beside it and states no cause — D619). The resolver still answers `null` for a slot no row covers (defensive: the only path is a transaction interrupted between the delete and the insert, which `withTransaction` rolls back whole), and the surface and D615 render that state truthfully rather than assuming it away. Nothing falls back to a guessed person: a fallback to a PERSON is an invented on-call. A fallback to the rule's own CHANNEL is not (D615).
- **D605 — v1 time is UTC, one handoff hour per rotation, and the packet says what that costs.** The mock's `until Mon Aug 11, 09:00` is a handoff instant; the phase-plan fence names "the mocked weekly-rotation shape only". A time zone per rotation is the obvious next column and it is NOT here, for a reason that is arithmetic rather than taste: "who is on call now" must be computed by TWO programs — the web tier for the surface and the ingest binary for the walker (D607) — and IANA zone handling is where those two disagree in practice (Node's `Intl` tables are ICU's; Go's are `tzdata`'s; the two ship different versions and different alias sets, and a DST transition is where a page goes to the wrong person). Proving parity across zones is a fixture of DST edges in both languages plus a vendored zone list generated from one side and diffed by the other — real work, and the M reading has no room for it. So: `handoff_hour_utc` (default 9), the surface prints `handoff 09:00 UTC` on every card and in the editor, and the resolver's fixture (D607) is over UTC weekdays only. The customer consequence, one sentence: **a team in a zone without daylight saving (Riyadh, 06:00 UTC for a 09:00 handoff) never drifts; a team in Berlin drifts by one hour twice a year** ⟨rev: doctrine — the first draft's Cairo example was typed, not computed: Egypt has observed daylight saving again since 2023⟩. Pre-registered, USER-VISIBLE (the D365 shape): `timezone` with the parity fixture. Flagged call (b).
- **D606 — the delay vocabulary is four members in a CHECK, and the first step waits for nothing.** `after_seconds IN (0, 60, 300, 900, 1800)` rendered as `1m | 5m | 15m | 30m` (the D482 pattern: a small stated vocabulary rather than a free integer, so a 4-second step and a 4-day step are both unwritable); position 0 is `0` by the DDL's CHECK. A widening is an ordinary migration (`0002_saved_views.sql:10-12`'s reason for a CHECK). The mock's `5 min`, `15 min` and `30 min` are members; what its second policy carries that is NOT representable is the condition-based wait (`unresolved`) and the `open incident` step, both fenced in §0 ⟨rev: tree⟩. Flagged call (e): a `seconds` integer with a 30 s floor is the alternative, and it is what a drive would prefer and what a product should not offer.

---

## 3. Q3 — Resolution and the walk: one function in two languages, emit-time routing, the deliveries claim, the third ticker, the ack

- **D607 — `onCallAt(rotation, nowMs)` is ONE function, written twice, frozen by ONE fixture (the D483 precedent).** Inputs: the seven slots, the secondary, `handoff_hour_utc`, and an instant. Rule: take the instant's UTC ISO weekday; if its UTC hour is before `handoff_hour_utc`, the PREVIOUS weekday's slot holds (the shift that began at yesterday's handoff has not ended); `since` and `until` are the two handoff instants bracketing it. ISO weekdays (Monday = 1 … Sunday = 7) are the DDL's and BOTH implementations' convention — Postgres `extract(isodow)` agrees; Go's `time.Weekday` (Sunday = 0) and JS `Date#getUTCDay()` (Sunday = 0) do NOT, and each side converts at exactly one line the fixture covers. The fixture lives where the D483 artifact lives — **`services/ingest/internal/oncall/testdata/oncall-cases.json`, read by `internal/oncall/resolve_test.go` and by `apps/web/src/lib/oncall-resolve.test.ts` through the `REPO_ROOT` path the way `alert-types.test.ts:30` reads `internal/alerting/testdata/condition-fixtures.json`** ⟨rev: tree — the first draft invented a `deploy/compose/alert-conditions.json` that does not exist⟩ — every weekday at the handoff hour minus one second and plus one second, Sunday→Monday wrap, midnight-UTC handoff, a rotation with no secondary, a rotation with a missing slot — so the surface's "now on call" and the walker's "page the primary" can never name different people for the same instant. The Go side reads the rotation with ONE statement at resolve time (`SELECT s.weekday, p.id, p.user_id, p.channel_id, p.is_secondary, r.handoff_hour_utc … WHERE r.workspace_id = $1 AND r.id = $2`, `workspace_id`-first — a read of OUR tables only) and resolves in memory against the ONE instant the claiming statement returned (D610).
- **D608 — routing is consulted at EMIT time, inside the producer's transaction, and EVERY event-insert site mints its step-0 delivery row — so the deliverer has one thing to claim and nothing is left `pending` forever ⟨rev: both — the first draft left `slo_evaluator.go` "unchanged" and the resolution event unaddressed, which under D609 strands both⟩.** The four sites, each stated: (1) the evaluator's `ok→firing` (`evaluator.go:386-387`): `claimSQL` (`:209-215`) gains `policy_id`; with a non-NULL policy the evaluator resolves step 0's target (D607 for `primary`/`secondary`, the step's own channel for `channel`; the rule's own channel with a reason kind when the target does not resolve — D615) and inserts the event with `channel_id` = that channel, `policy_id` captured, `escalation_step = 0`, `next_escalation_at` = `now() + steps[1].after_seconds` **iff the event's severity is `critical` and the policy has a position-1 step** (D610), plus its step-0 `alert_deliveries` row (`target_kind` = the step's kind, `paged_user_id` for a person); with a NULL policy **the event STATEMENT is unchanged and the transaction gains one deliveries insert** (`target_kind = 'own'`, the rule's channel) ⟨rev: tree⟩. (2) The evaluator's `firing→ok` resolution (`:360-365`, the `info` event): routed the same way at ITS emit — step 0 only, never a walk (`info` never schedules one) — so the person who was paged is the one told it cleared; unrouted, `own`. (3) `slo_evaluator.go:115-117`: the insert gains the step-0 `own` delivery row against `slos.channel_id`; **SLO transitions are NOT routed** through policies — the run-goal names routing for rules, an SLO is a definition with its own channel by D512, and routing SLOs is pre-registered and flagged (§7(l)) as a narrowing. (4) `sendTestNotification` (`alerts.ts:703`): the rule-less `pending` event AND its step-0 `own` row in one transaction — the test path rides the real path (D488/D491 kept), and `own` rather than `rule` because a test notification has no rule and a kind that says it does fabricates provenance ⟨rev: doctrine⟩. The D491 sentence is extended by one clause and one boundary: **minted deliveries are never retargeted; unwalked steps follow the policy and rotation AS THEY STAND when they walk** (D610) — a rotation edit mid-walk reaches the incoming primary, which is the only reading under which "page whoever is on call" is true, and a policy shortened mid-walk ends the walk (D612's `exhausted`) rather than reading a position that no longer exists ⟨rev: doctrine⟩.
- **D609 — the deliverer claims `alert_deliveries`, not `alert_events`, and mirrors step 0's outcome into the S7.1 columns under a PINNED invariant, so every S7.1 pin, the feed and both drive arms stay true without moving ⟨the load-bearing refactor, flagged (c) with three shapes⟩.** Today's `claimPendingSQL` (`deliverer.go:137-151`) claims events; after this sprint it claims delivery rows — `FROM alert_deliveries d JOIN alert_events e ON e.id = d.event_id LEFT JOIN notification_channels c ON c.id = d.channel_id LEFT JOIN alert_rules r ON r.id = e.rule_id LEFT JOIN slos s ON s.id = e.slo_id WHERE d.delivery = 'pending' AND d.attempts < $1 AND (d.step = 0 OR e.acknowledged_at IS NULL) ORDER BY d.created_at, d.id LIMIT $2 FOR UPDATE OF d SKIP LOCKED` — the same three LEFT JOINs for the same three real absences, the lock on `d` only, and the `acknowledged_at` clause that D611 needs ⟨rev: both⟩. `deliverOne` marks the DELIVERY row (`delivered`/`failed`/`pending` + attempts, `decided_at` on the terminal states) and, **when `d.step = 0`, ALSO runs today's `markSQL` against the event in the same transaction** — so `alert_events.delivery`/`attempts` hold "the first delivery's state", which is what S7.1's one delivery WAS, and `AlertsLive.tsx:33-37`'s pills, the alerts arm's `>delivered<` settle (`e2e-drive.mjs:3316`) and the slos arm's `delivered` read unchanged. **The mirror is a second copy of one fact and the packet says so ⟨rev: doctrine⟩:** it is kept because the alternative moves every S7.1/S7.3 pin for no customer gain, and it is made safe the way the house makes a copy safe — the invariant `alert_events.delivery = (SELECT delivery FROM alert_deliveries WHERE event_id = e.id AND step = 0)` is pinned by the deliverer's integration test after every marked batch, written in the SAME transaction, and the feed's pill is documented in `AlertsLive`'s own comment as the first delivery's state (there is no `/docs/alerts` page to say it on — the corpus has no alerts page, and this sprint adds none), with the full chain on `/app/oncall`. A routed critical event whose later pages failed reads `delivered` in the feed and `step 1 failed` on the on-call surface; both are true. `deliverable()` (`:187-190`) moves to the delivery row's joined channel with the same four conditions. Why one loop and not two: a second claim loop over `alert_events` for step 0 and this one for steps ≥ 1 would be two owners of one egress with two retry counters, two rate-cap call sites and two places for the D487 fences to be bypassed by accident; the S7.1 packet's whole point about the notifier is that it is ONE edge. Why `0013`'s pointer chose SET NULL and `alert_rules.policy_id` chooses RESTRICT (D602): `0013_incidents.sql:47-58` records the reason as the retention wedge — NO ACTION and RESTRICT raise 23503 under the sweeper's ctid-batch DELETE of `alert_events` and wedge that workspace's sweep forever, CASCADE deletes the postmortem — so SET NULL was the only action that neither wedged nor destroyed ⟨rev: tree — quoted, not paraphrased⟩; `escalation_policies` is never swept, so no sweeper ever DELETEs a policy and RESTRICT on the routing pointer can wedge nothing, which leaves the doctrine free to choose refuse-first. The new RESTRICTs and CASCADEs are checked against the sweeper the same way: the only swept table any of them touches is `alert_events`, and the only FK INTO `alert_events` this sprint adds is `alert_deliveries.event_id … ON DELETE CASCADE`, which the D602 proof runs.
- **D610 — the escalation walker is a third ticker in the ingest binary, `EscalationInterval = 5 s`, the house pattern; its claim is workspace-blind BY DESIGN, its clock is the claim's, and it resolves each step at WALK time.** `internal/oncall/walker.go`, wired in `main.go` beside the deliverer (`:337-341`) with the same ordered shutdown. Each tick, one transaction: `SELECT e.id, e.workspace_id, e.policy_id, e.escalation_step, now() AS at FROM alert_events e WHERE e.next_escalation_at <= now() AND e.acknowledged_at IS NULL AND e.walk_ended IS NULL ORDER BY e.next_escalation_at LIMIT $1 FOR UPDATE SKIP LOCKED` — **the claim is workspace-blind by design (it schedules, it does not read telemetry — D478's sentence, stated so no reviewer "fixes" it), and the `now()` it returns is the ONE instant the tick resolves against** (D534: one clock, sampled once — the first draft resolved on the host's clock and claimed on Postgres's, two clocks at a handoff second) ⟨rev: doctrine⟩ → for each: read the policy's step `escalation_step + 1` (scoped `WHERE workspace_id = $1 AND policy_id = $2 AND position = $3`); **no such step → `walk_ended = 'exhausted'`, `next_escalation_at = NULL`, no delivery row; policy gone (`policy_id` IS NULL) → `walk_ended = 'policy_deleted'`, likewise** ⟨rev: doctrine⟩; else resolve its target against `at` (D607 — the person on call NOW, not the person on call when the event fired), insert the `alert_deliveries` row (`pending`; or `own` with a reason kind when the target does not resolve — D615), `UPDATE alert_events SET escalation_step = $2, next_escalation_at = CASE WHEN a further step exists THEN $at + its after_seconds ELSE NULL END, walk_ended = CASE WHEN no further step THEN 'exhausted' END WHERE id = $1`, commit. The deliverer then picks the row up within 5 s like any other. Only `critical` events ever carry a `next_escalation_at` (D608): the run-goal's clause is "unacknowledged **critical** events", and paging a human twice about a `warning` is the alert fatigue the mock's own policies avoid; `warning`/`info` events are routed at step 0 and stop. Replica correctness by claim (D478), no leader, no watermark. Fail-open posture (D480's shape): a walker error leaves the row unchanged and logs once. One ops counter, `alert_escalations_total{outcome}`.
- **D611 — acknowledgement is ONE conditional UPDATE on `alert_events` in that table's own module, WITHOUT the workspace lock, it cancels the steps not yet in flight, and the packet states the window it cannot close ⟨rev: both — the first draft's race mechanism was backwards, and its lock would have stalled the workspace behind webhook POSTs⟩.** `acknowledgeAlertEvent(workspaceId, eventId, userId)` in `server/alerts.ts` (D618), inside `withTransaction` but with NO `lockWorkspace`: (1) `UPDATE alert_events SET acknowledged_at = now(), acknowledged_by = $3, next_escalation_at = NULL, walk_ended = COALESCE(walk_ended, CASE WHEN next_escalation_at IS NOT NULL THEN 'acknowledged' END) WHERE workspace_id = $1 AND id = $2 AND acknowledged_at IS NULL RETURNING …`; (2) `UPDATE alert_deliveries SET delivery = 'cancelled', decided_at = now() WHERE workspace_id = $1 AND event_id = $2 AND delivery = 'pending' AND step > 0` — the steps minted but not yet claimed get a TERMINAL state (a `pending` row nothing will ever claim is the stuck-pending D485/D490 exist to prevent); (3) read the row back. Zero rows from (1) means never-existed, another tenant's, OR already acknowledged — and the three are NOT one sentence ⟨rev: doctrine — D440's one sentence is for the first two; telling a member "no alert event with this id in your workspace" about a row they can see is a lie⟩: the read in (3) is scoped and unconditional, so a miss there is `NO_SUCH_ALERT_EVENT` (S7.4's sentence) and a hit with `acknowledged_at` already set returns the row as it stands — first-write-wins, the D544 promotion shape, and the surface then shows who got there first. Why no `lockWorkspace`: the S7.4 D526 objection to a web-tier UPDATE on `alert_events` was a ROW-LOCK argument — the deliverer's `DeliverPending` holds one transaction across `deliverBatch(10) × deliverTimeout(5 s)` of webhook POSTs (`deliverer.go:197-214`) and D609 keeps step-0's `markSQL` inside it — so an ack that had to wait for that row while holding `pg_advisory_xact_lock` (`postgres.ts:76`, transaction-scoped) would stall every mutation in the workspace for up to ~50 s; a single conditional UPDATE is atomic on its own and waits only on its own row ⟨rev: tree⟩. **The race, stated honestly:** the walker never writes `acknowledged_at`, so an ack always commits; what it cannot reach is a delivery the deliverer has ALREADY CLAIMED (`FOR UPDATE OF d`, in flight) — that POST completes, up to one `deliverTimeout` (5 s) after the click, and the row then reads `delivered` with an instant after the ack's; the deliverer's `e.acknowledged_at IS NULL` clause (D609) stops every row it has not yet claimed. The surface says it in one sentence beside the control: `a page already in flight when you acknowledge still goes out`. `acknowledged_by` is the SESSION's user id, never a field the client sends (D11: the actor is the session). Acknowledging a `warning`/`info` event is allowed (a person may claim any event) and changes nothing about a walk that was never scheduled.
- **D612 — the resolution transition ENDS the walk with its own word and does not acknowledge.** When a rule goes `firing→ok` (`evaluator.go:360-365`), the same transaction runs `UPDATE alert_events SET next_escalation_at = NULL, walk_ended = 'resolved' WHERE workspace_id = $1 AND rule_id = $2 AND next_escalation_at IS NOT NULL` — a condition that cleared must not keep paging people about itself, and the column records that THIS is why it stopped, so the surface's `resolved before anyone acknowledged` is a fact the product wrote, not a cause inferred from a NULL that four things can produce ⟨rev: doctrine⟩. It does NOT set `acknowledged_at`: acknowledgement is a person's act, and "resolved, unacknowledged" is what a team notices when nobody was reachable. Flagged (i): the alternative — walk to the end regardless — is defensible ("the page is about the person, not the condition") and cheaper, and it is the user's to pick.
- **D613 — the payload gains ONE additive key, `oncall`; `acknowledge` inside it is `null` in v1; `paged` carries a display name OUT of the product and that is a stated decision ⟨rev: doctrine, flagged (n)⟩.** `payload.go:39-57`'s document evolves by addition (the `slo` precedent: "a receiver written before this key existed sees one more key it did not ask for and nothing else"): `"oncall": { "step": 1, "target": "secondary", "rotation": "Platform", "paged": "Salma Nabil" | null, "acknowledge": null }` — present on every delivery, `null` when the event was not routed (the unrouted path's receivers see one more key, exactly as D486 promised). `paged` is the member's display name read from `"user"` at delivery time — **the ingest binary's first read of a better-auth table** (nothing under `services/ingest` reads one today); D112 forbids DDL binding, not a SELECT, but the "upstream rename our outage" reasoning reaches a column name typed into Go, so the read is ONE statement in ONE file with that risk stated in its comment, and §7(n) offers the alternative (no name outbound — the participant's channel name instead). A display name leaving the product to a customer-supplied URL is a new data class beside D554's withholdings; it is defensible (the receiver is the workspace's own channel and the name is the thing a pager exists to say) and it is written down here rather than assumed ⟨rev: doctrine⟩. `acknowledge` would be a deep link into `/app/oncall`, and the process holds no configured public base URL to build one (`evaluator.go:224-228`'s reason for `link` being NULL — the same absence, the same honesty); it is pre-registered with `OBSTACK_APP_ORIGIN` as the obvious source, and NOT built as a relative path, which a Slack client renders as a dead string. The Slack body's `text` gains one line when `oncall` is non-null, in two shapes: `paging {paged} (step {n} of {policy})` for a person, `notifying {channel} (step {n} of {policy})` for a `channel` step — never `paging null` ⟨rev: doctrine⟩. `payload_test.go` pins the key present-and-null on the unrouted path, the additive shape on the routed one, and both line shapes.
- **D614 — escalation deliveries count against the S7.1 per-workspace rate cap and the S7.1 retry, with no special path.** `notify.go`'s cap is per workspace per minute over ALL deliveries; a five-step policy against a flapping-free critical event is five POSTs spread over its delays, and a burst of routed rules can hit the cap — a rate-limited attempt is `pending` with `attempts + 1` and retried by the deliverer's own loop (`deliverOne:273-277`), and the walker never re-mints a row. The cap's constant does not move: it was set for egress abuse, and escalation is not a reason to raise it silently (the D479 shape — a pricing/ops decision, pre-registered USER-VISIBLE).
- **D615 — a step whose target does not resolve is delivered to the rule's OWN channel, recorded as `own` with a reason KIND, and the walk continues ⟨rev: doctrine — the first draft minted a `failed` row with a sentence naming a person, which was both a dropped page and a second copy of a name⟩.** The three ways a step cannot resolve: a slot no row covers (`no_slot`, with `reason_weekday`), a `secondary` step on a rotation with no secondary (`no_secondary` — the store refuses removing a secondary while a step needs it, `SECONDARY_IN_USE`, so this arises only through a policy edited against a rotation edited in between, which `setPolicySteps` re-checks), a participant's or a step's channel disabled after the fact (`channel_disabled`). Each mints `alert_deliveries (target_kind = 'own', channel_id = the rule's channel_id, reason_kind = …, reason_weekday = …)` — the page reaches the place the rule's author named rather than nowhere, and the row says why it went there instead of where the step said. The sentence is built at RENDER by `formatDeliveryReason` from the kind, the weekday and the participant's name read then (`no one is on call for Wednesday — delivered to the rule's own channel #ops`, `{name}'s channel is disabled — delivered to #ops`), so the row holds no name and no sentence. Then `next_escalation_at` advances to the NEXT step as if the target had resolved: a page that went to the fallback must not end the escalation, because the next step is exactly the step that exists for when the first one fails. A channel DELETED under a participant or a step cannot happen while referenced (RESTRICT, D602); a channel deleted under a minted delivery row (`SET NULL`) is the `deliverable()` false branch, unchanged from S7.1 — `failed`.

---

## 4. Q4 — The store, the contract module, the surface, the pins

- **D616 — `lib/oncall-types.ts`, zero imports, every vocabulary pinned to `0014`'s CHECKs (the D541 idiom), with the parse patterns and the one mapping stated ⟨rev: both⟩.** `oncall-types.test.ts` parses `target_kind`, `reason_kind`, `delivery`, `walk_ended` out of the DDL with `incident-types.test.ts:48`'s `<column> TEXT … CHECK (<column> IN (…))` pattern and asserts deep equality with the runtime lists; `after_seconds INTEGER … IN (0, 60, 300, 900, 1800)` and `weekday SMALLINT … BETWEEN 1 AND 7` need TWO NEW patterns (an integer set, a bounded range), and the assertion for `EscalationDelay` is stated as **the CHECK's set minus `0`, mapped one-to-one onto the four labels, with `0` ↔ position 0's `after: null`** — a literal deep-equal of a five-member set against a four-member list would be a pin that cannot pass. The DDL is the one authority. Types the client editors need — `RotationRow`, `EscalationPolicyRow`, `AlertRouteRow`, `PagingRow`, and the roster's `{ userId, name }` — live HERE, not in `server/*` (D541's unbuildable-prop lesson: `resolvedImports` catches `import type`).
- **D617 — `server/oncall.ts` in the `incidents.ts` shape: lock-first, shape checks before the lock, caps under the lock, refusals with remedies, ONE mapping point per row shape, and two departures.** `OncallRefusal extends Error`; `NO_SUCH_ROTATION`/`NO_SUCH_POLICY` in D440's one sentence; `newRotationId`/`newPolicyId`/… in the `randomBytes(8)` idiom. Caps: `MAX_ROTATIONS_PER_WORKSPACE = 20`, `MAX_POLICIES_PER_WORKSPACE = 20`, `MAX_PARTICIPANTS_PER_ROTATION = 20`, `MAX_STEPS_PER_POLICY = 5` (the DDL's `position` bound) — abuse bounds on tables that are never swept, counted under the same lock as the INSERT, each refusal naming the number and the remedy (D529's sentence shape). The departures: (i) `setRotationWeek` and `setPolicySteps` are WHOLESALE writes (delete the rotation's slots / the policy's steps, insert the new set, inside the one transaction) — a per-slot or per-step mutation invites the half-written schedule the gap refusal exists to prevent, and a form that edits seven cells or five rows submits once; (ii) `deleteRotation` refuses while a policy names it and `deletePolicy` refuses while a rule is routed to it, naming the dependents — the RESTRICT FKs are the backstop, the action's sentence is the product (D602). Reads: `listRotations` (participants LEFT JOINed to `"user"` for the name), `listPolicies`, `onCallNow` (D607 in TS over `listRotations`' rows and the request's ONE clock); and in `alerts.ts` — because a second module selecting from `alert_rules`/`alert_events` for the FEED's shapes would put two owners on those read shapes (D530; the existing `incidents.ts` reads are the promotion path's, a different shape with its own owner) — `listAlertRulesWithPolicy` and `listPaging` (critical events with `next_escalation_at IS NOT NULL OR EXISTS (a pending delivery)` as PAGING, plus those whose walk ended or were acknowledged within the last hour as RECENT, each with its deliveries and `walk_ended` — D619). The roster for the participant picker is `listOrgMembers(orgId, query)` (`invites.ts:222`) — the S3-era read, unchanged.
- **D618 — two mutations land in `server/alerts.ts` and `components/alerts/actions.ts`, ONE rendered line on `/app/alerts` changes, and the packet names the pins that move rather than pretending the S7.1 graph is untouched ⟨rev: both⟩.** `setAlertRulePolicy(workspaceId, ruleId, policyId | null)` — `lockWorkspace` → the policy proven the workspace's by a scoped SELECT (never the FK — D544) → `UPDATE alert_rules SET policy_id = $3, updated_at = now() WHERE workspace_id = $1 AND id = $2` → read back; and `acknowledgeAlertEvent` (D611, lock-free by ruling). Both write tables `alerts.ts` owns. `actions.ts`'s export list (`:77-139`, eight today) becomes TEN, and **`server/alerts.test.ts:663-674`'s eight-name `deepEqual` moves by exactly two** — recorded as a pin that moves, with the test's title ("exactly the eight mutations") re-taken. **`AlertsLive.tsx:140` changes:** it renders `→ {r.channelName}` per rule, and after D608 that channel is where a routed rule's pages go ONLY when a step falls back (D615), so for a routed rule the line reads `→ via {policyName} · fallback {channelName}` and for an unrouted one exactly what it reads today — a rules list that kept printing `→ #ops` beside a rule whose pages go to a rotation would state a target the product does not use (D13) ⟨rev: doctrine⟩. `AlertsEditor.tsx`'s rule form gains NO policy field this sprint: routing is edited in the routing section, where the policy it routes to is on screen. The ack control lives on `/app/oncall` only (D619) — the S7.4 D546 precedent (the promote control lived on the incidents surface, not on alerts) applied in the same direction; flagged (d).
- **D619 — the live surface: the mock's five sections in the mock's order, plus ONE section the mock cannot have, first — and every sentence it renders is a fact the product wrote ⟨rev: doctrine⟩.** `components/oncall/OncallLive.tsx` (server): **Paging now** (NEW, first — `listPaging`'s PAGING rows, each with its delivery chain — `step 0 · page the primary · {name} via {channel} · delivered {clock}`, `step 1 · after 5m · page the secondary · {reason sentence} · delivered {clock}`, `next step at {clock}` — and an `Acknowledge` button per row with the one-sentence in-flight note (D611); RECENT rows below with their own true word from `walk_ended` or `acknowledged_at`: `acknowledged by {name} at {clock}` — or `acknowledged at {clock}` with NO name and NO cause when the LEFT JOIN misses ⟨rev: doctrine — "a member who has since left" named a cause the product did not observe; no leave/remove-member control exists in the product⟩ — `resolved before anyone acknowledged`, `every step ran; nobody acknowledged`, `the policy was deleted mid-walk`; a workspace with nothing paging says `nothing is paging`, which is now a sentence the predicate makes true), **Now on call** (one card per rotation from `onCallNow`, `{primary} · secondary: {secondary} · until {handoff} UTC`; a slot no row covers prints `no one is on call for {weekday} — assign a primary`; a participant whose `"user"` row is gone prints the CHANNEL's name with no person's name and no cause; `no secondary` when none — never a blank that reads as a name), **Rotation schedule** (seven cells; the highlighted cell is the slot CURRENTLY HOLDING from `onCallNow`, not the calendar weekday — at 08:00 UTC Wednesday before a 09:00 handoff, Tuesday's cell is lit, because that is whose shift it is ⟨rev: doctrine⟩; the mock's `TODAY = "Sun"` literal is the fixture's), **Escalation policies** (the arrow chain rendered by `formatEscalationStep` from structured steps — one formatter, never a stored sentence), **Notification channels** (D600), **Alert routing** (every rule with its policy or `— the rule's own channel`, and the `Manage rules` link kept). The header derives `{n} rotations · {m} policies` from the counts — the same words as the mock, from the store's numbers. Client components, each fenced from `@/server/*` and `@/mock/*`: `RotationEditor` (name, handoff hour, the participant picker over the roster with a channel select over the workspace's channels, the seven-cell week, the secondary), `PolicyEditor` (name, rotation, up to five steps of kind + channel + delay), `RoutingEditor` (a policy select per rule), `AcknowledgeButton`; the `SlosEditor`/`IncidentEditor` hook shape, the store's sentence printed verbatim on refusal. Every absolute instant through the existing `formatIncidentClock` (`YYYY-MM-DD HH:MM:SS UTC`; the packet does not invent a fourth clock or a shorter one — a rail whose value is ordering inside a minute prints seconds, `incident-types.ts:441-446`'s reason).
- **D620 — the D13 absence list, and the pins that stop it being vacuous.** Banned from the comment-stripped source of every live component: `Nour`, `Salma`, `Omar`, `loopwork`, `PagerDuty`, `pagerduty`, `Email digest`, `oncall@`, `#incidents`, `#ai-quality`, `#infra`, `Business hours`, `Next morning digest`, `open incident`, `team lead`, `p-critical`, `p-ai-degraded`, `p-infra-batch`, `Aug 11`, `Aug 13`, `Sun Aug 9`, `TODAY =`, `has since left`, and `"/status"` (D547's reason does not transfer — nothing here loads the status page — so this is the S7.1/S7.2 posture: no guard, and the string is on the list only because the incidents guard's pattern is cheap to keep). Non-vacuity pins: `onCallNow(` is called by `OncallLive` and no weekday is computed inline; `formatEscalationStep(` and `formatDeliveryReason(` are used and no step or reason sentence is hand-rolled; `walk_ended`'s four members each have a rendered sentence in a table, and no other sentence names why a walk ended; both `EscalationTargetKind` and the four delivery states appear styled through tables; the header's counts come from `.length` of the store's rows; `maskTarget` is the only rendering of a target (a source assertion that `target` never reaches a prop unmasked); `data-tour` is asserted ABSENT on both `OncallMock` and `OncallLive` (there is no step — D599 — and an anchor without a step is the D404 class waiting to happen).
- **D621 — tenancy proven red per path (D7/D11), each defang run and its ACTUAL red count recorded ⟨rev: doctrine — two paths the first draft missed, one of them the sharpest leak on the surface⟩.** Path A the rotations read (with participants and slots — three `workspace_id = $1` predicates, each defanged in turn); Path B the policies read; Path C the member proof at `addParticipant` (D603 — bob's user id into alice's rotation must refuse, with the join's `w.id = $1` defanged to show the leak: a global `"user".id` proves existence, and nothing else would); Path D the channel proof at `addParticipant`/`setParticipantChannel`/`setPolicySteps` (D544's scoped SELECT — the one that must be run and SEEN); **Path E `createPolicy`/`updatePolicy`'s proof that `rotation_id` is THIS workspace's — rotation ids are global TEXT pks, so bob binding alice's rotation id to HIS policy would have the walker resolve alice's participants and POST alice's channels for bob's events: run and SEEN**; Path F `setAlertRulePolicy`'s policy proof; Path G `acknowledgeAlertEvent`'s event predicate (bob acknowledging alice's page must be zero rows changed and `NO_SUCH_ALERT_EVENT`, proven at the surface as workspace B against A's ids in the S7.4 T6 harness shape); Path H the walker's and the evaluator's resolve statements in Go (`workspace_id = $1`-first, the alerting integration test's defang idiom); **Path I `setRotationWeek`/`setSecondary`'s participant-of-THIS-rotation proof**; Path J the deliveries and paging reads. Each defanged to `$1::text IS NOT NULL` (the cast, D549's T3 note).
- **D622 — the absences page narrows to nothing for M6, the status-page clause survives, the retention page gains half a sentence, and the registry-coupled test is what proves the first ⟨rev: doctrine⟩.** `what-obstack-does-not-do/index.mdx:110-126`'s M6 paragraph loses its first sentence (`There are no on-call rotations — that surface renders sample data, and`) and keeps `the public status page is deliberately not fed by SLOs or by incidents` **nor by pages** — three more words, D590's reason extended: an on-call surface is the second most tempting place to offer "post this to the status page". S7.5's fence lands as absences in the paragraph: no pager or email channel kind, no fan-out (a rule pages one place at a time, one step after another), no calendar sync, no time zones (handoffs are stated in UTC), no acknowledgement from a channel, no re-page while firing, no schedule beyond one primary per weekday and one secondary. `mirror.test.ts:629-630` moves as D599(i) says. **`retention/index.mdx:17`** ("The two Postgres tables — alert events and change events — have no outer bound of their own") gains a clause: `and the delivery attempts recorded against an alert event go with it` — a third product table is deleted by that sweep through the cascade, and a page that names two would be counting wrong; pinned by a needle beside the D528(ii) mirror pin. `doc-links.test.ts`'s `all.length === 21` / `anchored.length === 5`, `docsManifest.length === 15`, `SURFACES.docs.pages().length === 15`, `corpus.length === 15` do NOT move — no new page, no new link (the alerts/slos/incidents precedent, now four sprints running). The frontmatter's `The twenty-two absences` over 23 bold-led absences (S7.4 T7's recorded miss) is NOT this sprint's either — a separate copy ruling, restated so it is not smoothed into this edit.

---

## 5. Q5 — The drive arm, the RED hook, the placement, and the seeder

- **D623 — no seeder leg: every on-call object goes through the front door, and the arm owns BOTH of its receivers for its whole life ⟨rev: both — the first draft borrowed the alerts arm's receiver, which `e2e-drive.mjs:3324` closes the moment that arm's settle loop exits, ~15–20 s after the fire; step 1 at +60 s would have met a closed port and landed `failed`⟩.** `exit-seed.mjs` is untouched (D559's rule: the sprint's net-new write paths are the deliverable, so a seeded rotation would leave them unexercised by the one harness that runs the real build). The arm opens receivers A and B on `host.docker.internal` (the `:3276-3286` idiom) in its CREATE block and closes them in its ASSERT block. Alice, through the editors: two channels (`webhook`, targets A and B), a rotation with HERSELF as the only participant reached through A and all seven weekdays assigned to her (bob cannot be her secondary — he is a member of his own organization and D603's member proof refuses him, which the arm asserts as its Path C check rather than working around), a policy against that rotation with THREE steps — `0: page the primary`, `1: after 1m, notify {channel B}`, `2: after 30m, page the primary` — and a route: a NEW critical rule created through `AlertsEditor` (`:239-296` — free-text metric, the D482 windows, the ops, severity, channel) over the gauge the alerts arm's fixture already sent (`exit-seed.mjs:1141-1215` seeds `${label}.queue.depth` with a critical live rule at `avg > 40 over 15m`; the arm's rule uses a second threshold on the same metric so both are true of the same data), routed to the policy through `RoutingEditor`. Then the facts, each asserted against OBSERVED state: receiver A holds a POST whose `oncall` key reads `step 0 · primary · paged "{alice's display name}"`; receiver B holds a POST with `step 1 · channel` and the `notifying …` Slack line shape; the `Paging now` section lists the event with both deliveries `delivered` and `next step at` a time ~30 m out; alice clicks `Acknowledge`; the row's `acknowledged_by` is her user id and **`next_escalation_at IS NULL` with `walk_ended = 'acknowledged'` — read by the drive's own `pgOne` (D71(b)) — which is the proof that the ack cancelled a step that had been scheduled and had not yet fired**; `count(*) = 2` over her deliveries at the end of the run is a BOUND, not the ack proof (a 30 m step would not fire inside a six-minute run either way) ⟨rev: doctrine⟩. Bob's `/app/oncall` renders none of alice's rotation, policy, rule or event and says `nothing is paging`; bob POSTing alice's event id to `acknowledgeAlertEvent` is refused with zero rows changed (Path G); bob's `createPolicy` against alice's rotation id is refused (Path E). The unauth probe gains `/app/oncall` and the D565 coverage check stays green over the registry's 23 entries.
- **D624 — the RED hook: `RED_WITHHOLD_ONCALL` in the drive, guarding the WHOLE interaction block including the acknowledge press, every downstream fact captured with a withheld default, zero `must()` after the block.** The acceptance clause, the only gate, stated as GREEN N/0 and RED (N−K)/K with K exactly the arm's checks — never a `must()` abort, never the epilogue (D564's shape; K recorded at the task, never predicted). The D566 restore is untouched by this arm: it lowers no catalog value.
- **D625 — placement: the objects and the rule are created EARLY and the facts are asserted LATE, the arm settles on ITS OWN observed state, and the slack is computed at the task, never typed here ⟨rev: both — the first draft assumed "the same evaluator tick" and wrote "tens of minutes of slack" into a run whose whole recorded band is 5m28s–6m02s⟩.** The alerts arm seeds at `:3288` and settles on its OWN rules' outcome (`:3311-3319`); the on-call CREATE block runs immediately after that seed returns and BEFORE that settle loop — but the new rule is created through page loads and a form submit, so a tick may pass between the seed and the create and the arm may NOT assume the seeded rules' tick; it assumes nothing: its ASSERT block, placed after the incidents arm, settles on receiver B holding the step-1 POST (or on a deadline of one `EvalInterval` + `after_seconds` + two ticks' slack from the create, after which the checks say which claim died), and by then the changes, slos and incidents arms have usually paid that wait in full. The slack between step 1 (~65–70 s after the fire) and the assert block is one to three minutes of the drive's own arms — measured and recorded at the task with the before/after `seconds`. Wall time (D560's discipline): the ~6-minute line is HELD, not re-based; the arm's added cost is its page reads and form submits (the incidents arm's 17 checks cost 5 s); if the line is crossed the ARM is re-cut. The S7.4 integration retro's rule binds the runner too: the local drive on a quiet box, and CI's e2e job is the exit gate when CI exists (§6.7).
- **D626 — the two receivers are the D492 test policy's, and nothing in the arm reads a delay it typed.** Both receivers are private-range HTTP, reachable only because compose/CI inject `OBSTACK_NOTIFIER_ALLOW_PRIVATE` (`main.go:310-312`, `docker-compose.yml:113`); production never sets it, unchanged. The 60 s the arm relies on is `after_seconds = 60` read back from the policy the drive created (the store's own row via `pgOne`), not a literal — the S6.4 condition-13 pattern; the assertion that step 1 has fired is against the receiver holding the POST and the deliveries read, never against elapsed time.

---

## 6. Conditions this packet ADDS to the S7.5 breakdown

1. **Critical items (advisor-return before merge):** the DDL and its FK actions (D602 — the deliveries cascade non-wedge proven on real Postgres in its own function, the two inline ALTERs proven raw AND through the runner, the double-apply computed, NO FK into a better-auth table); the resolver parity (D607 — one fixture, both suites, every case listed); the deliverer re-point (D609 — the S7.1 alerts arm and the S7.3 slos arm green UNCHANGED as the regression proof, the mirror invariant pinned, `payload_test.go`'s additive-key pins); the walker and the ack (D610/D611 — the claim predicates proven against real Postgres with a held lock, the cancel path proven, the in-flight window measured, never a grep); the flip (D598–D601 — the pin, the one registry entry, the three moved pins, the mock/state diff EMPTY, the channel-delete sentence widened).
2. **Every worked example is a claim to be COMPUTED, not typed** (S7.3's lesson, S7.4's rule): the resolver's fixture cases, the vocabulary deep-equals with D616's stated mapping, the double-apply's NOTICE count, the drive's slack and its `count(*) = 2` bound.
3. **RED-first drive arm** per D623–D626, the acceptance clause stated as GREEN/RED with K recorded, the wall time measured before and after.
4. **Mock byte-identity PROVEN two-build** (the S7.1–S7.4 method: APFS-clone `node_modules`, the pre-flip commit's worktree vs HEAD, script-stripped + asset-normalized DOM md5 of `/app/oncall` in mock mode) with `/app/pipelines` and `/app/mcp` as untouched controls, `git diff --stat master -- apps/web/src/mock apps/web/src/state` EMPTY.
5. **Tenancy red per path** (D621, ten paths), each defang run and its ACTUAL red count recorded.
6. **The absences page, the retention clause and the two moved test premises are DONE-CHECK items of the flip task** (D599, D622), not afterthoughts — the S7.2/S7.4 lesson, third application.
7. **The exit gate under a still-deferred CI, stated ⟨rev: doctrine⟩:** no build overlaps a test gate and the local drive runs on a quiet box (the S7.4 retro's two rules); **if CI is still refused by billing when S7.5 closes, the exit gate is the local drive GREEN and RED on the exact closing commit, taken twice, with the smoke floor, the web suite with the DSN, the Go module and the two-build identity — recorded as "CI deferred", never as passed**; a check that ran for 3 s is read before it is believed; and if the sprint merges in halves, the plan carries a gate line for EACH half.
8. **The `alerts/actions.ts` export guard moves by exactly two, `AlertsLive.tsx:140` gains the routed form, and `deleteNotificationChannel`'s sentence widens** (D600/D618) — each recorded as a moved S7.1 pin; `AlertsEditor.tsx` otherwise untouched, `slos` untouched except the one deliveries insert (D608), `incidents` untouched, `mock/` and `state/` untouched.
9. Standing D21/D114/D13 clauses as written; `/app/ask` stays the drive's one SAMPLE control.

---

## 7. Calls flagged for ruling — OPEN

- **(a) The price — L, not M, and R1 says escalate.** As drafted: five tables + two ALTERs, the deliverer re-pointed, a third ticker, a six-section surface with three editors and the ack, the drive arm with two receivers. **Alternative (the M reading):** routing to the CURRENT primary only — D608 without D609/D610/D611/D612, no deliveries table (the event's `channel_id` is the resolved channel and the S7.1 path is byte-identical), no walk, no ack, `escalation_policies` reduced to "which rotation and which channel for the rest" — and the run-goal's "escalation steps walk on unacknowledged critical events" clause recorded as a NARROWING with the walk pre-registered for S7.6. Honest, half the DDL, and it ships the surface's five sections true; it does not ship the sentence the phase plan wrote.
- **(b) D605 — UTC only, one handoff hour.** **Alternative:** an IANA `timezone` per rotation with the two-language parity fixture over DST edges and a vendored zone list generated from Go's `tzdata` and diffed by the TS side.
- **(c) D609 — three shapes, priced ⟨rev: doctrine — the first draft offered two and argued one⟩.** As drafted: `alert_deliveries` with the deliverer re-pointed (one egress loop, one retry counter; the S7.1 columns mirrored under a pinned invariant; every S7.1/S7.3 pin unmoved). **Alternative 1:** a SECOND claim loop over `alert_deliveries` for steps ≥ 1 with the S7.1 loop byte-identical — two owners of one egress, two retry counters, two rate-cap call sites. **Alternative 2 — escalation steps as CHILD `alert_events` rows** (`parent_event_id`, `escalation_step` on the event itself): `claimPendingSQL`, `markSQL`, D485/D490/D491, the feed's pills and the retention sweep all stay byte-true with ONE loop and no mirror; the costs are real and named — `alert_events_one_producer_check` widens (a child row has neither rule nor SLO of its own, or inherits its parent's under a new `conname`), S7.4's incident timeline stitches the alerts leg from `alert_events` and would render every page as an alert row on an incident (a page is not an alert — a D13 cost unless the leg filters children, which moves T4's statement pins), and the feed lists pages beside alerts. Cheaper than the deliveries table on DDL and the deliverer; dearer on the two S7.4 surfaces that read the table.
- **(d) D618/D619 — the ack control and the routing editor live on `/app/oncall`; `/app/alerts` changes ONE rendered line (`→ via {policy} · fallback {channel}` for routed rules) and two exports.** **Alternative:** the ack on the alerts feed too (a control in two places, one more pin on `AlertsLive`), or the rules line unchanged and the routed rule's own channel given no rendered role (which the review found dishonest).
- **(e) D606 — the delay vocabulary `1m | 5m | 15m | 30m` in a CHECK.** **Alternative:** a `seconds` integer with a 30 s floor.
- **(f) D603 — a participant is a member of the workspace's organization plus one of the workspace's channels; a person is paged through a channel.** **Alternative:** free-text participant names with the channel (the mock's shape, honest about nothing).
- **(g) D604 — all seven weekdays covered, enforced at the store; the resolver still renders a missing slot truthfully.** **Alternative:** the DDL enforces seven rows (a deferred constraint or a trigger — neither exists in the set).
- **(h) D608/D610 — only `critical` events schedule steps ≥ 1; `warning`/`info` route at step 0 and stop.** **Alternative:** every routed event walks its policy.
- **(i) D612 — the `firing→ok` transition ends the walk (`walk_ended = 'resolved'`) without acknowledging.** **Alternative:** the walk runs to the end regardless of the condition.
- **(j) D602 — `alert_rules.policy_id` is `ON DELETE RESTRICT` and `deletePolicy` refuses while routed.** **Alternative:** SET NULL, the rule silently back on its own channel.
- **(k) D625 — the drive's early-create / late-assert placement so the walk runs under existing waits, the arm settling on its own observed state.** **Alternative:** a self-contained arm after the incidents arm with its own ≥ 65 s settle, costing the wall-time line.
- **(l) D608 — SLO transitions are NOT routed; `slos.channel_id` stays their target (a narrowing, recorded).** **Alternative:** `slos.policy_id` too, the same resolve at emit in `slo_evaluator.go`.
- **(m) D613 — the payload's additive `oncall` key with `acknowledge: null` in v1.** **Alternative:** no key until the link can be built.
- **(n) D613 — NEW: `paged` carries the member's display name out of the product, read by the ingest binary from `"user"` in one stated statement.** **Alternative:** no name outbound — `paged` is the participant's CHANNEL name, and the binary never reads a better-auth table.
- **(o) D615 — NEW: a step whose target does not resolve is delivered to the rule's own channel as `own` with a reason kind, and the walk continues.** **Alternative:** a `failed` row with the reason and NO delivery — the page is dropped at that step and the walk continues.

**Next gate:** the ruling on (a) first — it decides whether (c), (h), (i), (k), (o) exist at all — then the rest; then the S7.5 breakdown (team plan) on the RULED packet. No DDL, walker or deliverer task dispatches before the packet is ruled.
