# S3.4 Exit Bundle — onboarding to first trace + the functional Connections hub

meta:
- date: 2026-08-20
- branch: `s3.4-onboarding-connections`, tip `820ca7d` (b249396..820ca7d, 15 commits)
- governing record: M3 phase plan §3 S3.C4 + §9 D94–D110; S3.3 exit bundle §6 inheritance list discharged in full (verified below); sprint record + rulings **D201–D222** at `.planning/2026-08-20-s3.4-team-plan.md`
- run shape: 12 task-slots approved (T1, T2, T3, T4, T5, T6, T7 + fix slots T4-F1, T2-F1, T3-F1, T4-F2, T3-F2, T6-F1 — five of them advisor-ordered ruled fixes), integrator **GO round 1, zero seam fixes**; iterations 0/3
- verification at tip: `apps/web` 353 tests / 0 fail / 37 skipped (no-DSN), `tsc --noEmit` clean; fresh-stack `smoke.sh` PASS; **e2e drive 151 checks / 0 failed** (executor 115s, reviewer 122s, integrator 132s local)

## 1. Exit criterion, clause by clause (all on drive or code evidence)

1. **The quickstart renders the workspace's real key at the frozen D78 API with the held names** — `Quickstart.tsx` reworked (T2 @ 5f03aa3): all four D101 defects closed (`obstack-py`/`obstack-js` install lines verbatim from the SDK READMEs with the single D202 caveat; zero-arg `init()`; `Authorization=Bearer%20…` URL-encoded; no `x-obstack-key`, no `ingest.obstack.dev`, no `api_key=`, no fake timer live); the D201 inline issue action returns the shown-once token into client state only; endpoints from the one `lib/ingest-endpoint.ts` definition (D215). Byte-level quickstart-vs-D78 review (critical item 1) PASSED clean. The drive issues a key through the real DOM and reads the token off the rendered snippet.
2. **The waiting panel polls real ingest and links the actual first trace** — `getOnboardingStatus` (D203/D209: arrival from the D100 health read, link via the scoped facade) served identically by first render and the GET poll route; the drive proves the flip within the 11.5s ceiling to the exact sent trace id (amendment 4 — id equality, not "a link appeared").
3. **The ruled connector set opens real guided flows** — OTLP/Docker over the compose artifacts with the `<OBSTACK_API_KEY>` slot; Kubernetes over the in-repo chart with the D214 boundary stated in step 1 and no token slot (a product key would be 401'd by the chart's own ingest); Vercel + CloudWatch "Coming soon" in both modes (D208). Every step command names an artifact that exists (pinned by test, fs-checked).
4. **Connected sources show last event / counts / errors from real counters** — the hub reads `getIngestHealth` only (the D100 rows; one-counter-path grep verified repo-wide by the integrator): per-key last event, cumulative accepted, quota as "sampled", errors = receive-path drops (D219), staleness as "as of <instant>". **"Rate" is delivered as cumulative counts + "as of" — the D218 SIGNED exit-wording deviation** (a /min figure would be invented from windowless counters; a true windowed rate is a pre-registered M4 item on the D100 mechanism; the drive asserts the absence of any rate figure).
5. **Both surfaces registered live-wired with real empty states** — `/app/onboarding` + `/app/connections` exact entries (critical item 3 signed on the T5 review's diff walk: `/app/costs` and every other route byte-untouched, D205/D106 held); content-aware empty states (D142); no SAMPLE badge on either route (drive-asserted, with `/app/costs` as the standing positive control).

## 2. The D115 discharge (S3.3 inheritance item 2)

The drive now sends on a UI-issued key and proves ATTRIBUTION content-aware: alice=3 spans / bob=0 / store=3, plus product-search both ways (alice sees the sent trace id; bob "0 of 0 traces"). OTLP-only per D214. The `e2e-drive.mjs:96-100` "deliberately not taken" header is discharged; the drive is again the one attribution evidence of record.

## 3. S3.3 inheritance list — discharged in full

1 (headroom) → measured, see §5. 2 (D115) → §2. 3 (quickstart truth, four defects) → §1.1. 4 (D100 one path) → §1.4. 5 (reconcile↔D132 coupling) → RESOLVED structurally per D206: mirrored `isError` regex pinned in `reconcile-wording.test.ts` (red-proven four ways by the T6 reviewer incl. quoted-string refusals), naming comments in all three files (T6-F1). 6–8 (patterns, one-definition vectors, plan catalog) → held; no new auth-surface action escaped the D150/D152 guard + standing test (T1's action covered). 9 (M4/S5 carry) → unchanged, grown below.

## 4. Honesty amendments of record (the mock freeze yielded three ruled times + one named implicit)

D208 (Vercel/CloudWatch cards SOON in both modes), D220 (the email promise deleted; honest local acknowledgment), D222 (the Bell icon swapped to Plus — a bell is the notification promise in pictogram form), and the **named implicit instance**: mock-mode quickstart now renders the truthful D78 snippets rather than the old fiction — same ground (demo *data* protected, false *product claims* never), required by the shared component. All other mock surfaces byte-unchanged (integrator-verified by hand-driving mock mode).

## 5. Budget (critical item 2 — OPEN pending the real runner)

Local drive: 115s (executor) / 122s (reviewer) / **132s (integrator)** — recorded as a range. Growth +19 checks (132→151), ~+7–13s local, mostly the flip's bounded flush+poll wait. At the S3.3 local→runner ratio (~3.07x on 109s→5m35s) the projection spans **~5m53s–6m45s (115–132s × ~3.07x), straddling the line with the top well over; the D207 contingency is therefore LIKELY to fire, and is pre-authorized** (advisor correction at signature — the projection must include its own worst point; S2.2 L2 rider: warm-local numbers, the ratio is the S3.3-measured bridge). Item-2 discharge rule of record: green e2e ≤6m auto-discharges with the number recorded in a §5 addendum; green >6m executes the D207 reclaim on the branch and merge waits for a green ≤6m run (**reclaim-then-merge** — a known-over-budget drive is a deficiency deferred, forbidden); a reclaim that fails to clear the line returns to the advisor; red = normal fix protocol.

## 6. Token hygiene (W4 amendment 2 — named done-check)

The UI-issued token appears nowhere in drive stdout/stderr, the serialized transcript, or any OUT_DIR artifact (9 outputs swept; Chrome profile dirs excluded with the stated ground); red-proven by injected leak (150/1 FAIL). The D132 log split now also protects a credential.

## 7. Residue pre-registered, not absorbed

- **S3.5**: mock-corpus fictions (`mock/docs.ts:85` `charts.obstack.dev` helm fiction; `mock/mcp.ts` `mcp.obstack.dev`; `ok_live_9f2e…` in `mock/workspace.ts`/`security.ts`; the demo hub's "Vercel · 86/min" fabricated source now sitting beside a SOON Vercel card — the D208 demo-data-vs-claim call); `WaitlistForm.tsx:27` "We'll email you" (U4 class, marketing); TourGuide aspirational copy; switcher/demo-footer (D134); palette/changelog (D60/D63); landing sentence + pricing copy (D106 fence); TopBar residue.
- **M4**: windowed-rate counter schema on the D100 mechanism (D218); `ai@7` `diagnostics_channel` integration (named-owner obligation, D101); K8s external-cluster connect = the self-hosted network-reachable ingest surface (D214); Vercel + CloudWatch receiver components (D101; activation S5); mock-mode standing test hand-enumeration (QA carry G1); prior inventory unchanged (D178/D187, D180, D153/D119/D123/D132).
- **S5-GATE**: unchanged from S3.3 (production Polar flip + D177 inventory + D200 webhook-reachability consequence + D104 re-verification).

## 8. Signature

> **Advisor signature — S3.4 exit bundle.** Signed on the record above with the §5 projection corrected to ~5m53s–6m45s (115–132s × ~3.07x). All five exit clauses evidence-walked; D218 stands as the one signed exit-wording deviation; the §4 third yield of the mock freeze is ratified on D208's ground; critical items 1 and 3 discharged on the cited reviews; critical item 2 discharges per the runner rule of record: green e2e ≤6m auto-discharges with the number recorded in the §5 addendum; green >6m executes the D207 reclaim on the branch and merge waits for a green ≤6m run (reclaim-then-merge); a reclaim that fails to clear the line returns to the advisor. **Effective at**: branch tip `820ca7d` plus the planning commit recording rulings D201–D222 and this bundle, upon all eight CI checks green on the PR to master under the item-2 rule. Rulings of record: D201–D222. — advisor, 2026-08-20
