# Team Plan — obstack M2 sprint 1 — `obstack-collector` distro and nearby log correlation on Kubernetes (exit: the demo app on a kind cluster in CI renders API→agent→LLM spans with solid and nearby pod logs in one view)

meta:
- date: 2026-08-16
- **CLOSED (2026-08-16): exit bundle advisor-signed at 02bd695; PR #11 merged @ `fd3ab6a` (four checks green at the signed tip: go 56s / kind 1m34s / web 2m25s / stack 4m17s).**
- sprint: **S2.2** (M2 dev sprint 1 — was "B1" in the phase plan; renamed by advisor D28)
- repo/branch: `master` @ `bdd1e57` (post S2.0 + S2.1: app at `apps/web` under npm workspaces, `web`/`go`/`kind` checks on every PR; branch protection deliberately skipped by user decision — checks advisory, merges process-gated)
- mode: workflow → hand-dispatch (S2.0 precedent)
- model override (user-directed 2026-08-16, mid-run): T4's (c)-rework executor and T5's executor run on **Fable 5** (prior T4 executor stopped mid-rework at the user's direction; elapsed time was dominated by cluster wall-clock, noted for the record). All other roles unchanged.
- team-cap: 5
- iteration-limit: 3
- iteration: 0/3
- phase plan: `.planning/2026-08-16-s2-m2-phase-plan.md` (§3 S2.B1 = this sprint; §8 = binding rulings D28–D37)
- dispatch order: after S2.1 (merged @ `8e21ef3`). S2.3 (search + LogsExplorer) follows this sprint and re-enters `apps/web/src/server/queries/` — this sprint **adds a second logs query, it does not restructure `traces.ts`** (D28).
- open user actions inherited (not this sprint's work, but they shape evidence): branch protection still platform-blocked (checks run, don't block); local `.env.local` must live at `apps/web/.env.local`.

## Advisor decisions
<!-- Binding for all agents. Full text: .planning/2026-08-16-s2-m2-phase-plan.md §8 -->
- **D35 (K8s artifact)**: kind runs the obstack stack through **the real Helm chart M4 will extend in place** — throwaway manifests are refused categorically. M2 chart scope: **CI-grade ClickHouse; ingest Deployment with `OBSTACK_MIGRATE_ON_BOOT=false` plus a migrate `Job` as a `pre-install`/`pre-upgrade` hook (PR #4's design, first real execution); `obstack-collector` DaemonSet; the demo app.** **NOT in it:** the `web` image (M4 — CI asserts via the D17 tsx facade harness against the cluster's ClickHouse), TTL tiers, docs, self-hosted values. **Helm is the ONLY kind path.** Collector image pinned to an exact upstream version (D14 precedent). **M4-GATE: M4 extends this chart in place — a second chart is drift.**
- **D37 (dup-logs mechanism, binding; clause 1 AMENDED by D43)**: (1) app OTLP routes **through the collector** — the collector buys the bearer-key hop, the NEARBY filelog source, the D37.3 exclusion, and node enrichment; **the SOLID rows' pod-metadata load-bearing source is the app's resource self-identification (Downward-API stamping, the recommended customer pattern), on which k8sattributes associates (resource_attribute first) and enriches node identity** — the original "k8sattributes enriches spans and logs so SOLID rows carry real pod metadata" was measured false (k8sattributes-disabled → bundle still green; the finding that forced D43); (2) NEARBY evidence gets a genuine source — **a second, uninstrumented container in the demo pod** whose stdout the collector tails; (3) **no line arrives twice** — the collector's filelog receiver excludes containers that ship via OTLP, the app keeps writing stdout so `kubectl logs` stays truthful, and the exclusion is documented as the recommended customer pattern; (4) **nearby join key = `(workspace_id, k8s_namespace, k8s_pod)` + a time window**, container is display data, and the window is **one shared constant surfaced to the UI** — `TraceExplorer`'s hardcoded "±10s" becomes data; (5) query-layer dedupe **REJECTED** (it lies about the data). **Exit evidence: ≥1 SOLID row with pod metadata, ≥1 NEARBY row from the uninstrumented container, zero duplicated bodies.**
- **Carry-forward 3 (T9 escalation, M2-GATE)**: ingest gains GenAI **event-form** prompt/completion extraction (`gen_ai.input.messages` / `gen_ai.output.messages`) into the **existing D7 `prompt`/`completion` columns** — additive, `internal/mapping` only. The span-attribute form (D8) stays the obstack-SDK convention and must keep working unchanged. **See kickoff question Q1: the ruling is implementable as written only for one of the two wire forms.**
- **D32 (infra layer)**: **no infra-layer producer in M2.** `infra` stays a reserved enum value, `classify()` gains no branch, collector self-telemetry classifies under the existing D8 rules. The former "collector-originated spans populate the infra layer" sentence is struck. M5-GATE.
- **D13 / D21 (honesty, binding on the wired trace-detail surface)**: `/app/traces/` is a live-wired route with no SAMPLE badge. Nearby logs are real rows or no rows — never invented, never inferred, and the rail's counter must never claim a number it did not render. In live mode with no nearby logs the rail simply shows the solid ones.
- **D28 (seam)**: search stays **exactly as M1 shipped it** — the 200-cap list and summary-level free text are S2.3's to fix, not this sprint's.
- **K3 (S2.0, still binding)**: workflow job/check names are stable identifiers — `web`, `go`, `kind` may **never** be renamed (a rename silently voids required-checks). Any new job introduced here takes a **new** name.
- **K5 (S2.0, revisited here)**: the ≤ ~6-minute rule decides trigger policy per job. Under it → every PR + joins the required set. Over it → label (`ci:kind`-style) + push-to-`master` + `workflow_dispatch`, and the job is **excluded** from required checks; a required-but-conditional check wedges PRs that never trigger it.
- **K1 (S2.0, precedent that binds T4 here)**: one definition of the D11 ClickHouse users — a second, drifting copy of a D11-binding artifact was refused for CI and is refused again for the chart. See T4.

### Kickoff gate rulings (advisor, 2026-08-16 — BINDING; verdict: wave 1 dispatches on the user's go)
- **D38 FINAL (Q1 — GenAI wire form; user override folded in, supersedes both earlier D38 drafts):** the **log-record form is THE ruled form** — upstream's Events API emits log records, so it is the form M2 meets in the wild; the user's override makes the schema implementable now. **Span-EVENT extraction is NOT implemented** (no known producer emits that form — speculation; returns only on evidence). Supported wire forms are exactly two: **span-attribute (D8, obstack-SDK convention)** and **log-record (upstream convention)**.
  - **(a) Schema (CORRECTED by D42 — advisor verified the code: `span_id` already exists on `obstack.logs` since M1, mapped at `logs.go:61`, inserted at `write.go:48`):** the delta is **exactly two columns** — `prompt String CODEC(ZSTD(3))`, `completion String CODEC(ZSTD(3))` (codec parity with spans, placed after `service`). NO token/model columns (usage stays span-attribute-sourced; cost computation unchanged). Fill rule: a log record carrying `gen_ai.input.messages`/`gen_ai.output.messages` maps input→prompt, output→completion, attribute string form verbatim (no re-encoding, no truncation); extracted keys excluded from the Map (D8-AMENDMENT extends to logs); everything else on the row maps as any log record. Content lands logs-side; query layer coalesces at read time. No writer-side join, no mutation, no ReplacingMergeTree (D23 stands). Fixtures pinned to the semconv revision they encode.
  - **(b) Migration:** edit `services/ingest/migrations/0002_logs.sql` **in place** — D22/F5 precedent (filename-tracked, pre-release, `down -v`; README line exists). No 0004, no ALTER debris. Fresh-boot cycle + second-boot no-op + PR #4's read-only verification are T2 done-check items.
  - **(c) Task structure:** **T2 re-scoped to the ingest side** (0002 edit + `internal/mapping` log-record extraction: a log record carrying `gen_ai.input.messages`/`gen_ai.output.messages` fills the row's span_id/prompt/completion; source attributes excluded from the Map — D8-AMENDMENT extends to the logs table; fixtures/tests; `mapping.go` doc-contract table gains the log-record form). **T6 (NEW, wave 2, own reviewer):** read-time coalesce — re-enters `queries/traces.ts` + `adapters.ts` **after T3 closes** (cross-wave re-entry, named): trace detail hydrates the LLM span's `LlmDetail` from event-derived rows when the span's own columns are empty; **content-carrier rows do NOT render in the LogsRail** (structured content, not narrative logs; a non-empty `body` still renders normally); orphan content rows fold nowhere and render nowhere.
  - **(d) Precedence per field (`prompt`, `completion`) independently:** non-empty span column wins; else event-derived; among multiple content rows for one span_id, **earliest timestamp** wins — deterministic. Content never enters cost/token computation (span-attribute-sourced, unchanged) — no double-count by construction. Both-forms test lives in T6 (read-time semantics).
  - **(e) T5 rider:** one collector-routed **event-form fixture** assertion joins T5's done-check (bring-your-own-OTel end-to-end) — a done-check line, not a fourth evidence pillar.
- **D39 rider (bearer key):** the collector config takes the bearer key via env; dev default `ok_dev_local` acceptable per M1 precedent; README states production supplies it via values/secret, never a committed literal.
- **D39 (Q2 — compose topology): (a).** Collector rides a compose **profile** (permanent structure, `demo`-profile precedent — not a stopgap); the default `demo → ingest` path and M1's signed `smoke.sh` stay byte-untouched. T1's direct-vs-routed row-equality check needs both paths anyway. **M4-GATE pre-registered:** the self-hosted bundle's default topology (direct vs collector-routed) is decided at M4 assembly.
- **D40 (Q3 — chart): location `deploy/helm/obstack/` CONFIRMED. Users config = verbatim copy pinned by a byte-equality contract test** in the existing Go suite (PR #3 cross-tree style, runs in the `go` check every PR) against `deploy/compose/clickhouse/users.d/obstack-users.xml`. Helm `.Files` cannot reach outside the chart; generation is a copy with extra machinery. The XML's `from_env` password mechanism works identically under K8s env. If verbatim genuinely cannot hold → escalation, not a semantic fork.
- **D41 (K5 revisit): the ~6-minute line is UNCHANGED** (it prices contributor latency, not job complexity). New check name = **`stack`** (K3: names are forever; full set `web`/`go`/`kind`/`stack`). Rider on the over-the-line branch: if `stack` is label/dispatch-triggered, it ALSO always runs on push to `master`, and a red `stack` on master is stop-the-line for the next PR.
- **Decision 3 (T3): window value = 10s** — matches shipped UI copy; it is data now, changing it later is a one-line edit.
- **Decision 4 (T3): window semantics = `[trace min_start − W, trace max_end + W]`**, pods drawn from the trace's spans' `k8s_pod` set, records restricted to `trace_id = ''`.
- **Decision 5 (T3): nearby cap = 200**, named constant in the same shared module (consistent with `DEFAULT_TRACE_LIMIT`); at the cap the rail renders an explicit truncation marker — the counter never claims rows it did not render (D13/D21).
- **T5 rider:** any refactor extracting shared assertion logic out of `deploy/compose/smoke.ts` is followed by a full compose `smoke.sh` green from clean volumes, recorded in T5's done-check — signed evidence is re-proven by running, not inspection.
- **Critical-item addition: T2 returns to the advisor before merge** — the D38 precedence table + Map-exclusion extension are the interop contract S2.4's SDKs and every foreign OTLP sender inherit (same class as S1's T4 contract review).

## Retro lessons carried in (binding on done-checks)
- **S2.0 L1 — guards must be proven hollow-proof, not green.** GOCACHE replayed 7/8 packages and a fail-open hold observed nothing; both read as green. **Every assertion in this sprint that claims an absence or a correlation carries a falsification probe** — the check must be shown going red when the thing it guards is removed.
- **S2.1 L1 — lockfile regeneration is a dependency-bump vector.** If any npm dependency is genuinely required, use a seeded `--package-lock-only` conversion and diff the version lines; an incidental float is a fence breach.
- **S2.1 L2 — cross-language repo-root literals live outside the tree you are editing.** The collector config, the chart and the CI job will all carry paths, service names, ports and user names that also exist in Go, SQL, TS and compose. Grep **all** trees before claiming a change is contained.
- **S2.1 L3 — CI runs the exact commands the runbook tells humans to run.** The stack-on-kind job invokes the same script a human runs locally; no bespoke inline sequence that only CI exercises.
- **S1 L1 — a seam nobody owns is a seam that does not get built.** T4 consumes T1's collector config; that hand-off is named in both tasks.

## Critical items
<!-- Must return to the advisor before merge (pre-registered in phase plan §8). -->
- **T1 — collector configuration.** It is the interop surface every customer copies; the filelog exclusion is a correctness mechanism, not a tuning knob.
- **T3 — nearby-join SQL + the shared window constant.** The join key is D37-fixed; the query pattern and its bound are what S2.3 and M4 inherit.
- **T4 — chart structure.** M4 extends this chart **in place** (D35), so its layout, values shape and hook wiring are near-irreversible.
- **T5 — D37 exit evidence.** The advisor signs the three-part evidence (SOLID with pod metadata, NEARBY from the uninstrumented container, zero duplicated bodies) plus the K5 measurement.
- **T2 — D38 mapping contract** (added at kickoff gate): precedence table + Map-exclusion extension return to the advisor before merge.
- Any deviation from **D32** (no infra producer), **D35** (no web image / TTL tiers / second chart), or **D13/D21** (no invented or unlabelled log rows) **escalates immediately**.

## Tasks
<!-- One block per task. Exclusive file ownership holds WITHIN a wave. -->

### T1: `obstack-collector` distro — config-only, container form
- status: CLOSED (2026-08-16) — REVIEWED: fixed ×2 + advisor contract APPROVED. Image otel/opentelemetry-collector-k8s:0.158.0 (components-verified). Reviewer catches: CRI-format auto-detect (pinned `docker` format silently emptied every k8s.* attribute on kind — the D37 join key), exclusion env-mechanism (closed the compose/K8s config fork), file_storage checkpointing (advisor-ruled after escalation; restart probe 5→5 with / 5→10→15 without; probe's second collector needed an isolated storage dir — bbolt lock). Config shared-bytes across both forms verified. Final T1 tip: 7990bf5.
- wave: 1
- depends: —
- owns: `deploy/collector/` (the distro: collector config + its README documenting the recommended customer pattern), `deploy/compose/docker-compose.yml` (collector service block), `deploy/compose/README.md` (collector section)
- goal: `obstack-collector` exists as a **configuration over an unmodified upstream collector image, pinned to an exact version** (D14 precedent; the config is obstack's, the binary is upstream's — PRD §5 "distribution (config, not fork)"). It receives OTLP (traces + logs) from applications and forwards them to ingest with the workspace's bearer key, tails container stdout with the filelog receiver, attaches pod/container metadata with `k8sattributes`, and **excludes from filelog any container that already ships via OTLP** so no line is ingested twice (D37.3). The container form runs under compose; the DaemonSet form is T4's packaging of this same config.
- binding detail: the upstream image must be the distribution that actually contains `filelog` and `k8sattributes` (the core distro does not) — the executor names the exact image and tag and says how it verified the components are present, rather than assuming.
- binding detail: **the compose default path is question Q1/Q2 territory** — until the kickoff gate rules Q2, implement the collector as a compose **profile** that leaves the existing `demo → ingest` path (and therefore M1's signed `smoke.sh`) byte-untouched.
- done-check: `docker compose --profile <collector-profile> up` brings the collector healthy against the existing stack; an OTLP payload sent to the collector arrives in ClickHouse through ingest with the same rows it would have produced direct (proven by comparing a direct send and a collector-routed send of the same fixture); filelog tails a container's stdout and those lines land as `logs` rows with **empty `trace_id`** and populated container metadata; **falsification probe (S2.0 L1)** — with the exclusion rule removed, the same line demonstrably lands twice, and with it in place exactly once; the README states the exclusion as the recommended customer pattern in terms a customer could copy.

### T2: GenAI log-record prompt/completion extraction + the D38 schema (carry-forward 3, RE-SCOPED per D38 FINAL)
- status: CLOSED (2026-08-16) — REVIEWED: fixed + advisor contract APPROVED (canonical-JSON two-case statement ratified; empty/absent collapse ratified; one-PR construction suffices for the T6 rail-honesty dependency). Probes 1–2 red-then-green. Commits d7349e3 → 1d478f2 → feeb5b5 → 5e94a49.
- wave: 1
- depends: —
- owns (per D42(c)): `services/ingest/migrations/0002_logs.sql` (in-place two-column edit), `services/ingest/internal/mapping/` (extraction + fixtures + `mapping_test.go` units + the `mapping.go` doc-contract table), `services/ingest/internal/write/write.go` (**`insertLogs` column list + `appendLogs` args ONLY** — the LogRow struct lives in mapping, which you own; anything non-mechanical in write/ escalates), `services/ingest/internal/write/integration_test.go` (ClickHouse-backed assertions)
- goal: per D38 FINAL + D42 — `obstack.logs` gains `prompt`/`completion` per D42(a) (`span_id` ALREADY EXISTS since M1 — do not re-add); a log record carrying `gen_ai.input.messages`/`gen_ai.output.messages` fills those columns on its own row verbatim; source attributes excluded from the Map (D8-AMENDMENT extended to logs). Span-EVENT extraction is NOT implemented. The D8 span-attribute form keeps working exactly as today. The read-time coalesce is T6's — no touch of `apps/web/**`.
- done-check (per D42(e) probes 1–2): fresh-boot migration cycle green (second boot no-op; PR #4 verify-only path reads applied); mapping units from fixtures: log-record form fills prompt/completion; span-attribute form unchanged (regression); a content row with empty body lands with `''` body honestly; DB-backed integration: row lands with columns filled AND Map exclusion asserted at the DB; **probe 1** — exclusion removed → `attributes['gen_ai.input.messages']` appears in the landed row, test red; **probe 2** — extraction disabled → columns empty for the same fixture, red. Full `go test ./...` + vet + gofmt green against real ClickHouse from clean volumes (`down -v` first — the 0002 edit makes stale volumes wrong by design). `mapping.go` doc table records the log-record form + D42(d) precedence as the contract of record, fixtures pinned to their semconv revision.

### T3: Nearby log join in the query layer + the shared window constant
- status: CLOSED (2026-08-16) — REVIEWED: fixed + E1 (assigned to T5: web.yml ClickHouse + skip trap), E2 (proximity selection, chronological display), E3 (cap+1 known-truncation) all ruled and landed (d9dc3ec, 0d64629, b25a64d); contract APPROVED with E2+E3 probes verified red-then-green. Commits b9691ec → 1f73c0c → d9dc3ec → 0d64629 → b25a64d.
- wave: 1
- depends: —
- owns: `apps/web/src/server/queries/traces.ts` (one added query — **not** a restructure, per D28), `apps/web/src/server/adapters.ts`, the new shared window/join constant module under `apps/web/src/lib/`, `apps/web/src/components/trace/TraceExplorer.tsx` (the counter line only), and the tests beside them
- goal: the trace-detail view gains its **nearby** logs. Solid logs stay exactly what they are today (a `trace_id` range read). Nearby logs are a **second** query on the D37-fixed key — `(workspace_id, k8s_namespace, k8s_pod)` for the pods the trace's spans actually ran on, within one shared time window around the trace, restricted to records with no trace context — adapted into the existing `LogRecord` shape with `traceId` **undefined**, which is already what `LogsRail` renders as `NEARBY` (`apps/web/src/components/trace/LogsRail.tsx:60-66`). The window is **one constant** consumed by both the server query and the UI: `TraceExplorer.tsx:212`'s hardcoded "±10s" stops being prose and reads the constant (D37.4).
- grounded context (why this is a second query, not a rewrite): `obstack.logs` is `ORDER BY (workspace_id, trace_id, timestamp)` and trace-less logs carry `trace_id = ''`, so nearby candidates are a **contiguous prefix range already ordered by time**, with `INDEX idx_pod k8s_pod TYPE bloom_filter` available for pod filtering (`services/ingest/migrations/0002_logs.sql:22,26`). The M1 detail path reads logs by `trace_id` only (`apps/web/src/server/queries/traces.ts:98-110`). **No schema change is expected.** If the executor concludes one is needed, STOP and escalate — a `logs` migration is a pre-release `down -v` decision (D22/F5 precedent), not an execution detail.
- constant placement: client-safe pure data outside `src/server/` — the D21-AMENDMENT precedent (`apps/web/src/lib/live-routes.ts`: pure data + pure predicate, imported directly by server and client, no re-export indirection). Same shape here.
- binding detail (honesty, D13/D21): the nearby result carries a **bound** — the executor names it and states what the UI does when it is hit. The rail's counter must never report a number larger than the rows it rendered. Parameterized SQL only (D11); no string interpolation.
- done-check: unit tests on the adapter (nearby rows produce `traceId: undefined`, correct signed `atMs` offsets including negative ones for logs before the trace start); against a seeded ClickHouse, a trace whose pods emitted trace-less logs inside the window resolves with those rows present and rows outside the window absent; **falsification probes (S2.0 L1)** — a log from a different pod, a log outside the window, and a log carrying a `trace_id` each stay out of the nearby set, each proven by a failing-then-passing assertion; `npm test` green from the repo root; `npm run build` clean; mock mode pixel-identical (the M1 F6/F7 standard); no SQL string interpolation; the ±10s prose is gone and the constant is the single source.

### T4: Helm chart — the real one, scoped (D35)
- status: CLOSED (2026-08-16) — REVIEWED ×2 + advisor contract APPROVED (ruling (c), then E-coldstart (b)) at final tip **e713c12**. Chain: 5da4846 → 9e2ebbb (review fixes) → 8bee59d (ruling (c) rework, Fable 5) → 0655d01 (re-verify fixes: hostPath honesty, hook-orphan cleanup, timeout budget, lookup dry-run) → cc52669 (E-coldstart delta: shared `obstack.waitForClickhouse` define, both includes) → e713c12 (delta re-verify fixes: values.yaml call-site comment, `--no-hooks` escape must state the fixing value — Helm v4 reuses release values, measured). Delta re-verify reproduced every claim independently with margin: rendered init containers byte-identical in both pod specs; template diff shows ONLY the ingest initContainers addition; cold sim held `Init:0/1` zero restarts 10m00s; inverted control red at 2.6s gap with init container present; restored ordering green 0 restarts; README 900s budget honest with no stale 1500s references. Record clarification: a warm install typically shows ONE refusal restart per ingest pod (init releases ~0.2s before the migration lands) — the schema-state invariant firing, exactly as the amended text describes. All probes' proof of record per the amended ruling (c). History: first pass escalated chart structure → advisor ruled (c); first rework executor stopped at user direction (run hang correctly diagnosed as emptyDir schema loss across ClickHouse restarts); Fable 5 rework executor landed ruling (c) at 8bee59d; independent re-verify re-ran the full probe set green (hook Complete 14:57:36 before first new pod's 0-restart `schema verified` same second; inverted control red — pod refused 4s before Job completion; bonus control: failed hook gates absolutely, zero new pods in 15m; rotation ×2 + rotation-with-migration all on the live password; image reconcile with schema surviving; D40/K1 byte-equality green). Both flagged subtleties resolved: `lookup` helper sound on every path (only gap was docs — dry-run divergence + missing-Deployment path now documented); hostPath limitations real and now stated (uninstall residue + same-name reinstall adopts predecessor DB — measured; single-node only; one release per cluster; M4's StatefulSet+PVC inherits the three costs written down). Fix commit 0655d01 = docs/comments only, no template behavior change; lint + contract test + fresh warm install (17s) + clean uninstall re-proven on top. Record corrections: "11m30s cold install" is machine-specific, not a bound (reviewer measured 15m45s and Helm marked the release failed at the 900s timeout while the stack converged healthy a minute later); "uninstall leaves zero" holds only when all upgrades succeeded (failed-hook Jobs survive uninstall — README now documents the labeled cleanup).
- wave: 2
- depends: T1 (packages T1's collector config as the DaemonSet form)
- owns: `deploy/helm/obstack/` (chart + templates + values + chart README)
- goal: one Helm chart installs the obstack stack on kind and is **the chart M4 extends in place** — CI-grade ClickHouse with the two D11 users; the ingest Deployment with `OBSTACK_MIGRATE_ON_BOOT=false`; a migrate `Job` as a `pre-install`/`pre-upgrade` hook running `/ingest migrate` (PR #4's design — **this is its first real execution**, `deploy/compose/README.md:147-186` is the design of record); the `obstack-collector` DaemonSet running T1's config; and the demo app pod carrying **a second, uninstrumented container whose stdout is the genuine NEARBY source** (D37.2), with the app's OTLP exporter pointed at the collector (D37.1). Explicitly **not** in the chart: the `web` image, TTL tiers, docs content, self-hosted values (D35).
- binding detail (K1, one definition): the D11 ClickHouse users are defined once. If the chart cannot reference `deploy/compose/clickhouse/users.d/obstack-users.xml` directly, the copy is pinned by a **contract test** in the established style (PR #3's cross-language vocabulary tests; S2.1 L2) so drift fails a check rather than a customer. A silent second copy is refused.
- binding detail: every image pinned to an exact tag — ClickHouse `26.3.17.110` as compose pins it, the collector as T1 pinned it, the sidecar image likewise. The demo app image is built locally and loaded into kind (S2.0's `imagePullPolicy: Never` mechanism is the precedent that keeps a registry out of the path).
- done-check: `helm install` on a fresh kind cluster brings every component Ready; the migrate Job runs **before** ingest pods and applies the schema; ingest pods log the read-only "schema verified" path rather than migrating; `helm upgrade` re-runs the hook as a no-op; **falsification probe** — an ingest pod started against an un-migrated ClickHouse fails with the documented `OBSTACK_MIGRATE_ON_BOOT is false` error instead of quietly serving a half-migrated schema; `helm lint` clean; the chart README states the scope boundary (what M4 adds later) so the next sprint does not re-decide it; no `web` Deployment, no TTL values, no second chart anywhere in the tree.

### T5: Stack-on-kind in CI + the D37 exit evidence
- status: CLOSED (2026-08-16) — REVIEWED: APPROVE-WITH-FIXES at 89f8eeb, then D43's F1 landed and approved on top; final tip **4bf0774**. All four checks green on PR #11 (draft, unmerged). Commits: b1762e8 (trace-checks extraction + acceptance.sh/.ts beside the chart + `stack` workflow + web.yml E1 + README), 84c95b2 (web job also starts compose `ingest` — schema owner, first enforced run proved `up clickhouse` alone = UNKNOWN_DATABASE; K5 recorded in stack.yml). D37 evidence green on CI run 31960145937 (SOLID 3 rows with pod metadata, NEARBY 3 sidecar rows, zero dups across 6); probes red-then-green on local kind incl. restart rider (checkpoint wiped + restart → 4 heartbeats doubled → red) and D38(e) event-form fixture. K5: 4m06s/4m14s measured → **under ~6 min → every PR, joins the required set**, number includes pre-pull (stated in stack.yml). E1 proof: run 31960145875, 45/45 pass 0 skipped, skip trap red-then-green locally. smoke.sh re-run green from clean volumes after the extraction. **Two recorded deviations awaiting advisor: (a) D37 probe 1's literal knob (k8sattributes disabled) measures GREEN — the demo's Downward-API `OTEL_RESOURCE_ATTRIBUTES` stamping (T4's ratified hairpin workaround) is the load-bearing pod-identity source; executor shipped the mechanism-accurate red (stamping strip). (b) E1's wording said `up clickhouse`; schema-owner `ingest` added with measured justification.**
- REVIEWED: APPROVE-WITH-FIXES at **89f8eeb** (2026-08-16). Reviewer re-ran the done-check ×4 on its own cluster and reproduced every load-bearing claim (all probe reds verbatim; CI runs and PR #11 check set confirmed; smoke.ts extraction proven byte-equivalent by RUNNING both versions on the same live trace, success and failure paths; clean-volume smoke.sh PASS 20.8s; fence sweep clean; FIXTURE_ROW_SQL parameterized). Fixes: acceptance.ts dedupe key used literal NUL bytes making git treat the file as binary — silently defeating S2.1 L2 literal sweeps (now ` ` escapes, re-proven red-then-green); acceptance.sh bare `helm upgrade` inherited Helm v4 reused values from probe installs (measured: `disabled-for-probe` persisted to rev 4) — now `--reset-values` with in-line reason. Claim corrections: "only span k8s_node empties" FALSE — k8s_node is empty even with k8sattributes enabled; "61s install" machine-specific (reviewer: 104s). **Deviation (a) sharpened by measurement into ESCALATION E-k8sattr (below).**
- **E-k8sattr ESCALATION (advisor, from T5 review):** `k8sattributes` is INERT on the app-OTLP path as shipped — `pod_association` rule 1 `from: connection` always short-circuits on the hairpin-SNAT'd node IP (matches no pod), rule 2 never evaluates; proof: rule 1 removed → every app span gains `k8s_node = t5rev-control-plane`. Pod-identity map: SOLID rows = 100% demo Downward-API stamping; NEARBY rows = 100% filelog `container` operator (stamping cannot reach them; k8sattributes could only add k8s.node.name, which logs schema doesn't store). Net: k8sattributes contributes ZERO observable data to the D37 bundle. Shipped P1 probe is a genuine mechanism-accurate red (check not hollow) but the bundle proves "SOLID rows' pod metadata has a real load-bearing source", not D37.1's "routing app OTLP through the collector buys real pod metadata" — on this topology the app must self-identify and would carry identical ns/pod exporting straight to ingest; what the collector demonstrably buys is the bearer-key hop, the filelog NEARBY path, and the D37.3 exclusion. Chart comments contradict each other (collector/daemonset.yaml:57-61 says hostPort exists SO connection-association works; demo/deployment.yaml:65-72 says it cannot resolve through the hairpin — measurement backs the demo comment). Cheapest remedy if advisor wants k8sattributes load-bearing (measured, NOT applied — T1/T4-owned): resource_attribute rule first or connection rule dropped in deploy/collector/config.yaml + chart byte-copy; restores enrichment but does not by itself make the literal probe red (stamping still supplies ns/pod independently). Question: does D37.1 mean "SOLID rows carry pod metadata" (satisfied) or "the collector is what puts it there" (not satisfied)? Probe text needs restating either way. Rider: ratify E1's measured amendment (compose `ingest` joins clickhouse in web.yml — schema owner; UNKNOWN_DATABASE proven on run 31959860812).
- E-k8sattr → **advisor ruled D43: BOTH — record restated per (i) AND pod_association reorder lands per (ii) as fix task F1** — each fixes a different defect: the restatement fixes a false sentence, the reorder fixes dead config in the customer-copied artifact (T1 critical item); "restate and keep the config" refused — a processor contributing zero observable data while its own comments (config.yaml:74-76) claim it is "the only source" is a shipped lie with blast radius (S2.4 SDKs + every foreign sender inherit it; a customer without stamping gets empty join keys and no error). **Mechanism: `resource_attribute` rule FIRST, `connection` KEPT second** (not dropped — self-identification authoritative when present; connection is a non-stamping sender's only chance and works wherever source IP survives; the hairpin SNAT is a property of the chart's own node-local hostIP+hostPort topology, not exclusively kind). Filelog rows unaffected by construction. **Record amendments: D37.1 evidence text restated** ("load-bearing source is the app's resource self-identification — the recommended customer pattern; k8sattributes associates on those attributes and enriches node identity; what the collector buys on this path is the bearer-key hop, the NEARBY filelog source, the D37.3 exclusion, and node enrichment"); **probe text restated to two mechanism-accurate reds** — (a) stamping stripped → SOLID metadata absent + NEARBY collapses (shipped P1), (b) association reverted connection-first → span `k8s_node` empties (new, F1); the "k8sattributes disabled → still green" measurement recorded as the finding that forced the restatement, not buried. E1 amendment RATIFIED (one schema owner = ingest; CI applying DDL itself would be a second migration runner, refused on "exactly one runner"). Reviewer fixes at 89f8eeb + K5 under-the-line call ratified without condition. **Signature gate: advisor signs the exit bundle when F1 is landed + re-probed (four probes) and the ledger carries the D37.1 restatement. Nothing else outstanding.**
- wave: 3
- depends: T1, T2, T3, T4
- owns: the new CI workflow/job for stack-on-kind under `.github/`, the K8s acceptance script + harness it invokes (placed beside the chart it exercises), the README section documenting how a human runs the same thing, **and (E1 ruling) `.github/workflows/web.yml`** — the `web` job gains ClickHouse via the K1 mechanism (post-checkout `docker compose up clickhouse --wait`, both web env vars exported for the readonly user) + fail-on-unexpected-skip mirroring `go.yml`'s D36 trap (node:test skip count asserted zero, empty allowlist), so T3's integration test executes on every PR; the `web` check NAME is untouched (K3)
- goal: CI stands the whole stack up on kind **through the chart** (D35 — Helm is the only kind path), drives one `POST /chat` against the demo app, and asserts the sprint's exit **through the D17 tsx facade harness against the cluster's ClickHouse** (D35 names this explicitly, because the `web` image is not in the chart): the four-layer waterfall resolves, and the correlated logs are **solid and nearby**, visually distinguishable in the data the UI renders. **This task also owns the K5 revisit**: measure the job's wall-clock and apply K5's rule — under ~6 min, every PR and joins the required set; over it, label + push-to-`master` + `workflow_dispatch` and excluded from required checks. The existing `kind` check name is immutable (K3); a new job takes a new name. The result line states the measurement, the branch taken, and how a contributor triggers it on demand.
- binding detail (S2.1 L3): CI invokes **the same script a human runs locally**. Assertion logic is **not** duplicated from `deploy/compose/smoke.ts` — the shared checks are reused, and the compose `smoke.sh` path keeps its M1 semantics byte-for-byte (it is signed exit evidence).
- **D37 evidence bundle (the sprint's exit, all three parts, each with a falsification probe):**
  1. **≥1 SOLID row with pod metadata** — a log carrying the trace's `trace_id` **and** populated `k8s_namespace`/`k8s_pod`. **(AMENDED by D43)** Load-bearing source: the app's resource self-identification (Downward-API stamping; the recommended customer pattern); the collector's k8sattributes associates on those attributes (resource_attribute first) and enriches node identity; what the collector buys on this path is the bearer-key hop, the NEARBY filelog source, the D37.3 exclusion, and node enrichment. Probes (two mechanism-accurate reds): (a) stamping stripped → SOLID metadata absent AND NEARBY collapses, red; (b) association reverted to connection-first → spans' `k8s_node` empties → the standing guard in acceptance.ts goes red. (The original probe text "with k8sattributes disabled, the metadata is absent" was measured FALSE — that knob leaves the bundle green; recorded as the finding that forced the D43 restatement, not buried.)
  2. **≥1 NEARBY row from the uninstrumented container** — a trace-less log from the sidecar, joined on `(workspace_id, namespace, pod)` + window and rendered distinct. Probe: remove the sidecar and the check goes red rather than silently passing on zero.
  3. **Zero duplicated bodies** — no log line present twice across the OTLP and filelog paths. Probe: remove the filelog exclusion and the duplicate appears and fails the check.
- done-check: the stack-on-kind job is green on a real PR with all three evidence parts and all three probes recorded; the four-layer assertion and the cost/token assertions still hold on the cluster run; job wall-clock measured and K5 applied with the reasoning stated; `web`, `go` and `kind` checks unchanged and still green; the README documents the local command and its prerequisites; a human running that command on a local kind cluster reproduces the result.

### T6: Read-time coalesce — event-derived LlmDetail (NEW per D38 FINAL, user override)
- status: CLOSED (2026-08-16) — REVIEWED: fixed (total-order fold determinism, per-field probe, required fill param at ed1ae21) under the same D8-contract review class as T2. Probes 3–4 red-then-green. Commits 3e0f7b1 → ed1ae21.
- wave: 2
- depends: T2 (schema + rows exist), T3 (owns the same files in wave 1 — cross-wave re-entry, named here per S1 convention)
- owns: `apps/web/src/server/queries/traces.ts`, `apps/web/src/server/adapters.ts` (re-entry after T3 closes), tests beside them
- goal: per D38(c)/(d) + D42 — `LOGS_SQL` in `queries/traces.ts` gains `span_id`/`prompt`/`completion` in the SELECT; the trace detail hydrates an LLM span's `LlmDetail` prompt/completion from event-derived rows when the span's own columns are empty. Precedence per field independently: non-empty span column wins always → else event-derived, earliest `timestamp` among content rows matching `(workspace_id, trace_id, span_id)` with that field non-empty (deterministic — the read is already timestamp-ordered). **Rail rule (D42(d)):** rows with non-empty extracted content and EMPTY body are content carriers — folded into LlmDetail, excluded from the rail AND its counter (counter counts rendered rows only, D13/D21); a content row with non-empty body renders in the rail as a normal log AND still folds per precedence; orphan content rows fold nowhere and render nowhere. The exclusion is documented in the adapter comment — a documented display rule, not a silent filter.
- done-check (per D42(e) probes 3–4): span-column-wins precedence (red when inverted); event-derived fill when span columns empty; a later-timestamp second content row does NOT displace the earliest (red if it does); non-empty-body content rows render AND fold; **probe 4** — carrier filter removed → duplicate content appears in the rail and counter mismatches, red; orphan rows invisible; coalesce disabled → event-form-only trace shows empty prompt (proving the fill is real). `npm test` + build green from root; mock mode untouched; no SQL interpolation. Advisor contract review before merge (same D8-contract class as T2).

### F1: pod_association reorder + k8s_node standing guard (D43, pre-integrator fix task)
- status: CLOSED (2026-08-16) — REVIEWED: APPROVE, no fixes, tip stays **4bf0774**. Reviewer reproduced probes 1 and 4 on its own cluster verbatim (guard the sole red line under connection-first; stamping-strip red shows ns/pod AND node all empty — connection fallback evaluated and still hairpin-dead, stamping not optional), full acceptance PASS 92s, contract test + live-ConfigMap-vs-source byte-identical (executor's mid-probe checkout incident confirmed absent from the landed tree), CI four-green at tip with the alive-line, D43 text conformance verified clause by clause (no overclaim; demo comment still accurate under the reorder; standing guard asserts real end-to-end data and counts honestly per D13/D21). All four D43 probes green on fresh kind: (1) connection-first restored → `5 of 5 span(s) carry no k8s_node` the ONLY red line (guard is the lone observable) → reorder → `k8s_node (t5f1-control-plane)` green; (2) full acceptance both paths PASS (94s/30s), all prior evidence unchanged; (3) D40/K1 contract test green uncached (executor also caught its own `git checkout --` restore reverting the byte-copy to pre-F1 bytes mid-probe — diagnosed via live ConfigMap, repaired, re-run); (4) stamping-strip still red under the reorder — association cannot resurrect ns/pod, stamping not made optional. CI at tip: go 53s / kind 1m40s / web 2m39s / stack 4m27s all green; stack log carries the D43 alive-line; still under the K5 line with the new guard.
- owns (per D43): `deploy/collector/config.yaml` (pod_association reorder: resource_attribute sources first, connection second; comment block 74-95 rewritten to the measured truth — self-identification primary, connection fallback that does not survive same-node hostPort hairpin SNAT, which is WHY the recommended pattern stamps; non-stamping/NATed senders land unenriched, loudly documented), the chart byte-copy `deploy/helm/obstack/files/collector-config.yaml` (D40/K1 contract test must stay green — proves both moved together), `deploy/helm/obstack/templates/collector/daemonset.yaml:57-61` (strike the "so from: connection sees the real source IP" clause; hostIP-reachability half stands; demo/deployment.yaml:65-72 stands as written), `deploy/collector/README.md` (customer-pattern row: stamping guarantees identity; connection association best-effort behind NAT), `deploy/helm/obstack/acceptance.ts` (ONE permanent assertion: the trace's spans carry non-empty `k8s_node` — the falsification probe promoted to a standing guard; only observable evidence k8sattributes is alive, guards against silent regression on a collector version bump).
- done-check (D43's four probes): (1) red-then-green on the new assertion — connection-first restored → `k8s_node` empty → red; reorder → populated → green; (2) full `acceptance.sh` green both paths (stamping SOLID, filelog NEARBY, zero-dup, restart rider unchanged — regression floor); (3) D40/K1 byte-equality green; (4) shipped P1 stamping-strip probe still red (the reorder must not make stamping look optional).

## Scope fence (binding on every task)
- **No search work.** Filters, totals, pagination, free-text reach and the `/app/logs` surface are **S2.3**. The traces list stays exactly as M1 shipped it, 200-cap included.
- **No SDKs**, no `packages/`, no sample TS app — **S2.4**.
- **No infra-layer producer** (D32). `classify()` gains no branch; no K8s-events ingestion.
- **No `web` image, no TTL tiers, no self-hosted values, no second chart** (D35).
- **No query-layer dedupe of log rows** (D37.5).
- **No schema change beyond the D38-sanctioned 0002 edit** (T2's three logs columns). If the nearby join or anything else appears to need one, STOP and escalate — the pre-release `down -v` window is a ruling, not a workaround.
- **No demo-app code change expected.** D37 routes its telemetry by configuration; if a code change seems necessary, escalate rather than absorb it (M1 and S2.4 own that file's contract).
- **No dependency drift.** New Go or npm dependencies require a stated reason; npm lockfile changes use the seeded `--package-lock-only` method and are diffed for incidental version floats (S2.1 L1).
- **No renaming of the `web`/`go`/`kind` checks** (K3).
- No hosted deployment, no registry pushes, no provider choice (M4/S5, user's decision).

## Questions for the kickoff gate — ALL ANSWERED (see Kickoff gate rulings above; kept for the record)
- Q1 → D38 FINAL (log-record form + schema, user override folded in; span-event form dropped). Q2 → D39 (profile). Q3 → D40 (chart at `deploy/helm/obstack/`, verbatim copy + byte-equality contract test).
<!-- Original question text below, superseded: -->
<!-- Advisor answers before wave 1 dispatches; answers become rulings recorded above. -->
1. **(BLOCKS T2) Which wire form does the GenAI event ruling mean?** The carry-forward says "log/event path … into the same prompt/completion columns (additive, `internal/mapping` only)". Those two halves are only simultaneously satisfiable for one of the two forms:
   - **(a) Span events** (`gen_ai.input.messages` on the LLM span's own events): reading `span.Events()` in `internal/mapping/spans.go` fills the same row's `prompt`/`completion` columns before it is ever written — genuinely additive, mapping-only, no writer or schema change. **PM recommendation.**
   - **(b) Separate log records** carrying the LLM span's `span_id`: `obstack.logs` has **no** prompt/completion columns, and `obstack.spans` is a plain `MergeTree` (D23 explicitly rejected `ReplacingMergeTree`), so landing that content in the spans row requires a writer-side join, a mutation, or a schema change — **none of which is "internal/mapping only"**. If (b) is meant, it needs its own ruling on mechanism and is plausibly not a one-task change.
   - **(c) Both**, with (b) deferred to a later sprint and (a) landing here.
2. **(BLOCKS T1's compose block) Does the compose bundle's default telemetry path change?** D37 routes app OTLP through the collector on K8s and calls the exclusion pattern "the recommended customer pattern".
   - **(a) Collector rides a compose profile**; the default `demo → ingest` path — and therefore M1's advisor-signed `smoke.sh` — stays byte-untouched. **PM recommendation** (the compose smoke is signed exit evidence; changing its topology re-opens it).
   - **(b) Compose mirrors the K8s topology** (`demo → collector → ingest`) so local dev and the cluster teach the same pattern, accepting that M1's acceptance path now runs through one more component.
3. **(cheap, confirm) Chart location and the D11 users.** The plan puts the chart at `deploy/helm/obstack/` as a sibling of `deploy/compose/` (the only existing convention). And per K1's refusal of a second copy of a D11-binding artifact: is the chart's ClickHouse user config allowed to be a copy **pinned by a contract test** (PR #3 precedent), or must it be generated from `deploy/compose/clickhouse/users.d/obstack-users.xml` at package time?

## Escalations log
<!-- Manager appends: {T#} — question → advisor decision (one line each). -->
- Kickoff Q1 → advisor deferred log-record form to S2.3 → **USER OVERRIDE at dispatch (schema window spent now)** → advisor D38 FINAL: log-record form IS the ruled form, span-event form dropped as speculation, schema per D38(a), 0002 in-place per (b), T2 re-scoped + T6 created per (c), precedence per (d), T5 fixture rider per (e). Team is 6 tasks.
- Kickoff Q2 → D39: collector as compose profile, default path + signed smoke untouched; M4-GATE on the self-hosted bundle's default topology; bearer-key rider.
- Kickoff Q3 → D40: chart at `deploy/helm/obstack/`; users XML verbatim copy pinned by byte-equality contract test in the go suite.
- K5 revisit → D41: same ~6-min line; new check name **`stack`** (K3-stable from first run); if label/dispatch-triggered, also always on push-to-master, red `stack` on master = stop-the-line.
- T2 critical item added (D38 contract); T6 under the same D8-contract review.
- T4 chart-structure escalation (reviewer measured both alternatives: pre-install hooks block all normal resources — ClickHouse-as-hook was FORCED under the old design of record; but hook-lifecycle ClickHouse = inert values, password-rotation release breakage, uninstall/failed-install orphans) → advisor ruled **(c), signing neither posed option**: (1) EVERY stack resource normal + release-managed (values live, uninstall clean; users.d ConfigMap gets the checksum-annotation mechanism so password changes restart CH); (2) migrations = ONE template, TWO renderings — install (`.Release.IsInstall`): normal revision-named Job + ttlSecondsAfterFinished, ingest crash-looping briefly until it completes is PR #4's verify-and-refuse WORKING, not a defect (17s measured, 2 restarts); upgrades: `pre-upgrade` hook (revision-named, before-hook-creation+hook-succeeded delete policy) — migrate-before-rollout preserved exactly where it is real (CH already running); (3) invariant unchanged: one runner per revision, serving pods verify read-only + refuse loudly. **Record amendments (advisor's own authority): D35's migrate clause becomes "install: normal Job; upgrades: pre-upgrade hook" — the pre-install half was empirically falsified (a pre-install hook cannot depend on anything in its own chart under --wait); `deploy/compose/README.md:147-186` cell + one sentence updated to match (T4 executor lands, named out-of-scope edit); "exactly one runner per upgrade" stands verbatim.** Re-probe set: cold install green; upgrade with a NEW migration file runs the hook to completion BEFORE any new ingest pod is Ready (red if inverted); uninstall leaves zero; clickhouse.image reconciles; password change rolls + converges. T5 note: all-normal install returns ~74s of K5 budget. T4 contract APPROVED conditional on (c).
- T1 review escalation (no filelog checkpointing: `start_at: beginning` re-ships every historical line on any collector restart — a duplicate door D37.3 doesn't cover) → advisor ruled **(a): file_storage checkpointing NOW, both forms** — (c) refused categorically (stopgap + converts T5's zero-dup evidence into a restart flake: "an exit assertion that can go red without a defect is a broken guard"); (b) refused (loses the NEARBY lines CI needs at near-simultaneous startup). Lands: T1 config.yaml both forms (file_storage extension, storage dir via env — same single-config mechanism as the exclusion), README assumption row; T4 chart (writable per-node hostPath e.g. /var/lib/obstack-collector + env; compose satisfies via named volume); probes — T1 compose restart probe (restart mid-tail, previously-shipped lines NOT re-shipped, red with file_storage removed), T5 does `kubectl rollout restart` + wait BEFORE the zero-duplicated-bodies assertion. T1 contract APPROVED at e23c6ba subject to the storage addition (CRI auto-detect catch called load-bearing). T4 dispatches with the hostPath/env contract folded in.
- T3 review escalations → advisor: **E1** assigned to T5 as a scope line — `.github/workflows/web.yml` gains ClickHouse via the K1 mechanism (post-checkout compose up, both web env vars for the readonly user) + fail-on-unexpected-skip mirroring go.yml (zero node:test skips, empty allowlist); deleting the nowhere-executed integration test REFUSED (the cure is enforcement); `web` check name untouched per K3. **E2 ruled: proximity SELECTION, chronological DISPLAY** — rank by `greatest(trace_start − ts, ts − trace_end, 0)`, then timestamp, then the tie-break chain; selected set re-sorted by timestamp for the rail; set-identical below the cap; probe: chatty-pod fixture where in-interval rows survive selection (red under timestamp-ASC). **E3 ordered: LIMIT cap+1 + slice** — truncation becomes known fact; marker fires only when true; wording states fact ("200 shown, more exist in this window"); composes with E2 (row cap+1's existence is the flag). E2+E3 owner: T3 executor, same files, same reviewer verifies both probes; T3 contract APPROVED subject to that landing.
- T2 contract review (advisor, post-review) → **APPROVED** subject to one doc rewording. Rulings: (1) **canonical-JSON RATIFIED** as the stored contract for structured values — plain string → verbatim; structured `AnyValue` → pcommon's canonical JSON (deterministic, sorted keys), test-pinned; there are no "producer bytes" for a protobuf AnyValue, serialization here is where it first becomes a string. Doc table reworded to the two-case statement. Carry-forward into S2.4: SDK conformance docs state the span-attribute form emits pre-serialized strings (always the verbatim branch). (2) **Empty/absent collapse RATIFIED** — exact span-path parity, clean two-state; three-state semantics refused as zero-value speculation. (3) **One-PR construction suffices** for the T6 rail-honesty dependency — D13/D21 bind the merged tree, not branch states; T6's probe 4 result joins the T5/integrator evidence the advisor signs, so the sprint cannot close with blank-line rendering extant.
- D42 design ruling (post-override, one pass): schema delta corrected to TWO columns — advisor verified `span_id` already exists on logs since M1 (its own D38(a) claim wrong, corrected); T2 grows with named write.go scope (insertLogs column list + appendLogs args only) instead of a new Go task; T6 spec refined (LOGS_SQL SELECT additions; empty-body carriers excluded from rail+counter, non-empty-body rows render AND fold); five falsification probes distributed T2/T6/T5; 0004 ALTER refused (would split the D7 logs DDL across files forever to save a step the pre-release window makes free).
- T4 re-verify → APPROVE-WITH-FIXES at 0655d01 (docs-only: hostPath scope honesty, failed-hook-Job uninstall orphans + labeled cleanup, cold-install timeout budget 1500s/300s, lookup dry-run divergence). **E-coldstart ESCALATION:** install-time ingest crash-loop, sized "17s/2 restarts" by ruling (c) on a warm node, reaches kubelet's 5-min backoff cap on a cold node and adds up to 5 min dead wait AFTER migration success (measured: migrate done 14:30:10, pods Ready 14:34:52; flipped `install --wait --timeout 900s` to a failed release while the stack converged healthy).
- E-coldstart → advisor ruled **(b) + one-definition**: ingest Deployment gains the migrate Job's `wait-for-clickhouse` init container, extracted to ONE `_helpers.tpl` define (`obstack.waitForClickhouse`-style) included by BOTH — poll budget cannot drift (K1-style, in-chart). Reasoning: the chart is the product, not CI's fixture (D35 — cold consumers = first-install humans + M4's bundle); (a) is acceptance of a measured broken guard (`--wait` FAILED while healthy = exit assertion red without a defect, S2.0 L1) with a CI-only bandage — refused as stopgap-shaped. The refusal invariant is about SCHEMA STATE, not ClickHouse reachability — install-time "connection refused" churn was incidental noise ruling (c) mistook for the demonstration; the proof of record is the inverted-control probe, which is init-container-independent; zero refusals on a cold install is honest, not a gap. Runtime semantics untouched (init containers affect startup only; running ingest losing CH still crash-loops as today; rolling upgrade with CH down holds new pods in Init while old pods serve). **Record amendments (advisor's authority): ruling (c) item (2) install branch — "ingest crash-looping briefly … WORKING, not a defect (17s, 2 restarts)" STRUCK as machine-specific-falsified; replaced with "install: normal Job; ingest carries the same wait-for-clickhouse init container (one define, two includes); install-time refusals may be zero — proof of record is the inverted-control probe." `deployment.yaml` header comment + `migrate-job.yaml` "Kept in BOTH renderings" comment rewritten to match; README cold budget re-derived (backoff-tail component gone; FAILED-while-healthy caveat removed because the mechanism is removed, one line names the init container as why `--wait` now tracks reality).** Implementation: T4 delta, same branch, Fable 5 executor (user override stands), same re-verify reviewer; T5 gated on the delta's APPROVE. Required re-probes: (1) warm fresh install `--wait` green 17s-class + clean uninstall; (2) inverted control red-then-green — refusal path demonstrably fires with the init container present; (3) cold simulation — unresolvable `clickhouse.image` tag → ingest holds `Init:0/1` ZERO restarts ≥3 min, then corrective `helm upgrade --no-hooks` with the good tag → CH up, init passes, main container refuses on reachable-but-unmigrated CH with the documented error (both assertions required); (4) lint + D40/K1 contract test green. T5 riders: image pre-pull NOT mandatory and NOT a correctness mechanism — `docker pull` + `kind load` of pinned images is the sanctioned optimization, done-check records whether used so the K5 number states what it includes; CI's helm timeout comes from the README's re-derived budget via the same script a human runs (S2.1 L3) — no CI-only timeout arithmetic.

## Integration
<!-- Filled by the integrator gate. Fix tasks get F# IDs, appended under ## Tasks. -->
- verdict: **FIXED-GO** (2026-08-16) — tip 4bf0774 → **02bd695** (four text-only truth fixes, all suites re-run after). E2E from clean state: go gofmt/vet/test 0-skip green vs clean-volume ClickHouse; npm test 45/45 0-skip + build clean; compose smoke.sh PASS 21s; acceptance.sh on own kind cluster PASS install 95s / upgrade 32s / fix-tip re-run 43s with full D37+D43+D38(e)+restart evidence each time; beyond the harness, live `next dev` against the cluster rendered `/app/traces/<id>` as "3 on trace · 4 nearby (same pods, ±10s)" with NEARBY badges and sidecar bodies in one view, no SAMPLE badge; `deploy/collector/up.sh` compose path proven too. Seams all clean with named proof (byte-copies cmp-identical + contract test; T2↔T6↔T3 traces.ts/adapters.ts coherent — NEARBY_LOGS_SQL selects no content and is trace_id=''-bound so never a coalesce candidate; carriers leave Trace.logs, the single source of both counters; cap+1 sliced in one place; window constant sole ±10s source; E1 proven by executing tests). Fixes: README.md CI section gains the `stack` bullet (was claiming three checks); stale integration-test header claiming web starts no ClickHouse (false since E1); compose README "M4 chart" phrasing; chart README acceptance list gains the D43 guard. Fence: 47 files in owned trees, lockfiles/go.sum/demo untouched, only the 0002 schema edit — one flag: fix 1 touches root README.md, which no task owned (flagged, not hidden). Goal line: MET, reproduced locally and by the `stack` check on CI.

## Goal check
<!-- Compares delivered work against the goal line at the top of this file. -->
- iteration 0: **MET** (2026-08-16). One `POST /chat` on a kind cluster installed only through the real chart produces api→agent→tool→llm spans rendered by the app's facade AND its actual UI together with trace-carrying pod-stamped rows and trace-less sidecar rows in the one correlated-logs rail, zero duplicated bodies — reproduced by the T5 reviewer (×4), the integrator (clean state, including a live `next dev` render: "3 on trace · 4 nearby (same pods, ±10s)", no SAMPLE badge), and the `stack` check on CI.
- **ADVISOR SIGNATURE (2026-08-16): exit bundle SIGNED at 02bd695.** D37 three parts signed with mechanism honestly attributed per amended D37.1 (stamping load-bearing; k8sattributes alive via the D43 standing guard; both mechanism-accurate reds on record); NEARBY sidecar probe red-then-green; zero-dup with restart rider (checkpoint-wipe red confirmed twice). K5 RATIFIED: 4m06s–4m27s incl. the guard, under the line → `stack` on every PR, joins the required set; D41 master rider stands (red `stack` on master = stop-the-line). Root-README edit SANCTIONED as a named out-of-scope truth repair (T4 compose-README precedent); retro item noted — name an owner for cross-cutting doc truths when a sprint changes the check set. **One merge condition: the four checks green at 02bd695 itself.** Merge decision is the user's per the process gate.
- Merge condition **MET** (2026-08-16): go 56s / kind 1m34s / web 2m25s / stack 4m17s all pass at 02bd695 (runs 31964256185/…209/…201/…187). PR #11 marked ready for review; **merge call with the user.**

## Retro
- **S2.2 L1 — a green pipeline is not proof its components are alive.** `k8sattributes` contributed zero observable data to the exit evidence while two comments called it load-bearing; deleting it entirely left the bundle green (the finding that forced D43). When a component's only honest proof is a falsification probe, promote that probe to a **standing guard** asserting data only that component can produce (the `k8s_node` guard) — otherwise the component can die on any version bump and nothing goes red.
- **S2.2 L2 — behavior sized on a warm machine writes a machine-specific number into a ruling.** Ruling (c)'s "17s, 2 restarts" and the executor's "11m30s cold install" were both falsified by one cold/slower node (kubelet's 5-min backoff cap added dead wait AFTER success; `--wait` marked a healthy release FAILED). Wall-clock claims entering the record need the cold path measured, and any accepted behavior must be checked for environment-scaled tails before it is called bounded.
- **S2.2 L3 — Helm v4 reuses the previous release's user-supplied values on upgrade.** Measured three separate times this sprint (acceptance re-run path, the `--no-hooks` corrective escape, F1's probe restore): a bare `helm upgrade` asserts a configuration nobody chose. Every scripted or documented upgrade path states the fixing values explicitly or passes `--reset-values`.
- **S2.2 L4 (amends S2.1 L2) — literal sweeps are only as good as the file's text-ness.** Three literal NUL bytes made `acceptance.ts` git-binary and grep-silent, defeating the cross-tree sweep without failing it. Separators and sentinels in checked-in sources use escaped forms; a sweep that can silently skip a file is a hollow guard (S2.0 L1 applies to the sweep itself).
- **S2.2 L5 (advisor's item) — cross-cutting doc truths need a named owner.** The root README's CI section was falsified by D41 the moment `stack` joined the check set, and sat outside every task's fence until the integrator repaired it out-of-fence (sanctioned after the fact). When a sprint changes a repo-wide contract (check set, ports, service names), the plan names the doc that records it and its owner.
- Machinery note for the record: two executor stops salvaged cleanly (T4's hung rework correctly diagnosed as emptyDir schema loss; a killed re-verify session's cluster salvaged as evidence); the advisor corrected its own record three times (D42 span_id, E-coldstart strike, D37.1 restatement) — the review machinery's cost bought eleven rulings and a truthful ledger.
