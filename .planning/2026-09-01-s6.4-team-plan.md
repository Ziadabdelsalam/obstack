# Team Plan — obstack M5 sprint 4 (S6.4): infra + costs — the obstack-collector distro ships k8s metrics (kubeletstats + k8s_cluster shape) into the S6.1 metric store with NO ingest/DDL change; `/app/infra` renders the signed-in workspace's real nodes/pods from those metrics with right-sizing recommendations fenced to what the data supports (no $ — no price input exists); `/app/costs` renders trace-derived LLM unit economics (by model, by service, over time, unpriced calls named) with every fenced-out figure ABSENT (infra $, revenue, margin, per-customer, per-feature); both flipped in `live-routes.ts`; mock mode byte-identical

meta:
- date: 2026-09-01
- repo/branch: `s6-4` (cut from `master` @ `4f7d94d`; production = `9be47d6`)
- mode: waves (house precedent S6.1–S6.3 — hand-dispatched waves, reviews as results arrive)
- team-cap: 10
- models: executor = Sonnet 5 · reviewer = Opus 5 · advisor = Fable 5 (user, Step 0: "same as S6.3"). **Per-task exception (user-approved 2026-09-01 on the advisor's tier note): T1, T2, T3, T5, T7 executors = Opus 5; T4, T6 = Sonnet 5.** Reviewers Opus 5 (xhigh carve-out). T0 = manager.
- iteration-limit: 3 — fix rounds allowed before escalating to the user
- iteration: 0/3
- governing docs (binding): `.planning/2026-08-31-s6-m5-full-platform-phase-plan.md` §5 S6.4 (:111-113 run-goal + scope fence), :79 (costs fence), :82 (collector leg is receiver + config work, not a new component), §7 standing constraints, §8 rulings D360 (Option C), D362 (costs fence: fenced-out figures ABSENT, not zeroed/estimated; per-section `SampleMark` precedent), D364 (sequential), D365 (metrics do not meter), :154 (S6.5 seam). S6.1 packet `.planning/2026-08-31-s6.1-metrics-ddl-packet.md` Q2 :93 (the 25k cap was SIZED for this leg: "kubeletstats + kube-state, ~50 nodes × ~30 pods × ~15 per-container ≈ 20k") and D375 :217 ("resource-borne groupBy keys work — k8s.pod.name for S6.4"). The S6.1–S6.3 standing set: **D366** (contract types client-safe in `lib/*-types.ts`), **D367** (server page branches on `dataMode`; mock body moves VERBATIM to `<X>Mock.tsx`; live is a props-fed sibling; byte-identity), **D391** (duplication across the mock/live boundary is the ratified shape; "no `@/mock/` in the live graph" is a SOURCE-TEXT rule via the D448 `resolvedImports` helper, files enumerated by glob), **D402** (every unbounded list ships a cap and says so), D384 (metric keyed `(name, type)`), D390 (`VALID_AGGS`), D13/D21/D114, D7/D11/D113/D130 (tenancy: scope-injected `workspace_id`, hermetic SQL-text proof + "a second workspace sees none" integration proof), D404 (tour copy mode-agnostic, guarded), D394 (one fixed window unless the surface needs a picker), D448.
- predecessor: S6.3 closed 2026-09-01 (PR #31, production `9be47d6`); retro lessons S6.1-L1..L3, S6.2-L1..L4, S6.3-L1..L4 apply (mock `next build` before `npm test` after a drive; rulings as observable RESULTS; tenancy fixtures same ids AND names in the foreign workspace; assert words/DOM slices never counts; source-text guards via `resolvedImports` + glob; a `"use server"` export is a public POST — none this sprint; grep the literals a sprint will move at breakdown — done, §"Literals that move").
- carried in from S6.3's resume point: D410, D413, the drive's URL-not-click path to the diff page, `e2e-drive.mjs:2911` near-tautological, the top-failing table still `sample={live}` on `/app`, D440 silent-no-op delete, Railway watch-path observation (all four services rebuild on every production push — RELEVANT: this sprint ships NO migration; the helm chart change reaches nobody via Railway, it reaches users via the chart).

## Foundation facts (audited 2026-09-01, this session — two Explore agents + manager spot checks, every claim cited)

**Collector distro (M2)**
- Image `otel/opentelemetry-collector-k8s:0.158.0` pinned identically at `deploy/compose/docker-compose.yml:353`, `deploy/helm/obstack/values.yaml:231`, `deploy/collector/README.md:31`. **Manager verified 2026-09-01 (`docker run --rm … components`)**: the pinned distro ships `kubeletstats`, `k8s_cluster`, `hostmetrics`, `prometheus` receivers and `filter`, `transform`, `metrics_transform`, `cumulativetodelta`, `resourcedetection`, `memory_limiter` processors. **No image change is needed.**
- Three configs, ZERO metrics anywhere: `deploy/collector/config.yaml` (158 lines; receivers `otlp` :20-25 + `file_log` :44-69; processors `batch` :72, `k8s_attributes` :105-119; exporter `otlp_http` :122-130 with `Authorization: Bearer ${env:OBSTACK_COLLECTOR_API_KEY}` :130, endpoint `${env:OBSTACK_INGEST_ENDPOINT:-http://ingest:4318}` :123; extensions `health_check` :135-136, `file_storage` :144-146; pipelines :150-158 = traces + logs). `deploy/helm/obstack/files/collector-config.yaml` is a BYTE-EQUAL copy pinned by `services/ingest/internal/mapping/chart_contract_test.go:44-47,50-70` (K1: copy source over copy, never edit the copy). `deploy/collector/config.compose.yaml` (compose, no k8s — untouched this sprint). `deploy/helm/obstack/files/collector-events-config.yaml`: single `k8s_events` receiver (`auth_type: serviceAccount` :52-53), `batch` :56, same bearer exporter :59-68, `health_check` only :74-75, one `logs` pipeline :94-97; header :21-29 explains why it is a singleton Deployment, not in the DaemonSet.
- Workloads: DaemonSet `deploy/helm/obstack/templates/collector/daemonset.yaml:6` (env block :50-58 — NO `K8S_NODE_NAME` today; hostPorts 4317/4318 :67-72; hostPath mounts :104-121; `runAsUser: 0` :40-42). Events collector = `Deployment`, `replicas: 1`, `Recreate` (`events-deployment.yaml:15,23,30`), gated on `.Values.collector.k8sEvents.enabled` (:1; default `true` at `values.yaml:281`; resources :289-295). Chart values block `values.yaml:230-295`.
- RBAC: `templates/collector/serviceaccount.yaml:1-7`; `clusterrole.yaml:26-33` grants `get/list/watch` on `pods, namespaces` unconditionally, `events` behind the k8sEvents gate; `replicasets` deliberately absent (:1-6). **kubeletstats needs `nodes`, `nodes/stats` (and `nodes/proxy` on some distros); `k8s_cluster` needs list/watch on a wide resource set — neither granted today.**
- CI: the only cluster job is `.github/workflows/stack.yml` (job `stack:` :118) running `deploy/helm/obstack/acceptance.sh` on kind (:185-186; collector restart proof :191-201; collector-routed GenAI fixture :207-210; events rider :218-232; port-forwards clickhouse/demo/web :160-175 — web `/login` only, no session). `kind.yml` is capability-only. Compose has a profile-gated collector (`docker-compose.yml:352-395`, `profiles: [collector]`) that is NOT in the drive's default path.
- Docs: `deploy/collector/README.md` (284 lines) is the install doc; web docs mention the collector at `apps/web/src/content/docs/self-hosting/helm-chart/index.mdx:10,40,48`; no doc describes a metrics leg.

**Ingest metrics path (S6.1) — nothing to change**
- `services/ingest/migrations/0005_metrics.sql`: `metric_points` :39-62 (raw, 3-day TTL), `metric_points_1m` :71-96 (AggregatingMergeTree; `attributes SimpleAggregateFunction(anyLast, Map(String,String))` = the D375 `mapUpdate(resource_attributes, attributes)` MERGE :101; `gauge_min/gauge_max` simple, `gauge_avg AggregateFunction(avg)`, `gauge_last AggregateFunction(argMax, Float64, DateTime64)`, `sum_delta`, histogram states; `ORDER BY (workspace_id, name, series_hash, bucket)`; 90-day TTL), `metric_points_1h` :113-151 (no web consumer), `metric_series` :158-180 (catalog; `attr_keys`).
- Attribute survival: `mapping/metrics.go:184` → `mapping.go:213-222` `flattenAttributes(attrs)` with NO skip list — every resource attribute is stored verbatim, so `k8s.node.name`, `k8s.pod.name`, `k8s.namespace.name`, `k8s.container.name`, `k8s.pod.uid` all land in `resource_attributes` and, merged, in the rollups' `attributes`. `MetricRow` (`metrics.go:148-166`) has no k8s columns (spans/logs promotion constants `mapping.go:138-142` do not apply). Series hash `metrics.go:646-662` = sipHash64(name ‖ type ‖ service ‖ sorted merged KVs). `service` comes from `service.name` (`mapping.go:138`) — kubeletstats/k8s_cluster emit NO `service.name`; executors MUST NOT filter on `service` in infra SQL and T2's integration seed must prove the empty-service path.
- Delta normalization `metrics.go:27-50, 274-362, 541-597` (first observation emits no row; k8s cumulative sums such as `container.cpu.time` become deltas — the infra views read GAUGES only, see P1). Cap `DefaultSeriesCap = 25_000` (`write/write.go:37-40`, enforced :149-157; drop reason `ReasonCardinality` metered `receive.go:267,370`); `maxPointAttrs = 64` (`metrics.go:140-141`). Web parity `apps/web/src/app/app/explore/series-cap.ts:14`.
- Receive: HTTP `POST /v1/metrics` `receive/http.go:72-78`; gRPC `receive/grpc.go:36,106-108`.

**Web metrics contract & query module (S6.1)**
- `apps/web/src/lib/metrics-types.ts` (85 lines): `MetricCatalogEntry` :13-23, `MetricRange` :26, `MetricAgg` :29, `VALID_AGGS` :36-40 (gauge → avg|min|max|last), `MetricSeriesQuery` :42-59, `MetricSeriesResult` :82-85.
- `apps/web/src/server/queries/metrics.ts` (363 lines): `listMetricCatalog` :71, `histogramQuantile` :100, `queryMetricSeries` :290-363 — groupBy ANY attr key (`attributes[{group_by:String}]` :161, `mapContains` gate :160), filter ANY attr (:157-160), top-10 groups `.slice(0,10)` :331-333, `totalGroups` pre-truncation :359-362, `RANGES` :135-139 (1h/1m, 6h/5m, 24h/15m), reads `metric_points_1m` only :191; merge idiom `avgMerge(gauge_avg_state)` / `argMaxMerge(gauge_last_state)` :170-171 over `avgMergeState`/`argMaxMergeState` :184-185. **No "latest value per group" query exists** — S6.4 needs one (P3).
- No `app/api/*` metrics route (only `auth/[...all]`, `billing/webhook`).

**`/app/infra` today** — ONE file, pure mock, server component, no `dataMode` branch, no test: `apps/web/src/app/app/infra/page.tsx` (159 lines; imports `{ nodes, pods, rightsizing } from "@/mock/infra"` :3; fixture header "prod-cluster (GKE) · 4 nodes · N pods · synced via obstack-collector" :25; node cards :30-66; pod table :70-133 with deep link `` `/app/logs?pod=${encodeURIComponent(p.name)}` `` :121; right-sizing panel :136-155 with basis "derived from 30d of pod telemetry + the traces those pods served" :150-153). `apps/web/src/components/infra/` DOES NOT EXIST. `apps/web/src/mock/infra.ts` (62 lines): `K8sNode {name, pool, cpuPct, memPct, pods, status: "ready"|"pressure"}` :3-10 (4 GKE rows :12-17); `K8sPod {name, service, node, status: "running"|"crashloop"|"pending", restarts24h, rssMi, limitMi, cpuPct, age}` :19-29 (12 rows :31-44; NO cpu request/limit, NO memory request); `rightsizing` :46-61 = untyped `{title, detail, impact}` with `$` inside free text (`"~$31/mo compute savings"` :55) — no structured price field anywhere.

**`/app/costs` today** — ONE file, pure mock, server component, no branch, no test: `apps/web/src/app/app/costs/page.tsx` (synchronous `CostsPage` :20; 6 mock bindings :2-9 from `@/mock/economics`; hardcoded `const aiTotal = 2232` :21; `marginOf` :11-18; "Revenue via your Stripe connection." :144; infra-allocation prose :142-145; cross-links "$31/mo" → `/app/infra` :174-178 and "~$347/mo" → `/app/evals` :232-242; `data-tour="costs"` :82). `apps/web/src/components/costs/` DOES NOT EXIST; mock module is `apps/web/src/mock/economics.ts`: `CustomerCost {org, plan, revenueMo, llmCostMo, infraCostMo, trendPct, reqs30d}` :3-12 + 5 rows :14-20 (revenue/infra/plan = external inputs; no org attribute exists in the schema — `users.ts:50` `enduser.id`/`user.id` is the only per-person key); `InfraCost`/`infraCosts` :22-33 + `infraTotalMo` 412 :35 (cloud billing — external); `FeatureCost`/`featureCosts` :37-51 (needs a feature dimension on LLM spans — `obstack.agent.step` lives on AGENT spans as an attr, not on the llm span; cross-span join — fenced); `modelCosts` :53-57 (trace-derivable); `econStats` :59-64 (AI+infra / revenue composites — external as written; LLM-only total and cost/LLM-call are derivable).

**LLM data in ClickHouse**
- `spans` (`0001_spans.sql`): `layer Enum8(… 'llm'=4 …)` :21; `gen_ai_system`, `gen_ai_request_model`, `gen_ai_response_model`, `input_tokens`, `output_tokens`, **`cost_usd Float64`**, `finish_reason` :23-33; k8s trio+node :35-38; `attributes`/`resource_attributes` :40-41; `ORDER BY (workspace_id, trace_id, span_id)`, `PARTITION BY toDate(start_time)`, TTL 30d :43-46. `trace_summaries` (`0003`): per-trace `total_input_tokens`, `total_output_tokens`, `total_cost_usd`, `models Array(String)` :21-29 — NO model/service dimension on cost; TTL 30d :42. Per-model spend must read `spans`.
- Mapping: GenAI columns + `CostUSD = prices.Cost(requestModel, responseModel, in, out)` filled ONLY when `layer == LayerLLM` (`spans.go:84-99`); `classify` :104-125 (any `gen_ai.` attr ⇒ llm). **Pricing is ingest-time** (`services/ingest/internal/pricing/pricing.go:185-200`; `pricing/prices.json` ~45 rows, `as_of 2026-08-15`; per-workspace overrides D108): an UNPRICED model ⇒ `cost_usd = 0` + counter `obstack_ingest_unpriced_models_total{model}` (:194). **Honesty hazard**: in `cost_usd`, "unpriced" is indistinguishable from "free" — the costs surface must name unpriced calls separately (P5). The SDK never emits cost (`packages/obstack-js/src/instrumentation/llm-span.ts:20-25`, D9); streaming uninstrumented to avoid $0 mispricing (:53-56). `apps/web/src/lib/format.ts:11-19` `fmtCost` renders exactly-0 as `"—"`.
- Existing live economics: `services.ts:49` `sum(cost_usd)` per service + `:51` models (typed `services-types.ts:51-54`, rendered `ServicesLive.tsx:69-105`, `ServiceDetailLive.tsx:123`); overview `overview.ts:14-15,46-47,155-158` "LLM cost · range" from `trace_summaries`; model identity idiom `adapters.ts:167` `gen_ai_response_model || gen_ai_request_model`; `users.ts` computes no cost. No revenue anywhere in server/lib.
- Query precedent for costs = `apps/web/src/server/queries/services.ts` (`listServices` :212; SQL :41-56; scope-injected `workspace_id` :52 via `clickhouse.ts:80-84` with refusals :64-79, `forWorkspace` :98-109; window :54; `count() OVER ()` + `LIMIT {cap}` D402 :83-99). Tests `services.test.ts` (hermetic tripwires :26-42, recorder :44-50) + `services.integration.test.ts` (seeds via the ingest user so MVs fire :15-18; run-scoped workspaces :49-51). Bucket-map precedents `overview.ts:30-33`, `metrics.ts:135-138`.

**Harness**
- `deploy/compose/exit-seed.mjs`: metrics leg constants :509-534, names `metricNames(label)` :545-549 (`${label}.queue.depth` gauge / `.requests.total` sum / `.request.duration` histogram), expectations :577-596, builder `metricsExport` :598-653 (only resource attr `service.name` :608; datapoint attr `deployment.environment=e2e` :605), `seedMetrics` POSTs `/v1/metrics` :663-673. NO k8s attr on the metrics leg; k8s attrs exist on logs/spans legs: `k8s_namespace: "exit-ns"`, `k8s_pod: "exit-app-1"`, `k8s_container: "app"`, `k8s_node: "exit-node"` :164-167, :192-194; `exit-db-0` :384-390; `exit-carrier` :403. Exactly ONE LLM span seeded :345-362 (`chat.completion`, `exit-agent`, openai `gpt-4o-mini`, 41 in / 17 out, `cost_usd 0.000031`); other spans `cost_usd: 0`, tokens 0 :155-160.
- `deploy/compose/e2e-drive.mjs`: metrics block :2460-2572 (explore live-wired :2554-2572), dashboards leg :2767; **`/app/costs` is the SAMPLE-badge positive control** :1250-1256 and is referenced as the control at :1978, :2566, :2827; NO `/app/infra` step.
- `apps/web/src/lib/live-routes.ts:11-32` — `/app/infra` and `/app/costs` both ABSENT. `live-routes.test.ts:91-96` `"/app/costs stays unwired — the drive's positive control (D205)"` — goes RED on the flip; `:114` `isProductChromeRoute("/app/costs") === false` (unchanged).

**D367 precedent to copy** — `apps/web/src/app/app/services/page.tsx:1-29` (`if (dataMode !== "live") return <ServicesMock />; await connection(); const session = await getSessionContext(); if (!session) redirect("/login"); return <ServicesLive list={await listServices(forWorkspace(session.workspaceId))} />;`), pair `components/services/ServicesMock.tsx`/`ServicesLive.tsx` (server component), guard `app/app/services/page.test.ts` (comment-strip :21-29, `assertMockBranchFirst` :42-51, branch pin :53-66, `resolvedImports(...).some(spec => /(^|\/)mock\//.test(spec)) === false` :70-77,:106-110, verbatim-move markers :127-149, `SampleMark` title pin :121-123). Helper `apps/web/src/test-utils/import-specifiers.ts:34-42`. `SampleMark({ title })` at `components/ui/SampleMark.tsx:7` (D362's per-section primitive; call sites `ServiceDetailLive.tsx:220`, `SettingsSuite.tsx:1361`, `NotificationsPanel.tsx:58`, `app/app/page.tsx:57,169`).

**Literals that move or break (S6.3-L3 — enumerated at breakdown, each assigned to a task's owns)**
- `apps/web/src/lib/logs-filter.test.ts:246-258` reads `app/app/infra/page.tsx` SOURCE and asserts the `/app/logs?pod=` deep-link literal and the absence of `/app/logs?q=` / `p.name.split("-")` → T5 owns it (re-point to `InfraMock.tsx` AND `InfraLive.tsx`).
- `apps/web/src/lib/live-routes.test.ts:91-96` positive control → T7 (moves to `/app/ask`, P6).
- `deploy/compose/e2e-drive.mjs:1250-1256, :1978, :2566, :2827` positive control → T7.
- `apps/web/src/components/shell/TourGuide.tsx:97-101` costs step narrates demo facts ("Meridian costs $84/mo against $299 revenue… draft_reply is 44%") — D404 class; NO guard exists in `TourGuide.test.ts` (issues/users guards :235-259 are the pattern) → T6.
- `apps/web/src/content/docs/what-obstack-does-not-do/index.mdx:92-94` "The dashboards, trace diff, explore, infra, map, services, users and cost surfaces render sample data and save nothing" — already false since S6.2/S6.3 → T7 rewrites.
- `SideNav.tsx:55,59`, `CommandPalette.tsx:30,35` — hints derive from `live-routes.ts`; no edit needed (guarded `shell-honesty.test.ts:212-217`).
- Costs mock cross-links to `/app/infra` "$31/mo" (:174-178) and `/app/evals` (:232-242) stay VERBATIM in `CostsMock.tsx` (byte-identity); the Live surface carries neither.
- `apps/web/src/app/page.tsx:35` marketing claim "Token cost attribution per request and model" — becomes TRUE in live with T3/T6; no edit.

## Manager's proposed design (for the advisor to rule — decide, amend, or replace; ledger continues at D450)

- **P1 — the collector metrics leg = config + RBAC only, two receivers, one whitelist.**
  - `kubeletstats` in the DaemonSet config (`deploy/collector/config.yaml`, copied byte-for-byte to the chart): `collection_interval: 30s`, `auth_type: serviceAccount`, `endpoint: "https://${env:K8S_NODE_NAME}:10250"`, `insecure_skip_verify: true` (kubelet serving certs are self-signed on kind/most distros — stated in the README), `metric_groups: [node, pod, container]`, explicit `metrics:` enables for exactly: `k8s.node.cpu.usage`, `k8s.node.memory.working_set`, `k8s.node.memory.available`, `k8s.pod.cpu.usage`, `k8s.pod.memory.working_set`, `container.cpu.usage`, `container.memory.working_set` (all GAUGES — no cumulative `.time` counters, so the delta path is never exercised). DaemonSet gets `K8S_NODE_NAME` from `fieldRef: spec.nodeName`. The compose config (`config.compose.yaml`) is untouched (no kubelet).
  - `k8s_cluster` in the singleton (`collector-events-config.yaml` — the cluster-singleton collector already exists for exactly the "once per cluster" reason; the doc rename is "cluster collector: events + cluster metrics"; NO new values flag — simplest implementation, the gate stays `collector.k8sEvents.enabled`, its comment widened): `auth_type: serviceAccount`, `collection_interval: 30s`, `node_conditions_to_report: [Ready, MemoryPressure]`, `allocatable_types_to_report: [cpu, memory]`; kept metrics: `k8s.node.condition_ready`, `k8s.node.condition_memory_pressure`, `k8s.node.allocatable_cpu`, `k8s.node.allocatable_memory`, `k8s.pod.phase`, `k8s.container.restarts`, `k8s.container.cpu_request`, `k8s.container.cpu_limit`, `k8s.container.memory_request`, `k8s.container.memory_limit`.
  - A `filter/k8s` processor (`metrics: include: match_type: strict, metric_names: [<the 17 above>]`) in BOTH metrics pipelines so nothing outside the whitelist ships; a `transform/k8s-resource` processor deleting `k8s.pod.uid`, `k8s.node.uid`, `container.id`, `k8s.pod.start_time` from resource attributes (uid churn per pod incarnation would double series for zero product value; the pod table keys on `(k8s.namespace.name, k8s.pod.name)`). Pipelines: `metrics: receivers [kubeletstats] processors [memory_limiter? no — batch, filter/k8s, transform/k8s-resource] exporters [otlp_http]` (DaemonSet) and `metrics: receivers [k8s_cluster] …` (singleton). Series budget stated in the README: ≈ 3/node + 2/pod + 2/container + 1/pod + 5/container + 4/node → ~10 per pod at one container each → 50 nodes × 30 pods ≈ 15k < 25k cap (packet Q2 :93).
  - RBAC: `clusterrole.yaml` unconditionally adds `nodes`, `nodes/stats`, `nodes/proxy` (`get, list, watch`); behind the existing k8sEvents gate adds what `k8s_cluster` lists/watches (`nodes, namespaces, pods, replicationcontrollers, resourcequotas, services` in core; `apps: daemonsets, deployments, replicasets, statefulsets`; `batch: jobs, cronjobs`; `autoscaling: horizontalpodautoscalers`) — the executor takes the receiver's documented minimal role at v0.158.0, no wider.
  - Done-check is mechanical and REAL: `docker run --rm -v <cfg>:/etc/otelcol/config.yaml otel/opentelemetry-collector-k8s:0.158.0 validate --config=…` for BOTH configs (an unknown metric name under `metrics:` fails validate — that is how the whitelist names are proven against v0.158.0, not assumed); `helm lint` + `helm template` renders; `go test ./internal/mapping -run TestChartFilesAreByteEqual`; the kind proof lives in T7 (P9).
- **P2 — NO ingest change, NO DDL (ClickHouse or Postgres), NO web parity constant change.** The S6.1 store already keeps every resource attribute and merges it into the rollups (D375). A task believing it needs DDL STOPS and escalates (seam trigger, see Q4).
- **P3 — infra contract (`lib/infra-types.ts`, client-safe, D366; the advisor freezes the text; the MANAGER lands it as T0)** and the query module `server/queries/infra.ts` = the first "latest value per group" reader over `metric_points_1m`:
  ```ts
  export const INFRA_STALE_MINUTES = 10;     // a series with no bucket in the last 10m is ABSENT, not "running"
  export const INFRA_RECS_WINDOW_HOURS = 24; // recs basis; named in the UI
  export const INFRA_NODE_CAP = 100; export const INFRA_POD_CAP = 500;  // D402
  export interface InfraNode { name: string; ready: boolean | null; memoryPressure: boolean | null;
    cpuUsageCores: number | null; cpuAllocatableCores: number | null;
    memWorkingSetBytes: number | null; memAllocatableBytes: number | null; podCount: number; lastSeen: string }
  export type PodPhase = "pending" | "running" | "succeeded" | "failed" | "unknown";
  export interface InfraPod { namespace: string; name: string; node: string | null; phase: PodPhase | null;
    restarts: number | null; cpuUsageCores: number | null; cpuLimitCores: number | null;
    memWorkingSetBytes: number | null; memLimitBytes: number | null; lastSeen: string }
  export type RecKind = "memory-limit-oversized" | "memory-near-limit" | "cpu-near-limit";
  export interface RightsizingRec { namespace: string; pod: string; container: string; kind: RecKind;
    observed: number; limit: number; ratio: number }   // NO $ field — by construction (D13: no price input exists)
  export interface InfraSnapshot { nodes: InfraNode[]; totalNodes: number; pods: InfraPod[]; totalPods: number;
    recs: RightsizingRec[]; hasKubeletMetrics: boolean; hasClusterMetrics: boolean; asOf: string | null }
  ```
  - "Latest" = `argMaxMerge(gauge_last)` per `(name, attributes['k8s.node.name'] / ['k8s.namespace.name','k8s.pod.name'] / +['k8s.container.name'])` over buckets `>= now() - 10 MINUTE` in `metric_points_1m` (the S6.1 merge idiom, `metrics.ts:170-185`); every `null` above is "that metric had no fresh series for this key". `podCount` = distinct fresh pods whose `k8s.node.name` matches. `phase` decodes `k8s.pod.phase` 1..5 → pending|running|succeeded|failed|unknown. `hasKubeletMetrics` / `hasClusterMetrics` = any fresh series from each receiver's name set — they drive the D13 states (P4). No `service` predicate anywhere (k8s metrics carry none).
  - Recs over 24h from `metric_points_1m` `gauge_max` per container vs the container's latest limit, ONLY where the limit metric exists: `memory-limit-oversized` when max working_set < 40% of `memory_limit`; `memory-near-limit` when > 90%; `cpu-near-limit` when max `container.cpu.usage` > 85% of `cpu_limit`. Thresholds are constants in the module and printed in the rec sentence ("peaked at 18% of its 512 MiB limit over 24h"). Nothing about $, nothing about "30d of traces".
  - House test pair: `infra.test.ts` hermetic (SQL text: every statement reads `metric_points_1m`, is scope-bound, has no `service` predicate, honours the caps; decode table; threshold arithmetic incl. limit=0 ⇒ no rec) + `infra.integration.test.ts` (seeds through the ingest user so the 1m MV fires — `services.integration.test.ts:15-18` idiom; two workspaces, SAME node/pod names; stale series absent; empty-service path; k8s_cluster-only-missing ⇒ `hasClusterMetrics=false` with null limits).
- **P4 — `/app/infra` live = D367 pair `components/infra/InfraMock.tsx` (verbatim move of today's body) + `InfraLive.tsx` (server component, props `{ snapshot: InfraSnapshot }`)**, page branch per the services precedent; guard `app/app/infra/page.test.ts` (D448 helper). D13 states, in order: (a) `!hasKubeletMetrics && !hasClusterMetrics` → ONE empty state: "No cluster metrics yet — the obstack-collector chart ships node and pod metrics from this release; install or upgrade it" + link to the helm-chart doc; no tables. (b) `hasKubeletMetrics && !hasClusterMetrics` → tables render; `ready`/`allocatable`/`phase`/`restarts`/`limit` cells show `—` and ONE per-section note names the missing leg ("limits, restarts and readiness arrive from the cluster collector (`collector.k8sEvents.enabled`)"); recs panel says recommendations need container limits. (c) both → full render; recs panel with the 24h basis named; empty recs ⇒ "nothing to right-size in the last 24h" (never hidden). Header = real counts ("N nodes · M pods · as of HH:MM"), no cluster name, no provider, no "synced via" fiction. Pod deep link keeps `/app/logs?pod=<name>` (a real join: `logs.k8s_pod`). Caps sentence per D402 ("showing 100 of N nodes"). No `SampleMark` anywhere on the live infra page — nothing on it is sample.
- **P5 — costs contract (`lib/costs-types.ts`, T0) + `server/queries/costs.ts`** over `spans WHERE layer = 'llm'`:
  ```ts
  export type CostsRange = "24h" | "7d" | "30d";   // spans TTL is 30d — the picker cannot exceed it
  export const COSTS_GROUP_CAP = 10;               // D402
  export interface CostsTotals { costUsd: number; inputTokens: number; outputTokens: number; calls: number;
    unpricedCalls: number; unpricedModels: string[] }   // unpriced = tokens > 0 AND cost_usd = 0 (ingest's "no price row" sentinel)
  export interface ModelCost { model: string; system: string; costUsd: number; inputTokens: number; outputTokens: number; calls: number; unpriced: boolean }
  export interface ServiceCost { service: string; costUsd: number; calls: number; models: string[] }
  export interface CostsPoint { t: string; costUsd: number; calls: number }
  export interface CostsReport { range: CostsRange; totals: CostsTotals; byModel: ModelCost[]; totalModels: number;
    byService: ServiceCost[]; totalServices: number; series: CostsPoint[] }
  ```
  - Model identity = `if(gen_ai_response_model != '', gen_ai_response_model, gen_ai_request_model)` (the `adapters.ts:167` idiom; `services.ts:51` collects both — costs attributes spend to ONE). Buckets 24h→1h, 7d→6h, 30d→1d (`overview.ts:30-33` shape; fixed grid, `costUsd: 0` for an empty bucket IS the truth here — no calls means no cost — unlike metric gaps). Range is a URL param with the S6.1 explore deep-link validation idiom (invalid ⇒ default `24h`, never an error). ABSENT in live by construction: per-customer table, revenue, margin, infra $, "AI vs infra split", per-feature breakdown (no feature dimension on llm spans — fenced to a later run; the mock's `featureCosts` has no honest counterpart), forecasts. `unpricedCalls > 0` renders its own stat + a per-model "unpriced" mark and ONE sentence ("N calls across [models] carry no price — add a workspace price override (docs link)"); an unpriced model's cost cell is `—`, never `$0.00`.
  - Reconciliation (integrator asserts): costs `24h` `totals.costUsd` equals the overview's 24h `LLM cost` on the seeded workspace (different tables — `spans` vs `trace_summaries` — same sum), and equals `sum(cost_usd)` by direct SELECT (S1.8 acceptance).
  - House test pair (`costs.test.ts` hermetic incl. "every statement names `layer = 'llm'`", unpriced predicate text, range map, caps; `costs.integration.test.ts` two workspaces SAME model/service names; an unpriced row; cap with 11 models ⇒ `totalModels = 11`).
- **P6 — flip both; the drive's SAMPLE-badge positive control moves from `/app/costs` to `/app/ask`** (M7 capstone — the route that stays unwired longest; one move now instead of one per M6 sprint). `live-routes.test.ts:91-96` rewritten to `/app/ask` with the D205 comment updated; the four drive sites (:1250-1256, :1978, :2566, :2827) re-pointed.
- **P7 — tour**: the costs step body becomes mode-agnostic (D404) — names what the page shows (spend by model and service, unpriced calls named), no customer names, no dollar figures; guard added to `TourGuide.test.ts` on the issues/users pattern. No infra tour step is added (none exists; outside the title).
- **P8 — docs**: `what-obstack-does-not-do/index.mdx:92-94` rewritten to the post-S6.4 truth (what STILL renders sample in live: the services "recent deploys" panel and the overview top-failing table; dashboards DO persist since S6.3); `deploy/collector/README.md` gains "Kubernetes metrics" (receivers, RBAC, series budget, the kubelet TLS note, what `/app/infra` reads, and that the events Deployment is now the cluster collector); the helm-chart doc page gets one paragraph.
- **P9 — the "workspace running the collector" proof is the kind acceptance, not the compose drive**: `acceptance.sh` gains a step after install — poll the port-forwarded ClickHouse (up to 180s) until `SELECT countDistinct(attributes['k8s.node.name']) FROM obstack.metric_points_1m WHERE name = 'k8s.node.cpu.usage'` ≥ 1 AND `k8s.container.memory_limit` has a fresh row for the demo pod's container (the demo Deployment sets limits per D283) — that is the live kubelet + live API server writing through the DaemonSet and the singleton into the S6.1 store, in CI (`stack.yml`). Web in kind has no session, so the page render is proven by the drive (P10) on shaped seeds; the two together are the exit evidence.
- **P10 — compose drive**: `exit-seed.mjs` gains a k8s leg posting the 17 whitelisted names with kubeletstats/k8s_cluster-shaped resource attrs to `/v1/metrics` (2 nodes `exit-node`/`exit-node-2`; 3 pods in `exit-ns` incl. `exit-app-1` — the SAME pod name the logs leg already uses, so the pod→logs deep link is a real join; one container at 20% of its memory limit ⇒ `memory-limit-oversized`, one at 95% ⇒ `memory-near-limit`, one with `restarts = 5` and `phase = pending`; one pod ONLY in kubeletstats names ⇒ null limits cells) plus ONE unpriced LLM span (`gen_ai_request_model: "exit-custom-ft"`, tokens 12/3, `cost_usd: 0`) — the drive asserts `/app/infra` live: no SAMPLE badge, the `<h1>`, `exit-node-2` present, the `exit-app-1` row with its logs link, the oversized rec sentence with its percentage, bob's workspace shows the empty state (tenancy); `/app/costs` live: no SAMPLE, `gpt-4o-mini` row, total text equals the seeded cost, `exit-custom-ft` marked unpriced with `—` not `$0.00`, "Meridian"/"$412"/"margin" ABSENT, bob empty; both arms proven RED first (the pre-flip run shows the badge). Pre-existing drive counts (225/0) stated and unchanged apart from the new steps.

## Packet questions for the advisor (rule each; the manager applies the rulings before wave 1)
- **Q1 — receiver placement and gating** (P1): k8s_cluster rides the existing singleton Deployment under `k8sEvents.enabled` with no new flag, versus its own Deployment/flag. Manager recommends riding it (simplest; the singleton rationale is identical).
- **Q2 — kubelet TLS**: `insecure_skip_verify: true` stated in the README, versus mounting the cluster CA. Manager recommends skip-verify (kind and most managed distros serve self-signed kubelet certs; the connection is node-local).
- **Q3 — resource-attr scrubbing**: drop `k8s.pod.uid`/`k8s.node.uid`/`container.id`/`k8s.pod.start_time` via `transform` (P1), versus keeping the wire verbatim. Manager recommends dropping — series stability and the cap budget; the product keys on names.
- **Q4 — the S6.5 seam**: keep costs in S6.4 (T3 + T6, zero dependency on T1; total 7 tasks + T0, ≈ S6.3 class) versus split now. Manager recommends KEEP; trigger to split mid-run = T1 needs anything beyond config + RBAC (an image change, an ingest change, or DDL).
- **Q5 — executor tiers** (meta): T1/T2/T3/T5/T7 Opus, T4/T6 Sonnet.
- **Q6 — evidence split** (P9/P10): kind acceptance proves the collector→store leg on a live cluster; the compose drive proves the pages on shaped seeds. Confirm this is the exit evidence, or require a kind-side page render (would need a session in kind — not built).
- **Q7 — recs thresholds and window** (P3): 40% / 90% / 85% over 24h `gauge_max`; state the ruling as a RESULT the drive can read.

## Advisor decisions (BINDING — advisor kickoff 2026-09-01, verified against `s6-4` @ 4f7d94d; ledger D450–D469; every ruling is a RESULT a reviewer or the drive reads)

**Verification deltas that changed the design (►):**
- ► The pinned distro names the receiver **`kubelet_stats`** (not `kubeletstats`) — house spelling like `file_log`/`k8s_attributes`/`otlp_http`. Processors present: `filter`, `resource`, `transform`, `memory_limiter`, `metrics_transform`, `resource_detection`, `attributes`, `batch`.
- ► Upstream `kubeletstatsreceiver/internal/kubelet/accumulator.go` @ v0.158.0: `SetK8sNodeName` is called for NODE resources only (:67,:76); pod/container series carry **NO `k8s.node.name`** on the wire → the DaemonSet stamps it (D453).
- ► `k8sclusterreceiver` @ v0.158.0: `k8s.node.condition_*` / `k8s.node.allocatable_*` are DYNAMIC names from `node_conditions_to_report` / `allocatable_types_to_report` (`internal/node/nodes.go:196,222`) — NOT enumerable under `metrics:`, so `validate` cannot prove those 4; they are proven emitted in kind (D466). `k8s.pod.phase` (metadata :877), `k8s.container.restarts` (:609), `k8s.container.{cpu,memory}_{request,limit}` (:553-600) ARE metadata metrics, default-enabled. All 7 kubelet names exist as gauges, default-enabled (kubeletstats metadata :121,:193,:279,:311,:351,:441,:537).
- Price table path is `services/ingest/pricing/prices.json`; no zero-priced model exists → the unpriced predicate is sound. NO pricing-override doc exists under `content/docs`; `pricing_overrides` (pg 0005) has no UI → the unpriced sentence links to the not-do doc (D461). `format.ts` has no bytes/cores formatter (D468). Series cache "active" = seen within the current UTC day (`metrics.go:432-445`) — churn counts per day; budget stands.

- **D450 — Collector leg = config + RBAC + docs only; two receivers, seventeen names, no image change.** DaemonSet (`deploy/collector/config.yaml`, byte-copied to the chart, K1) gains receiver `kubelet_stats`; the cluster singleton gains `k8s_cluster`. RESULT: `validate` exits 0 on both configs at v0.158.0; in kind, `SELECT DISTINCT name FROM obstack.metric_points_1m WHERE name LIKE 'k8s.%' OR name LIKE 'container.%'` is a non-empty subset of the 17 names and nothing else. Whitelist (frozen; the same literals are `KUBELET_METRICS`/`CLUSTER_METRICS` in `lib/infra-types.ts`): kubelet_stats (7 gauges) `k8s.node.cpu.usage` `{cpu}`, `k8s.node.memory.working_set` By, `k8s.node.memory.available` By, `k8s.pod.cpu.usage`, `k8s.pod.memory.working_set`, `container.cpu.usage`, `container.memory.working_set`; k8s_cluster (10 gauges) `k8s.node.condition_ready`, `k8s.node.condition_memory_pressure` (1/0/-1), `k8s.node.allocatable_cpu`, `k8s.node.allocatable_memory`, `k8s.pod.phase` (1..5), `k8s.container.restarts`, `k8s.container.cpu_request`, `k8s.container.cpu_limit`, `k8s.container.memory_request`, `k8s.container.memory_limit`. Receiver settings (frozen): kubelet_stats `collection_interval: 30s`, `auth_type: serviceAccount`, `endpoint: "https://${env:K8S_NODE_IP}:10250"`, `insecure_skip_verify: true`, `metric_groups: [node, pod, container]`, `metrics:` enabling exactly the 7 (validate proves the names); k8s_cluster `auth_type: serviceAccount`, `collection_interval: 30s`, `node_conditions_to_report: [Ready, MemoryPressure]`, `allocatable_types_to_report: [cpu, memory]`, `metrics:` enabling exactly its 6 metadata names. Both metrics pipelines: `filter/k8s` (whitelist) → `resource/k8s` (D453) → `batch`. **Proof of the 4 dynamic names (T1's result MUST include):** (i) a deliberately misspelled name under `metrics:` fails `validate` once (transcript kept); (ii) the executor quotes `receiver/k8sclusterreceiver/internal/node/nodes.go:196,222` at tag v0.158.0 from the raw file (curl — no guessing); (iii) kind (D466) proves them emitted. README series budget: ≈ 3/node + 4/node + 2/pod + 1/pod + 2/container + 5/container ≈ 10 per single-container pod; 50 nodes × 30 pods ≈ 15k < 25k; churn counts per UTC day.
- **D451 (Q1) — The singleton becomes the cluster collector; values key `collector.k8sEvents` → `collector.cluster`, no alias.** Files/templates/objects/labels rename `-collector-events` → `-collector-cluster`, `component: collector-cluster`; `files/collector-events-config.yaml` → `files/collector-cluster-config.yaml`; README section "The cluster-events collector" → "The cluster collector (events + cluster metrics)"; Chart 0.5.0 → 0.6.0 with an "Upgrading" note ("`collector.k8sEvents.*` is now `collector.cluster.*`"). RESULT: `helm template x deploy/helm/obstack --set collector.cluster.enabled=false` renders no cluster Deployment/ConfigMap and no `k8s_cluster` RBAC; `grep -rn k8sEvents deploy/` returns nothing. Rationale: the flag is named in product copy (D459) and docs forever; a flag called "events" gating readiness/limits/phase is a permanent misnomer; pre-GA chart, no alias debt.
- **D452 (Q2) — kubelet via node IP, TLS skip-verify, stated in the README.** DaemonSet env gains `K8S_NODE_IP` (`fieldRef: status.hostIP`) for the endpoint AND `K8S_NODE_NAME` (`fieldRef: spec.nodeName`) for D453. RESULT: `helm template … | grep -c 'K8S_NODE_IP\|K8S_NODE_NAME'` ≥ 2. Rationale: node names are not DNS-resolvable from pods on every distro; the host IP always is; node-local connection; kubelet serving certs are self-signed on kind and most managed distros.
- **D453 (Q3) — `resource/k8s` processor: stamp + scrub.** DaemonSet: `insert k8s.node.name = ${env:K8S_NODE_NAME}` (insert = only when absent), `delete k8s.pod.uid`, `delete container.id`. Cluster: `delete k8s.pod.uid`, `delete k8s.node.uid`, `delete container.id`, `delete container.image.name`, `delete container.image.tag`. RESULT: every stored k8s series' `attributes` has `k8s.node.name`; none has `k8s.pod.uid` / `container.id` / `container.image.*`.
- **D454 — RBAC.** Unconditional (DaemonSet): `nodes/stats` get, `nodes/proxy` get. Behind `collector.cluster.enabled`: the k8sclusterreceiver README's ClusterRole at v0.158.0 verbatim MINUS the dead `extensions` group MINUS `events` (already granted under the same gate): core `namespaces, namespaces/status, nodes, nodes/spec, persistentvolumes, persistentvolumeclaims, pods, pods/status, replicationcontrollers, replicationcontrollers/status, resourcequotas, services`; `discovery.k8s.io: endpointslices`; `apps: daemonsets, deployments, replicasets, statefulsets`; `batch: jobs, cronjobs`; `autoscaling: horizontalpodautoscalers` — all `get, list, watch`. The `clusterrole.yaml` header's "replicasets deliberately absent" paragraph is rewritten (now present, gated, for the cluster receiver's informers). RESULT (kind, D466): `kubectl logs deploy/obstack-collector-cluster` has zero lines matching `forbidden|Failed to watch|Failed to list`.
- **D455 (P2, Q4) — NO DDL, NO ingest change, NO image change, NO web parity change. Seam trigger (either direction):** T1 needing an image/ingest/DDL change → costs splits to S6.5 and this run keeps infra; T2 proving the latest-per-group read cannot be correct from `metric_points_1m` without a new MV/table → infra (T1/T2/T4-k8s/T5) splits to S6.5 and this run keeps costs. A task believing either STOPS and escalates; it never lands DDL.
- **D456 — `apps/web/src/lib/infra-types.ts` frozen text = the file landed by T0 (commit on `s6-4`); executors read the file, not a copy.** Key constants: `INFRA_STALE_MINUTES = 10`, `INFRA_RECS_WINDOW_HOURS = 24`, `INFRA_RECS_MIN_OBSERVED_HOURS = 12`, caps 100/500/50, thresholds 0.4 / 0.9 / 0.85, `KUBELET_METRICS` (7), `CLUSTER_METRICS` (10); types `InfraNode` (adds `memAvailableBytes`), `PodPhase`, `InfraPod` (limit completeness rule), `RecKind`, `RightsizingRec {peak, limit, ratio, observedHours}` (no $ field by construction), `InfraSnapshot {…, totalRecs, hasKubeletMetrics, hasClusterMetrics, asOf}`.
- **D457 — `queryInfraSnapshot(ch: ScopedClickHouse): Promise<InfraSnapshot>` results.** Reads `metric_points_1m` only; exactly TWO statements: (1) one row per fresh series (`bucket >= now() - 10 MINUTE`, `name IN {names:Array(String)}` bound from the two constant lists, per-series `argMaxMerge(gauge_last)`, `anyLast(attributes)`, `max(bucket)`); (2) one row per container series of `container.memory.working_set` / `container.cpu.usage` over 24h with `max(gauge_max)`, `min(bucket)`, `max(bucket)`. Assembly, caps and totals in TypeScript (row count bounded by the 25k cap). No `FINAL`, no bare SELECT, no `service` predicate (hermetic: `!/\bservice\b\s*(=|IN)/.test(sql)`), `{workspace_id:String}` in both. Nodes sorted by name; pods by `memWorkingSetBytes` desc (nulls last) then `namespace/name`; recs by kind order near-limit (ratio desc) then oversized (ratio asc). `podCount` counts fresh `k8s.pod.memory.working_set` series per node. Pod-level `cpuLimitCores`/`memLimitBytes` = sum of container limits, non-null ONLY when every container with fresh usage also has a fresh limit. Integration pair proves: two workspaces SAME node/pod/container names; a 30-min-old series ABSENT; kubelet-only ⇒ `hasClusterMetrics=false`, all limit/phase/restart fields null; cluster-only ⇒ `hasKubeletMetrics=false`, usage null; limit 0 ⇒ no rec; incomplete pod limits ⇒ pod-level limit null.
- **D458 (Q7) — Recs as results.** Per container, over the last 24h of `gauge_max` against the container's LATEST limit, only where that limit has a fresh series: `memory-limit-oversized` = peak working set < 40% of memory_limit AND `observedHours ≥ 12` — sentence `` `<ns>/<pod>/<container>` peaked at <R>% of its <fmtBytes(limit)> memory limit over the last <H>h — the limit is oversized``; `memory-near-limit` = peak > 90% — ``… peaked at <R>% of its <fmtBytes(limit)> memory limit over the last <H>h — near its limit``; `cpu-near-limit` = peak `container.cpu.usage` > 85% of cpu_limit — ``… peaked at <R>% of its <fmtCores(limit)> CPU limit over the last <H>h — near its limit``. `R = Math.round(ratio*100)`, `H = Math.round(observedHours)`. Panel basis line (always): "Peak usage against each container's current limit over the last 24h; a limit is called oversized only after 12h of observation. No prices — obstack has no price input for compute." Empty: "Nothing to right-size in the last 24h."
- **D459 (P4) — `/app/infra` live: FOUR D13 states, exact sentences.** (a) `!hasKubeletMetrics && !hasClusterMetrics`: `<h1>Infrastructure</h1>` + "No cluster metrics yet. The obstack-collector chart ships node and pod metrics from chart 0.6.0 — install or upgrade it, and this page fills in from the kubelet and the cluster API." + link text "Helm chart docs" → `/app/docs/self-hosting/helm-chart`; no tables, no recs panel. (b) kubelet only: tables render; one note above them: "Readiness, pod phase, restarts, requests and limits come from the cluster collector (`collector.cluster.enabled`), which is off or not yet reporting — those cells show — until it does."; recs body: "Right-sizing needs container limits from the cluster collector." (c) both: full render; recs per D458. (d) cluster only: tables render; note: "CPU and memory usage come from the node collector (the DaemonSet's `kubelet_stats` receiver), which is not reporting — check its `nodes/stats` access; usage cells show — until it does."; recs body: "Right-sizing needs usage from the node collector." Header: "`<totalNodes>` nodes · `<totalPods>` pods · as of `<fmtClock(asOf)>`" — no cluster name, provider, or "synced via". Node card: name, ready dot (grey when null), `podCount` pods, cpu/mem bars as % of allocatable only when allocatable is non-null, else absolute. Pod table columns: pod (`ns/name`), node, phase, restarts (label "restarts", no window), memory (working set, "/ limit · R%" only when `memLimitBytes` non-null), cpu (same), logs → `` `/app/logs?pod=${encodeURIComponent(p.name)}` ``. No "age" column. Cap sentences only when truncated: "showing 100 of N nodes" / "showing 500 of N pods" / "showing 50 of N recommendations". No `SampleMark` on Live; no `$` anywhere on Live (guard: `/\$\d/` absent in `InfraLive.tsx` source).
- **D460 — `apps/web/src/lib/costs-types.ts` frozen text = the file landed by T0.** `COSTS_RANGES = ["24h","7d","30d"]`, `DEFAULT_COSTS_RANGE = "24h"`, `COSTS_GROUP_CAP = 10`; `CostsTotals {costUsd, inputTokens, outputTokens, calls, unpricedCalls}`, `ModelCost {model, system, costUsd, inputTokens, outputTokens, calls, unpricedCalls}`, `ServiceCost {service, costUsd, calls, unpricedCalls, modelCount}`, `UnpricedModel {model, calls}`, `CostsPoint {t, costUsd, calls}` (empty bucket = 0/0 — no calls IS no cost), `CostsReport {range, totals, byModel, totalModels, byService, totalServices, unpricedModels, totalUnpricedModels, series}`. Unpriced = `input_tokens + output_tokens > 0 AND cost_usd = 0`.
- **D461 — Unpriced honesty.** `queryCosts(ch, range)`: every statement names `layer = 'llm'` (hermetic) and `{workspace_id:String}`; `byModel` sorted cost desc then calls desc; `unpricedModels` is its own list (an unpriced model has cost 0 and would fall out of the top 10). Live: a model row whose `unpricedCalls === calls` shows `—` in the cost cell; a mixed row shows the priced sum plus "(N unpriced)". Stat card "unpriced calls" always present (0 allowed — a count, not a price). When `unpricedCalls > 0`, ONE sentence (frozen): "`<N>` calls across `<M>` models carry no price row, so their cost is unknown — not $0." followed by the `unpricedModels` table (model · calls · cost `—`) and link text "why" → `/app/docs/what-obstack-does-not-do`. T7 adds to that doc: "**obstack does not guess a price** — a model with no row in its price table or your workspace's `pricing_overrides` is counted as unpriced on the cost page, never as $0." Zero calls in range: "No LLM calls in this workspace's traces in the last `<24 hours|7 days|30 days>`." (bob's proof). Model `""` renders `unknown`; service `""` renders `(no service)`.
- **D462 — Range picker (D394 exception: spend is a monthly unit).** `?range=` parsed by `parseCostsRange(raw: string | undefined): CostsRange` (invalid ⇒ `"24h"`, never an error), read AFTER the mock branch. Buckets: 24h→1h (24 points), 7d→6h (28), 30d→1d (30). Picker = three links marked by `aria-current`. Reconciliation (integrator): `queryCosts(ws,"24h").totals.costUsd` === overview 24h LLM cost === direct `sum(cost_usd)` on the seeded workspace.
- **D463 (P6) — Flip both; positive control moves to `/app/ask`.** `live-routes.test.ts:91-96` → `"/app/ask stays unwired — the drive's positive control (D205/D463)"`; drive :1250-1256, :1978, :2566, :2827 re-pointed, comments updated. RESULT: on the pre-flip tree the new infra/costs arms FAIL on "SAMPLE DATA" present (transcript kept); post-flip green.
- **D464 (P7) — Tour costs step (frozen).** title "Costs — LLM unit economics"; body "Token spend by model, straight from your traces and priced at ingest from the model and token counts. A model with no price row is counted and named as unpriced — never shown as free." `TourGuide.test.ts` guard on the issues/users pattern bans `Meridian`, `$84`, `$299`, `44%`, `draft_reply` from the `/app/costs` step object (comment-stripped source).
- **D465 (P8) — Docs.** `what-obstack-does-not-do/index.mdx:92-94` → "**The services page's recent-deploys panel and the overview's top-failing table render sample data** — every other surface reads the signed-in workspace; dashboards persist since S6.3." + the D461 pricing paragraph. `deploy/collector/README.md` gains "Kubernetes metrics" (both receivers, the 17 names, RBAC, node-IP + skip-verify note, series budget, `/app/infra` reads these); helm README section renamed per D451 + "Upgrading to 0.6.0"; helm-chart docs page: one paragraph.
- **D466 (Q6, P9/P10) — Exit evidence = kind proves collector→store through the REAL module; the compose drive proves the pages.** `deploy/helm/obstack/acceptance.ts` gains subcommand `k8s-metrics <demo_pod>` (it already runs with the web tsconfig and `CLICKHOUSE_URL`): polls ≤180s until `queryInfraSnapshot(forWorkspace(<the dev key's workspace>))` returns `hasKubeletMetrics && hasClusterMetrics`; then asserts: a node whose `name` equals a `kubectl get nodes` name with `cpuAllocatableCores > 0` and `ready === true`; the demo pod present with `phase === "running"`, `memLimitBytes === 167772160` (128Mi + 32Mi — both containers have limits, D457 completeness), `cpuLimitCores === 0.55`, `memWorkingSetBytes > 0`; `recs` contains no `memory-limit-oversized` (observation < 12h — D458 proven negative live). `acceptance.sh` step placed after "firing the demo agent", before the restart rider; also asserts the D450 name-set subset (SQL via `harness`) and the D454 zero-`forbidden` log grep. No page render in kind (no session there).
- **D467 (T4) — Seed shape, post-processor wire, new `--leg k8s`, timestamps at the metrics leg's `METRICS_BUCKET_MS` unless stated.** Resource attrs on pod/container series: `k8s.namespace.name`, `k8s.pod.name`, `k8s.node.name` (+ `k8s.container.name`); node series: `k8s.node.name`; NO `service.name`, NO uid. Nodes: `exit-node` (allocatable 4 / 17179869184; usage 1.2 / 6442450944; available 10737418240; ready 1; pressure 0), `exit-node-2` (2 / 8589934592; 0.4 / 2147483648; 6442450944; 1; 0). Pods in `exit-ns`: (1) `exit-app-1`@exit-node, container `app`: memory_limit 536870912, working_set 107374182 at BOTH now-13h and now-3m ⇒ `memory-limit-oversized` "peaked at 20% of its 512 MiB memory limit"; cpu_limit 0.5, cpu usage 0.1; requests 0.25 / 268435456; restarts 0; phase 2. (2) `exit-worker-1`@exit-node-2, container `worker`: memory_limit 268435456, working_set 255013683 ⇒ `memory-near-limit` (95%); cpu_limit 1, cpu usage 0.9 ⇒ `cpu-near-limit` (90%); restarts 0; phase 2. (3) `exit-crash-1`@exit-node-2, container `app`: phase 1, restarts 5, memory_limit 134217728, working_set 10485760 (ONE fresh point ⇒ NO oversized rec), cpu_limit 0.2, usage 0. (4) `exit-kubelet-only-1`@exit-node, container `app`: kubelet names only ⇒ `—` cells. (5) `exit-stale-1`@exit-node: every point at now-30m ⇒ ABSENT. Header result "2 nodes · 4 pods". Unpriced LLM span in the default leg: `layer llm`, service `exit-agent`, `gen_ai_system "custom"`, `gen_ai_request_model "exit-custom-ft"`, response model `""`, tokens 12/3, `cost_usd 0`; placement chosen so the existing drive stays 225/0 on the T4 tree (that run IS the proof). Costs results the drive reads: total text `$0.000031`, `gpt-4o-mini` row, `exit-custom-ft` in the unpriced table with `—`, the D461 sentence ("1 calls across 1 models carry no price row" — T6 may pluralise to "1 call across 1 model"; pick one, state it in T6's result, the drive asserts that form). `k8sExpectations` exported.
- **D468 — `fmtBytes(n)` / `fmtCores(n)` in `lib/format.ts` (T5).** `fmtBytes`: < 1 MiB → "N KiB"; < 1 GiB → "N MiB" (integer); else "N.N GiB". `fmtCores`: < 1 → "Nm" millicores integer; else cores with up to 2 decimals trimmed. Tests in `format.test.ts`.
- **D469 — Wave-3 drive arms.** Infra (alice): status 200, `<h1>Infrastructure</h1>`, no "SAMPLE DATA", "2 nodes · 4 pods", `exit-node-2`, `exit-app-1` row with `href="/app/logs?pod=exit-app-1"`, the D467 oversized sentence prefix, `exit-crash-1` with "5" restarts and "pending", `exit-kubelet-only-1` row, `exit-stale-1` ABSENT; bob: the D459(a) sentence, no table. Costs (alice): no "SAMPLE DATA", `$0.000031`, `gpt-4o-mini`, `exit-custom-ft`, "Meridian"/"$412"/"margin"/"revenue"/"Stripe" ABSENT; bob: the D461 empty sentence. `/app/ask` carries "SAMPLE DATA". The `--leg k8s` seed runs immediately after the `--leg metrics` step (same accepted-count placement rationale, drive :2462-2471).
- **D470 (T1 escalation) — D450 done-check amended:** the DaemonSet validate gate = the README-documented command (fake SA dir mounted at `/var/run/secrets/kubernetes.io/serviceaccount/{ca.crt,token}`, `K8S_NODE_NAME`/`K8S_NODE_IP` set); the cluster config validates bare. RESULT added: `daemonset.yaml` sets both env vars via `fieldRef` (D452) AND the README's "What config.yaml assumes of its DaemonSet" section names them as required — a DaemonSet missing them fails at boot visibly. nodes.go quote accepted as the D450(ii) proof.
- **D471 (T1 escalation) — D451 RESULT amended:** `grep -rn 'Values.collector.k8sEvents' deploy/` empty AND `grep -rnE '^\s*k8sEvents:' deploy/` empty (`Trace.k8sEvents` is the web facade field; Chart history and the Upgrading sentence legitimately name the retired key).
- **D472 (T3 escalation) — The chart window IS the total's window; no caption may reconcile a mismatch.** `series` covers exactly `[now - N hours, now]` — the same rolling window as `totals` (condition 9 untouched). RESULTS: `series[0].t` = ISO of the window start (not a bucket boundary); later `t`s are whole-bucket boundaries; last slot = current partial bucket; point counts 24h → 25, 7d → 29, 30d → 31 when the start is off a boundary, one fewer when exactly on one (hermetic pin covers both with a frozen clock); integration: a span at `now-(N h - 10 min)` and one at `now-30 min` ⇒ `|Σ series.costUsd − totals.costUsd| < 1e-12` and `Σ series.calls === totals.calls` on all three ranges. **T6 copy (frozen):** headline stat label `LLM cost · last 24 hours` / `last 7 days` / `last 30 days`; chart heading `Spend per hour · last 24 hours` / `Spend per 6 hours · last 7 days` / `Spend per day · last 30 days`; chart footnote (always) `The first and last bars are partial — they cover the window's edges — so the bars sum to the total above.`; range label map for every sentence incl. the D461 empty state: `24h → "24 hours"`, `7d → "7 days"`, `30d → "30 days"`.
- **D473 (T2 sign-off) —** D457 hermetic no-service guard ratified as the stricter `!/\bservice\b/.test(sql)` on both statements (the `(=|IN)` form stays as a second line); cluster-only pods keep non-null limits (vacuous completeness — D459(d) renders limits from the cluster leg alone); node rows from node-level series only (the D453 stamp is a pod→node join key, never a node source; a k8s_cluster-only node row IS D459(d)); single-level `argMaxMerge(gauge_last)` accepted (no regroup across series).
- **D474 (T1 sign-off) —** signed; carry-forward for T7: the kind run is the ONLY place the four `condition_*`/`allocatable_*` names and the D454 zero-`forbidden` log are proven emitted — D466's `k8s-metrics` subcommand + the name-set subset SQL are the exit gate.
- **D475 (T3 sign-off) —** signed; tolerances accepted: clamped-index fold (≤1 bar, only within |δ| of an edge, never a total); DB clock for the window (one clock across overview/services/metrics/costs keeps condition 9 a fact). Binding T6 handoff: the chart renders `series` as returned, never assumes a fixed length; captions per D472; `series[0].t` / last `t` are the partial edges the footnote names.
- **D476 (T5 sign-off) —** signed; header template stays VERBATIM ("1 nodes · 1 pods" accepted — ruled-verbatim beats grammar, no plural branch); pod-table column header renamed `drill` → `logs` (one word in InfraLive.tsx + its pin; the drive asserts the href, not the header) — reviewer applies and re-stamps. House form for remaining returns: done-check counts stated BOTH ways (whole-suite npm test + scoped npx tsx per file).
- **Packet Q1–Q7: all RULED above (Q1→D451, Q2→D452, Q3→D453, Q4→D455, Q5→meta tier line, Q6→D466, Q7→D458). S6.5 seam NOW: none.**

## Critical items (advisor-confirmed; return to the advisor before merge)
- T1 — first metrics leg of the distro + the D451 rename + RBAC breadth; returns with validate transcripts (incl. the deliberate failure), `helm template` diff, the nodes.go quote.
- T2 — first latest-per-series reader over the AMT, completeness rule, four flags, tenancy.
- T3 — tenancy + unpriced predicate + reconciliation.
- T5 — four-state render, byte identity, `$`-absence guard.
- T7 — flip, positive-control move, kind `k8s-metrics` subcommand, RED-first transcripts.

## Tasks

### T0: land the ruled contracts (MANAGER, not an agent — Fable carve-out, two files)
- status: approved
- wave: 0
- depends: advisor ruling
- owns: `apps/web/src/lib/infra-types.ts`, `apps/web/src/lib/costs-types.ts`
- goal: the frozen P3/P5 contract text (as ruled) committed first on `s6-4` so wave 1 compiles against it; zero imports; doc comments name the ruling IDs.
- done-check: `cd apps/web && npx tsc --noEmit -p .` green; both files import nothing.
- result: landed verbatim per D456/D460 (manager, first commit on s6-4); tsc green; 0 imports in both files.

### T1: the collector k8s metrics leg — kubeletstats (DaemonSet) + k8s_cluster (cluster singleton), whitelist, RBAC, docs (critical)
- status: approved (advisor-signed D474)
- wave: 1
- depends: T0 (contract names the metric set)
- owns: `deploy/collector/config.yaml`, `deploy/helm/obstack/files/collector-config.yaml` (byte copy — copy source over it, K1), `deploy/helm/obstack/files/collector-events-config.yaml` → RENAMED `deploy/helm/obstack/files/collector-cluster-config.yaml` (D451), `deploy/helm/obstack/templates/collector/events-configmap.yaml` → `cluster-configmap.yaml`, `deploy/helm/obstack/templates/collector/events-deployment.yaml` → `cluster-deployment.yaml`, `deploy/helm/obstack/templates/collector/daemonset.yaml`, `deploy/helm/obstack/templates/collector/clusterrole.yaml`, `deploy/helm/obstack/values.yaml` (`collector.k8sEvents` → `collector.cluster`, D451), `deploy/helm/obstack/Chart.yaml` (0.5.0 → 0.6.0 + history line), `deploy/collector/config.compose.yaml` (line 12 comment only), `deploy/helm/obstack/acceptance.sh` (lines 214-232 slice ONLY — deployment name + comment), `deploy/helm/obstack/acceptance.ts` (line 145 comment ONLY), `deploy/collector/README.md`, `deploy/helm/obstack/README.md` (sections at :335, :601-690 + "Upgrading to 0.6.0")
- goal: D450–D454 as ruled — both configs carry a `metrics` pipeline (`filter/k8s` → `resource/k8s` → `batch`) with exactly the 17 whitelisted names (the literals in `apps/web/src/lib/infra-types.ts` `KUBELET_METRICS`/`CLUSTER_METRICS`), `K8S_NODE_IP` + `K8S_NODE_NAME` reach the DaemonSet, the singleton is the cluster collector (rename, flag `collector.cluster.enabled`, no alias), the ClusterRole grants what the two receivers need at v0.158.0 and nothing wider, READMEs document the leg (series budget, node-IP + skip-verify note, what `/app/infra` reads). No image change; no ingest change; no compose topology change.
- done-check: `docker run --rm -v "$PWD/deploy/collector/config.yaml:/etc/otelcol/config.yaml" otel/opentelemetry-collector-k8s:0.158.0 validate --config=/etc/otelcol/config.yaml` AND the same for `collector-cluster-config.yaml` both exit 0, with a deliberately misspelled metric name under `metrics:` shown to FAIL once then reverted (transcript in the result); the `nodes.go:196,222` quote at tag v0.158.0 fetched raw; `helm lint deploy/helm/obstack`; `helm template x deploy/helm/obstack | grep -c 'K8S_NODE_IP\|K8S_NODE_NAME'` ≥ 2; `helm template x deploy/helm/obstack --set collector.cluster.enabled=false` renders no cluster Deployment/ConfigMap and no `k8s_cluster` RBAC; `grep -rn k8sEvents deploy/` empty; `cd services/ingest && go test ./internal/mapping -run TestChartFilesAreByteEqual`; `bash -n deploy/helm/obstack/acceptance.sh`.
- result: 54eda3e + review 00d916c (README validate command wrote an empty token; fixed + both real failure errors named). Reviewer re-ran all gates green; filter lists + receivers' metrics keys byte-equal to infra-types.ts; D450/D453/D454 exact; gated ClusterRole = upstream README @ v0.158.0 minus extensions/events; D470/D471 satisfied. Escalations: none.

### T2: infra query module — `server/queries/infra.ts` latest-per-group + recs, house test pair (critical)
- status: approved (advisor-signed D473)
- wave: 1
- depends: T0
- owns: `apps/web/src/server/queries/infra.ts`, `apps/web/src/server/queries/infra.test.ts`, `apps/web/src/server/queries/infra.integration.test.ts`
- goal: `queryInfraSnapshot(ch: ScopedClickHouse): Promise<InfraSnapshot>` per P3 as ruled — reads `metric_points_1m` only via the S6.1 merge idiom, scope-injected `workspace_id`, no `service` predicate, 10-minute freshness, D402 caps with totals, three-state flags, recs by the ruled thresholds over 24h `gauge_max`; hermetic + two-workspace integration proofs (same node/pod names in the foreign workspace; stale series absent; kubelet-only ⇒ null limits, `hasClusterMetrics=false`; limit 0 ⇒ no rec).
- done-check: `cd apps/web && npm test -- src/server/queries/infra.test.ts` green; `OBSTACK_TEST_CLICKHOUSE_URL=… npm test -- src/server/queries/infra.integration.test.ts` green with 0 skipped (the integration file's skip arm carries `assert.ok(!process.env.CI, …)`).
- result: bee96a2 + review 7a1fd2a (WINDOW_NAMES typed from the frozen list; condition 13). Reviewer proved: 2 statements on metric_points_1m, scope-injected, single-level argMaxMerge correct (regroup never leaves the series key), mutations red (service predicate, MINUTE→HOUR, completeness guard, max(gauge_max)); cluster-only pods keep non-null limits (vacuous completeness); node rows from node-level series only, cluster-only node still a row. 18/18 + 8/8, 0 skipped. NOTE: D457's ruled regex misses `AND service != ''`; executor added `!/\bservice\b/` — advisor asked to ratify.

### T3: costs query module — `server/queries/costs.ts` over llm spans, house test pair (critical)
- status: approved (advisor-signed D475)
- wave: 1
- depends: T0
- owns: `apps/web/src/server/queries/costs.ts`, `apps/web/src/server/queries/costs.test.ts`, `apps/web/src/server/queries/costs.integration.test.ts`
- goal: `queryCosts(ch: ScopedClickHouse, range: CostsRange): Promise<CostsReport>` per P5 as ruled — `layer = 'llm'` on every statement, response-model-first identity, the unpriced predicate (`input_tokens + output_tokens > 0 AND cost_usd = 0`), fixed-grid series with the ruled buckets, top-10 by model and by service with pre-truncation totals, `parseCostsRange(raw): CostsRange` (invalid ⇒ `"24h"`); hermetic + two-workspace integration proofs (same model AND service names in the foreign workspace; one unpriced row; 11 models ⇒ `totalModels = 11`; `totals.costUsd` equals direct `sum(cost_usd)`).
- done-check: `cd apps/web && npm test -- src/server/queries/costs.test.ts` green; integration file green with 0 skipped against the local ClickHouse.
- result: a1761cb + review e0af320 (dead test conditional). Reviewer proved: 4 statements all `layer='llm'` + scope-injected ws; totals ride byModel via OVER () and equal full sum under truncation (11 models → 10 rows, totalModels 11, 16.5 incl. dropped model); last grid slot = current partial bucket (a 1-min-old call renders); tenancy same service+model names. 14/14 + 10/10, 0 skipped. D472 fix cc708a6: series window = totals window; six frozen-clock grid cases (25/29/31 | 24/28/30); EDGE workspace Σ bars === totals on all three ranges; re-review APPROVE. Tolerance judged: clamped-index fold moves an edge row ≤1 bar only within |δ| (CH now() − web now) of a window edge; single web-side clock rejected (would desync headline vs overview, condition 9). T6 note: series.length is clock-dependent — render as given.

### T4: harness seed — the k8s metrics leg + the unpriced LLM span in `exit-seed.mjs`
- status: approved
- wave: 1
- depends: T0 (metric names per the ruled whitelist)
- owns: `deploy/compose/exit-seed.mjs`
- goal: D467 VERBATIM — a new `--leg k8s` posting the 17 whitelisted names (gauges, post-processor wire: pod/container series carry `k8s.namespace.name`/`k8s.pod.name`/`k8s.node.name`(+`k8s.container.name`), node series `k8s.node.name`; NO `service.name`, NO uid) for the 2 nodes and 5 pods (4 fresh, `exit-stale-1` at now-30m) with the exact values in D467; plus ONE unpriced LLM span in the DEFAULT leg (`exit-custom-ft`, custom, 12/3 tokens, `cost_usd 0`, `layer llm`, service `exit-agent`); `k8sExpectations` exported; existing metric expectations and counts unchanged.
- done-check: `node deploy/compose/exit-seed.mjs --leg k8s` against the local stack exits 0 and `SELECT countDistinct(attributes['k8s.pod.name']) FROM obstack.metric_points_1m WHERE workspace_id = '<seed ws>' AND name = 'k8s.pod.memory.working_set' AND bucket >= now() - INTERVAL 10 MINUTE` returns 4 (5 without the freshness predicate); the EXISTING `node deploy/compose/e2e-drive.mjs` on the T4 tree stays 225/0 green (proves the unpriced span's placement — S6.1-L1 posture after).
- result: 1b8b003 + review 5c6432c (k8sExpectations gained crashRestarts from the fixture). Reviewer verified D467 verbatim AGAINST THE STORE: 12 resources / 52 series / 53 points; 4 fresh / 5 pods; DISTINCT names = exactly the 17, order-identical to infra-types.ts; 0 uid/container.id/service.name on k8s rows; exit-app-1 spans 13.15h at ratio 0.2; exit-stale-1 30.9 min old; unpriced span placement robust (all drive counts are live SQL). Ops notes for T7: (a) the k8s leg has ~6 min of freshness headroom against INFRA_STALE_MINUTES=10 — the infra arm must run right after the k8s seed; (b) obstack-web was left restarting after a lowercase-path bind-mount incident — recreate from the correctly-cased path before the drive.

### T5: the infra surface — D367 pair, three D13 states, recs, guard (critical)
- status: approved (advisor-signed D476; drill→logs re-stamp ad139ac)
- wave: 2
- depends: T2
- owns: `apps/web/src/app/app/infra/page.tsx`, `apps/web/src/app/app/infra/page.test.ts`, `apps/web/src/components/infra/InfraMock.tsx`, `apps/web/src/components/infra/InfraLive.tsx`, `apps/web/src/lib/logs-filter.test.ts` (lines 246-258 only — re-point via glob `components/infra/*.tsx`: Live MUST contain the `/app/logs?pod=` literal; NEITHER may contain `/app/logs?q=` or `p.name.split("-")`), `apps/web/src/lib/format.ts` + `apps/web/src/lib/format.test.ts` (D468 `fmtBytes`/`fmtCores` — additive)
- goal: D459 VERBATIM — page branches on `dataMode` before the first `await`, mock body moved VERBATIM to `InfraMock.tsx` (rendered DOM byte-identical), `InfraLive` (server component, props `{ snapshot: InfraSnapshot }`) renders the FOUR states with the exact sentences, header with real counts and `fmtClock(asOf)`, node cards, pod table columns as ruled, `—` cells + per-state note, recs per D458 with the basis line, the pod→logs deep link, D402 cap sentences only when truncated; `page.test.ts` on the services guard pattern (branch-first, `resolvedImports` mock ban on the Live file, the four state sentences pinned, no `SampleMark` and no `/\$\d/` in Live source).
- done-check: `cd apps/web && npm test -- src/app/app/infra/page.test.ts src/lib/logs-filter.test.ts src/lib/format.test.ts` green; mock `next build` renders `/app/infra` with a DOM md5 equal to master's (the integrator re-proves on two worktrees).
- result: 700e352, review APPROVE no fixes. All D459/D458 strings character-exact; InfraMock diff vs master = 1 line; byte-identity md5 9e957b6e29a7abb1a079850fe85bca5a both trees; 16 total sabotage mutations red (10 executor + 6 reviewer); D473 renders both directions. Escalated to advisor: n=1 "1 nodes · 1 pods" verbatim-vs-plural; `drill` column header vs D459's "logs". Integrator note: npm test cannot be scoped by argument — plan done-check counts are whole-suite; use scoped npx tsx counts.

### T6: the costs surface — D367 pair, fenced economics, unpriced honesty, tour scrub
- status: approved
- wave: 2
- depends: T3
- owns: `apps/web/src/app/app/costs/page.tsx`, `apps/web/src/app/app/costs/page.test.ts`, `apps/web/src/components/costs/CostsMock.tsx`, `apps/web/src/components/costs/CostsLive.tsx`, `apps/web/src/components/shell/TourGuide.tsx` (the `/app/costs` step body only), `apps/web/src/components/shell/TourGuide.test.ts` (one added guard)
- goal: P5/P7 as ruled — page branches before the first `await`, reads `?range=` through `parseCostsRange`, mock body (incl. `aiTotal`, `marginOf`, the infra/evals cross-links) moved VERBATIM to `CostsMock.tsx`; `CostsLive` (props `{ report: CostsReport }`) renders totals (cost, calls, tokens in/out, unpriced), the range picker (24h/7d/30d as links), spend over time, by-model and by-service tables with D402 sentences, the unpriced mark + sentence, `data-tour="costs"` on the by-model section; NOTHING about customers/revenue/margin/infra/features/forecast; tour step copy mode-agnostic with a `TourGuide.test.ts` guard banning "Meridian", "$84", "$299", "44%" from the costs step.
- done-check: `cd apps/web && npm test -- src/app/app/costs/page.test.ts src/components/shell/TourGuide.test.ts` green; mock `/app/costs` DOM md5 equal to master's.
- result: 9a8c2e7 + review afa91b4 (zero-cost bars visible floors per the IssuesLive Spark precedent; D461 unpriced ternary/mixed-row/stat-card/why-link pinned; cap sentences gated + no series.length assumptions). CSS-bars judged the house server-rendered-chart pattern (IssuesLive precedent). CostsMock diff vs master = 1 line; byte-identity re-proven independently (md5 f581f2d92a11d2ed91c8d6ee0c2da23b both trees, route still static). Whole-suite 712/0/76; scoped 10/10 + 6/6. N=1 sentence one Fizz text node; zero-calls empty state correctly drops the stat row.


### T7: the flip + exit evidence — live-routes, positive control → `/app/ask`, drive arms, kind acceptance, docs (critical)
- status: built (Fable direct, user-directed; CI `stack` kind run on the PR is the remaining exit gate)
- wave: 3
- depends: T1, T4, T5, T6
- owns: `apps/web/src/lib/live-routes.ts`, `apps/web/src/lib/live-routes.test.ts`, `deploy/compose/e2e-drive.mjs`, `deploy/helm/obstack/acceptance.sh` (the new step; T1's rename slice is already landed), `deploy/helm/obstack/acceptance.ts` (new `k8s-metrics <demo_pod>` subcommand, D466), `.github/workflows/stack.yml` (one Diagnose line: `kubectl logs deploy/obstack-collector-cluster --tail=80`), `apps/web/src/content/docs/what-obstack-does-not-do/index.mdx`, `apps/web/src/content/docs/self-hosting/helm-chart/index.mdx`, `deploy/compose/README.md`
- goal: D463/D465/D466/D469 VERBATIM — `/app/infra` and `/app/costs` join `liveWiredRoutes`; the positive control is `/app/ask` in the unit test and all four drive sites; drive arms for infra and costs exactly per D469 (both proven RED on the pre-flip tree, transcript kept); `acceptance.ts k8s-metrics` polls `queryInfraSnapshot` on the live kind cluster and asserts the D466 results, `acceptance.sh` runs it after "firing the demo agent" and before the restart rider plus the name-set-subset SQL and the zero-`forbidden` log grep; docs rewritten per D465 (+ the D461 pricing paragraph); deploy README documents the two arms and `--leg k8s`.
- done-check: `cd apps/web && npm test` green after a mock `next build` (S6.1-L1); `node deploy/compose/e2e-drive.mjs` green with the new step count stated (was 225/0); `bash -n deploy/helm/obstack/acceptance.sh`; `npx tsc --noEmit` over `acceptance.ts` with the web tsconfig; the kind run in CI `stack` green on the PR (the exit gate).
- result: all owned files delivered per D463/D465/D466/D469. RED proof: pre-flip drive (flip stashed, arms present) FAIL(5), both arms red on "SAMPLE DATA" present (infra + costs badge checks, plus bob's two pages), exit 1, transcript kept. Green drive **239/0** (was 225/0). Two drive corrections surfaced by the runs: (1) D469's bare `"margin"` fence word can NEVER pass — Next embeds its built-in not-found page's inline `margin:0` styles in every page's RSC flight payload — so the fence search strips `<script>` blocks first (the words are what a reader sees); (2) the dashboards leg's accepted-unmoved pair raced the 5s metering flush (measured once: both metric legs' 60 points held across a failed-flush retry landed inside the pair, 407→467) — the drive now baselines before the metrics leg and settles deterministically: accepted delta === metricsSent.points_sent + k8sSent.points_sent (53+7=60, exactly) before the before-read, a new check. `k8s-metrics <demo_pod>` subcommand: polls `queryInfraSnapshot` ≤180s for both legs, asserts kubectl-named node (ready===true, allocatable>0), demo pod running with SUMMED limits (167772160 bytes / 0.55 cores — D457, `0.5+0.05===0.55` exact in IEEE754), working set >0, D450 name-set ⊆ the infra-types 17 (condition 13: list imported, never quoted), D458 negative; acceptance.sh step after the D37 assert / before the restart rider + D454 zero-`forbidden` grep; stack.yml Diagnose line added. Docs: not-do D264c→D465 rewrite + D461 paragraph (count 21→22 in frontmatter; mirror.test caught `collector.k8sEvents` naming a removed key — reworded to "the old `k8sEvents` values block"); helm-chart page one 0.6.0 paragraph; compose README two arm bullets + `--leg k8s` block + two stale `/app/costs` control mentions → `/app/ask`. Done-checks: `bash -n` ✓; `tsc --noEmit` strict ✓ (scratch tsconfig mirroring web paths/@types, per T1's method); post-drive `down -v` → `up --wait` → mock `next build` → `npm test` 889 tests 0 fail (55 = local integration skips); drive 239/0. CI kind `stack` on the PR = exit gate, pending push.

## Run-goal conditions (BINDING — advisor-confirmed 2026-09-01 with edits to 3, 7, 10, +12, +13)
1. NO DDL (ClickHouse or Postgres), NO ingest code change, NO collector image change, NO web parity change; a task believing it needs one STOPS and escalates (D455 seam, either direction).
2. Every ClickHouse statement in the two new modules is scope-bound (`workspace_id` injected by `forWorkspace`, never caller-supplied), proven hermetically AND by the two-workspace integration pair with the SAME names in the foreign workspace (S6.2-L2).
3. D13 everywhere: a missing metric is `null` → `—` with the missing leg NAMED; FOUR infra states per D459 (cluster-only included); an unpriced model is `—` with "unpriced" named, never `$0.00`; live never falls back to mock; zero data renders the ruled empty state.
4. D362 verbatim: no infra $, revenue, margin, per-customer, per-feature, forecast on the live costs page; no $ anywhere on the live infra page; no `SampleMark` stands in for a fenced figure — the figure is ABSENT.
5. D367 shape on `/app/infra` and `/app/costs`; byte-identity of both mock routes proven by the integrator's two-worktree `next build` DOM md5 (S6.2 normalisation).
6. D21 flip = exit, asserted in CI against the NEW positive control `/app/ask`; the drive arms are proven RED first (S6.3 condition 7) and pre-existing counts stated.
7. validate + helm template are T1's gate; the kind proof is `queryInfraSnapshot` on a live cluster (D466) plus name-set ⊆ whitelist and zero `forbidden` — the `stack` workflow green on the PR is the exit gate.
8. S6.1-L1 harness posture (mock `next build` before `npm test` after a drive; `plans.free` restored).
9. D462 reconciliation: costs 24h total === overview 24h LLM cost === direct `sum(cost_usd)` on the seeded workspace (integrator).
10. Scope fence: no metrics for non-k8s hosts (`hostmetrics` stays off), no node/pod detail routes, no right-sizing $ or "apply" actions, no per-workspace price-override UI, no costs alerts/budgets; no image tag/uid attributes stored (D453); no `age` column; the unpriced sentence links only to the not-do doc (D461).
11. Source-text guards use `resolvedImports` and enumerate files by glob (S6.3-L1).
12. `collector.k8sEvents` is gone from the chart with no alias; Chart 0.6.0 with the Upgrading note (D451).
13. Every metric name literal lives in `lib/infra-types.ts`; T1's config, T2's SQL and the kind subcommand read/quote that list; the mjs seed's duplicate is checked against it by the integrator (`node -e` diff).

## Escalations log
- T3 — rolling `now()-N h` window (condition 9 parity with overview) vs whole-bucket grid: totals can exceed the chart's visible sum by one partial bucket (≤1h at 24h, up to ~24h on 30d) → RULED D472: series window = totals window (T3 returned for the fix + two pins; T6 copy frozen).
- T1 — D450 validate gate unreachable off-cluster (SA files read at receiver-create; null env) → advisor asked to amend (fake-SA documented command); D451 `grep k8sEvents` RESULT impossible (web facade field `Trace.k8sEvents`, history line, Upgrading sentence) → RULED D470/D471 (both ratified as proposed; D470 adds the README 'assumes of its DaemonSet' RESULT — T1 reviewer to confirm the section names both env vars as required).

## Integration
- verdict: pending
- rounds: 0
- e2e:
- seam findings:
- fix tasks:

## Goal check (Step 7, one entry per iteration)

## Retro (filled at end of run)
