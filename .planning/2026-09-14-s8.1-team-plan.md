# Team Plan — obstack M7 sprint 1, pulled forward (S8.1): the MCP server — a read-only, key-scoped `/mcp` over the query layer, the agent-driven setup surface (recipes, one gated mint, the arrival check, one prompt), the `/app/mcp` flip and the agent docs; mock mode byte-identical

meta:
- date: 2026-09-14
- repo/branch: `s8-1` (cut from master @ `02bf550`, the S7.4 close merge)
- mode: **Fable-direct, session-wide** — every task executed in the main session, sequentially in wave order; adversarial review lenses spawned read-only per task where the plan says so. The per-task `model:` lines record what a routing policy WOULD have chosen, for the retro's calibration only.
- iteration-limit: 3
- iteration: 0/3
- governing docs (binding, read before executing): `.planning/2026-09-14-s8.1-mcp-packet.md` (PROCEEDING AS DRAFTED on the user's "continue", D640–D670 — implemented VERBATIM; deviations escalate, never smoothed); the M6 phase plan §7 standing constraints; the S7.4 team plan's carried rules (no build overlaps a test gate; a worked example is computed, not typed; a red check that ran for 3 s is read before it is believed).

## Kickoff notes

- **Ruling posture, stated so nobody mistakes it for a signature:** the packet's calls (a)–(l) were not put individually. The user directed the two that change what gets built — `mcp` pulled forward ahead of S7.5 (a), and the setup surface IN (j)/(k)/(l), which prices the sprint at L — and answered the packet as a whole with "continue". The rest proceed AS DRAFTED under the S7.4 "as drafted ratifies all" convention, put with that reading stated in the session. Any call the user flips re-opens exactly its task; nothing else moves.
- Seams surfaced by the breakdown are numbered from **D671**. D598–D627 belong to the S7.5 draft; D680–D700 to the pilot packet.
- **The box has no Docker this sprint.** Docker Desktop's content store returns I/O errors and the engine did not come back after a relaunch (S7.4 close). Consequence, stated once: every gate that needs a store — the web integration legs, the Go integration packages, the compose stack, the drive — runs in CI on the PR, not here; the local gates are the unit suites, `tsc`, lint, `next build`, and the two-build byte-identity (Node only). The drive's "foreground, full stop" rule (S7.2/S7.3) cannot apply to a drive that cannot start; CI's `e2e` job is the drive's gate, and each push is therefore a deliberate act, not a save.
- **T2's first act** (packet condition 5): confirm the SDK 2.0.0 export names against the installed package before a line of the route exists — `WebStandardStreamableHTTPServerTransport`, `sessionIdGenerator`, `enableJsonResponse`, `registerTool`, `registerPrompt`, the client's `StreamableHTTPClientTransport` and its custom-`fetch` option (the protocol tests inject the route handler as `fetch`, so no listener is needed). A name that does not exist is a plan correction under the A-rule, recorded, never smoothed.
- **The chart is NOT touched here.** D653's `OBSTACK_PUBLIC_MCP_ENDPOINT` line on the web Deployment belongs to the pilot packet's 0.7.0 bump (§12) — one bump per sprint (D268), and that bump has six other items waiting. This sprint's loopback default is the true address for every path the drive and compose run.
- **Number collisions, named:** `0014` is taken by this sprint (the S7.5 draft renumbers to `0015` at its own breakdown); `live-routes.ts:32` gains `/app/mcp` (the S7.5 draft's `/app/oncall` lands one line later when it comes).
- Lessons carried in as plan items: the flip's four sentences are done-check items of T5 (S7.4 T7); the drive arm is withheld whole under one hook and K equals its check count (D564); the sha pin names the pre-flip COMMIT (S7.3 lesson); no build overlaps a test gate (S7.4 retro).

## Critical items (advisor-return before merge)
- T1 — the scope through BOTH doors: `0014` double-applied raw and through the runner; a `read` key and a `setup` key each 401 at ingest, an `ingest` key and a `setup` key each refused by the web resolve for `read`-only paths — every arm proven red-then-green on real Postgres (CI).
- T3 — the endpoint's refusal matrix proven with a REAL protocol client against the route: unknown/revoked/ingest-scoped/malformed all the same 401 with the challenge header; 429 with `Retry-After` on the boundary; 404 in mock mode; an initialised session listing exactly the registry's tools.
- T4 — tenancy RED per tool: every id-taking tool called across workspaces answers the not-found sentence, every list tool answers one workspace, each first proven red by a deliberately unscoped handler in the test's scratch; `issue_ingest_key` refused for `read` with the exact sentence and honoured for `setup` with an `ingest`-scoped, `mcp:`-prefixed row.
- T5 — the flip: the sha pin against `02bf550`, the registry entry, the tour step, the absences paragraph with its registry-coupled pin, the docs page from the same constant as the prompt, the mock `mock/` diff EMPTY.
- T6 — the drive: the arm's proven-red run (K = its check count), the two doors, the cross-tenant probe, the arrival flip for the minted key alone, the unauth probe entry.

## Tasks

### T1: `0014_api_key_scope.sql`, the scope through both doors, and the settings picker
- status: done locally; the Postgres arms owed to CI (see result)
- wave: 1 · depends: — · model: Sonnet (DDL in the 0012 house style; Go and web changes in named precedents' shapes)
- owns: `services/ingest/pgmigrations/0014_api_key_scope.sql` (new), `services/ingest/internal/keystore/store.go` (`lookupSQL` gains `AND scope = 'ingest'`), `services/ingest/internal/keystore/integration_test.go` (the RED: a `read` row and a `setup` row are `ErrUnauthorized`), `services/ingest/internal/pgmigrate/qa_a6_migrations_test.go` (the applied-set literal gains `,0014_api_key_scope`), `apps/web/src/lib/mcp-types.ts` (NEW, this task's half: `ApiKeyScope` + `API_KEY_SCOPES` + `MCP_ADMITTED_SCOPES`, zero imports), `apps/web/src/lib/mcp-types.test.ts` (the zero-import assertion and the CHECK-clause parse of `0014` in the `landing-fence.test.ts:84-85` idiom), `apps/web/src/server/api-keys.ts` (`ApiKey.scope`, `issueApiKey(workspaceId, name, scope, query)`, `LIST_SQL`/`INSERT_SQL` carry `scope`, NEW `resolveApiKey(token, query)` with `RESOLVE_SQL` per D642), `apps/web/src/server/api-keys.test.ts` (the fourth loop case with its stated reason; the resolve vector), `apps/web/src/server/api-keys.integration.test.ts` (DSN guard names `0014_api_key_scope`; resolve against real rows: unknown → null, revoked → null, `ingest` → null, `read` → hit, `setup` → hit with `scope`), `apps/web/src/app/app/settings/actions.ts` (`parseKeyScope`, total parse, default `ingest`), `apps/web/src/components/settings/SettingsSuite.tsx` (a `<select name="scope">` beside the name field; the list shows the scope beside the prefix).
- goal: packet D642/D644/D658 VERBATIM. The column arrives with `ADD COLUMN IF NOT EXISTS … DEFAULT 'ingest' CHECK (scope IN ('ingest','read','setup'))`, every existing row is an ingest key, ingest refuses the two new scopes, the web resolve admits exactly the two and returns the scope, and the picker issues all three with `ingest` selected by default so the drive's existing key step (`e2e-drive.mjs:1781-1793`) is unchanged.
- done-check: `psql -f 0014_api_key_scope.sql` twice — pass 2 is a no-op with ONE column NOTICE (compute it in CI's Go run, not here); `go test ./internal/pgmigrate/ ./internal/keystore/ -count=1` green in CI with the two REDs recorded first (the pin edited before the file exists; the scope rows inserted before `lookupSQL` moves); `npm test` unit green locally; the integration legs green in CI; `gofmt -l` + `go vet` + lint + `tsc` clean. `mock/` diff EMPTY (the settings suite in mock renders its own fixture — verify the mock tab's DOM is byte-identical before/after, since the picker is a live-only control or the mock body is untouched: STATE WHICH, and pin it).
- result: **done locally**, fourteen files, exactly T1's `owns` plus one it needed and the plan missed — `app/app/onboarding/actions.ts` (the quickstart's own issuance, D201, a second `issueApiKey` caller the packet's grep had not named; it passes `DEFAULT_API_KEY_SCOPE`) and `app/app/settings/errors.ts` (`key-scope-invalid`, the code `parseKeyScope`'s null maps to). `0014` is one guarded ALTER with a NAMED constraint (`api_keys_scope_check`, so a later widening has a name to DROP); ingest's `lookupSQL` carries `AND scope = 'ingest'`; the web's `RESOLVE_SQL` binds `MCP_ADMITTED_SCOPES` as `$2::text[]` so the constant is the authority and the SQL text lists no scope. `lib/mcp-types.ts` (T1's half) is zero-import and its test parses the members AND the DEFAULT out of the migration's bytes. The picker is inside `LiveKeysTab`, which mock mode never mounts (`SettingsPage` renders `<SettingsSuite live={null} />` when `dataMode !== "live"`), so the demo's bytes do not move — T7's byte-identity run takes `/app/settings` as an extra control to prove it rather than assert it. **Gates (local, no store on this box):** `gofmt -l` + `go vet` clean and both Go packages compile (their Postgres tests SKIP here and RUN in CI); `tsc --noEmit` clean; `npm run lint` clean; web unit suite **1051 tests, 930 pass, 0 fail, 121 skipped** (the DSN-guarded integration legs) under a fresh mock build — the first run had 5 reds: three prerender-dependent arms reading a stale `.next` (an environment gap, cleared by the build) and two of T1's own (an empty-array edge in `parseKeyScope`, and the listed-key expectation lacking `scope`), both fixed. **Owed to CI on the PR:** `TestReadAndSetupScopedKeysAreRefusedAtIngest` (read and setup refused at ingest, D6's one answer, the CHECK refusing a fourth member), the `0014` pin, and the web integration test that walks both doors on real rows (ingest's lookup refuses read/setup; the MCP resolve admits exactly those two, refuses ingest and revoked, and the list carries the scope). RED-first as the plan asked: the pin was edited before the file existed (`want` mismatched), and the two doors' assertions are written against a predicate that did not exist — CI's first run is their red-to-green.


### T2: `lib/mcp-types.ts` (the registry, the prompt text, the constants), `server/mcp-endpoint.ts`, and the recipe lift
- status: done (local gates; see result)
- wave: 1 · depends: T1 (the same file) · model: Opus (a single-sourced contract three readers consume, with the coupling tests that keep it honest)
- owns: `apps/web/src/lib/mcp-types.ts` (the rest: `McpToolName` union + `MCP_TOOL_NAMES`, `McpToolSpec`/`MCP_TOOLS` — twelve read + three setup — `MCP_PROMPTS` with the six-step `setup` text, `MCP_PATH`, `MCP_LOOPBACK_ENDPOINT`, `MCP_RATE_LIMIT`), `apps/web/src/lib/mcp-types.test.ts` (D647: `get_pipeline_runs` absent while `/app/pipelines` is unwired; every name shared with `mock/mcp.ts` spelled identically; no `root-cause`/`RCA` in any description; the prompt names every tool it tells the agent to call and no other), `apps/web/src/server/mcp-endpoint.ts` (NEW, D653: `resolveMcpEndpoint()` = env override or loopback, server-only, request-time), `apps/web/src/lib/setup-recipes.ts` (NEW, D665: `snippetsFor` and the available connectors' `connectSteps` lifted, with `components/onboarding/snippets.ts`, `components/docs/QuickstartSnippets.tsx` and `components/connections/connectors.ts` re-pointed — ONLY if the originals carry a client directive or a React import; otherwise the tool imports them where they are and this file is not created — measure, then decide, and record which), `apps/web/src/mock/corpus-honesty.test.ts` (the ban list extended to the recipe source, condition 12).
- goal: one definition per fact. The page, the server, the docs and the prompt read the same constants; a tool named without a description, a prompt step naming a tool that does not exist, or a `get_pipeline_runs` that reappears while pipelines is mock — each is a failing test, not a review comment.
- done-check: `mcp-types.test.ts` green with each assertion first shown red by a one-line sabotage (recorded); `npm test` unit green; `tsc` clean; the quickstart and the hub render byte-identically before/after the lift (DOM md5 of `/app/onboarding` and `/app/connections` in the mock build — a lift that changes a rendered byte is not a lift).
- result: **done**, five files. `lib/mcp-types.ts` now carries both halves: the fifteen-entry `MCP_TOOLS` registry with `kind`, `MCP_TOOL_NAMES`, the six `MCP_SETUP_TARGETS`, the one `setup` prompt with its six steps, `MCP_PATH`/`MCP_LOOPBACK_ENDPOINT`/`MCP_SERVER_NAME`/`MCP_SERVER_VERSION`/`MCP_RATE_LIMIT`/`MCP_KEY_NAME_PREFIX`, and the two sentences (D655, D666) — still zero-import. `server/mcp-endpoint.ts` is `resolveIngestEndpoints()`'s shape verbatim (`||`, request-time, loopback default), with its own test. **The lift was measured and NOT taken (D665's own condition):** `components/onboarding/snippets.ts` carries no imports and no directive at all, and `components/connections/connectors.ts` is already consumed by a server page (`app/page.tsx`) and a client component (`Quickstart.tsx`) alike — both are pure data modules, so the setup tool imports them where they are and no `lib/setup-recipes.ts` exists; the quickstart and the hub are byte-untouched by construction. `mcp-types.test.ts` grew the D647 coupling (`get_pipeline_runs` absent while `isLiveWiredRoute("/app/pipelines")` is false — the registry idiom, so the tool is owed the day pipelines wires), the seven-shared-names pin with a near-miss normaliser, the `get_metrics` absence with its two honest halves present, the description ban on `root cause`/`RCA`/`pipeline`, the D665 cross-source (`MCP_SETUP_TARGETS` deep-equals the quickstart's tab ids followed by the connectors that ship steps — computed from both modules, not typed), the D668 prompt pins (exactly `get_setup_recipe`, `issue_ingest_key`, `check_arrival`, `get_trace` named, every target offered in step 1, the secrets line present), and the version pinned to `apps/web/package.json`. `corpus-honesty.test.ts` scans the two recipe sources and the contract module under the same fabricated-host bans (condition 12). **Gates:** the three touched suites 17/17; `tsc` clean; lint clean; the full unit suite recorded on the commit line. **One packet deviation, recorded not smoothed — D671 (escalations log):** the `github-actions` change-events recipe is not a T2 target.


### T3: the endpoint — SDK pinned, transport per request, the refusal matrix, and `get_trace` end to end
- status: done (local gates; see result)
- wave: 2 · depends: T1, T2 · model: Opus (a new network surface with an auth boundary; the packet's least-verifiable section)
- owns: `apps/web/package.json` + root `package-lock.json` (`@modelcontextprotocol/server` 2.0.0 exact, `zod` 4.4.3 exact, dev `@modelcontextprotocol/client` 2.0.0 exact), `apps/web/src/app/mcp/route.ts` (NEW, D640/D642/D645/D651: mock 404 with the tripwire line; bearer → `resolveApiKey`; 401 + `WWW-Authenticate: Bearer realm="obstack"` uniform; per-key sliding window → 429 + `Retry-After`; then the transport), `apps/web/src/server/rate-limit.ts` (`RateLimitAction` gains `"mcp"`, keyed by key id, at `MCP_RATE_LIMIT`), `apps/web/src/server/mcp/server.ts` (NEW: `createMcpServer(ctx)` — one `McpServer` + one stateless JSON transport per request, `registerTool` from `MCP_TOOLS` with `readOnlyHint`, a `Record<McpToolName, Handler>` that fails to compile on a gap), `apps/web/src/server/mcp/tools.ts` (NEW: this task lands ONE handler, `get_trace`, and stubs NOTHING — the handler map is typed total, so T3 ships the map with the twelve entries pointing at `notYet()` handlers that return the honest `isError` sentence "this tool is not served yet" and a test that fails if any such entry survives T4), `apps/web/src/server/mcp/route.test.ts` (NEW: protocol tests through `@modelcontextprotocol/client` with `fetch` injected as the route's `POST` — no listener; the refusal matrix; `tools/list` equals the registry; `get_trace` over the mock facade in mock mode is 404 — so the positive test runs under the DSN guard in `mcp.integration.test.ts`, T4).
- goal: packet D640–D646 VERBATIM. A stock client of the protocol reaches the endpoint; nothing but a live, admitted key gets past the first line; the SDK's answers (405s, JSON-RPC errors) are the SDK's.
- done-check: the export-name confirmation recorded FIRST; `route.test.ts` green with each refusal arm first red (the auth check commented out → the test that expects 401 fails); `next build` in both modes succeeds with the route present; the mock-mode 404 asserted; `npm run lint` + `tsc` clean; `images` on the PR proves the image still builds from the one lockfile.
- result: **done**, nine files. **The export-name confirmation came first** (condition 5): `@modelcontextprotocol/server` 2.0.0 exports `McpServer`, `WebStandardStreamableHTTPServerTransport`, `createMcpHandler`, `requireBearerAuth`, `InMemoryTransport`; the client exports `Client`, `StreamableHTTPClientTransport`; the transport's options carry `sessionIdGenerator`, `enableJsonResponse`, `allowedHosts`/`enableDnsRebindingProtection` (default `false`, deprecated in favour of middleware); `registerTool(name, { description, inputSchema, annotations }, cb)`, `registerPrompt(name, { description, argsSchema }, cb)`; the client transport takes `requestInit` and a `fetch` override — the last is what the protocol tests inject the handler as, so no listener and no store. Pinned exact: server 2.0.0, client 2.0.0 (dev), zod 4.4.3 (now a direct dependency). **The shape the plan did not name:** the handler is built from its dependencies — `server/mcp/handler.ts`'s `mcpRequestHandler({ mode, resolve, contextFor })` — and `app/mcp/route.ts` wires `dataMode`, `resolveApiKey` and the scoped readers; the tests drive the same function with a fake resolver and a fake `WorkspaceData`. `McpContext` carries `data` (the facade) beside `ch` and `query`, so `get_trace` reads through the same door the trace page does. `TOOL_DEFINITIONS` is `Record<McpToolName, …>` — total by the compiler — with `get_trace` live and fourteen `notYet()` entries pinned by name in `tools.test.ts` (the assertion T4 rewrites to an empty list). Annotations from the registry: read-only and idempotent for every tool but the mint, which is neither and is not destructive. **Gates:** `handler.test.ts` + `tools.test.ts` 9/9 — a stock client initialises against the handler, `tools/list` equals the registry, `get_trace` hit (structured + text), miss (the `NO_SUCH_*` sentence), a not-yet tool (its sentence); the refusal matrix (no header, `Basic`, bare `Bearer`, an unresolvable token → one bodiless 401 with `WWW-Authenticate: Bearer realm="obstack"`); mock mode 404 with exactly one tripwire line; the per-key window (the 120th call 200, the 121st 429 with `Retry-After: 60`, a second key untouched); the method matrix as the SDK actually answers it (below). `tsc` clean, lint clean, full unit suite **1068/0** (121 DSN skips), `next build` in BOTH modes with the route present (recorded on the commit line). **One departure from the packet's wording, recorded — D672 (escalations log):** GET is refused here, not delegated.


### T4: the fourteen remaining tools, the prompt, and the tenancy RED per tool
- status: done locally; the real-store proof owed to CI (see result)
- wave: 3 · depends: T3 · model: Opus (fifteen thin wrappers whose only risk is a scope leak — the proof is the work)
- owns: `apps/web/src/server/mcp/tools.ts` (the read tools per D648 — `query_traces`, `search_logs`, `get_service_map`, `list_issues`, `list_metrics`, `get_metric_series`, `get_slo_status`, `list_incidents`, `get_incident`, `list_alerts`, `list_changes` — each a zod face mirroring its contract's own filter, ALL fields, and the contract object returned verbatim; the setup tools per D665–D667 — `get_setup_recipe`, `issue_ingest_key` (scope-gated, `mcp:`-prefixed, `ingest`-scoped, the token once), `check_arrival`), `apps/web/src/server/mcp/prompts.ts` (NEW, D668: `setup` from `MCP_PROMPTS`), `apps/web/src/server/mcp/mcp.integration.test.ts` (NEW: two workspaces, two read keys, one setup key, real Postgres + ClickHouse under the DSN guard — the cross-workspace not-found per id tool, one-workspace lists, the setup matrix, the arrival flip after a seeded span; each tenancy arm first red by an unscoped handler in the test's scratch), `apps/web/src/server/mcp/tools.test.ts` (NEW: the zod faces refuse what the contracts refuse; `isError` sentences are the `NO_SUCH_*` shape; no `spendExplainRun`/`getExplain` import anywhere under `server/mcp/` — a grep test).
- goal: packet §2 and §6 VERBATIM. Every tool is one call into an existing export; every error is a product sentence; the one mutation is the settings tab's own issue path with a scope gate in front of it.
- done-check: `notYet()` count 0 (T3's test flips green); `tools.test.ts` + `route.test.ts` green locally; `mcp.integration.test.ts` green in CI with the RED counts recorded per arm; the D646 grep test green; `tsc` clean.
- result: **done locally**, seven files. `tools.ts` carries all fifteen: the reads through the facade where the pages take it (`query_traces`, `get_trace`, `search_logs` via `ctx.data`) and through the query modules and M6 stores otherwise, each face mirroring its contract's own filter — every field, the two `MetricSeriesQuery` fields the packet's first draft dropped included, with `groupBy`/`filters` defaulting rather than demanded — and every array contract under a named key (`{ metrics }`, `{ slos }`, `{ changes }`, `{ rules, events }`); `get_incident` takes the incident page's own two trips (row, then the stitch under `getUsage`'s retention floor); `get_metric_series` refuses an aggregation its type cannot take with the contract's own sentence before the store is asked. The setup three: `get_setup_recipe` serves the quickstart's three tabs and the three connectors with steps with the deployment's real endpoints (`resolveIngestEndpoints`), refuses `github-actions` naming the docs page (D671) and any other target naming the six; `issue_ingest_key` is gated on `setup`, prefixes `mcp:`, always mints `ingest`, and returns the token once with `shownOnce: true`; `check_arrival` wraps `getOnboardingStatus` + `getIngestHealth`, narrows to one key, and refuses an unknown key id with the `NO_SUCH_*` shape. `prompts.ts` registers the one prompt from `MCP_PROMPTS` with `target`/`service` stated on the first line when given. `resolveStepSnippet` and `ModalEndpoints` were LIFTED from `ConnectModal.tsx` (a `"use client"` file the server cannot import) into `connectors.ts` (pure), with the modal re-exporting them so the hub and its test keep their path — the same single-source rule D665 stated for the recipes. Every store failure is `guard`'s one sentence and one log line. **Gates (local):** the four MCP suites + the modal's 26/26, `tsc` clean, lint clean; the full unit suite on the commit line. **Owed to CI:** `mcp.integration.test.ts` — two workspaces, three keys, a seeded trace/incident/change/SLO/alert event; a reads all of its rows and the recipes through a stock client; b gets the not-found sentence per id tool and empty lists; the RED in the test's own scratch (a MIS-SCOPED context built through the handler factory returns a's trace to b's key — so the green bites on scoping and nothing else); the setup key mints an `mcp:`-prefixed `ingest` key whose token opens ingest's lookup and NOT the MCP door and whose row cannot reproduce it; a health row makes `check_arrival` flip for that key alone. **Seam, recorded — D673 (escalations log):** the recipe keeps the placeholder; no token travels in an argument.


### T5: the flip — the page in two bodies, the registry, and the four sentences
- status: done locally (byte identity two-build owed to T7; see result)
- wave: 4 · depends: T4 · model: Sonnet (the S7.3/S7.4 flip template, followed)
- owns: `apps/web/src/app/app/mcp/page.tsx` (server component; mock branch `<McpPage />` before any await; live: `connection()`, session, `listApiKeys` filtered to admitted scopes, `resolveMcpEndpoint()`, `<McpLive />`), `apps/web/src/app/app/mcp/page.test.ts` (NEW: `McpPage.tsx` sha-pinned at `4a761c90…` and `mock/mcp.ts` at `9f8d3adf…`, regeneration recipe naming `02bf550`), `apps/web/src/components/mcp/McpLive.tsx` (NEW, D654: endpoint with copy button, the three client snippets from the real origin with `<OBSTACK_API_KEY>`, the admitted keys by name/prefix/scope with revoked marked and a link to Settings, the fifteen tools, the one D655 sentence, `data-tour="mcp"` on the endpoint section), `apps/web/src/lib/live-routes.ts` (`"/app/mcp"` after line 32), `apps/web/src/components/shell/TourGuide.tsx:130-134` (the step rewritten true in both modes), `apps/web/src/content/docs/what-obstack-does-not-do/index.mdx:92-94` (the MCP sentence narrowed; "no agent skill and no CLI" kept), `apps/web/src/lib/docs/mirror.test.ts` (the registry-coupled pin), `apps/web/src/content/docs/mcp/index.mdx` (NEW) + `manifest.ts` (section "Agents": the three client setups, the scopes, the fifteen tools, the six setup steps rendered from `MCP_PROMPTS`, the two absences), `apps/web/src/components/shell/shell-honesty.test.ts` (the "Every call is audit-logged" fiction pin STAYS and is re-asserted against the new tour text).
- goal: packet §3 VERBATIM. Mock mode renders the frozen fiction byte for byte; live mode renders the workspace's real endpoint, keys and tools and says the one thing it does not do.
- done-check: the two-build DOM md5 of `/app/mcp` in mock mode identical (pre-flip worktree @ `02bf550` vs HEAD, script-stripped + asset-normalized) with `/app/oncall` and `/app/pipelines` as controls; `git diff --stat master -- apps/web/src/mock` EMPTY; `mirror.test.ts`'s new pin red on the old paragraph first; `docs.test.ts` green (manifest ↔ tree); the corpus-honesty bans green over the new docs page; `npm test` unit green; lint + `tsc` clean.
- result: **done locally**, twenty files. The page is a server component in the D431/D519 shape — `if (dataMode !== "live") return <McpPage />` before any await, then `connection()`, the session, `listApiKeys` filtered to `MCP_ADMITTED_SCOPES`, `resolveMcpEndpoint()`, `<McpLive>`; `page.test.ts` pins `McpPage.tsx` at `4a761c90…` and `mock/mcp.ts` at `9f8d3adf…` (regeneration recipe naming `02bf550`), the branch-before-await order, and that the live markup — comments stripped — carries no token shape and no word `token`. `McpLive.tsx` (`"use client"`) is D654's four sections: the real endpoint with a copy button (not a credential), the header line with the placeholder, the three client setups from `mcpClientSetups(endpoint)` (the mock's three ids in order, pinned), the admitted keys by name/prefix/scope with revoked marked and the Settings link, the fifteen tools from THE registry with their kind, the `/mcp__obstack__setup` line, and the one D655 sentence; `data-tour="mcp"` on its endpoint section. `live-routes.ts` gained `"/app/mcp"` after the incidents pair (exact; `live-routes.test.ts` pins exact-not-subtree and that `/mcp` itself carries no badge). The four sentences: the tour step rewritten at the altitude true in both modes; the absences paragraph narrowed to "There is no agent skill and no CLI" with the endpoint's three properties stated, and `mirror.test.ts` gained the D656 registry-coupled pin (red on the old paragraph first — the old sentence was asserted absent against the file before the edit); `content/docs/mcp/index.mdx` under a new "Agents" section, its blocks (`components/docs/McpDocs.tsx`: the setups from the host placeholder, the scopes, the tools table, the six steps) rendered from the same constants the server registers — `docs.test.ts` accounts for both new render-path modules, `mirror.test.ts` lists `server/mcp-endpoint.ts` among the files that define the variables the docs name. **D671 paid here:** `GITHUB_ACTIONS_DEPLOY_STEP` lifted into `lib/change-types.ts`, pinned byte-for-byte to the docs page's fence by `change-types.test.ts` (the fence stays: the drive extracts and runs it), served by `get_setup_recipe` as the seventh target; `MCP_SETUP_TARGETS` and the prompt's step 1 gained it; the integration test now expects the served step. Five counts moved deliberately, each with its note: the docs corpus 15→16, the docs surface's prerendered pages 15→16, the corpus's `/docs/…` links 21→24 (twice, source and rendered), the manifest 15→16. **Gates:** every touched suite green in isolation, `tsc` clean, lint clean, `next build` (mock) 69 static pages with `/docs/mcp` and `/app/docs/mcp` prerendered, full unit suite **1077/0** (122 DSN skips). **Owed to T7:** the two-build DOM md5 of `/app/mcp` in mock mode against a `02bf550` worktree, with `/app/oncall`, `/app/pipelines` and `/app/settings` as controls — one build pair for the sprint, not one per task.


### T6: the drive's MCP arm — the RED hook, the two doors, the setup leg, the probe entry
- status: written; GREEN and RED owed to CI (see result)
- wave: 4 · depends: T4, T5 · model: Opus (front-door claims recomputed, on a drive that cannot run on this box — every line lands through CI)
- owns: `deploy/compose/e2e-drive.mjs` (the arm after the incidents arm under `RED_WITHHOLD_MCP`, D659–D663; a `setup` key and a `read` key issued through the settings tab; `tools/list` equals the registry read from `lib/mcp-types.ts`'s source; `get_trace` span count recomputed over `obstack.spans`; `query_traces({status:"error"})` total recomputed; `get_incident` entry count equals the rendered page; `get_slo_status`/`list_changes` against the earlier arms' rows; bob's read key cross-tenant; unauth 401 with the header; the 429 boundary; the setup leg: `issue_ingest_key` under the setup key → an `mcp:` row → one span exported under it via `otlp()` → `check_arrival` flips for that key alone → `get_trace` resolves it; the ingest key at `/mcp` → 401, the read and setup keys at `/v1/traces` → 401; `/app/mcp` added to the unauth probe list), `apps/web/package.json` (the client is already a devDependency from T3 — nothing new).
- goal: packet §5 and condition 11 VERBATIM. K equals the arm's check count; a red run fails on withheld defaults and never on a `must()`.
- done-check: CI `e2e` GREEN on the PR with the arm's count and wall time printed; RED proven by a `RED_WITHHOLD_MCP=1` dispatch of `e2e.yml` on the branch (the workflow reads the variable — add the input if it does not) with exactly K failures; the probe coverage assertion green (`/app/mcp` present).
- result: **written, parsed, and handed to the runner** — the drive cannot start on this box (kickoff notes), so the arm's first execution is CI's. Placement: after the incidents arm's wall-time line and before the plan-change step, at function scope (the incidents RED block closes at its own `}` three hundred lines earlier, so neither hook shadows the other). Shape (D662): every fact the fourteen claims read is declared as a withheld default BEFORE `if (!process.env.RED_WITHHOLD_MCP)`, the block holds only interactions and `must()`s, and the claims follow it — K = 14 exactly on a withheld run. The keys go through the front door: the settings tab's own picker (`choose("scope", …)` — the picker is now inside a `<label>` so the drive's helper can reach it, the reason that markup moved), `type("Key name", …)`, the shown-once banner read the way alice's ingest key was, dismissed; three keys — alice read, alice setup, bob read. A STOCK client (`@modelcontextprotocol/client`, resolved from the root `node_modules` the way `pg` is) drives `${BASE}/mcp`. The claims: the live page (200, the path, the tag, `tools · N` with N read off `lib/mcp-types.ts`); `tools/list` equals the registry read off the same file (the D565 idiom); `get_trace(PROMPT_TRACE).spanCount` equals the drive's own `count()` over `obstack.spans`; `query_traces({ q: SPAN_PROMPT_TOKEN })` finds exactly the one trace the traces page found for that token; `get_incident(historicalId)` is her incident under her plan's retention floor (recomputed through `plans`/`workspace_plans`) with only the four timeline kinds — guarded so a withheld incidents arm yields a default, not a thrown invalid-params; `get_slo_status` and `list_changes` equal her `slos`/`change_events` rows; bob's read key gets the not-found sentence for her trace and lists only his own incidents; the two doors (her INGEST key at `/mcp` → 401 + challenge; her read and setup keys at `/v1/traces` → 401); no credential → 401 + challenge; the boundary (the 121st call → 429, `Retry-After: 60`); a read key's mint → the refusal sentence read off the contract file; a setup key's mint → an `ok_live_` token, `scope ingest`, `mcp:e2e-agent` on the stored row, and that token refused at `/mcp`; the minted key exports one span (200), `check_arrival` flips for it alone within a 30 s settle, and her read key's window is unmoved. The unauth probe list gained `/app/mcp` (D663); `e2e.yml` gained the `red_withhold_mcp` dispatch input mapped to `RED_WITHHOLD_MCP`, empty on every pull_request run. **Local gates:** `node --check` clean, `tsc` + lint clean (the picker markup), full unit suite 1077/0. **Owed to CI, recorded on the next line when it lands:** GREEN on the PR's `e2e`; RED via `workflow_dispatch` with the input set — exactly K = 14 failures.


### T7: integration gate, goal check, retro
- status: pending
- wave: 5 · depends: T1–T6 · model: Fable (the gate)
- owns: this file's Integration, Goal check and Retro sections; `.planning/2026-09-14-s8.1-mcp-packet.md` (status line only, when the user signs).
- goal: the run-goal met clause by clause — the endpoint, the scopes, the fifteen tools, the prompt, the flip, the docs — on CI evidence at the PR's tip; the PR opened, all checks green, the merge left to the user.
- done-check: web suite (CI) 0 failed / 0 skipped; Go 15/15 packages (CI); lint + `tsc`; byte identity two-build recorded with the controls; the drive's arm count and RED count recorded; no seam smoothed — every D671+ entry in the log.
- result: —

## Escalations log
- **D673 (T4, narrows D665's "rendered with it")** — D665 said a recipe is rendered with the key "when D666 just minted one in the same call sequence". A per-request server (D641) has no sequence to remember, and the alternative — the agent passing the token back as a tool ARGUMENT — would put a credential into the one channel some clients log verbatim. So the recipe ALWAYS carries `<OBSTACK_API_KEY>` (the hub's own placeholder, single-sourced), the tool result names it as `keyPlaceholder`, and the prompt's step 4 has the agent substitute the key it already holds; no token ever travels in a tool argument or a recipe body. The integration test pins the placeholder in the served recipe.
- **CI observation on the T3 push (not a seam of this sprint):** `go` went red once on `c84432e` — `internal/receive`'s `TestOTLPMetricsLandInClickHouseThroughTheRealWriter`, "read 1m sum rollup: sql: no rows in result set" at `integration_test.go:384` — with no Go line changed since the green run on `44cc232`; the rerun was green. The S7.4 T1 result already named this class (a ClickHouse rollup that had not materialised when read, the `WaitForAsyncInsert` race); the fix owed is a settle/retry in that test, not a weakened assertion — pre-registered for the next Go-touching task, not taken here.
- **D672 (T3, amends D640's "each delegating to the transport"; the principle — the SDK's answers are the SDK's — stands for every method but one)** — measured against the installed 2.0.0: an authenticated GET with `Accept: text/event-stream` gets a **200 SSE stream** from the stateless transport, not a 405 — the optional server-to-client notification stream, which on a per-request server would hold an idle `McpServer` open for every client that asks and carry nothing (this server sends no notifications). The protocol lets a server decline that stream with 405, and a stock client treats the 405 as "no stream" and carries on, so the handler answers GET itself — 405 with `Allow: POST, DELETE` — AFTER the 401 gate (no method answer before a credential, no oracle). DELETE and PUT stay the SDK's: a stateless DELETE is accepted as a no-op with 200 (nothing to terminate), PUT is the SDK's 405 with its JSON-RPC body; both asserted as given.
- **D671 (T2, amends D665's target list; the ruling's principle stands)** — D665 named "the S7.2 change-events recipe as the `github-actions` target". The recipe has no module source: its YAML lives only in `content/docs/connectors/github-actions/index.mdx:77-95`, and the standalone image reads no file at runtime (D320), so a tool serving it would have to carry a second copy — the class S7.4 named and refused. The honest v1 target list is the six the human surfaces already single-source (three quickstart tabs, three connectors with steps); the `github-actions` target lands in **T5**, which owns the docs page, by lifting the YAML into a constant BOTH the MDX and the tool read (the `change-types.test.ts` cross-source then pins the docs against the constant instead of the other way round). Until T5, `get_setup_recipe` refuses `github-actions` with the docs page's own address — absence, not a recipe.

## Integration
- verdict: —
- rounds: 0

## Goal check (Step 7, one entry per iteration)
- (none yet)

## Retro (filled at end of run)
