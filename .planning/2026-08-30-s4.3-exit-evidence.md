# S4.3 — exit evidence — obstack M4 sprint 3: obstack-js launch alignment (`ai@7` + the OpenAI Responses API, additive under D78; D298's CI shape; the D103(a) triage)

meta:
- sprint record: `.planning/2026-08-30-s4.3-team-plan.md` (rulings D299–D317, tasks T1–T5, review record)
- branch: `s4.3-obstack-js-launch-alignment` from `f7260a9` (master after PR #22)
- tip at bundle time: `f797a84` (F3); CI numbers pending
- CI numbers: _(§6 — filled from the first real runner)_
- signature: _(advisor)_

## 1. The exit criterion, clause by clause

**Exit line (team plan title):** an app on `ai@7` produces the same four-layer trace an `ai@6` app gets today, proven by a CI-driven sample; the OpenAI auto-instrumentation covers the Responses API; both land ADDITIVELY under D78 with the verdict recorded; D298's CI shape is implemented; the D103(a) triage is recorded for S4.4's honest-absence sentences.

| clause | evidence | status |
|---|---|---|
| an `ai@7` app produces the same four-layer trace an `ai@6` app gets | `demo/sdk-sample-ts-ai7` (a copy of the v6 sample on exact pins `ai 7.0.85` / `@ai-sdk/openai 4.0.52` / `openai 7.4.0`, `generateText` with **no telemetry option**) driven by `deploy/compose/sdk-evidence.sh` through the real compose stack: trace `b662141e…` (T3 local) / re-driven by the integrator on the rebuilt tree — ≥4 spans under one trace_id, **all four layers**, every llm span with the full D8 set and non-zero cost, prompt/completion in the dedicated columns, D92 both halves, **zero `ai.*` keys with the v6 trace as positive control**, the stock-collector OTLP leg and the dead-port fail-open leg. Seam: the v6 and v7 `POST /chat` traces classify **identically** in ClickHouse (`api=1 agent=1 tool=1 llm=3`, same span names); the v6 trace's one extra `other` span is `ai@6`'s own `ai.generateText` wrapper, absent on v7 by AI1 | **MET** locally; CI `sdk-e2e` number in §6 |
| proven by a CI-driven sample | the twin is a third service under the `sdk` compose profile inside the existing `sdk-e2e` job (K3: no new job, no rename); `sdk-js` runs the v7 unit suite on the real `ai7` alias | **MET** — first-runner numbers in §6 |
| the OpenAI auto-instrumentation covers the Responses API | second `InstrumentationNodeModuleFile` on `openai/resources/responses/responses.js` (`>=4.87 <8`; first appears 4.87.0 — OR1), `RESPONSES_SHAPE`; `parse()` covered through `create` (OR3), `stream:true`/`.stream()` declined like chat; verified against the shipped `dist` on **4.87.0, 5.23.2, 7.4.0, 7.8.0** (R1 re-verify) and CI-driven on the v6 sample (`finish_reasons ["completed"] · 62+30 tokens`) | **MET** |
| both land ADDITIVELY under D78 with the verdict recorded | **D299** (verdict, grounds AI1/AI2/OR1/OR9) — `git diff 98d2fcf <tip> -- packages/obstack-js/src/index.ts src/attributes.ts src/helpers.ts` **empty** (verified by T1, R1, F1, R1-re, the integrator); `init()` signature untouched; no new option/env var; `LlmShape`/`LlmRequest`/`LlmResponse` field-free (`llm-span.ts` gained only an optional observation parameter, D313); peer `ai` `>=5 <8` (D310, floor 7.0.0 measured on 19 of 80 published 7.0.x); `openai` stays `>=4.85 <8` | **MET** |
| D298's CI shape is implemented | `stack.yml`: path-filtered `pull_request` on the CI2 true set (verified against `acceptance.sh` + the Dockerfile by T4 and R2), `push: master` (new — zero master-push history existed), `workflow_dispatch`, `cancel-in-progress` PR-only (D306(b)); header: not required, D41 rider primary, `ci:stack` sentence deleted; the D122 guard moved **byte-identical** into `e2e.yml` after the compose step, its Postgres riders gone from `stack`; root README reconciled (nine every-PR checks, `stack` excluded) | **MET** — first master-push `stack` number opens the re-based watch (§6/run log) |
| the D103(a) triage is recorded for S4.4 | **D309** + the S4.4 handoff list in §7 | **MET** |

## 2. Measured facts (S2.4 L1 — measured at breakdown, never believed)

Recorded in full in the team plan (AI1–AI12, OR1–OR10, CI1–CI10) with the advisor's nine imprecision riders. The ones the sprint turned on:
- **AI1** `ai@7` emits zero OTel spans (grep + empirical, both telemetry option spellings) → the v5/6 processor and the v7 integration are mutually exclusive; both ship live.
- **AI2/AI3** two public doors, telemetry on by default; **D304** chose ONE (the `globalThis` registry), wrapper-owned span lifetime, `onStart.operationId` as the stream marker — the channel adds no reach for a Node-only SDK (advisor rider 4).
- **AI7/OR4 → D300**: the system prompt relocates to `instructions` on both APIs; folded back as the leading `system` element — byte-parity with `ai@6`'s pinned form (T1 measured: element `[0]` byte-equal; the user turn differs only by ai's own normalisation, pinned).
- **OR1** the Responses resource first appears at 4.87.0; below it no span, no error (D87 shape). **OR7 → D301** no `finish_reason` exists; `[status]`.
- **CI2** the honest path filter includes `apps/web/**` (root build context) — the reclaim comes from the required-set drop, not the filter. **CI3** "required" is advisory (private free-plan repo, 403 on branch protection). **CI4** `e2e`'s worst point is 6m02s over the last 11, 6m44s over the window — the line HELD with D207 pre-authorized (D306(d)). **CI6** the JS sample lives in `sdk-e2e`, not `e2e`. **CI8** the peer range was the hard blocker (`ERESOLVE`) for any `ai@7` sample.
- **Bisect (T1, D310):** 19/80 published 7.0.x releases, four predicates, no divergence → floor 7.0.0.
- **Measured after the fact, by the review:** `openai` ≥7.5 installs a per-instance `_thenUnwrap` closing over the module-local `parse` (`client.js:508`) — the memo approach was dead on the current openai; **D313** replaced it with a `Response.clone()` observed off `responsePromise` (field present 4.87.0 → 7.8.0).

## 3. Seam evidence (integrator, rebuilt tree from a fresh `npm ci`)

- `npm ci` 1m08s resolves `openai@7.8.0`, `ai7@7.0.85`, `openai487@4.87.0`; package suite **68/68 twice** (the 4.87.0 scratch-copy mechanism is idempotent); root `npm test` **463/463/0 with the DSNs live** (0 skips — the K4 trap satisfied) and 408/355/0 without; `next build` 36/36.
- `sdk-evidence.sh` from `down -v`: **PASS 43/0 (195s)**; both sample images carry F1's `dist` (`responsePromise` ×3, `shareOneParse` ×0).
- `e2e` mirror: compose up → **guard PASS on node 24** (`15 statement(s) … identical`) → smoke PASS → drive **166/0**.
- D13 sweep over tracked files: clean (the one `>=5 <7` hit is the README's deliberate mechanism-split row beside the `>=7 <8` row, with the declared interval stated and test-pinned).
- Three prose slips fixed by the integrator (`94d3e69`, `d7eb83d`, `a260568`); verdict **FIXED-GO**.

## 4. Review record

- **R1 (obstack-js, T1+T2):** MUST-FIX — M1 `shareOneParse` a no-op on openai 7.5–7.8 (own repro on four versions; the lockfile pin at 7.4.0 hid it from CI); M2 the v7 map unbounded for abandoned streams (reproduced on `ai@7.0.85`); M3 D310 wording; S1–S8. Ruled **D313–D315**; fixed by **F1** (`26b0ee4`, `1649569`).
- **R1 re-verify:** APPROVE WITH SHOULD-FIXES — C passes on 4.87.0/5.23.2/7.4.0/7.8.0 against the shipped `dist`; S-a–S-d fixed by **F3** (`f797a84`, 70/70).
- **R2 (CI shape + sample, T4+T3):** MUST-FIX on two false sentences + six prose/arithmetic corrections; mechanism sound (filter re-derived, guard byte-identical, discriminator real, twin's own trace selected, BuildKit dedupe holds). Fixed by **F2** (`5614723`).
- Real catches that mattered: the openai ≥7.5 promise-internals bypass (a launch-claim defect invisible to CI); the abandoned-stream leak; the fence guard firing on the peer-range move (T1, by design); the "two sample apps" / "standing `stack` step" sentences; `cancel-in-progress` cancelling master-push runs (advisor, D306(b)).

## 5. Rulings record (D299–D317)

Kickoff **D299–D309**; D303 return **D310–D312**; escalation after R1 **D313–D315**; integrator-gate nods **D316–D317**. All in the team plan §"Advisor decisions". Escalation triggers (D299) checked by every executor and reviewer: none fired.

## 6. Verification at the signed tip

_(filled at close)_

| check | result |
|---|---|
| `npm test --workspace packages/obstack-js` | |
| root `npm test` (DSNs live) | |
| `tsc --noEmit` / `eslint --max-warnings 0` | |
| `actionlint` / `helm lint` / `docker compose config` | |
| `sdk-evidence.sh` (local, rebuilt) | |

### First-runner CI numbers (the D137/D280 return)

| job | wall-clock | budget / band | note |
|---|---|---|---|
| `e2e` | | ≤ ~6m line HELD; band 5m28–6m02 (window worst 6m44s); D207 executes on the first number over | now the PR critical path, guard aboard |
| `sdk-e2e` | | projected worst point ~5m23s over the 4m04s base | third sample + OTLP/fail-open legs |
| `sdk-js` | | ~1m10s | `ai7` + `openai487` aliases, 68+ tests |
| `web` | | | Quickstart change |
| `stack` (PR) | | 5m58–7m17 PR prior; ≈6m56 expected post-move | fires on this PR (filter hit) |
| `stack` (first master push) | | opens the re-based watch | after merge |

## 7. Carried forward

- **To S4.4 (docs) — the D309 handoff sentences:** on `ai@7` telemetry is on by default (opt-out `telemetry:{isEnabled:false}`); on `ai@5/6` `experimental_telemetry:{isEnabled:true}` per call; a per-call `telemetry:{integrations:[…]}` replaces the global array so obstack draws nothing for that call (D315); the `ai` floor is 7.0.0 measured (D310 wording); `ai@7` needs node ≥22; register obstack-js OR `@ai-sdk/otel`, not both (D302); Responses API covered from openai 4.87 (`parse()` through it; `stream`/`.stream()`, `beta.responses`, `compact()`, `retrieve(id,{stream:true})` not — D308); the Responses API has no finish_reason — obstack records `status` (D301); streaming calls are not instrumented for any of the three SDKs; the JS logging bridge is absent (D259); the D316 fail-open sentence (request attributes only). Plus: `images`/`lint` have no per-check README bullets (S4.1 gap).
- **To the M5-planning packet:** streaming instrumentation + JS logging bridge (D259) with the AI8 cost correction (usage is on the integration's `onEnd`/`onStepEnd` — the D304 hook design is the base); auto tool spans from `executeTool` (D305, payload in AI6); the user-captured **agent-evaluation idea** (`.planning/2026-08-30-idea-agent-evaluation.md`): the three capture decisions (opt-in tool/agent I/O — a D78 escalation; the task-outcome signal; the feedback attach path); the compose harness's port-freeness refusal not covering 5432.
- **Standing:** D178 did not fire (`@polar-sh/sdk` 0.49.0 untouched — CI10); D207 unconsumed unless §6 says otherwise; the `stack` watch re-based to master pushes; the node-22 `ddl-drift-check.mts` crash (before the diff — integrator correction to CI5) is a local-only hazard while CI pins node 24.

## Run log

- 2026-08-30 — breakdown with measurements first (three read-only Opus passes); advisor kickoff **D299–D309**, CLEARED WITH CONDITIONS; T1 ∥ T4 → T2 → T3 (Opus executors, manager-directs); D303 return **D310–D312**; R1 MUST-FIX → escalation **D313–D315** → F1; R2 MUST-FIX (prose) → F2; integrator-gate nods **D316–D317**; R1 re-verify APPROVE WITH SHOULD-FIXES → F3; integrator **FIXED-GO** on the rebuilt tree (`a260568`).
- _(PR opened / first-runner numbers / signature — at close)_
