# T8 (U6/D249) — real-key Explain evidence run

Date 2026-08-20 · branch `s3.5-explain-honesty` · repo untouched (`git status` clean, nothing committed).

## Sequence actually executed

1. `docker compose -f /Volumes/SSD/Workspace/observer-stack/deploy/compose/docker-compose.yml down -v` then `up -d --wait` (D249: the drive's D172 overrides poison shared Postgres — the fresh volume is why `plans.explain_quota` reads the shipped 20, not the drive's 2).
2. `npx next build` in `apps/web` with the drive's app env (`OBSTACK_DATA_MODE=live`, `CLICKHOUSE_URL=http://127.0.0.1:8123`, `CLICKHOUSE_USER=obstack_web`, `CLICKHOUSE_PASSWORD=obstack_web_dev`, `OBSTACK_POSTGRES_DSN=postgres://obstack:…@127.0.0.1:5432/obstack`, `BETTER_AUTH_SECRET=<dummy>`). Clean; `/app/traces/[id]/explain` registered ƒ.
3. `npx next start` with the same env plus the sourced credential file (`set -a; . /Users/ziad/.claude/jobs/7c575a7a/tmp/explain-evidence.env; set +a` → `OBSTACK_EXPLAIN_MODE=anthropic` + `ANTHROPIC_API_KEY`). The key entered process env only — never a command line, never a file, never the repo.
   - **Port note:** the drive's default 3210 and 3211/3213 were held by stale `next-server` processes from earlier sessions (one from Aug 19 19:55). Nothing was killed; the run used **3217**, free at the time. `BETTER_AUTH_URL` stayed unset (D119), so the production cookie name applies.
   - App stdout/stderr → `/Users/ziad/.claude/jobs/7c575a7a/tmp/app.log` (outside the repo).
4. `evidence-run.mjs` (job tmp dir, mirrors the drive's helpers verbatim in behaviour):
   - CDP headless Chrome → `/signup` form → real account + org + workspace `ws_4d249e8eba9ebc8b`.
   - Settings → API keys tab → **key issued in the product**, `ok_live_a774…` shown once.
   - That key carried a **failing** trace over the published OTLP/JSON wire (`http://127.0.0.1:4318/v1/traces` + `/v1/logs`, Bearer): 3 spans (root `POST /checkout` status=error, `SELECT carts` ok, `POST payments.charge` status=error "connection reset by peer after 750ms") and 3 trace-carrying log records, one ERROR naming pool exhaustion. Ingest 200 on both; ClickHouse then held 3 spans + 3 logs for the trace.
   - `POST /app/traces/7d807f489c68184ff2f29ec048e258dc/explain` with the session cookie; the response body was written **verbatim** to `explain-evidence-transcript.ndjson`.
5. Web process stopped (`kill` on 3217); Chrome gone; compose stack left running; env file left in place.

## Result

- Trace `7d807f489c68184ff2f29ec048e258dc`, workspace `ws_4d249e8eba9ebc8b`.
- HTTP **200**, `content-type: application/x-ndjson`, `cache-control: no-store` (`private, no-store`-class — the D227 frame).
- **9 delta events then exactly 1 terminal `result`** carrying a validated `Explanation`; the result is the last frame. 2,619 bytes.
- Wall time of the POST: **8.18 s**. Model: **`claude-sonnet-4-5-20250929`** (`DEFAULT_MODEL`, `server/explain/anthropic.ts:34`; no `OBSTACK_EXPLAIN_BASE_URL`/`OBSTACK_EXPLAIN_MODEL` set, so Anthropic's own endpoint).
- Headline: "Checkout failed because the payment service connection was reset due to connection pool exhaustion" — a real reading of the trace, not a restatement: it fused the failing child span's status message with the ERROR log's `0 idle of 32` to name pool exhaustion, which no fake could produce.

### Evidence-id resolution (D223/D102)

Three evidence items, **all three resolve to this trace's own ids**; zero dropped, so no `is not in this trace` note appears anywhere:

| item | id | kind | resolves against |
| --- | --- | --- | --- |
| payment service call | `0000000000000003` | `spanId` | `obstack.spans` row for this trace (the failing `POST payments.charge`) |
| pool exhaustion error | `7d807f489c68184ff2f29ec048e258dc-1` | `logRef` | the trace's log #1 (`adapters.ts:197` builds a log id as `${trace_id}-${index}` over `LOGS_SQL` order) — the ERROR body |
| checkout failure | `0000000000000001` | `spanId` | `obstack.spans` row for this trace (the failing root) |

Span ids were checked against the drive's own ClickHouse SQL (`SELECT span_id … WHERE workspace_id=… AND trace_id=…`), not against the answer that produced them. **No reference was dropped**, so — correctly — the static warn+count line is absent from the server log.

### Quota

- `explain_runs` after the run: `ws_4d249e8eba9ebc8b | used=1 | 2026-08` (exactly one row, this calendar month, UTC).
- No `workspace_plans` row → `COALESCE(wp.plan_id,'free')` → `plans.free.explain_quota = 20` (fresh volume, unmutated).
- So the panel-visible counter line reads **1 of 20**, and the D226 settings meter divides the same two numbers.

### Hygiene (U6/D132/D248)

- Grepped every artifact written by this run — `app.log`, `chrome.log`, `evidence-run.mjs`, `evidence-run-stdout.txt`, `explain-evidence-facts.json`, `explain-evidence-transcript.ndjson`, plus the Chrome profile — for the Anthropic key prefix: **0 hits in every one (0 files match)**. (This notes file itself is the only tmp-dir file besides the credential file that contains the prefix as a *word*, and only because the sweep is described here; it holds no key material.)
- Same grep over the repo (excluding node_modules/.next/.git): **0**.
- App log contains **no model content**: `grep -ci 'connection pool|pool exhaustion|payments.internal|Checkout failed because' app.log` → **0**. The whole log is 1,750 bytes: Next's banner plus three Better Auth config warnings, nothing from `server/explain/`.
- Nothing committed; `git status` clean; the credential file is untouched at mode 600 outside the repo.

## Artifacts

- Transcript (verbatim NDJSON): `/Users/ziad/.claude/jobs/7c575a7a/tmp/explain-evidence-transcript.ndjson`
- Machine-readable facts: `/Users/ziad/.claude/jobs/7c575a7a/tmp/explain-evidence-facts.json`
- Run stdout: `/Users/ziad/.claude/jobs/7c575a7a/tmp/evidence-run-stdout.txt`
- Server log: `/Users/ziad/.claude/jobs/7c575a7a/tmp/app.log`
- The script: `/Users/ziad/.claude/jobs/7c575a7a/tmp/evidence-run.mjs`

## For the advisor

1. The run cost one Anthropic call and one quota unit; nothing else was spent.
2. Zero dropped references means the drop path (D248 static warn+count, `droppedReferenceNote` in the panel) was **not** exercised by this run — its evidence remains the deterministic rail's (drive 166/0 + the drop tests), as designed.
3. Stale `next-server` processes from earlier sessions occupy 3210/3211/3213 on this machine; they were left alone. Anyone re-running the drive on 3210 will hit its own busy-port refusal.
