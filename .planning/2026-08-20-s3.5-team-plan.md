# Team Plan — obstack M3 sprint 5 — the `explain` server function and the M3 honesty pass (exit: Explain on a failed live trace returns a structured root-cause summary with linked evidence, rate-limited per plan, and no surface a signed-up stranger can reach still renders unlabelled sample content)

meta:
- date: 2026-08-20
- repo/branch: `s3.5-explain-honesty` (from `master` @ `9c79eae`)
- mode: workflow (execute→review pipeline via Workflow tool; advisor, escalations, fix rounds, integration in the manager session)
- team-cap: 10
- models: executor=opus (standing user ruling — never Sonnet) reviewer=opus advisor=fable
- iteration-limit: 3 — fix rounds allowed before escalating to the user
- iteration: 0/3
- sprint: S3.5 · milestone M3 · **exit-blocking per D104** · dependencies S3.1–S3.4 (ALL CLOSED)
- governing record: phase plan `.planning/2026-08-18-s3-m3-phase-plan.md` §3 S3.C5 + §9 D94–D110 (esp. **D102** Explain, D103, D104, D106 landing fence, D107 e2e); S3.4 exit bundle `.planning/2026-08-20-s3.4-exit-evidence.md` §5 addendum + §7 residue (BINDING); U6 (real Anthropic key for the evidence run; deterministic fake in CI — **CI never spends**); U4 (no email in M3); sprint records D111–D137, D138–D160, D161–D200, D201–D222; `.ziarmy/retro.md`
- **this sprint also closes M3**: the M3 exit-evidence bundle (D104 wording, U9 method = the D107 `e2e` script) is the advisor's to sign at the end.
- kickoff: advisor rulings **D223–D233** recorded below, 2026-08-20. Verdict: **CORRECTED AND APPROVED** — boundaries corrected per D231 (types.ts + contract.ts → T2; adapters out of T4; SettingsSuite meter → T4; layout.tsx mock branch → T5). Waves (D232): W1 = T1, T2, T5, T6 · W2 = T3 · W3 = T4 · W4 = T5-F1, T6-F1 · W5 = T7 · then integrator → goal check → T8. Critical items (D233): T1, T2, T3+T4 joint, T7, T8.

## Sprint exit criterion (verbatim from the phase plan, S3.C5 → S3.B4's exit + content items)

The `explain` server function assembles a failed trace's spans + correlated logs through the existing query layer and returns the structured summary; the Explain control appears on failed live traces (today `TraceExplorer.tsx:159` renders it only when `trace.explanation` is set, and `adapters.ts:8` leaves that undefined in live mode by design); the cloud path uses Claude via API and the self-hosted path takes a configurable model/endpoint (PRD §6.7); runs are rate-limited per plan and the "N of 20 free Explain runs" line stops being the hardcoded string at `ExplainPanel.tsx:111`; with no model configured the surface says so rather than fabricating. Plus the pre-registered M3 content items closed: the CommandPalette's mock trace links (D60 as widened by D63); the changelog/marketing "live tail" copy sweep (D60); the S1 shell-chrome M3-gate items (real org/identity/notifications replacing the SAMPLE markers — TopBar residue).

## Scope fence

No new telemetry, no incident/RCA surface (M6), no evals or prompt management (M7). The content pass is a truth pass over surfaces M3 already owns — it does not wire an M5/M6/M7 surface. No multi-provider abstraction (D102 REFUSED it — `@anthropic-ai/sdk` against Anthropic or any Anthropic-compatible endpoint via `OBSTACK_EXPLAIN_BASE_URL`/`OBSTACK_EXPLAIN_MODEL`/`OBSTACK_EXPLAIN_API_KEY`). Landing-page fence per D106 exactly: the "no signup, no setup" sentence + pricing-section copy truth + waitlist copy — nothing else on the M4-owned page. Changelog: **copy truth only**, no pipeline work (M4 owns the surface). No email (U4). No retention enforcement (M4). CI never calls Anthropic (U6): the deterministic fake carries CI and the e2e drive; the one real-key run is the human evidence run. D78 SDK API stays FROZEN.

## Binding inheritance (S3.4 exit bundle §5 addendum + §7 → S3.5 kickoff)

1. **e2e headroom: ~11s at the measured 5m49s.** D137 trip protocol governs; the pre-named reclaim is the ~35s flush+TTL propagation wait (drive-time TTL/flush override) — **the D207 reclaim is pre-authorized for this step growth and LIKELY needed.**
2. **Honesty-pass residue enumeration (per-surface, S1 L2):** mock-corpus fictions (`mock/docs.ts:85` `charts.obstack.dev`; `mock/mcp.ts:77,83,89` `mcp.obstack.dev` + `McpPage.tsx:57,59` CopyBtn; `ok_live_9f2e…` in `mock/workspace.ts:36`/`mock/security.ts:61`; the demo hub's fabricated "Vercel · 86/min" beside a SOON Vercel card — the D208 demo-data-vs-claim call); `WaitlistForm.tsx:27` "We'll email you when your invite is ready" (U4 class); TourGuide aspirational copy; switcher affordance + demo-workspace footer (D134); palette/changelog (D60/D63); landing sentence + pricing copy (D106 fence); TopBar residue.
3. **Pattern inheritance standing:** D121/D127/D129 forms, D68 corpora, D142 content-aware evidence bar, D150/D152 mock-mode guard + standing test on every new auth-surface server action/route, D189 reads-are-reads/PRG, D193 log-level grading, D198/D199 `TxQuery` brand for any lock-dependent write, D206 wording coupling (the drive's `isError` regex at `e2e-drive.mjs:2430` is load-bearing — any deliberate Explain refusal logged on an authenticated path must not match `/Error\b|⨯|unhandledRejection/`), D215 one-definition endpoint constants.
4. **Token hygiene now covers credentials (S3.4 §6):** the drive's sweep must also never print the Anthropic key or the fake's key; the D132 split protects credentials.
5. **Plan catalog is the one quota definition (D163):** the Explain quota joins `plans` — never a constant in either language.
6. **One-definition contracts with pinned vectors:** any amendment sweeps every implementer in the same round.
7. **M4/S5 pre-registration carry unchanged** (D218 windowed rate, D101 `ai@7` owner, D214 K8s external connect, Vercel+CloudWatch receivers, G1 hand-enumeration, D178/D187, D180, D153/D119/D123/D132, S5-GATE list incl. D200).

## Measured facts the breakdown stands on (survey 2026-08-20, HEAD `9c79eae`)

- **ExplainPanel** (`components/trace/ExplainPanel.tsx`, 116 lines): pure client presentational — props exactly `{explanation: Explanation, onClose}`; RAF fake-stream at :33-62 (~1100 chars/s, `prefers-reduced-motion` jump at :34-38); hardcoded `4 of 20 free Explain runs used this month` at :109-113 (matches `mock/workspace.ts:42 explainRuns {used:4, quota:20}`, also consumed by `SettingsSuite.tsx:782`); evidence renders as plain text :93-104 — **no links exist**.
- **`Explanation` type** (`lib/types.ts:61-67`): `{headline, failedWhere, rootCause, evidence: {label, detail}[], suggestion}`; `Trace.explanation?` at :85. **Evidence carries no span/log ids — "linked evidence" is a type + renderer change.**
- **Gate:** `TraceExplorer.tsx:159` button and :233 panel both gated on `trace.explanation`; `hasFailure` at :70. Live mode never sets it: `server/adapters.ts:271-291` `toTrace` never assigns `explanation`, **pinned by `adapters.test.ts:193/:199`** (goes red if the adapter changes). Unrelated fiction: `TraceExplorer.tsx:67` fabricated `https://obstack.dev/share/…` URL.
- **Scoped facade:** `forWorkspace(workspaceId): ScopedClickHouse` (`server/clickhouse.ts:98-109`), one method `queryRows`; tripwires at :69/:75. **`queryTrace(ch, id)` (`server/queries/traces.ts:332-361`) returns the whole `Trace` incl. spans + correlated logs — the one call Explain needs.** Pages import only the `server/data.ts` facade.
- **Plans catalog:** Postgres only (`services/ingest/pgmigrations/0005_metering.sql:118-126`): `id,name,event_quota,retention_days,price_usd_month`; free 50k/7d/$0, pro 1M/30d/$49. **No explain/rate-limit column. Next migration is `0006_*.sql`.** `workspace_plans` absent-row=free via COALESCE join. TS reader `server/usage.ts` (`Plan` :48-54, `getUsage` :155 React-cached, `listPlans` :179).
- **The 20/200 Explain-run numbers exist only in fiction:** `ExplainPanel.tsx:111`, `mock/workspace.ts:42`, landing `app/page.tsx:398` ("20 Explain runs/mo" Free) / :405 ("200 Explain runs/mo" Pro). Nothing in Postgres backs them.
- **No rate limiter exists in web code** (grep: all `429` hits are mock narrative). Quota enforcement precedent is monthly + Go-side (keystore/store.go:120). **An Explain run counter is net-new:** plans column + counter storage (D99 ledger class: `usage_ledger` UPSERT-add, PK (workspace_id, period_start) hour buckets) + a check in the route. **TS-side Postgres write precedent exists** (`billing/reconcile.ts:34` INSERTs `workspace_plans`).
- **Zero AI deps in `apps/web`** (`package.json`: no `ai`, no `@anthropic-ai/sdk`, no `openai`; `@anthropic-ai/sdk` is only an `obstack-js` devDep ^0.117.1). **No `ANTHROPIC_*`/`OBSTACK_EXPLAIN_*` env name exists anywhere yet** (grep 0 hits — S3.4 L3 namespace check done).
- **Mode-selector pattern to copy:** `OBSTACK_BILLING_MODE` (`server/billing/client.ts:28`, default `"fake"`), fake at `billing/fake.ts` ("not a stub: the rail with the third party removed — the only one CI ever runs", D168), one-import-path barrel `billing/index.ts`. **Deterministic streaming fake precedent:** `packages/obstack-py/tests/fake_provider.py` (D77(d), has an SSE path at :64; `API_KEY="obstack-test-no-credential"`); TS: `obstack-js` `fakeProvider(RESPONSE)` harness.
- **Route-handler template verbatim:** `app/app/onboarding/status/route.ts` — mock guard FIRST (404 + `console.error` tripwire), bare 401 no-session ("a poll is not a navigation"), workspace from `getSessionContext()` never the URL (D96/D148), `cache-control: no-store`. `getSessionContext` verified to work in route handlers (`server/session.ts:79-80`).
- **Server-action gate:** `settingsSession` shape (`settings/actions.ts:81-89`) — mode check FIRST (D150/D152); standing test `app/signup/mock-mode.test.ts` enumerates actions (G1: hand-enumerated).
- **Honesty targets, measured:**
  - `CommandPalette.tsx:46-52`: `storyTraces` → `/app/traces/<mock-id>` hrefs unconditionally, live mode included (404-class); static `pages` :25-44 lists 14 unwired routes.
  - `TopBar.tsx`: fallback identity `"Ziad Abdelsalam"`/`"ziad@loopwork.ai" · owner` (:115,:118), dead Docs `href="#"` (:129); mock-gated fabrications `ingesting · 9.4k events/min` (:181), `PROD · EU-CENTRAL` (:190).
  - `SideNav.tsx:93-114`: **dead switcher** — chevroned button, no onClick, no menu; mock says `Loopwork`/`loopwork-prod` (:102,:110); `data-workspace-id` (:107) is drive-read (D115).
  - **D134 gap confirmed:** in mock mode (the public demo) NOTHING on screen says "this is a demo" — `SampleDataBadge` renders only in live mode (`layout.tsx:116`); no demo footer exists.
  - `TourGuide.tsx` fiction lines: :22,:28,:34,:46 ("Try Explain this trace"),:68,:74,:86 (competitive claim),:98 ("Vercel, CloudWatch work today" — contradicts D208 SOON cards),:104 (MCP "every call is audit-logged"),:110.
  - `WaitlistForm.tsx:27`: "We'll email you when your invite is ready" — claims an email pipeline (U4) and an invite flow; the action only writes a row. Landing :123-126 already carries the honest wording ("Leave your email and we'll write when we do") — the two contradict.
  - Landing `app/page.tsx`: the sentence at :456-458 "The demo is live sample data — no signup, no setup" (self-contradictory); pricing :388-446 — Free/Pro restate catalog + unbacked seats/Explain-runs; **Scale (:407-412) and Self-hosted (:413-418) tiers have no `plans` row and no product**; comparison table :18 claims Explain shipped.
  - Changelog `app/changelog/page.tsx`: 8 static entries, all announcing mock-only features; "live tail" :27 (the single repo hit); "Explain this trace" :42-47; :51-52 claims Vercel/CloudWatch "at launch".
  - Mock corpus: `docs.ts:85` copy-pasteable fake helm repo (guard `connectors.test.ts:21` does NOT cover docs.ts); `mcp.ts:77,83,89` + `McpPage.tsx:59` CopyBtn for `https://mcp.obstack.dev`; `ok_live_9f2e…` `workspace.ts:36`/`security.ts:61`; `SettingsSuite.tsx:590` full demo token literal (docblocked); `connectors.ts:24-31` Vercel `86/min` demo row (mode-tagged, structurally honest per D208).
  - `live-routes.ts:11-24` registered: `/app`, `/app/traces`, `/app/traces/` (subtree), `/app/logs`, `/app/onboarding`, `/app/connections`, `/app/settings`; exclusion `/app/traces/diff`; drive positive control `/app/costs` (D205).
- **e2e drive:** 2497 lines, 44 steps/151 runtime checks. **An Explain step must land BEFORE :2361 (`alice signs out`)**; thematic anchors :1893 (EVIDENCE_FREE_QUOTA override, D172) and :2080. Constraints inherited: the :2430 `isError` regex (D206) and the :2448 token-hygiene sweep (extends to any Explain credential).

## Draft task breakdown (PM draft — returns to the advisor before dispatch)

- **T1 — the quota substrate: `plans.explain_quota` + the explain-runs counter + one TS definition.**
  owns: new `services/ingest/pgmigrations/0006_explain_quota.sql`, `apps/web/src/server/usage.ts` (extend `Plan`/`getUsage` or a sibling reader — advisor rules the shape), new `apps/web/src/server/explain/quota.ts` (+ tests).
  Migration adds `explain_quota` to `plans` (free/pro values — advisor confirms 20/200, the numbers the landing JSX already promises) and the run-counter storage in the D99 ledger class (hour-bucketed UPSERT-add or a monthly row — advisor rules; TS-side write per the `reconcile.ts` precedent). One definition: the panel counter, the settings meter and the route's enforcement all read the same function. D163 holds: no constant in either language.
  depends: —. Wave 1.
- **T2 — the Explain engine: mode selector + Anthropic client + deterministic streaming fake + prompt/validation contract.**
  owns: new `apps/web/src/server/explain/` (client selector, `fake.ts`, `anthropic.ts`, prompt assembly, response validation, **`contract.ts` — the D227 NDJSON frame, one definition**; barrel index), **`apps/web/src/lib/types.ts` (the D223 evidence amendment — moved from T4 per D231.1)**, `apps/web/package.json` (+`@anthropic-ai/sdk`), (+ tests).
  `OBSTACK_EXPLAIN_MODE = "fake" | "anthropic"` default fake, the `billingMode()`/`billing/fake.ts` pattern verbatim (D168 class: the fake is the engine with the provider removed — streams deterministically, same code path). Anthropic path: `@anthropic-ai/sdk` streaming; env `ANTHROPIC_API_KEY` (cloud) / `OBSTACK_EXPLAIN_BASE_URL`+`OBSTACK_EXPLAIN_MODEL`+`OBSTACK_EXPLAIN_API_KEY` (self-hosted, D102). Input: the `Trace` from `queryTrace` via the scoped facade — spans + correlated logs only. Output: validated server-side into `Explanation`; **evidence ids validated against the trace's actual span/log ids — a model-invented id is dropped and the drop is stated** (D102). No model configured in anthropic mode → a typed "not configured" result, never fabrication.
  depends: —. Wave 1.
- **T3 — the route: streaming Explain handler with per-plan enforcement.**
  owns: new `apps/web/src/app/app/traces/[id]/explain/route.ts` (or advisor-ruled path), extension of the mock-mode standing test, (+ tests).
  The `status/route.ts` template verbatim: mock guard first, bare 401, workspace from session never URL; POST (an Explain run is a spend — reads-are-reads D189). Enforces the plan quota from T1 before calling T2 (over-quota → honest refusal payload, wording D206-safe); increments the counter on a run; streams T2's output; never logs the trace content or any key.
  depends: T1, T2. Wave 2.
- **T4 — the surface: real streaming panel, live gate, linked evidence.**
  owns (as corrected by D231): `components/trace/ExplainPanel.tsx`, `components/trace/TraceExplorer.tsx` (incl. deleting the `:67` fabricated share-URL — render none, D231.4), `components/settings/SettingsSuite.tsx` (the `:782` explain-runs meter → the T1 reader in live mode; mock unchanged, D231.3), mock `stories.ts` evidence backfill (MANDATORY per D223), (+ tests). **NOT owned: `adapters.ts`/`adapters.test.ts` (D224 — pins stand byte-unchanged; touching them is an escalation), `lib/types.ts` (T2's), `mock/workspace.ts` (T6's — the mock counter path keeps reading `explainRuns` unchanged, D231.5).**
  RAF fake DELETED (D102); the panel consumes the real stream in live mode and renders the three mock stories' prepared `Explanation`s in mock mode unchanged (D208 demo-data ground); the Explain button appears on failed live traces; evidence renders as links to the trace's own spans/logs; the :111 line becomes the real per-plan counter from T1; "not configured" and "over quota" render as stated refusals. `prefers-reduced-motion` semantics for a real stream stated.
  depends: T2 (contract), T3 (route). Wave 2–3.
- **T5 — honesty: shell chrome + demo truth (D134).**
  owns: `components/shell/TopBar.tsx`, `SideNav.tsx`, `CommandPalette.tsx`, `TourGuide.tsx`, `NotificationsPanel.tsx` if touched, **`app/app/layout.tsx` (mock branch only — the D228 demo footer)**, (+ tests).
  CommandPalette: mock trace links leave the live path (D60/D63); the static pages list stops offering unwired routes in live mode or is truth-labelled — advisor rules the shape. TopBar: fallback identity/fabricated `9.4k events/min`/`PROD · EU-CENTRAL` become honest demo labels; dead Docs link resolved. SideNav: the dead switcher either becomes a real minimal affordance or stops promising one (D134 — advisor rules); `data-workspace-id` preserved (drive contract). D134 demo-workspace footer: mock mode states "demo data" persistently. TourGuide: every fictional claim rewritten to present-tense truth (the Explain line may become TRUE this sprint — coordinate with T4's landing).
  depends: — (content); T4 for the Explain tour line only. Wave 1 (that one line deferred to a same-file follow-up if T4 lands later — advisor rules).
- **T6 — honesty: marketing + mock corpus (D106 fence, exactly).**
  owns: `app/page.tsx` (the :456 sentence + pricing section + comparison row ONLY), `components/marketing/WaitlistForm.tsx`, `app/changelog/page.tsx` (copy only), `mock/docs.ts`, `mock/mcp.ts`, `components/mcp/McpPage.tsx`, `mock/workspace.ts`, `mock/security.ts`, `components/trace/TraceExplorer.tsx:67` share-URL — **conflict: TraceExplorer is T4's; the share-URL line moves to T4's list**, (+ guard-test extensions: the `connectors.test.ts:21` lie-list pattern extended to docs.ts/mcp.ts).
  WaitlistForm :27 → the landing's own honest wording (U4). Landing sentence describes the demo truthfully; pricing tiers state only what exists (Scale/Self-hosted — advisor rules: delete, or re-label as intent); Explain comparison row goes true only if T4 ships. Changelog entries stop claiming unshipped features (advisor rules the edit shape: re-word vs remove). Mock corpus: fake domains/keys replaced with format-true non-resolving placeholders or removed; McpPage CopyBtn stops copying a fake endpoint.
  depends: T4's landing decides two lines (comparison row, changelog Explain entry) — sequenced same-file at wave boundary. Wave 1 for the bulk.
- **T7 — e2e drive growth: the Explain step + honesty assertions + budget.**
  owns: `deploy/compose/e2e-drive.mjs`, `.github/workflows/e2e.yml` if the reclaim requires.
  New step(s) before :2361: alice opens a failed trace, runs Explain (fake mode — deterministic output), the panel streams to a stable result with evidence links resolving to real span ids, the counter line shows the real quota; over-quota refusal proven honest (EVIDENCE_FREE_QUOTA-class override on explain_quota — D172 pattern); no SAMPLE badge regression; demo-footer/switcher assertions per T5's landed shape. Budget: ~11s headroom — **the pre-authorized D207 propagation-wait reclaim is expected to fire**; first-real-runner number reported to the advisor (D137). Hygiene: the sweep covers any Explain credential; refusal wording D206-safe.
  depends: T1–T6. Final wave.
- **T8 — the evidence run + the two bundles (manager/advisor-owned, not an executor task).**
  The one real-Anthropic-key Explain run (U6 — the user supplies the key at evidence time; CI never spends), recorded with its output as exit evidence; the S3.5 exit bundle AND the M3 milestone bundle (D104 wording, U9 method) — always the advisor's to sign.

## Kickoff questions for the advisor

- **Q1 (contract):** D102 says `Explanation` stays the contract, but `evidence: {label, detail}[]` carries no ids — "linked evidence" needs a shape amendment (e.g. optional `spanId?/logRef?`). Rule the amendment + the mock-stories backfill obligation + the `adapters.test.ts:199` pin.
- **Q2 (counter):** hour-bucketed D99 ledger class vs a monthly counter row; written from TS in the route (first TS metering write — `reconcile.ts` precedent) — rule placement + TxQuery/lock needs (D197/D198 class: concurrent Explain runs racing the cap).
- **Q3 (quota values):** free=20/pro=200 (the numbers the landing already promises) into `plans.explain_quota` — confirm; and whether the landing pricing JSX must now read the catalog (D163 reach) or stays hand-written truth within the D106 fence.
- **Q4 (route path/verb):** POST `/app/traces/[id]/explain` streaming; does the panel fetch-stream client-side or is there a server-component leg?
- **Q5 (D134 shape):** minimal real switcher vs honest static label; demo-footer placement.
- **Q6 (changelog + Scale/Self-hosted tiers):** the edit shape for claims about unshipped features on M4-owned surfaces (copy-truth-only fence).
- **Q7 (mock Explain):** mock mode keeps the three prepared stories through the SAME panel while live streams — confirm the D208 ground covers the RAF deletion (typed-out effect goes; prepared content stays).
- **Q8 (waves):** proposed W1 = T1, T2, T5, T6 · W2 = T3 · W3 = T4 · W4 = T7. T5/T6 same-file follow-ups for Explain-dependent lines — rule the sequencing.

## Wave plan (D232 — RULED)

- W1: T1, T2, T5, T6
- W2: T3
- W3: T4
- W4: T5-F1 + T6-F1 (Explain-truth lines only: TourGuide Explain line; landing comparison row; changelog Explain entry)
- W5: T7 (drive asserts the FINAL landed surface)
- Then: integrator gate → goal check → T8 (evidence run + S3.5 + M3 bundles, advisor-signed).

## Advisor rulings (D223+ — recorded as they land)

**Kickoff verdict (2026-08-20): CORRECTED AND APPROVED** — three boundary corrections (D231), one missing owner (the SettingsSuite meter), wave plan amended (D232). No missing task. Advisor spot-checked every load-bearing citation at HEAD `9c79eae` before ruling; survey trusted.

### D223 (Q1a) — `Explanation.evidence` amendment: `{ label; detail; spanId?; logRef? }`; mock backfill MANDATORY
Evidence items gain optional `spanId?: string` and `logRef?: string`. Optional at the type level because D102's drop-is-stated path means the renderer must handle unlinked items anyway — an evidence line whose model-supplied id failed validation renders unlinked with the drop stated. **Mock-stories backfill is mandatory**: the three prepared `Explanation`s in `mock/stories.ts` gain real span ids from their own mock traces, so mock and live render through one renderer with no mode fork — the demo demonstrates the true product shape. `logRef` resolves to the trace view's correlated-log anchor, `spanId` to the span row; T4 rules nothing here — the shape is closed.

### D224 (Q1b) — The gate MOVES to `hasFailure`; the adapter and its pin stand UNTOUCHED
`TraceExplorer.tsx:159/:233` re-gate: mock mode keeps `trace.explanation` (prepared stories), live mode gates the Explain button on `hasFailure` alone — the explanation is fetched on demand via the route, never part of `toTrace`. `server/adapters.ts` continues to never assign `explanation`; the `adapters.test.ts:193/:199` pins stand byte-unchanged. Consequence: `adapters.ts` and `adapters.test.ts` leave T4's owns list — if T4 finds it must touch them, that is an escalation, not an edit.

### D225 (Q2) — Counter: MONTHLY row, one atomic statement, no lock, no TxQuery
New table `explain_runs (workspace_id text, period_start date, used int, updated_at, PK (workspace_id, period_start))`, `period_start = date_trunc('month', now() AT TIME ZONE 'UTC')` — calendar month UTC, same period definition as D163's over-quota clause. Hour buckets REFUSED: D99's hour granularity exists for idempotent batch reporting; Explain is a synchronous single increment racing a cap — buckets buy nothing and complicate enforcement. Enforcement and increment are ONE statement (the reconcile.ts TS-write precedent): `INSERT … ON CONFLICT (workspace_id, period_start) DO UPDATE SET used = explain_runs.used + 1, updated_at = now() WHERE explain_runs.used < $quota RETURNING used` — zero rows returned = over quota, refused. Single atomic statement ⇒ no advisory lock ⇒ D198 `TxQuery` brand does not apply. Order in the route: mock guard → session → **config check** (not-configured refuses BEFORE any spend) → atomic increment-or-refuse → provider call. A provider error after increment is a counted run — no refund path; stated in the route comment. Fake mode increments identically (D168: same code path).

### D226 (Q3) — free=20 / pro=200 CONFIRMED into `plans.explain_quota` (migration 0006); landing stays hand-written truth
The numbers the landing already promises become the catalog of record — same adoption move as D163's 50k/1M. `0006_explain_quota.sql`: `ALTER TABLE plans ADD COLUMN explain_quota int` + seed + the `explain_runs` table. D163's reach stops at product surfaces: panel counter, settings meter, and route enforcement all read one TS function (`server/explain/quota.ts`); the landing pricing JSX stays hand-written copy verified true at review — wiring the M4-owned page to Postgres exceeds the D106 fence. No test coupling across the fence.

### D227 (Q4) — POST `/app/app/traces/[id]/explain/route.ts`; client-side fetch-stream; NDJSON frame ruled now, owned by T2
Path and verb confirmed (a run is a spend — D189). No server-component leg: the run is user-initiated; the panel fetch-streams the POST response via ReadableStream. **Wire frame, one definition in `server/explain/contract.ts` (T2-owned), consumed by fake, anthropic, route, and panel:** newline-delimited JSON, `content-type: application/x-ndjson`, `cache-control: no-store`; events `{type:"delta", text}` → terminal `{type:"result", explanation: Explanation}` | `{type:"refusal", reason: "not-configured" | "over-quota", detail}`. The fake emits the identical frame (D168: the engine with the provider removed). Route template = `onboarding/status/route.ts` verbatim: mock guard first (404 + tripwire), bare 401, workspace from `getSessionContext()` — trace id from the URL is a resource id and is tenancy-safe because `queryTrace` runs through `forWorkspace`. Refusal wordings must not match `/Error\b|⨯|unhandledRejection/` — D206 mirrored-predicate test extended to the Explain refusal strings (T3 owns the extension, same pinned-mirror shape as `reconcile-wording.test.ts`).

### D228 (Q5) — Switcher: HONEST STATIC LABEL. Demo footer: app-shell layout, mock branch
A real switcher is speculative — no M3 requirement has a user in two workspaces. `SideNav.tsx`: chevron/button affordance deleted; workspace name renders as a static label (live: real name; mock: the demo workspace's name); `data-workspace-id` preserved (D115 drive contract). D134 footer: a persistent slim bar rendered from the app shell layout's mock branch (`app/app/layout.tsx`) stating the workspace is a demo with sample data — layout placement so every mock surface carries it, no per-page duplication. `layout.tsx` (mock branch only) joins T5's owns.

### D229 (Q6) — Changelog: rewrite-to-truth, delete entries with no true referent. Scale/Self-hosted tiers: re-label as PLANNED, strip unbacked specifics
Changelog (copy only): each entry restates what actually shipped at its date; an entry with no true referent is deleted; "at launch" Vercel/CloudWatch claims go — consistent with D208 SOON cards. The Explain entry and the landing comparison row go true ONLY after T4 lands (the W4 follow-up, D232). Scale/Self-hosted pricing tiers: re-label as stated intent ("Coming soon" class — the D101/D208 re-label precedent), deletion refused because self-hosted is a PRD commitment and the D102 `OBSTACK_EXPLAIN_*` config now partially backs it; every unbacked concrete number (seats, prices on those two tiers) is stripped — intent may be stated, specifics may not be invented.

### D230 (Q7) — CONFIRMED: RAF deleted everywhere; prepared stories render statically through the same panel
The typed-out RAF effect is a fabricated product behavior (goes, D102); the three prepared `Explanation`s are demo data (stay, D208 ground). Mock mode renders them instantly — no fake stream, no animation. Live mode renders real chunks as they arrive; a real stream is progress, not animation, so no `prefers-reduced-motion` fork exists — one sentence in the panel comment states this.

### D231 — Breakdown corrections (boundaries and owners)
1. **`lib/types.ts` amendment moves from T4 to T2.** T2's server-side validation drops invented evidence ids — it is the contract owner and needs the D223 fields; T4 consumes. Wrong boundary as drafted.
2. **`adapters.ts` + `adapters.test.ts` leave T4's owns** (D224).
3. **`SettingsSuite.tsx` explain-runs meter (the `:782` consumer) had NO owner — it is T4's.** The meter swaps from `mock/workspace.ts:42` to the T1 reader in live mode; mock rendering unchanged.
4. **`TraceExplorer.tsx` is exclusively T4's — CONFIRMED**, including the `:67` fabricated share-URL (delete the fabrication; a real share feature is out of scope — render no share URL rather than a fake one).
5. **`mock/workspace.ts` stays exclusively T6's**; T4's mock counter path keeps reading the existing `explainRuns` object unchanged — no cross-edit.

### D232 (Q8) — Wave plan AMENDED
- **W1: T1, T2, T5, T6** (T2 now carries the types.ts amendment + the D227 contract file — T4 and T3 both build against ruled, landed contracts).
- **W2: T3** (needs T1's quota fn + T2's engine/frame).
- **W3: T4** (needs the route live).
- **W4: T5-F1 + T6-F1** — the Explain-truth lines only (TourGuide Explain line; landing comparison row; changelog Explain entry), same-file follow-ups after T4 merges.
- **W5: T7** — the drive asserts the FINAL landed surface; it starts only after the follow-ups land.
- Then integrator gate → goal check → T8.

### D233 — Critical items (must return to the advisor before merge)
1. **T1** — migration 0006 byte-check + the D225 atomic statement + one-definition proof (panel/meter/route read one function).
2. **T2** — fake fidelity (D168: same frame, same code path), validation drop-is-stated, credential hygiene (no key in logs; fake carries no real credential — the `obstack-test-no-credential` precedent).
3. **T3 + T4 joint** — end-to-end frame contract, D224 gate change, refusal wordings against the D206 mirror test, quota refusal honest.
4. **T7** — first real-runner number (D137); D207 reclaim pre-authorized and expected.
5. **T8** — the real-key evidence run + the S3.5 exit bundle + the M3 milestone bundle (D104 wording, U9 method) — advisor-signed, always. Ledger continues at D234.

### W1 outcome (2026-08-20)
T2 **fixed→approved** (reviewer catches: multi-line log content could forge a line of the fake's answer — oneLine() flattening + forgery pin test; prompt.ts privacy claim now test-backed). T5 **fixed→approved** (reviewer restored a TRUE "layout persists" claim the rewrite had falsified — WatchWidgets localStorage — and re-pointed the guard). T6 **approved** (all rewrites verified true against source). T1 **must-fix** on exactly the out-of-scope Go pin test; T1's own artifacts clean (SPEND_SQL = D225 + a zero-quota INSERT-arm guard, atomicity proven 40-parallel; migration nullable→UPDATE→SET NOT NULL).

### D234 (esc. 1) — Pin bump is T1-F1's; authorized exactly as proposed
The migration author owns every pin the migration trips. T1's owns list extends by exactly `services/ingest/internal/pgmigrate/qa_a6_migrations_test.go` for the 2-line bump (:106 len 5→6, :174 applied-set want-string) + the :15 header comment. Integrator-edits refused: the fix belongs in the reviewed fix slot, same round.

### D235 (esc. 2) — IN. A real person's PII is not a deferrable scope question; T6-F2 owns it
A real person's name/address in a public demo corpus is a defect class above copy truth — privacy, not fiction. `mock/oncall.ts:13,34` and `mock/inbox.ts:71,74,76` get a clearly fictional persona, format-true; the corpus-honesty guard pins the banned literals so they cannot return — same literals T5's shell test bans, one shared constant list if importable, else mirrored with the D206 naming-comment pattern.

### D236 (esc. 3) — The "not built yet" badge overstates absence. Tier wording ruled
Self-hosting is the mode that ships today; the un-built thing is the paid tier. Ruled wording class: the tier states "Self-host today — free, run it yourself" as present truth; the paid offering carries the planned label: price "Planned", no invented number, one line "A supported paid tier is planned." Consistency check: must agree with the hero :114 and the docs' helm path.

### D237 (esc. 4) — RATIFIED. The `ok_live_9f2e…` mask stays; the guard is the right mechanism
4-hex-masked, format-true, non-usable — honest demo data on the D144/D208 ground. The guard fails if anyone lengthens it toward a plausible credential. No rewrite.

### D238 — the `explain.test.ts:234` flake gets an owner NOW
T3's wave opens with a reproduction attempt (run the file 20x); deterministic-green closes it with the count recorded; any second failure makes it a **T2+T3 joint critical item** returning to the advisor before the route merges — a nondeterministic fake violates D168's premise. Ledger continues at D239.

### W2 outcome (2026-08-20)
T1-F1 **approve** (exactly the three D234 edits; QA A6 4/4 RUN+PASS on live Postgres — T1 CLOSES). T6-F2 **fixed→approved** (Salma Nabil persona; audit IPs → RFC 5737 203.0.113.0/24 regex-pinned; mirrored ban lists with a cross-file pin). T6-F3 **approve** (D236 wording verbatim; helm bullet matches docs + chart; intro-paragraph edit disclosed, inside the D106 pricing fence). T3 **fixed→approved** (D238 flake NOT reproduced — 20/20 green, recorded; reviewer closed a mirror-scan gap: the D206 scan now sweeps refusals at any console level, console.error the only deliberate exemption).

### D239 (esc. 1) — T6-F4 AUTHORIZED; D235's list was incomplete, this completes it
`workspace.ts:12` owner object is the same PII class: one-line swap to the Salma Nabil persona (demo coherence restored — the acting owner is a member) + `workspace.ts` joins `personaCarriers` in `corpus-honesty.test.ts:41`. D231.5 confirmed: persona line and `explainRuns` disjoint; T4's read untouched. Same-round fix slot.

### D240 (esc. 2) — RATIFIED; the order of record amends D225's sequence
A nonexistent trace must never cost a run; the trace read is a read (D189). **Order of record:** mock guard → session → trace read via scoped facade (404 absent) → config check (not-configured refusal, no spend) → atomic increment-or-refuse → provider call. The D233.3 joint return checks this exact sequence.

### D241 (esc. 3) — RATIFIED as the D227 completion; NO drive-allowlist rider — deliberately
Refusals are product outcomes: terminal `{type:"refusal"}` NDJSON over HTTP 200; status codes reserved for non-product outcomes (401 no session, 404 mock/no such trace). Binding on T4. Grading: refusal=warn D206-safe; mid-stream provider failure=error, deliberately. No allowlist rider: CI runs the fake, so the provider-failure error line must NEVER appear in a drive run — its appearance means the fake broke determinism or the route called a provider in CI, and the drive SHOULD go red. The line carries a stable greppable prefix, no trace content, no credential; the T3+T4 joint return verifies the wording. Ledger continues at D242.

### W3 outcome (2026-08-20)
T4 **fixed→approved under D244** (reviewer catches: close/reopen double-spend — run state lifted to the page, D189/D225 pin test; aria-busy on the streaming region). T6-F4 **approve** — the D235 PII fix is complete repo-wide; the banned literals survive only inside the two mirrored ban lists. Suite: 415 tests / 0 fail / tsc clean.

### D242 — T4's two boundary extensions RATIFIED (D234 class, properly disclosed)
`traces/[id]/page.tsx` (mode-gated dynamic imports, D114 byte-invariance held) and `settings/page.tsx` (+1 in the existing Promise.all) join T4's owns retroactively. Second disclosure also RATIFIED: deleting the whole share button+modal is D231.4 executed correctly — the "Public link" toggle was the same fabrication; the real copy-link stays.

### D243 — The per-view quota read is ACCEPTED, on the record
One indexed row per trace view, parallel with the page's own reads; gating on failure would serialize trace→quota and fork the prop shape. D109 pattern: reopens only on a measured number from real scale.

### D244 — D233.3 JOINT T3+T4 CRITICAL ITEM: PASS, with ONE rider
All clauses verified on the tree (frame one-writer-one-reader; D240 sequence verbatim; mirrored refusal predicate incl. emitted strings; no-refund trade documented; provider-failure line clean; cancel() releases the provider; close/reopen spends nothing). **Rider (W4, T3's file):** `explain-wording.test.ts`'s sweep covers route.ts only, but `validate.ts:123` warns on an authenticated path with a model-supplied id interpolated — extend the glob to `server/explain/*.ts` non-error lines; no wording change needed.

### D245 — GO for W4, then W5
W4 = T5-F1/T6-F1 (Explain-truth lines, now TRUE claims) + the D244 rider. W5 = T7: the drive walks the over-quota refusal ON PURPOSE, `EVIDENCE_FREE_QUOTA`-class override on `explain_quota` (D172), hygiene sweep covers any Explain credential, first runner number back to the advisor (D137; D207 pre-authorized). Ledger continues at D246.

### W4 outcome (2026-08-20)
T5-F1 **fixed→approved** (gate claim mode-scoped to live; card copy compressed to fit the tour card; coupling entries real). T6-F1 **fixed→approved** (comparison row verified TRUE byte-unchanged; changelog Explain entry rewritten dated Aug 20, "correlated" corrected to "trace id + nearby lines" to match adapters). T3-F1 **fixed→approved** (directory-read sweep over server/explain + route, >=2-line narrowing tripwire, `debug` added to the alternation).

### D246 (esc. 1) — T5-F2 owns BOTH edits of the self-satisfying live-tail guard fix
Drop `changelog/page.tsx` from `ALLOWED` in `TourGuide.test.ts` AND reword the changelog comment so it stops spelling the phrase. One atomic change — the one-line reach into T6's `changelog/page.tsx` pre-ratified (D234 class). Review checks the guard fails if the phrase returns anywhere in the file.

### D247 (esc. 2) — Fix NOW (W5, T2's file); level-separation, not sanitizing
M4 deferral REFUSED — the exposure includes T8's own evidence run. The warn line goes fully static (drop fact + count, no interpolation); the model-supplied reference moves to a `debug` line. Rule stated in the validate.ts comment: **untrusted content never enters a log level the drive's error predicate governs.** D102 drop-is-stated lives in the panel; the log is ops. Reviewer confirms the debug line is outside the swept levels by design.

**D245/D247: GO for W5** — T7 + T5-F2 + T2-F1. Ledger continues at D248.

### W5 outcome (2026-08-20)
T5-F2 **approve** (guard proven red on phrase-return; file restored byte-identical). T2-F1 **approve on implementation**, premise escalated. T7 **approve** — drive **166 passed / 0 failed**; the Explain step runs INSIDE the metering propagation wait (~3-4s of a 33s absolute-deadline window; ~9-12s of ~30s at the 3.07x ratio), runner projection unchanged at 5m49s, **D207 NOT consumed**; over-quota refusal read by the live :2430 predicate on an authenticated path (the drive-level D206 proof); the D241 mid-stream prefix asserted absent whole-log, deliberately un-allowlisted; EXPLAIN credentials deleted from served appEnv + swept + non-fake mode refused by the drive.

### D248 — D247's mechanism CORRECTED on the reviewer's falsification: the debug line is DELETED
Node's `console.debug` is stdout; the drive's predicate reads every line unleveled — "a level the predicate does not govern" does not exist in this stack. Option (b) (teaching the predicate levels) REFUSED — :2430 is the load-bearing constant the D206 record protects. Ruled: the debug line goes entirely; no information lost (panel states the drop via `droppedReferenceNote`; ops log keeps the static warn + count). Invariant restated in its true form in validate.ts: **model-supplied content never enters the server log at any level — the panel is where untrusted content is shown, stated as untrusted.** The explain.test.ts:284-285 false-premise comment corrected in the same slot. Same-round review confirms no interpolation of model content anywhere under server/explain/ (the directory sweep now proves this structurally). On the fix landing: integrator gate. The 5m49s runner number with D207 unconsumed discharges the budget watch (record in the bundle). T8 stays advisor-signed, both bundles; the real key never enters git, logs, or CI (U6/D132). Ledger continues at D249.

### T2-F2 outcome (2026-08-20)
**fixed→approved.** Debug loop deleted; the only console call under server/explain/ is the static warn+count (reviewer-confirmed); drop tests assert model text in NO log line across all five levels; forgery control-char assertion moved to the panel path. Reviewer noted one out-of-scope comment residue: `explain-wording.test.ts:93-94` still names "a model-supplied reference" as a swept interpolation that no longer exists — handed to the integrator as a candidate seam fix (comment-only).

**All 15 task slots approved: T1–T7 + T1-F1, T6-F2, T6-F3, T6-F4, T5-F1, T6-F1, T3-F1, T5-F2, T2-F1, T2-F2.** Integrator gate next.

## Goal check

### Integrator gate (2026-08-20): FIXED-GO round 1
One comment-only seam fix (explain-wording.test.ts:93-94 → cites D248). Evidence: fresh-stack **434/434 tests with live DSN (0 skips)**, tsc clean, `go test ./...` 11 ok (0006 pin re-run `-count=1`), `next build` clean with `/app/traces/[id]/explain` registered ƒ, **drive 166/0 in 129s** (mid the 118-154s band; 5m49s runner projection holds, D207 unconsumed). All seams pass; all four honesty guards red-proven by mutation and restored byte-exact; honesty sweep of record clean; D106 fence held at exactly 4 hunks; D224 adapters byte-untouched vs master. Non-blocking observation recorded: the drive's D172 overrides mutate shared compose Postgres — a DSN-backed suite run AFTER a drive shows 4 environment-ordering failures (pre-existing class); `down -v` restores 434/434. **Operational instruction for T8 (D249): `down -v` before any DSN-backed suite run in the evidence sequence.**

### D249 — GOAL CHECK: MET, clause by clause, 0/3 fix rounds consumed
1. Structured root-cause summary on a failed live trace — MET (route→engine→panel proven end-to-end on the deterministic rail, drive 166/0; the cloud-provider leg's evidence is T8's real-key run by design — a named evidence obligation of the bundle, not a gap).
2. Linked evidence — MET (D223 shape; drive asserts links resolve to the trace's own span ids; mock backfill test-pinned; drop-is-stated in the panel per D248).
3. Rate-limited per plan — MET (plans.explain_quota + the D225 atomic statement on real Postgres; one definition proven end-to-end; over-quota refusal walked on purpose under the live :2430 predicate).
4. No unlabelled sample content on any stranger-reachable surface — MET on the integrator's sweep of record.

**GO: commit, push, PR to master (S3.4 precedent).** T8 protocol binding: user's real Anthropic key via env at run time only — never git, never logs, never CI (U6/D132); evidence-run output attaches to the bundle verbatim; both bundles (S3.5 exit + M3 milestone, D104 wording, U9 method) advisor-signed at the effective tip, checks green. Ledger continues at D250.

### T8 outcome (2026-08-20) — evidence run DONE; BOTH BUNDLES SIGNED
PR #20 opened @ `2443922`, **all eight checks green, e2e 5m47s = the runner number of record** (D137 discharged, D207 unconsumed). Evidence run (U6, key via env at runtime only, `down -v` first): real signup → in-product key → OTLP failing trace `7d807f489c68184ff2f29ec048e258dc` in `ws_4d249e8eba9ebc8b` → POST explain: 200 x-ndjson no-store, 9 deltas + one terminal result, 8.18s, `claude-sonnet-4-5-20250929`. All 3 evidence ids resolve in-trace; zero dropped; `explain_runs` used=1 → panel 1 of 20. Hygiene: `sk-ant` 0 hits everywhere; app log zero model content — the D248 invariant held under the real provider. Genuine-reading check passed (pool-exhaustion fusion the fake cannot produce). Artifacts: `.planning/2026-08-20-s3.5-explain-evidence-transcript.ndjson` + `-notes.md`. **Advisor signed the S3.5 exit bundle (`.planning/2026-08-20-s3.5-exit-evidence.md`) and the M3 milestone bundle (`.planning/2026-08-20-m3-milestone-bundle.md`) — M3 CLOSED at the effective tip.** Post-merge ledger continues at D250.
