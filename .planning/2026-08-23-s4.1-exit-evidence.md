# S4.1 Exit Evidence — the distributable artifact

meta: sprint S4.1 (M4 sprint 1) · plan `.planning/2026-08-23-s4.1-team-plan.md` · rulings **D265–D284** (kickoff + two escalation rounds) on top of phase rulings D250–D264 · mode: **manager-executes** (user directive mid-run: Workflow orchestration retired; manager implemented, two batched Opus@xhigh reviewers + single advisor + single integrator kept the gates) · iteration **1/3** · integrator **GO round 1, zero seam fixes** · evidence tip: the post-14:52Z uncommitted tree on `m4-planning` @ base `a11ae7a` (all pre-14:52Z compose evidence superseded — a stale web image served the stack until the integrator rebuilt it; recorded, not glossed) · **merge/commit remains the user's call.**

## The run-goal, met clause by clause

> obstack M4 sprint 1 — the distributable artifact (exit: a clean checkout produces the web and ingest images, the compose bundle and the Helm chart both boot the full stack from those same images, a mode-mismatched serve of the web artifact refuses rather than renders, and ingest shuts down cleanly inside the chart's termination grace).

1. **"a clean checkout produces the web and ingest images"** — one root-context Dockerfile at `apps/web/Dockerfile` (D251(a), vendored-doc-cited standalone build), both D262 variants build (`obstack-web:live`/`:mock`); ingest's existing image re-proven in the bundle context; the new K3 forever-job **`images`** builds both, runs the refusal matrix, boots the mock demo artifact standalone, and boots the compose bundle from the same commit. The reviewer caught and killed a non-hermetic context (`.env.local` secrets entering the build) before it ever shipped.
2. **"the compose bundle … boots the full stack from those images"** — cold `down -v` boot to all-healthy (clickhouse, postgres, ingest, web) with `/login` 200; integrator's cold full-rebuild 4m26s with a **real signup through the product's own server action** (303 + session cookie + workspace row) and 9 authenticated surfaces 200. Secret posture: `OBSTACK_BETTER_AUTH_SECRET` host-side only, one in-container name `BETTER_AUTH_SECRET` (D272), no baked default — the boot check refuses instead (D265(a2)).
3. **"and the Helm chart both"** — chart 0.3.0 (D268), extended in place: web workload + optional Ingress for web AND ingest (the D214 surface), StatefulSets+PVCs (D253/D270 breaking cut, documented), chart-owned Secret with `existingSecret` on every credential and **zero password substrings inside any rendered DSN** (D275), managed-Postgres as a real mode (D284: in-chart store gone, pg-migrate retargets, the Job IS the reachability gate), resources on all 7 workload containers (D283). Virgin-kind acceptance PASS **226s** and, post-D283, **153s** with 9/9 PodScheduled — the scheduling proof. Brought-Secret install converged end-to-end. **D276:** "The 0.2.0→0.3.0 upgrade refusal was measured twice — once as the naked data-loss repro, once with the guard: UPGRADE FAILED, revision 1, prior Deployments untouched."
4. **"a mode-mismatched serve … refuses rather than renders"** — the stamp-file + OCI-label mechanism (D267) with `console.error` + `process.exit(1)` (D273 — a `register()` throw measurably does NOT stop Next 16.3.0); four-outcome matrix proven on real containers three independent times: mismatch rc=1 naming both values, runtime-unset rc=1, live-without-secret rc=1 with the `openssl rand -base64 32` instruction, stamp-absent no-op (checkout serves — proven by the drive itself).
5. **"ingest shuts down cleanly inside the chart's termination grace"** — `Writer.Close(ctx)` threads the 10s shutdown deadline; the retry ladder gates on the LIVE deadline between attempts with abandoned rows counted `ReasonShutdown` (D278); `beginClose(ctx)` publishes the deadline to BOTH batchers before either is waited (the infra review's measured ≈2×writeTimeout catch — the bound of record is deadline + one writeTimeout **shared across both batchers**); grace explicit at **45s** in chart and compose, sized for that bound. Red-then-green on the ctx threading, the multi-batch drain, and the shared-deadline fix; `-race -count=3` clean.

## Statements of record (advisor's verbatim wording)

- **Same-images:** "This sprint discharges the build half of PRD line 28. The web image is the only sanctioned self-hosted serve (D251(d)); the cloud web tier's serve path awaits U10, and any deviation for that tier will be recorded as a user-owned PRD deviation, never silently."
- **D274:** "The expected unauthenticated flow produces zero error-class lines **by construction** (redirect, never a throw) — proven at server level three independent ways. No claim of NoSessionError elimination beyond that construction is made." **D274.4 branch taken: the digest marker is REMOVED** — the post-refit grep found every remaining `dataForSessionContext` caller guards null first, so no request-path caller can throw; the class survives only on the non-request `dataForSessionContext(null)` contract (`tenancy.test.ts:210` untouched).
- **D284:** "Managed-Postgres is proven in shape against an out-of-release store; **real-provider verification (TLS/sslmode, provider auth) is pre-registered to S5** and recorded in README and values." Evidence: managed install PASS 28s with both Jobs Complete and `workspaces`/`saved_views` present in the EXTERNAL store; unreachable-DSN install rc=1 with the Job Failed and the lookup error named.
- **D280:** "All timing numbers in this bundle are local working references; the deciding numbers are the first real-runner runs, pre-registered to return to the advisor." Local references: cold boot 32s (pre-D281 image) / 4m26s full rebuild; smoke 47s web-surviving, zero web-build lines; drive 134s → **166/0 in 151s** at the integrator tip; acceptance 226s → 153s.
- **D281/D282 (display truth):** endpoint one-definition across lib constants → server resolver → Quickstart/connections modal, placeholder-substituted at render; four-corner matrix proven in unit tests (no-empty-export class test) and live (integrator's both-corner probe: public endpoint ×3, loopback occurrences 0, empty-export shapes 0). The falsified D265(a1)/D269 mechanisms were replaced on measurement (D271/D274) — both falsifications were the xhigh reviewers' work.

## Advisor signature

> **ADVISOR SIGNATURE: S4.1 exit bundle SIGNED (2026-08-23).** S4.1 — the distributable artifact — exit criterion met clause-by-clause on the integrator evidence of 2026-08-23 (evidence tip: the post-14:52Z tree; all pre-14:52Z compose evidence superseded, as the bundle itself records). Iteration 1 of 3; zero seam fixes at integration. Merge/commit remains the user's call.

**Two conditions ride the signature (post-merge returns, not blockers):**
1. First real-runner numbers for `images` and `lint`, plus the first post-merge `e2e` number, return to the advisor (D137/D261 shape) — trigger policies confirm then; D207 remains unconsumed.
2. This bundle records the D274.4 branch actually taken — **done above: marker removed.**

**Corrections of record (advisor's own, non-void-making):** the evidence file's "D284 shared-deadline correction" is the **D278** correction; the bound of record is deadline + one writeTimeout shared across both batchers.

## S4.2 kickoff inheritance (binding, verbatim from the signature)

1. D252 measured questions at breakdown, results attached to the critical-item return: lightweight `DELETE` vs `ALTER TABLE … DELETE` semantics, mutation cost against `toDate` partitions, behavior on the `AggregatingMergeTree` + MV pair.
2. D105 binds: disclaimer removal (`SettingsSuite.tsx:726`) in the same change as enforcement; exit evidence proves both directions for at least two tiers; absent plan row = free (D163); outer TTL migration to 90d lands with the sweep in the same sprint.
3. D254 receiver contract: routes on the existing mux behind the D98 bearer path; forwarder = dependency-free single-file Node zip, no SAM/Terraform; Vercel drain format + verification handshake and the CloudWatch `awslogs` envelope measured at breakdown with fixture provenance recorded; SOON cards do NOT flip (D101/D208); no invented correlation.
4. D260: windowed-rate schema on the D100 rows; the hub display flips to windowed in the same sprint — one definition.
5. D264d: the D180 gate does not fire; the sprint plan carries the not-the-M6-engine fence verbatim (one fixed cadence, one query shape, no user schedules, no notifications).
6. Live shutdown confirmation rides S4.2's evidence run: observe one rolling restart landing inside the 45s grace with zero uncounted loss.
7. Named small item: the stamp path spelled twice (`Dockerfile:76` writes, `mode-stamp.ts:26` reads) becomes a D206-class mirror test.
8. Standing: environment-ordering `down -v` before DSN-backed suites; D178 re-measure on any `@polar-sh/sdk` bump; `e2e` envelope inherited consciously (local reference 151s).
9. Recorded artifacts, not defects: collector Pending under two coexisting releases on one node (S5 topology note); store resource limits reopen-on-measurement at S5 (D283); managed-PG real-provider leg at S5 (D284).
10. Pre-commit hygiene: the stray `.planning/scratch-t4-chart-storage-secrets.md` — **removed.**
