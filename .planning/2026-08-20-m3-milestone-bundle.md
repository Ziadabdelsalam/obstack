# M3 Milestone Bundle — exit met

meta: milestone M3 · exit wording **D104, user-signed 2026-08-18** · method **U9 = the D107 `e2e` job** (one drive, one definition — the full stranger path against the real compose stack) · effective tip `2443922` (PR #20, eight checks green, e2e **5m47s**) · environment: **U1 = LOCAL** — M3 proves the stranger path locally under the ratified U9 protocol; deployment and public reachability are M4/S5's, and this narrowing is stated here rather than glossed (E21/E30 closure, phase plan §user-round 5).

## The D104 exit, verbatim, met clause by clause

> **Exit:** a stranger signs up, connects a source, and reaches a correlated trace unassisted — on a workspace that meters its own usage against a plan, degrades to sampled ingestion behind a banner rather than a hard cut when that plan's quota is exceeded, and can explain a failed trace on demand.

1. **"a stranger signs up"** — S3.1 exit bundle (`.planning/2026-08-19-s3.1-exit-evidence.md`): the `e2e` job born with the signup + tenancy-disjointness drive; S3.2 exit bundle (`.planning/2026-08-19-s3.2-exit-evidence.md`): real `/signup`, DB-backed keys, invites, settings.
2. **"connects a source"** — S3.4 exit bundle (`.planning/2026-08-20-s3.4-exit-evidence.md`): onboarding + connections real, quickstart truth, D208 card honesty; in-product key issuance on the stranger path.
3. **"and reaches a correlated trace unassisted"** — S3.4 §5 addendum: the full stranger path (signup → key → send → correlated trace) green in the one drive; correlation itself of record since S3.1/S3.2 lineage checks.
4. **"on a workspace that meters its own usage against a plan"** — S3.3 exit bundle (`.planning/2026-08-19-s3.3-exit-evidence.md`): ingest-path metering into the D99 Postgres ledger, one definition (banner == tab proven in-drive), plan change round-tripped through Polar sandbox; QA addendum `.planning/2026-08-19-s3.3-qa-fixes-plan.md` closed.
5. **"degrades to sampled ingestion behind a banner rather than a hard cut"** — S3.3 exit bundle: per-trace head sampling with whole-present/whole-absent pinned vectors, drops counted under `reason="quota"`, banner raised from real rows; fail-open/fail-static asymmetry on the record (D164(d)).
6. **"and can explain a failed trace on demand"** — S3.5 exit bundle (`.planning/2026-08-20-s3.5-exit-evidence.md`): the route over the scoped facade, real streaming, linked evidence resolving to the trace's own ids, per-plan rate limit from the one catalog, honest refusals; real-provider evidence run attached (transcript in-repo).

**Method of record (U9):** every clause above is exercised by the single D107 `e2e` drive at the effective tip — 166 checks, 0 failed, 5m47s on the runner — plus each sprint's advisor-signed bundle for surface-specific claims (the L5 durability rule held all sprint).

**Constraint attestations:** U4 — no email claim survives anywhere in M3 surfaces (WaitlistForm honest wording; PII class closed D235/D239). U6 — CI never called Anthropic; the deterministic fake carried every CI run; exactly one real-key run exists and it is the S3.5 §2 evidence run. D78 — SDK API remained frozen. D13/D21 honesty posture — the S3.5 sweep of record: no surface a signed-up stranger reaches renders unlabelled sample content.

## Consolidated M4/S5 gate inventory (nothing absorbed silently)

- **M4-GATE:** D105 retention TTL enforcement on the D7 tables + disclaimer removal, same sprint.
- **M4-PLANNING:** D103(a) JS logging bridge / streaming instrumentation / Responses API; D101 `ai@7` owner; D218 windowed-rate revisit; Vercel + CloudWatch receivers (the SOON cards flip); D214 K8s external connect; landing/changelog surfaces are M4-owned beyond the D106 fence; environment-ordering test class (S3.5 §5).
- **S5-GATE:** Polar sandbox→production promotion (D173.5 — production promotion remains a critical item); D23 dup-exposure revisit (D103(b)); `/signup` public reachability (U1); D200 list; G1 hand-enumeration watch continues.
- **Reopen-on-measurement:** D109 (skip index), D243 (per-view quota read).

> **Advisor signature — M3 milestone bundle.** M3 is CLOSED. The D104 exit wording is met in full on the U9 method at the effective tip, each clause standing on its sprint's advisor-signed bundle: S3.1, S3.2, S3.3 (+QA addendum), S3.4, S3.5 — all five exit-blocking sprints closed, D34-shape preserved. The U1=LOCAL narrowing is stated above as part of the exit, not beside it. **Effective at:** `2443922` plus the planning commit recording this bundle, eight checks green on PR #20 (confirmed). — advisor, 2026-08-20
