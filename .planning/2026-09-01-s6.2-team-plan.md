# Team Plan — obstack M5 sprint 2 (S6.2): the trace-derived query pack — map, services, users, traces/diff, issues render the signed-in workspace's real data in live mode, each flipped in `live-routes.ts` with honest empty states; query modules + UI wiring over EXISTING tables only; mock mode byte-identical

meta:
- date: 2026-09-01
- repo/branch: `s6-2` (to cut from `master` @ `25548fa`)
- mode: waves
- team-cap: 10
- models: executor = routed by complexity (Opus 5 `effort:high` for the query-module tasks, Sonnet 5 `effort:high` for mechanical riders) · reviewer = Opus 5 `effort:xhigh` · advisor = Opus 5 `effort:xhigh` (user, Step 0 — the S6.1 configuration)
- iteration-limit: 3 — fix rounds allowed before escalating to the user
- iteration: 0/3
- governing docs (binding): `.planning/2026-08-31-s6-m5-full-platform-phase-plan.md` §5 S6.2 (run-goal, scope fence), §7 standing constraints, §8 rulings **D360 (Option C), D361 (issues pull-forward, STATELESS fence), D362 (services deploys panel = rendered with `SampleMark`, not hidden), D364 (sequential)**; the S6.1 plan's standing rulings that generalize: D366 (contract types client-safe in `lib/*-types.ts`), D367 (server page branches on `dataMode`; mock body moves VERBATIM to `<X>Mock.tsx`; live is a props-fed sibling; byte-identity), D13/D21/D114, D7/D11 (tenancy tripwires per query module), D389 vocabulary rule.
- predecessor: S6.1 closed 2026-09-01 (PR #29, production `2d181d4`); retro lessons S6.1-L1..L3 apply (mock `next build` before `npm test` after a drive; rulings phrased as observable results; ordering constraints measured).

## Foundation facts (audited 2026-09-01, this session — Explore inventory + DDL)
- Tables available (NO new DDL this sprint — §5 fence; any need escalates): `spans` (PARTITION BY toDate(start_time), ORDER BY (workspace_id, trace_id, span_id), TTL 30d; promoted columns `service`, `parent_span_id`, `kind`, `layer` Enum8, `status_code` Enum8('unset','ok','error'), `status_message`, `name`, `start_time`, `duration_ns`, gen_ai/cost columns, `attributes Map`, `resource_attributes Map`), `logs` (same partition/TTL; `severity_number/text`, `body`, `service`, k8s cols, Maps), `trace_summaries` AMT (ORDER BY (workspace_id, trace_id); `min_start`, `max_end`, `span_count`, `error_count`, tokens, `total_cost_usd`, `services[]`, `models[]`, `root_name/method/service` argMinIf states, `max_seen_date`), plus S6.1's metrics objects (not used here).
- `enduser.id` is NOT a promoted column anywhere (zero hits in ingest + web) — if an SDK sends it, it lives in `spans.attributes['enduser.id']` (the `flattenAttributes` path, `spans.go:79`). `http.status_code` likewise Map-only. `service.name` → `service` column.
- Reader rules: `forWorkspace(workspaceId)` → `ScopedClickHouse.queryRows(sql, params)`; every SQL carries `{workspace_id:` bound from scope (runtime tripwire); trace_summaries read with `-Merge` combinators, never FINAL (traces.ts:74–88).
- Wiring pattern (the finish line): `connections/page.tsx` — mock branch returns FIRST (before `connection()`/session), live branch reads query modules directly with `forWorkspace(session.workspaceId)` (explore's S6.1 form); flip = one line in `lib/live-routes.ts`; guard test = `explore/page.test.ts` (source-text A2 guard: no `@/mock/` in the live graph, mock branch before `await connection()`).
- Per surface today:
  - **map**: `app/app/map/page.tsx` (5 lines, server) → `components/map/ServiceMap.tsx` (165 lines, "use client", NO fixture — 11 nodes/12 edges are literals at :29–56 with hand-placed x/y; rendering :60–165). No tests.
  - **services**: `services/page.tsx` (96, server) + `services/[id]/page.tsx` (172, server, async params) read `mock/catalog.ts` (`ServiceEntry` {id,name,layer,team,tier,runtime,deps,sloStatus,score{coverage,alerts,runbook,slo}}, `grade()`; importers = ONLY these two pages → editable) and `[id]` also reads `deploys` from `mock/intelligence.ts` (shared with evals/alerts/oncall/AskChat → A2-protected, untouchable). No components dir; markup inline. No tests.
  - **users**: `users/page.tsx` (104, server) reads `mock/users.ts` (`ImpactedUser` {org,user,plan,reqs7d,failures7d,slow7d,lastFailure{label,traceId,href,when},risk}; sole importer → editable). No tests.
  - **traces/diff**: `traces/diff/page.tsx` (10, server, Suspense) → `components/trace/TraceDiff.tsx` (188, "use client") imports `allTraces,getTrace` from `mock/traces.ts` and `slowTrace` from `mock/stories.ts` (both A2-protected: data.ts mock branch, TraceExplorer, DemoArrival, ExplainPanel, HeroTrace, CommandPalette, landing-fence). `/app/traces/diff` is in `liveWiredRouteExclusions`. `traces/[id]/page.tsx:57` passes `compareEnabled={dataMode !== "live"}`; `TraceExplorer.tsx:75–80,162` picks a compare partner from mock `allTraces` and links `/app/traces/diff?a=&b=`. No diff tests.
  - **issues**: `issues/page.tsx` (84, server, inline `Spark`) reads `mock/issues.ts` (`Issue` {fingerprint,title,layer,service,count7d,spark[14],firstSeen,lastSeen,status:"ongoing"|"resolved"|"new",exampleTraceId,exampleLink}; sole importer → editable). No tests.
- `WorkspaceData` facade (data.ts) has getTrace/searchTraces/getOverview/searchLogs only. S6.1 precedent: pages call query modules directly; the facade is NOT extended (A4 below).
- e2e: `deploy/compose/e2e-drive.mjs` has ZERO references to the five routes; `/app/costs` is the badged positive control. `exit-seed.mjs`'s trace fixture: services `exit-gateway`/`exit-agent`, `parent_span_id` links inside traces, `status_code: "error"` on every 20th trace (i%20===7), **`attributes: {}` everywhere — no `enduser.id`**, no cross-service parent→child edge asserted.
- Test posture: `apps/web` = `tsx --conditions react-server --test`; hermetic `X.test.ts` (fake `ScopedClickHouse`) + seeded `X.integration.test.ts` (compose ClickHouse) is the house pair (metrics.ts precedent). Client components with recharts cannot be imported under react-server → guards are source-text tests.

## Advisor decisions
<!-- Manager applies advisor output here. Binding for all agents. -->
- A1 (proposed, from S6.1): §5 fence + the contract set ruled at kickoff are binding verbatim; an executor who finds them false against the code/engine STOPS and reports (S4.4 L5).
- A2 (proposed): `mock/traces.ts`, `mock/stories.ts`, `mock/intelligence.ts` are NOT edited (shared by still-mock surfaces + the live pages' mock branches). `mock/catalog.ts`, `mock/users.ts`, `mock/issues.ts` stay in place unedited too — each feeds its surface's VERBATIM mock branch (D367 byte-identity); they are retired only if a later sprint removes mock mode.
- A3 (proposed): the D367 shape per surface — `app/app/<x>/page.tsx` becomes/stays a SERVER component branching on `dataMode` with the mock branch first; today's mock body moves VERBATIM to `components/<x>/<X>Mock.tsx`; the live branch renders a NEW props-fed `components/<x>/<X>Live.tsx` fed only by the surface's query module, typed via `lib/<x>-types.ts` (D366); a source-text `page.test.ts` guards the live graph (explore/page.test.ts pattern).
- A4 (proposed): pages call query modules directly with `forWorkspace(session.workspaceId)` (S6.1 explore form); `server/data.ts`'s `WorkspaceData` is NOT extended this sprint (no shared-file coupling across five tasks).
- A5 (proposed): one shared range vocabulary for the four aggregate surfaces: `PackRange = "1h" | "24h" | "7d"` with 7d the default where the mock said "7d" (users, issues) and 1h/24h where the surface is rate-shaped (map, services); the range is a URL param like explore's. Every aggregate query is bounded by `start_time >= now() - range` so partition pruning (`toDate(start_time)`) applies; the ClickHouse per-query `max_execution_time` posture is whatever `clickhouse.ts` already sets (D293 precedent — executor confirms, never raises).
- **KICKOFF QUESTIONS for the advisor (rule before dispatch; these ARE the design surface):**
  - Q1 the five LIVE contracts (fenced to what traces carry; fixture fields with no trace analogue are ABSENT, not invented — D13): proposals in each task block below (`lib/*-types.ts`). Rule each shape, especially: services v1 fields; users identity = `attributes['enduser.id']` only (what happens when a workspace sends none = honest empty state naming the attribute); issues fingerprint definition + status windows (D361 stateless); map edge definition + layout rule + node status thresholds; diff = two `getTrace` reads + a picker.
  - Q2 the "7d over spans" query cost: spans are ordered by trace, not time — a 7d aggregate for a busy workspace scans all its partitions in range. Rule whether v1 caps ranges at 24h (7d escalates to a later rollup sprint) or ships 7d with a measured budget (the drive can measure on the 220-trace seed only). Users/issues counts prefer `trace_summaries` where a per-trace answer suffices (error_count>0, root_service) and `spans` only for the per-span attributes.
  - Q3 D362 mechanics for services/[id]: the deploys panel keeps rendering `mock/intelligence.deploys` under a `SampleMark` in LIVE mode — that is a sanctioned `@/mock/` import inside a live page; the A2 guard for that page must allow exactly `deploys` and nothing else. Confirm.
  - Q4 traces/diff entry point in live mode: `TraceExplorer`'s compare picker reads mock `allTraces` (gated off live today). Proposal: live compare = the diff page's own picker (recent traces via `searchTraces`, or an explicit `?b=` deep link) and `traces/[id]` passes `compareEnabled` in live mode too but the button links to `/app/traces/diff?a=<id>` (partner chosen there) — TraceExplorer.tsx then needs a live-safe branch that never touches `allTraces`. Rule the shape (T5 owns both files).
  - Q5 critical items: proposed T1 (map topology derivation — the sprint's design surface), T4 (issues — D361 statelessness fence + fingerprint), T6 (exit evidence). Confirm/amend.
  - Q6 the seed fixture (D115, one definition): T6 adds `enduser.id` attributes, a deliberate cross-service parent→child edge set, and two distinct error signatures to `exit-seed.mjs`'s `dataset()` so map/users/issues have something real to assert. Confirm this is a fixture amendment, not a new seeding definition — and that it must not disturb any count the drive already asserts (S6.1-L3: measure, don't reason).

## Critical items
- T1 — map: the topology query + deterministic layout is the sprint's design surface; returns to the advisor with the integration proof before merge (proposed).
- T4 — issues: D361's statelessness fence and the fingerprint definition; returns with the fingerprint's unit proof + the recency-window proof (proposed).
- T6 — exit evidence (S6.1 D372 analogue): the drive's five-surface arm + proven-red run return before merge (proposed).

## Tasks

### T1: map — span-topology query + live service map
- status: pending
- wave: 1
- depends: —
- owns: apps/web/src/lib/topology-types.ts (new), apps/web/src/server/queries/topology.ts (new), apps/web/src/server/queries/topology.test.ts (new), apps/web/src/server/queries/topology.integration.test.ts (new), apps/web/src/lib/map-layout.ts (new, pure) + map-layout.test.ts, apps/web/src/components/map/ServiceMapMock.tsx (new — today's ServiceMap.tsx VERBATIM, renamed), apps/web/src/components/map/ServiceMapLive.tsx (new, props-fed), apps/web/src/components/map/ServiceMap.tsx (deleted after the move), apps/web/src/app/app/map/page.tsx, apps/web/src/app/app/map/page.test.ts (new)
- goal: `queryTopology(ch, {range})` derives nodes = distinct `service` values with spans in range (per node: layer = the dominant `layer` by span count, spanRate/min, errorPct = error spans / spans, p95 ms) and edges = parent→child pairs where `parent.service != child.service`, from a self-join of `spans` on `(workspace_id, trace_id, parent_span_id = span_id)` bounded to the range (per edge: rate/min, errPct of the CHILD span); an entry pseudo-node is NOT invented — a service whose root spans have no parent is simply marked `isEntry`. `map-layout.ts` places nodes deterministically by layer column (api → agent → tool/llm → infra/other, then by name) so the SVG needs no hand x/y. Node status thresholds from errorPct (proposed: ok <1%, warn <5%, err ≥5% — advisor rules). Mock mode: `ServiceMapMock` renders the untouched SVG byte-identically. Live: honest empty state when no spans in range ("no services have sent spans in the last {range}").
- done-check: `npm test --workspace apps/web` green incl. topology.test.ts (fake ch: join predicate + range bound + tripwire red-proven), topology.integration.test.ts (seeded cross-service parent/child rows → the exact edge set with rates; a same-service parent/child pair yields NO edge; a second workspace sees none), map-layout.test.ts (deterministic positions, no overlap for the seeded shape); `page.test.ts` guards no `@/mock/` in the live graph and mock-branch-first; mock explore-style byte-identity holds (integrator proves by build diff).
- result:

### T2: services — trace-derived catalog + scorecards (D362 deploys panel marked)
- status: pending
- wave: 1
- depends: —
- owns: apps/web/src/lib/services-types.ts (new), apps/web/src/server/queries/services.ts (new) + services.test.ts + services.integration.test.ts (new), apps/web/src/components/services/ServicesMock.tsx (new — today's services/page.tsx body VERBATIM), apps/web/src/components/services/ServiceDetailMock.tsx (new — today's [id] body VERBATIM), apps/web/src/components/services/ServicesLive.tsx (new), apps/web/src/components/services/ServiceDetailLive.tsx (new), apps/web/src/app/app/services/page.tsx, apps/web/src/app/app/services/[id]/page.tsx, apps/web/src/app/app/services/page.test.ts (new)
- goal: `listServices(ch, {range})` from `spans` (one aggregate query GROUP BY service): name, dominant layer, spans/min, errorPct, p50/p95 ms, distinct `deps` (child services via the T1 join predicate — duplicated SQL fragment is acceptable over a shared file; advisor may rule a shared `lib/topology-sql.ts` instead), models[], costUsd, lastSeen; `getService(ch, id, {range})` = the same row + its top span names by count and recent error traces (trace_summaries via `-Merge`, `has(services, {id})`). Fixture-only fields (`team`, `tier`, `runtime`, `sloStatus`, `score.*`, `grade`) are ABSENT in live mode — the live scorecard is the trace-derived one (error rate, latency, cost) and says so. `[id]` in live mode renders the deploys section from `mock/intelligence.deploys` under `SampleMark` per D362 (the ONE sanctioned mock import; the page.test allows exactly it). Unknown id in live → honest 404-style empty ("no spans from this service in the last {range}"). Mock branches byte-identical.
- done-check: web suite green; services.test.ts (tripwire red-proven, range bound, unknown id) + integration (seeded two services with a cross-service edge → deps, errorPct, p95 exact); page.test.ts: live graph imports no `@/mock/` EXCEPT `deploys` from `@/mock/intelligence` in `ServiceDetailLive`/`[id]/page.tsx` (asserted allow-list); `/app/services` and `/app/services/[id]` both flip (T6).
- result:

### T3: users — impacted-user aggregates from `enduser.id`
- status: pending
- wave: 1
- depends: —
- owns: apps/web/src/lib/users-types.ts (new), apps/web/src/server/queries/users.ts (new) + users.test.ts + users.integration.test.ts (new), apps/web/src/components/users/UsersMock.tsx (new — today's page body VERBATIM), apps/web/src/components/users/UsersLive.tsx (new), apps/web/src/app/app/users/page.tsx, apps/web/src/app/app/users/page.test.ts (new)
- goal: `listImpactedUsers(ch, {range})`: identity = `attributes['enduser.id']` on ROOT spans (`parent_span_id = ''`) with a non-empty value; per user: requests (root spans), failures (root spans with `status_code = 'error'` OR any error span in the trace — advisor rules which), slow (root `duration_ns` > a ruled threshold — proposed p95 of the workspace's root spans in range, computed in the same query; or a fixed 2s — advisor rules), lastFailure = the most recent failing trace id + root name + time (links to the live `/app/traces/[id]`), risk derived by rule (proposed: at-risk if failures/requests ≥ 10%, degraded if ≥ 2% or slow ≥ 20%, else healthy). `org`/`plan` are ABSENT (no trace analogue). Empty state names the attribute: "no spans carried `enduser.id` in the last {range} — set it on your root span to see impacted users" (D13 honest, actionable). Mock branch byte-identical.
- done-check: web suite green; users.test.ts (tripwire, Map-key read is parameterized/literal never interpolated, threshold rule unit-proven) + integration (seeded spans with `attributes: {'enduser.id': …}` for two users, one with an error trace → exact counts, lastFailure trace id; rows without the attribute are excluded; second workspace empty); page.test.ts guard.
- result:

### T4: issues — stateless error grouping over spans/logs (D361)
- status: pending
- wave: 1
- depends: —
- owns: apps/web/src/lib/issues-types.ts (new, incl. the pure `fingerprint()`/`normalizeSignature()` helpers) + issues-types.test.ts (new), apps/web/src/server/queries/issues.ts (new) + issues.test.ts + issues.integration.test.ts (new), apps/web/src/components/issues/IssuesMock.tsx (new — VERBATIM body incl. `Spark`), apps/web/src/components/issues/IssuesLive.tsx (new), apps/web/src/app/app/issues/page.tsx, apps/web/src/app/app/issues/page.test.ts (new)
- goal: `listIssues(ch, {range})` groups error spans (`status_code = 'error'`) — and, if the advisor rules logs in, `logs` with `severity_number >= 17` — by a QUERY-TIME fingerprint computed in SQL and reproduced by the TS helper: proposed `cityHash64(service, layer, name, normalize(status_message))` where normalize strips digits/hex ids/quoted strings (SQL `replaceRegexpAll`; the TS helper implements the same regex and the unit test pins identical outputs for a corpus). Per issue: title = the normalized signature, service, layer, count (range), spark = 14 equal buckets over the range (counts, oldest first — `null` never fabricated; empty bucket = 0 is a real count here, unlike metrics), firstSeen/lastSeen (within the TTL'd data — stated as "first seen in retained data"), status derived from recency windows ONLY (proposed: new = firstSeen within the last 24h; resolved = lastSeen older than 24h; ongoing otherwise — advisor rules the windows), exampleTraceId = the most recent trace carrying the signature. NO persistence, no assign/mute/resolve (D361). Mock branch byte-identical.
- done-check: web suite green; issues-types.test.ts (fingerprint stable across id/digit variation, distinct across services; corpus parity SQL↔TS proven in the integration test by comparing `cityHash64` from ClickHouse with the TS value for the same inputs — or the advisor rules the fingerprint is computed ONLY in SQL and the TS side merely displays); issues.test.ts (tripwire, range bound, window rule); integration (seeded three signatures across two services incl. one that differs only by an id → 3 issues not 4, sparks sum to counts, statuses per window; second workspace empty); page.test.ts guard.
- result:

### T5: traces/diff — two real traces side by side
- status: pending
- wave: 1
- depends: —
- owns: apps/web/src/app/app/traces/diff/page.tsx, apps/web/src/app/app/traces/diff/page.test.ts (new), apps/web/src/components/trace/TraceDiffMock.tsx (new — today's TraceDiff.tsx VERBATIM), apps/web/src/components/trace/TraceDiffLive.tsx (new, props-fed: `{a: Trace | null, b: Trace | null, recent: TraceSummaryRow[]}`), apps/web/src/components/trace/TraceDiff.tsx (deleted after the move), apps/web/src/components/trace/TraceExplorer.tsx (the compare affordance: live-safe branch — never reads `allTraces` in live), apps/web/src/app/app/traces/[id]/page.tsx (the `compareEnabled` gate)
- goal: the diff page becomes a server component: mock branch renders `TraceDiffMock` (byte-identical, still reads `?a=&b=` client-side as today); live branch reads `?a=&b=` and calls `dataForSession().getTrace()` twice (A4 exception: this surface reuses the EXISTING facade reads, no new query module) plus `searchTraces()` for the picker list when a side is missing; `TraceDiffLive` renders the same span-by-span comparison the mock renders (name/layer/duration/status deltas) for two REAL traces, an honest "pick a second trace" state when `b` is absent, and "trace not found in this workspace" for a foreign id (tenancy: `getTrace` already scopes). `traces/[id]` in live mode shows the compare button linking `/app/traces/diff?a=<id>` (partner chosen on the diff page); `TraceExplorer`'s mock partner-pick stays for mock mode only. `liveWiredRouteExclusions` loses `/app/traces/diff` in T6.
- done-check: web suite green; page.test.ts: live graph imports no `@/mock/` (TraceDiffLive, and TraceExplorer's live path proven by a source-text guard that the `allTraces` read is behind `compareEnabled`/mock only); an integration-style test through `dataForWorkspace` with two seeded traces proves the diff rows; the mock build byte-identical (integrator).
- result:

### T6: the flip + exit evidence — live-routes, seed fixture, drive arm, README
- status: pending
- wave: 2
- depends: T1, T2, T3, T4, T5
- owns: apps/web/src/lib/live-routes.ts, apps/web/src/lib/live-routes.test.ts, deploy/compose/exit-seed.mjs (fixture amendment per Q6 — `enduser.id` on root spans for two seeded users, a cross-service parent→child edge set gateway→agent→(tool|llm) with the label woven into names, two distinct error signatures), deploy/compose/e2e-drive.mjs (one step per surface + the tenancy half), deploy/compose/README.md ("What it asserts" + the seed sentence — S6.1-L: docs in owns from the start)
- goal: `/app/map`, `/app/services`, `/app/services/` (subtree), `/app/users`, `/app/issues` join `liveWiredRoutes`; `/app/traces/diff` leaves `liveWiredRouteExclusions`; the drive proves per surface, on alice's real seed: no SAMPLE badge, the page renders alice's label-bearing words (a service name / an enduser id / an issue signature / a diff of two seeded trace ids), and bob's browser renders none of alice's words on the same surfaces (D142 content-aware disjointness, the existing idiom at :1571); `/app/costs` remains the badged positive control. The new arm is proven RED first by seeding without the enduser/edge/error additions (S2.0 L1) and by a literal sabotage the reviewer chooses. Every count the drive already asserts stays exact — measured before/after the fixture amendment (S6.1-L3).
- done-check: `node deploy/compose/e2e-drive.mjs` green locally; red transcript captured; `live-routes.test.ts` updated (the exclusion removed, five additions); README lists the new steps in the drive's real order.
- result:

## Escalations log
<!-- Manager appends: {T#} — question → advisor decision (one line each). -->

## Integration
- verdict: pending
- rounds: 0
- e2e:
- seam findings:
- fix tasks:

## Goal check (Step 7, one entry per iteration)
- iteration 0: pending

## Retro (filled at end of run)
- tasks: /6
- breakdown corrections by advisor:
- reviewer verdicts:
- lesson:
