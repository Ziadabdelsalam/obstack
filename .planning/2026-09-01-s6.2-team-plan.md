# Team Plan — obstack M5 sprint 2 (S6.2): the trace-derived query pack — map, services, users, traces/diff, issues render the signed-in workspace's real data in live mode, each flipped in `live-routes.ts` with honest empty states; query modules + UI wiring over EXISTING tables only; mock mode byte-identical

meta:
- date: 2026-09-01
- repo/branch: `s6-2` (to cut from `master` @ `25548fa`)
- mode: waves
- team-cap: 10
- models: executor = routed by complexity (Opus 5 `effort:high` for the query-module tasks, Sonnet 5 `effort:high` for mechanical riders) · reviewer = Opus 5 `effort:xhigh` · advisor = Opus 5 `effort:xhigh` (user, Step 0 — the S6.1 configuration)
- iteration-limit: 3 — fix rounds allowed before escalating to the user
- iteration: 0/3
- governing docs (binding): `.planning/2026-08-31-s6-m5-full-platform-phase-plan.md` §5 S6.2 (run-goal, scope fence), §7 standing constraints, §8 rulings **D360 (Option C), D361 (issues pull-forward, STATELESS fence), D362 (services deploys panel = rendered with `SampleMark`, not hidden), D364 (sequential)**; the S6.1 plan's standing rulings that generalize: D366 (contract types client-safe in `lib/*-types.ts`), D367 (server page branches on `dataMode`; mock body moves VERBATIM to `<X>Mock.tsx`; live is a props-fed sibling; byte-identity), D13/D21/D114, D7/D11 (tenancy tripwires per query module), D389 vocabulary rule.
- predecessor: S6.1 closed 2026-09-01 (PR #29, production `2d181d4`); retro lessons S6.1-L1..L3 apply (mock `next build` before `npm test` after a drive; rulings phrased as observable results; ordering constraints measured).

## Foundation facts (audited 2026-09-01, this session — Explore inventory + DDL)
- Tables available (NO new DDL this sprint — §5 fence; any need escalates): `spans` (PARTITION BY toDate(start_time), ORDER BY (workspace_id, trace_id, span_id), TTL 30d; promoted columns `service`, `parent_span_id`, `kind`, `layer` Enum8, `status_code` Enum8('unset','ok','error'), `status_message`, `name`, `start_time`, `duration_ns`, gen_ai/cost columns, `attributes Map`, `resource_attributes Map`), `logs` (same partition/TTL; `severity_number/text`, `body`, `service`, k8s cols, Maps), `trace_summaries` AMT (ORDER BY (workspace_id, trace_id); `min_start`, `max_end`, `span_count`, `error_count`, tokens, `total_cost_usd`, `services[]`, `models[]`, `root_name/method/service` argMinIf states, `max_seen_date`), plus S6.1's metrics objects (not used here).
- `enduser.id` is NOT a promoted column anywhere (zero hits in ingest + web) — if an SDK sends it, it lives in `spans.attributes['enduser.id']` (the `flattenAttributes` path, `spans.go:79`). `http.status_code` likewise Map-only. `service.name` → `service` column.
- Reader rules: `forWorkspace(workspaceId)` → `ScopedClickHouse.queryRows(sql, params)`; every SQL carries `{workspace_id:` bound from scope (runtime tripwire); trace_summaries read with `-Merge` combinators, never FINAL (traces.ts:74–88).
- Wiring pattern (the finish line): `connections/page.tsx` — mock branch returns FIRST (before `connection()`/session), live branch reads query modules directly with `forWorkspace(session.workspaceId)` (explore's S6.1 form); flip = one line in `lib/live-routes.ts`; guard test = `explore/page.test.ts` (source-text A2 guard: no `@/mock/` in the live graph, mock branch before `await connection()`).
- Per surface today:
  - **map**: `app/app/map/page.tsx` (5 lines, server) → `components/map/ServiceMap.tsx` (165 lines, "use client", NO fixture — 11 nodes/12 edges are literals at :29–56 with hand-placed x/y; rendering :60–165). No tests.
  - **services**: `services/page.tsx` (96, server) + `services/[id]/page.tsx` (172, server, async params) read `mock/catalog.ts` (`ServiceEntry` {id,name,layer,team,tier,runtime,deps,sloStatus,score{coverage,alerts,runbook,slo}}, `grade()`; importers = ONLY these two pages → editable) and `[id]` also reads `deploys` from `mock/intelligence.ts` (shared with evals/alerts/oncall/AskChat → A2-protected, untouchable). No components dir; markup inline. No tests.
  - **users**: `users/page.tsx` (104, server) reads `mock/users.ts` (`ImpactedUser` {org,user,plan,reqs7d,failures7d,slow7d,lastFailure{label,traceId,href,when},risk}; sole importer → editable). No tests.
  - **traces/diff**: `traces/diff/page.tsx` (10, server, Suspense) → `components/trace/TraceDiff.tsx` (188, "use client") imports `allTraces,getTrace` from `mock/traces.ts` and `slowTrace` from `mock/stories.ts` (both A2-protected: data.ts mock branch, TraceExplorer, DemoArrival, ExplainPanel, HeroTrace, CommandPalette, landing-fence). `/app/traces/diff` is in `liveWiredRouteExclusions`. `traces/[id]/page.tsx:57` passes `compareEnabled={dataMode !== "live"}`; `TraceExplorer.tsx:75–80,162` picks a compare partner from mock `allTraces` and links `/app/traces/diff?a=&b=`. No diff tests.
  - **issues**: `issues/page.tsx` (84, server, inline `Spark`) reads `mock/issues.ts` (`Issue` {fingerprint,title,layer,service,count7d,spark[14],firstSeen,lastSeen,status:"ongoing"|"resolved"|"new",exampleTraceId,exampleLink}; sole importer → editable). No tests.
- `WorkspaceData` facade (data.ts) has getTrace/searchTraces/getOverview/searchLogs only. S6.1 precedent: pages call query modules directly; the facade is NOT extended (A4 below).
- e2e: `deploy/compose/e2e-drive.mjs` has ZERO references to the five routes; `/app/costs` is the badged positive control. `exit-seed.mjs`'s trace fixture: services `exit-gateway`/`exit-agent`, `parent_span_id` links inside traces, `status_code: "error"` on every 20th trace (i%20===7), **`attributes: {}` everywhere — no `enduser.id`**, no cross-service parent→child edge asserted.
- Test posture: `apps/web` = `tsx --conditions react-server --test`; hermetic `X.test.ts` (fake `ScopedClickHouse`) + seeded `X.integration.test.ts` (compose ClickHouse) is the house pair (metrics.ts precedent). Client components with recharts cannot be imported under react-server → guards are source-text tests.

## Advisor decisions
<!-- Kickoff verdict 2026-09-01 (advisor, fresh spawn for S6.2; ledger continues D391+). Breakdown SOUND with five corrections C1–C5 applied to the task blocks. Binding for all agents. -->
- A1: the §5 fence + the contracts ruled below are binding verbatim; an executor who finds them false against the code/engine STOPS and reports (S4.4 L5). APPROVED.
- A2: `mock/traces.ts`, `mock/stories.ts`, `mock/intelligence.ts` are NOT edited; `mock/catalog.ts`, `mock/users.ts`, `mock/issues.ts` stay unedited (each feeds its surface's VERBATIM mock branch). APPROVED.
- A3: D367 shape per surface — server page branching on `dataMode`, mock branch FIRST and before any `await`; today's body moves VERBATIM to `components/<x>/<X>Mock.tsx`; live is a NEW props-fed sibling `<X>Live.tsx` typed via `lib/<x>-types.ts`; source-text `page.test.ts` guard. APPROVED.
- A4: pages call query modules directly with `forWorkspace(session.workspaceId)`; `WorkspaceData` NOT extended. APPROVED (T5's use of the EXISTING facade reads is the one correct exception — C4).
- A5: REPLACED by D394/D395 (no range vocabulary).
- **D391 (standing set):** D366, D367, D389, D13/D21/D114, D7/D11 bind verbatim. Duplication across the mock/live boundary is the ratified shape (a shared "smart" component is not). (a) `server/data.ts` statically imports `@/mock/*`, so "no `@/mock/` in the live graph" is a SOURCE-TEXT rule on the `<X>Live.tsx` file, never a transitive-graph claim — reviewers may not demand the impossible; (b) a page-level static mock import beside the `dataMode` branch is the ratified `connections/page.tsx:11,37` idiom.
- **D392:** every `<X>Live.tsx` in S6.2 is a SERVER component (no `"use client"`) — selection is a URL or a `<Link>`; zero interactivity JS for interactivity that does not exist.
- **D393:** services v1 has NO `deps` — the dependency answer is the map's (T1), one derivation; catalog row + detail link to `/app/map`. Removes the T1/T2 SQL duplication and the shared-file coupling a `topology-sql.ts` would create (C1).
- **D394:** NO range vocabulary, NO range picker. Every aggregate surface renders ONE fixed window, the last 24h, stated in words on the surface ("last 24h"); each `lib/<x>-types.ts` owns `WINDOW_HOURS = 24`; query modules take no range argument; the bound is proven by seeding a row outside it (mutation-proven), not by a parameter.
- **D395 (Q2):** 24h is the ceiling; 7d does not ship (≈8 partitions, no rollup, rollup = DDL = fenced). Budget = the server profile `obstack_web` `readonly=2, max_execution_time=30` (`deploy/compose/clickhouse/users.d/obstack-users.xml:21-25`, mirrored in the chart) — NO per-query `max_execution_time` override this sprint; a page that cannot answer in 30s is a rollup escalation. Table split: map / services-list / services-detail-stats / users / issues read `spans`; services-detail's recent error traces read `trace_summaries` (`-Merge`, GROUP BY, never FINAL); diff reads the existing facade.
- **D396 (map contract):** `TopologyNode {service, layer, spans, spansPerMin, errorPct, p95Ms, isEntry}`, `TopologyEdge {from, to, calls, callsPerMin, errorPct}`, `Topology {nodes, edges, totalServices, nodeCap}`. node = distinct `service` with ≥1 span in the window; `layer` = plurality layer, ties by `layerOrder` (`lib/layers.ts`); `errorPct` = error spans ÷ spans ×100; `p95Ms` = `quantile(0.95)` guarded `ifNotFinite(…,0)` (`overview.ts:59-60` idiom verbatim); `spansPerMin` = spans ÷ 1440; `isEntry` = ≥1 span with `parent_span_id = ''`; no entry pseudo-node. edge = parent span + child span in the SAME trace whose services differ; an edge exists only when both spans are in the store (uninstrumented hop draws nothing, the surface says so); edge `errorPct` is the child's. Status thresholds, ONE pair for live nodes and edges, defined once in `lib/topology-types.ts`: ok <1%, warn 1–5%, err ≥5% (`ServiceMapMock.tsx` keeps its own constants). Layout (`lib/map-layout.ts`, pure): column per layer in `layerOrder` order, rows by service name (codepoint); position = f(layer, name-rank) only; returns viewBox width/height. Cap 40 nodes by span count with "showing 40 of N services by span volume"; edges = those between rendered nodes, stated. ABSENT: pod counts, restarts, "external", the demo's incident sentence. Empty: "no service has sent a span in the last 24h".
- **D397 (services contract):** `ServiceRow {name, layer, spans, spansPerMin, errorPct, p50Ms, p95Ms, costUsd, models[], lastSeenAt}`; `ServiceDetail = ServiceRow & {topSpanNames: {name, count, errorPct}[], recentErrorTraces: {traceId, rootName, startedAt}[]}`. Same layer/percentile/rate rules as D396. ABSENT: `team`, `tier`, `runtime`, `sloStatus`, `score.*`, `grade`, `deps`; the header says the score is derived from traces. Unknown id → "no spans from this service in the last 24h" + link to the catalog. Cap 100 rows + banner. `recentErrorTraces` from `trace_summaries` with `-Merge`, GROUP BY, never FINAL, `has(services, {service:String})`.
- **D398 (users contract):** `ImpactedUser {userId, requests, failures, slow, lastFailure: {traceId, rootName, at} | null, risk}`. identity = `attributes['enduser.id']`, else `attributes['user.id']` (current semconv; both key literals are SQL-text constants, never interpolated — D11). population = ROOT spans (`parent_span_id = ''`) with a non-empty identity in the window; `requests` = those; `failures` = those with `status_code = 'error'` (no summaries join); `slow` = root `duration_ns ≥ 2s` (constant once in `lib/users-types.ts`; header states "slow = ≥2s"); `risk` in order: at-risk if failures/requests ≥10%; degraded if failures/requests ≥2% OR slow/requests ≥20%; else healthy — rule printed on the surface; `lastFailure` = most recent failing root span → `/app/traces/<id>`. ABSENT: `org`, `plan`. Cap 50 by requests + banner. Empty: "no span in the last 24h carried `enduser.id` or `user.id` — set one on your root span to see impacted users."
- **D399 (issues contract):** `Issue {fingerprint, title, service, layer, count, spark: number[24], firstSeenAt, lastSeenAt, status, exampleTraceId}`. Source = error spans ONLY (`status_code = 'error'`); LOGS OUT of v1. Fingerprint computed ONLY in SQL, returned as a string — no TS helper, no parity corpus. Inputs: `service`, `layer`, `name`, normalized `status_message`. Normalization as an observable result: two error spans differing ONLY in digits, hex/uuid ids, quoted strings, or numeric URL path segments group together; differing in service, layer, span name, or any remaining word do not. `title` = normalized message when non-empty, else span name. `spark` = 24 hourly buckets, oldest first, zero = real count. first/lastSeenAt within the window ("first seen in this 24h window"). `status` from recency only (D361), anchored to D50's 6h: new = every occurrence inside the last 6h; resolved = no occurrence inside the last 6h; ongoing otherwise — legend states it. Cap 50 by count + banner.
- **D400 (diff, Q4):** no new types; props `{a: Trace | null, b: Trace | null, recent: Trace[]}` from `@/lib/types`. Page = server component; mock branch returns `<Suspense><TraceDiffMock/></Suspense>` BEFORE `await searchParams`/`connection()` (mock stays static, byte-identical). Live: two `getTrace` reads in parallel + `searchTraces({})` for the picker rendering the 30 most recent; missing `b` → "pick a second trace" + picker; unknown/foreign id → "trace not found in this workspace". Entry point: `TraceExplorer`'s `compareEnabled: boolean` is REPLACED by `compare: { href: string } | null` computed by `traces/[id]/page.tsx` — mock branch computes today's partner from `@/mock/traces` at the page (same href, same DOM), live passes `/app/traces/diff?a=<id>`. `import { allTraces }` is DELETED from `TraceExplorer.tsx` (the mock corpus stops shipping in the live trace-detail bundle). `rowsFor` is duplicated in `TraceDiffLive` (D391).
- **D401 (Q3, D362 mechanics):** `services/[id]/page.tsx` imports `deploys` from `@/mock/intelligence` and passes it as a prop; `ServiceDetailLive.tsx` imports NO `@/mock/`. Section renders in live under `SampleMark title="sample data — deploy tracking arrives with the changes feed (M6)"`. Guard `app/app/services/page.test.ts` (reads both pages + both Live files as source): Live files contain zero `@/mock/`; `[id]/page.tsx` contains exactly ONE `@/mock/` import = `deploys` from `@/mock/intelligence`; `mock/catalog.ts` appears only in the mock branch.
- **D402:** every unbounded GROUP BY ships a cap and says so (D381 idiom): map 40, services 100, users 50, issues 50, "showing N of M by <ranking>" computed in the same query.
- **D403 (tenancy, join-specific):** `runScoped`'s tripwire is satisfied by ONE `{workspace_id:` occurrence, so a self-join can leak with the tripwire green. T1 proves BOTH join sides carry `workspace_id = {workspace_id:String}` with an integration test seeding the SAME trace_id/span_id pair in two workspaces and asserting no cross-tenant edge. Every new module ships the house pair: hermetic tripwire (red-proven) + "a second workspace sees none".
- **D404 (C2 — TourGuide):** the `/app/map`, `/app/issues`, `/app/users` steps in `components/shell/TourGuide.tsx` narrate demo facts that become false at the flip; rewritten to capability-only claims true in both modes (D60 `/app/logs` treatment) with three `TourGuide.test.ts` analogues of `:173`. Each Live component carries its surface's existing `data-tour` anchor (`map`, `issues`, `users`). Owner T6.
- **D405 (Q6, seed):** a D115 fixture amendment inside `exit-seed.mjs` (same direct `insert()` path :296, never OTLP → health counters cannot move). `span()` extends `labelled()` to `status_message`; the enduser id derives from the label (D142) while services/pods/ids/counts stay identical (D135). INVARIANT, MEASURED via `denominators` (:1174) before/after, table pinned in T6's result: (1) `total` stays 220 (no new trace ids; `:1183` `200 of ${a.total}`, `:1326-1339` `page 2 of 2`); (2) `serviceTotal` strictly below `total` (≥20 traces keep no `exit-agent` span, `:1310`); (3) `errorTotal` in (0,total); `phantoms`/`renderableLogs`/`dbPodErrorRows` unchanged (no log rows added); (4) new spans carry zero tokens/cost; (5) no `SPAN_PROMPT_TOKEN`/`LOG_BODY_TOKEN`/`CARRIER_TOKEN` in new content — the three single-result legs (:1198-1200) still return one trace each; (6) the Explain step's lowest-trace_id error span (:2114) still lands on a rendering trace — re-measured.
- **D406 (critical items):** T1, T4, T6 confirmed; T5 ADDED (narrow: only the `TraceExplorer` prop change + live compare entry point — a production-live surface); T2 NOT critical.
- **Run-goal conditions (binding):** (1) NO DDL — a task believing it needs one STOPS and escalates; (2) every query module takes `ScopedClickHouse`, binds `{workspace_id:String}` on EVERY table reference incl. both join sides, bounded by 24h, bound mutation-proven; (3) 30s `obstack_web` profile is the ceiling, no per-query settings; (4) D13 per surface — fixture-only fields absent, every derived number's rule stated on the surface in observable terms; (5) D367 shape, byte-identity proven by the integrator's two-worktree `next build` DOM md5; (6) D21 flip = exit, asserted in CI against `/app/costs`; (7) drive arm proven RED first + reviewer sabotage + the D405 table; (8) S6.1-L1 harness posture; (9) **deploy touchpoint pre-registered: S6.2 close is WEB-ONLY** — master merge + `git push origin <sha>:production`, no migration, no ingest redeploy; any landed migration returns the run to the advisor.

## Critical items
- T1 — map: returns with the topology contract + the D403 cross-tenant same-id edge proof + the layout determinism proof.
- T4 — issues: returns with the SQL-only fingerprint's grouping proof (id-variants merge; service/name variants do not) + the status-window proof.
- T5 — diff (NARROW, D406): returns only on the `TraceExplorer` prop change + the live compare entry point.
- T6 — exit evidence: returns with the five-surface drive arm, its proven-red run, and the D405 before/after count table.

## Tasks

### T1: map — span-topology query + live service map
- status: approved (ADVISOR-SIGNED D407; riders D408 (columns = present layers, falsifier: fixed-index → exactly the two new tests red; api/llm/infra width 696 vs 1180) + D409 (one fmtPerMin in lib/format.ts, tiers pinned incl. 12,345; map appends /min, services bare under a /min header) landed + re-review approve. NOTE for advisor: ConnectionsHub.tsx:58 formatRate keeps the "0.0/min" hedge and :307 prints a raw toLocaleString rate — outside D409's named scope.) Reviewer: legend interpolates WARN_ERROR_PCT/ERR_ERROR_PCT (defined once, printed once); perMin gained the two-decimal tier (72 calls/24h no longer prints "<0.01/min"); WORKSPACE_C seeds 41 one-span services → 40 nodes, totalServices 41 (pre-LIMIT proven red by swapping to nodeRows.length). D403 sabotages: parent unscoped → 3 fail, child unscoped → 3 fail, bound dropped → 5 fail; hermetic test pins exactly two {workspace_id: occurrences in the edge SQL. Nodes (single table) and edges (two scoped subqueries) are separate statements; top-40 coupling in TS AFTER SQL aggregation — the only render-visible leak class is the same-named pair the fixture asserts. Node link /app/traces?service=&range=24h honored by parseTracesUrl. Rename ServiceMap→ServiceMapMock landed in T3's commit (hygiene note).)
- wave: 1
- depends: —
- owns: apps/web/src/lib/topology-types.ts (new), apps/web/src/server/queries/topology.ts (new), apps/web/src/server/queries/topology.test.ts (new), apps/web/src/server/queries/topology.integration.test.ts (new), apps/web/src/lib/map-layout.ts (new, pure) + map-layout.test.ts, apps/web/src/components/map/ServiceMapMock.tsx (new — today's ServiceMap.tsx VERBATIM, renamed), apps/web/src/components/map/ServiceMapLive.tsx (new, props-fed), apps/web/src/components/map/ServiceMap.tsx (deleted after the move), apps/web/src/app/app/map/page.tsx, apps/web/src/app/app/map/page.test.ts (new)
- goal: implement D396 VERBATIM — `queryTopology(ch)` (no range argument; `WINDOW_HOURS = 24` in `lib/topology-types.ts`) derives nodes and edges from `spans` per D396 (self-join on `(trace_id, parent_span_id = span_id)` with `workspace_id = {workspace_id:String}` on BOTH sides — D403; same-trace, services differ; edge exists only when both spans are stored; child's errorPct), the 40-node cap with `totalServices`/`nodeCap` for the D402 banner, thresholds ok<1/warn 1–5/err≥5 defined once in the types file, `map-layout.ts` pure column-per-layer layout returning viewBox size. `ServiceMapMock.tsx` = today's `ServiceMap.tsx` VERBATIM (its own constants untouched). `ServiceMapLive.tsx` is a SERVER component (D392) rendering the SVG from props, the "last 24h" statement, the cap banner when totalServices > 40, the uninstrumented-hop note, the `data-tour="map"` anchor (D404), and the empty state "no service has sent a span in the last 24h". ABSENT: pod counts, restarts, "external", the demo's incident sentence.
- done-check: `npm test --workspace apps/web` green incl. topology.test.ts (fake ch: both-sides workspace binding asserted on the SQL text; 24h bound; tripwire red-proven), topology.integration.test.ts (seeded cross-service parent/child rows → exact edges with calls/callsPerMin/errorPct; same-service pair → NO edge; a row 25h old → excluded (mutation-proven bound); the D403 same-trace_id/span_id-in-two-workspaces case → no cross-tenant edge; second workspace sees none), map-layout.test.ts (positions are a pure function of (layer, name-rank); no overlap for the seeded shape); `page.test.ts` source-text guard: `ServiceMapLive.tsx` has no `@/mock/` and no `"use client"`, mock branch before any `await`; byte-identity proven by the integrator's build diff.
- result:

### T2: services — trace-derived catalog + scorecards (D362 deploys panel marked)
- status: approved (review: fixed — one comment corrected. D397 field-for-field; fixture-only fields appear only in doc comments, zero renders; recentErrorTraces via -Merge + merged has(groupUniqArrayArray(services)) + max_seen_date prefilter AND exact min_start HAVING bound (mutation-proven load-bearing); 4 statements/4 placeholders; D401 guard sabotage-red; [id] mock receives the unawaited params Promise so the await point matches HEAD. D402-forced additions accepted: totalSpanNames, fmtPerMin (1/1440→"0.001"). ESCALATE→advisor: per-minute formatting diverges T2 fmtPerMin "0.003" vs T1 "<0.01/min".)
- wave: 1
- depends: —
- owns: apps/web/src/lib/services-types.ts (new), apps/web/src/server/queries/services.ts (new) + services.test.ts + services.integration.test.ts (new), apps/web/src/components/services/ServicesMock.tsx (new — today's services/page.tsx body VERBATIM), apps/web/src/components/services/ServiceDetailMock.tsx (new — today's [id] body VERBATIM), apps/web/src/components/services/ServicesLive.tsx (new), apps/web/src/components/services/ServiceDetailLive.tsx (new), apps/web/src/app/app/services/page.tsx, apps/web/src/app/app/services/[id]/page.tsx, apps/web/src/app/app/services/page.test.ts (new)
- goal: implement D397 VERBATIM — `listServices(ch)` (one aggregate over `spans`, 24h, GROUP BY service, cap 100 + total for the D402 banner) and `getService(ch, name)` (the same row + `topSpanNames` from `spans` + `recentErrorTraces` from `trace_summaries` with `-Merge`, GROUP BY, never FINAL, `has(services, {service:String})`). NO `deps` (D393) — row and detail link to `/app/map`. Fixture-only fields ABSENT; the live header says the score is derived from traces (error rate / latency / cost). `ServicesMock.tsx`/`ServiceDetailMock.tsx` = today's two page bodies VERBATIM; `ServicesLive.tsx`/`ServiceDetailLive.tsx` are SERVER components (D392) with the "last 24h" statement and cap banner. D401: `[id]/page.tsx` imports `deploys` from `@/mock/intelligence` (its ONLY `@/mock/` import) and passes it as a prop; `ServiceDetailLive` renders the deploys section under `SampleMark title="sample data — deploy tracking arrives with the changes feed (M6)"` and imports NO `@/mock/`. Unknown name → "no spans from this service in the last 24h" + catalog link.
- done-check: web suite green; services.test.ts (tripwire red-proven; 24h bound; unknown name; cap/total) + services.integration.test.ts (two seeded services → errorPct/p50/p95/models/cost exact; a 25h-old row excluded; recentErrorTraces exact; second workspace sees none); `page.test.ts` per D401's allow-list, plus no `"use client"` in either Live file and mock-branch-first on both pages; byte-identity by the integrator.
- result:

### T3: users — impacted-user aggregates from `enduser.id`
- status: approved (review: fixed — totalUsers assertion made exact (2, not >=2: distinguishes D402 distinct identities from the 14 root rows); lastFailure.at pinned to the seeded start_time so the formatDateTime format is proven. Red-proofs: tenancy drop → 3 fail; 24h→48h → 25h row leaks; root-only drop → 3→4. count() OVER () proven to count groups pre-LIMIT.)
- wave: 1
- depends: —
- owns: apps/web/src/lib/users-types.ts (new), apps/web/src/server/queries/users.ts (new) + users.test.ts + users.integration.test.ts (new), apps/web/src/components/users/UsersMock.tsx (new — today's page body VERBATIM), apps/web/src/components/users/UsersLive.tsx (new), apps/web/src/app/app/users/page.tsx, apps/web/src/app/app/users/page.test.ts (new)
- goal: implement D398 VERBATIM — `listImpactedUsers(ch)`: identity `attributes['enduser.id']` else `attributes['user.id']` (both key literals as SQL-text constants), ROOT spans only, 24h; `requests`/`failures` (root `status_code = 'error'`)/`slow` (root `duration_ns ≥ 2s`, `SLOW_NS` once in `lib/users-types.ts`)/`lastFailure`/`risk` (≥10% at-risk; ≥2% failures OR ≥20% slow degraded; else healthy — evaluated in that order); cap 50 by requests + total. `UsersMock.tsx` = today's body VERBATIM; `UsersLive.tsx` SERVER component (D392) rendering "last 24h", the slow rule in the column header ("slow = ≥2s"), the risk rule printed on the surface, the cap banner, the `data-tour="users"` anchor (D404), and the empty state naming both keys. ABSENT: `org`, `plan`.
- done-check: web suite green; users.test.ts (tripwire red-proven; 24h bound; key literals present in SQL text and never interpolated from input; risk rule unit-proven at the boundaries 10%/2%/20%) + users.integration.test.ts (two seeded users — one via `enduser.id`, one via `user.id` — with error + slow root spans → exact counts/risk/lastFailure trace id; a child span carrying the id → NOT counted; a 25h-old row excluded; second workspace sees none); `page.test.ts` guard (no `@/mock/`, no `"use client"` in UsersLive, mock-branch-first); byte-identity by the integrator.
- result:

### T4: issues — stateless error grouping over spans/logs (D361)
- status: approved (ADVISOR-SIGNED D411; D414 rider landed + re-review approve: 51 signatures → 50 rows, total 51; name is NOT normalized so digit-suffixed names stay distinct (engine-confirmed uniqExact 51); tie-break parity JS↔CH proven on mixed-length hex). Reviewer catch, engine-proven: the hex rule swallowed 8+-digit runs as <id> while shorter runs became <num> — two messages differing only in digit-run length split into two issues (D399 violation); fixed by emitting ONE placeholder <num> for uuid/hex/digit rules (alternatives "require a–f" and "digits first" both proven incomplete); corpus gained the 12345678/1234567 pair → one issue count 2. SQL-only fingerprint confirmed (no TS helper), logs out, argMax most-recent asserted, tripwire red-proven, 24h bound by seeded 25h row, count() OVER () pre-LIMIT engine-proven. ESCALATE→advisor: placeholder token name (<num> vs <id>) cosmetic; residual: hex-letter runs <8 chars split (stated in code); >50 cap path unproven at scale.)
- wave: 1
- depends: —
- owns: apps/web/src/lib/issues-types.ts (new — types + `WINDOW_HOURS` + status-window constants ONLY; no fingerprint helper, D399), apps/web/src/server/queries/issues.ts (new) + issues.test.ts + issues.integration.test.ts (new), apps/web/src/components/issues/IssuesMock.tsx (new — VERBATIM body incl. `Spark`), apps/web/src/components/issues/IssuesLive.tsx (new), apps/web/src/app/app/issues/page.tsx, apps/web/src/app/app/issues/page.test.ts (new)
- goal: implement D399 VERBATIM — `listIssues(ch)` over error spans ONLY (`status_code = 'error'`, 24h; logs OUT), fingerprint computed ONLY in SQL over (`service`, `layer`, `name`, normalized `status_message`) and returned as a string; normalization = digits, hex/uuid ids, quoted strings, numeric URL path segments collapse — anything else distinguishes; `title` = normalized message else span name; `spark` = 24 hourly buckets oldest-first (zero = real count); first/lastSeenAt within the window; `status` from recency only vs D50's 6h (new = all occurrences inside 6h; resolved = none inside 6h; ongoing otherwise); `exampleTraceId` = most recent trace; cap 50 by count + total. `IssuesMock.tsx` = today's body VERBATIM incl. `Spark`; `IssuesLive.tsx` SERVER component (D392) with "last 24h", "first seen in this 24h window", the status legend stating the 6h rule, the cap banner, the `data-tour="issues"` anchor (D404). NO persistence/assign/mute/resolve (D361).
- done-check: web suite green; issues.test.ts (tripwire red-proven; 24h bound; the SQL text contains no interpolated input) + issues.integration.test.ts (seeded error spans: A `"timeout after 1234ms for order 9f8e7d6c"` and A' `"timeout after 98ms for order 0a1b2c3d"` in the same service/layer/name → ONE issue with count 2; the same message under another service → a second issue; the same message under another span name → a third; a quoted-string variant merges; a message differing in a plain word does not; sparks sum to counts; a span 7h old only → resolved, one 1h old only → new, both → ongoing; a 25h-old row excluded; second workspace sees none); `page.test.ts` guard (no `@/mock/`, no `"use client"`, mock-branch-first); byte-identity by the integrator.
- result:

### T5: traces/diff — two real traces side by side
- status: approved (ADVISOR-SIGNED D415; D416 rider landed + re-review fixed: caption "most recent trace — pick another to compare" only when ?a= absent AND a resolves; guards hardened to pin the derivation and the render (two mutations proven red); mock untouched, branch still precedes the first await). Reviewer: dead href fallback deleted; mock byte-identity = export line only; /app/traces/diff still prerenders static in mock (prerender-manifest); the only TraceExplorer consumer is [id]/page.tsx passing `compare`; `compareEnabled` gone repo-wide; live-branch sabotage → guard red; tenancy discriminates both ways. ESCALATE (recommend keep): absent ?a= resolves to the most recent trace (the frozen mock does the same via slowTrace default).)
- wave: 1
- depends: —
- owns: apps/web/src/app/app/traces/diff/page.tsx, apps/web/src/app/app/traces/diff/page.test.ts (new), apps/web/src/app/app/traces/diff/page.integration.test.ts (new — accepted into T5 owns by the manager: house test pair, nobody else owns the dir), apps/web/src/components/trace/TraceDiffMock.tsx (new — today's TraceDiff.tsx VERBATIM), apps/web/src/components/trace/TraceDiffLive.tsx (new, SERVER component, props `{a: Trace | null, b: Trace | null, recent: Trace[]}` from `@/lib/types` — D400), apps/web/src/components/trace/TraceDiff.tsx (deleted after the move), apps/web/src/components/trace/TraceExplorer.tsx (D400: `compareEnabled: boolean` → `compare: { href: string } | null`; `import { allTraces }` DELETED), apps/web/src/app/app/traces/[id]/page.tsx (computes `compare` per branch)
- goal: implement D400 VERBATIM — `traces/diff/page.tsx` becomes a server component whose mock branch returns `<Suspense><TraceDiffMock/></Suspense>` BEFORE `await searchParams`/`connection()`; live branch reads `?a=&b=`, calls `dataForSession().getTrace()` twice in parallel (C4: the EXISTING facade reads, no new query module) and `searchTraces({})` for the picker (30 most recent); `TraceDiffLive` (SERVER component, `rowsFor` duplicated per D391) renders the same span-by-span comparison for two REAL traces, "pick a second trace" + picker when `b` is absent, "trace not found in this workspace" for an unknown/foreign id. `TraceExplorer.tsx`: prop `compareEnabled` → `compare: { href: string } | null`, `import { allTraces }` DELETED; `traces/[id]/page.tsx` computes `compare` — mock branch = today's partner from `@/mock/traces` at the page (same href, same DOM), live = `/app/traces/diff?a=<id>`. `liveWiredRouteExclusions` is T6's.
- done-check: web suite green; `traces/diff/page.test.ts` source-text guard: `TraceDiffLive.tsx` has no `@/mock/` and no `"use client"`; `TraceExplorer.tsx` has no `@/mock/traces` import; the diff page's mock branch precedes any `await`; an integration-style test through `dataForWorkspace` with two seeded traces proves the diff rows and the foreign-id result; existing TracesSearch/trace-detail tests stay green; mock byte-identity (trace detail AND diff) by the integrator.
- result:

### T6: the flip + exit evidence — live-routes, seed fixture, drive arm, README
- status: ADVISOR-SIGNED D418, condition D419 (red re-run with final strings) pending. Drive 211/0 (132s); RED 198/13 = exactly the new arm; D405 re-derived by the reviewer: total 220→220, phantoms 0, serviceTotal 146→183 (<220), errorTotal 11, renderableLogs 244, dbPodErrorRows 1, span_rows 221→295, chained 37, end_user_root_spans 88 (44/44; 11 failures on user-1 → at-risk), new spans 0 tokens/cost, edges exactly gateway→agent, agent→tool; reviewer sabotage: drop /app/users from the registry → live-routes test red. Reviewer fixes: claim details print the missing flags (S2.2 L3), README tenancy sentence scoped honestly, "below"→"above", JSDoc→comment. Evidence gap noted: the issues-grouping claim's final word-based form differs from the red-run form (entailed-red: the unamended fixture writes no status_message). liveWiredRouteExclusions DELETED (no consumers). ESCALATE→advisor: /app/services/ subtree entry + deleted exclusion mechanism = the condition that created the list for /app/traces/.)
- wave: 2
- depends: T1, T2, T3, T4, T5
- owns: apps/web/src/lib/live-routes.ts, apps/web/src/lib/live-routes.test.ts (:11 asserts `/app/services` NOT wired today), apps/web/src/components/shell/TourGuide.tsx + TourGuide.test.ts (D404), deploy/compose/exit-seed.mjs (D405 fixture amendment: `enduser.id` on a SUBSET of root spans for two label-derived users, a cross-service parent→child chain gateway→agent→(tool|llm) on a SUBSET of traces, two distinct error `status_message` signatures via `labelled()`, zero tokens/cost on new spans, no new trace ids, no log rows), deploy/compose/e2e-drive.mjs (one step per surface + the tenancy half + the five routes and `/app/traces/diff?a=…` in the no-session probe :2814-2824 + the stale "this sprint's two routes" at :1973), deploy/compose/README.md ("What it asserts" in real order, the seed sentence, the stale :265 copy)
- goal: `/app/map`, `/app/services`, `/app/services/` (subtree), `/app/users`, `/app/issues` join `liveWiredRoutes`; `/app/traces/diff` leaves `liveWiredRouteExclusions`; TourGuide's map/issues/users steps become capability-only claims true in both modes with three `TourGuide.test.ts` analogues of `:173` (D404); the D405 fixture amendment lands with its six invariants MEASURED (the `denominators` table before/after pinned in the result); the drive proves per surface on alice's real seed: no SAMPLE badge, the page renders alice's label-bearing words (a service name on map + services, the label-derived enduser id on users, the labelled error signature on issues, two seeded trace ids on diff), bob's browser renders none of alice's words on the same surfaces (D142 idiom at :1571), and `/app/costs` remains the badged control; the no-session probe covers the five routes + diff. Proven RED first by seeding without the amendment (S2.0 L1) + a reviewer-chosen sabotage.
- done-check: `node deploy/compose/e2e-drive.mjs` green locally (every pre-existing count unchanged — D405 table); red transcript captured; `live-routes.test.ts` updated; `TourGuide.test.ts` green with the three new tests; README lists the new steps in the drive's real order and drops the stale copy.
- result:

## Escalations log
<!-- Manager appends: {T#} — question → advisor decision (one line each). -->
- 2026-09-01 wave-1 returns (advisor, read-only vs 91819cf) — **D407** T1 SIGNED-WITH-CONDITION (riders D408+D409; D403 proof stronger than asked; bound layerOrder better than ruled). **D408** RIDER T1 (`lib/map-layout.ts` + test): columns = layers PRESENT in `layerOrder` order — fixed-index NOT ratified (two-layer workspace paid ~970 viewBox units of dead space); determinism unchanged, test pins that a new layer takes its `layerOrder` place. **D409** RIDER (escalation 4): ONE `fmtPerMin` in `apps/web/src/lib/format.ts` — tiers `0→"0"`, `≥10→Math.round(n).toLocaleString("en-US")`, `≥0.1→1 decimal`, else 3 decimals, NO `<0.01` floor (D13: never hedge a number we hold); unit appended at the call site; owner T1 executor, files `lib/format.ts` (add), `components/map/ServiceMapLive.tsx` (delete local perMin), cross-assigned `lib/services-types.ts`, `components/services/ServicesLive.tsx`, `ServiceDetailLive.tsx`, `server/queries/services.test.ts` (delete fmtPerMin, import from `@/lib/format`, move the four pinned cases); re-review T1 reviewer, T2 reviewer confirms the services call sites. **D410** accepted limit: `EDGES_SQL` has no LIMIT (coupled to the top-40 in TS); backstop = the 30s profile; pre-registered fix = bind the 40 rendered names into both edge subqueries, never a timeout raise. **D411** T4 SIGNED-WITH-CONDITION (rider D414; the digit-length split was the sprint's best catch). **D412** `<num>` stands as the ONE token. **D413** hex-letter runs <8 chars split = known limit, code comment only. **D414** RIDER T4 (`issues.integration.test.ts`): seed 51 distinct signatures → exactly 50 rows, `total = 51`; re-review T4 reviewer. **D415** T5 SIGNED-WITH-CONDITION (rider D416). **D416** D400 AMENDED: with `?a=` absent, KEEP the auto-pick and CAPTION slot A "most recent trace — pick another to compare" (only in that case); page passes whether `a` was absent; mock untouched; owner T5 executor, re-review T5 reviewer. **D417** rename-in-T3's-commit = shared-tree history noise, retro note only. Sequencing: the three riders run in parallel with T6; no further advisor return owed on T1/T4/T5 after re-review; T6 still returns (D406).
- 2026-09-01 T6 return (advisor vs 68d84a5) — **D418** T6 SIGNED-WITH-CONDITION (D405 discharged item by item on independently re-derived numbers; +74 span_rows = 37×2 reconciles with chained_traces; D404 satisfied; reviewer sabotage hit the predicate SampleDataBadge reads). **D419** CONDITION: one RED re-run with the FINAL claim strings (revert dataset() to HEAD's byte-identical form, run once, expect the same 13 new-arm failures, restore) — transcript attached; the GREEN of record = the integrator's run at HEAD; owner T6 executor, re-review T6 reviewer. **D420** subtree entries stay legal; `liveWiredRouteExclusions` stays deleted; the carve-out returns with its first real entry + reason (pre-registered; module docblock is its home); route-inventory test declined. **D421** D409 EXTENDS to `components/connections/ConnectionsHub.tsx` NOW: `formatRate(0.003)` printed `0.0/min`, indistinguishable from a measured zero on a live production surface — `formatRate` becomes `ratePerMin === null ? "—" : `${fmtPerMin(ratePerMin)}/min`` and the demo branch's raw `toLocaleString()/min` (:307) calls the same; pinned cases survive (0→"0/min", 2.4→"2.4/min", 8420→"8,420/min"); add the sub-0.1 case to `ConnectionsHub.test.tsx`; owner T1 executor, re-review T1 rider reviewer. Sequencing (manager): both riders wait for the integrator's drive/test run to finish (shared tree + ports), then run in parallel.
- 2026-09-01 integrator r1 NO-GO brief (advisor) — **D422** ONE p95 derivation: drop the cast in `topology.ts:38`, adopt `ifNotFinite(quantile(0.95)(duration_ns) / 1e6, 0)` verbatim (casting T2 would truncate a measured percentile and destroy the reviewed 38.5 assertion; un-casting T1 = zero test churn); **D396 AMENDED**: p95 = "the 95th percentile of span duration, interpolated at full precision and rounded once at render" — not "the overview.ts idiom verbatim" (a ruling naming a mechanism can only be conformed to, S6.1-L2 again); `overview.ts` untouched; owner T1 executor, re-review T1 reviewer; expectation: seeded 200/300 unchanged, the drive's exit-agent map node reads 325ms = the catalog's. **D423** ONE service population from the schema's own rule `service != ''` (trace_summaries_mv's groupUniqArrayIf): (a) `topology.ts` adds `AND service != ''` to NODES_SQL and BOTH edge subqueries (+ one integration case: an unnamed span → no node, no edge, no totalServices bump) — same rider commit as D422; (b) `issues.ts` UNCHANGED (an unnamed span is still a failure); `IssuesLive` renders an empty service as `—` — owner T4 executor, re-review T4 reviewer. Neither reopens a signed verdict.

## Integration
- verdict: **NO-GO (round 1)** — every gate green, one seam ruling owed (map vs catalog print different p95 for the same service, same window). Nothing else blocks; the fix is a one-line SQL cast either way, but it changes a reviewed assertion in whichever task loses, so the integrator does not rule it.
- rounds: 1 (vs code state 68d84a5 = c8f5bff; c8f5bff is `.planning`-only)
- e2e: mock `next build` at HEAD FIRST (S6.1-L1) — green; `/app/{map,services,users,issues,traces/diff}` prerender ○, `/app/services/[id]` + `/app/traces/[id]` ƒ. Web suite **754 tests / 754 pass / 0 fail / 0 skipped** (19.2s) — the 47 Postgres tests and every ClickHouse integration file RAN. First run was 750/4: the four failures were the pre-registered local drift (`plans.free` at explain_quota=2 / event_quota=300); restored to 0005_metering.sql:129 / 0006_explain_quota.sql:20's 50000/20, re-ran green, and restored again after the drive re-lowered them — harness posture, not a S6.2 defect. `npx tsc --noEmit -p .` clean; `npx eslint src --max-warnings 0` clean. **Drive at HEAD: `node deploy/compose/e2e-drive.mjs` = 211 passed / 0 failed in 132s** — all 18 claims of the new arm ok (5 surfaces + detail + diff + the D142 tenancy halves + 6 no-session probes), `/app/costs` still badged. `git diff master..s6-2 --stat` EMPTY for `services/ingest`, `deploy/railway`, `deploy/helm`, `apps/web/src/mock/` (A2 + run-goal condition 9 hold); `go vet ./...` clean.
- byte-identity (run-goal condition 5): two-worktree mock `next build`, master `25548fa` vs HEAD, DOM with all `<script>` stripped. `/app/map` `/app/services` `/app/users` `/app/issues` `/app/traces/diff` from the prerender; `/app/services/gateway` `/app/services/agent-worker` `/app/traces/a3f8c1d92b6e407f` `/app/traces/b7e2d94a1c8f5e30` rendered through `next start` on both builds (they are ƒ). **All nine differ in exactly one token — the global stylesheet chunk name** `03iu1yoexhm2_.css` → `1mnd_dz2mmc5u.css`. Provably the D367 chunk-name class, twice over: the same single token differs on untouched controls `/app/costs`, `/app/alerts`, `/app/oncall`, and the two CSS files differ by 5 pure ADDITIONS, 0 removals, 0 modifications (new Tailwind utilities the Live components introduced). Normalising the chunk hash makes every route md5-identical. Trace detail's mock compare href is byte-identical across the D400 prop change (`?a=a3f8c1d92b6e407f&b=27ed8abb0c38833a` on both builds).
- seam findings: (1) T6↔T1–T5 claim strings all match the Live source: `&lt;num&gt;` + `7× in 24h` vs IssuesLive's `{issue.title}` / `{issue.count}× in {WINDOW_HOURS}h`; `AT RISK` = `riskStyle["at-risk"].label`; the map's `<line>` count = edges only (a node is 2 rects + circle + 3 texts, no line); every negated empty-state/cap string exists verbatim on its own surface (`No service has sent a span` map / `no service has sent a span` services / `No spans from` detail / `set one on your root span` users / `No errors in the last` + `by occurrences in the last` issues / `trace not found in this workspace` + `pick a second trace` diff), so no negation is vacuous; `SampleMark` renders `SAMPLE` and its reason in `title=`, so `!includes("SAMPLE DATA")` and `includes("deploy tracking arrives with the changes feed")` are both true at once — the D362/D401 seam works. (2) **T1↔T2 (D393/D409): spans, errorPct, spansPerMin and layer agree EXACTLY for all 5 services in alice's seeded workspace `ws_0436b55fc60335dc`; p95 does NOT** — see the brief. (3) D394: `WINDOW_HOURS = 24` in all four types files and no range argument anywhere; topology/services bind `now() - toIntervalHour({window_hours:UInt32})`, users interpolates the same constant, issues anchors `since_s` in TS (`nowS - 24*3600`) so the scan bound, the 24 spark buckets and the 6h status cut measure from one instant — a documented, deliberate idiom split, same window. (4) `/app/traces/diff` resolves through the `/app/traces/` subtree; `liveWiredRouteExclusions` has ZERO hits repo-wide; `SampleDataBadge`/`CommandPalette`/`DemoFooter` read only `isLiveWiredRoute`/`isProductChromeRoute`; the drive proves the badge is gone on diff and still on `/app/costs`. (5) `data-tour="map"|"issues"|"users"` present in BOTH the Mock and the Live component of each surface, pinned by `TourGuide.test.ts:231/245/257`. (6) D401: `services/[id]/page.tsx` has exactly one `@/mock/` import (`deploys`); `map/services/users/issues/traces-diff` pages have none; every `<X>Live.tsx` has zero `@/mock/` and zero `"use client"` (the ServiceMapLive hit is the word inside its docblock). (7) D395: zero `SETTINGS`/`max_execution_time` in topology/services/users/issues. (8) One `fmtPerMin` and one `WARN/ERR_ERROR_PCT` pair, each defined once and imported by both surfaces.
- fix tasks: none applied by the integrator (no slip was fixable without overturning a reviewed assertion). ONE advisor ruling owed — see the brief below; it lands with the D419/D421 riders, then round 2 re-verifies (build + suite + drive + the T1/T2 p95 probe).
- **ADVISOR BRIEF (round 1)**
  1. **Two p95 idioms across the T1↔T2 seam, visibly disagreeing on the drive's own data.** `server/queries/topology.ts:38` computes `toUInt32(ifNotFinite(quantile(0.95)(duration_ns / 1000000), 0))`; `server/queries/services.ts:49` computes `ifNotFinite(quantile(0.95)(duration_ns) / 1e6, 0)`. Engine-proven identical underneath (`exit-agent` in `ws_0436b55fc60335dc`: 324 vs 324.70000000000005; `exit-gateway`: 328 vs 328.04999999999995) — the ONLY difference is the `toUInt32` cast. Both surfaces render through the same `fmtMs` (`Math.round`), so **`/app/map` prints `p95 324ms` for `exit-agent` while `/app/services` and `/app/services/exit-agent` print `325ms`** — same metric, same 24h window, same stated rule, one click apart. Structural, not a slip: D396 pins "`overview.ts:59-60` idiom verbatim" (which IS the cast) and D397 says "same percentile rules as D396", so T1 is literal-conformant; but `services.integration.test.ts:298-303` deliberately and reviewedly pins the FRACTIONAL interpolated value (`p95 = 38.5`, "85% of the way from 30 to 40"), so casting T2 overturns a reviewed assertion, and un-casting T1 walks back a signed (D407) task's named idiom. Decision needed: ONE p95 derivation for the sprint — cast both to `toUInt32` (integer ms everywhere, D396 verbatim, T2's 38.5 assertion becomes 38), or drop the cast in `topology.ts` (float everywhere, `fmtMs` does the rounding, no test churn — `topology.integration.test.ts` seeds uniform durations so 200/300 stay exact) — and whether `p50Ms`/`p95Ms` in `lib/services-types.ts` and `lib/topology-types.ts` should say which.
  2. **(low, unexercised) map and catalog disagree on what counts as a service.** `services.ts:28` filters `service != ''` (`NAMED_SERVICE`, mirroring `trace_summaries_mv`); `topology.ts` NODES_SQL and `issues.ts` do not. A workspace with spans carrying no `service.name` therefore gets a nameless node plus a `totalServices` on `/app/map` that `/app/services` does not agree with — two answers to "how many services do you have". Zero such rows exist in any workspace in the compose store today, so nothing is currently wrong on screen. Decision: is the unnamed service a service (drop the filter), or not (add it to topology + issues)? Cheap to decline as a known limit + code comment, like D413.
  - not brief-worthy, recorded: the live compare ENTRY POINT (`traces/[id]/page.tsx:49` → `/app/traces/diff?a=<id>`) is proven by T5's guards and by mock byte-identity, but the drive reaches `/app/traces/diff` by URL and never clicks the link on a live trace-detail page — an evidence gap, not a defect.
  - **note for the manager, not the advisor:** D421's rider changes `ConnectionsHub.tsx`'s DEMO branch rendering (`:307` raw `toLocaleString()/min` → `fmtPerMin`). That is an advisor-ratified mock-DOM change on `/app/connections`, so round 2's byte-identity will show `/app/connections` differing beyond the chunk name; it is expected and in scope, and the five S6.2 surfaces are unaffected.

## Goal check (Step 7, one entry per iteration)
- iteration 0: pending

## Retro (filled at end of run)
- tasks: /6 (T1 Opus, T2 Opus, T4 Opus, T5 Opus (critical, production-live surface), T3 Sonnet — advisor sizing)
- breakdown corrections by advisor:
- reviewer verdicts:
- lesson:
