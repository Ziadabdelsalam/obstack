# Team Plan — obstack M5 sprint 3 (S6.3): dashboards for real — dashboards/widgets CRUD persists per-workspace in Postgres on the `saved_views` pattern, widget charts render from S6.1's frozen metrics contract, the overview's WatchWidgets read real data; `/app/dashboards` (+ subtree) flipped in `live-routes.ts`; no sharing/ACL beyond workspace scope; no chart kind beyond the four mocked (timeseries, stat, topn, table); mock mode byte-identical

meta:
- date: 2026-09-01
- repo/branch: `s6-3` (to cut from `master` @ `1000c39`; production = `eb2ba23`)
- mode: waves (house precedent S6.1/S6.2 — hand-dispatched waves, reviews as results arrive)
- team-cap: 10
- models: executor = Sonnet 5 · reviewer = Opus 5 · advisor = Fable 5 (user, Step 0: "skill defaults"). **Per-task exception (user-approved 2026-09-01 on the advisor's tier note): T1, T3, T6 executors = Opus 5.** T2/T4/T5 stay Sonnet 5.
- iteration-limit: 3 — fix rounds allowed before escalating to the user
- iteration: 0/3
- governing docs (binding): `.planning/2026-08-31-s6-m5-full-platform-phase-plan.md` §5 S6.3 (run-goal, scope fence: persistence follows `saved_views`; no sharing/ACL; no new chart types), §7 standing constraints, §8 rulings D360 (Option C), D364 (sequential), D365 (metrics do not meter); the S6.1/S6.2 standing set that generalizes: **D366** (contract types client-safe in `lib/*-types.ts`), **D367** (server page branches on `dataMode`; mock body moves VERBATIM to `<X>Mock.tsx`; live is a props-fed sibling; byte-identity), **D391** (duplication across the mock/live boundary is the ratified shape; "no `@/mock/` in the live graph" is a SOURCE-TEXT rule on Live files), **D402** (every unbounded list ships a cap and says so), D381 (`totalGroups` pre-truncation → "showing 10 of N"), D384 (a metric is keyed `(name, type)`), D390 (`VALID_AGGS` is the ONE agg-validity definition), D13/D21/D114, D7/D11/D113/D130 (tenancy: every statement names `workspace_id` and binds it from scope; hermetic SQL-text proof + "a second workspace sees none" integration proof — the `saved-views.test.ts`/`.integration.test.ts` pair), D195/D197/D199 (read-modify-write under `withTransaction` + `lockWorkspace`, `TxQuery` brand).
- predecessor: S6.2 closed 2026-09-01 (PR #30, production `eb2ba23`); retro lessons S6.1-L1..L3 and S6.2-L1..L4 apply (mock `next build` before `npm test` after a drive; rulings phrased as observable RESULTS not mechanisms; join/tenancy fixtures use the same ids AND names in the foreign workspace; assert words/DOM slices over server HTML, never counts; commit per task from a scoped `git add`).
- carried in from S6.2's resume point: D410, D413, the drive's URL-not-click path to the diff page (not touched here), the Railway watch-path observation (ingest + clickhouse rebuild on every production push — RELEVANT: this sprint's close DOES ship a Postgres migration, see run-goal condition 9).

## Foundation facts (audited 2026-09-01, this session — Explore agent, read-only, every claim cited)
- **Dashboards today are pure client in-memory state in BOTH modes.** `app/app/dashboards/page.tsx` (`"use client"`, :1) and `app/app/dashboards/[id]/page.tsx` (`"use client"`, :1) read `useWorkspace()` (`state/workspace-store.tsx:75-79`) whose `dashboards` is `useState<Dashboard[]>(seedDashboards)` (`:20`) seeded from `mock/dashboards.ts` — NO localStorage, NO Postgres, NO `dataMode` branch anywhere under `app/app/dashboards/`. `WorkspaceProvider` wraps the whole `/app` layout unconditionally (`app/app/layout.tsx:12,109,126`) and also carries oncall `channels` — it stays mounted in live (untouched). Store API: `createDashboard(name)→id`, `addWidget(dashboardId, Omit<Widget,"id">)`, `removeWidget`, `moveWidget(…, "up"|"down")` (`workspace-store.tsx:7-15`). Call sites: the two pages, `components/explore/SaveToDashboardModal.tsx:13`, `components/explore/ExploreMock.tsx:71`, `components/oncall/ChannelToggles.tsx:15` (unrelated).
- **Mock widget contract** (`mock/dashboards.ts:3-19`): `WidgetKind = "timeseries"|"stat"|"topn"|"table"`; `Widget {id, title, kind, metricId, groupBy: GroupBy}` with `GroupBy = "none"|"service"|"model"|"route"` from `mock/explore.ts:33`; `Dashboard {id, name, owner, updated, widgets}`. 4 seed dashboards / 18 widgets; all four kinds exercised. NO `range` field (charts hardcode `rangeHours: 6`, `WidgetCard.tsx:64`), NO per-widget filters. `components/dashboards/WidgetCard.tsx` (client) calls `exploreSeries(...)` from `@/mock/explore` synchronously (`:58-65`); `components/dashboards/AddWidgetModal.tsx` (client) picks from the OLD mock `metricCatalog` (`:5-6`). Both are mock-only by construction → they become the verbatim mock branch, never the live path.
- **S6.1 frozen contract** = `apps/web/src/lib/metrics-types.ts` (no `explore-types.ts` exists): `MetricCatalogEntry {name, type: "gauge"|"sum"|"histogram", unit, lastSeen, attrKeys}`, `MetricRange = "1h"|"6h"|"24h"`, `MetricAgg` (10 values), `VALID_AGGS` per type (D390), `MetricSeriesQuery {metric, type, range, agg, groupBy: string|null, filters: Record<string,string>}`, `MetricSeriesPoint {t, v: number|null}`, `MetricSeries {group: string|null, points}`, `MetricSeriesResult {series, totalGroups}`. Server: `server/queries/metrics.ts` `listMetricCatalog(ch)` (:71), `queryMetricSeries(ch, q)` (:290-293; throws on agg∉VALID_AGGS[type] :297-299; `v: null` for gaps never 0; top-10 groups with `totalGroups` pre-truncation :328-361). Bucket widths 1h→1m / 6h→5m / 24h→15m. **There is NO `app/api/*` route handler for metrics** — the live explore path is server-component props (`explore/page.tsx:34` mock-first, `:81-82` query) into the client `ExploreLive.tsx` (`:126-141` props `{catalog, query, result, seriesCap, capReached}`), recharts copied not shared (`:37-39`). `ExploreLive.tsx:41-44` documents the honest ABSENCE of save-to-dashboard pending S6.3; `explore/page.test.ts:87` bans `SaveToDashboardModal` from the Live source.
- **Overview / WatchWidgets**: `app/app/page.tsx` is a SERVER component (`:130 live`, `:133 connection()`, `:137-141 getOverview + checklist`). `components/dash/WatchWidgets.tsx` (client, no props, `:257`) reads everything from `@/mock/watch` (`:6-14`; `WidgetType` = service | service-compare | route | pod | model | tool | queue | pipeline; `WidgetConfig {id,type,a,b?}`) and persists its config in `localStorage` key `obstack-watch-widgets` (`:32`, `useSyncExternalStore` :245-278). In live it renders wrapped in `<SampleWidget>` (`page.tsx:243-249`, `SAMPLE_TITLE` :28) — explicitly still-sample. Also still sample in live: the "top failing operations" table (`page.tsx:198-202`, `topFailing` from `@/mock/metrics` :5) — NOT WatchWidgets, out of this sprint's title (carried, see conditions). `server/queries/overview.ts` exports `OverviewRange`, `OverviewPoint`, `OverviewStat`, `Overview`, `queryOverview(ch, range)` (:163-166).
- **`saved_views` pattern (the persistence template)**: DDL `services/ingest/pgmigrations/0002_saved_views.sql` — `id TEXT PK` (app-generated, D116), `workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE` (in-set FK allowed, D112), `surface TEXT CHECK (...)` (CHECK never enum), `filters JSONB`, `created_at/updated_at TIMESTAMPTZ`, `UNIQUE (workspace_id, surface, name)`. Web repo `server/saved-views.ts` (`import "server-only"` :1): every statement `WHERE workspace_id = $1` (LIST :53-57, UPSERT :66-70 `ON CONFLICT … DO UPDATE`, DELETE :72-74); functions take `(workspaceId, …, query: QueryRows)`; mutations answer with the fresh list; validation (blank name, vocabulary, payload shape) refuses BEFORE Postgres. Tests: `server/saved-views.test.ts` (:48 "every statement is bound to the workspace it was handed", :64 binding never post-filter, :101/:110/:123/:136 refusals, :148 app-generated id, :167 mutation answers with what the store holds) + `server/saved-views.integration.test.ts` (narrow skip ONLY when `OBSTACK_TEST_POSTGRES_DSN` unset; D36; schema applied ONLY by the ingest migrator, K1; two workspaces, same names, disjoint). Server actions `components/saved-views/actions.ts` (`"use server"` :1; `activeWorkspace()` :27-33 = `dataMode !== "live" → null`, else `getSessionContext()?.workspaceId`; `listViews/saveView/deleteView` :35-55 return `… | null`). Postgres client `server/postgres.ts`: `getPool()` :21-28, `queryRows` :31-37, `QueryRows` :44, `TxQuery` brand :70, `lockWorkspace(query, ws)` :85-87, `withTransaction(body)` :105-124.
- **Migrations**: highest = `services/ingest/pgmigrations/0008_metrics_health.sql`; `//go:embed *.sql` (`migrations.go:16-17`); applied by `internal/pgmigrate` (`schema_migrations`), at boot when `OBSTACK_PG_MIGRATE_ON_BOOT` (default true, `cmd/ingest/main.go:150-151`) or `ingest pg-migrate` (:66-68). Pin to update: `services/ingest/internal/pgmigrate/qa_a6_migrations_test.go:178` `const want = "0001_workspaces,…,0008_metrics_health"` (the S6.1 precedent added 0008 there); `pgmigrate_test.go:47` and `cmd/ingest/main_test.go:459,465` pin only 0001/0002 on stub data (unaffected). CI `web.yml:150-153` runs `npm test` with a real `OBSTACK_TEST_POSTGRES_DSN`, so `*.integration.test.ts` RUN in CI.
- **Flip & badge**: `lib/live-routes.ts:11-30` — `/app/dashboards` ABSENT (no hit in `live-routes.test.ts` either); trailing-slash entries are subtrees (`/app/services/` precedent, D420). `SampleDataBadge.tsx:14-21` renders unless chrome or wired; layout `:118` `{live && <SampleDataBadge/>}`. `dataMode` from `server/data.ts:75`.
- **Tests & harness**: `apps/web` `npm test` = `tsx --conditions react-server --test "src/**/*.test.{ts,tsx}"` — client components with recharts cannot be imported under react-server → page guards are SOURCE-TEXT tests (`explore/page.test.ts`, `services/page.test.ts` precedents). No dashboards tests exist anywhere. e2e = `deploy/compose/e2e-drive.mjs` (`check()` :405, `step()` :401; explore live step :2552-2572 parses `<h1>` and asserts `!html.includes("SAMPLE DATA")`; five-surface step :2588; D142 disjointness :2728; no-session probe ≈:2820) — ZERO `dashboards` references. Seeder `deploy/compose/exit-seed.mjs` already opens Postgres directly (`pg.Client`, :458, `LOWER_FREE_QUOTA_SQL` :454) and POSTs the metrics leg (gauge value 42, sum 60, histogram p90 36 through `/v1/metrics`, `:542-562`); the drive has its own `pgPool` (:651). TourGuide has NO dashboards step (grep empty) → no D404 work this sprint.
- **D367 precedent to copy**: `app/app/services/page.tsx:21-29` — `if (dataMode !== "live") return <ServicesMock />; await connection(); const session = await getSessionContext(); if (!session) redirect("/login"); return <ServicesLive list={await listServices(forWorkspace(session.workspaceId))} />;`. Origin idiom `connections/page.tsx:36-38`.

## Manager's proposed design (for the advisor to rule — decide, amend, or replace; ledger continues at D424)
- **P1 persistence shape**: ONE new table via `0009_dashboards.sql`: `dashboards (id TEXT PRIMARY KEY, workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE, surface TEXT NOT NULL CHECK (surface IN ('dashboard','watch')), name TEXT NOT NULL, widgets JSONB NOT NULL DEFAULT '[]', created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE (workspace_id, surface, name))` + `CREATE UNIQUE INDEX dashboards_one_watch_per_workspace ON dashboards (workspace_id) WHERE surface = 'watch'`. Widgets are an ORDERED JSONB array (position = index; move = reorder) — mirrors `saved_views.filters JSONB`; no second table. Widget mutations are read-modify-write → `withTransaction` + `lockWorkspace` (the D195/D197 idiom), `TxQuery`-branded. Alternative rejected: a `dashboard_widgets` table with a `position` column (more DDL, more statements, no reader that needs a per-widget row).
- **P2 WatchWidgets in live = the workspace's `watch` dashboard** (surface `'watch'`, one per workspace by the partial unique index, name fixed `"Overview watch"`, created on first pin). The overview renders `WatchWidgetsLive` from that row through the SAME widget contract and the SAME renderer as any dashboard; "edit" links to `/app/dashboards/<watch-id>` (same `DashboardDetailLive`). Fixture-only watch types (pod, queue, pipeline, route, service-compare…) are ABSENT in live (D13); the mock `WatchWidgets.tsx` and its localStorage key are untouched (byte-identity). Alternative rejected: keep the localStorage config and fetch series client-side — needs a JSON API that does not exist (S6.1 chose server-component props), is per-browser not per-workspace, and would have to fabricate the fixture's widget types.
- **P3 live widget contract** (`lib/dashboard-types.ts`, client-safe per D366 — the advisor freezes the text; the MANAGER lands it as the first commit on `s6-3` so wave 1 compiles against it):
  ```ts
  import type { MetricAgg, MetricCatalogEntry, MetricRange } from "./metrics-types";
  export const WIDGET_KINDS = ["timeseries", "stat", "topn", "table"] as const;   // the four mocked kinds — the fence
  export type WidgetKind = (typeof WIDGET_KINDS)[number];
  export const DASHBOARD_SURFACES = ["dashboard", "watch"] as const;
  export type DashboardSurface = (typeof DASHBOARD_SURFACES)[number];
  export const WATCH_DASHBOARD_NAME = "Overview watch";
  export const MAX_WIDGETS_PER_DASHBOARD = 12;   // D402: stated on the surface when reached
  export const MAX_DASHBOARDS = 50;              // D402
  export const MAX_TITLE_CHARS = 80;
  export interface DashboardWidget { id: string; title: string; kind: WidgetKind; metric: string; type: MetricCatalogEntry["type"]; agg: MetricAgg; range: MetricRange; groupBy: string | null; }
  export interface DashboardSummary { id: string; surface: DashboardSurface; name: string; widgetCount: number; updatedAt: string; }
  export interface Dashboard extends DashboardSummary { widgets: DashboardWidget[]; }
  ```
  `filters` is always `{}` in v1 (explore live also sends `{}`); `topn`/`table` REQUIRE `groupBy !== null`, `stat` REQUIRES `groupBy === null` — validated in the repo module before Postgres (saved-views posture) and by the add-widget UI.
- **P4 widget semantics as observable results (D13; `lib/widget-view.ts`, pure, unit-tested)**: timeseries = one line per series, null gaps unplotted, D381 caption when `totalGroups > 10`; stat = the value of the LAST non-null point of the single series, the bucket's time under it ("as of 14:05"), "—" with "no points in the last {range}" when none; topn = one bar per group ordered by group value desc; table = one row per group (group, value, points) same order; **group value** = `sum` of non-null points when `agg === "sum"`, else the `average` of non-null points — the rule printed on the card ("sum over last 6h" / "avg of 5m buckets, last 6h"). No fabricated deltas, no sparklines from nothing.
- **P5 render path**: `DashboardDetailLive` is a SERVER component that runs `queryMetricSeries` for every widget in parallel (`Promise.all`, each widget's failure isolated to "couldn't load this widget" rather than the page); charts are a props-fed CLIENT `WidgetLive.tsx` (recharts copied from `ExploreLive`, not shared — D391). Interactivity (add/remove/move/create/delete) via server actions in `components/dashboards/actions.ts` (`saved-views/actions.ts` pattern: `dataMode` gate + session → workspaceId; returns the fresh `Dashboard`/list or `null`) called from a small client `DashboardEditor.tsx`, followed by `router.refresh()`. Add-widget UI in live picks from `listMetricCatalog` passed as props (keyed `(name,type)` D384) and offers only `VALID_AGGS[type]` (D390).
- **P6 explore → dashboard in live**: `ExploreLive` gains "Save to dashboard" (the D367 note said "until S6.3"): a client `SaveToDashboardLive.tsx` listing the workspace's dashboards (props from `explore/page.tsx`'s live branch via `listDashboards`) + "new dashboard"; calls the `addWidget` action with the current `{metric,type,agg,range,groupBy}`; kind = `topn` when grouped bar else `timeseries`. Mock `SaveToDashboardModal`/`ExploreMock` untouched. Advisor may rule this OUT of S6.3 if it judges the sprint at the ceiling; the manager's default is IN (it is the "build a dashboard" front door).
- **P7 D367 shape for the two dashboards routes**: pages become SERVER components; mock branch returns `<DashboardsMock/>` / `<DashboardDetailMock …/>` FIRST and before any `await` (`[id]` passes the unawaited `params` promise to the mock, S6.2 T2 precedent); today's two client page bodies move VERBATIM (`"use client"` kept, `ExploreMock` precedent). Live siblings `DashboardsLive.tsx` (server; list + create) and `DashboardDetailLive.tsx` (server; widgets + editor). Source-text `page.test.ts` guards: Live files contain no `@/mock/`, no `workspace-store`, mock branch precedes the first `await`.
- **P8 tenancy proofs** (house pair, per module): hermetic `dashboards.test.ts` asserts every SQL string names `workspace_id` and binds it as `$1` from the argument, never from the caller's payload; `dashboards.integration.test.ts` seeds the SAME dashboard name AND the same widget ids in two workspaces (S6.2-L2) and proves list/get/mutate/delete in one never reach the other, the watch partial-unique index holds (second watch row for the same workspace fails, a second workspace's watch succeeds), and `ON DELETE CASCADE` via the FK. Unknown/foreign dashboard id → "no dashboard with this id in your workspace" + link to the list (never 500, never the other tenant's row).
- **P9 flip**: `/app/dashboards` + `/app/dashboards/` (subtree, D420 legal) join `liveWiredRoutes`; `/app` is already wired — the `<SampleWidget>` wrapper around WatchWidgets is REPLACED in the live branch by `<WatchWidgetsLive …/>` (the mock branch keeps `<WatchWidgets/>` verbatim). The top-failing table's `sample={live}` stays (carried forward, not this title).
- **P10 deploy touchpoint (pre-registered NOW, S6.1 precedent)**: S6.3 close is NOT web-only — `0009_dashboards.sql` is applied by the ingest binary at boot (`OBSTACK_PG_MIGRATE_ON_BOOT`), so the production push must confirm ingest's boot log shows `0009_dashboards` applied on the intact Postgres volume (K0 staging first). Additive table; rollback = web revert (`git push --force-with-lease origin eb2ba23:production`), the table may stay. The Railway watch-path observation means ingest rebuilds anyway.

## Advisor decisions
<!-- Kickoff verdict 2026-09-01 (advisor = Fable 5, fresh spawn for S6.3; every Foundation-facts citation verified against 1000c39). Breakdown SOUND with corrections applied to the task blocks below. Two misses corrected: `SampleWidget` (page.tsx:36-51) becomes dead code at the flip; the TourGuide `/app` "Your watches" step (TourGuide.tsx:29-34) goes false at the flip. Binding for all agents; ledger continues at D439. -->
- A1: the §5 fence + the rulings below are binding verbatim; an executor who finds them false against the code/engine STOPS and reports (S4.4 L5). Edits to `lib/dashboard-types.ts` after T0 return to the advisor.
- **D424 — P1 AMENDED: DDL of `services/ingest/pgmigrations/0009_dashboards.sql`, verbatim.** No `surface` column, no partial index. A second dashboard under a taken name is REFUSED, never upserted (an upsert would silently replace widgets — the one place the saved_views idiom must not be copied). List order `ORDER BY created_at, name`. Pin `qa_a6_migrations_test.go:178` gains `,0009_dashboards`.
  ```sql
  -- Dashboards (S6.3, D424): workspace-scoped rows on the saved_views pattern (0002).
  -- One table: a dashboard's widgets are an ORDERED JSONB array of
  -- lib/dashboard-types.ts's DashboardWidget (position = index, pin = flag) — no
  -- reader needs a per-widget row. Name is the identity within a workspace
  -- (UNIQUE) and creating under a taken name is REFUSED, never upserted: an
  -- upsert here would silently replace someone's widgets. CHECK never enum
  -- (0002's rule); the workspace FK is in-set (D112) and a deleted workspace takes
  -- its dashboards with it.
  CREATE TABLE IF NOT EXISTS dashboards
  (
      id           TEXT PRIMARY KEY,
      workspace_id TEXT NOT NULL REFERENCES workspaces (id) ON DELETE CASCADE,
      name         TEXT NOT NULL CHECK (name <> ''),
      widgets      JSONB NOT NULL DEFAULT '[]'::jsonb CHECK (jsonb_typeof(widgets) = 'array'),
      created_at   TIMESTAMPTZ NOT NULL DEFAULT now(),
      updated_at   TIMESTAMPTZ NOT NULL DEFAULT now(),

      UNIQUE (workspace_id, name)
  );
  ```
- **D425 — P2 REPLACED: the overview watch is a VIEW over pinned widgets, not a row.** Every widget has a `pinned` flag; `/app` in live renders exactly the workspace's pinned widgets, in dashboard list order then widget order, each linking to its home dashboard; nothing pinned → the D436 sentence. No "Overview watch" row, no reserved id, no create-on-read, no partial index; the flag is toggled in the editor (D428) and is a widget mutation like any other (D429). Cap `MAX_PINNED_WIDGETS = 8`, stated when reached. Rationale: one derivation (the D393 shape).
- **D426 — P3 FROZEN: the complete text of `apps/web/src/lib/dashboard-types.ts` = the file landed by T0 (read it there; the plan does not restate it).** `filters` is NOT stored: every widget query sends `filters: {}` (fence 10). Types: `WIDGET_KINDS`/`WidgetKind`, `METRIC_RANGES`, caps `MAX_DASHBOARDS = 50`, `MAX_WIDGETS_PER_DASHBOARD = 12`, `MAX_PINNED_WIDGETS = 8`, `MAX_NAME_CHARS = 80`, `DashboardWidget {id, title, kind, metric, type, agg, range, groupBy, pinned}`, `Dashboard {id, name, widgets, updatedAt}`, `NewDashboardWidget = Omit<DashboardWidget,"id">`, `WidgetLoad = {ok:true; result} | {ok:false}`, `PinnedWidget {dashboardId, dashboardName, widget}`, `DashboardActionResult = {dashboard} | {refused: string} | null`.
- **D427 — P4 AMENDED: ONE fold per agg, printed on every card.** Stat, topn and table reduce a series' non-null points by the SAME rule chosen by `agg`: `sum` → sum; `min` → min; `max` → max; `last` → the last non-null point; `avg`, `rate`, `p50/p90/p95/p99` → arithmetic mean of the non-null bucket values. Caption on every stat/topn/table card, exact form: `{sum|min|max|mean} · last {range} · {n} of {N} buckets had data`; for `last`: `latest · as of {t} · last {range}` where `t` is that point's `t`. Timeseries = one line per series, null gaps unplotted (`connectNulls` false), legend when >1. Topn = one bar per group ordered by folded value desc, ties by group name, width ∝ max. Table = one row per group `group | value | buckets` (`n/N`), same order. D381 caption `showing {series.length} of {totalGroups} groups` whenever `totalGroups > series.length`. Zero series or all points null → the D436 empty sentence, no chart. No deltas, sparklines, colors-by-unit or fabricated units — `fmtVal` copied from `ExploreLive.tsx:51-54` (D391). Bucket width is NOT restated in the web; the `n of N` count carries sparsity honestly.
- **D428 — P5 AMENDED: pages read, Live renders.** `dashboards/[id]/page.tsx` live branch: `getDashboard` + `listMetricCatalog` + `loadWidgetResults` in parallel, then `<DashboardDetailLive dashboard catalog loads />` (SERVER component, zero fetching). `app/app/page.tsx` live branch: `listDashboards` → `pinnedWidgets()` → `loadWidgetResults` → `<WatchWidgetsLive pinned loads />`. The one shared read is `apps/web/src/server/widget-results.ts` (T2, `server-only`): `loadWidgetResults(ch: ScopedClickHouse, widgets: DashboardWidget[]): Promise<WidgetLoad[]>` — index-aligned, `Promise.all`, each widget's throw becomes `{ok:false}` for that slot only (hermetic test: a fake `ch` that throws for widget 2 → slots 1,3 ok). `apps/web/src/lib/widget-view.ts` (T2, pure) owns `widgetQuery(w): MetricSeriesQuery` (`filters: {}`), `pinnedWidgets(dashboards): PinnedWidget[]`, `foldSeries`, `statView`, `groupRows`. `WidgetLive` (T2, client, recharts copied) props `{ widget, load, href? }`. Interactivity = `DashboardEditor.tsx` (client) calling T1's actions then `router.refresh()`; `AddWidgetLive` offers metric `(name,type)` from the catalog prop, `VALID_AGGS[type]` only, range from `METRIC_RANGES`, groupBy from the entry's `attrKeys` (forced null for stat, required for topn/table), title default `${metric} · ${agg}`, a pin checkbox. Editor per widget: remove, up, down, pin/unpin.
- **D429 — mutation transaction rule.** Every mutation (`createDashboard`, `renameDashboard`, `deleteDashboard`, `addWidget`, `removeWidget`, `moveWidget`, `setWidgetPinned`) takes `query: TxQuery` as its LAST argument and its FIRST statement is `lockWorkspace(query, workspaceId)`; the server action wraps `withTransaction` (the `settings/actions.ts:224` / `ingest-health.ts:425` idiom — D199's single brand cast stays where it is; hermetic tests hand the recorder `as never` per `ingest-health.test.ts:45-50`). Reads (`listDashboards`, `getDashboard`) take `QueryRows`. Every mutation answers with the fresh row read back after the write (saved-views :167). Refusals are thrown as `class DashboardRefusal extends Error` (the `OverrideLimit` precedent); the action catches ONLY that class → `{ refused: message }`, everything else propagates. `deleteDashboard` action returns `Dashboard[] | null` (fresh list); all others `DashboardActionResult`. `moveWidget` past either end = no statement after the lock and read, row returned unchanged.
- **D430 — validation before Postgres (observable: refused with the sentence, ZERO statements run).** Name/title trimmed non-empty and ≤80 ("name is required" / "name is longer than 80 characters"); `kind` ∈ WIDGET_KINDS; `type` ∈ gauge|sum|histogram; `agg` ∈ VALID_AGGS[type]; `range` ∈ METRIC_RANGES; `metric` non-empty; `groupBy` null or non-empty string; stat ⇒ null, topn/table ⇒ non-null ("a stat has no group-by" / "top-n and table need a group-by"); `pinned` boolean; stored JSON carries EXACTLY the DashboardWidget keys (extra keys, a client-supplied `id` or `workspace_id` are dropped — proven by inspecting the bound JSON). Cap refusals need a read and happen inside the tx after the lock+read, before any write: "this dashboard is full (12 widgets)", "workspace limit reached (50 dashboards)", "overview is full (8 pinned)". Unique violation (`23505`) on create/rename → "a dashboard named “{name}” already exists" (never a 500). Metric existence is NOT checked; an absent metric renders the empty sentence.
- **D431 — P7 ACCEPTED with mechanics.** `dashboards/page.tsx`: `if (dataMode !== "live") return <DashboardsMock />;` then `await connection()`, session, redirect. `dashboards/[id]/page.tsx`: `if (dataMode !== "live") return <DashboardDetailMock params={params} />;` — the unawaited promise, the body keeps `use(params)` (`services/[id]/page.tsx:33` precedent verbatim). Mock files = today's bodies VERBATIM modulo the export line (`export function DashboardsMock()`, `export function DashboardDetailMock({ params })`), `"use client"` kept. Guards in `dashboards/page.test.ts`: both mock branches precede the first `await`; every `*Live.tsx`, `DashboardEditor.tsx`, `AddWidgetLive.tsx`, `WidgetLive.tsx` has zero `@/mock/`, zero `workspace-store`, zero `WidgetCard`/`AddWidgetModal`; `DashboardsLive`/`DashboardDetailLive`/`WatchWidgetsLive` have no `"use client"`.
- **D432 — P8 ACCEPTED, sharpened.** Hermetic `dashboards.test.ts`: every statement names `workspace_id` and binds the scope argument as `$1`; the first statement of every mutation is the advisory lock; each D430 refusal runs zero statements; a payload's own `id`/`workspace_id` never reaches a binding. Integration `dashboards.integration.test.ts` (skip ONLY when `OBSTACK_TEST_POSTGRES_DSN` unset; schema by the ingest migrator): two workspaces holding the SAME dashboard name and the SAME widget ids (S6.2-L2; dashboard ids differ — PK); list/get/add/remove/move/pin/rename/delete in one never touch the other; `getDashboard(wsB, aliceId)` → null; cascade on workspace delete; 13th widget, 51st dashboard, 9th pin refused with the sentences; duplicate name refused; mutation-proven: dropping the `$1` predicate in the fixture's own SELECT leaks.
- **D433 — P6 AMENDED: explore saves what the user is looking at.** `ExploreLive` shows "Save to dashboard" only when `query !== null`; `SaveToDashboardLive` lists `dashboards` (name, widget count, "full" at 12) + "new dashboard…" (name field → `createDashboard` action, then `addWidget`). The saved widget: `kind: "timeseries"` ALWAYS (explore's bar is bars over time — `ExploreLive.tsx:179,283` — not a ranking), `{metric, type, agg, range, groupBy}` = the current query, `pinned: false`, `title` = `${metric} · ${agg}` + (groupBy ? ` by ${groupBy}` : ""). `{refused}` prints in the modal; `{dashboard}` → toast "saved to {name}" linking `/app/dashboards/{id}` (toast pattern copied from `ExploreMock`, D391). `explore/page.tsx` live branch adds `listDashboards(session.workspaceId, queryRows)` to its `Promise.all` → `dashboards` prop. `ExploreLive.tsx:41-44` comment replaced by the truth. Guard: keeps the four bans, adds REQUIRED `SaveToDashboardLive` import (sabotage red).
- **D434 — P9 AMENDED.** `/app/dashboards` + `/app/dashboards/` join `liveWiredRoutes`. In `app/app/page.tsx` the `:243-249` ternary becomes `live ? <WatchWidgetsLive pinned={pinned} loads={loads} /> : <WatchWidgets />`; `SampleWidget` (`:36-51`) is DELETED (its one consumer is gone; `SAMPLE_TITLE` stays for the header and the top-failing card). `WatchWidgetsLive` (server): root `<div data-tour="watches">`, header `pinned from your dashboards · {n}`, each card = `WidgetLive` with `href` to its dashboard, or the D436 sentence + link. TourGuide `/app` step (`TourGuide.tsx:29-34`, `target: "watches"`) rewritten capability-only, true in both modes (D404): pin any dashboard widget to the overview; no pods/queues/pipelines, no "kept in this browser"; `TourGuide.test.ts` gains the `:173`-shaped analogue banning "pod", "queue", "pipeline", "this browser" from that step.
- **D435 — P10 CONFIRMED with wording** (= run-goal condition 9 below).
- **D436 — the sentences, exact words.** List empty: `no dashboards yet — create one, or save a chart from Explore`. Unknown/foreign id: `no dashboard with this id in your workspace` + link `← back to dashboards`. Dashboard with no widgets (live): `no widgets yet — add one`. Widget empty (all kinds): `no points in the last {range}`. Load failure: `couldn't load this widget`. Overview nothing pinned: `nothing pinned to the overview yet — pin a widget from any dashboard` + link `dashboards`. Caps/duplicates per D430.
- **D437 — Postgres ids.** `dash_<16 hex>` / `wdg_<16 hex>` from `randomBytes(8)` (saved-views :76 idiom). The seed may use any id text of that shape.
- **D438 — the mock stays byte-identical on four routes**: `/app/dashboards`, `/app/dashboards/[id]`, `/app`, `/app/explore`; `mock/` diff EMPTY; `WorkspaceProvider`, `WidgetCard`, `AddWidgetModal`, `SaveToDashboardModal`, `WatchWidgets` untouched.
- Advisor tier note (recorded): T1, T3, T6 are above Sonnet's tier by the user's routing policy — manager asks the user for Opus on these three before dispatch (see meta).

## Critical items
- T1 — a new tenancy boundary plus a browser-reachable mutation surface; D429/D430/D432 evidence returns before merge.
- T3 — mutations on a production-live app; D367 byte-identity on two routes.
- T4 (narrow) — `/app` live branch changes + the TourGuide rewrite; returns with the `SampleWidget` deletion and `data-tour` anchor proven.
- T5 (narrow, ADDED) — `/app/explore` is production-live; returns on the `ExploreLive` diff + the page's live-branch read only (D406 rule).
- T6 — drive arm, proven-red run, seed leg.

## Tasks

### T0: land the ruled contract (MANAGER, not an agent — Fable carve-out, one file)
- status: approved (manager landed the D426 text verbatim; tsc clean; committed on s6-3)
- wave: 0
- depends: advisor kickoff
- owns: apps/web/src/lib/dashboard-types.ts (new)
- goal: the advisor's ruled contract text (D426) lands VERBATIM as the first commit on `s6-3`, so every wave-1 executor compiles against it and nobody edits it afterwards (A1: verbatim-or-escalate; edits return to the advisor).
- done-check: `npx tsc --noEmit -p apps/web` clean; file content = the ruled text.
- result:

### T1: Postgres persistence — `0009_dashboards.sql`, `server/dashboards.ts`, server actions (critical)
- status: approved (ADVISOR-SIGNED D439; condition discharged: rider D441 c4493bf re-reviewed approve — 7 mutation exports in D429 shapes, no pooled read path reachable from the action module, consumers all import `@/server/dashboards`, sabotage red once more; 811/0/0 with DSN). Review: fixed 89322f6 — `VALID_AGGS[type]` bare lookup let `type: "constructor"|"__proto__"|…` walk the prototype chain to a TypeError → 500 through a POSTable action (the D68 shape); fixed with `Object.hasOwn` + a five-name zero-statements guard. 810/0/0 with DSN; go ok incl. the :178 pin; DDL byte-matches D424; eight D430 sentences character-exact; stored JSON = nine contract keys. Red proofs: GET_SQL predicate dropped → 4 red; read-before-lock → 1 red; direction check after lock → 1 red; guard vs pre-fix → red. D440: delete-of-foreign-id silent no-op, authored sentences, LATERAL pin budget all ruled correct.
- wave: 1
- depends: T0
- owns: services/ingest/pgmigrations/0009_dashboards.sql (new), services/ingest/internal/pgmigrate/qa_a6_migrations_test.go (:178 pin only), apps/web/src/server/dashboards.ts (new), apps/web/src/server/dashboards.test.ts (new), apps/web/src/server/dashboards.integration.test.ts (new), apps/web/src/components/dashboards/actions.ts (new)
- goal: implement D424/D429/D430/D432/D437 VERBATIM — DDL = the D424 text (no `surface`, no partial index; `UNIQUE (workspace_id, name)`; duplicate name REFUSED never upserted); `server/dashboards.ts` (`import "server-only"`): reads `listDashboards(ws, query: QueryRows): Promise<Dashboard[]>` (`ORDER BY created_at, name`) and `getDashboard(ws, id, query: QueryRows): Promise<Dashboard | null>`; mutations `createDashboard(ws, name, tx)`, `renameDashboard(ws, id, name, tx)`, `deleteDashboard(ws, id, tx)`, `addWidget(ws, id, w: NewDashboardWidget, tx)`, `removeWidget(ws, id, widgetId, tx)`, `moveWidget(ws, id, widgetId, "up"|"down", tx)`, `setWidgetPinned(ws, id, widgetId, pinned, tx)` — each takes `tx: TxQuery` LAST, its FIRST statement is `lockWorkspace(tx, ws)`, every statement `WHERE workspace_id = $1`, answers with the fresh row read back (D429); ids `dash_<16 hex>`/`wdg_<16 hex>` via `randomBytes(8)` (D437); `class DashboardRefusal extends Error` for every D430 refusal with the exact sentences (shape refusals run ZERO statements; cap refusals — 12 widgets / 50 dashboards / 8 pinned — after lock+read, before any write; `23505` → "a dashboard named “{name}” already exists"); stored JSON carries EXACTLY the `DashboardWidget` keys (client `id`/`workspace_id`/extras dropped). `components/dashboards/actions.ts` (`"use server"`): `activeWorkspace()` verbatim from `saved-views/actions.ts:27-33`; one action per repo function; mutations wrapped in `withTransaction` (the `settings/actions.ts:224` idiom); catches ONLY `DashboardRefusal` → `{ refused }`; result shapes `DashboardActionResult` (delete → `Dashboard[] | null`); no rendering, no `@/mock/`. Pin `qa_a6_migrations_test.go:178` gains `,0009_dashboards`.
- done-check: `go test ./internal/pgmigrate/... ./cmd/ingest/...` in `services/ingest` green (compose Postgres, DSN per `go.yml`); `npm test --workspace apps/web` green incl. `dashboards.test.ts` (D432 hermetic: every SQL string names `workspace_id` and binds the scope argument as `$1`; the first statement of every mutation is the advisory lock; each D430 shape refusal runs zero statements; a payload's own `id`/`workspace_id` never reaches a binding; `moveWidget` past an end issues no write) and `dashboards.integration.test.ts` (D432: two workspaces, SAME dashboard name + SAME widget ids → fully disjoint on list/get/add/remove/move/pin/rename/delete; `getDashboard(wsB, aliceId)` → null; cascade on workspace delete; 13th widget / 51st dashboard / 9th pin / duplicate name refused with the exact sentences; mutation-proven: dropping the `$1` predicate in the fixture's own SELECT leaks).
- result:

### T2: widget rendering — `lib/widget-view.ts` (pure) + `components/dashboards/WidgetLive.tsx` (client, props-fed)
- status: approved (review: fixed — one caption per CARD on topn/table via pure `cardCaption(result, widget)` (card n = buckets with data anywhere, N = grid width; `last` → newest such bucket) + 2 exact-string tests; per-bar caption deleted; table gained its fold caption; dead `useMemo` removed. Verified: fold per agg, ties, `connectNulls` false, D381 caption, D436 sentences byte-match, `filters: {}`, pin order, per-slot isolation, zero mock/store/ExploreChart, `fmtVal` copied verbatim. Mutation proofs red both sides. 735/0/55 after fixes; commit 620fc3a. Hand-off to T3: `dashboards/page.test.ts` must extend the source-text bans to `WidgetLive.tsx`.)
- wave: 1
- depends: T0
- owns: apps/web/src/lib/widget-view.ts (new), apps/web/src/lib/widget-view.test.ts (new), apps/web/src/server/widget-results.ts (new, `server-only`), apps/web/src/server/widget-results.test.ts (new), apps/web/src/components/dashboards/WidgetLive.tsx (new)
- goal: implement D427/D428 VERBATIM — `lib/widget-view.ts` (pure): `widgetQuery(w: DashboardWidget): MetricSeriesQuery` (always `filters: {}`), `pinnedWidgets(dashboards: Dashboard[]): PinnedWidget[]` (dashboard list order, then widget index), `foldSeries(points, agg)` = ONE fold per agg (`sum`→sum, `min`→min, `max`→max, `last`→last non-null point, `avg`/`rate`/`p50`/`p90`/`p95`/`p99`→arithmetic mean of non-null values) returning value + `n` non-null of `N` buckets (+ `t` for `last`), `statView(result, widget)`, `groupRows(result, widget)` (one row per group ordered by folded value desc, ties by group name; carries `totalGroups`); `server/widget-results.ts`: `loadWidgetResults(ch: ScopedClickHouse, widgets: DashboardWidget[]): Promise<WidgetLoad[]>` — index-aligned `Promise.all`, a widget's throw → `{ok:false}` for that slot only; `WidgetLive({ widget, load, href? })` (client, recharts copied from `ExploreLive.tsx`, `fmtVal` copied from `:51-54`): timeseries = one line per series, `connectNulls` false, legend when >1; stat/topn/table with the fold caption EXACTLY `{sum|min|max|mean} · last {range} · {n} of {N} buckets had data` (for `last`: `latest · as of {t} · last {range}`); topn bar width ∝ max; table columns `group | value | buckets` (`n/N`); D381 caption `showing {series.length} of {totalGroups} groups` when `totalGroups > series.length`; empties `no points in the last {range}` (zero series or all-null), `couldn't load this widget` when `!load.ok`; `href` renders the title as a link. No fifth kind, no deltas, no sparklines, no `@/mock/`, no `workspace-store`, no `ExploreChart`.
- done-check: `npm test --workspace apps/web` green incl. `widget-view.test.ts` (fold per agg at the boundaries — min/max/last/mean/sum on a series with leading/trailing nulls; `last` picks the LAST non-null point not the last bucket; all-null → empty; `n of N` counts; ordering ties by group name; `totalGroups` passthrough; `pinnedWidgets` order = dashboard order then index; `widgetQuery` sends `filters: {}`) and `widget-results.test.ts` (fake `ch` throwing for widget 2 → slots 1,3 `ok:true`, slot 2 `ok:false`, length preserved); `npx tsc --noEmit -p apps/web` clean; source-text: `WidgetLive.tsx` has `"use client"`, zero `@/mock/`, zero `workspace-store`, zero `ExploreChart`.
- result:

### T3: the dashboards surface — D367 shape on both routes, live list/detail, editor, add-widget (critical)
- status: approved (ADVISOR-SIGNED D447, no condition) (review: fixed cae5638 — D431 bans matched only `@/`-aliased imports (relative evasions green) → resolved-specifier bans, three evasions red; a raw NUL byte in `AddWidgetLive.tsx:133` made the file binary to git/grep → `\u0000`; `aria-pressed` on the kind toggle. Verified mock-first, params unawaited, bodies ≡ master modulo export line, `/app/dashboards` DOM byte-identical, sentences byte-match, caps exact, seven mutation actions only. 646/0/74 DSN unset (Docker down). Finding 4 → advisor: the same alias-only hole in `explore/page.test.ts:68,103` + S6.2 page guards, no owner.)
- wave: 2
- depends: T1, T2
- owns: apps/web/src/app/app/dashboards/page.tsx, apps/web/src/app/app/dashboards/[id]/page.tsx, apps/web/src/app/app/dashboards/page.test.ts (new), apps/web/src/components/dashboards/DashboardsMock.tsx (new — today's list page body VERBATIM, `"use client"`), apps/web/src/components/dashboards/DashboardDetailMock.tsx (new — today's `[id]` body VERBATIM), apps/web/src/components/dashboards/DashboardsLive.tsx (new, server), apps/web/src/components/dashboards/DashboardDetailLive.tsx (new, server), apps/web/src/components/dashboards/DashboardEditor.tsx (new, client), apps/web/src/components/dashboards/AddWidgetLive.tsx (new, client)
- goal: implement D428/D431/D436 VERBATIM — both pages become SERVER components: `dashboards/page.tsx` `if (dataMode !== "live") return <DashboardsMock />;` then `await connection()`, session, `redirect("/login")`, `listDashboards(ws, queryRows)` → `<DashboardsLive dashboards />`; `dashboards/[id]/page.tsx` `if (dataMode !== "live") return <DashboardDetailMock params={params} />;` (the UNAWAITED promise; the mock body keeps its `use(params)`), then live: `getDashboard` + `listMetricCatalog(forWorkspace(ws))` + `loadWidgetResults` in parallel → `<DashboardDetailLive dashboard catalog loads />`. Mock files = today's two page bodies VERBATIM modulo the export line (`export function DashboardsMock()`, `export function DashboardDetailMock({ params })`), `"use client"` kept. `DashboardsLive` (server): list (name, widget count, updated), create form (name required; server action → redirect to the new id), the D402 cap sentence when 50, empty `no dashboards yet — create one, or save a chart from Explore`. `DashboardDetailLive` (server): `null` dashboard → `no dashboard with this id in your workspace` + link `← back to dashboards`; `no widgets yet — add one` when empty; one `WidgetLive` per widget from `loads[i]`; `DashboardEditor` (client: edit mode, remove, up, down, pin/unpin, rename, delete, add → `AddWidgetLive`) calling T1's actions then `router.refresh()`, printing `{refused}` sentences verbatim; `AddWidgetLive` (client) offers metric `(name,type)` from the `catalog` prop, `VALID_AGGS[type]` only, range from `METRIC_RANGES`, groupBy from that entry's `attrKeys` (forced null for stat, required for topn/table), title default `${metric} · ${agg}`, a pin checkbox; add button states "this dashboard is full (12 widgets)" at the cap. Live files import NO `@/mock/`, NO `workspace-store`, NO `WidgetCard`/`AddWidgetModal`; no "pinned to the overview" heading (D425: pins are per widget).
- done-check: `npm test --workspace apps/web` green incl. `dashboards/page.test.ts` (D431 source-text: both mock branches precede the first `await`; `DashboardsLive.tsx`/`DashboardDetailLive.tsx`/`DashboardEditor.tsx`/`AddWidgetLive.tsx` have zero `@/mock/`, zero `workspace-store`, zero `WidgetCard`/`AddWidgetModal`; the two Live server files have no `"use client"`; `DashboardsMock.tsx` ≡ `master:apps/web/src/app/app/dashboards/page.tsx` modulo the export line, likewise the detail mock); `npx tsc --noEmit -p apps/web` + `npx eslint src --max-warnings 0` clean; `npm run build` (mock) green with `/app/dashboards` and `/app/dashboards/[id]` unchanged in route class; byte-identity of both mock routes proven by the integrator's two-worktree DOM md5 (D438).
- result:

### T4: overview WatchWidgets live — `WatchWidgetsLive.tsx` from the workspace's `watch` dashboard (critical, narrow)
- status: approved (ADVISOR-SIGNED D445, no condition; D446: pin-cap refusal at the toggle discharges D425/D402, no overview sentence. Review: fixed f830146 — `data-tour` guard matched a doc comment (hollow guard) → pins the literal `<div data-tour="watches">`; dead flex wrapper folded. D442 copy byte-exact; mock branch gains no await; six red proofs. 646/0/74 DSN unset; 57 integration reds with DSN = Docker forwarder down → integrator gate.)
- wave: 2
- depends: T1, T2
- owns: apps/web/src/components/dash/WatchWidgetsLive.tsx (new, server, props-fed), apps/web/src/app/app/page.tsx (delete `SampleWidget` `:36-51`; the `:243-249` ternary; the live-branch read ONLY), apps/web/src/app/app/page.test.ts (new, source-text guard), apps/web/src/components/shell/TourGuide.tsx (the `/app` step `:29-34`, `target: "watches"` ONLY — D442 copy verbatim), apps/web/src/components/shell/shell-honesty.test.ts (`:236-247` only — D442 assertions; TourGuide.test.ts dropped from owns per D442)
- goal: implement D425/D434 VERBATIM — in the live branch `page.tsx` reads `listDashboards(ws, queryRows)` → `pinnedWidgets()` → `loadWidgetResults(forWorkspace(ws), …)` (inside the existing `Promise.all` where it fits) and the `:243-249` ternary becomes `live ? <WatchWidgetsLive pinned={pinned} loads={loads} /> : <WatchWidgets />`; `SampleWidget` (`:36-51`) is DELETED (its one consumer is gone; `SAMPLE_TITLE` stays for the header and the top-failing card — that table remains `sample={live}`, not this title); `WatchWidgetsLive` (server): root `<div data-tour="watches">`, header `pinned from your dashboards · {n}`, one `WidgetLive` per pinned widget with `href` to `/app/dashboards/{dashboardId}`, or `nothing pinned to the overview yet — pin a widget from any dashboard` + link `dashboards`. TourGuide `/app` step rewritten capability-only, true in both modes (D404): pin any dashboard widget to the overview; no "pod"/"queue"/"pipeline", no "kept in this browser"; the `TourGuide.test.ts` analogue bans those words from that step. Nothing else on the overview moves.
- done-check: `npm test --workspace apps/web` green incl. `app/app/page.test.ts` (source-text: the watch slot's mock branch renders `<WatchWidgets />` and the live branch renders `<WatchWidgetsLive`; `SampleWidget` no longer defined in `page.tsx`; `WatchWidgetsLive.tsx` has zero `@/mock/`, zero `workspace-store`, zero `"use client"`, contains `data-tour="watches"`) + the new `TourGuide.test.ts` case; existing overview/TourGuide tests green; `npx eslint src --max-warnings 0` clean; `/app` mock DOM byte-identical by the integrator (D438).
- result:

### T5: explore → dashboard in live — `SaveToDashboardLive.tsx` + `ExploreLive` button + page read
- status: approved (ADVISOR-SIGNED D443; condition discharged: rider D444 ccb40ef re-confirmed approve — sentence byte-exact, shown only when empty, pin red once more; mock/explore diff EMPTY); D443 ruled `router.refresh()` acceptable (review: fixed 76e3c96 — thrown action froze the modal → try/catch/finally; no `router.refresh()` after save → added; D433 payload unguarded → source-text pins on kind/pinned/title, each mutation 1 red. 719/0/78, tsc+eslint clean, mock/ diff EMPTY. ESCALATE → advisor: empty-dashboards sentence in the modal; refresh cost.)
- wave: 2
- depends: T1
- owns: apps/web/src/components/explore/SaveToDashboardLive.tsx (new, client), apps/web/src/components/explore/ExploreLive.tsx (add the button + modal wiring ONLY), apps/web/src/app/app/explore/page.tsx (live branch: `listDashboards` read → `dashboards` prop ONLY), apps/web/src/app/app/explore/page.test.ts (guard update)
- goal: implement D433 VERBATIM — `ExploreLive` shows "Save to dashboard" only when `query !== null`; `SaveToDashboardLive` (client) lists the `dashboards` prop (name, widget count, "full" at 12) + "new dashboard…" (name field → `createDashboard` action, then `addWidget`); the saved widget is `kind: "timeseries"` ALWAYS, `{metric, type, agg, range, groupBy}` = the current query, `pinned: false`, `title` = `${metric} · ${agg}` + (groupBy ? ` by ${groupBy}` : ""); `{refused}` prints in the modal verbatim; `{dashboard}` → toast "saved to {name}" linking `/app/dashboards/{id}` (toast pattern copied from `ExploreMock`, D391); `explore/page.tsx` live branch adds `listDashboards(session.workspaceId, queryRows)` to its `Promise.all` → `dashboards` prop; the `ExploreLive.tsx:41-44` "honest absence" comment is replaced by the truth. `ExploreMock`/`SaveToDashboardModal`/`mock/*` untouched.
- done-check: `npm test --workspace apps/web` green; `explore/page.test.ts` still bans `@/mock/`, `ExploreChart`, `workspace-store`, `SaveToDashboardModal` from `ExploreLive` and now REQUIRES the `SaveToDashboardLive` import (sabotage: remove the import → red); `npx tsc --noEmit -p apps/web` clean; `/app/explore` mock DOM byte-identical by the integrator (D438).
- result:

### T6: the flip + exit evidence — live-routes, seed leg, drive arm, README
- status: in-review (executor done bc83dc7: drive 225/0 in 137s, 211 pre-existing unchanged + 14 new; RED 218/7 = the new arm only; accepted 414→414; live-routes 12/12, sabotage red; volume intact, 0009 present; mock build + plans.free restored. Note: flight payload after `</main>` → slices bounded there)
- wave: 3
- depends: T1, T2, T3, T4, T5
- owns: apps/web/src/lib/live-routes.ts, apps/web/src/lib/live-routes.test.ts, deploy/compose/exit-seed.mjs (a `--leg dashboards` Postgres insert of alice's dashboard + watch row — D115 one seeding definition; the ONLY legal direct-SQL seed because no ingest path exists for dashboards, `LOWER_FREE_QUOTA_SQL` precedent), deploy/compose/e2e-drive.mjs (steps + no-session probe entries), deploy/compose/README.md ("What it asserts" in the drive's real order; seeder usage)
- goal: `/app/dashboards` + `/app/dashboards/` join `liveWiredRoutes` (test asserts both and that `/app/costs` is still unwired); the seed leg (`--leg dashboards`, direct Postgres INSERT into `dashboards` — the ONLY legal direct-SQL seed, no ingest path exists; ids per D437) inserts for alice ONE dashboard "Exit dashboard" with four widgets — one per kind — over the metrics leg's metrics (stat on the gauge with `agg: "last"` → value 42, `pinned: true`; timeseries on the gauge; topn + table on the sum, `agg: "sum"`, grouped by `service.name` → one group); NO watch row (D425). The drive proves on alice's session: `/app/dashboards` lists "Exit dashboard" with no "SAMPLE DATA"; `/app/dashboards/<id>` renders the four widget titles, the stat card reads "42" with `latest · as of`, the sum cards carry `sum · last` and `buckets had data`, no SAMPLE badge; `/app` renders `pinned from your dashboards · 1` + the stat title + "42" inside the `data-tour="watches"` slice and no `SAMPLE_TITLE` chip inside that slice (DOM-scoped, S6.2-L3); bob's session: the list shows `no dashboards yet — create one, or save a chart from Explore`, alice's id → `no dashboard with this id in your workspace`, the overview shows `nothing pinned to the overview yet`; the no-session probe covers `/app/dashboards` and `/app/dashboards/<id>` (307 → `/login`); `/app/costs` stays the badged control. Proven RED first by withholding the seed leg (expect exactly the new arm's claims to fail) + a reviewer-chosen sabotage (e.g. drop `/app/dashboards/` from the registry → the detail-page badge claim goes red).
- done-check: `node deploy/compose/e2e-drive.mjs` green locally with every pre-existing count unchanged (metrics accepted counters — the seed leg touches Postgres only, so `api_key_health` cannot move; state the measured before/after); red transcript captured in the scratchpad; `live-routes.test.ts` green; README lists the new steps in the drive's real order.
- result:

## Run-goal conditions (BINDING — advisor-confirmed 2026-09-01 with edits to 4, 9, 10, +11)
1. NO ClickHouse DDL; exactly ONE Postgres migration (`0009_dashboards.sql`), additive; a task believing it needs a second STOPS and escalates.
2. Every Postgres statement in the new module names `workspace_id` and binds it as `$1` from the scope argument (D7/D11/D113/D130); the house pair (hermetic SQL-text proof + two-workspace integration proof with the SAME names and ids) ships with the module; mutations are serialized under `withTransaction` + `lockWorkspace`.
3. Every metric read goes through `queryMetricSeries`/`listMetricCatalog` on `forWorkspace(session.workspaceId)` — no new ClickHouse SQL anywhere in this sprint.
4. D13 per widget: values are what the contract returned (null never 0); the fold is named on every stat/topn/table card (D427); fixture-only concepts (owner, deltas, pod/queue/pipeline watch types) ABSENT in live.
5. D367 shape on `/app/dashboards`, `/app/dashboards/[id]`, `/app`, `/app/explore` (D438); byte-identity of all four mock routes proven by the integrator's two-worktree `next build` DOM md5 (with the S6.2 chunk-name + `<!-- -->` normalisation).
6. D21 flip = exit, asserted in CI against `/app/costs`; live never falls back to mock.
7. Drive arm proven RED first + reviewer sabotage; pre-existing drive counts unchanged and stated.
8. S6.1-L1 harness posture (mock `next build` before `npm test` after a drive; `plans.free` restored).
9. **(D435) S6.3 close ships ONE Postgres migration inside the ingest binary.** Order at K0 staging and again at production: (1) ingest boot log shows `schema migrations applied store=postgres versions=["0009_dashboards"]` and `store=clickhouse` `schema already up to date` — ONLY the new version, proving the volume intact; (2) only then walk web: `/app/dashboards` and `/app/dashboards/x` 307 → `/login`, `/app/costs` still badged. Ingest MUST redeploy on the production push (the migration is in its binary) — the S6.1 watch-path observation makes it happen, but verify, do not assume; a web request before ingest boots is the transient to expect. Rollback = `git push --force-with-lease origin eb2ba23:production`; the table stays (additive, unread by old web).
10. Scope fence: no sharing/ACL (a dashboard is visible to every member of its workspace and to nobody else — that IS the ACL); no fifth chart kind; no `filters` stored (always `{}`); no dashboard-level range picker (range is per widget); the overview is a view over pinned widgets, never its own row (D425).
11. Every mutation's first statement is the advisory lock; refusals run zero statements (D429/D430), proven hermetically.

## Escalations log
<!-- Manager appends: {T#} — question → advisor decision (one line each). -->
- 2026-09-01 T1 return (advisor vs 89322f6) — **D439** T1 SIGNED-WITH-CONDITION (condition = rider D441 + T1 reviewer confirm; the D68 prototype-chain catch was the finding that mattered). **D440** foreign/unknown-id delete = silent no-op answering the caller's own fresh list — RULED CORRECT (a refused slot would let a caller probe other workspaces' ids by error text); executor-authored shape-refusal sentences join D430 as ruled text; workspace-wide pin budget confirmed. **D441** RIDER T1: `components/dashboards/actions.ts` exposes ONLY mutations — delete `listDashboards()`/`getDashboard()` (:56-64) + unused imports; `grep -c "export async function"` = 7; source-text pin in `dashboards.test.ts` that actions.ts has no `listDashboards`/`getDashboard` store call (sabotage red); owner T1 executor, re-review T1 reviewer; wave-2 impact none (pages read the store directly — an import of the deleted actions is the defect, not the deletion).
- 2026-09-01 T4 blocker + T5 return (advisor vs 76e3c96) — **D442** D434 AMENDED (executor's A1 stop correct): the watches step names NO storage location and NO one-mode mechanism; exact copy ruled (title "Your watches", body "The widgets you chose to keep in view, shown on the overview above everything else. Add one you care about and it stays; remove it and it is gone — the overview shows only what you picked."); `shell-honesty.test.ts:236-247` → (1) mock `WatchWidgets.tsx` still has `localStorage.setItem` (mock-only guard), (2) the step slice contains none of `this browser`/`your account`/`saved`/`synced`/`pod`/`queue`/`pipeline`, (3) contains `overview`; the TourGuide.test.ts analogue DROPPED (one honesty home); T4 owns amended accordingly. **D443** T5 SIGNED-WITH-CONDITION (rider D444); `router.refresh()` after a mutation = house pattern, accepted. **D444** RIDER T5: empty `dashboards` → `no dashboards yet — name one below` in the list slot (joins D436) + source-text pin; owner T5 executor, re-confirm T5 reviewer; no further return owed on T5.
- 2026-09-01 T4 return (advisor vs f830146) — **D445** T4 SIGNED, no condition; the 57 DSN-set integration reds are environmental and the INTEGRATOR's gate (must run green with the DSN before the merge verdict). **D446** the overview never truncates (8 is the most that can exist) → no D402 caption; `overview is full (8 pinned)` at the pin toggle discharges D425; no rider.
- 2026-09-01 T3 return (advisor vs cae5638) — **D447** T3 SIGNED, no condition (alias-only bans = hollow guards on a live surface, the catch that mattered; NUL-byte key = the class a grep-guarded sprint cannot tolerate; 76e3c96 index collision = retro note). **D448** RIDER REPO-WIDE NOW: every `app/app/**/page.test.ts` source-text ban matches the RESOLVED import specifier (alias AND relative) from ONE helper `apps/web/src/test-utils/import-specifiers.ts` (`resolvedImports(source, filePath)`, own unit test); seven guards (dashboards, explore, services, map, users, issues, traces/diff) import it; per file one relative-import evasion proven red; owner T5 executor, re-review T3 reviewer; diff confined to `*.test.ts` + `test-utils/`. → D448 LANDED f240f57 (helper + test + seven guards; 754/0/55; seven evasions red; one definition) — re-review FIXED a749479: side-effect `import "@/mock/x"` resolved to `[]` and passed every guard (a REGRESSION vs services' old text ban, measured green→caught) → third regex alternative + two unit cases; five red proofs incl. the side-effect form; 826/4/0 with DSN — the 4 = `plans.free` lowered by T6's in-flight drive (condition 8 chore), not the rider. D448 DISCHARGED.

## Integration
- verdict: pending
- rounds: 0
- e2e:
- seam findings:
- fix tasks:

## Goal check (Step 7, one entry per iteration)
- iteration 0:

## Retro (filled at end of run)
- tasks:
- breakdown corrections by advisor:
- reviewer verdicts:
- lesson:
