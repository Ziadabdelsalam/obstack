# S3.1 exit bundle + D137 — advisor return VERBATIM (2026-08-19)

- sprint: **S3.1 — Postgres arrives and the workspace becomes a real identity** (phase plan `.planning/2026-08-18-s3-m3-phase-plan.md` §3, D94; team plan `.planning/2026-08-18-s3.1-team-plan.md`)
- milestone: **M3 — Product Shell**
- status: **SIGNED by the advisor 2026-08-19 @ `0bea93a`.** Eight checks green at the signed tip. **PR #14 MERGED — `master` @ `9ba55bb`; the planning commit is on `master` @ `987ea1b`.**
- recorded by: project manager, verbatim from the advisor's return. Nothing below is paraphrased or added.

---

## D137 (the D107/D136 trigger ruling) — `e2e` runs EVERY-PR; the provisional wiring is confirmed

4m12s real sits inside the standing envelope (35s–5m10s, `stack` at the ceiling), and the job now carries both the M1 smoke floor and the tenancy spine — exactly the coverage that must not wait for a label. The ~3× local-to-runner gap landed as S2.4 L3 predicted, which is why the decision waited for this number. **Watch pre-registered, same shape as D41/D93: three consecutive `e2e` runs over the ~6-minute K5 line → the over-branch is decided then, not now.** `stack`'s existing watch continues unchanged (5m10s post-Postgres is inside its recorded range). K3 stands: both names are forever.

---

## S3.1 — Exit-evidence bundle (advisor-signed)

**Sprint:** S3.1 — Postgres arrives and the workspace becomes a real identity · **PR #14**, branch `s3.1-postgres-identity`, tip **`0bea93a`** · integrator FIXED-GO round 1 · iteration 0/3 · 16/16 task-slots approved (T1–T8, F1–F8 incl. reassignments) · rulings D111–D137 · goal MET clause-by-clause (Step 7 verdict of record).

**Exit clauses, each on evidence:**

1. **Postgres boots in compose** — cycled from `down -v`: `up -d --wait` exit 0 (12.2s), postgres healthy in the default profile, smoke PASS (18.0s) on the same stack; volumes exactly `clickhouse-data` + `postgres-data`. (Integration evidence §3.)
2. **Postgres in the M2 chart, extended in place** — virgin-kind `helm install --wait`: deployed, **both** migrate Jobs Complete on one install, both ingest replicas converged through the D123-accepted refusal race with D118-conformant naming; acceptance PASS 174s; D35 held (chart extended in place, `BETTER_AUTH_*` absent from `deploy/` by design per D112(b)). (§4.)
3. **A stranger signs up into their own auto-created workspace** — 79/79 drive claims, two fresh CDP profiles, two signups; post-drive Postgres: exactly 2 `workspaces` rows under 2 distinct orgs, zero `ws_demo` rows (D115); the D117 no-half-state property proven red-then-green end-to-end (T3 review §2).
4. **Every telemetry read is workspace-scoped, env constant dead** — `clickhouse.ts` exports exactly `forWorkspace` + `ScopedClickHouse`; four product pages on `dataForSession()`, four harnesses + bench on explicit-workspace entries with one `DEMO_WORKSPACE` definition; `OBSTACK_WORKSPACE_ID` → 0 hits repo-wide; tripwire proven red four ways; content-aware disjointness + in-drive negative probe (D135) green in the both-populated steady state. (§1 S4, T4/T8 records.)
5. **Saved views in Postgres, browser store deleted** — real `saved_views` row written through the product by the drive; `lib/saved-views.ts` + test deleted at the tip (verified staged); module path + `obstack.saved-views` key survive only in prose declaring the deletion; D30/D52 discard-never-read held; mock-mode absence is the ruled D125 exception.

**Checks at the signed tip (all green):** web 3m16s · go 2m34s · kind 1m39s · stack 5m10s · e2e 4m12s · sdk-py 35s · sdk-js 52s · sdk-e2e 3m46s. Correction of record: the every-PR set is **eight** checks (M2's seven + `e2e`), not the "nine" my condition line echoed. Commit hygiene verified: sprint pins only, float-free lockfile, `@vercel/blob` absent, T8's two deletions in the commit.

**SIGNATURE: S3.1 exit bundle SIGNED at `0bea93a` (2026-08-19).** Standing guards left armed: tripwire, cross-tenant probe, D120 allowlist matrix with live-ammunition falsifier, ddl-drift stack step (D122), D68 corpus, the smoke floor + drive in `e2e` every-PR (D137).

**S3.2 kickoff MUST inherit (binding list):**

- **D29 cap + `as_of` — BLOCKING: merges inside S3.2, before any second workspace's data can arrive** (D97 placement; the one hard ordering constraint).
- **D98 key move, whole:** prefix + deterministic SHA-256 storage, shown-once; ingest read-through cache, bounded TTL, fail-static, revocation ≤TTL; **`OBSTACK_API_KEYS` DELETED — no dual lookup path** — with the `ok_dev_local:ws_demo` continuity row seeded through the migration/seed path (compose `:48`, chart `values.yaml` apiKeys + collector apiKey literals all touched; S2.3 L1 sweep is a named done-check).
- **D120 allowlist widens door-by-door**, each new door carrying its stated transactional property; owner-pinned resolution inherited — accepting an invitation must be additive, never re-home (the measured tenancy break stays impossible). Invites are U4 in-app copyable links; the unused `invitation` table is schema of record.
- **D106:** `/signup` landing CTA goes live; settings registers under the per-section badge rule (General/Members/API keys real; Billing & usage SampleMark'd until S3.3; Audit log/Compliance through M3).
- **The `e2e` drive grows its S3.2 step in place** (one drive, one definition, D107); the full issued-key wire path remains pre-registered to S3.4 (D115).
- **Pattern inheritance:** D121/D127/D129 error-code vocabulary for every new form; D68 corpus on every new contract module's parse (D65 standing clause); ddl-drift guard catches any `authConfig()` change S3.2 makes.
- **Pre-registered residue (not S3.2 work):** S3.5 — switcher affordance + demo-workspace footer (D134), palette/changelog (D60/D63), TopBar residue; M4 — `BETTER_AUTH_URL` in the hosted env inventory (D119), `postgres.password` rotation story (D123), NoSessionError log-volume note (D132).
