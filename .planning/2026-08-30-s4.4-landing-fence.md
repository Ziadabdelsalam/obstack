# S4.4 — the public-surface claim enumeration (D257 critical item; S1 lesson 2)

- measured: 2026-08-30, read-only, master @ `893db97`. Surfaces walked: `apps/web/src/app/page.tsx` (555 lines) + its four imported components + four PNG shots + the hero MP4 (frames extracted); `layout.tsx` metadata; `/changelog`; `/status`; `/login`, `/signup` (mock branches — what a stranger sees on the D262 marketing host); `public/**`.
- absent (no claims to fence): `/pricing`, `/docs`, `not-found`, `error`, `robots.txt`, `sitemap`, `manifest`, OG image. No external URLs, logos, testimonials, customer counts, SOC2/SSO/99.9%-class claims anywhere (grepped).
- classes: **T** true · **F** false · **U** unverifiable (‡ = needs an advisor ruling) · **S** sample/fabricated content. Source = the deciding file:line.
- **Totals: 116 rows — TRUE 51 · FALSE 40 · UNVERIFIABLE 16 (14 ‡) · SAMPLE 9.** Landing alone 85 rows (35 T / 27 F / 15 U / 8 S). D257's four "floor" items account for 7 of the 40 FALSE rows — the fence is 5.7× the floor.

## Landing — `apps/web/src/app/page.tsx` + components

| # | file:line | claim | ⬥ | source of truth | sprint action |
|---|---|---|---|---|---|
|L1-4|`:56-59`|nav: Product / Connections / Pricing / Changelog|T|anchors `:195,:225,:394`; `/changelog` exists|keep|
|L5|`:55-59`|(gap) no Docs, no Status|—|D257|**add both**, after D255/D256 land|
|L6|`:62`|"Open the demo" → `/app`|T|`app/page.tsx`|keep (D262: cross-host URL)|
|L7|`:66`|"Create your workspace" → `/signup`|T|`signup/page.tsx`|keep; **must point at the live host** (mock build renders no form) — K13|
|L8|`:83-87`|"Every layer. One trace."|T|`0001_spans.sql:21` layer enum|keep|
|L9|`:90-101`|"whole story of a request — API, agents, LLM calls, infrastructure"|T|same|keep|
|L10|`:105`|"Open the **live** demo"|F|`:514` says sample data|"Open the demo"|
|L11|`:110`|"OTel-native · **one env var** to try it"|F|`connectors.ts:83` = 3 vars|"OTel-native · three env vars to try it"|
|L12|`:118-121`|signing up → own workspace, key in settings, traces land there|T|`0004_api_keys.sql`, D98|keep|
|L13|`:126`|"cloud is in private preview"|F|U1: no cloud, no preview|**delete (D257)**|
|L14|`:129-130`|"We don't host obstack for you yet"|T|U1|delete with block|
|L15|`WaitlistForm.tsx:56`|"Join the waitlist" (writes Vercel Blob)|F|`server/waitlist.ts:18` needs `BLOB_READ_WRITE_TOKEN`; the mock build has none → **every submit returns "failed"**|**delete (D257)**|
|L16|`WaitlistForm.tsx:30`|"We'll write when we host obstack for you."|U|no mail pipeline (U4)|delete with block|
|L17|`:137-144`|hero video, no alt/caption/track|S|`hero-sting.mp4` (untouched since `afb21b1`, Aug 16)|keep; a11y note|
|L18|video @9s|"trace a3f8c1d92b6e · POST /v1/tickets/{id}/reply"|S|fabricated|illustration; **loses its only label at `:547`**|
|L19|`:152`|"**a real failure**, joined: pod OOM-kill → …"|F|`HeroTrace.tsx:6` imports `mock/stories.ts:251`|"a sample failure, joined:"|
|L20-23|`HeroTrace.tsx:31,40,9-17,56,92-94`|trace id, 502 route, 3 log lines, pod name, "Explain: …"|S|`mock/stories.ts`|**needs its own SAMPLE label** once `:547` goes|
|L24|`:161`|"six tabs and a guess"|U‡|rhetoric|ruling|
|L25-27|`:167,:171,:175`|LLM tool / infra tool / kubectl cards|U‡|competitor claims, unmeasured|ruling (keep-as-positioning?)|
|L28|`:184-189`|"one trace…correlated by OTel trace context, container logs joined to the exact request"|T|`queries/traces.ts:299-320`|keep|
|L29|`:202`|unified waterfall incl. prompts + container logs|T|`0001_spans.sql:31-32`|keep|
|L30|`:207`|"Cloud, PaaS, Kubernetes, databases, LLM gateways, CI — every place…becomes a connection"|F|`connectors.ts`: 3 of 18 available|replacement 4|
|L31|`:212`|"Explain…reads correlated evidence…with receipts"|T|D229/D232, `explain/route.ts`|keep|
|L32|`:229`|"If it writes a log, it plugs in."|F|3 of 18|replacement 4|
|L33|`:232-245`|18 connector chips, 15 SOON|T|`connectors.ts` (pinned by `connectors.test.ts:118`)|keep|
|L34|`:248`|"one environment variable away"|F|3 vars|replacement 1|
|L35|`:257`|"A full observability platform — not just a trace viewer."|U‡|no metrics/alerts/SLOs/dashboards wired (`live-routes.ts:11-23`)|ruling|
|L36|`ScreensShowcase:8-9`|pipeline trace blurb|S|mock `pipelineTrace`|label as sample|
|L37|`ScreensShowcase:14-15`|"your own watches — pin any service, pod, model, or pipeline"|F|`/app/dashboards` not in `live-routes.ts`; changelog deleted the entry|replacement 5|
|L38|`ScreensShowcase:20-21`|"**Live tail** across every pod"|F|D48/D60; `changelog:31` "Nothing tails"; **the one allowlisted lie**, `TourGuide.test.ts:49`|replacement 6 — and empty the allowlist|
|L39|`ScreensShowcase:26-27`|"Every log source you run becomes a connection"|F|as L30|replacement 4|
|L40|`ScreensShowcase:62`|`alt="obstack — {label}"`|T|—|keep|
|L41|`shots/dashboard-widgets.png`|"ingesting · loopwork-prod · last 6h"|F|`shell-honesty.test.ts:30` bans exactly "ingesting"/region in TopBar|**re-shoot**|
|L42|same png|"ziad@loopwork.ai"|F|`shell-honesty.test.ts:46` + `corpus-honesty.test.ts:39` BANNED_IDENTITY|**re-shoot**|
|L43|same png|"YOUR WATCHES · 3" / "+ Add widget"|F|mock-only surface|**re-shoot**|
|L44|same png + logs png|sidebar shows Alerts / Ask / Evals|U‡|M6/M7, not products|ruling: re-shoot without them?|
|L45|`shots/logs-explorer.png`|"live tail · 200 shown"|F|D48 — **invisible to the D60 sweep (a PNG)**|**re-shoot**|
|L46|`shots/connections.png`|Vercel + AWS CloudWatch carry green **CONNECT**; "loopwork-web (production) · Vercel · degraded" listed as connected|F|`connectors.ts` both `coming-soon` (D101/D208)|**re-shoot**|
|L47|`shots/trace-pipeline.png`|"INFRA · PODS & K8S EVENTS" track|F|`k8sEvents` set only by `mock/generate.ts:69`; `server/adapters.ts` never sets it|**re-shoot**|
|L48|all 4 shots|"DEMO WORKSPACE — Sample data from a fictional AI support-agent company"|S|label baked into the image|keep|
|L49|`:267`|"The join is the product. You can't bolt it on."|U‡|positioning|ruling|
|L50|`:278-283`|column heads: Datadog · Grafana / Langfuse · Helicone|U‡|no measurement of any|ruling|
|L51|`:11` R1|"One trace across API, agents, LLM calls and pods" — yes|T|spans schema + `k8s_pod`|keep|
|L52|`:12` R2|"Prompts & completions inline" — yes|T|`0001_spans.sql:31-32`|keep|
|L53|`:13` R3|"Container logs joined to the exact request" — yes|T|`traces.ts:299`|keep|
|L54|`:14` R4|"Agent steps, tool calls & **retries** as first-class spans" — yes|F|layer enum has agent/tool; **no retry concept anywhere**|drop "& retries"|
|L55|`:15` R5|"Async queue hops inside the same trace" — yes|T|`kind` enum producer/consumer|keep|
|L56|`:16` R6|"Token cost attribution per request **& feature**" — yes|F|`cost_usd` per span/trace ✔; no feature dimension; `/app/costs` mock|"per request and model"|
|L57|`:17` R7|"**K8s events** on the trace timeline" — yes|F|mock-only (L47)|replacement 9|
|L58|`:22` R8|"Root-cause explanation from correlated evidence" — yes|T|D229/D232|keep|
|L59|`:24` R9|"**One env var** to try with existing OTel" — yes|F|3 vars|replacement 1|
|L60|`:288-297`|18 competitor cells (apm/llm marks)|U‡|unmeasured|ruling|
|L61|`:303`|"partial = possible with significant setup…"|T|legend|keep|
|L62|`:317`|Loki/Tempo/Mimir/Grafana names|T|factual|keep|
|L63|`:324-327`|4 assembled-stack bullets|U‡|competitor claims|ruling|
|L64|`:342`|"obstack — **one store**, one data model"|F|ClickHouse **and** Postgres|"one telemetry store, one data model"|
|L65|`:346`|"traces, logs, **k8s events** and cost share one trace_id by construction"|F|no events ingested|drop "k8s events"|
|L66|`:347`|"correlation is the default, not a config file"|T|`traces.ts`|keep|
|L67|`:348`|"prompts, agent steps, tokens and cost are first-class columns"|T|`0001_spans.sql:24-32`|keep|
|L68|`:349`|"one env var to try · **one container** to self-host"|F|3 vars; compose = ingest+web+ClickHouse+Postgres|replacements 1, 11|
|L69|`:353-357`|Grafana/LGTM paragraph|U‡|positioning|ruling|
|L70|`:370`|"Point your OTel at **us**"|F|U1: nothing hosted|"at your obstack"|
|L71|`:371`|"our **2-line** SDK for Python / TypeScript"|U|both READMEs say two lines ✔, but npm/PyPI hold a do-not-use `0.0.1` placeholder|gate on the S5 publish, or say "install from source"|
|L72|`:376`|"One DaemonSet or container ships Docker & Kubernetes logs with pod metadata"|T|`deploy/collector/config.yaml:105,152-158`|keep|
|L73|`:381`|"joined, searchable, explainable"|T|traces/logs/explain|keep|
|L74|`:406-410`|"Free and Pro are the plans the product carries today…Scale is what we intend to build"|T|`0005:128-129`|keep|
|L75|`:414-417`|Free: **$0 · 50k events/mo · 7-day retention · 20 Explain runs**|T|`0005_metering.sql:128-129`, `0006:19-22`, sweep D252|**keep + mirror test**|
|L76|`:419-424`|Pro: **$49 · 1M events · 30-day · 200 Explain**|T|same|**keep + mirror test**|
|L77|`:422`|"/mo **+ usage**"|U|meter exists (`billing/reporter.ts:54`); the per-unit price lives in Polar's catalog, not the repo|verify against Polar at S5|
|L78|`:427-431`|Scale: "Planned" + "not built yet" + 3 dashed items|T|**no Scale row in the seed** — correctly carries no numbers|keep|
|L79|`:431`|"Priority support"|U‡|no support commitment exists|ruling|
|L80|`:434-446`|Self-hosted: "Planned" price, "Self-host today — free", `helm install obstack deploy/helm/obstack`|T|`deploy/helm/obstack/` exists|keep (`connectors.ts:107` adds `--timeout 900s`)|
|L81|`:508`|"See your whole stack in one trace."|T|—|keep|
|L82|`:514`|"The demo runs on sample data — no signup, no setup."|T|D106|keep|
|L83|`:517-519`|"The hosted one is in **private preview**:"|F|nothing to preview|**delete with the waitlist**|
|L84|`:536`|"Open the **live** demo" (2nd)|F|as L10|"Open the demo"|
|L85|`:547`|"© 2026 obstack · prototype — all data on this site is fictional"|T|true today|**D257 removal — see §6**|

## `layout.tsx`, `/changelog`, `/status`, auth, app shell

| # | file:line | claim | ⬥ | source | action |
|---|---|---|---|---|---|
|M1|`layout.tsx:21`|title "obstack — every layer, one trace"|T|—|keep|
|M2|`layout.tsx:22-23`|meta description|T|—|keep|
|M3|—|no OG image / robots / sitemap / manifest|—|absent|S5 item, named|
|C1|`changelog:73`|"What shipped, when."|T|—|keep|
|C2-C5|`changelog:25,31,37,43`|Explain / Logs explorer ("Nothing tails") / traces carry logs / connections hub|T|D229, D48, D208|keep (migrate verbatim, D255)|
|C6|`changelog:24,28,34,40`|dates Aug 20 / Aug 8 / Aug 6 / Jul 28 2026|U|not pinned to any commit|check against git history|
|C7-C8|`changelog:66,104`|"Open the demo"; "© 2026 obstack"|T|—|keep|
|S1|`status:5`|"SLO-fed, incident history included"|F|no SLO product|**delete page (D256)**|
|S2|`status:51-52`|"Loopwork status · status.loopwork.ai · powered by obstack SLOs"|F|D256|delete|
|S3|`status:8-11`|99.97% / 99.89% / 99.99% / 100% uptime|F|fabricated|delete|
|S4-S5|`status:72,74`|"All systems operational"; "recovered from a partial degradation earlier today"|F|fabricated|delete|
|S6|`status:16-19`|90-day uptime strip from `mulberry32`|F|RNG|delete|
|S7-S8|`status:115-125`|INC-42 narrative; "sourced from obstack automatically"|F|fabricated|delete|
|S9|`status:131`|"demo page — what your customers would see, generated from your SLOs"|S|the page's only label, itself a capability claim|delete|
|A1-A3|`signup:44-53`|"Signup runs on your own obstack"; "prototype…fictional data…keeps no accounts"; "We don't host obstack for anyone yet"|T|`dataMode`|**keep — per-surface honesty label**|
|A4,A7|`signup:57`, `login:52`|"Open the **live** demo"|F|as L10|"Open the demo"|
|A5,A8|`signup:60`, `login:55`|"Join the cloud private preview" → `/#waitlist`|F|**dead link the moment D257 lands**|replace both|
|A6|`login:39-49`|same honest no-form state|T|D150|keep|
|A9|`signup:85-87`|"Telemetry sent with a key you issue…lands in that workspace and no other."|T|D98|keep|
|X1|`app/layout.tsx:89-93`|"DEMO WORKSPACE — every screen here is sample data…nothing is being ingested"|T|D134/D228|**keep — the app's own label**|
|X2|`SampleDataBadge.tsx:29`|"SAMPLE DATA — preview" (live mode, per route)|T|`live-routes.ts`|keep|
|X3|`app/slos/page.tsx:19`, `CommandPalette.tsx:33`|two links into `/status`|F|target is deleted|remove both with D256|

## 2. FALSE claims → proposed true replacements (the PM's proposal; the advisor rules)

1. **"one env var" ×4** (L11, L34, L59, L68) → "three env vars" / "one exporter setting" — `connectors.ts:83` needs `OTEL_EXPORTER_OTLP_ENDPOINT`, `_PROTOCOL`, `_HEADERS`.
2. **"Open the live demo" ×4** (L10, L84, A4, A7) → "Open the demo".
3. **L19** → "a sample failure, joined:" — it is `mock/stories.ts:251`.
4. **L30/L32/L39 connector breadth** → "Kubernetes, Docker and any OpenTelemetry source today — cloud, PaaS, databases, LLM gateways and CI are on the way." Headline "If it writes a log, it plugs in." → "If it speaks OTel, it plugs in today."
5. **L37** → "Requests, errors, latency and LLM cost for your workspace." (custom widgets are demo-only.)
6. **L38 "Live tail across every pod"** → "Search log bodies across every pod, each correlated line one click from its trace." Then **delete the `TourGuide.test.ts:49` allowlist entry** — the sweep goes clean.
7. **L54** → "Agent steps and tool calls as first-class spans" (drop "& retries").
8. **L56** → "Token cost attribution per request and model" (drop "& feature").
9. **L57 / L65** → drop the K8s-events row; replace with "Pod, node and container identity on every span and log line" (`0001_spans.sql:34-37`).
10. **L64** → "one telemetry store, one data model".
11. **L68** → "one compose file to self-host".
12. **L70** → "Point your OTel at your obstack".
13. **L13 / L15 / L16 / L83** — the waitlist block, both instances: delete (D257). It is also functionally broken on the D262 mock host (no blob token).
14. **A5 / A8** → "Open the demo" or the docs quickstart; not `/#waitlist`.
15. **L41-L43, L45-L47 — the four screenshots must be re-shot.** They date from `afb21b1` (Aug 16, pre-D48/D60/D208/D229/D246) and carry, in pixels, four claims the test suite bans in source. No sweep can read a PNG — S1 lesson 2 in its purest form.
16. **S1-S9** — `/status` deleted wholesale (D256); **X3**: remove the two in-app links.

## 3. Needs a ruling (positioning with no code behind it)

L24-L27 (the "six tabs" problem framing), L35 ("a full observability platform"), L44 (Alerts/Ask/Evals visible in marketing screenshots — M6/M7), L49 ("the join is the product"), L50/L60/L63/L69 (every Datadog/Grafana/Langfuse/Helicone cell and bullet — 18 table cells plus 4 bullets, none measured), L79 ("Priority support" on Scale). Two verifications, not rulings: L71 (the SDK claim is true in content but neither package is published — gate on S5) and L77 (Pro "+ usage" — the meter exists; the price lives in Polar).

## 4. Link / route audit

Every landing link resolves: `#product` `:195`, `#connections` `:225`, `#pricing` `:394`, `/changelog`, `/app`, `/signup`. No external URLs on any public surface. Zero `href="#"`. Gaps: no `/docs` or `/status` in the nav (D257 adds both); `/status` is reachable only from inside the demo (`/app/slos:19`, `CommandPalette:33`) and is being deleted; `/app/docs` is the mock surface D255 deletes. Live-wired vs mock: only `/app`, `/app/traces(/…)`, `/app/logs`, `/app/onboarding`, `/app/connections`, `/app/settings` read real data (`live-routes.ts:11-23`); on the D262 marketing host everything is mock and carries `DemoFooter`. **D262 cross-host wiring:** both "Create your workspace" CTAs (`:66`, `:526`) must point at the live app host, or a stranger lands on "there is no workspace to create here."

## 5. Tests — what pins this today, and the fence test S4.4 owes

**Today nothing reads `page.tsx`.** Indirect coverage only: `TourGuide.test.ts:49` allowlists `ScreensShowcase.tsx` (it *pins the lie in place*); `connectors.test.ts:118` pins the SOON chips. `signup/mock-mode.test.ts:97,115` asserts both auth pages link to `/#waitlist` — D257's removal turns it red; sequence the edit with it.

A `landing-fence.test.ts` in the `ingest-endpoint.test.ts` / D206 mirror shape (read the source as text — the page is not importable under `--conditions react-server`) must pin: (a) **the pricing mirror** — `$0/50,000/7/20` and `$49/1,000,000/30/200` against `0005_metering.sql:128-129` + `0006_explain_quota.sql:19-22` read from disk, and **no Scale numbers** since the seed has no Scale row; (b) **the banned-phrase sweep for the landing** — "one env var", "live demo", "live tail", "real failure", "k8s events", "one container to self-host", "private preview", "waitlist" — with an empty allowlist; (c) **the sample-label invariant** — if `page.tsx` imports from `@/mock/*` (it does, via `HeroTrace`), a SAMPLE marker must be present in the same file — the rule that replaces `:547`; (d) **the connector-breadth mirror** — the count of `status: "available"` in `connectors.ts` (3) against the breadth sentence; (e) **the link set** — every `href` in `page.tsx`/`login`/`signup` resolves to a route that exists (would have caught `/#waitlist` going dead); (f) **an image manifest** — each `shots/*.png` pinned by content hash, so a re-shot screenshot is a deliberate diff and a stale one cannot silently outlive a copy fix.

## 6. The `:547` finding (D257 item h)

`page.tsx:547` is today the **only** honesty label covering the hero trace (L19-L23), the four screenshots (L41-L48) and the video (L17-L18) — every one of them fabricated. The app's mock surfaces carry their own (`app/layout.tsx:89-93` `DemoFooter` on every `/app` screen; `SampleDataBadge` per route in live mode); `/login` + `/signup` carry theirs in prose (A1-A3, A6) — D21's per-surface grain holds inside the app. It does not hold on the landing. Removing `:547` without adding a per-surface sample label to the hero trace and the screens showcase leaves the landing's fabricated content unlabelled for the first time — the exact failure S1 lesson 2 names.
