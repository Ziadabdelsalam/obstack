# Team Plan — obstack M5 sprint 1 (S6.1): metrics end-to-end — OTLP metrics accepted (gRPC+HTTP), stored per the D363 packet, queryable through the frozen contract, explore live-wired; mock mode byte-identical

meta:
- date: 2026-09-01
- repo/branch: `s6-1` @ `a9f95f8` (cut from master)
- mode: waves
- team-cap: 10
- iteration-limit: 3 — fix rounds allowed before escalating to the user
- iteration: 0/3
- governing docs (binding, read before executing): `.planning/2026-08-31-s6-m5-full-platform-phase-plan.md` §5 S6.1 + §9 conditions; `.planning/2026-08-31-s6.1-metrics-ddl-packet.md` (the ruled design — implemented VERBATIM, deviations escalate to the advisor)

## Advisor decisions
<!-- The D363 packet IS the architecture decision set; entries below are breakdown-level rulings. -->
- A1: the DDL packet §0–§4 is binding verbatim; any executor who finds it false against the code/engine STOPS and reports (S4.4 L5 — deviations go in the ledger, never smoothed).
- A2: `apps/web/src/mock/explore.ts` is NOT edited in this sprint — `WidgetCard.tsx`/`AddWidgetModal.tsx` (dashboards, still mock) import it; the live explore path must not depend on it in live mode (dynamic-import/branch per the `data.ts` D114 idiom).
- A3: retro S1-L1 applied — T4 is the explicit WIRING task (receiver→mapping→batcher seam owner); no skeleton-without-consumer gap.
- **Kickoff verdict (2026-09-01): breakdown validated with corrections, rulings D366–D372 (applied to the task blocks below):**
- D366: contract TYPES live at `apps/web/src/lib/metrics-types.ts` (client-safe; T7's client components cannot import a `server-only` module for a type — S1.5/D10 precedent). `server/queries/metrics.ts` imports them. Packet §0 amended for location only.
- D367: explore restructure shape — `explore/page.tsx` becomes a SERVER component branching on `dataMode` (the `connections/page.tsx:36` idiom); the current 199-line client body moves VERBATIM to `components/explore/ExploreMock.tsx` ("use client", mock branch, byte-identity holds, `mock/explore.ts` unedited); the live branch renders a NEW `components/explore/ExploreLive.tsx` fed exclusively by T6's contract (types from D366); `ExploreChart.tsx` stays mock-only (it calls `exploreSeries` internally at :17 — reusing it drags the mock module into the live graph; the live chart is a props-fed sibling). **Save-to-dashboard is ABSENT in live mode** (not disabled-with-promises) — `SaveToDashboardModal` reads the mock workspace store; real dashboards are S6.3 (D13 honest absence).
- D368: the `Meter` gains `RecordAcceptedMetrics(workspaceID, keyID string, points int64)` — updates `health.accepted` + `lastEventAt` ONLY, never `m.ledger` (`RecordAccepted` at `metering.go:244-270` writes the ledger and is UNUSABLE for metrics — the D365 schema-level enforcement). `RecordDropped` gains `case DropCardinality`. The "quota cannot fire on metrics" proof lives in T4 (receive owns quota), not T3; T3 proves "RecordAcceptedMetrics writes zero ledger cells".
- D369: the engine-compat probe lives at `services/ingest/internal/migrate/metrics_probe_test.go` (house `requireClickHouse` pattern, `OBSTACK_TEST_CLICKHOUSE_DSN`); `migrations/` stays embed-only. D36 skip-trap regime applies — the `go` job's compose ClickHouse must actually run it.
- D370: T8 owns exactly `deploy/compose/e2e-drive.mjs`, `deploy/compose/exit-seed.mjs` (D115: the ONE seeding definition — metrics leg POSTs the three temperaments to the real `/v1/metrics` with the workspace's real key, proving T4's receiver; never direct ClickHouse inserts), + new `deploy/compose/metrics-checks.ts` (the `trace-checks.ts` pattern). `smoke.sh`/`smoke.ts` NOT owned — the smoke floor is M1's signed exit, frozen (D136); railway smoke is OUT (production metrics verification = the §9.5 deploy touchpoint).
- D371: T8 moves to wave 4, depends T4/T6/T7 — its done-check asserts the D21 flip, true only after T7's `live-routes.ts` edit. Stripping the badge assertion instead was rejected (would hollow the D21 proof out of CI). Waves: 3/3/1/1.
- D372: critical items amended to T1, T2, T6, T8 (S1 precedent: DDL / mapping / facade boundary / exit evidence).

- **Advisor rulings D386–D390 (2026-09-01, fresh advisor after the pause; full text in the packet tail):** D386 T8 SIGNED-WITH-CONDITION (condition = D388 doc rider before merge; claims-JSON pattern ratified; one agg per temperament confirmed). D387 D384 wording amended — landed unknown-pair behavior RULED CORRECT (E-T6b closed). D388 README doc rider on T8 (owner T8 executor, re-review T8 reviewer; README.md joins T8's owns for the rider; exit-seed.mjs:376-381 comment corrected). D389 E-T7obs(a) accepted as-is with the rule "every post-receipt drop reason joins sourceErrors, stays out of receiveErrors"; third drop class pre-registered. D390 E-T7obs(b) rider now: VALID_AGGS single definition in lib/metrics-types.ts (owner T7 executor, re-review T7 reviewer; done-check web suite + tsc, T6's invalid-agg test unchanged).

## Critical items
- T1 — the DDL migration (D363 gate): returns to the advisor with the engine-compat probe output attached, before merge.
- T2 — delta-normalization mapping: returns to the advisor with the contract tests (packet §5.4), before merge.
- T6 — the facade-boundary analogue (D372): frozen contract, never-FINAL/never-bare-SELECT rule, per-table tripwire proofs return to the advisor before merge.
- T8 — the exit-evidence analogue (D372): the drive's metrics arm + its proven-red run return to the advisor before merge.

## Tasks

### T1: ClickHouse metrics DDL (0005) + engine-compat probe
- status: approved (ADVISOR-SIGNED, D363 gate discharged; deviation stamped D374 — metric_series_mv GROUP BY includes type/unit/service, packet amended; no conditions)
- wave: 1
- depends: —
- owns: services/ingest/migrations/0005_metrics.sql, services/ingest/internal/migrate/metrics_probe_test.go (new, D369 — house `requireClickHouse` pattern, no compose changes)
- goal: migration `0005_metrics.sql` creates `metric_points`, `metric_points_1m`(+mv), `metric_points_1h`(+mv), `metric_series`(+mv) exactly per packet §1/§3/§4 (workspace_id-first ORDER BY, TTLs as ruled), idempotent like 0001–0004. The engine-compat probe (packet §5.1) proves on pinned `clickhouse-server:26.3.17.110`: `AggregateFunction(sumForEach, Array(UInt64))` + `sumForEachState/-Merge`, `SimpleAggregateFunction(anyLast, Map(LowCardinality(String),String))` in an AggregatingMergeTree, and the `last_seen`-column TTL. Probe failure = STOP + escalate with output, no substitution.
- done-check: `go test ./... -run Migration -count=1` (or the house migration-test target) green against compose ClickHouse; migration applies twice cleanly; probe output captured to a file named in the report.
- result:

### T2: mapping — series hash, delta normalization, MetricRow
- status: approved (ADVISOR-SIGNED via D380 condition, reviewer-confirmed independently. Two review rounds; catches that mattered: health-leg drop counts (dropped_cardinality would have read permanently zero), seeded-baseline lifetime-total spike (measured 1e7 on restart), fixture-date time bomb, wall-clock activity clock proven discriminating. Identity per D375/D378, pin 0x486361b149902e2e independently reconstructed.)
- wave: 1
- depends: —
- owns: services/ingest/internal/mapping/metrics.go (new), services/ingest/internal/mapping/metrics_test.go (new)
- goal: `MetricRows(workspaceID string, md pmetric.Metrics, cache *SeriesCache) []MetricRow` per packet §1: series_hash = sipHash64(name‖service‖sorted point-attr KVs) computed here (D8); cumulative→delta via the per-series cache (first observation emits no row; reset = value-as-delta; delta passthrough; cumulative-histogram bucket subtraction); gauges pass through; >64-attr points dropped whole (ReasonMapping); histogram explicit-bounds columns filled; exemplars ignored. The `SeriesCache` also answers the per-workspace active-series count for T4's cap.
- done-check: `go test ./internal/mapping/ -count=1` — MUST include, proven red first (S4.3 L4): reset-by-value-drop, reset-by-start_time-change, first-observation-no-row, cumulative-histogram subtraction, attr-limit drop.
- result:

### T3: drop accounting — ReasonCardinality + health columns (pg 0008)
- status: approved
- wave: 1
- depends: —
- owns: services/ingest/pgmigrations/0008_metrics_health.sql (new), services/ingest/internal/metrics/ (ReasonCardinality), services/ingest/internal/metering/ (DropCardinality + upsert columns) and their tests
- goal: `dropped_cardinality` column on `api_key_health` AND `api_key_health_windows` (D162 add-never-set upsert pattern); new drop reason `ReasonCardinality` riding the existing 5s flush with `RecordDropped` gaining `case DropCardinality`; the `Meter` implementation gains `RecordAcceptedMetrics(workspaceID, keyID string, points int64)` per D368 — health.accepted + lastEventAt ONLY, never the ledger (`RecordAccepted` at metering.go:244-270 is unusable for metrics).
- done-check: `go test ./internal/metering/ ./internal/metrics/ -count=1` — including a test proving `RecordAcceptedMetrics` writes ZERO `usage_ledger` cells; pgmigration applies idempotently.
- result: approved (must-fix→fixed r1). Reviewer findings that mattered: (1) pgmigrate pinned applied-set constant missed 0008 (out-of-scope, routed back, one line); (2) windows-upsert positional args had zero runtime coverage — all-BIGINT columns fail silently on a swap; reviewer added the windows-row assertion, proven red by swapping quota/cardinality params. Zero-ledger proof runs against real Postgres (PASS, not SKIP). Scope note: SignalMetrics constant → widened into T4's owns.

### T4: the wiring task — receivers + third batcher + cardinality cap
- status: approved (must-fix→fixed r1 + routed seam test + reviewer's uniqExact fix. Catches that mattered: direction-blind boot-Seed test (stubbed seed still passed), server-TZ today() vs UTC window, missing auth parity on both metrics transports, no-op third-batcher close coverage, bare count() on AMT reading 4-not-3 across flush ticks (measured). D383 stamped: accepted and drops are separate axes.)
- wave: 2
- depends: T1, T2, T3
- owns: services/ingest/internal/receive/grpc.go, services/ingest/internal/receive/http.go, services/ingest/internal/receive/receive.go, services/ingest/internal/receive/metrics_test.go (new), services/ingest/internal/write/write.go, services/ingest/internal/write/write_test.go, services/ingest/internal/write/integration_test.go, services/ingest/internal/metrics/metrics.go (T3 reviewer scope note: add `SignalMetrics` for the Accepted counter's signal label — T3 lands before wave 2, no conflict)
- goal: `pmetricotlp.RegisterGRPCServer` beside traces/logs (grpc.go:33-34 pattern); `POST /v1/metrics` beside `/v1/traces` (http.go:57 block verbatim — the `payload` interface is satisfied by `pmetricotlp.ExportRequest`); BOTH interface edges are T4-owned per the kickoff: `receive.go`'s `Consumer` (line 43) gains `ConsumeMetrics(ctx, workspaceID string, md pmetric.Metrics)` and the `Meter` interface (line 57) gains `RecordAcceptedMetrics` (D368 signature — T3 ships the implementation against it); `ConsumeMetrics` on the Writer with `newBatcher("metric_points", …)` (same D5 bounds) and `Writer.Close` flushing it; cap plumbing per D376 (replaces the earlier cap wording): plumb the 25k cap from config into `NewSeriesCache(cap)`, call `SeriesCache.Seed(workspaceID, hashes)` at boot with the active-window set from `metric_series` (last_seen within current UTC day), and meter `MetricRows`' returned cardinality-drop counts: add `dropCardinality = dropReason{metrics.ReasonCardinality, metering.DropCardinality}` beside the existing dropReasons (receive.go:331-333) and call `countDrop` with the returned count (T2 reviewer's seam analysis — `MetricRows` returns `(rows, cardinalityDrops int)`; mapping fires NO cardinality metric itself) — admission itself lives in mapping (T2), T4 plumbs and meters, never decides. `cmd/ingest` wiring is inherited (the Writer is the Consumer) — no other file touched.
- done-check: `go test ./internal/receive/ ./internal/write/ -count=1` against compose — an integration test POSTs real OTLP metrics (gauge, cumulative sum, cumulative histogram) and asserts rows in `metric_points` AND the 1m rollup AND `metric_series`; a cap test proves drop+count past a lowered test cap and no drop for established series; the D368 quota proof: the metrics consume path never consults `OverQuota` and `ReasonQuota` never increments on a metrics POST from an over-quota workspace.
- result:

### T5: retention sweep joins metrics tables
- status: approved (review: fixed — direction-blind metrics sweep test made discriminating via surviving-names assertion, re-proven red; brittle positional plans INSERT replaced with in-place UPDATE exercising the D163 COALESCE path; stale three-table comments swept. Off-by-one direction verified benign (over-retention). first_seen semantics stamp with advisor, non-blocking)
- wave: 2
- depends: T1
- owns: services/ingest/internal/retention/retention.go, services/ingest/internal/retention/retention_test.go
- goal: `metric_points_1m`, `metric_points_1h`, `metric_series` join the per-plan sweep in place (packet §4; keyed on `max_seen_date`/`last_seen`); raw `metric_points` is NOT swept (3-day table TTL only, stated in a comment).
- done-check: `go test ./internal/retention/ -count=1` — sweep deletes past-retention metric rows for a short-retention plan and leaves in-retention rows; raw table untouched.
- result:

### T6: web query module — the frozen contract
- status: approved (ADVISOR-SIGNED via D381 condition, reviewer-confirmed. D384 rider CONFIRMED by the same reviewer 2026-09-01: unknown (name,type) under groupBy:null = one all-null series/totalGroups:1 (honest empty, the f844a3a baseline was never an error); type filter mutation-proven (dropping it read 21 = avgMerge across both types); 3 doc/dead-import fixes committed. Two rounds; catches that mattered: quantile undefined-through-null on empty-bounds histograms; ILLEGAL_AGGREGATION workaround re-measured (alias shadowing only — killed a test asserting its own assumption); nondeterministic dual-type resolution; sum/histogram temperaments previously unproven on the real engine. D381 MetricSeriesResult landed with genuine pre-truncation proof; D382 ceiling pre-registered.)
- wave: 2
- depends: T1
- owns: apps/web/src/lib/metrics-types.ts (new, D366 — the contract types, client-safe), apps/web/src/server/queries/metrics.ts (new, imports the types), apps/web/src/server/queries/metrics.test.ts (new), apps/web/src/server/queries/metrics.integration.test.ts (new, if the house integration pattern applies; seeding inserts raw rows into `metric_points` directly — MVs fire on INSERT, no receiver dependency)
- goal: implement packet §0 VERBATIM — `listMetricCatalog` (from `metric_series`, never raw), `queryMetricSeries` (from `metric_points_1m` with `-Merge` combinators, GROUP BY series key + bucket, never FINAL, never bare SELECT — the trace_summaries house rule); bucket widths 1h→1m/6h→5m/24h→15m; groupBy top-10 with total count; null for empty buckets (never 0); invalid agg-for-type = request error; histogram quantile helper as ONE function unit-tested against a known distribution with exact expected quantiles (packet §5.3). Every function takes `ScopedClickHouse` (D11). D378 amendment: `listMetricCatalog` groups by `(name, type)` — dual-emission renders as two honest entries, never an anyLast(type) pick.
- done-check: `npm test --workspace apps/web -- metrics` green, quantile helper proven against the known distribution, an unscoped-SQL tripwire proven red per new table (phase plan §9.3).
- result:

### T7: explore live wiring + honest cap statement
- status: approved (review r2: fixed — byte-identity RE-PROVEN by two-worktree `next build` (DOM-only /app/explore md5 8f517f80… identical at 5442de2 and bd26c93; only chunk ids/RSC module table differ = the D367 shape); D384 verified on real data (?type switches the answer; (name,type) keys selection/sidebar/URL, name-only fallback kept for old deep links); D381 conditional `totalGroups > 10`; cap banner from `count(DISTINCT series_hash)` never estimated; D385 parity red-proven BOTH sides (TS 24000≠25000, Go 26000 → expected 26000 = parsed Go source); live-graph guard red-proven (ExploreChart import → red). Reviewer fix: series-cap.test.ts's restated `25_000` deleted (a third place to edit the cap). Hand-off to T8: e2e-drive.mjs:1991-1993 asserts the old hub footnote "errors are receive-path only" — T7 changed it to "receive-path or cardinality-cap drops" (ConnectionsHub.tsx:330); README.md:261 fixed by the manager. Observations for the advisor: (a) settings 'receive-path errors' (ingest-health.ts:364 receiveErrors excludes droppedCardinality) vs hub 'errs' (includes it) — two meanings of errors on two surfaces, labels differ so neither lies; (b) agg-validity table exists 3× (metrics.ts:141 authority, page.tsx:20, ExploreLive.tsx:88) — MetricAgg union bounds it. Known: unknown-pair/all-null renders a blank chart with axes, no range-level empty sentence — packet §2 doesn't ask for one.)
- wave: 3
- depends: T4, T6
- owns: apps/web/src/app/app/explore/page.tsx, apps/web/src/components/explore/ExploreMock.tsx (new — the current 199-line client body moved VERBATIM), apps/web/src/components/explore/ExploreLive.tsx (new — props-fed live chart sibling), apps/web/src/components/explore/ExploreChart.tsx, apps/web/src/components/explore/SaveToDashboardModal.tsx, apps/web/src/app/app/explore/ (any new server wrapper/route files), apps/web/src/lib/live-routes.ts, apps/web/src/server/ingest-health.ts (droppedCardinality column read), apps/web/src/components/connections/ConnectionsHub.tsx (sourceErrors classification, line 89), apps/web/src/app/app/connections/page.tsx (field copy), and the explore/live-routes tests
- goal: implement the D367 shape exactly — `explore/page.tsx` becomes a server component branching on `dataMode` (the `connections/page.tsx:36` idiom); mock branch renders `ExploreMock.tsx` (verbatim move — rendered DOM identical, byte-identity holds); live branch renders `ExploreLive.tsx` fed exclusively by T6's contract, typed via `lib/metrics-types.ts` (D366); `ExploreChart.tsx` stays mock-only; save-to-dashboard ABSENT in live mode (D367/D13 — renders only in the mock branch until S6.3); `/app/explore` added to `liveWiredRoutes`; the cap banner per packet §2 ("Series limit reached (25,000)…", numbers from health rows + `metric_series`, never estimated); connections hub classifies `dropped_cardinality` in `sourceErrors`. `mock/explore.ts` untouched (A2 — dashboards still import it).
- done-check: full web suite green (`npm test --workspace apps/web`); the mock-mode byte-identity/route-table checks pass; live explore against compose renders a real series from T4's integration data.
- result:

### T8: drive/e2e metrics leg
- status: approved (ADVISOR-SIGNED via D386; D388 doc rider landed and reviewer-fixed: metrics bullet moved to the honest position after Explain (drive order 2467/2549 is after the Data & ingest counters at 2425), the argv/ps sentence separated from the hygiene-sweep claim (the sweep reads outputs, never `ps`), the re-run sentence corrected to the stream-restart/reset rule. Reviewer-reported README omissions outside D388 (no bullets for plan-change 2571, identity census 2742, clean-log 2849, D165 vectors 2274, drops-both-places 2339, one-usage banner 2391, Data & ingest tab 2425; stale 'this sprint's two routes' at README:265/drive:1973) → recorded for the integrator's doc pass or a later sprint, not blocking.) Executor: exit-seed.mjs `--leg metrics` POSTs gauge/cumulative-sum/cumulative-histogram to the real /v1/metrics with alice's Quickstart key via env (never argv, never a CH insert); metrics-checks.ts asserts through listMetricCatalog/queryMetricSeries + forWorkspace (no SQL): catalog (name,type)/unit/attrKeys, 60×1m grid, exact value in the expected bucket, null-never-0, totalGroups (D381), groupBy service.name = one group, bob's catalog absence (D142); drive step at :2467 + explore D21 step at :2549 (deep link carries type=, D384; h1 = the sent name; no SAMPLE badge; no cap banner; /app/costs control kept). Placement MEASURED after the last exact accepted assertion (407→414 = +7 points, D368). Green 185/0 → reviewer 186/0; RED with the POST withheld = 4 downstream failures (3 catalog + explore h1), never the seeder's own status; reviewer sabotage (gauge 43 vs expected 42) flips the series value claim + exit code red with catalog still green. Reviewer fix: /app/explore added to the 'every wired route refuses no session' probe (:2820). Hand-off from T7 applied (:1992/:1994). Hygiene sweep readdirSync(OUT) covers the 3 new artifacts (12 outputs, 0 token hits).
- wave: 4
- depends: T4, T6, T7
- owns: deploy/compose/e2e-drive.mjs, deploy/compose/exit-seed.mjs, deploy/compose/metrics-checks.ts (new, the `trace-checks.ts` pattern) — per D370; smoke.sh/smoke.ts and deploy/railway/smoke.ts are NOT owned (smoke floor frozen D136; railway verification = the §9.5 deploy touchpoint)
- goal: the metrics leg lives in `exit-seed.mjs` (D115: the one seeding definition) — POSTs the three temperaments (gauge, cumulative sum, cumulative histogram) to the real `/v1/metrics` with the workspace's real key, proving T4's receiver (never direct ClickHouse inserts); `e2e-drive.mjs` grows one step asserting the query contract answers (catalog lists the metric; a series query returns the ingested points at 1m buckets) via `metrics-checks.ts`; proves the D21 flip (explore live = no SAMPLE badge; an unwired route still badged).
- done-check: the e2e job green locally via its house command; the new arm proven red first by withholding the metrics POST (S2.0 L1 — no hollow guards).
- result:

## Escalations log
- E-T1 (2026-09-01) — engine 26.3.17.110 rejects `SimpleAggregateFunction(anyLast, Map(LowCardinality(String), String))` (anyLast returns Map(String,String), wrapper dropped; probe 2/3 PASS otherwise) → **D373**: rollup `attributes` becomes `SimpleAggregateFunction(anyLast, Map(String, String))` in all three rollup objects; raw table unchanged; MV SELECTs unchanged (implicit LC→String coercion); T6 contract unaffected; re-probe hardened to assert `toTypeName(fn(col))` = declared storage for EVERY SimpleAggregateFunction column. Packet amended. Wave 2 unblocks on T1's green hardened re-probe; T1 still returns to the advisor.
- E-T1b (2026-09-01) — reviewer escalation: metric_series_mv GROUP BY carries type/unit/service beyond the packet §3 sketch → **D374 STAMPED**: SQL-complete form is the ruled text (columns are functions of series_hash; cardinality unchanged; the sketch was shorthand). T1 SIGNED, no conditions; packet amended.
- E-T2 (2026-09-01) — four reviewer design escalations, all ACCEPTED as packet defects → **D375** identity = OTLP stream identity (resource attrs + type join the hash; MVs store mapUpdate-merged labels; 0005 edit rides T1's evidence machinery, gate not reopened), **D376** cap admission inside SeriesCache (rejected series allocates NO entry; Seed() boot reconciliation; T4 plumbs+meters only), **D377** lastSeen activity window, >24h eviction (~≤2× cap memory; silent-24h cumulative re-enters as first-observation — accepted cost), **D378** catalog keys on (name, type). T2 returns once more (revised hash + re-proven pin + multi-resource regression + eviction tests); T4 unheld, dispatches when T2's ruled API lands in tree (compile dependency). False-alarm note: T2's reviewer saw 0005:71 pre-D373 mid-review — that was T1's reviewer's red-flip proof window; tree+commit verified D373-correct.

- E-T6b (2026-09-01) — T6 reviewer, wording defect in D384: "an unknown (name, type) pair is the same request error as an unknown name" mislabels its baseline — an unknown name was never a request error in this module (only invalid agg-for-type is, per §0), and with `resolveType` deleted no error can be raised without reinstating the catalog round trip D384 removes. Behavior confirmed as one all-null ungrouped series, totalGroups:1. → **D387**: wording amended in the packet; landed behavior ruled correct. CLOSED.

- E-T8 (2026-09-01) — T8 reviewer: D370's file set omits `deploy/compose/README.md` — its "What it asserts, in order" list (:204-250), artifact list (:200-202) and seeder usage block (:340-352) now omit the metrics arm, `--leg metrics`/`SEED_METRICS_TOKEN`, and the three new artifacts. T8 stayed out (A1). → **D388**: doc rider on T8 (README.md + exit-seed.mjs:376-381 comment), the D386 sign-off condition.
- E-T7obs (2026-09-01) — T7 reviewer observations for the advisor: (a) settings "receive-path errors" (ingest-health.ts:364 `receiveErrors` excludes droppedCardinality) vs hub per-key "errs" (includes it) — two meanings of "errors" on two surfaces; labels differ so neither lies; (b) the agg-validity table exists 3× (metrics.ts:141 authority, explore/page.tsx:20, ExploreLive.tsx:88), bounded by the MetricAgg union. → **D389** (a) accepted as-is + vocabulary rule; **D390** (b) rider now — VALID_AGGS single definition in lib/metrics-types.ts.

## Integration
- verdict: **GO** (round 1, no fix round needed — zero integration slips found, working tree untouched)
- rounds: 1
- e2e (all at HEAD `1b0e518`, compose clickhouse+postgres+ingest up; full evidence: `/Volumes/SSD/dev-tools/claude-tmp/claude-501/-Volumes-SSD-Workspace-observer-stack/c5ad68e7-ed18-4e26-a4f8-bca16734822f/scratchpad/integ/INTEGRATOR-EVIDENCE.md`):
  - go vet `./...` exit 0. `go test` with the go.yml:105-112 DSNs verbatim: 12/12 packages ok, 343 PASS, **0 `--- SKIP`** — the D369 probe RAN (`TestEngineProbe_SumForEachArray`, `_LastSeenColumnTTL`, `_SimpleAggregateFunctionTypesMatchDDL` incl. the D373 `anyLast Map(String,String)` subtest) and so did every metrics integration test (writer→MV→series, cap, boot Seed, retention sweep, zero-ledger vs real Postgres, both transports).
  - web: **644/644, 0 fail, 0 skipped** (the 47 Postgres skips ran); `tsc --noEmit` clean; `eslint src --max-warnings 0` clean; `npm run build` (mock) exit 0 with `/app/explore` still ○ Static, the `connections/page.tsx` sibling class.
  - drive: `node deploy/compose/e2e-drive.mjs` run ONCE at HEAD → **186 passed, 0 failed in 139s** — identical to the T8 reviewer's run, so the D388/D390 riders are behaviorally inert as claimed.
  - FALSE ALARM resolved, not a defect: the first web run was 640/4, all four reading `plans.free` at `explain_quota=2 / event_quota=300` — the values an earlier e2e-drive run wrote and, per its own comment at `e2e-drive.mjs:2094-2101`, never undoes. Restoring the two cells to what `0005_metering.sql:129`/`0006_explain_quota.sql:20` declare gave 644/644. Pre-existing since S3.5; CI unaffected (separate stacks per job).
- seam findings (9 walked, all green):
  1. T1 DDL ↔ T2 `MetricRow` ↔ T4 writer ↔ T6 reader: `write.go:60-63`'s 17 columns are 0005's raw-table list name- and order-for-order, `batch.Append` is field-for-field `MetricRow`, both MVs and `metric_series_mv` SELECT those names, and `metrics.ts:164-192` merges exactly the rollup's own column names. Proven on real receiver-written data by the drive (3 temperaments, 60×1m grids, 42/60/p90 36, nulls elsewhere); integrator spot-check of what the drive does NOT touch — gauge `min`/`max`/`last` = 42 (exercises `gauge_last`'s `AggregateFunction(argMax,…)`), sum `rate` = 1 (60 delta / 60s, exact), histogram `p50` = 20 and `p99` = 39.6 (exercises `bounds` + `AggregateFunction(sumForEach, Array(UInt64))`), all arithmetically correct.
  2. D373/D375 `attributes`: a filter on the RESOURCE-borne key `service.name=zzalice-svc` matched and `=nope` did not — the `mapUpdate` merge proven discriminating through the filter path the drive never exercises (it only proves groupBy).
  3. T3 pg 0008 ↔ T4 `RecordDropped(DropCardinality)` ↔ T7 `ingest-health.ts` ↔ hub (D389): the middle link had no coverage against real Postgres, so the integrator set `api_key_health.dropped_cardinality = 7` on a live row and read through `getIngestHealth` → `droppedCardinality: 7`, `receiveErrors: 1` (EXCLUDES it), `sourceErrors: 8` (INCLUDES it). Both halves of D389 hold and the read is discriminating, not permanently-zero. Row restored to 0. (`WINDOW_SQL` reads only `sum(accepted)` and never read any drop column — 0008's windows column is write-side completeness, not a read seam.)
  4. T5 ↔ T1 names: `retention.go:135-137` sweeps `metric_points_1m`/`_1h` on `max_seen_date` and `metric_series` on `last_seen`; raw `metric_points` deliberately absent (3-day TTL). No 5m/15m rollups exist — the 6h/24h widths are query-time `toStartOfInterval` over 1m (`metrics.ts:179`).
  5. D385 parity: the test's `../../../../../../services/ingest/internal/write/write.go` resolves to the real file, whose `:40` is `const DefaultSeriesCap = 25_000`; test ran and passed; exactly one TS restatement of the number.
  6. Mock byte-identity (D367/A2): `git diff master..s6-1 -- apps/web/src/mock/` EMPTY; `ExploreMock.tsx` vs `master:explore/page.tsx` differs in exactly one line (`70c70`, the function name) and nothing else. A2 enforced by `explore/page.test.ts:65-91` banning `@/mock/`, `ExploreChart`, `workspace-store` and `SaveToDashboardModal` from `ExploreLive`; `page.tsx`'s static mock import beside the `dataMode` branch is `connections/page.tsx`'s ratified shape.
  7. D21: `liveWiredRoutes` and the drive's unauth probe list (`e2e-drive.mjs:2812-2825`) are the same 8-route set (the `/app/traces/` subtree entry covered by the detail path) — exact parity, all green, plus the no-badge/`/app/costs`-control pair.
  8. T8 ↔ T6/T4 (D370/D115): `metrics-checks.ts` reaches the store only via `listMetricCatalog`/`queryMetricSeries` + `forWorkspace` (zero SQL); `exit-seed.mjs:542-562` POSTs the real `/v1/metrics` with the token from the environment; hygiene found it in 0 of 12 outputs.
  9. Pin sets + D390: `qa_a6_migrations_test.go:178` includes `0008_metrics_health`, `migrate_test.go:40` pins `0005_metrics.sql: 7`; `VALID_AGGS` is one definition (`metrics-types.ts:36`) with three importers (server enforcement, deep-link guard, client vocabulary) and no fourth copy.
- fix tasks: none — no integration slip found, nothing edited, tree still clean at `1b0e518`.
- non-blocking, carried forward (already logged under T8, NOT escalated): `README.md:265` / `e2e-drive.mjs:1973` still say "this sprint's two routes", stale since `/app/explore` made it three (editorial; changing the drive string costs a re-run); and the drive's permanent `plans.free` lowering makes a local `npm test` after a local drive show the 4 failures above until fresh volumes — pre-existing S3.5 posture, out of S6.1 scope.

## Goal check (Step 7, one entry per iteration)
- iteration 0 (2026-09-01, HEAD 1b0e518, integrator GO): **GOAL MET** — measured against the sprint title, clause by clause. (1) OTLP metrics accepted gRPC+HTTP: T4's receiver on both transports with auth parity, proven by Go integration tests (343 PASS, 0 SKIP) and by the drive's front-door POST with a real workspace key (e2e-drive.mjs:2467). (2) Stored per the D363 packet: 0005 DDL engine-probed on 26.3.17.110 (D369/D373), 17 raw columns writer↔DDL order-for-order, MVs to 1m/1h + metric_series, delta normalization + cap admission (D375–D380), retention joins the three swept objects. (3) Queryable through the frozen contract: T6's `listMetricCatalog`/`queryMetricSeries` on `metric_series`/`metric_points_1m` with -Merge, never FINAL, never bare SELECT, tripwires red-proven; D381 totalGroups, D384 required type, D390 one VALID_AGGS; the drive's `metrics-checks.ts` gets exact bucket values for all three temperaments (42 / 60 / p90 36) through that contract only. (4) Explore live-wired: server page branching on dataMode, ExploreLive on the contract, `/app/explore` in liveWiredRoutes with the D21 flip asserted in CI against the `/app/costs` badged control; honest cap statement from `count(DISTINCT series_hash)` with D385 cross-language parity. (5) Mock mode byte-identical: `mock/` diff vs master EMPTY; ExploreMock = master's page body modulo one line; two-worktree prerender DOM md5 identical. Nothing narrowed, nothing deferred that the title promised; save-to-dashboard is an honest absence in live mode per D367/D13 (S6.3).

## Retro (filled at end of run)
- tasks: 8/8 approved; 4/4 critical items advisor-signed (T1 D374, T2 D380, T6 D381, T8 D386); integrator GO round 1 with zero slips; riders: T4×1 (D385 comment), T6×1 (D384), T7×2 (D384/D385 r2, D390), T8×1 (D388).
- breakdown corrections by advisor: kickoff D366–D372 (7); in-run D373–D390 (18) — the packet took 18 amendments, every one a reviewer or executor finding the packet false against the engine or the code (D373 engine type, D375 identity, D376–D377 cap/eviction, D378 catalog key, D381 totalGroups, D383 axes, D384 required type, D387 wording). The packet was binding verbatim (A1) and that is exactly why the defects surfaced as escalations instead of being smoothed.
- reviewer verdicts: T1 fixed→signed; T2 two rounds (four design escalations); T3 approved; T4 must-fix→fixed; T5 fixed; T6 two rounds + rider confirm; T7 r1 fixed → r2 fixed (byte-identity re-proven by two-worktree build; D385 red-proven both sides) → D390 rider approved; T8 fixed (reviewer's own value sabotage) → D388 doc rider fixed. Every reviewer produced at least one mutation/red proof rather than reading alone.
- lesson: (S6.1-L1) a test-mode `.next` is shared state — the drive's live-mode production build silently turned the mock-reading landing-fence/doc-links tests red for the next agent (3 'pre-existing' failures that were environmental); the drive's `plans.free` lowering does the same to Postgres-reading tests. Both are pre-existing harness posture; record: run `npm run build` (mock) before `npm test` after any drive run, and note that CI never sees either. (S6.1-L2) the D384 'request error' wording shows a ruling can be self-contradictory with its own clause (`resolveType` deleted ⇒ no lookup ⇒ no error) — reviewers reading rulings literally caught it; keep rulings in terms of observable results, not mechanisms they delete. (S6.1-L3) ordering constraints in a shared drive (exact accepted-count assertions before the metrics POST) must be MEASURED (407→414) not reasoned; the executor did, and the placement comment carries the number.

## SHIP LOG — S6.1 CLOSED (2026-09-01)
- PR #29 `s6-1` → `master` merged by the user (`2d181d4`); all 10 CI checks green (e2e 6m04s with the metrics arm, stack 11m27s, go, web, lint, images, kind, sdk-js/py/e2e).
- **K0 staging gate** (auto-deploy of master, Wait-for-CI): ingest `2925abd5` + web `8c92195a` SUCCESS 13:55–13:57; ingest boot applied `clickhouse ["0005_metrics"]` + `postgres ["0008_metrics_health"]` (10:56:39Z); 0 errors; `/v1/metrics` 401 no-auth and bogus-key (parity with `/v1/traces`); `/app/explore` → `/login`.
- **§9.5 deploy touchpoint — DONE, user-confirmed** ("push to production"): `git push origin 2d181d4:production` (2310697 → 2d181d4, 40 commits). Production deployments SUCCESS 13:59–14:01: ingest `e99c6c58`, web `a096058a`, marketing `08a12da8`, clickhouse `ba28a02f`. Ingest boot 11:00:26Z: `schema migrations applied store=clickhouse versions=["0005_metrics"]`, `store=postgres versions=["0008_metrics_health"]` — ONLY the new versions, so 0001–0004 / pg 0001–0007 persisted: both volumes intact (the S5 wipe precedent would have shown every version re-applied). ClickHouse restarted on its existing volume (`vol_94aw63oqef38vwfp` mounted). 0 `[ERROR]`/panic in ingest, none in web. Public: `ingest.obstack.dev/v1/metrics` 401 (was 404 pre-promotion) no-auth + bogus key; `/v1/traces` 401; `app.obstack.dev/app/explore` and `/app/costs` → `/login`; `/login` 200; `obstack.dev` 200; `status.obstack.dev` 200.
- NOT run (needs operator credentials, offered to the user): the authed §8 smoke leg — a real metrics POST with a production key and `/app/explore` rendering a series under a session cookie.
- **Deploy-doc observation, carried to the next deploy sprint (not edited now):** the `clickhouse` service REBUILT on the production push although nothing under its README §1 watch paths changed between 2310697 and 2d181d4 (only `deploy/railway/README.md`). Either the watch-path lists are not applied on the production environment or a branch fast-forward bypasses them — verify against the dashboard before relying on "unchanged watch paths ⇒ no rebuild" for a volume-backed service. Harmless this time (volume reused, proven above).

## RESUME POINT (S6.1 complete; next = M5 S6.2 planning per the phase plan)
- master `2d181d4` = production. Carried forward: README.md:265 / e2e-drive.mjs:1973 "this sprint's two routes" stale; README "What it asserts" omits several drive steps (under T8); D382/D385/D389 pre-registrations; the clickhouse watch-path observation above; local harness posture S6.1-L1 (mock `next build` before `npm test` after a drive run; `plans.free` lowered by the drive).
