# Team Plan — obstack M3 sprint 4 — onboarding to first trace and a functional Connections hub (exit: a brand-new workspace follows the quickstart with its own key and the "waiting for data…" screen flips to its own first correlated trace, and the Connections hub shows live source health for the ruled connector set)

meta:
- date: 2026-08-20
- repo/branch: `s3.4-onboarding-connections` (from `master` @ `2458093`)
- mode: workflow (execute→review pipeline via Workflow tool; advisor, escalations, fix rounds, integration in the manager session)
- team-cap: 10
- models: executor=opus (standing user ruling — never Sonnet) reviewer=opus advisor=fable
- iteration-limit: 3 — fix rounds allowed before escalating to the user
- iteration: 0/3
- sprint: S3.4 · milestone M3 · dependencies S3.1 (CLOSED), S3.2 (CLOSED), S3.3 (CLOSED incl. QA round)
- governing record: phase plan `.planning/2026-08-18-s3-m3-phase-plan.md` §3 S3.C4 + §9 D94–D110 (esp. D100, D101, D106, D107); S3.3 exit bundle `.planning/2026-08-19-s3.3-exit-evidence.md` §6 ("S3.4 kickoff MUST inherit" — BINDING) + its QA addendum (D195–D200); sprint records D111–D137, D138–D160, D161–D194, D195–D200; `.ziarmy/retro.md`
- kickoff: advisor rulings **D201–D213** recorded below, 2026-08-20. Verdict: **CORRECTED AND APPROVED** — breakdown corrected (T7 recut D213; Q5 premise falsified D205; four new seams ruled D209–D212). Waves: W1 = T1, T4 · W2 = T2, T3 · W3 = T5, T7 · W4 = T6.

## Sprint exit criterion (verbatim from the phase plan, S3.C4 → S3.B3's exit)

The quickstart renders the workspace's real key and install lines that name the packages that exist (`obstack-py` / `obstack-js`, D79) at the frozen D78 API; the waiting panel polls real ingest and links the workspace's actual first trace; the ruled connector set (D101: OTLP, Docker, Kubernetes) opens real guided connect flows; connected sources show last event / rate / errors from real counters; both surfaces are registered live-wired with real empty states.

## Scope fence

No billing work, no Explain, no identity changes (S3.1/S3.2 own those models). No Vercel or CloudWatch receiving components — both cards flip to the honest "Coming soon" re-label (D101; M4 builds the receivers, S5 activates them). No second counter path: connections health reads the D100 `api_key_health` rows only (S2.3 L3). No `ai@7` integration — S3.4 documents the measured fence (`ai >=5 <7`) and states `ai@7` unsupported (D101/D88; the integration is a pre-registered M4-PLANNING item). No SDK API change — D78 is FROZEN; a breaking change escalates. Honesty-pass residue stays S3.5 (palette/changelog D60/D63, TopBar residue, switcher/demo-footer D134, landing sentence + pricing copy D106). No email (U4). No retention enforcement (M4). No hosted endpoint — the environment is local compose (U1); quickstart/connector copy renders the environment's real endpoint per D101.

## Binding inheritance (S3.3 exit bundle §6 → S3.4 kickoff)

1. **e2e headroom:** 5m35s real, ~25s under the ~6-minute line; S3.4's step growth must land inside it — D137 trip protocol governs; the pre-named reclaim candidate is the ~35s flush+TTL propagation wait (drive-time TTL/flush override, decided on measured numbers).
2. **D115 (standing):** the issued-key wire drive step — the S3.4 drive sends on a UI-issued key and proves ATTRIBUTION (token issued in the product lands its telemetry in its own workspace and no other). `e2e-drive.mjs:96-100` names this as deliberately not taken until now.
3. **Quickstart truth per D101, all four named defects:** package names (`obstack` → `obstack-py`/`obstack-js`), the `api_key=` argument the frozen D78 API does not take (both SDKs are env-only, zero-arg `init()`), the fake 5-second arrival (`Quickstart.tsx:58-62`), and the `x-obstack-key` header (`Quickstart.tsx:47`) contradicting the D4/D6 wire contract (`Authorization: Bearer`). **Byte-level quickstart-vs-D78 review is a pre-registered critical item.**
4. **Connections-hub health reads the D100 `api_key_health` rows** — mechanism exists and is drive-proven (`apps/web/src/server/ingest-health.ts:291` `getIngestHealth`); S3.4 consumes it, never a second counter path.
5. **The reconcile-wording↔D132-predicate coupling** — resolved or restated at this breakdown (see kickoff question Q6).
6. **Pattern inheritance standing:** D121/D127/D129 forms, D68 corpora, D130 integration class, D11 posture, D142 content-aware evidence bar, D150/D152 mock-mode guard + standing test (`apps/web/src/app/signup/mock-mode.test.ts`) on every new auth-surface server action, D189 reads-are-reads/PRG for any new return-URL surface, D193 log-level grading, D198/D199 `TxQuery` brand for any lock-dependent write.
7. **One-definition contracts with pinned cross-language vectors:** any amendment sweeps every implementer in the same round.
8. **Plan catalog is the one quota/retention definition (D163)** — any S3.4 surface reads `plans`, never a constant.
9. **M4/S5 pre-registration carry** unchanged (D178/D187, D180, D153/D119/D123/D132 inventory, S5-GATE production-Polar list incl. D200's grown webhook-reachability consequence).

## Measured facts the breakdown stands on (survey 2026-08-20, HEAD `2458093`)

- **Quickstart today:** `apps/web/src/components/onboarding/Quickstart.tsx` — module-level `tabs` (:11-49) rendering `pip install obstack` / `npm install obstack` / `obstack.init(api_key=…)` / `init({ apiKey: … })` / `x-obstack-key` (:47) / `https://ingest.obstack.dev` (:46,:141); fake arrival `setTimeout(…, 5000)` (:58-62); mock `allTraces` first-trace link (:6,:56,:150). Route `/app/onboarding` renders only this component.
- **The real D78 API (both SDKs env-only, zero-arg):**
  - Python: package **`obstack-py`**, import **`obstack`**; `import obstack` / `obstack.init()` **above every other import**; env `OTEL_SERVICE_NAME`, `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf`, `OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer%20<token>` (URL-encoded — a raw space drops the header); `init()` defaults `OTEL_SEMCONV_STABILITY_OPT_IN=http`.
  - JS: package **`obstack-js`**; `import { init } from "obstack-js"; init();` before instrumented libs; same env vars; does NOT set `OTEL_SEMCONV_STABILITY_OPT_IN` (deliberate — README warns not to copy the Python line).
  - Version fences (measured, `packages/obstack-js/package.json:40-44`): `openai >=4.85 <8`, `@anthropic-ai/sdk >=0.50 <1`, **`ai >=5 <7`** (`ai@7` emits no OTel span — the D88 fence to document).
  - Neither package's real release is published; registry holds U5 name-hold `0.0.1` placeholders. In-repo install today is from source/pack (`packages/obstack-*/README.md`).
- **Key issuance:** `apps/web/src/server/api-keys.ts` — `listApiKeys` (:165) returns `{id,name,prefix,createdAt,revokedAt}` — **no token; a stored token is unrecoverable by design (D98)**. `issueApiKey` (:181) returns `{token, key}` once. The one value-returning server action is `issueKey` (`app/app/settings/actions.ts:104-120`) behind `settingsSession` (:81-89, the D150/D152 guard).
- **First-data signal:** no "has data arrived" function exists. `getIngestHealth(workspaceId, query)` (`server/ingest-health.ts:291`) is the cheapest honest signal (`accepted > 0` / `lastEventAt !== null`, Postgres-side, workspace-keyed, staleness = 5s flush + display "as of"). First-trace link candidates: `queryTraceSearch` (`server/queries/traces.ts:370`, window-bounded) via the D96/D113 scoped facade.
- **Connections hub today:** `ConnectionsHub.tsx:5` imports `{categories, connectedSources, connectors}` from `@/mock/connectors` — 5 "available" cards (otlp, kubernetes, docker, **vercel, aws-cloudwatch**) + 14 coming-soon; 3 fabricated `connectedSources`; `ConnectModal` static steps with the fake key, `charts.obstack.dev`, `obstack/collector:latest`, `cf.obstack.dev/forwarder.yaml` — **none of which exist**; a hardcoded never-updating "waiting for first event…" row; "Request this connector" sets local state only.
- **Real signed artifacts the guided flows point at:** compose `ingest` OTLP at `127.0.0.1:4317` (gRPC) / `127.0.0.1:4318` (HTTP); collector distro `deploy/collector/` (config.yaml, config.compose.yaml, up.sh, README) on image `otel/opentelemetry-collector-k8s:0.158.0`; Helm chart in-repo `deploy/helm/obstack/` with `templates/collector/daemonset.yaml` (values `collector.apiKey`, `collector.storageDir`).
- **Route registry:** `apps/web/src/lib/live-routes.ts` — `/app/onboarding` and `/app/connections` NOT registered. **The e2e drive uses an unwired route as its SAMPLE-badge positive control (`e2e-drive.mjs:988`)** — registration must not silently vacate that control.
- **e2e drive:** 2090 lines; metering steps end :1795 ("Data & ingest tab"); billing :1814; the ~35s propagation wait = 30s poll on `usage_ledger` + `STATE_TTL_MS`-anchored sleep (:1634-1647); `FLUSH_MS=5_000`/`STATE_TTL_MS=30_000` (:202-203) mirror Go constants. D115 header :96-100. D132 rider: log-cleanliness allowlists exactly the NoSessionError line on unauthenticated-probe steps only; S3.3's integrator recorded that `reconcile.ts`'s refusal wording is now load-bearing for that predicate (neither file names the other).
- **Mock-mode:** `dataMode` (`server/data.ts:52-75`); guard shape at `settingsSession` + siblings; standing test `apps/web/src/app/signup/mock-mode.test.ts` (settings-action enumeration at :188 — QA carry G1 notes it is hand-enumerated). `SAMPLE_TABS` now exactly Audit log + Compliance.
- **Nav touchpoints:** SideNav :62 Connections / :76 Quickstart; TopBar :128; CommandPalette :36,:40; TourGuide :95-97 (`data-tour="connections"` anchor at `ConnectionsHub.tsx:56`); `components/dash/OnboardingChecklist.tsx:8` hardcodes `done: true` for "Connect a source" on the live-wired `/app` dashboard.

## Draft task breakdown (PM draft — returns to the advisor before dispatch)

- **T1 — onboarding server layer: real key + first-data signal + first-trace link.**
  owns: `apps/web/src/app/app/onboarding/page.tsx`, new `apps/web/src/app/app/onboarding/actions.ts`, new `apps/web/src/server/onboarding.ts` (+ tests), extension of `apps/web/src/app/signup/mock-mode.test.ts`.
  Server component resolves session → workspace, lists keys (prefix display), exposes an onboarding-scoped issue-key action (wrapping `issueApiKey`, D150/D152 guard + standing-test entry, shown-once semantics identical to settings), and a first-data read: `getIngestHealth` for arrived/not + a first-trace resolver through the scoped facade. Mock mode renders exactly today's behavior (D125 class).
  depends: —. Wave 1.
- **T2 — Quickstart truth rework (client).**
  owns: `apps/web/src/components/onboarding/Quickstart.tsx` (+ component tests).
  All four D101 defects closed: held package names + real install lines, zero-arg `init()` env-var snippets (the two SDKs' documented env blocks verbatim, incl. the URL-encoded Bearer header and the Python-only SEMCONV line), `Authorization: Bearer` in the OTel tab, the environment's real endpoint, fake timer deleted — the waiting panel is prop/poll-fed from T1's signal and links the workspace's actual first trace. Documents the `ai >=5 <7` fence + `ai@7` unsupported (D88). **Byte-level quickstart-vs-D78 review = the critical item.**
  depends: T1. Wave 2.
- **T3 — connections live data + hub wiring.**
  owns: `apps/web/src/components/connections/ConnectionsHub.tsx`, `ConnectModal.tsx`, `apps/web/src/app/app/connections/page.tsx` (+ tests).
  Live mode: connected sources = the workspace's keys with D100 health (last event / rate derived / errors = dropped counts) via `getIngestHealth` — no second path; real empty state ("no sources yet"); "as of" staleness shown; modal's waiting row polls the same signal or is removed as fiction. Mock mode keeps today's render. Tour anchor preserved.
  depends: T1 (shares the health read shape). Wave 2.
- **T4 — connector card truth.**
  owns: `apps/web/src/mock/connectors.ts` split or a new live connectors definition (advisor: where the live card copy lives), category list.
  OTLP/Docker/K8s `connectSteps` rewritten over the real signed artifacts (compose OTLP endpoint, `deploy/collector/` distro + `up.sh`, in-repo Helm chart + `otel/opentelemetry-collector-k8s:0.158.0`; real issued key placeholder wired from T1/T3's surface, never `ok_live_9f2e…`); **Vercel + `aws-cloudwatch` re-labelled "Coming soon" (D101)**; fabricated `connectedSources` leave the live path.
  depends: —. Wave 1 (content) with T3 consuming. Advisor may merge into T3.
- **T5 — registration + shell truth.**
  owns: `apps/web/src/lib/live-routes.ts` (+ test), `components/dash/OnboardingChecklist.tsx`, drive positive-control relocation coordination (with T6).
  `/app/onboarding` + `/app/connections` register live-wired (D106 registry rule); the drive's SAMPLE positive control moves to a route that stays unwired; `OnboardingChecklist`'s hardcoded `done: true` becomes real or leaves the live path (it renders on live-wired `/app`).
  depends: T2, T3. Wave 3.
- **T6 — e2e drive growth (D115 + onboarding flip + connections health) + D132 coupling.**
  owns: `deploy/compose/e2e-drive.mjs`, `.github/workflows/e2e.yml` if budget work requires.
  New steps: alice opens `/app/onboarding`, issues a key in the UI, the drive sends OTLP with `Authorization: Bearer <that token>` → asserts attribution (alice's workspace and not bob's — content-aware, D142), the waiting panel flips to her real first trace, `/app/connections` shows that key's health, and both routes carry no SAMPLE badge. Budget: fits the ~25s headroom or executes the pre-named propagation-wait reclaim; first-real-runner numbers reported to the advisor (D137). Resolves the reconcile-wording↔D132 coupling per kickoff ruling.
  depends: T1–T5. Wave 4.
- **T7 — docs truth.**
  owns: `packages/obstack-py/README.md` + `packages/obstack-js/README.md` (fence wording only if needed), `deploy/compose/README.md` runbook lines for the new drive steps, any quickstart-adjacent doc claims.
  depends: T2. Wave 3. Advisor may fold into T2/T6.

Waves (draft): W1 = T1, T4 · W2 = T2, T3 · W3 = T5, T7 · W4 = T6.

## Advisor kickoff rulings (2026-08-20 — BINDING, verbatim)

### D201 (Q1) — Quickstart issues its own key inline: option (a)
The exit says "renders the workspace's real key" and the snippet must carry a pasteable `Authorization=Bearer%20<token>`; a prefix cannot be pasted, and D98 makes an existing token unrecoverable — issuance at the surface is the only honest source. Shape, binding: new guarded server action in `app/app/onboarding/actions.ts` wrapping `issueApiKey` — `settingsSession`-class guard (D150/D152), entry added to the standing mock-mode test, default key name `"Quickstart"` through `parseKeyName`, value-returning `{token}` exactly like settings `issueKey`. **Token lifetime: client component state only** — never persisted, never in a URL/query/sessionStorage, gone on unmount. Before issuance the snippets render a literal `<OBSTACK_API_KEY>` placeholder plus the issue affordance. `settings/actions.ts:94`'s "the ONE action that answers with a value" comment is now false; T1 amends it in the same round.

### D202 (Q2) — Install lines: the in-repo install that works, naming the held names; one stated caveat line
The registry `0.0.1` placeholders install a package marked do-not-use — rendering that line is the S2.2 L1 lie. The quickstart renders the SDK READMEs' documented install verbatim (`pip install './packages/obstack-py[fastapi]'`; `npm pack` + `npm install ./obstack-js-<version>.tgz`), which names `obstack-py`/`obstack-js` — satisfying the exit's wording — plus exactly one caveat: registry names are reserved (U5), real releases publish at launch. Not both forms as tabs; no dual copy. The tgz version literal is checked against `package.json` at the byte-level review.

### D203 (Q3) — Arrival = D100 health; first-trace = scoped facade; poll = session-scoped GET route handler at 5s cadence
`getIngestHealth` is the ONE counter path and the arrival predicate (`lastEventAt !== null`); the first-trace link comes from `queryTraceSearch` through the D96/D113 facade — the link resolver, not a second arrival signal. If health says arrived but the trace is not yet queryable, the panel stays waiting. Transport: new GET route handler `app/app/onboarding/status/route.ts` returning `OnboardingStatus` (D209); session-scoped, unauthenticated → 401/redirect, mock mode → 404 with D193-graded log line. Client polls every 5s (matches `FLUSH_MS`), stops on arrival. `router.refresh()` REFUSED. GET is a read is a read (D189).

### D204 (Q4) — Live connector definitions live in new `apps/web/src/components/connections/connectors.ts` — the ONE card/category/steps definition for BOTH modes
Live surfaces importing `@/mock/*` is the D125/D158 class; two card-copy definitions WILL diverge (S2.3 L3). `@/mock/connectors` shrinks to the fabricated `connectedSources` only (sample data, D208). Step snippets carry a `<OBSTACK_API_KEY>` placeholder slot — no fake `ok_live_9f2e…` anywhere in the live module.

### D205 (Q5) — Premise falsified; no relocation
The positive control at `e2e-drive.mjs:988` is `/app/costs`, not in S3.4 scope, stays unwired. T5's relocation item is DELETED; T6 keeps the negative assertions (no SAMPLE badge on the two new routes) and the `/app/costs` positive control untouched.

### D206 (Q6) — Coupling RESOLVED structurally: mirrored predicate + pinned test, both sides owned by T6
Measured sharpening: the drive allowlists by the `NoSessionError` name token (`e2e-drive.mjs:2067`); the load-bearing fact is that `reconcile.ts`'s refusal wordings (`:158,165,169,174`) must never match the drive's `isError` regex (`/Error\b|⨯|unhandledRejection/`, `:2057`) on an authenticated path. Resolution: mirror the `isError` regex as a named constant in a pinned unit test beside `reconcile.ts` asserting every refusal wording does NOT match it, comments naming the source in both files (the `FLUSH_MS` mirror pattern). Convention-only cross-reference REFUSED (S3.3 QA lesson).

### D207 (Q7) — Reclaim HELD, pre-authorized as the contingent first response
T6 implements without the override; the first real-runner number returns to the advisor (D137). If it trips the ~6-minute line, the propagation-wait reclaim executes immediately without a new ruling, on the measured number.

### D208 (Q8) — PM read AMENDED: fabricated `connectedSources` stay in mock mode untouched, but the CARDS unify on D204's live module in both modes — Vercel + CloudWatch render "Coming soon" in mock mode too
Card availability is a product claim, not sample data; D101's re-label names no mode exception. The one deliberate, ruled exception to "mock renders untouched": the D125 class protects demo *data*, not false *product claims*.

### D209 (new seam) — the `page.tsx` ↔ `Quickstart` prop contract, ruled now
T1 defines in `server/onboarding.ts`: `type OnboardingStatus = { arrived: boolean; firstTrace: { id: string } | null; asOf: string | null }` — returned by both the server read (initial render) and the D203 GET handler (poll). Quickstart props: `{ initialStatus: OnboardingStatus, endpoint: string }` + the issue action. T1 owns the type; T2 consumes; any amendment sweeps both in the same round.

### D210 (new seam) — ConnectModal gets NO real token and NO second issuing surface
Steps render the `<OBSTACK_API_KEY>` placeholder + a link to issue a key (Quickstart or Settings → API keys); the clipboard `Snippet` copies the placeholder form. The hardcoded "waiting for first event…" row (`ConnectModal.tsx:135`) is **deleted as fiction** — the hub's connected-sources section with live health + "as of" is the arrival truth surface; a second poll consumer is a second path.

### D211 (new seam) — `OnboardingChecklist` goes real, owned by T5
Live mode: dashboard server component threads real flags — "Connect a source" + "See your first trace" from `getIngestHealth`/`OnboardingStatus` (the ONE path), "Invite your team" from member count; SLO/alerts rows render `done: false` honestly. Mock keeps today's render. Removal REFUSED.

### D212 (new seam) — SideNav/TopBar/CommandPalette/TourGuide: NO copy edits in S3.4
Labels stay accurate; TourGuide's aspirational copy is S3.5 residue. T3 owns preserving the `data-tour="connections"` anchor through the rewrite; T5 owns a verify-only sweep that all four touchpoints still resolve to registered routes.

### D213 — T7 recut
`deploy/compose/README.md` runbook lines for the new drive steps move to T6. T7 keeps SDK README fence wording + quickstart-adjacent doc claims only; stays a separate wave-3 task.

## Mid-run rulings (W1 close, 2026-08-20 — BINDING)

### D214 (E1) — K8s card reframed "run obstack on your cluster"; boundary stated; placeholder REMOVED from helm snippets; rotate step DELETED; T6 attribution is OTLP-only
The chart's ingest reads `api_keys` from the chart's OWN Postgres (only the 0004 `ok_dev_local` continuity row) — a product-issued token in `--set collector.apiKey=` gets **401'd by the chart's ingest**. (a) The card states the boundary, D87 honest-absence class, one sentence in step 1: the chart brings up a self-contained obstack on your cluster; telemetry lands in THAT stack, not this workspace; external-cluster connection to this deployment needs a network-reachable ingest = M4's self-hosted surface. Card stays `available` (artifact real, kind-CI-proven). Both helm snippets drop `${API_KEY_PLACEHOLDER}`; install runs with the chart default; the "Rotate the key" step is deleted as fiction (no `web` workload until M4). D204 amended: `API_KEY_PLACEHOLDER` appears only in snippets targeting THIS deployment's ingest — today OTLP and Docker. (b) T6 attribution covers **OTLP only**; helm aliveness is the standing kind CI guard; Docker key-health rides the `/app/connections` step. `connectors.test.ts` gains: no `API_KEY_PLACEHOLDER` in the kubernetes card.

### D215 (E2) — one endpoint definition: env var REMOVED from the web layer; client-safe constants in new `apps/web/src/lib/ingest-endpoint.ts`; D209 `endpoint` prop DELETED
`OBSTACK_INGEST_ENDPOINT` is a name collision — compose defines that exact name as the collector's upstream (`docker-compose.yml:249`, `http://ingest:4318`); a shell exporting it for collector work would repoint the quickstart to an unreachable address. Nothing sets it for web; U1 fixes the environment. Binding shape: `lib/ingest-endpoint.ts` (client-safe, no env): `OTLP_HTTP_ENDPOINT = "http://127.0.0.1:4318"`, `OTLP_GRPC_ENDPOINT = "http://127.0.0.1:4317"`, source comment naming compose :101-102, pinned mirror test (FLUSH_MS/D206 pattern). `connectors.ts` imports and interpolates the constants at module level. `server/onboarding.ts` drops `INGEST_ENDPOINT`/env read; page stops threading it. **D209 amended: Quickstart props = `{ initialStatus: OnboardingStatus }` + the issue action**; Quickstart imports the constant directly. S3.2 L1 sweep of the closed implementer (T1's files) executes in the same round — owned by T2.

### D216 — T1's 401 branch confirmed left to T6's drive probe; bare 401 stands (a poll is not a navigation; the done-check's "401→login" wording superseded on that point). T2's poll consumer stops polling on any non-200 — the client half, lands in T2.

### W2 dispatch (advisor): GO — D214 T4 fix lands FIRST (before T3's render tests pin card copy); T2 spec grows the D215 sweep + non-200 poll stop; T2's byte review adds two greps (no `process.env.OBSTACK_INGEST_ENDPOINT` under apps/web; no placeholder in the K8s card); T3 unchanged beyond consuming the fixed module.

## Mid-run rulings (W2 close, 2026-08-20 — BINDING)

### D217 (E3) — the "no live import of @/mock/*" done-check binds LITERALLY; the compliant shape is a module move
`DemoArrival` moves out of `Quickstart.tsx` into a mock-owned component (`components/onboarding/DemoArrival.tsx`, which imports `@/mock/traces`); Quickstart accepts it via the existing live/mock prop signal — the mock-mode caller supplies the demo panel; live callers never import the module. No next/dynamic, no page fork. Pin test follows the component. Small T2 fix commit, lands before W4. Ground: "unreachable by construction" mock content is the S1 L2 / D125/D158 leak class — the import sits in the bundle waiting for the next refactor to re-reach it.

### D218 (E4) — SIGNED exit-wording deviation: cumulative count + "as of" satisfies "rate"; a /min figure REFUSED as invented
D100 counters are cumulative with no window; a rate would be fabricated. A true windowed rate = counter-schema change, pre-registered as an M4 item on the D100 mechanism. Recorded in the exit bundle as a signed deviation.

### D219 (E5) — RATIFIED: errors = decode+unsupported; quota renders separately as "sampled" (the dispatch brief's "errors incl. quota" superseded; `ingest-health.ts:86-87` D162 basis is the one definition; the hub never re-derives).

### D220 (E6) — in-sprint T3 fix commit: ConnectModal's "we'll email you" claim DELETED (false product claim on a route registering this sprint — not S3.5 residue); the request affordance keeps local-state acknowledgment with honest copy, no delivery promise, no channel named, no backend.

### D221 (E7) — fix commit riding T4's ownership this round: `values.yaml:63-64` comment amended to the D214 truth (key authenticates against THIS chart's own Postgres 0004 continuity row; chart ships no key-issuing surface until M4's `web` workload). Comment-only diff.

### W3 dispatch (advisor): GO for T5 + T7, not gated on the three fix commits (disjoint owns); ALL THREE fix commits land before W4/T6 dispatches (T6 drives the rendered surfaces). T7 must reflect D215 and D218 wording. Critical items unchanged.

## Mid-run rulings (W3 close, 2026-08-20 — BINDING)

### D222 (E8) — the Bell icon is part of the false claim; swap ordered to `Plus`; the mock-freeze yields
A bell is the notification promise in pictogram form — D220 removed the words; the icon re-states the claim in a channel the copy review doesn't read. D208's ground (demo *data* protected, false *product claims* never) yields the mock-freeze, as with the card re-label and D220. Swap: `Plus` ("register interest" semantics, no follow-up channel); the clicked `Check` + "Noted for this session" stays. D220 pinned test extends: no `Bell` import in the file. Fix commit lands before T6 dispatch (the drive walks this surface).

### W4 dispatch (advisor): GO for T6 with four spec amendments
1. Assertions match the LANDED copy: connections-step content-aware evidence asserts D218/D219's rendered truth — cumulative counts + "as of", errors = receive-path only, quota as "sampled"; no rate figure, no summed error count.
2. **Token hygiene is a named done-check**: the UI-issued token must never appear in drive stdout, saved artifacts, or the CI log — grep the drive's own output; the literal absent everywhere except the OTLP Authorization header it sends.
3. Unauth probe per D216: the `/app/onboarding/status` 401 probe joins the unauthenticated-probe steps under the `NoSessionError` allowlist discipline; bare-401, no login-page assertion.
4. Timing discipline: the waiting-panel flip polls bounded by `FLUSH_MS` + 5s client cadence (~10s ceiling, a failure past it — never a longer sleep) and asserts the flipped link's trace id equals the trace the drive sent.
Standing: D206 pinned test; compose README runbook lines; `/app/costs` control untouched; no-SAMPLE on both routes; attribution alice-AND-not-bob, OTLP-only; budget per D207 — first real-runner number returns as critical item 2; contingency fires without a new ruling on a trip.
Residue list (WaitlistForm U4, mock-corpus fictions, ok_live_9f2e mock literals) confirmed correctly placed S3.5/M4 — no action this sprint.

## Corrected breakdown (of record — supersedes the PM draft above)

- **T1 — onboarding server layer.** owns: `app/app/onboarding/page.tsx`, `app/app/onboarding/actions.ts` (D201 action), `app/app/onboarding/status/route.ts` (D203 GET), `server/onboarding.ts` (+ `OnboardingStatus`, D209) + tests, mock-mode standing-test extension, the `settings/actions.ts:94` comment amendment. depends: —. **Wave 1.**
- **T4 — connector definitions truth.** owns: new `components/connections/connectors.ts` (D204), `@/mock/connectors.ts` shrink to `connectedSources`, type home. OTLP/Docker/K8s steps over the real signed artifacts (compose `127.0.0.1:4317/4318`, `deploy/collector/` + `up.sh`, `deploy/helm/obstack` + `collector.apiKey`); Vercel + CloudWatch → Coming soon (both modes, D208). depends: —. **Wave 1.**
- **T2 — Quickstart truth rework.** owns: `Quickstart.tsx` + component tests. All four D101 defects; D202 install lines + caveat; env blocks verbatim incl. URL-encoded Bearer + Python-only SEMCONV line; `ai >=5 <7` fence + `ai@7` unsupported; D201 token flow; D209 props; live waiting panel poll-fed; mock mode keeps the timed demo flip + mock trace link. depends: T1, T4. **Wave 2.**
- **T3 — connections hub wiring.** owns: `ConnectionsHub.tsx`, `ConnectModal.tsx`, `app/app/connections/page.tsx` + tests. Live sources = workspace keys with D100 health via `getIngestHealth` only; "as of" shown; real empty state; D210 modal; tour anchor preserved (D212); mock keeps 3 fabricated sources (D208). depends: T1, T4. **Wave 2.**
- **T5 — registration + shell truth.** owns: `lib/live-routes.ts` (+ test), `OnboardingChecklist.tsx` (D211), D212 verify-only touchpoint sweep. No positive-control relocation (D205). depends: T1, T2, T3. **Wave 3.**
- **T7 — docs truth.** owns: `packages/obstack-py/README.md` + `packages/obstack-js/README.md` fence wording, quickstart-adjacent doc claims (D213 recut). depends: T2. **Wave 3.**
- **T6 — e2e drive growth.** owns: `deploy/compose/e2e-drive.mjs`, D206 mirrored-predicate pinned test beside `reconcile.ts`, `deploy/compose/README.md` runbook lines, `.github/workflows/e2e.yml` only if D207's contingency fires. Steps: onboarding open → UI key issue → OTLP send with `Authorization: Bearer <that token>` → attribution content-aware in alice's workspace AND absent from bob's (D115/D142) → waiting panel flips to her real first trace → `/app/connections` shows that key's health → no SAMPLE badge on either route; `/app/costs` control untouched. depends: T1–T5. **Wave 4.**

## Critical items (return to the advisor before merge)

1. **T2 — byte-level quickstart-vs-D78 review** (pre-registered): every rendered snippet against the SDK READMEs — names, zero-arg `init()`, env vars, URL-encoded Bearer, Python-only SEMCONV, endpoint, tgz version literal — PLUS the D201 token-lifetime check (client state only, greppable: no persistence, no URL).
2. **T6 — first real-runner e2e number (D137) + the D115 attribution evidence** — the advisor signs the budget outcome and whether D207's contingency fires.
3. **T5 — the registry diff** — D106 rule satisfied on both routes, `/app/costs` untouched.

## Done-checks (advisor-ruled)

- Every task: full suite green; mock-mode render unchanged except the one D208-ruled card amendment; no live import of `@/mock/*` on the two surfaces.
- T1: standing mock-mode test covers the new action; GET handler refuses unauth (401→login) and mock (404, D193-graded); `OnboardingStatus` served identically by server read and GET.
- T2: byte-review PASS (critical item 1); fake timer absent; `x-obstack-key` and `ingest.obstack.dev` absent from the file.
- T3: grep-clean of `mock/connectors` import; "as of" rendered; empty-state evidence content-aware (D142), not count.
- T4: no `ok_live_9f2e…`, `charts.obstack.dev`, `obstack/collector:latest`, `cf.obstack.dev` in the live module; every step command names an artifact that exists in-repo or in compose.
- T5: registry test green; checklist flags derived, no hardcoded `done: true` on live path.
- T6: **the e2e step extension is the named done-check (D107)** — all steps green incl. D206 pinned test, log-cleanliness split still green, both SAMPLE assertions; runner number reported.
- T7: doc claims match measured fences verbatim (`ai >=5 <7`, `openai >=4.85 <8`, `@anthropic-ai/sdk >=0.50 <1`).

## Kickoff questions for the advisor (RESOLVED above — retained as the record of what was asked)

- **Q1 — Where does the quickstart's "real key" come from?** (a) onboarding issues its own key inline (shown once, D98 semantics, new guarded action); (b) prefix-only + "issue in settings" link. The exit says "renders the workspace's real key" — (a) is the PM read; the token-in-snippet lifetime (client state only, never persisted/URL) needs its ruled shape.
- **Q2 — Install-line honesty:** registries hold `0.0.1` name-hold placeholders (U5); `pip install obstack-py` installs a package marked "do not depend on this version". Does the quickstart render the registry install line (the ruled names, exit-criterion wording) with a stated caveat, the in-repo install (what actually works today), or both?
- **Q3 — First-data signal + poll mechanism:** `getIngestHealth.accepted > 0` vs a trace-count read; and the poll transport (client interval → GET route handler? `router.refresh()` cadence?) under D189 reads-are-reads.
- **Q4 — Live connector card copy placement:** live copy cannot live in `@/mock/connectors` (mock modules feeding live surfaces is the class D125/D158 polices). Ruled home needed.
- **Q5 — Positive-control relocation** for `e2e-drive.mjs:988` when both remaining unwired app routes register.
- **Q6 — The reconcile-wording↔D132 coupling:** resolve (name the dependency in both files / structural token) or restate.
- **Q7 — Drive budget:** pre-authorize the propagation-wait reclaim (drive-time TTL/flush override) or hold it for a measured trip.
- **Q8 — `connectedSources` mock split:** does the mock-mode hub keep the fabricated 3 sources (demo honesty = SampleMark'd route in mock mode — today's posture) — PM read: yes, untouched.
