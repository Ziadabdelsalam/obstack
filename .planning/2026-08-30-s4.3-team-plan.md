# Team Plan — obstack M4 sprint 3 — obstack-js launch alignment (exit: an app on `ai@7` produces the same four-layer trace an `ai@6` app gets today, proven by a CI-driven sample; the OpenAI auto-instrumentation covers the Responses API; both land ADDITIVELY under D78 with the verdict recorded; D298's CI shape is implemented; the D103(a) triage is recorded for S4.4's honest-absence sentences)

meta:
- date: 2026-08-30
- repo/branch: `s4.3-obstack-js-launch-alignment` @ `f7260a9` (master after PR #22 merged S4.2 + the D298 record)
- mode: **manager-directs** (standing user policy 2026-08-24, global: Fable 5 leads, never executes; Opus 5 executors do the work). The manager (Fable 5, full context) writes this plan, dispatches one Opus@high executor per task **sequentially** — the falsify→re-rule→execute loop S4.1 L2 credits to a manager holding full context is preserved by the manager reading every executor result against the done-check before the next dispatch; review gate preserved via batched Opus@xhigh reviewers one per coherent scope; advisor + integrator gates unchanged. This supersedes S4.2's "manager-executes" line (2026-08-23), recorded here so the mode change is visible, not silent.
- team-cap: 1 executor per disjoint file set (T1 ∥ T4 are disjoint and run in parallel; T2 after T1 — shared README/package.json prose; T3 after T1+T2); executors stage explicit paths only, never `git add -A`; 2 batched reviewers; 1 advisor; 1 integrator
- models: executor=opus@high reviewer=opus@xhigh advisor=fable@xhigh integrator=opus@xhigh
- iteration-limit: 3 — fix rounds allowed before escalating to the user
- iteration: 0/3
- source of truth: `.planning/2026-08-23-s4-m4-phase-plan.md` §9 rulings **D258, D259** (BINDING), D250 row S4.3 + its demoable line ("an `ai@7` sample produces the four-layer trace"), §"Verdict and gates" critical item 3; **D298** (`.planning/2026-08-24-s4.2-exit-evidence.md` §6, BINDING, "implemented in S4.3"); **D78** frozen API (`.planning/2026-08-17-s2.4-team-plan.md:46-48`), **D88** (`:77`), D82 attribute contract, D81 (names live once in `attributes.ts`), D87 (below-floor = no span, no error, stated), D76 (samples pin exact, never `latest`), D92 content perimeter
- standing guards inherited: `e2e` D137 watch (three consecutive runs over ~6m) with **D207's reclaim pre-authorized as the first response on the measured number**; `stack` watch re-based by D298 to master pushes (band 5m58–7m17); K3 forever-names only (`go`/`web`/`lint`/`images`/`stack`/`e2e`/`kind`/`sdk-e2e`/`sdk-js`/`sdk-py`); environment-ordering `down -v` before DSN-backed suites; D178 standing check (fires only on a `@polar-sh/sdk` bump — **none planned, pinned 0.49.0 at `apps/web/package.json:16`, untouched by this scope**); S2.2 L1 (no activation claims); D13/D21 honesty on every user-facing sentence (README is product surface)

## Measured facts (S2.4 L1 discharge — measured at breakdown, never believed)

Three measurement passes, 2026-08-30, read-only on the tree at `f7260a9`; scratch installs under the session scratchpad. Every claim below has a file:line; the ones marked **(ruling)** are decisions the measurement surfaced, not facts it settled.

### AI — the `ai@7` telemetry surface (measured on `ai@7.0.85`, latest 7.x; `@ai-sdk/openai@4.0.52`; node 22.22.2)
- **AI1 — `ai@7` emits ZERO OpenTelemetry spans.** `grep` over `node_modules/ai` for `@opentelemetry|startActiveSpan|getTracer` (excluding docs/CHANGELOG) → 0 hits; its deps are only `@ai-sdk/gateway|provider|provider-utils`. Empirically, with a real `NodeTracerProvider` + in-memory exporter + `AsyncLocalStorageContextManager` registered, `generateText` with `experimental_telemetry:{isEnabled:true}` AND with `telemetry:{isEnabled:true}` → **0 spans, 0 `ai.operationId`**. `experimental_telemetry` still type-checks on 7.x as a deprecated alias (`src/embed/embed.ts:48-49`), so the README's documented v5/6 call form silently produces nothing on v7. **Consequence:** `VercelAiTranslationProcessor` early-returns at `vercel-ai.ts:137` on every v7 span → the v5/6 and v7 code paths are mutually exclusive by measurement; no double translation when both ship live.
- **AI2 — two registration doors, neither needs an `ai` import or an env var, both PUBLIC.** (1) `registerTelemetry(...integrations)` (`src/telemetry/telemetry-registry.ts:6-11`) is literally `globalThis.AI_SDK_TELEMETRY_INTEGRATIONS ??= []; push(...)` — a **`globalThis` property, not an env var**; pushing onto it **before `ai` is loaded at all** delivers every callback (probed). (2) `tracingChannel('ai:telemetry')` from `node:diagnostics_channel` (`src/telemetry/tracing-channel.ts:1`). Exported unprefixed from `src/index.ts:55` (`export * from './telemetry'`), zero `@experimental`/`@internal` tags in `src/telemetry/*.ts`, documented at `docs/03-ai-sdk-core/60-telemetry.mdx:205-300`.
- **AI3 — telemetry is ON by default on v7.** `create-telemetry-dispatcher.ts:75-77`: only `telemetry:{isEnabled:false}` short-circuits. A bare `generateText({model, prompt})` fires everything. **This is a docs-visible improvement over v5/6, which needs `experimental_telemetry:{isEnabled:true}` per call (`README.md:123`).**
- **AI4 — integration shape.** `interface Telemetry` (`src/telemetry/telemetry.ts:106-273`), all methods optional: `onStart/onEnd`, `onStepStart/End`, `onLanguageModelCallStart/End`, `onToolExecutionStart/End`, `onAbort`, `onError`, embed/rerank hooks, plus two context wrappers `executeLanguageModelCall({...event, callId, execute})` and `executeTool({...event, callId, toolCallId, execute})` (`:250-272`). Integration errors are caught upstream.
- **AI5 — channel event types and nesting are real.** `TelemetryTracingEventType` = `generateText | streamText | step | languageModelCall | executeTool | embed | embedMany | rerank` (`tracing-channel.ts:3-11`); messages `{type, event}` + `result` on `asyncEnd`, `error` on `error` (`tracing-channel-publisher.ts:159-197`). `tracingChannel.start.bindStore(als, fn)` yields the tree `generateText → step → {languageModelCall, executeTool}` (probed, 2 steps + 1 tool). Error path publishes `error` on `languageModelCall`, then `step`, then `generateText`; the app's exception propagates untouched.
- **AI6 — payloads carry the whole D8 set.** `LanguageModelCallStartEvent` (`language-model-events.ts:27-34`): `callId, provider ("openai.chat"), modelId, instructions, messages (ModelMessage[]), tools`. `LanguageModelCallEndEvent` (`:42-109`): `provider, modelId` (**the RESPONSE model, e.g. `gpt-4o-mini-2024-07-18`**), `finishReason` (**scalar** `"stop"`), `responseId`, `usage.{inputTokens, outputTokens, totalTokens, inputTokenDetails, outputTokenDetails}` (flat ints), `content[]`, `performance`. Channel `asyncEnd.result` is the raw `LanguageModelV4GenerateResult` (`usage.inputTokens.total`, `finishReason.unified`). `executeTool` start: `{callId, toolCall:{toolCallId, toolName, input}, messages}`; end: `{output:{...}, toolExecutionMs}`.
- **AI7 — the system prompt RELOCATES.** v7 delivers system text as `event.instructions`, a sibling of `messages`, not inside it. A verbatim `JSON.stringify(event.messages)` silently drops it. **(ruling — K2.)** Unmeasured: whether ai@6's `ai.prompt.messages` string carried the system message inline (needs the repo's pinned `ai@6` fixture side by side — T1 done-check).
- **AI8 — streaming is structurally reachable.** `streamText → step → languageModelCall` publish with nesting, but channel `asyncEnd` carries **no** `result` for `streamText` and only `{stream}` for `languageModelCall`; usage/text/finishReason for streams exist **only** on the integration callbacks (`onEnd`/`onStepEnd`). D259 rules streaming OUT of M4; this fact goes to the M5 packet as a cost correction. **(ruling — K6: the v7 path must not emit a half-span for stream calls.)** Unmeasured: whether the `languageModelCall` start event distinguishes stream from generate — T1 measures.
- **AI9 — the channel is Node-only; the `globalThis` door is not gated.** `tracing-channel-publisher.ts:29` gates on `isNodeRuntime()` (`process.release?.name === 'node'`). Edge/workerd get no channel events; the integration array is read regardless. **(ruling — K7: which door carries what.)**
- **AI10 — `@ai-sdk/otel` is NOT a viable dependency.** `@ai-sdk/otel@1.0.85` declares `ai: "7.0.85"` as an **exact hard dependency with no peerDependencies** — depending on it would drag a pinned second `ai@7.0.85` into every install, including ai@5/6 apps. It ships `OpenTelemetry` (a `gen_ai.*` emitter) and `LegacyOpenTelemetry`, which **re-emits the exact ai@5/6 span shape** (`ai.generateText.doGenerate`, `ai.model.provider`, `ai.prompt.messages`, `ai.response.text` — `legacy-open-telemetry.ts:275-277,389,547,681`). An app registering either alongside obstack gets **two llm spans per call**; with `LegacyOpenTelemetry`, obstack's *existing* processor would translate theirs. **(ruling — K4.)** `ai@7` itself contains zero `gen_ai` strings outside docs.
- **AI11 — ambient-context join is BUILD-time proof, not breakdown-time.** OTel's `AsyncLocalStorageContextManager` exposes no bindable store; obstack must keep its own `AsyncLocalStorage` via `channel.start.bindStore(...)` and pass explicit parent contexts. `bindStore` proves the ai-internal tree; it does **not** prove that a span opened in channel `start` parents under a surrounding `traceAgent` span — T1's done-check proves it red-then-green.
- **AI12 — v7 floor unmeasured.** Only 7.0.85 was exercised; D88 cites 7.0.66 as span-less. Whether `executeTool` events and the `Telemetry` interface exist across 7.0.0–7.0.85 is unmeasured; the D87 precedent (openai's 4.85 floor was bisected, not assumed) applies. **(ruling — K5.)** Sub-range gaps in the registry: 7.0.24, .53, .72, .75, .80, .81 (unpublished).

### OR — the OpenAI Responses API surface (measured on openai 4.85.4 · 4.86.0 · 4.86.2 · **4.87.0** · 4.87.4 · 5.23.2 · 6.49.0 · 7.4.0 · **7.8.0** = latest)
- **OR1 — the resource first appears at 4.87.0**: `openai/resources/responses/responses.js` is `MODULE_NOT_FOUND` and `client.responses === undefined` on 4.85.4/4.86.x; present with class `Responses` and prototype `create` (+ `retrieve/del/parse/stream`, later `cancel`, `compact` from 6.49.0) from 4.87.0 through 7.8.0. `Object.getPrototypeOf(client.responses).create === Responses.prototype.create` is **true on every version that has it** — the same prototype shape the chat patch relies on (`openai.ts:44-51`). Distractor paths that are NOT the dispatch target: `resources/responses.js` (re-export, 5.x+), `resources/beta/responses/responses.js` (separate `beta` resource with its own class, 6.x+), `lib/responses/ResponseStream.js`.
- **OR2 — the chat path still holds on 7.8.0**: `openai/resources/chat/completions/completions.js` exists and matches; the peer `>=4.85 <8` claims stay true.
- **OR3 — `stream()` and `parse()` route THROUGH `create`.** `client.responses.stream()` → `ResponseStream.createResponse` (`responses.js:72-74`) → `client.responses.create({...params, stream:true})` (`lib/responses/ResponseStream.js:50`); `parse()` → `create` (`responses.js:64-68`). Live-verified: exactly one `create` invocation each. So one `body.stream === true` guard in the mapper covers both `create({stream:true})` and `.stream()`, mirroring the chat posture (`openai.ts:95` → `llm-span.ts:84`: no span, no wrapper, no error); `parse()` gets a span free with no double count.
- **OR4 — request/response shapes** (`ext/7.8.0/resources/responses/responses.d.ts`): `create` overloads `:51-53`; `ResponseCreateParamsBase` `:7996` — **every field optional on 7.8.0** (`input?: string | ResponseInput` `:8048`, `model?` `:8079`, `instructions?` `:8056`, `tools?` `:8239`, `previous_response_id?` `:8094`, `stream?` `:8192`); on **4.87.0 `input` and `model` are required** (`:2057,:2065`) — a `typeof model === "string"` guard is correct on both. `Response` `:762`: `id`, `output_text: string` (`:771`, **client-side decoration** via `addOutputText` in `lib/ResponsesParser.js`, only when `object === 'response'` — if the server omits `object`, `output_text` is `undefined`), `error` `:775`, `incomplete_details` `:779` (`reason?: 'max_output_tokens'|'content_filter'` `:1025`), `model` `:804`, `output: ResponseOutputItem[]` `:818` (28-member union by `type`: `message`, `function_call`, `reasoning`, …), `status?: ResponseStatus` `:981` (`completed|failed|in_progress|cancelled|queued|incomplete` `:5967`; 4.87.x lacks `cancelled/queued`), `usage?: ResponseUsage` `:1010`. **`status` and `usage` are OPTIONAL in the type.**
- **OR5 — usage**: `ResponseUsage` `:6226` → `input_tokens` `:6230`, `output_tokens` `:6238`, `total_tokens` `:6246` **stable across the whole range**; `input_tokens_details.{cached_tokens, cache_write_tokens}` `:6256-6265` (absent on 4.87.0, added 4.87.4); `output_tokens_details.reasoning_tokens` `:6270-6275`. `cached_tokens`/`reasoning_tokens` have **no D8 name** — D81 forbids inventing one.
- **OR6 — live objects against a local fake (7.8.0)**: URL hit **`POST /v1/responses`**; wire body `{"model":"gpt-4o-mini","input":"…"}`; resolved value is a plain object with own keys `id,object,created_at,status,error,incomplete_details,model,instructions,output,usage,…,output_text`; `output_text` is an own enumerable data property. A `function_call`-only turn (`output = [reasoning, function_call]`, `status: incomplete`, `incomplete_details.reason: max_output_tokens`) yields `output_text === ""` — exactly as the chat path yields `""` for a tool-call-only choice. `create({prompt:{id}, input})` with **no `model`** is accepted and returns `model` from the server.
- **OR7 — there is no `finish_reason` anywhere in the Responses schema.** The nearest provider-native analogues are `status` and `incomplete_details.reason`. D82 fixes the array form and "provider-native, verbatim" but predates this API. **(ruling — K3.)**
- **OR8 — semconv**: installed `@opentelemetry/semantic-conventions@1.43.0`; `gen_ai.operation.name`'s enumerated values are `chat, create_agent, embeddings, execute_tool, generate_content, invoke_agent, invoke_workflow, retrieval, text_completion` — **no `responses` value exists**. D85 already ratified that `gen_ai.operation.name` is not emitted; nothing changes. Ingest classifies the llm layer by **any `gen_ai.*` attribute** (`services/ingest/internal/mapping/mapping.go:77`), not span name — reusing `chat <model>` verbatim keeps classification identical with no new constant.
- **OR9 — the current chat patch, for mirroring**: one `InstrumentationNodeModuleDefinition("openai", [">=4.85 <8"], …, [completions])` (`openai.ts:60-66`) whose 5th arg is a `files` array holding one `InstrumentationNodeModuleFile` (`:40-58`, own `supportedVersions` at `:41`); attributes emitted centrally in `llm-span.ts:88-94/:137-143`; `LlmShape` (`llm-span.ts:51-59`) already expresses "return `undefined` to leave the call alone". Test fake `testing/harness.ts:51-82` answers **one canned JSON body for every route**, so a Responses-shaped body needs no harness change; the suite requires `openai` **inside `before()`** (`openai.test.ts:34-38`) so the patch precedes the load; `llmSpan()` asserts exactly one span carrying `gen_ai.system`.
- **OR10 — size by analogy**: ≈70–75 lines in `openai.ts` (second file block ~20, `RESPONSES_SHAPE` ~35 incl. the `output[]` walk fallback, doc comment ~15) + `responses.test.ts` ≈130 lines mirroring `openai.test.ts` + README/`package.json` prose.

### CI — the shape for D298 and the sample leg (measured on the tree + `gh` run history)
- **CI1 — no workflow has a `push:` trigger, none has a `paths:` filter, none has `needs:`** (grep-verified). `gh run list --branch master` → `[]`: **zero master-push runs have ever existed.** D298's "`push` to master stays always-on" is therefore a **new trigger, not a retention**; "the `ci:stack` label is superseded" supersedes a **comment** (`stack.yml:33`, `e2e.yml:62`), not code. `stack.yml:40-46` and `e2e.yml:67-74` trigger blocks quoted in the measurement record.
- **CI2 — what `acceptance.sh` actually builds** (`deploy/helm/obstack/acceptance.sh`): `:100` ingest image ← `services/ingest/**`; `:101` demo-agent image ← `demo/agent-app/**`; `:110-117` web image `-f apps/web/Dockerfile` with **root context** — the Dockerfile copies `package.json package-lock.json` (`:22`), `apps/web/package.json` (`:23`), `packages/obstack-js/package.json` (`:24`), **`apps/web` whole** (`:29`), `services/ingest/pricing/prices.json` (`:34`); `:126/:155/:158` the chart ← `deploy/helm/obstack/**`; `:91-97,:175,:190-198` `acceptance.ts` imports `@/lib/types` ← `apps/web/src/**` and needs root `npm ci`; `stack.yml:162,170-174,187` use `deploy/compose/**`. **True filter set:** `.github/workflows/stack.yml`, `deploy/**`, `services/ingest/**`, `demo/agent-app/**`, **`apps/web/**`**, `packages/obstack-js/package.json`, `package.json`, `package-lock.json`. **Discrepancy vs D298's list:** `apps/web/Dockerfile*` is far too narrow (root context + `COPY apps/web`), and the three package files are missing. **Consequence:** an honest filter fires on most PRs, so the PR-time reclaim comes almost entirely from `stack` leaving the required set, not from the filter. **(ruling — K9a.)**
- **CI3 — "required" is advisory.** `gh api …/branches/master/protection` → 403 (private repo, free plan): **no branch protection exists.** The required set is the ten PR checks the merge process gates on, as `stack.yml:29-30` states. Dropping `stack` is a documentation + process change, not an API change. **(K9c.)**
- **CI4 — bands.** `stack` successes since S4.1: 7m17 · 5m58 · 6m45 · 7m06 · 7m02 → **5m58–7m17**, matching D298. `e2e` last 11 successes: 5m47 · 5m33 · 5m28 · 5m35 · 5m35 · **6m02** · 5m54 · 5m52 · 5m55 · 5m41 · 5m31 → **5m28–6m02**. **The S4.2 bundle wrote 5m33s as the last run, not the band: `e2e`'s worst measured point is 6m02s (run 32649593390), already at/over the ~6m line.** Under S3.4 L4 ("project a budget from its worst measured point") `e2e` has **negative headroom before S4.3 adds anything**. The D137 over-branch (three consecutive over) has not fired. **(K9d.)**
- **CI5 — the D122 guard, exactly.** `stack.yml:170-174` runs `deploy/compose/ddl-drift-check.mts` (138 lines): scratch DB on compose Postgres (`:98-103`), `getMigrations(authConfig()) + compileMigrations()` (`:114-115`), drop `WITH (FORCE)` (`:119`), diff vs `services/ingest/pgmigrations/0003_auth.sql` (`:36,:123-130`). Failure source `apps/web/src/server/auth.ts:22-32`. Needs: an **empty Postgres** (makes its own scratch DB), root `npm ci`, `tsx --tsconfig apps/web/tsconfig.json --conditions react-server`, `BETTER_AUTH_SECRET` (any literal). **No kind, no ClickHouse, no built web.** Measured CI cost **4s** (both PR-#22 runs) + Start Postgres 6s + Stop 0s ≈ **10s removable from `stack`** (7m06 → ~6m56, **still over K5** — `acceptance.sh` alone is 241s; D298 accepted this). `e2e` **already** starts compose Postgres (`e2e.yml:113`, 103s), already runs root `npm ci` (`:98`), already tears down with `--profile '*' down -v` (`:155`) → the move is **one step, zero new services, +4s**. The chart-Postgres schema step that STAYS is `stack.yml:140-143` (`kubectl exec statefulset/obstack-postgres -- psql …`, needs the cluster). Latent: on node 22 the script crashes at `:119` with an unhandled `pg` `BoundPool` `'error'` (57P01) after `getPool().end()` at `:116`; it passes in CI on node 24 — confirm on node 24, not a blocker.
- **CI6 — TWO demo apps; the sample that uses the Vercel AI SDK is NOT in `e2e`.** `demo/agent-app/` is **Python** (what `acceptance.sh:101` builds and `smoke.sh` drives in `e2e`'s floor). `demo/sdk-sample-ts/` is the JS sample, driven by **`sdk-e2e`** via `deploy/compose/sdk-evidence.sh` → compose profile `sdk` (`docker-compose.yml:284-307`). `sdk-sample-ts/package.json` pins exact (D76): `ai 6.0.256`, `@ai-sdk/openai 3.0.97`, `openai 7.4.0`, `obstack-js` from the tarball, with its own comment naming 7 as span-less. **The `ai@7` sample leg's home is `sdk-e2e` (3m46s, 30-min timeout), not `e2e`.** `sdk-e2e` phase breakdown (run 32669133733, 193s): build+boot both samples 136.6s · requests+trace select 6.2s · four-layer/D8/D92 assertions 0.2s · facade 0.8s · OTLP legs 20.9s · fail-open legs 28.4s. The "166 assertions" are `e2e-drive.mjs` tenancy checks — unrelated to the four-layer trace.
- **CI7 — the `ai`-version-specific literals**: `packages/obstack-js/src/vercel-ai.ts:65-70` (`ai.generateText.doGenerate`, `ai.operationId`, `ai.model.provider`, `ai.prompt.messages`, `ai.response.text`) and `:96-102` (the D92 content-key list); `vercel-ai.test.ts:117-121` (whose failure message already names the ai@7 break verbatim), `:125-126`, `:143-171`, `:172` (the 15-attribute enumeration on 6.0.256); `package.json` peer `"ai": ">=5 <7"`; `demo/sdk-sample-ts/src/agent.ts:22,73-77` (`experimental_telemetry`); `deploy/compose/sdk-evidence.sh:233-235,242-243`; `deploy/compose/sdk-checks.ts:73-95`.
- **CI8 — hard blocker, measured in a real image build:** a sample installing `obstack-js` + `ai@7.0.85` fails today with `npm error ERESOLVE … peerOptional ai@">=5 <7" from obstack-js@0.1.0`. **Widening the peer range is a precondition for any sample leg** — package metadata, inside the K1 additivity question.
- **CI9 — sample-leg options, measured:** (i) a second sample dir on `ai@7` as a third service under the `sdk` compose profile + a leg in `sdk-evidence.sh`: twin image build with the shared `obstack-js` pack stage cached (what one `docker compose build` does) = **38s** (`npm install` 28.4s) + ~5s `npm run build` → worst-point budget ~45s image + ~5s boot + ~3s select + assertions ≈ **+55s**, **+24s more** with OTLP + fail-open legs → `sdk-e2e` **~4m41s (or ~5m05s)**. (ii) an alias devDep `ai7: npm:ai@7.0.85` beside `ai ^6.0.256` in `packages/obstack-js` **resolves cleanly** (both trees, 34MB, +2s warm install; `zod@4.4.3` already in the root lockfile satisfies ai@7's `^3.25.76 || ^4.1.8`); **no npm alias exists anywhere in the repo today — this would be the first**; `vercel-ai.test.ts:83` builds its own stub model so no `@ai-sdk/openai` alias is needed; this runs in **`sdk-js` (1m01s → ~1m10s)**, not `web`. Does not prove the four-layer trace end-to-end on its own. (iii) bumping the existing sample to `ai@7` outright deletes the ai@6 coverage the peer range advertises and goes red by design at `vercel-ai.test.ts:117-121` and `sdk-evidence.sh:233-235` — **refused by the PM**. **PM recommendation: (i) + (ii).** Both stay inside K3 names; neither touches `e2e`.
- **CI10 — D178:** `@polar-sh/sdk` pinned exact `0.49.0` (`apps/web/package.json:16`, lock `:2546-2549`), consumers `apps/web/src/server/billing/{client,polar}.ts`. Nothing in scope touches it — **the standing check does not fire**; recorded, not assumed.

## Advisor decisions
<!-- Kickoff round 2026-08-30, recorded as D299–D309 continuing the ledger. Citations verified on the tree at f7260a9 and in the scratch installs; verdict: CLEARED WITH CONDITIONS (listed after the rulings). -->
- **D299 (K1) — D78 additivity: ADDITIVE on both paths. Verdict recorded; no escalation.** Grounds: `src/index.ts:11-12` exports (`init`, `ObstackSDK`, `traceAgent`, `traceTool`) and `init(): ObstackSDK` (`init.ts:38`) are untouched — the v7 registration is one push onto `globalThis.AI_SDK_TELEMETRY_INTEGRATIONS` inside the existing try block (`init.ts:41-74`); AI2 verified (`telemetry-registry.ts:6-11`), and the array is read per operation at dispatch (`create-telemetry-dispatcher.ts:80-83`), so there is no import-order constraint on this door. Responses is a second `InstrumentationNodeModuleFile` in the existing `files` array (`openai.ts:60-66`) plus a second `LlmShape` (`llm-span.ts:51-59`); OR1 verified. `attributes.ts`, `llm-span.ts`, `index.ts` unchanged; peer widening `ai >=5 <8` is metadata (CI8). No `ai` import: obstack declares local structural types for the hooks it implements. **Converts to escalation mid-build** (executor STOPS before the change): any diff to `src/index.ts`; any new `init()` option, argument or obstack env var; any change to `traceAgent`/`traceTool` signatures, span names or attributes; any new name in `attributes.ts`; any field added to `LlmShape`/`LlmRequest`/`LlmResponse` to express Responses.
- **D300 (K2) — `gen_ai.prompt` wire form, one rule for both paths: always a JSON array of `{role, content}` in verbatim order; a system/instructions string outside the messages becomes the LEADING `{role:"system", content}` element; a bare-string Responses `input` becomes `[{role:"user", content: input}]`; array elements are serialized untouched.** Ground: parity, not a synthesized envelope — `ai@6`'s `ai.prompt.messages` already carries the system message inline as `messages[0]` (pinned on the tree at `vercel-ai.test.ts:143-149`; AI7's "unmeasured" is already measured), and the chat path carries it as `messages[0]` (`openai.ts:101`). The string→user envelope is the Responses API's own documented equivalence. No new attribute (D81). T1 done-check (7) becomes: the v7 string for the same prompt equals the v6 form modulo ai's message normalization, recorded.
- **D301 (K3) — Responses `gen_ai.response.finish_reasons` = `[status]` when `status` is a string; omitted otherwise (`llm-span.ts:141`).** Ground: D82 "provider-native, verbatim" — `status` is the one field present on every response (`responses.d.ts:981`, enum `:5967`); folding `incomplete_details.reason` in is a composite obstack would invent — refused. README sentence: "the Responses API has no finish_reason; obstack records the response `status` (`completed`, `incomplete`, `failed`, …)". The value doubles as the sample discriminator (D308).
- **D302 (K4) — `@ai-sdk/otel`: (b) ratified — no detection, a documented bound.** Ground: AI10 verified (`@ai-sdk/otel/package.json:28-32`, exact `ai: 7.0.85` dependency, no peers); a stand-down keyed on a third party's class names is a promise obstack cannot keep. README: "register obstack-js or `@ai-sdk/otel`, not both — both registered means two llm spans per call". S4.4 carries the sentence.
- **D303 (K5) — floor: bisect, then the peer range EXCLUDES the dead sub-range.** T1 bisects published 7.0.x for all of: the integrations array read at dispatch; `executeLanguageModelCall` invoked around `doGenerate` with `callId`; `onStart` carrying `operationId` and the same `callId`; the resolved result shape (`usage.inputTokens.total`, `finishReason.unified`, `content[]`, `response.modelId`). Floor = 7.0.0 → `>=5 <8`; otherwise `>=5 <7 || >=7.0.N <8` — option (a), PM lean (b) REFUSED. Ground: D87 — below the floor NOTHING in obstack-js works on `ai`, so metadata advertising it is the coverage fiction D87 refused; the Responses case differs because chat coverage keeps 4.85–4.86 honest. README states the floor in the D87 paragraph shape. Record `ai@7` `engines.node >=22` (`ai/package.json:75`); obstack-js `engines` stays `>=20`. **The bisect result and the chosen range return to the advisor before T1 merges.**
- **D304 (K6 + K7, merged — the door decides the stream marker) — ONE door: the `globalThis` integration. The channel is NOT subscribed; no obstack-owned `AsyncLocalStorage`. The `executeLanguageModelCall` wrapper owns the span lifetime.** Grounds: obstack-js is Node-only by construction (NodeSDK + CJS require-patching, `init.ts:49`), so AI9's portability argument is moot; nothing in this sprint consumes the ai-internal tree (D305); the join to `traceAgent` is `context.active()` at wrapper entry — the same ALS mechanism the channel would ride; and the channel's outer `streamText` store is re-entered only via `.run()` (`tracing-channel-publisher.ts:196-206`), so it is not a reliable stream marker anyway. **Hooks, exactly these:** `onStart` records `operationId` by `callId` in a `Map`; `executeLanguageModelCall` is the span; `onEnd`/`onError`/`onAbort` delete the entry — upstream's own `@ai-sdk/otel` uses this exact pattern (`open-telemetry.ts:123-128,185-199,319`); `callId` is per operation and shared with the call-level event (`generate-text.ts:647,1004`; `stream-text.ts:1689`, `stream-language-model-call.ts:289,334`). **Wrapper:** recorded `operationId` starts with `ai.stream` → `return execute()` untouched (K6 — the wrapper IS invoked for `doStream`, `stream-language-model-call.ts:349`, with no stream marker in its options, so a priori knowledge is required). Otherwise `startSpan("chat <modelId>", CLIENT, {system ← bareProvider(provider), request.model ← modelId, prompt per D300}, context.active())`, run `execute()` inside `context.with(trace.setSpan(...))`; rejection → `recordFailure` + `endSpan` + rethrow (`fail-open.ts:25-35`); resolution → `response.model ← result.response?.modelId ?? modelId`, `completion ← content[type==="text"].text` joined, tokens ← `usage.inputTokens.total`/`usage.outputTokens.total`, `finish_reasons ← [finishReason.unified]` (parity with v6's pinned `["stop"]`), then end. Each retry attempt is one span — each is a real call. **Why not `onLanguageModelCallEnd`:** it fires after `parseToolCall` (`generate-text.ts` ~1075-1130), which can throw → a span ended there leaks; the integration's `onError` is operation-level (`generate-text.ts:1564`), not per call. `bareProvider` exported from `vercel-ai.ts`, shared. `telemetry:{isEnabled:false}` is ai's own gate (dispatcher `:75-77`). No `ai.*` attributes on v7 spans — D92 strip N/A, stated. **T1 red-then-green:** red = parent forced to `ROOT_CONTEXT`, green = `context.active()`, real `ai7` alias + stub V4 model inside `traceAgent`. **Escalation trigger:** `onStart.callId` ≠ wrapper `callId` on any version in the D303 range.
- **D305 (K8) — auto tool spans OUT. Ratified.** Ground: the exit line is parity; `traceTool` is the D78 API; a v7-only auto layer diverges the two peer lines and reopens D78's "no input/output capture" line (`executeTool` payloads carry `input`/`output`). M5 packet, with AI6's payload attached.
- **D306 (K9) — D298 implemented with four corrections ratified and one added.** (a) Filter = CI2's true set verbatim (verified `acceptance.sh:100-117`, `apps/web/Dockerfile:22-29`, `stack.yml:162-174`); record that the reclaim comes from the required-set drop, not the filter. (b) `push: branches:[master]` is new; the re-based watch opens on the first master-push number; band 5m58–7m17 is the PR-run prior. **Added, binding:** `concurrency` must not cancel master runs — the group `stack-${{ github.head_ref || github.ref }}` (`stack.yml:44-46`) collapses to `stack-refs/heads/master` on push, so a second merge would CANCEL the first's run, a hole in D41's backstop; set `cancel-in-progress: ${{ github.event_name == 'pull_request' }}`. (c) Required set is prose + process (CI3 verified, 403): root `README.md:132` (stack trigger sentence) and `README.md:205` (the check list — it currently says "eight" and names `stack`; executor reconciles the count against `images`/`lint` on the tree) plus the `stack.yml` header; T4's README target resolves to root `README.md`. (d) **HOLD the ~6m line; D207 executes on the first `e2e` number over it, no return trip (D261).** Re-basing refused on D298's own ground. Record correction: the worst `e2e` point in the recorded window is **6m44s** (run 32269078423, s3.3), one run outside the plan's 11-run window; 6m02s is the worst of the last 11. Guard placement: immediately after the compose step (`e2e.yml:113`), before smoke — fails fast, attributable; secret literal verbatim from `stack.yml:172`. The node-22 latent crash is out of T4's scope; carried forward. (e) `stack` ≈6m56 post-move stays over K5 — accepted by D298, restated, not a regression.
- **D307 (K10) — sample leg lives in `sdk-e2e`; shape (i)+(ii) ratified with constraints.** CI6 verified (`docker-compose.yml:284-307`, `sdk` profile). Twin `demo/sdk-sample-ts-ai7/` is a copy of the sibling whose differences are enumerated in its `//` note and are exactly: pins (`ai 7.0.85`, `@ai-sdk/openai 4.0.52`, `openai 7.4.0`), `generateText` with no telemetry option, nothing else; base image stays `node:22.23.1-slim` (`Dockerfile:12,21` — satisfies ai@7's `>=22`); the pack stage byte-identical so BuildKit dedupes. OTLP and fail-open legs **IN** for the twin (OTLP is the literal exit clause on new emission code; fail-open is the D26 promise per sample; +24s). **Budget correction:** base is the worst point **4m04s** (run 32668332270), not 3m46s → worst-point ~4m59–5m23s; under 6m, 30-min timeout. Alias devDep `ai7: npm:ai@7.0.85` ratified, first alias in the repo, stated in the note; the v7 suite runs in `sdk-js`. `deploy/compose/README.md` (`:405` names the sdk services) added to T3's owns.
- **D308 (K11) — Responses edges and the sample call.** OUT list ratified (`beta.responses`, `compact()`, `retrieve(id,{stream:true})`); `parse()` covered free (OR3 verified, `responses.js:64-74`, `ResponseStream.js:50`). The v6 sample gains one `responses.create` call — ratified; T3's owns MUST add `demo/sdk-sample-ts/src/fake-openai.ts` (a `/v1/responses` route: `object:"response"`, `status:"completed"`, `model` gpt-4o-mini, non-zero `usage`, one `output[]` message) so every llm span stays "complete" under `sdk-evidence.sh:233-235` (cost>0). `sdk-checks.ts` identifies the Responses span as the llm span with `finish_reason === "completed"` (D301) and asserts its full D8 set.
- **D309 (K12) — D103(a) triage record ratified as written, plus:** the ai@7 node ≥22 sentence; the v7 opt-out (`telemetry:{isEnabled:false}`) beside v5/6's opt-in; the D303 floor; the D304 hook design recorded as the M5 base for streaming (AI8: usage on `onEnd`/`onStepEnd`).

### D303 pre-merge return (2026-08-30, after T1) — D310–D312, BINDING
- **D310 (D303 return) — peer range `"ai": ">=5 <8"` RATIFIED.** Ground: 19 of 80 published 7.0.x exercised, log2-spaced plus every registry-gap neighbour, all four D303 predicates green on each, `onStart.callId === wrapper.callId` throughout — floor 7.0.0, D303's first branch. Binding: the `//peerDependencies` note names the 19 versions and the four predicates (D87's paragraph shape); the README floor sentence says 7.0.0 measured, never "all of 7".
- **D311 — Quickstart fence owns amendment RATIFIED, owner T2 as ruled.** Ground: `Quickstart.tsx:347` is now a false sentence on the D258 surface; the guard at `Quickstart.test.ts:245` fired exactly as D88/D101 designed. Constraints: one sentence changes, every other quickstart word byte-identical (D78-verbatim; the S3.4 byte-check reruns at S4.4); the guard's literals move with it and stay a mirror of `package.json` — **assert the range by reading `jsPackage.peerDependencies.ai`, not a second hand-typed string**; the sentence states the v7 on-by-default / v5-6 opt-in split, Responses from 4.87, streaming absent in both SDKs — nothing more (D13). T2's owns gain `Quickstart.tsx` (that `<p>` only) and `Quickstart.test.ts`.
- **D312 — the four T1 decisions: RATIFIED, no objection.** Blank `modelId` → no span mirrors `openai.ts:97`. `asRecord`/`asCount` reused read-only keeps one definition. Separator `""` equals `result.text` — record that equality in the test. Cast-not-`declare global` is correct. `"ai.stream"` prefix test is D304 verbatim and fails safe. No change to the D299 verdict; T1 may merge once D311's amendment lands with the guard green.

### Escalation round after R1 (2026-08-30) — D313–D315, BINDING
- **D313 (E1) — Option (b) RULED; (a) ceiling and (c) piggyback REFUSED.** Ground: (a) fences off the current `openai` — the exact D259 failure; (c) makes the span contingent on app behaviour. Binding on the fix: the wrapper registers on the promise's `responsePromise` FIRST and observes `props.response.clone().json()`; guard = `responsePromise` not a thenable → no span, no error, `swallowed(...)` once; `shareOneParse` deleted whole. Doc comment records the field name as measured on 4.87.0/7.4.0/7.8.0 (`api-promise.js` constructor, `core.js:77` on 4.x) and the `client.js:508` per-instance `_thenUnwrap` bypass that killed the memo. Tests pin R1's A/B/C red-then-green on 4.87.0 AND 7.8.0; `openai` devDep → `^7.8.0` ratified (library dev range; the compose sample stays exact 7.4.0 per D76 — two-version coverage, stated in the `//devDependencies` note). README gains the fail-open exit sentence.
- **D314 (E2) — D304 wrapper rule RESTATED.** New literal wording: "span only when the `callId` entry EXISTS and its `operationId` does not start with `ai.stream`; missing or evicted entry → `return execute()` untouched." Bound = 1024, insertion-order eviction at `onStart`, ratified — bounded state is the honest cost of stream hooks that fire only when the consumer drains (`stream-text.ts` flush/pull; `stream-object.ts` never dispatches `onAbort`). Comment states that conditionality. Test: 1025 abandoned streams → map size 1024, next `generateText` still spans.
- **D315 (E3) — Confirmed.** Per-call `telemetry.integrations` replaces the global array (`create-telemetry-dispatcher.ts:79-83`): README clause plus a D309 handoff sentence for S4.4.
- **M3, S1–S8: proceed.** Constraint on S7: the package test reads ranges from `package.json` and greps README — never a third hand-typed copy (D86 shape).

### Integrator-gate nods (2026-08-30) — D316–D317, BINDING
- **D316 (F1-f1) — (a) RATIFIED, D313's guard wording restated.** The guard exit is "request-half span, no error, swallowed once": the span must open before `create()` so the HTTP child nests (`llm-span.ts:88-107`), and OTel cannot retract a started span. (b) re-parenting refused — unreachable on every measured version. Binding: the `llm-span.ts` comment names the branch as defensive and unreachable today; **README's fail-open sentence says "a span with request attributes only", not "no span"** (integrator checks).
- **D317 (R2-N1) — RATIFIED as the pin's own consequence.** `engines.node ">=22"` follows from `ai@7`'s engines (D303 already recorded it); `name`/`description`/paths are copy cost; D307's "nothing else" reads as "nothing behavioural beyond these". T3's owns amendments (`server.ts` route branch, `sdk-e2e.yml` diagnose loop) recorded, no objection.

**Verdict: CLEARED WITH CONDITIONS.** (1) T1 built per D304 — no channel subscription, wrapper-owned lifetime, `onStart` marker; the plan's original T1 goal text is superseded where it said otherwise (T1 rewritten below). (2) The D306(b) concurrency fix lands in T4. (3) T3's owns extended per D307/D308. (4) The D303 bisect result and chosen range return to the advisor before T1 merges. (5) First-runner numbers recorded against the corrected bases (D306(d), D307).

**Imprecisions the advisor found in the evidence (nine, none void-making) — corrections applied to the tasks below, the measured-facts text left as measured with these riders:** (1) AI7 "unmeasured" — ai@6's inline system message is already pinned at `vercel-ai.test.ts:143-149`. (2) CI4 — `e2e` worst point is 6m44s (run 32269078423), not 6m02s; the 11-run window stopped one short. (3) CI6/CI9 — `sdk-e2e` base 3m46s is the best of the last three (3m46/3m59/4m04); worst-point base is 4m04s. (4) AI11/K7 — the channel adds no reach: its `streamText` store is `AsyncResource`-scoped (`tracing-channel-publisher.ts:196-206`); the wrapper fires for `doStream` with no marker; the marker is `onStart.operationId` (`generate-text-events.ts:32-34`). (5) AI4/AI5 — integration `onError` is operation-level (`generate-text.ts:1564`); `onLanguageModelCallEnd` fires after a throwable `parseToolCall` — decisive for lifetime ownership. (6) AI9 — the edge/workerd gate is moot for a Node-only SDK. (7) AI-series omits `ai@7` `engines.node >=22` (`ai/package.json:75`). (8) CI1/D298 — `cancel-in-progress: true` would cancel master-push runs (`stack.yml:44-46`). (9) K9c — the required-check list is root `README.md:205` (says "eight", names `stack`) and `:132`, not a helm README.

## Kickoff questions for the advisor
- **K1 — the D78 additivity verdict (the packet's critical item 3), both paths.** Measured: (a) `ai@7`: `init()` registers both doors (AI2) with **zero change to any exported name or signature** in `src/index.ts:11-12`, no new option, no env var, no `ai` import; the v5/6 processor and the v7 integration are mutually exclusive by AI1. (b) Responses: a second `InstrumentationNodeModuleFile("openai/resources/responses/responses.js", [">=4.87 <8"], …)` in the existing `files` array (OR9) + a second `LlmShape`; `llm-span.ts` unchanged; below 4.87 the module does not exist → no span, no error (OR1), the D87 honest-absence shape. **Package metadata:** `ai` peer becomes `>=5 <8` (required — CI8); `openai` peer stays `>=4.85 <8` (narrowing to 4.87 would drop measured chat coverage), with Responses documented as `>=4.87` inside it in the README's existing below-floor paragraph shape (`README.md:135-140`). **PM lean: ADDITIVE on both, no escalation; ratify or restate.**
- **K2 — `gen_ai.prompt` and the relocated system prompt (both paths, one ruling).** v7 puts system text in `instructions` beside `messages` (AI7); Responses puts it in `instructions` beside `input` (OR4). Options: (a) serialize `messages`/`input` alone — verbatim, but silently drops the system prompt (a product-visible content gap against D82's "content capture ON and full"); (b) fold `instructions` in as a leading `{role:"system", content}` element of the serialized array — the same wire form the chat path already carries as `messages[0]`, but a synthesized envelope, not verbatim; (c) a new attribute (refused by D81 unless ruled). **Measure first (T1/T2 done-check): does the pinned `ai@6`'s `ai.prompt.messages` string include the system message inline?** If yes, (b) is byte-parity with today; if no, (a) is parity and the gap already exists. PM lean: (b) if ai@6 carried it, else (a) + an S4.4 honest sentence.
- **K3 — `gen_ai.response.finish_reasons` on the Responses path.** No `finish_reason` exists (OR7). Options: (a) `[status]` — provider-native, verbatim, present on every completed call (`completed|incomplete|failed|…`); (b) `[incomplete_details.reason]` when `status === "incomplete"` else `[status]` — closer to `length`/`stop` semantics but a composite; (c) omit (valid — emitted only when truthy, `llm-span.ts:141`). PM lean: (a). **Rule; do not let the executor pick.**
- **K4 — `@ai-sdk/otel` coexistence.** Never a dependency (AI10). Options for the double-span case: (a) a stand-down check — if `globalThis.AI_SDK_TELEMETRY_INTEGRATIONS` already holds an integration whose constructor name is `OpenTelemetry`/`LegacyOpenTelemetry`, obstack's v7 integration logs once and yields; (b) no machinery, a documented bound ("register obstack OR `@ai-sdk/otel`, not both") — the D87 honest-sentence shape; (c) detect + still emit. PM lean: (b) — a check keyed on a third party's class name is a promise obstack cannot keep across their releases; the README sentence is true by construction. Either way the S4.4 docs carry the sentence.
- **K5 — the v7 floor.** Only 7.0.85 measured (AI12). T1 bisects `registerTelemetry` + `ai:telemetry` + the `executeTool` event across the published 7.0.x line (D87 shape). If the floor is 7.0.0, peer `>=5 <8` is exact. If not: (a) `>=5 <7 || >=7.0.N <8` (honest, unusual); (b) `>=5 <8` with the sub-floor stated in README like the pre-4.85 chat paragraph. PM lean: measure, then (b) unless the gap is wide.
- **K6 — streaming stays OUT (D259) — confirm the v7 posture.** `streamText` publishes structurally (AI8). The v7 integration must mirror the `stream:true` chat posture: **no span for stream calls** (never a half-span with zero tokens). T1 measures how a stream call is distinguishable at `onLanguageModelCallStart`/channel `start` (outer type `streamText` vs `generateText` is measured; a per-call flag is not). AI8's cost correction (usage/text are available on `onEnd` for streams) is recorded as an M5-packet fact. Ratify.
- **K7 — which door carries what.** PM proposal: the **`globalThis` integration carries the data** (portable, not runtime-gated — AI9) and **the channel carries only the nesting** (Node-only) — where the channel is absent, llm spans still emit, parented on the ambient OTel context. Ratify or restate. Sub-item: obstack keeps its own `AsyncLocalStorage` via `channel.start.bindStore` (AI11); the join to a surrounding `traceAgent` span is T1's red-then-green done-check.
- **K8 — auto tool spans from `executeTool` (AI6): IN or OUT?** New capability, not parity: today the agent/tool layers come from `traceAgent`/`traceTool` (D78). PM lean: **OUT of S4.3** — the exit line is parity on `ai@7`, and a v7-only auto layer would make the two peer lines behave differently; recorded as an M5-packet item with the measured payload.
- **K9 — D298 implementation, four measured corrections.** (a) The honest path filter is CI2's true set including `apps/web/**` — ratify it, and record that the reclaim comes from the required-set drop, not the filter; (b) `push: branches: [master]` is a **new** trigger with zero history — the re-based `stack` watch starts from nothing; (c) the "required set" edit is `stack.yml`'s header (`:29-33`) + the merge-process line, no API (CI3); (d) **`e2e`'s worst measured point is 6m02s** (CI4) — the guard move adds +4s (worst-point projection 6m06s). Options: re-base the ~6m line to the measured band's top, or hold the line and let **D207's pre-authorized reclaim execute on the first runner number** if it crosses. PM lean: hold the line; D207 is already the pre-authorized first response, "executes immediately without a new ruling, on the measured number". Also (e) `stack` post-move ≈6m56, still over K5, accepted by D298 — restated here so the number is not read as a regression.
- **K10 — the sample leg's home and shape.** The JS sample lives in `sdk-e2e`, not `e2e` (CI6 — the S4.2 bundle's "sample-app leg" was silent on the job; this corrects the manager's own brief). PM recommendation CI9 **(i) + (ii)**: `demo/sdk-sample-ts-ai7/` (exact pins `ai 7.0.85`, `@ai-sdk/openai 4.0.52`, `openai` at the same pin as the sibling, D76) as a third service under the `sdk` compose profile + its leg in `sdk-evidence.sh` (four layers + D8 set + D92 on the v7 sample; OTLP + fail-open legs: **in or out?** PM lean: in — the fail-open leg is the D26-class promise and costs 24s), budget `sdk-e2e` → ~4m41s–5m05s worst-point (30-min timeout); plus the alias matrix in `packages/obstack-js` (first npm alias in the repo — state it in the `//devDependencies` note) so the version-specific literals are pinned at unit level in `sdk-js` (~+10s). Ratify.
- **K11 — Responses scope edges and the sample.** `client.beta.responses` (separate class, 6.x+) OUT; `compact()` and `retrieve(id,{stream:true})` (produce `Response` objects without `create`) OUT; `parse()` covered free (OR3). **Does the sample gain one `responses.create` call** so the D259 "at launch" claim has CI-driven evidence (cost ≈ one request, seconds), or does unit-level evidence suffice? PM lean: the v6 sample (`sdk-sample-ts`, already on `openai 7.4.0`) gains one Responses call and `sdk-checks.ts` asserts its llm span — the claim then rides the existing leg.
- **K12 — the D103(a) triage record.** Streaming + JS logging bridge OUT (D259); Responses IN (this sprint). S4.3 records for the M5 packet: AI8 (streaming is now cheap on v7's integration callbacks); and hands S4.4 the honest sentences: v7 telemetry on-by-default vs v5/6 `experimental_telemetry` opt-in (AI3), Responses `>=4.87`, the `@ai-sdk/otel` bound (K4), streaming/logging-bridge absence. No code. Ratify as the sprint's triage outcome.

## Review record
- **R1 (obstack-js: T1 + T2, commits `6e1ee72` `4b55ce9` `1b42d23`; Opus, read-only) — verdict MUST-FIX.** Frozen files byte-unchanged (verified); red-then-green re-verified in a scratch copy (8/1 → 9/0); packaging clean (no `ai7`/`declare global` in `dist/`, lockfile adds only dev entries); `npm test` 59/59. **M1 — `shareOneParse` is a no-op on `openai` 7.5.0–7.8.0:** from 7.5.0 `client.js:508` installs a per-instance `_thenUnwrap` that calls a closed-over `parse`, never `this.parseResponse`, so an instrumented `responses.parse()` throws `Body is unusable` on 7.5–7.8 (R1's own repro: A uninstrumented OK / B observer no-fix THROWS / C with-fix OK on 4.87.0, 5.23.2, 7.4.0 — and **THROWS on 7.8.0**). Inside the advertised peer range; invisible to CI because the lockfile pins 7.4.0 under `^7.4.0`. `openai.ts:133-135`, README:122 and the `//peerDependencies` "checked on … 7.8.0" sentence are false as written. **M2 — the v7 `Map` grows unbounded:** for `streamText`/`streamObject` the terminal hooks fire inside `TransformStream.flush`/`pull` (`stream-text.ts:1468,1571-1607`; `stream-object.ts:891` dispatches no `onAbort`), so an abandoned stream (routine on client disconnect) leaves its entry forever; reproduced on `ai@7.0.85`; the `:139-145` "measured: exactly one always fires" comment is false. **M3 — README:217 "the whole major is covered"** is the phrasing D310 forbids. Should-fixes S1–S8: `init()`'s v7 registration untested (deleting `init.ts:77` keeps 59/59); `responses.stream()` refusal untested; README's "produces no span" pinned by nothing (assert total span count); `executeLanguageModelCall` is the one hook `ai` does not error-isolate — say the try/catch is load-bearing; the `ai.streamObject` arm is unreachable (`generateObject`/`streamObject` call the model directly); per-call `telemetry:{integrations:[…]}` REPLACES the global array so obstack draws nothing for such calls — README needs the clause; README coverage ranges pinned by no test; the fence says "either SDK" while naming three. Notes: wrapper is inside `retry()` (one span per attempt, verified); `execute()` resolves to the raw `LanguageModelV4GenerateResult` (verified); `generateObject` draws no llm span on either leg (symmetric). → **Escalation round to the advisor (M1 mechanism, M2 fail-safe restatement of D304) — ruled D313–D315; fix executor F1 dispatched after T3 landed.** **F1 DONE @ `26b0ee4` (D313) + `1649569` (D314, M3, S1–S8): package suite 68/68, root 408/0, typecheck/build clean, `dist/` clean, lockfile +19 all dev.** D313 as shipped: `shareOneParse` deleted; the Responses patch passes an `observeResponsesBody` to `traceLlmCall`, which registers on `p.responsePromise` synchronously before `p` returns and parses `props.response.clone().json()` in the first reaction; guards (`responsePromise` not thenable / `clone` absent / non-JSON) all `swallowed()` once with the call untouched; `responsePromise` verified on 4.87.0 (`core.js:77`), 7.4.0, 7.8.0; the 7.8.0 `client.js:501-509` closure verified. `llm-span.ts` plumbing: one optional 6th `observe?` parameter + a local `Observe` type, the thenable branch generalised — no shape-type field; `index.ts`/`attributes.ts`/`helpers.ts` byte-unchanged vs `ca9e386`. A/B/C: A green both versions with the patch lifted; B documented with both historical reds (observe-the-promise 8/3; `shareOneParse` on 7.8.0 6/2); C green on 4.87.0 AND 7.8.0 — the 4.87.0 leg is carried by a test-time scratch copy of the alias into `node_modules/.obstack-openai487/node_modules/openai` (require-in-the-middle matches the module name from the PATH, so `node_modules/openai487/…` is never patched — measured); fixture carries both `type:"response"` (4.87.0's `addOutputText` trigger) and `object:"response"` (5+). D314: bound 1024, eviction red-first (`1025 abandoned streams left 1025 entries` → ok), missing entry → `execute()` untouched. M3 D310 wording; S1 red-first (`init.ts:84` commented → red); S2 `.stream()`; S3 total count; S4/S5 docs; S6/D315 clause + an extra pin test (per-call `integrations` → obstack draws nothing); S7 `package.test.ts` (README carries each peer range verbatim, every table row names a declared peer); S8 "for any of the three" + negative. **Decision flagged for the advisor (F1-f1):** the D313 guard cannot yield literally "no span" — the span starts before `create()` and OTel cannot retract it — so the guard takes the file's existing fail-open exit (request-half-only span, `swallowed()` once, value untouched); defensive path only, unreachable on 4.87–7.8. Batched with R2-N1 (`engines.node >=22`) for the integrator-gate nod → ruled D316/D317.
- **R1 re-verify (after F1; Opus, read-only) — verdict APPROVE WITH SHOULD-FIXES.** Rebuilt `dist` from `git archive HEAD`, re-ran its own A/B/C repro: **C passes on 4.87.0, 5.23.2, 7.4.0 and 7.8.0** — `create()`, `parse()`, `withResponse()` each succeed with exactly one span carrying the full D8 set (`finish=["completed"]`, in=3 out=2); no double span. Frozen files empty diff vs `ca9e386`; `llm-span.ts` adds only the `Observe` alias + an optional 6th param — no shape-type field. D314 test is real (live registry instance, 1025 abandoned streams → 1024, next `generateText` spans) plus the unseen-`callId` and D315 pins; M3 gone; S1–S8 as specified; S7's `package.test.ts` checks README↔`package.json` in both directions. The 4.87.0 scratch-copy mechanism is honest and CI-safe (RITM names the module from the segment after the LAST `node_modules/`; `npm ci` wipes and the test recreates; `.gitignore:4` covers it; `.package-lock.json` untouched). Note: `Response.clone()` tees the body for the span's life — bounded, documented. **SHOULD-FIX (F3, before merge):** S-a `responses.test.ts:434` guards the major only (7.4.0 would pass — the exact blindness this round proved) → require `7.x, x ≥ 5`; S-b the D316 request-half branch is unreached by any test → force `observe` to return `undefined` (a stub `create` with no `responsePromise`) and assert request-half-only span + app value untouched, plus the README clause (D316 wording); S-c `loadOpenAI487` gates on `package.json` existing — an interrupted `cpSync` leaves a broken cached copy → copy to a temp sibling + `renameSync`, or a marker written last; S-d `openai.ts:295` says the client decorates on `object:"response"` — on 4.87.0 the trigger is `rsp.type === "response"` (`responses.js:41`), one clause.
- **R2 (CI shape + sample leg: T4 + T3, commits `7540a18` `25a2540` `43a56ca` `54f851c` `ca9e386`; Opus, read-only) — verdict MUST-FIX, prose only; mechanism sound.** Verified: guard byte-identical after the move (diff empty); `actionlint`/`helm lint`/`docker compose config`/`bash -n` clean; the filter set re-derived from `acceptance.sh:100-117` + `apps/web/Dockerfile` + workspaces — no gap (`packages/obstack-js/src/**` correctly outside it: the demo agent is Python); guard preconditions on the tree (`npm ci` :120 → compose `--wait` with postgres :134 → guard :164 → smoke :174), `e2e` pins node 24 so the node-22 `BoundPool` crash does not apply; the Responses discriminator is real (chat route returns `stop`, both `ai` lines normalise to `stop`, so `completed` selects exactly the Responses span); the twin's leg selects its own trace (`service=` exact), probe names/ports unique, freeness loop covers nine ports; BuildKit pack-stage lines 12–19 byte-identical; D76 pins exact, samples ship no lockfile, `sdk-js` node 24 ≥ ai@7's ≥22, no `engine-strict`; `cancel-in-progress` expression form valid at workflow level. **MUST-FIX (false sentences):** `deploy/compose/README.md:376` "a standing `stack` step (D122)" — the guard is in `e2e` now (the `ddl-drift-check.mts` fix missed the human copy); root `README.md:105` "The two sample apps are the proof" — there are three. **SHOULD-FIX:** root `README.md:133` (`e2e` "provisionally, pending the advisor's ruling" — D306(d) has ruled; state band/held line/D207), `:137` (`sdk-e2e` "~3m43s" measured a two-sample job — add the 4m04s worst point + projection), `sdk-e2e.yml:28-38` ("measured" attached to the 45s allowance — CI9 measured 38s; with OTLP+fail-open IN the projection is a single ~5m23s, 4m59s is the without-legs figure), `stack.yml:72-74` (≈6m56 is 7m06−10s, the S4.2 PR run, not the list's last 7m02), `stack.yml:100-101` ("master runs never are superseded" overclaims — GitHub cancels a PENDING run when a newer one queues into the group; in-progress master runs are never cancelled, the pending-supersede case must be named), `demo/sdk-sample-ts/README.md:96` ("three different patches" → mechanisms). **NOTES:** N1 the twin's third difference `engines.node >=22` (enumerated, consequence of the pin) — advisor ratification line owed; N2 `sdk-e2e.yml`'s diagnose loop gained the twin (necessary) — **owns amendment ratified by the manager**; **N3 root `README.md:86` still `ai >=5 <7`, no Responses row — false once T1/T2 ship, fixed in F2**; N4 `:203` "advisory" soft-false for `stack` (caveat two paragraphs up); N5 `sdk-checks.ts` cast unreachable; N6 `deploy/**` over-broad by `deploy/compose/**` (safe over-fire; header to say so); N7 port-freeness excludes 5432 (recorded). → **F2 DONE @ `5614723`** (5 files, prose only, actionlint clean): the nine edits landed as specified — the guard's new home in `deploy/compose/README.md`, three samples in root README `:105` (plus the same paragraph's "both samples" → "all three", verified against `sdk-evidence.sh`), root README coverage rows now `ai >=5 <8` with the split and Responses from 4.87 (mirroring the package README, D310 wording), the `e2e` bullet on the ruled band/held line/D207, the `sdk-e2e` bullet's two-sample rider + ~5m23s projection, `sdk-e2e.yml` header ("38s measured, 45s allowance", single ~5m23s), `stack.yml` header (≈6m56 paired with the S4.2 run's 7m06; in-progress-never-cancelled vs pending-supersede stated; `deploy/**` over-cover clause), "mechanisms". **Still owed (post-F1, integrator pass):** root `README.md:144` `sdk-js` bullet "42 tests / 11.8s" (stale — final count after F1); R2 N4 "advisory" wording near `:210`.

- **Integrator (Opus@xhigh, rebuilt tree from a fresh `npm ci`, compose Postgres on host 15432 via a throwaway override because a foreign container holds 5432 — committed files byte-unchanged) — verdict FIXED-GO @ `a260568`.** Static: actionlint/helm lint/compose config/typecheck/eslint clean; `services/` and `deploy/helm` untouched (`git diff --stat` empty → go and `acceptance.sh` are CI's legs). Package: `npm ci` 1m08s resolves `openai@7.8.0`, `ai7@7.0.85`, `openai487@4.87.0`; suite **68/68 twice** (scratch copy idempotent); typecheck/build clean. Web: root `npm test` 408/355/0 without DSNs and **463/463/0 with CI's DSNs live** (the K4 skip trap satisfied); `next build` 36/36 pages. `sdk-evidence.sh` from `down -v`: **PASS 43/0 in 195s** — twin's four layers, D8, D92 both halves, zero `ai.*` with the v6 positive control, OTLP + fail-open legs, the Responses span `finish_reasons ["completed"] · 62+30 tokens`; both sample images carry F1's `dist` (`responsePromise` ×3, `shareOneParse` ×0 — the pack stage is content-addressed, no `--no-cache` needed). `e2e` mirror: compose up 18s → **guard PASS on node 24** (`15 statement(s) … identical`, 6s) → `smoke.sh` PASS (api=2 agent=1 tool=1 llm=1) → drive **166/0 in 166s**. Seam: the v6 and v7 `POST /chat` traces classify identically in ClickHouse (`api=1 agent=1 tool=1 llm=3`, same names; the v6 trace's one extra `other` span is `ai@6`'s own `ai.generateText` wrapper, absent on v7 by AI1) — the exit line literally. D299 diff empty; fence renders; D13 sweep clean (the one `>=5 <7` hit is the README's deliberate mechanism-split row beside the `>=7 <8` row, with the declared `>=5 <8` interval stated and test-pinned). **Slips fixed:** `94d3e69` root README `sdk-js` bullet (68 tests / 17.5s measured locally) + R2-N4 scoped to the nine; `d7eb83d` D316 wording in the package README ("request attributes only — no completion, no token counts") with the defensive/unreachable clause placed in `openai.ts` beside the three-version measurement (the `llm-span.ts` branch is shared with the chat/Anthropic non-promise exit, where "unreachable" would be false — correctly not said there); `a260568` `sdk-js.yml` header's stale "11.8s over 42 tests" (missed by the record — found by `git grep` over tracked files). **Correction to CI5, recorded:** on node 22 the guard crashes BEFORE the diff (`57P01` at the `DROP … WITH (FORCE)`), not after — a node-22 run proves nothing; `e2e.yml` pins node 24 where it passes. Teardown clean, 0 dangling images, foreign containers untouched.

## Critical items
<!-- Per phase plan §"Verdict and gates" item 3, these return to the advisor before merge. -->
- **The D78 additivity verdict on both paths (K1) — recorded with the measured grounds attached (AI1/AI2/OR1/OR9); any executor finding that a non-additive change is needed STOPS the task and escalates before the change** (D258's own clause)
- T1 — the v7 integration with the ambient-context join proven red-then-green (AI11), the stream posture (K6), the measured floor (K5), the K2 prompt decision applied and measured against the pinned `ai@6`
- T2 — the Responses patch with the K2/K3 decisions applied; `stream`/`parse` behaviour pinned by test (OR3)
- T3 — the sample leg on `ai@7` producing all four layers through the real compose stack; first-runner `sdk-e2e`/`sdk-js` numbers against the CI9 budget
- T4 — D298 implemented with the CI2 true filter set; the guard demonstrably running in `e2e` (log line); first-runner `e2e` number against the 6m line (D207 executes on its own terms if crossed); `stack`'s first master-push number opens the re-based watch
- The D103(a) triage outcome (K12) and the D178 non-fire (CI10), recorded in the exit bundle

## Tasks

### T1: `ai@7` integration in obstack-js (D258) — the diagnostics-channel / integration-registry path
- status: pending
- wave: 1
- depends: — (K1–K2, K5–K8 rulings applied)
- owns: packages/obstack-js/src/vercel-ai-v7.ts (new), packages/obstack-js/src/vercel-ai-v7.test.ts (new), packages/obstack-js/src/init.ts (registration lines only), packages/obstack-js/src/vercel-ai.ts (doc comment naming the two paths only — the v5/6 translation is untouched), packages/obstack-js/src/attributes.ts (only if a name is ruled), packages/obstack-js/package.json (peer range, alias devDep, the two `//` notes), packages/obstack-js/README.md ("What is actually covered" rows + the v7 paragraph), package-lock.json (the alias resolution)
- goal (**as ruled by D299/D300/D302/D303/D304**): `init()` (D78-frozen, signature untouched) registers ONE obstack `Telemetry` integration by pushing onto `globalThis.AI_SDK_TELEMETRY_INTEGRATIONS` inside the existing try block (`init.ts:41-74`) — **the only door; the `ai:telemetry` channel is NOT subscribed; no obstack-owned `AsyncLocalStorage`; no `ai` import (local structural types for the hooks implemented), no env var, no new option**. Hooks, exactly these (D304): `onStart` records `operationId` by `callId` in a `Map`; `executeLanguageModelCall({provider, modelId, instructions, messages, callId, execute})` owns the span; `onEnd`/`onError`/`onAbort` delete the map entry. Wrapper: if the recorded `operationId` for `callId` starts with `ai.stream` → `return execute()` untouched (stream calls: no span, K6); otherwise `startSpan("chat <modelId>", CLIENT, {gen_ai.system ← bareProvider(provider) (exported from `vercel-ai.ts:163-166`, shared not copied), gen_ai.request.model ← modelId, gen_ai.prompt per D300: JSON array, `instructions` folded in as the LEADING `{role:"system",content}` element, `messages` serialized untouched}, context.active())`, run `execute()` inside `context.with(trace.setSpan(...))`; rejection → `recordFailure` + end + rethrow untouched (`fail-open.ts:25-35`); resolution → `gen_ai.response.model ← result.response?.modelId ?? modelId`, `gen_ai.completion ← result.content[type==="text"].text` joined, `gen_ai.usage.input_tokens/output_tokens ← result.usage.inputTokens.total / outputTokens.total`, `gen_ai.response.finish_reasons ← [result.finishReason.unified]`, then end. Each retry attempt is one span. `telemetry:{isEnabled:false}` is ai's own gate. No `ai.*` attributes exist on v7 spans — D92 strip N/A, stated in the doc comment. **Never depends on `@ai-sdk/otel` (D302).** Peer `ai` → `>=5 <8` if the D303 bisect floor is 7.0.0, else `>=5 <7 || >=7.0.N <8` (D303 — dead sub-range EXCLUDED); the `//peerDependencies` note carries the bisect record and `ai@7`'s `engines.node >=22` (obstack-js `engines` stays `>=20`); the `//devDependencies` note states the alias `ai7: npm:ai@7.0.85` is the first npm alias in the repo. README: v7 telemetry is on by default (no `experimental_telemetry` needed) with the opt-out named; v5/6 unchanged; the D302 `@ai-sdk/otel` sentence; streaming sentence (no span, D259); node ≥22 for ai@7; the floor paragraph in the D87 shape. **Escalation triggers (STOP, do not change):** any diff to `src/index.ts`, any new `init()` option/arg/env var, any new name in `attributes.ts`, any field added to `LlmShape`/`LlmRequest`/`LlmResponse`; `onStart.callId` ≠ wrapper `callId` on any version in the D303 range.
- done-check: `npm test --workspace packages/obstack-js` green: the new suite drives the REAL `ai@7.0.85` (alias) with a stub `LanguageModelV4` model (the `vercel-ai.test.ts:83` pattern) — (1) `generateText` with `system` + `prompt` → exactly one llm span, full D8 set asserted value-by-value against the stub's known usage/finishReason/model, `gen_ai.prompt` beginning with the `system` element (D300); (2) a 2-step tool round inside `traceAgent` → two llm spans, both children of the `traceAgent` span, **the join proven red-then-green** (red = parent forced to `ROOT_CONTEXT`, green = `context.active()`; both states cited in the test's comment/commit); (3) `streamText` → zero spans and the stream still consumed; (4) `telemetry:{isEnabled:false}` → zero spans; (5) a throwing model → span ended with ERROR status and the error rethrown untouched; (6) the existing `vercel-ai.test.ts` on `ai@6` unchanged and green — both paths live in one process with no double span; (7) D300 parity: the v7 `gen_ai.prompt` string for the same system+user prompt equals the v6 form (`vercel-ai.test.ts:143-149`) modulo ai's message normalization, recorded; (8) the D303 bisect executed over the published 7.0.x line (four predicates) with the floor written into `package.json`/README and **the result reported back for the advisor's pre-merge check**. `npm run typecheck` + `npm run build` clean.
- result: DONE @ `6e1ee72`. **D303 bisect: floor = 7.0.0** — 19 of the 80 published 7.0.x releases exercised (log2-spaced + every registry-gap neighbour: 7.0.0/.1/.5/.11/.21/.23/.25/.31/.42/.51/.52/.54/.61/.66/.71/.73/.79/.82/.85), all four predicates pass on every one, no dead sub-range → peer **`"ai": ">=5 <8"`**, single interval; bisect record + `ai@7` `engines.node >=22` in the `//peerDependencies` note, obstack-js `engines` stays `>=20`. **Escalation triggers checked, none fired:** `onStart.callId === wrapper.callId` on every version; `git diff 98d2fcf HEAD` shows `src/index.ts`, `attributes.ts`, `instrumentation/**` byte-unchanged. `vercel-ai-v7.ts` (380 lines) is the one-door D304 shape: registry push inside `init.ts`'s try block, `Map` keyed by `callId`, wrapper-owned span, `"ai.stream"` prefix → untouched `execute()`. **Red-then-green proven:** with the parent forced to `ROOT_CONTEXT` 8/9 pass and only the join fails (`actual undefined / expected '2376c0b137e9d725'` — the call succeeds and every D8 assertion passes, which is why the join needed its own falsification); with `context.active()` 9/9; the test carries a `PROVEN RED FIRST` comment. Package suite **51/51** (42 pre-existing incl. `vercel-ai.test.ts` untouched, both legs registered on one provider, no double span); typecheck/build clean, no `declare global` in the shipped `.d.ts`, no `ai7` reference in `dist/`. **D300 parity measured:** element `[0]` `{"role":"system","content":…}` byte-equal v6↔v7 (the half D300 exists for); the user turn differs by ai's own normalisation (v6 `ai.prompt.messages` is the converted provider prompt `content:[{type:"text",…}]`, v7's event is the standardised prompt `content:"…"`) — both passed through untouched, pinned in the test so either moving goes red. **Decisions recorded, ratified by the manager:** missing/blank `modelId` → no span, run uninstrumented (same fail-open exit as streaming); `asRecord`/`asCount` reused read-only from `llm-span.ts`; completion join separator `""` (measured from ai's `extract-text-content.ts`, equals `result.text`); the global reached by cast, no `declare global` (would collide with ai's own); stream marker is a prefix test so `ai.streamObject` and future stream entry points default to no span. **Root `npm test` RED by design — 1 fail:** `apps/web/src/components/onboarding/Quickstart.test.ts:245` (the D88/D101 fence guard) fires because the peer range moved and `Quickstart.tsx:347` still says "ai >=5 <7 — ai@7 is not yet supported" — now a false sentence on the launch surface this sprint exists for. Executor correctly declined to edit outside its owns; **manager ruling: owns amendment to T2** (the fence sentence + its assertion; T2 rewrites the openai fence for Responses in the same sentence). Recorded for the advisor's D303 pre-merge check together with the bisect.

### T2: OpenAI Responses API instrumentation (D259)
- status: pending
- wave: 1
- depends: — (K1–K3, K11 rulings applied; parallel-safe with T1 — disjoint files except `README.md`/`package.json` prose, sequenced after T1 by the manager)
- owns: packages/obstack-js/src/instrumentation/openai.ts, packages/obstack-js/src/instrumentation/responses.test.ts (new), packages/obstack-js/README.md (Responses rows/paragraph), packages/obstack-js/package.json (`//peerDependencies` sentence only), **apps/web/src/components/onboarding/Quickstart.tsx (the version-fence sentence at `:347` only — owns amendment after T1: the `ai` fence becomes `>=5 <8` with the v7 on-by-default / v5-6 opt-in split named, the `openai` fence gains "Responses API >=4.87", the streaming sentence stays true) and apps/web/src/components/onboarding/Quickstart.test.ts (the `:245` fence-guard assertions reconciled to the new truth — the guard stays, its literals move)**
- goal: a second `InstrumentationNodeModuleFile("openai/resources/responses/responses.js", [">=4.87 <8"], onPatch, onUnpatch)` in the existing `files` array (OR9) wrapping `Responses.prototype.create` with a `swallowed(...)` bail when the class moves, and a `RESPONSES_SHAPE: LlmShape` beside `OPENAI_SHAPE`: request → `undefined` when `body.stream === true` (covers `.stream()` too, OR3), else `gen_ai.system="openai"`, `gen_ai.request.model` ← `body.model` when `typeof === "string"` (optional on 7.8.0, OR4), `gen_ai.prompt` per K2 over `body.input` (string or array, JSON-stringified verbatim); response → `gen_ai.response.model` ← `body.model`, `gen_ai.completion` ← `output_text` when a string **else** walk `output[]` for `type==="message"` → `content[]` `type==="output_text"` → `.text` joined, `""` otherwise (OR6 — `output_text` is client-side decoration, not trusted alone), `gen_ai.usage.input_tokens/output_tokens` ← `usage.input_tokens/output_tokens` (guarded — `usage` is optional in the type, OR4), `gen_ai.response.finish_reasons` per K3. Span name stays `chat <model>` (OR8: classification is by any `gen_ai.*`, no new constant; `cached_tokens`/`reasoning_tokens` are NOT given names — D81). `llm-span.ts` and `src/index.ts` untouched. Peer `openai` stays `>=4.85 <8`; README gains the "Responses below 4.87: no span, no error" paragraph in the `:135-140` shape and the `//peerDependencies` note gains the OR1 measurement sentence. `beta.responses`, `compact()`, `retrieve(…,{stream:true})` OUT (K11), stated in the README coverage table.
- done-check: `responses.test.ts` green against the existing `fakeProvider` harness (no harness change) with the real `openai@7.x` devDep loaded inside `before()`: (1) `responses.create({model,input})` → exactly one span, D8 set value-by-value, canned body carries `object:"response"` so the real `addOutputText` path runs; (2) a `function_call`-only body → `gen_ai.completion === ""` and the K3 finish value; (3) a body with `object` omitted → completion from the `output[]` walk; (4) `stream:true` → zero spans, the request demonstrably left the client (the `openai.test.ts:105-129` shape); (5) `.parse()` → exactly one span; (6) the chat suite unchanged and green; `typecheck`/`build` clean. Measurement statement in README that 4.85–4.86 produce no span and no error (OR1), cited as measured on 4.85.4/4.86.2.
- result: DONE @ `4b55ce9` (instrumentation) + `1b42d23` (quickstart fence, D311). Resolved devDep `openai@7.4.0`, `parse` present and dispatching through `create` (OR3) — pinned counted-once. Package suite **59/59** (+8); typecheck/build clean; **root `npm test` 408 / 0 fail** (back from T1's designed red). `/v1/responses` evidence from the real client: `withResponse()` URL assertion + the wire body deep-equal in `requests[0]`; three `fakeProvider` instances, no harness change. `git diff 6e1ee72 HEAD` on `index.ts`/`attributes.ts`/`llm-span.ts` empty; no escalation trigger fired. **Fence:** the one `<p>` now reads `openai >=4.85 <8 (the Responses API from 4.87) · @anthropic-ai/sdk >=0.50 <1 · ai >=5 <8 — on ai@7 telemetry is on by default, on ai@5 and 6 pass experimental_telemetry: { isEnabled: true }; streaming calls are not instrumented in either SDK`; every other quickstart word byte-identical. Guard per D311: the hand-typed range literal DELETED, the existing loop over `Object.entries(jsPackage.peerDependencies)` is the only range check; wording claims asserted separately plus a negative on "not yet supported"; falsified both ways (fence range drift / Responses floor moved → red alone). **Real bug found red-first by the `parse()` test, fixed inside the owned file:** `responses.create()` returns a derived `APIPromise` (unwraps to attach `output_text`) and `parse()` derives a second; `_thenUnwrap` calls the parent's `parseResponse` directly, bypassing the memo, so with an observer's `.then` as first reader the app's `parse()` threw `TypeError: Body is unusable` (identical code 4.87.0 → 7.x; chat path unaffected — nothing derived). `shareOneParse` memoises `parseResponse` on the promise before anything observes it (try/catch + `swallowed()`, no-op if absent), applied only to the Responses patch (an `openai` quirk, not an LLM-span fact — kept out of `llm-span.ts`). **Reviewer attention item:** this touches the client's promise internals — verify it holds on 4.87.0 and 7.8.0 by test or measurement. Two judgement calls following precedent, ratified by the manager: no string `model` (legal on 7.x via stored `prompt:{id}`) → no span, uninstrumented (T1/D312's blank-`modelId` exit); `output[]` walk joins with `""` (matches `addOutputText`). Handed to T3: the `/v1/responses` fake route must carry `object:"response"` and a string `status`, or the span has no `finish_reasons` for `sdk-checks.ts` to select on.

### T3: the `ai@7` sample leg — four layers through the real stack, in `sdk-e2e`
- status: pending
- wave: 2
- depends: T1, T2 (the tarball the sample installs must carry both)
- owns: demo/sdk-sample-ts-ai7/** (new — a twin of `demo/sdk-sample-ts` on exact pins `ai 7.0.85`, `@ai-sdk/openai 4.0.52`, `openai 7.4.0`; D307), deploy/compose/docker-compose.yml (the `sdk` profile: one new service), deploy/compose/sdk-evidence.sh (the v7 leg), deploy/compose/sdk-checks.ts (v7 assertions + the D308 Responses assertion), deploy/compose/README.md (`:405`, the sdk services list — D307), demo/sdk-sample-ts/src/agent.ts (the one `responses.create` call — D308), demo/sdk-sample-ts/src/fake-openai.ts (the `/v1/responses` route — D308), .github/workflows/sdk-e2e.yml (header comment + budget line only — no job rename, K3)
- goal (**as ruled by D307/D308**): D250's demoable line made literal — "an `ai@7` sample produces the four-layer trace". The twin is a copy of the sibling whose differences are enumerated in its `//` note and are EXACTLY: the three pins, and `generateText` with **no telemetry option** (AI3 — the on-by-default truth is the thing being proven); nothing else. Base image stays `node:22.23.1-slim` (`Dockerfile:12,21`); the pack stage byte-identical so BuildKit dedupes. `sdk-evidence.sh` boots it under the `sdk` profile beside the existing two, sends the same requests, selects its trace and asserts all four layers, the D8 llm set and the D92 perimeter (no `ai.*` keys — trivially true on v7, asserted anyway), **plus the OTLP and fail-open legs for the twin (IN, D307)**. The v6 sample gains one `responses.create` call; `fake-openai.ts` gains a `/v1/responses` route answering `object:"response"`, `status:"completed"`, `model` gpt-4o-mini, non-zero `usage`, one `output[]` message (so every llm span stays "complete" under `sdk-evidence.sh:233-235`); `sdk-checks.ts` identifies the Responses span as the llm span with `finish_reason === "completed"` (D301) and asserts its full D8 set. Compose build reuses the shared `obstack-js` pack stage (CI9: 38s cached). Budget recorded in the workflow header against the corrected base: `sdk-e2e` worst point **4m04s** → worst-point ~4m59s–5m23s (D307), under 6m.
- done-check: `deploy/compose/sdk-evidence.sh` passes locally for all samples from `down -v`; the v7 leg's assertion count and the trace id cited; the Responses span asserted on the v6 sample; CI `sdk-e2e` green with its wall-clock recorded here against 4m59–5m23; `sdk-js` green with the alias matrix time recorded
- result: DONE @ `43a56ca` + `54f851c` + `ca9e386`; **CI (PR #23): `sdk-e2e` 4m40s (evidence script 242s, +49s for the twin, under the ~5m23s projection); `sdk-js` 1m06s.** Locally: **`sdk-evidence.sh` from `down -v`: PASS (110s), 43 passed / 0 failed**; twin trace `b662141e…` — ≥4 spans under one trace_id, all four layers, every llm span with the full D8 set and non-zero cost, prompt/completion in the dedicated columns, D92 both halves, **zero `ai.*` keys on v7 with the v6 trace as positive control**, the stock-collector OTLP leg and the dead-port fail-open leg both green; facade: `POST · 80.3ms · 6 spans (api=1 agent=1 tool=1 llm=3)`; the Responses span on the v6 sample: `chat gpt-4o-mini · gen_ai.system openai · finish_reasons ["completed"] · 62+30 tokens`. Twin image build **32.4s** with the shared pack stage `CACHED` (under CI9's 45s allowance). `diff -r` twin vs sibling: `config.ts`/`server.ts`/`fake-openai.ts`/`tsconfig.json` byte-identical; differences are exactly the `//twin` note's — the pins (`ai 7.0.85`, `@ai-sdk/openai 4.0.52`, `openai 7.4.0` unchanged), `engines.node >=22` (ai@7's own floor, enumerated), the deleted `experimental_telemetry` line, and copy-cost prose/paths. **The `ai@7` API needed no code change** (`createOpenAI(...).chat(model)` + `generateText({model,system,prompt}).text` unchanged from v6 — measured on 7.0.85 before writing the twin). Compose: `sdk-sample-ts-ai7` under the `sdk` profile, container port 8100 like its sibling (keeps `config.ts` identical), host `127.0.0.1:8110`. `docker compose config` valid; `actionlint` clean; root `npm test` 408/0; `sdk-checks.ts` typechecked manually against `apps/web`'s compiler options (it is not typechecked by any repo script — recorded). **Owns amendments ratified by the manager:** `demo/sdk-sample-ts/src/server.ts` (the route table lives there — one branch for `/v1/responses`); `demo/sdk-sample-ts/src/main.ts` + `README.md` ("two llm spans" prose would have been false). **Recorded caveats:** the local run needed a temporary, reverted host-port rebind for compose Postgres (a foreign container on this host holds 5432; nothing in the harness touches host Postgres; the committed file is unchanged; CI closes the gap); the harness's port-freeness refusal covers its nine HTTP ports but not 5432 (pre-existing, not fixed — S4.4/M5 note).

### T4: D298 — the CI shape, implemented
- status: pending
- wave: 2
- depends: — (K9 rulings applied; parallel-safe with T3, sequenced by the manager)
- owns: .github/workflows/stack.yml (triggers + concurrency, header `:15-33`, delete `:161-162`, `:170-174`, `:187`, `:189-191`), .github/workflows/e2e.yml (one added step immediately after the Compose-stack step `:113`, before smoke; header comment), README.md (root — `:132` the stack trigger sentence and `:205` the check list; prose only — D306(c))
- goal (**as ruled by D306**): D298 with the CI2 true filter set: `on: pull_request: branches:[master] paths:[.github/workflows/stack.yml, deploy/**, services/ingest/**, demo/agent-app/**, apps/web/**, packages/obstack-js/package.json, package.json, package-lock.json]`, `workflow_dispatch`, **`push: branches:[master]`** (new, CI1); **concurrency: `cancel-in-progress: ${{ github.event_name == 'pull_request' }}`** so a second merge never cancels the first's master run (D306(b), binding). Header rewritten: `stack` is **not in the required set**; the reclaim comes from that drop, not the filter (D306(a)); D41's rider (a red `stack` on master is stop-the-line) is the primary backstop; the `ci:stack` label sentence deleted (superseded, D298); the watch line re-based to master pushes, opening on the first master-push number, PR-run prior band 5m58–7m17; post-move ≈6m56 stays over K5 by D298's acceptance (D306(e)). The D122 guard step moves to `e2e.yml` verbatim (`stack.yml:170-174` → immediately after `e2e.yml:113`, before the smoke floor, `BETTER_AUTH_SECRET` literal verbatim from `stack.yml:172`), and its Start/Stop Postgres riders + diagnose line leave `stack` (CI5); the chart-Postgres schema step `stack.yml:140-143` stays. `e2e.yml` header: the guard is here because its failure source is app code ordinary web PRs touch (D298's carve-out); `e2e` is now the PR critical path; recorded band 5m28–6m02 over the last 11 with the window's worst point 6m44s (run 32269078423, D306(d)); the ~6m line HELD, D207's reclaim executes on the first number over it with no return trip. Root `README.md:132` and `:205` reconciled: the check list's count corrected against the tree (`images`/`lint` exist; `stack` leaves the PR set). The node-22 `ddl-drift-check.mts:119` latent crash is OUT of scope — carried forward.
- done-check: `actionlint` clean; `helm lint` unaffected; on this PR: the guard step demonstrably ran inside `e2e` (log line cited), `e2e` wall-clock recorded (D207 executes if >6m), `stack` ran (this PR touches `packages/obstack-js/package.json` and `deploy/**`, so the filter fires — stated) and its number recorded; after merge, the first master-push `stack` run exists, was not cancelled, and its number opens the re-based watch (recorded in the exit bundle's run log)
- result: DONE @ `7540a18` + `25a2540`; **CI (PR #23): the guard ran inside `e2e` (step 6, 3s, PASS); `e2e` 5m30s — inside the band, D207 NOT consumed; `stack` fired on this PR (lockfile in the filter) at 9m37s, explained by the web-image deps-layer cache miss the lockfile change caused (pre-build 16s → 206s; acceptance 225s), recorded on the re-based watch — see the exit bundle §6.** Locally: `stack.yml`: the D306 filter set verbatim (each path re-verified against `acceptance.sh:100-117` / `apps/web/Dockerfile:21-34` — no discrepancy with D306; D298's original `apps/web/Dockerfile*` confirmed too narrow), `push: branches:[master]` + `workflow_dispatch`, `cancel-in-progress: ${{ github.event_name == 'pull_request' }}` with the D306(b) reason in a comment; header rewritten (not required, reclaim from the drop not the filter, D41 rider primary, `ci:stack` sentence deleted, watch re-based to master pushes with the 5m58–7m17 PR prior, ≈6m56 accepted); Start/Stop Postgres + guard + postgres diagnose line removed, `grep 'docker compose\|deploy/compose' stack.yml` → none; the chart-Postgres schema step stays. `e2e.yml`: the guard step **byte-identical** (diff of `HEAD~1:stack.yml:170-174` vs `e2e.yml:164-168` empty) immediately after the compose step (`:135`, which brings up `postgres`; root `npm ci` at `:120` precedes it), before the smoke floor; header carries the carve-out, the critical-path statement, band 5m28–6m02 with the window's worst 6m44s, the line HELD with D207 pre-authorized. Root `README.md`: `:127`, `:132`, `:205` reconciled — nine every-PR checks named (`web go lint kind images e2e sdk-py sdk-js sdk-e2e`), `stack` explicitly outside the set with the filter enumerated and the honest "still fires on most PRs" sentence. `actionlint 1.7.12` clean (all ten workflows); `helm lint` 0 failed. **Owns amendments ratified by the manager:** README `:127` (same claim as `:205`, would have contradicted itself) and `stack.yml`'s S3.1 paragraph (described the guard as living there); follow-up dispatched for `deploy/compose/ddl-drift-check.mts:108`'s "Invoked by stack.yml" header (doc comment only). Recorded, not fixed: `images`/`lint` have no per-check README bullets (S4.1 gap → S4.4 docs); the node-22 `BoundPool` crash carried forward; the guard was not run locally (Docker state shared with T1's tree — CI is the proof).

### T5: records — the D103(a) triage, the S4.4 handoffs, D178, the ledger
- status: pending
- wave: 3
- depends: T1–T4
- owns: this file (results, exit evidence), .planning/2026-08-30-s4.3-exit-evidence.md (new), .ziarmy/retro.md (S4.2 + S4.3 entries — S4.2's is missing)
- goal: K12 as ruled: the triage outcome and the M5-packet facts (AI8 streaming cost correction; AI6/K8 auto tool spans); the S4.4 handoff list (AI3 on-by-default sentence, Responses `>=4.87`, K4 bound, K5 floor, streaming + logging-bridge absence); D178 non-fire (CI10); the D78 verdict record with grounds; first-runner numbers for `sdk-e2e`, `sdk-js`, `e2e`, `stack` (PR + first master push).
- done-check: exit bundle assembled in the S4.2 shape (§1 clause-by-clause … §7 carried forward); `git status` clean at the signed tip
- result: DONE — `.planning/2026-08-30-s4.3-exit-evidence.md` §1–§7 assembled with PR #23's numbers; D309 triage + S4.4 handoffs in §7; D178 non-fire recorded; retro entries for S4.2 (missing) and S4.3 appended to `.ziarmy/retro.md`; awaiting the advisor's signature.

## Exit evidence (to be assembled at close)
- The D78 additivity verdict, both paths, with the measured grounds (AI1/AI2/OR1/OR9) and the `src/index.ts` diff = empty
- T1's red-then-green ambient-context join; the both-paths-in-one-process proof (v6 suite + v7 suite, no double span)
- T2's `stream`/`parse` pins; the below-4.87 measurement sentence
- T3's four-layer trace on `ai@7` from the real compose stack: trace id + the assertion lines from CI
- T4's guard log line inside `e2e`; the `stack.yml` trigger block as merged; first-runner numbers (`e2e` vs 6m with D207's disposition; `sdk-e2e` vs 4m41–5m05; `sdk-js`; `stack` PR + first master push)
- K12's triage record and the S4.4 handoff list; D178 non-fire

## Run log
- 2026-08-30 — **F3 @ `f797a84`** (R1 re-verify's S-a–S-d): the 7.5+ guard parses major/minor; the D316 request-half branch pinned by two tests through the REAL patched `create` with a stand-in transport (no production seam), proven red by removing `endSpan` in the `!observed` branch and reverted (`llm-span.ts` byte-identical); the 4.87.0 copy is atomic (`cpSync` to `.tmp-<pid>` + `renameSync`, self-heals a half copy); both `output_text` triggers named. Suite **70/70** ×3 (cached / fresh / cached); typecheck/build clean; frozen files + `llm-span.ts` unchanged vs `a260568`. Tree at `f797a84`, PR opened for the first-runner numbers (§6 of the exit bundle).
- 2026-08-30 — breakdown with measurements taken first (AI1–AI12, OR1–OR10, CI1–CI10; three read-only Opus passes, scratch installs only); kickoff packet K1–K12 to the advisor.
- 2026-08-30 — advisor kickoff ruled **D299–D309**: additivity ADDITIVE on both paths (D299, no escalation); one-door integration with wrapper-owned span lifetime and the `onStart.operationId` stream marker (D304); nine evidence imprecisions corrected, none void-making. **CLEARED WITH CONDITIONS** (T1 per D304; D306(b) concurrency fix in T4; T3 owns extended; D303 bisect returns pre-merge; first-runner numbers against corrected bases). T1 dispatched.
