# Team Plan — obstack Phase 1 Core Pipeline — OTLP→ingest→ClickHouse→trace view end-to-end (exit: demo app trace renders in real TraceExplorer via smoke.sh)

meta:
- date: 2026-08-15
- repo/branch: master @ ac75db3
- mode: workflow
- team-cap: 10
- models: executor={opus, user override} reviewer={opus, default} advisor={fable, default}
- iteration-limit: 3
- iteration: 0/3
- resumed: 2026-08-15 session ef9a2ebf — prior session interrupted mid-wave-4; Docker Desktop VM was corrupted and restarted; stack rebuilt healthy; advisor re-spawned (prior instance lost with its session)

## Advisor decisions
<!-- Manager applies advisor output here. Binding for all agents. Full text: /private/tmp/claude-501/-Volumes-SSD-Workspace-observer-stack/9053756b-13fb-45d6-9bbf-d214bd174313/scratchpad/advisor-founding-doc.md §2 -->
- D1: Monorepo — backend lives beside the app in new top-level dirs `services/ingest/` (Go), `deploy/compose/`, `demo/agent-app/`; no separate repos — same-images constraint + solo velocity. (founding doc §2 D1)
- D2: Ingest is one Go module `services/ingest/`, module path `github.com/Ziadabdelsalam/observer-stack/services/ingest`, layout `cmd/ingest/main.go` + `internal/{config,auth,receive,mapping,pricing,write,migrate}` + `migrations/{0001_spans.sql,0002_logs.sql,0003_trace_summaries.sql}` (go:embed); one stateless binary. (founding doc §2 D2)
- D3: OTLP decode via `go.opentelemetry.io/collector/pdata` (`ptraceotlp`/`plogotlp`) for both transports — gRPC on `:4317`, HTTP on `:4318` at `/v1/traces` and `/v1/logs`, accepting `application/x-protobuf` and `application/json`; ClickHouse writes via `clickhouse-go/v2` native protocol. (founding doc §2 D3)
- D4: Phase-1 API keys from env `OBSTACK_API_KEYS=ok_dev_<rand>:<workspace_id>[,…]`; clients send `Authorization: Bearer ok_dev_…` (settable via `OTEL_EXPORTER_OTLP_HEADERS`); missing/unknown key → 401; this wire format survives to M3, only the lookup moves env→Postgres. (founding doc §2 D4)
- D5: Batching is an in-service micro-batcher — no ClickHouse `async_insert`, no queue; per-table buffered channels, flush at 10k rows or 1s whichever first, synchronous native batch INSERT; on write failure bounded retry (3×, backoff) then drop + count. (founding doc §2 D5)
- D6: Error posture — unmarshal failure → single 4xx, never a 5xx retry loop; per-record mapping failure → drop the record, keep the batch; all drops counted in Prometheus-style `/metrics` as `obstack_ingest_dropped_total{workspace_id,reason}` alongside `obstack_ingest_accepted_total`; `/healthz` for the compose healthcheck; UI surfacing of the counter is M3. (founding doc §2 D6)
- D7: ClickHouse schema, DDL owned by `services/ingest/migrations/` — database `obstack`; all timestamps `DateTime64(9,'UTC')`; IDs lowercase-hex `String`. `spans`: `MergeTree`, `PARTITION BY toDate(start_time)`, `ORDER BY (workspace_id, trace_id, span_id)`, `TTL toDateTime(start_time) + INTERVAL 30 DAY`, `layer Enum8('api','agent','tool','llm','infra','other')` computed at ingest, GenAI columns first-class (`gen_ai_system`, `gen_ai_request_model`, `gen_ai_response_model`, `input_tokens`, `output_tokens`, `cost_usd`, `finish_reason`, `prompt`/`completion` `CODEC(ZSTD(3))`), k8s resource columns, `attributes`/`resource_attributes` `Map(LowCardinality(String), String)`. `logs`: `MergeTree`, `PARTITION BY toDate(timestamp)`, `ORDER BY (workspace_id, trace_id, timestamp)`, `TTL … 30 DAY`, `INDEX idx_pod k8s_pod TYPE bloom_filter GRANULARITY 4`. `trace_summaries`: `AggregatingMergeTree`, `ORDER BY (workspace_id, trace_id)`, fed by a materialized view over `spans`, `SimpleAggregateFunction` rollups + `AggregateFunction(argMinIf,…)` roots (condition `parent_span_id = ''`), `max_seen_date` TTL. **Query rule (binding): summaries are always read with `GROUP BY workspace_id, trace_id` + `-Merge` combinators — never `FINAL`, never trust unmerged parts.** Search/list views read only `trace_summaries`. (founding doc §2 D7 — exact column list is binding)
- D8: GenAI extraction contract (documented in `services/ingest/internal/mapping/`) — tokens from `gen_ai.usage.input_tokens`/`gen_ai.usage.output_tokens`, models from `gen_ai.request.model`/`gen_ai.response.model`, system from `gen_ai.system`, prompt/completion from span attributes `gen_ai.prompt`/`gen_ai.completion`. Layer classification: `gen_ai.*` present → `llm`; `obstack.agent.step` → `agent`; `obstack.tool.name` → `tool`; `http.request.method`/`http.route` → `api`; else `other` (`infra` reserved for M2 collector spans). (founding doc §2 D8)
- D9: Cost computed at ingest from `services/ingest/pricing/prices.json` (`go:embed`), rows `{"match","input_per_mtok","output_per_mtok"}`, longest-prefix match on request model with response model as fallback; no match → `cost_usd = 0` + counter `obstack_ingest_unpriced_models_total{model}`; per-workspace overrides are M3, same row shape. (founding doc §2 D9)
- D10: Telemetry view-model types move to `src/lib/types.ts` — `Layer`, `SpanStatus`, `LlmDetail`, `Span`, `K8sEvent`, `Severity`, `LogRecord`, `Explanation`, `Trace` relocate from `src/mock/types.ts`; all imports updated mechanically; connector/workspace types stay in `src/mock/types.ts`; **no re-export shim**. Deviation escalates immediately. (founding doc §2 D10)
- D11: Query layer lives in `src/server/` behind an `import "server-only"` guard — `src/server/clickhouse.ts` (`@clickhouse/client` singleton reading `CLICKHOUSE_URL`/`CLICKHOUSE_USER`/`CLICKHOUSE_PASSWORD`), `src/server/queries/traces.ts`, `src/server/queries/overview.ts`, `src/server/adapters.ts`, `src/server/data.ts`; add dependency `@clickhouse/client`; **all SQL parameterized via `query_params` — no string interpolation**; the web ClickHouse user is `obstack_web` with `readonly=2` defined in `deploy/compose/clickhouse/users.d/obstack-users.xml` alongside `obstack_ingest` (write grants) — read-only enforced at the database, not by convention. (founding doc §2 D11)
- D12: Adapter, not new UI types — `adapters.ts` maps CH rows to existing shapes: `startMs`/`atMs` as ms offsets from `min_start`, `durationMs` from `duration_ns`, `llm?: LlmDetail` built when `layer='llm'`, `finish_reason` mapped into the existing union (unknown → `"stop"`, status error → `"error"`), `k8sEvents` and `explanation` left undefined; trace detail = 2 queries (spans by `(workspace_id, trace_id)`, logs by same) + 1 summary lookup. (founding doc §2 D12)
- D13: Live/mock switching in `src/server/data.ts` exposing `getTrace(id)`, `listTraces(filter)`, `getOverview(range)`; mode from `OBSTACK_DATA_MODE=live|mock`; unset or `mock` → current mock modules; `live` requires `CLICKHOUSE_URL` (fail fast at startup if absent); pages import only the facade, never `@/mock/traces` or `src/server/queries/*`. **In live mode with zero data the list renders the empty state — no silent fallback to mock**, ever (fallback-on-error masks pipeline breakage). Deviation escalates immediately. (founding doc §2 D13)
- D14: Compose bundle `deploy/compose/docker-compose.yml` — `clickhouse` from official `clickhouse/clickhouse-server` **pinned to an exact LTS tag**, volumes for data + `users.d`, healthcheck; `ingest` built from `services/ingest/Dockerfile` (multi-stage, distroless/static), `depends_on: clickhouse: condition: service_healthy`, **runs schema migrations at boot** via `internal/migrate` tracked in `obstack.schema_migrations` and idempotent — no `docker-entrypoint-initdb.d` DDL; `demo` built from `demo/agent-app/Dockerfile` under compose `profile: demo`; `web` is NOT containerized in Phase 1 (runs `next dev` locally) — web image + Helm are M4. (founding doc §2 D14)
- D15: Demo app is Python + plain OTel SDK (no obstack SDK — proves the "bring your own OTel" path): `demo/agent-app/` FastAPI, `POST /chat` runs API span (auto via `opentelemetry-instrumentation-fastapi`) → agent-step span (`obstack.agent.step`) → tool span (`obstack.tool.name`, a real HTTP self-call so context propagation is exercised) → LLM span with full D8 GenAI attributes; LLM is a **built-in deterministic fake by default**, real API only when `OPENAI_API_KEY` is set; logs via OTel `LoggingHandler` + OTLP log exporter; optional `LOOP_INTERVAL_S` self-traffic; exporter config is pure standard env (`OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_EXPORTER_OTLP_HEADERS`). (founding doc §2 D15)
- D16: Phase-1 testing baseline — Go integration tests against real ClickHouse (compose/testcontainers) covering protobuf + JSON + gRPC accept, malformed → 4xx + counter, batch flush, and summary rollup across split batches (root span in a later batch); web adapter unit tests on row→type mapping; acceptance is the single script `deploy/compose/smoke.sh` that boots the stack, fires the demo, and asserts the trace via the web facade. (founding doc §2 D16)
- D17 (ruling 2026-08-15): smoke mechanism — `deploy/compose/smoke.sh` runs `npx tsx --conditions react-server deploy/compose/smoke.ts`, which sets `OBSTACK_DATA_MODE=live` and calls `listTraces`/`getTrace` from `src/server/data.ts` directly (`--conditions react-server` satisfies the `server-only` guard; tsx resolves `@/` tsconfig paths). No throwaway API route, no running Next server; browser render stays a manual T10 check with screenshot.
- D18 (ruling 2026-08-15): pricing split out of T4 into its own task (T11) — self-contained matcher + `prices.json` + unit tests (longest-prefix, response-model fallback, unpriced→0 + counter); T4 depends on it and shrinks to mapping + writer. T4 remains a critical item; pricing is reviewed as part of the same D8/D9 contract review.
- D19 (ruling 2026-08-15): T9 stays in wave 4 with an ingest-boundary done-check (compose build+run, `POST /chat` 200, `/metrics` accepted counters incrementing for traces and logs — satisfiable with T3 alone); the ClickHouse row-level assertion (four layers under one trace_id, logs carrying trace_id) moves to T10, the end-to-end gate.
- D20 (ruling 2026-08-15): M2/M3 sprint splits deferred to each phase's planning moment, with the seam constraint fixed now: every split must end in a shippable, demoable increment. Presumptive seams — M2a = collector distro + nearby log joins + real search (demoable via bring-your-own-OTel); M2b = SDKs + auto-instrumentation; M3a = auth/workspaces/DB-backed keys + onboarding; M3b = billing/metering + Connections hub + Explain. Confirmed or corrected when each phase's plan returns to the advisor.
- D22 (T2 DDL ruling): `root_method` via argMinIf from `attributes['http.request.method']` APPROVED (empty string for non-HTTP roots; adapter maps empty → UI fallback, a T6 concern); `models` = union of non-empty request+response models APPROVED. Layer enum AMENDED: `Enum8('other'=0,'api'=1,'agent'=2,'tool'=3,'llm'=4,'infra'=5)` — edit `0001_spans.sql` in place (schema pre-data; devs `docker compose down -v`, one README line). Binding on T4: writer sets `layer` explicitly on every row; integration test asserts a mapped fixture produces zero unintended `'other'` rows.
- D23 (dup-delivery ruling): plain MergeTree ACCEPTED for Phase 1 explicitly; ReplacingMergeTree rejected (MV fires at insert — wouldn't fix summaries). D5 clarified as **ACK-on-enqueue**: ingest returns OTLP success once records pass auth+decode and enter the batcher; ClickHouse write failures are drops counted `reason="write"`, never an error to the exporter. Residual transport-retry dup risk documented in `0003` header (one sentence). Billing (M3) meters at ingest accept-time in Postgres, never from `trace_summaries`. Revisit only on M3 design-partner evidence.
- D24: `internal/metrics` RATIFIED into D2 — layout now `internal/{config,auth,receive,mapping,pricing,write,migrate,metrics}`; one shared registry; drop `reason` values stay a closed const set in that package — no stage invents its own string.
- D25: UI `Layer` union gains `"other"` — folded into T6's owns: `src/lib/types.ts` (union) + `src/lib/layers.ts` (neutral color token, label "OTHER", appended last in `layerOrder`). Mapping `'other'` onto an existing layer in the adapter is REJECTED. Mock generators unaffected.
- D8-AMENDMENT (resume ruling 2026-08-15): `gen_ai.response.finish_reasons[]` array form (take first element) RATIFIED into D8 alongside the scalar — M2 SDKs may emit either. Also RATIFIED: prompt/completion live ONLY in the dedicated ZSTD columns, excluded from the attributes Map — binding on T6's adapter and M2 SDK docs (read the columns, never attributes['gen_ai.prompt']); documented in internal/mapping/.
- D27 (T6 rulings 2026-08-15): (a) D13 RIDER, binding on T7/T8: in live mode, facade-reading pages MUST force dynamic rendering per the Next 16 docs mechanism (live data baked into a static prerender = stale masquerading as live); mock mode may stay static-as-today. (b) D17 amendment: smoke.ts uses async main() (CJS repo, no top-level await). (c) F5 ordered: root_service via argMinIf lands in 0003 + MV pre-release (down -v precedent per D22), T6 query reads argMinIfMerge, adapter drops alphabetical fallback; T4 writer unchanged (MV derives from spans.service — if executor finds otherwise, immediate escalation); T10 gated on F5.
- D21-AMENDMENT (2026-08-15, supersedes registry placement): the D21 registry (liveWiredRoutes, liveWiredRouteExclusions, isLiveWiredRoute) lives in client-safe `src/lib/live-routes.ts` — pure data + pure predicate; NO re-export from data.ts (dead indirection rejected); all callers server and client import it directly; future wiring tasks flip one line there. Ordered after T12's client mirror drifted within one review round (exclusion list landed server-side only). Executed by T6 executor (module move) + T12 executor (badge import switch, mirror deleted); matrix verify: live /app/traces/diff = badge, /app/traces/<id> = none, mock = none anywhere.
- D26 (resume ruling 2026-08-15): `obstack_ingest_dropped_total` = any POST-AUTH telemetry refused or lost, by `reason`; unit = records when decodable, else 1 per rejected request. Consequences: (a) pre-auth gRPC codec rejections stay uncounted — gap ratified, exclusion documented in one sentence in `internal/metrics`; (b) HTTP 415 with a valid key MUST increment `reason="unsupported"` (new D24 closed-set value); (c) both transports gain panic recovery counting `reason="panic"` — gRPC via a ~10-line hand-written unary recovery interceptor (log stack, count, return codes.Internal — terminal for OTLP exporters), HTTP via middleware incrementing the same counter; ingest never crashes on a poison payload. Carried by fix task F3. POST-F3 RIDERS (2026-08-15): panic = 1 per request RATIFIED as the counting unit, always (sentence added to the D26 definition comment in internal/metrics — F4); gRPC codec-panic vector closed by F4's recover-only codec guard via grpc.ForceServerCodecV2 — required because grpc-go's proto codec registers only as CodecV2 and the v1 option would add a per-message bridging copy; the API is Experimental but supported throughout grpc-go 1.x, so a future bump replacing it is a deliberate re-decision, not drift. (Panic → unmarshal error → normal terminal rejection; stays pre-auth and uncounted.) RATIFIED RESIDUAL: pre-codec stdlib-gzip panic vector accepted for Phase 1 (stdlib returns errors on hostile input by design; no user hook exists in front of recvAndDecompress; disabling gzip rejected) — documented next to the codec guard, revisit rides any grpc-go bump.
- D21 (ruling 2026-08-15): live-mode presentation for unwired surfaces — in `OBSTACK_DATA_MODE=live`, unwired surfaces keep rendering mock content with a persistent "SAMPLE DATA — preview" badge; wired surfaces render real data with real empty states, never mock fallback. Mechanism: a const registry of live-wired route prefixes in `src/server/data.ts`; the app shell layout renders the badge for unregistered routes when mode=live. (Platform-coverage audit ruling 8 — the only part of that audit that touches M1 scope; carried by T6 (registry) + T12 (badge).)

## Critical items
<!-- Task IDs that must return to the advisor before merge. -->
- T2 (A2 — DDL) — ordering keys, partition, TTL, and the `AggregatingMergeTree` summary definition are near-irreversible once data flows; advisor reviews the exact migration SQL before any dependent task merges.
- T4 (A4 — mapping + writer; pricing split to T11 per D18) — the D8 extraction contract is the interop surface for the M2 SDKs and for foreign OTLP; advisor reviews the attribute map and the layer classifier, with T11's pricing artifact covered by the same D8/D9 contract review.
- T6 (A6 — query layer) — the facade + adapter is the boundary every later phase builds on; advisor reviews `data.ts` signatures, SQL parameterization, and the `trace_summaries` `-Merge` query pattern.
- T10 (A10 — acceptance) — advisor signs off the Phase 1 exit-criterion evidence.
- Any deviation from D10 (type relocation) or D13 (no silent mock fallback) escalates immediately, from whichever task hits it.

## Tasks
<!-- One block per task. Keep field names exactly — agents parse by heading. -->
<!-- Exclusive file ownership holds WITHIN a wave. Cross-wave re-entry into an earlier task's file is allowed because waves are sequential; each such case is called out in the owning task's `owns` line. -->

### T1: Compose foundation + ClickHouse (A1)
- status: pending
- wave: 1
- depends: —
- owns: `deploy/compose/docker-compose.yml`, `deploy/compose/clickhouse/users.d/obstack-users.xml`, `deploy/compose/clickhouse/` (config + volume layout), `deploy/compose/README.md` — note: T2 (wave 2) and T9 (wave 4) later add the `ingest` service block and the `demo` profile to the same `docker-compose.yml`, and T10 (wave 7) appends the runbook to the same `README.md`; no in-wave conflict because those tasks run in strictly later waves.
- goal: `docker compose up clickhouse` yields a healthy ClickHouse pinned to an exact LTS tag, with persistent data volume and two distinct users per D11 — `obstack_ingest` with write grants and `obstack_web` with `readonly=2` — defined in `users.d` config, not created by convention.
- done-check: `clickhouse-client` connects as each user; `SELECT` succeeds for both; an `INSERT` as `obstack_web` is rejected by the server; compose healthcheck reports healthy.
- result: FIXED→approved. ClickHouse pinned 26.3.17.110 (current LTS, registry-verified). Reviewer fix: published ports rebound to 127.0.0.1 (dev creds are in-repo). Notes: users.d mounted file-by-file (dir mount crash-loops); passwords via from_env (dev defaults obstack_ingest_dev/obstack_web_dev); no system.* grants for either user (introspecting clients get ACCESS_DENIED, migration runner unaffected).

### T2: Ingest skeleton + schema migrations (A2)
- status: pending
- wave: 2
- depends: T1
- owns: `services/ingest/go.mod`, `services/ingest/cmd/ingest/main.go`, `services/ingest/internal/config/`, `services/ingest/internal/migrate/`, `services/ingest/migrations/` (`0001_spans.sql`, `0002_logs.sql`, `0003_trace_summaries.sql`), `services/ingest/Dockerfile`, plus the `ingest` service block added to `deploy/compose/docker-compose.yml` — that file is owned by T1 in wave 1; resolved by sequencing (T2 is a later wave), so the append is allowed and no wave-1 peer touches it.
- goal: the ingest binary boots from compose, applies the full D7 DDL idempotently via a tracked `obstack.schema_migrations` table (never `docker-entrypoint-initdb.d`), reads config from env per D2 (`CLICKHOUSE_DSN`, `OBSTACK_API_KEYS`, ports), and serves `/healthz` plus a Prometheus-style `/metrics`.
- done-check: fresh `docker compose up` → `spans`, `logs`, `trace_summaries` and the materialized view all exist in database `obstack`; a second boot applies nothing (no-op); a manual multi-batch `INSERT` into `spans` followed by a `GROUP BY workspace_id, trace_id` + `-Merge` query on `trace_summaries` returns exactly one correct row; `curl /healthz` 200 and `/metrics` serves the counter names.
- result: FIXED→ADVISOR-APPROVED subject to fix task F1 (D22 enum renumber + D23 header note). Rulings: root_method + models constructs approved; dup exposure accepted (D23 ACK-on-enqueue); internal/metrics ratified (D24). Done-check all green incl. split-batch argMinIfMerge rollup. Reviewer fix: healthcheck port deduped via exported DefaultAdminAddr. Escalated to advisor: (a) three DDL constructs beyond D7 literal text (root_method argMinIf, models = request∪response, layer Enum8 1..6 — silent 'api' default when column omitted, T4 writer must always set layer); (b) plain-MergeTree redelivery double-count exposure in summaries; (c) new internal/metrics package outside D2 layout. Stack left running, tables truncated for T3.

### F1: DDL amendments per D22/D23 (advisor-ordered fix)
- status: pending
- wave: 3
- depends: —
- owns: `services/ingest/migrations/0001_spans.sql` (layer enum renumber only), `services/ingest/migrations/0003_trace_summaries.sql` (one-sentence dup-risk header comment only), `deploy/compose/README.md` (one line: schema changed pre-release → `docker compose down -v`)
- goal: 0001's layer column becomes `Enum8('other'=0,'api'=1,'agent'=2,'tool'=3,'llm'=4,'infra'=5)` per D22; 0003 gains the D23 residual-duplicate note; README gains the down -v line. Nothing else changes.
- done-check: `docker compose down -v && docker compose up -d --build` → ingest healthy, second boot no-op; `SELECT toTypeName(layer) FROM obstack.spans LIMIT 0` / DESCRIBE shows the new enum with 'other'=0; an INSERT omitting `layer` yields `'other'`.
- result: APPROVED (advisor CONFIRMED on resume, 2026-08-15). F1 closed.

### T3: OTLP receive + auth + error posture (A3)
- status: delivered (see t3-notes.md in scratchpad) — in review
- wave: 3
- depends: T2
- owns: `services/ingest/internal/receive/`, `services/ingest/internal/auth/`
- goal: both OTLP transports accept traces and logs per D3 — gRPC on `:4317`, HTTP on `:4318` at `/v1/traces` and `/v1/logs` for `application/x-protobuf` and `application/json` — behind D4 bearer-key auth, with the D6 error posture (unmarshal failure → one 4xx, never a 5xx retry loop) and accepted/dropped counters exposed on `/metrics`.
- done-check: integration tests — a valid fixture returns 200/OK on each transport and content-type; a garbage body returns 400 and increments `obstack_ingest_dropped_total`; an unknown or missing bearer key returns 401; an `otelcol telemetrygen` export against the running service is accepted.
- result: REVIEWED: fixed (resume session). Reviewer fixes: 401/415 error bodies now answer in the request's encoding (was always proto), pinned by new TestHTTPErrorBodyUsesRequestEncoding; grpc.go comment corrected (codes.Internal, non-retryable). Full ./internal/... suite green. telemetrygen leg covered by executor's harness run + F2/T9 done-checks. ESCALATED to advisor: (1) gRPC codec-level decode failures structurally uncounted, (2) HTTP 415 drops uncounted — what does dropped_total mean, (3) no gRPC recovery interceptor (panic in Consumer kills process). Advisor ruling pending.

### T4: Mapping + batch writer (A4)
- status: delivered — in review
- wave: 4
- depends: T3, T11, F1
- binding (D22/D23): the writer sets `layer` explicitly on every row — integration test asserts a mapped fixture produces zero unintended `'other'` rows; batcher implements ACK-on-enqueue per D23 (OTLP success after auth+decode+enqueue; write failures are counted drops `reason="write"`, never exporter errors).
- owns: `services/ingest/internal/mapping/`, `services/ingest/internal/write/`
- goal: decoded pdata becomes correctly typed ClickHouse rows per D7 — layer classification and GenAI extraction per D8, ingest-time cost via T11's pricing package per D9 — written by the D5 micro-batcher (10k rows or 1s, bounded retry then drop + count), so that ingested traces roll up correctly in `trace_summaries` even when a trace's spans arrive across batches.
- done-check: integration tests against real ClickHouse — an exported fixture containing API + agent + tool + LLM spans and logs produces rows with the right `layer`, GenAI columns, and prompt/completion; `cost_usd` matches the embedded pricing table for a priced model; an unpriced model yields `cost_usd = 0` and increments `obstack_ingest_unpriced_models_total{model}`; the summary rollup is correct when the root span arrives in a second batch.
- result: REVIEWED: fixed (resume session) — only fix: hand-rolled contains → slices.Contains. Full D8/D9/D22/D23 contract verified vs code + live ClickHouse (zero unintended 'other'; exact cost math; split-batch argMinIfMerge rollup; ACK-on-enqueue enforced by type; write-failure test uses real obstack_web rejection). Advisor contract sign-off pending. ESCALATED: (4) overload-shed counted as reason="write" — indistinguishable from CH-reject; (5) writer-side retry = second dup vector beyond D23's client case — header sentence?; (6) prompt/completion excluded from attributes Map — ratify as interop surface. Note: Writer.Close can block ~91s/table during CH outage (signature change would touch main.go).

### T11: Pricing table + matcher (A4 split, per D18)
- status: delivered — in review
- wave: 3
- depends: T2
- owns: `services/ingest/internal/pricing/`, `services/ingest/pricing/prices.json`
- goal: D9 in full as a self-contained package — `go:embed`-ed `prices.json` with rows `{"match","input_per_mtok","output_per_mtok"}` covering current OpenAI/Anthropic/major models, longest-prefix matcher on request model with response-model fallback, unpriced → cost 0 + `obstack_ingest_unpriced_models_total{model}` counter hook.
- done-check: `go test ./internal/pricing/` green — cases for exact match, longest-prefix match, response-model fallback, and unpriced model returning 0 with the counter incremented.
- note: stdlib + `embed` only — `services/ingest/go.mod` is owned by T2 and edited by T3 (same wave) for OTLP deps; if pricing genuinely needs a new module dependency, STOP and report the conflict instead of editing `go.mod`.
- result: REVIEWED: fixed (resume session). Reviewer added 4 longer-prefix rows (gpt-5-pro 15/120, o3-pro 20/80, o1-pro 150/600, claude-opus-4-5 5/25) — family rows were silently mispricing sibling tiers. pricing.go beside prices.json accepted (go:embed cannot cross dirs; mirrors migrations.go). Tests green. Pending advisor D8/D9 contract sign-off (with T4). ESCALATIONS queued: (1) unpriced counter label = client-controlled model string, unbounded cardinality — cap or sample before M3; (2) embedded prices frozen at build with no as-of date/staleness check — refresh mechanism ruling before M3 overrides.

### T5: Telemetry type relocation to src/lib/types.ts (A5)
- status: pending
- wave: 1
- depends: —
- owns: `src/lib/types.ts`, `src/mock/types.ts`, and the mechanical import updates across `src/components/`, `src/mock/`, `src/app/` — note: T7 (wave 6) and T8 (wave 6) later edit specific files under `src/app/` and `src/components/traces/`; sequencing resolves it, and no wave-1 peer touches `src/`.
- precondition: read the relevant guide in `node_modules/next/dist/docs/` before writing Next.js code.
- goal: D10 executed — `Layer`, `SpanStatus`, `LlmDetail`, `Span`, `K8sEvent`, `Severity`, `LogRecord`, `Explanation`, `Trace` live in `src/lib/types.ts` and every consumer imports them from there, with no re-export shim left behind; `src/mock/types.ts` retains only connector/workspace types.
- done-check: `npm run build` and `npm run lint` both clean; a repo-wide search finds zero remaining imports of the moved type names from `@/mock/types`.
- result: APPROVED. All 9 types relocated verbatim, no shim; mock/types.ts retains only connector types; build clean, lint's 7 problems proven byte-identical to the HEAD baseline (pre-existing, unowned files). One in-scope deviation accepted: src/lib/layers.ts import updated. ESCALATED to advisor: Layer union lacks "other" (D7/D8 ingest default) — affects T6 adapter + Record<Layer,string> maps.

### T6: Web query layer + adapters + live/mock facade (A6)
- status: pending
- wave: 5
- depends: T4, T5
- owns: `src/server/clickhouse.ts`, `src/server/queries/traces.ts`, `src/server/queries/overview.ts`, `src/server/adapters.ts`, `src/server/data.ts`, `package.json` (add `@clickhouse/client`), plus per D25: `src/lib/types.ts` (Layer union gains `"other"`) and `src/lib/layers.ts` (neutral color, label "OTHER", last in `layerOrder`) — both finalized by T5 in wave 1; sequencing resolves the re-entry — note: T8 (wave 6) later owns and completes `src/server/queries/overview.ts`; T6 establishes it, T8 fills in the time-bucketed overview queries; sequencing resolves the overlap and no wave-5 peer touches `src/server/`.
- precondition: read the relevant guide in `node_modules/next/dist/docs/` before writing Next.js code.
- goal: `getTrace(id)`, `listTraces(filter)`, `getOverview(range)` return `src/lib/types.ts` shapes from ClickHouse per D11/D12 — server-only guard, parameterized SQL, summaries read with `GROUP BY` + `-Merge` — behind the D13 facade whose mode comes from `OBSTACK_DATA_MODE`, failing fast in `live` when `CLICKHOUSE_URL` is absent and rendering the empty state (never mock) when live data is empty; **and** per D21 the same file exports the const registry of live-wired route prefixes (exported so T12 and every future wiring task flip one line), seeded with the prefixes T7/T8 wire in wave 6.
- done-check: adapter unit tests on row→type mapping pass; against the ClickHouse seeded by T4, `getTrace` returns a `Trace` whose `spans[].startMs` and `logs[].atMs` offsets are correct relative to `min_start` and whose `durationMs` matches `duration_ns`; `TraceExplorer` renders that trace with `explanation` and `k8sEvents` undefined.
- result: executor DONE, REVIEWED: fixed (resume session) — reviewer added services-array search to live list (mock/live parity). D21 registry present (liveWiredRoutes + isLiveWiredRoute, verified vs T12 cases). D11/D12/D13/D25/D8-amendment all verified incl. live TraceExplorer render and zero-mock-leak empty state. Deps accepted: server-only (load-bearing for D17), tsx devDep, new env OBSTACK_WORKSPACE_ID (default ws_demo). npm test 12/12, build clean, lint baseline. ADVISOR-APPROVED (facade boundary; dep deviations ratified). ESCALATED (all ruled): (1) no root_service in summaries — F5 now or M2; (2) Static-prerender rider on T7/T8 (dynamic rendering in live mode); (3) D17 smoke.ts = CJS async main(); (4) T7 handoff: delete traceMatches client copy. T10 heads-up recorded: CH client singleton exits cleanly under tsx.

### T7: Wire trace surfaces to the facade (A7)
- status: pending
- wave: 6
- depends: T6
- owns: `src/app/app/traces/page.tsx`, `src/app/app/traces/[id]/page.tsx`, `src/components/traces/TracesSearch.tsx` (data-sourcing changes only)
- precondition: read the relevant guide in `node_modules/next/dist/docs/` before writing Next.js code.
- goal: the traces list and trace detail read only through the D13 facade — never `@/mock/traces` or `src/server/queries/*` directly — with list filtering done server-side over `trace_summaries` in live mode, while mock mode stays pixel-identical to today.
- done-check: with `OBSTACK_DATA_MODE=live` the list shows ingested traces and the detail page renders the full waterfall plus the logs rail; with `OBSTACK_DATA_MODE=mock` both pages are unchanged from current behavior; the executor states which Next 16 doc it consulted for any route/API touched.
- result: executor DONE, REVIEWED: fixed (resume session). Facade-only imports (grep-clean); traceMatches deleted per handoff; URL-driven server-side filtering both modes; D27a via searchParams (list) + connection() (detail), doc citations verified vs vendored Next 16.3; live smoke full waterfall + logs rail + 404; zero mock leak; reviewer fix: unfiltered denominator query now only when a filter narrows. ESCALATED, all ruled: (1) 200-cap denominator ACCEPTED for Phase 1, M2-GATE (owner: M2a real search/pagination); (2) mock ○→ƒ ACCEPTED explicitly (one search semantic outranks static optimization for dev-only mode); (3) desync recorded on the M2 search ticket. T7 CLOSED.

### T8: Wire overview dashboard to trace_summaries (A8)
- status: pending
- wave: 6
- depends: T6
- owns: `src/app/app/page.tsx` (data sourcing only), `src/server/queries/overview.ts` — note: `src/server/queries/overview.ts` is established by T6 in wave 5; sequencing resolves the overlap, and T7 (same wave) owns disjoint files.
- precondition: read the relevant guide in `node_modules/next/dist/docs/` before writing Next.js code.
- goal: in live mode the overview stat cards and the requests / error-rate / latency / cost charts are computed from time-bucketed `trace_summaries` queries through the facade, while non-Phase-1 widgets (WatchWidgets etc.) remain on mock.
- done-check: live overview numbers reconcile with direct `SELECT` totals from `trace_summaries` over the same window; mock mode unchanged.
- result: executor DONE, REVIEWED: APPROVED (resume session). connection() mechanism verified vs doc; overview.ts confirmed by direct SQL (exact reconciliation); D13 empty states hold; builds ƒ live / ○ mock. ESCALATED → advisor ordered F6 (per-widget SAMPLE marking + real workspace label + DEPLOY_MARKS gated to mock); build/runtime mode seam pre-registered M4-GATE (same entry as badge-env).

### T9: Demo agent app (A9)
- status: delivered (see t9-notes.md in scratchpad), done-check blocked on F2 — review after F2
- wave: 4
- depends: T3
- owns: `demo/agent-app/` (FastAPI app, `Dockerfile`, `requirements.txt`, `README.md`), plus the `demo` profile service block added to `deploy/compose/docker-compose.yml` — that file is owned by T1 in wave 1 and appended by T2 in wave 2; resolved by sequencing (T9 is a later wave than both), and T9's wave-4 peer T4 owns only `services/ingest/**`, so there is no in-wave conflict.
- goal: D15 in full — `POST /chat` emits an API span (auto-instrumented) → agent-step span (`obstack.agent.step`) → tool span (`obstack.tool.name`, via a real HTTP self-call so context propagation is exercised) → LLM span carrying full D8 GenAI attributes, with logs emitted through the OTel `LoggingHandler` so trace context attaches automatically; LLM is a deterministic built-in fake unless `OPENAI_API_KEY` is set; exporter configured purely by standard OTel env vars including the dev bearer key.
- done-check (per D19, ingest-boundary only): `docker compose --profile demo up` builds and runs; `curl -X POST .../chat` returns 200; ingest `/metrics` shows `obstack_ingest_accepted_total` incrementing for both traces and logs — all satisfiable with T3 alone. The ClickHouse row-level assertion (four layers under one `trace_id`, logs carrying the `trace_id`) belongs to T10.
- result: REVIEWED: fixed (resume session) — one README token-count doc fix. D15 fidelity verified end-to-end post-F2: 5 spans/turn (zero plumbing spans, zero healthcheck traces), self-call traceparent join confirmed, OTEL_SEMCONV_STABILITY_OPT_IN=http load-bearing for D8 api classification, LOOP_INTERVAL_S exercised, clean shutdown flush. ESCALATED (M2-gate): upstream GenAI semconv moved prompt/completion to log events (gen_ai.input/output.messages) — D8 event-form acceptance ruling before M2 locks in.

### T12: Live-mode sample-data badge (D21)
- status: pending
- wave: 6
- depends: T6
- owns: `src/app/app/layout.tsx`, plus whichever single shell component under `src/components/shell/` renders the badge — the executor picks the minimal one and names it in its result. Must NOT touch files owned by its wave-6 peers T7 and T8: `src/app/app/traces/page.tsx`, `src/app/app/traces/[id]/page.tsx`, `src/components/traces/TracesSearch.tsx`, `src/app/app/page.tsx`, `src/server/queries/overview.ts`. (`src/app/app/layout.tsx` may have been import-touched by T5 in wave 1; sequencing resolves that.)
- precondition: read the relevant guide in `node_modules/next/dist/docs/` before writing Next.js code.
- goal: D21 implemented — when `OBSTACK_DATA_MODE=live`, a persistent "SAMPLE DATA — preview" badge renders on every route whose prefix is absent from T6's live-wired registry; no badge in mock mode anywhere, and no badge on registered routes (traces and overview, once T7/T8 register them).
- done-check: `npm run build` clean; with `OBSTACK_DATA_MODE=live`, `/app/alerts` shows the badge and `/app/traces` does not; with `OBSTACK_DATA_MODE=mock`, no badge appears on any route.
- result: executor DONE, REVIEWED: APPROVED, then D21-AMENDMENT executed (resume session). Badge = src/components/shell/SampleDataBadge.tsx (client, usePathname per Next 16 docs) + 1 render line in layout.tsx; after amendment the badge calls the REAL isLiveWiredRoute from @/lib/live-routes (mirror deleted; registry data never crosses server/client boundary). Full matrix green: live /app/alerts + /app/traces/diff = badge, /app/traces + /app/traces/<id> = none; mock = none anywhere. Layout deliberately not force-dynamic (would override T7/T8 per-page choices); build-time-env caveat pre-registered as M4-gate item.

### T10: End-to-end acceptance bundle (A10)
- status: pending
- wave: 7
- depends: T1, T2, T3, T4, T5, T6, T7, T8, T9, T11, T12
- owns: `deploy/compose/smoke.sh`, `deploy/compose/smoke.ts`, `deploy/compose/README.md` (runbook section), `README.md` ("Run the real pipeline" section) — note: `deploy/compose/README.md` is owned by T1 in wave 1; resolved by sequencing (T10 is the last wave and the only task in it).
- goal: one command boots the stack, fires the demo app, and asserts through the D13 facade — per D17: `smoke.sh` runs `npx tsx --conditions react-server deploy/compose/smoke.ts`, which sets `OBSTACK_DATA_MODE=live` and calls `listTraces`/`getTrace` from `src/server/data.ts` — that the trace both lists and resolves with ≥4 layers (`api`, `agent`, `tool`, `llm` under one `trace_id`, prompt/completion/tokens populated) and ≥1 correlated log carrying that `trace_id` (row-level assertion moved here from T9 per D19), with the runbook documenting the Phase 1 exit criterion and the manual browser verification recorded.
- done-check: `deploy/compose/smoke.sh` runs green from a clean checkout; a screenshot of the real trace rendered in `TraceExplorer` is attached to the PR; advisor signs off the exit-criterion evidence.
- result: executor DONE, REVIEWED: fixed (resume session). Smoke green twice incl. clean-volume run; facade-only assertion (zero SQL in smoke.ts, D17); async main (D27b); D19 coverage complete + span_count==rows rollup check; failure paths exit 1 with specific messages; reviewer fixes: prerequisites (Node+npm install), doc-vs-assertion wording, stale intros, env-default dedup. Screenshot authenticated (real waterfall, 5 spans, 3 logs, real OTel attrs) and delivered to user. ADVISOR SIGN-OFF: APPROVED — Phase 1 exit criterion MET (2026-08-15). Cost>0 gate landed + sabotage-verified; final screenshot on shipped tree authenticated. T10 CLOSED.

### F2: Wire receiver + batcher into ingest main (fix task — T3/T9 blocker)
- status: pending
- wave: 4-fix
- depends: T3, T4
- owns: `services/ingest/cmd/ingest/main.go` (T2-owned, T2 closed; manager-authorized re-entry)
- goal: `run()` wires the delivered pieces per t3-notes.md (scratchpad): build `auth.New(cfg.APIKeys)`, construct the write batcher from `internal/write` as the `receive.Consumer` (D23 ACK-on-enqueue — Consumer methods return nothing; write failures are counted drops reason="write"), construct `receive.New(receive.Config{GRPCAddr, HTTPAddr, Auth, Consumer})`, `Start()`, select on `receiver.Err()` alongside the admin-server errors, and on shutdown stop the receiver first then flush/close the writer. No other changes to main.go.
- done-check: `docker compose --profile demo up -d --build` all healthy; `curl -X POST localhost:8000/chat` → 200; ingest `/metrics` shows `obstack_ingest_accepted_total` incrementing for signal="traces" and signal="logs"; `SELECT count() FROM obstack.spans` as obstack_ingest rises above 0.
- result: executor DONE (2026-08-15 resume session) — pipeline live e2e: one /chat → 5 spans (api 2, agent 1, tool 1, llm 1) + 3 logs in ClickHouse under one trace_id, zero drops; boot order write.New→receive.New→Start, shutdown receiver→writer flush→admin. REVIEWED: APPROVED — advisor rider confirmed (accepts counted once: one /chat → accepted +5/+3 == spans/logs rows +5/+3); D23 seam enforced by type; clean-restart flush exercised. Non-blocking notes: Writer.Close unbounded by ctx (advisor: deferred, ratified acceptable until M4 Helm grace periods); residual close-channel race if receiver Shutdown times out mid-handler (packages' concern, not F2's).

### F3: D26 drop-counting + panic recovery + contract-approval riders (advisor-ordered fix)
- status: pending
- wave: 4-fix
- depends: T3 (reviewed), T4 (reviewed), F2 (approved)
- owns: `services/ingest/internal/receive/`, `services/ingest/internal/metrics/metrics.go`, `services/ingest/internal/write/batcher.go` (reason swap only), `services/ingest/migrations/0003_trace_summaries.sql` (header clause only), `services/ingest/internal/mapping/` (doc comment only)
- goal: implement D26 + the advisor's T4-approval riders — (a) one-sentence comment in `internal/metrics` documenting that pre-auth gRPC codec rejections are excluded from dropped_total; (b) add `unsupported`, `panic`, and `overload` to the D24 closed reason const set; (c) HTTP 415 with a valid key increments `dropped_total{reason="unsupported"}` (auth already ran; keep 401-before-415 ordering); (d) hand-written gRPC unary recovery interceptor: recover, log stack, increment `dropped_total{reason="panic"}`, return codes.Internal; (e) HTTP middleware counting the same `reason="panic"`; (f) batcher full-queue shed at batcher.go:59 switches from reason="write" to reason="overload" (retry-exhaustion stays "write"); (g) 0003 header D23 sentence gains one clause naming the writer's bounded retry as a second duplicate vector; (h) one doc line in internal/mapping/ where D8 is documented: prompt/completion excluded from the attributes Map by design. No new dependencies. Tests for c/d/e/f.
- done-check: `go test -count=1 ./... && go vet ./... && go build ./...` green from services/ingest/; 415-with-valid-key test asserts unsupported counter; panicking-Consumer test asserts codes.Internal + panic counter on gRPC and 500 + counter on HTTP, process alive; full-queue test asserts reason="overload"; because 0003 changed pre-release: `docker compose --profile demo down -v && docker compose --profile demo up -d --build` → all healthy, migrations apply fresh, second boot no-op, one /chat lands spans again.
- result: executor DONE, REVIEWED: APPROVED (resume session). All D26 (a)-(e) + riders (f)(g)(h) verified in code + live; -race clean; fresh-boot migration cycle green; write_test.go ownership deviation accepted (mechanical consequence of the ordered reason swap). ESCALATED: (1) ratify panic=1-per-request counting unit; (2) gRPC codec-PANIC residual (kills process; would need ForceServerCodec wrapper) — accept for Phase 1 or F4. Advisor ruling pending.

### F4: gRPC codec panic guard (advisor-ordered fix)
- status: pending
- wave: 4-fix
- depends: F3 (approved)
- owns: `services/ingest/internal/receive/` (codec wrapper + test + the one `grpc.ForceServerCodec(...)` server option), `services/ingest/internal/metrics/metrics.go` (one sentence added to the D26 definition comment: panic = 1 per request, always)
- goal: recover-only codec wrapper per advisor ruling — `defer recover` in `Unmarshal`, convert panic → unmarshal error so grpc-go rejects with its normal terminal code (the ratified codec-rejection path, pre-auth, uncounted). No new dependency. Test feeds a payload through a stubbed panicking codec and asserts a terminal error, process alive.
- done-check: `go test -count=1 ./internal/receive/ ./internal/metrics/ && go vet ./... && go build ./...` green from services/ingest/.
- result: executor DONE, REVIEWED: APPROVED (resume session). recoverCodec via grpc.ForceServerCodecV2 (V2 confirmed necessary against vendored grpc-go 1.82.1 — proto codec registers only as CodecV2); recover only in Unmarshal → normal unmarshal error → codes.Internal terminal; no metrics import (stays pre-auth/uncounted); byte-identical Marshal pinned; process-alive test passes; full suite + -race green. ESCALATED: (1) amend ruling text to name V2; (2) residual pre-codec gzip-decompressor panic vector (no user hook exists) — accept or disable gRPC gzip. Advisor ruling pending.

### F5: root_service in trace_summaries (advisor-ordered fix)
- status: pending
- wave: 6 (parallel; T10 gated on this)
- depends: T6 (approved)
- owns: `services/ingest/migrations/0003_trace_summaries.sql` (add `root_service AggregateFunction(argMinIf, String, DateTime64(9,'UTC'), UInt8)` + `argMinIfState(service, start_time, parent_span_id='')` in the MV), `src/server/queries/traces.ts` (read with argMinIfMerge), `src/server/adapters.ts` (drop the alphabetically-first-service fallback for list-view Trace.service)
- goal: list-view service is the root span's service, per advisor D27(c). T4 writer unchanged — the MV derives from the existing spans.service column; if that turns out false, STOP and escalate (not scope growth).
- done-check: schema changed pre-release → `docker compose --profile demo down -v && up -d --build` all healthy, migrations apply fresh, second boot no-op; re-seed via 3× `curl -X POST localhost:8000/chat`; live `listTraces()` returns Trace.service == 'demo-agent' (root's service) sourced from root_service (not the fallback); `npm test` and `go test -count=1 ./...` (services/ingest) green.
- result: executor DONE (no writer change needed — MV derives from spans.service), REVIEWED: APPROVED (resume session). Construct byte-identical to root_name/root_method; -Merge pattern preserved; alphabetical fallback gone with a regression-pinning fixture (accepted out-of-owns test edit); fresh-boot cycle + live reads all green. T10 gate CLEARED. Operational note: migrate tracks by filename only (no checksum) — stale volumes need down -v, already documented in README.

### F6: Live-overview honesty markers (advisor-ordered, D21 extension)
- status: pending
- wave: 6-fix (T10 gated on this)
- depends: T8 (approved), T12 (closed)
- owns: `src/app/app/page.tsx`, `src/components/dash/Charts.tsx`, `src/server/data.ts` (only if workspaceId needs exporting)
- goal: advisor ruling — D21 extends to partially-wired surfaces. In live mode: every unwired widget on /app (topFailing table, OnboardingChecklist, WatchWidgets, the "ingesting" pulse) gets a small "SAMPLE" marker using the badge's established visual language (deleted widget-by-widget as M2/M3 wire them); the workspace label renders the facade's real workspaceId instead of the hardcoded "loopwork-prod"; `DEPLOY_MARKS` in Charts.tsx is gated to mock mode — HIDDEN in live, not marked (a ReferenceLine overlays real data). Real-ingest-recency health dot REJECTED for Phase 1 — mark the pulse instead. Mock mode pixel-identical everywhere.
- done-check: `npm run build` clean both modes, lint baseline (7); live: /app shows real workspace id, SAMPLE markers on the four unwired widgets, no deploy ReferenceLine; mock: byte-identical rendering to pre-F6 (no markers, loopwork-prod, deploy mark present); npm test green.
- result: executor DONE (core + org-switcher extension), REVIEWED: APPROVED both rounds (resume session). Live: 4 page SAMPLE chips + SideNav org chip, ws_demo everywhere, zero loopwork-prod, deploy marks hidden at both call sites (verified in hydrated DOM); mock byte-identical at the string level. SAMPLE-chip duplication deliberately NOT extracted (markers are short-lived, deleted as M2/M3 wire widgets; below rule-of-three). SideNav workspaceId prop rides the existing M4-GATE build/runtime env entry. WAVE 6 CLOSED.

### F7: Shell-chrome honesty + cost-precision bug (advisor-ordered, gates T10 sign-off)
- status: pending
- wave: 7-fix
- depends: F6 (approved), T10 (delivered, review in flight)
- owns: the shell component(s) under `src/components/shell/` rendering the top bar ("ingesting · 9.4k events/min", "PROD · EU-CENTRAL"), the free-tier usage banner, and the header "LLM COST" stat (executor locates and names exact files); `src/app/app/layout.tsx` only if prop plumbing is needed. Must not touch reviewed F6 files (page.tsx, Charts.tsx, SideNav.tsx) unless the cost-stat fix genuinely lives there — name it if so.
- goal: advisor ruling — F6's principle extends to shell chrome. In live mode: "9.4k events/min" and "PROD · EU-CENTRAL" (fabricated system claims) and the free-tier usage banner (fabricated billing state, M3 territory) each get the SAMPLE marker or are gated out (executor's pick per element, F6 visual language); the self-labeling "DEMO WORKSPACE — sample data" card STAYS. MUST-FIX bug: the header cost stat rounds real $0.00003 to "$0.0000" — reuse the overview's small-value precision handling so real non-zero cost displays non-zero. Mock mode byte-identical (F6-style proof).
- done-check: npm run build both modes clean (rm -rf .next between), lint baseline, npm test green; live: no unmarked events/min / PROD·EU-CENTRAL / free-tier banner, cost stat shows non-zero for the smoke trace; mock: byte-identical.
- result: executor DONE, REVIEWED: fixed (resume session). All three chrome items gated out (judged sound); cost fixed centrally in shared fmtCost (4 call sites — traces list had the same $0.0000 bug, fixed too); reviewer fixes: sub-1e-10 "$0." clamp bug + regression pin, deploy-README claim narrowed. Mock parity proven over all 66 reachable mock cost values; live matrix clean on 4 routes; costs reconcile with CH sums. ESCALATED: (1) mock notification bell + fabricated account identity in shell — extend or M3; (2) demo-card copy overclaims on wired routes in live. Advisor rulings pending; T10 cost>0 gate landed + sabotage-verified separately.

## Escalations log
<!-- Manager appends: {T#} — question → advisor decision (one line each). -->
- T10 — how does `smoke.sh` reach the web facade given `web` is not containerized in Phase 1 (D14)? → advisor ruled: tsx harness invoking `src/server/data.ts` directly (D17).
- T4 — fattest critical-item task → advisor ruled: pricing split into T11, wave 3 (D18).
- Platform-coverage audit → advisor ruled M5/M6/M7 buckets own every unowned surface plus the D21 badge; of that audit only D21 touched M1 scope (registry in T6, badge in new task T12).
- T9 — done-check silently depended on T4's writer → advisor ruled: ingest-boundary done-check in wave 4; row-level assertion moved to T10 (D19).
- M2/M3 sprint splits → advisor ruled: deferred to per-phase planning with shippable-increment seam constraint; presumptive seams recorded (D20).
- T2 DDL review → advisor: root_method + models constructs approved; layer enum amended to 'other'=0 via fix task F1 (D22); T2 approved subject to F1.
- T2 dup-delivery exposure → advisor: accepted for Phase 1; D5 clarified ACK-on-enqueue (D23).
- T2 internal/metrics package → advisor: ratified into D2 (D24).
- T5 Layer-union gap → advisor: add "other" to UI union + maps, folded into T6 (D25); adapter remapping rejected.
- T1 localhost port binding + no system.* grants → advisor: ratified / no action.
- T3 D6 escalations → advisor D26: codec gap ratified; dropped_total = post-auth loss; 415 → reason="unsupported" (bug, F3); panic recovery both transports reason="panic" (F3).
- T4 contract review → advisor: T4+T11 D8/D9 contract APPROVED conditional on rulings 4/5 (folded into F3, no re-review round). finish_reasons[] ratified into D8; ruling 4: reason="overload" ordered; ruling 5: writer-retry dup exposure confirmed as-is + 0003 header clause; ruling 6: prompt/completion Map exclusion ratified as interop surface; Writer.Close deferral ratified until M4 Helm grace periods.
- F2 → reviewer APPROVED; advisor accept-counter rider confirmed with measured counter==row deltas.
- T6 → advisor APPROVED (facade boundary). Rulings: F5 ordered now (pre-release schema edit beats M2 migration); D13 dynamic-rendering rider binding on T7/T8 (D27a); async main() blessed for smoke.ts (D27b); wave 6 cleared, T10 gated on F5.
- F4 → reviewer APPROVED; advisor amended ruling wording to name ForceServerCodecV2 and ratified the pre-codec gzip residual (keep gzip; one doc sentence by F4 executor).
- F3 semantics → advisor: panic=1-per-request RATIFIED (only unit that never lies vs partially-enqueued records); gRPC codec-panic residual → F4 ORDERED (recover-only ForceServerCodec wrapper).
- T12 registry gap → advisor: exclusion list ordered (later superseded by D21-AMENDMENT placement) ("wired prefixes minus named exceptions", entry /app/traces/diff) — edit by T6 executor, client mirror by T12 executor same round; verify live /app/traces/diff = badge, /app/traces/<id> = none.
- T12 build-time badge env → advisor: pre-registered M4-GATE item — at M4 containerization, badge-vs-env must be re-decided (runtime env read or forced-dynamic shell). RESURFACE AT M4 PLANNING.
- F7 final-ext → reviewer APPROVED (bell/aria gated, markers verified in client chunks, mock byte-identical incl. dropdown-chunk equivalence; shared SampleMark extraction accepted at 4 call sites — rule of three met). Residuals recorded: page.tsx duplicate SampleMark → one-line consolidation dispatched; email/avatar strings inside the marked identity block + Wordmark eq-bar delta from D25 layerOrder — accepted, M3-gate default per advisor's closed enumeration.
- F7 final extension → advisor: bell count GATED in live + panel contents SAMPLE-marked; AccountMenu identity SAMPLE-marked (no invented operator identity); SideNav card live-variant copy ("demo workspace · unwired surfaces show sample data" — current copy called real data sample, inverse falsification class). SHELL-CHROME ENUMERATION DECLARED COMPLETE — further items default to M3-GATE pile.
- T10 residual fiction → advisor: F7 ORDERED (mark/gate events-min + PROD·EU-CENTRAL + usage banner; DEMO card stays; $0.0000 cost = MUST-FIX bug — real value rendered as zero falsifies the acceptance evidence). T10 sign-off gated on F7; evidence bundle (smoke output + refreshed screenshot) goes to advisor when F7 lands.
- F6-ext delivered: SideNav.tsx switcher (org name SAMPLE-marked, workspace line = real ws id via layout prop, null==mock); mock switcher markup byte-identical. M4-GATE seam got a concrete repro: `npm run build` reuses cached prerenders across OBSTACK_DATA_MODE changes — matrix checks need rm -rf .next first (recorded for M4 planning).
- F6 org-switcher residual → advisor: fix now, consistently with F6 (worse in persistent shell — every route). Extension: switcher workspace line = real workspaceId in live; org name gets SAMPLE marker (no fabricated org label); M3 shell work replaces marker with real org data. Org-half recorded as M3-GATE note.
- T8 escalations → advisor: (1) live-page fiction = D21-class violation (registered-route badge exemption assumed FULLY-wired pages) → F6 ordered: per-widget SAMPLE marking + real workspaceId label + pulse marked (recency dot rejected); (2) build/runtime mode seam = M4-GATE, same entry as badge-env, mechanism decided once at M4; (3) DEPLOY_MARKS gated to mock (hidden in live, not marked). Wave 6 closes on F6; T10 gated on F5+F6.
- T7 escalations → advisor: 200-cap "N of M" denominator = M2-GATE (M2a search/pagination owns real totals); mock SSR-per-request accepted; TracesSearch same-route desync → M2 search ticket. RESURFACE AT M2 PLANNING.
- T9 GenAI semconv drift → advisor: M2-GATE item — at M2 planning, ingest's log/event path gains gen_ai.input/output.messages extraction into the same prompt/completion columns (additive, internal/mapping only); span-attribute form stays the obstack-SDK convention. No Phase-1 change. RESURFACE AT M2 PLANNING.
- T11 escalations → advisor: pre-registered as M3-GATE items (not Phase-1 blockers): (a) unpriced-model label cardinality must be capped/allowlisted before M3 multi-tenant; (b) prices.json gains "as_of" date with the M3 override design. RESURFACE AT M3 PLANNING.
- Resume 2026-08-15 — advisor (re-spawned) confirmed F1 per D22/D23; F2 spec ruled SOUND; rider: T4/F2 reviews must confirm the writer does NOT double-count obstack_ingest_accepted_total (receive owns that counter, per t3-notes contract).

## Integration
<!-- Filled by the integrator gate (integrator skill). Fix tasks get F# IDs, appended under ## Tasks. -->
- verdict: GO (round 2, 2026-08-15) — F8 verified closed: live detail 0 compare-link/0 mock rootName leaks, mock byte-identical with working compare. Prior: NO-GO round 1 → F8 ordered (advisor: honesty principle extends into every pixel of a wired route; compare-vs-fabricated-healthy-run gated out of live via compareEnabled prop, default true; real compare returns with M5 diff surface). Re-verify pending.
- rounds: 1
- e2e: Go suite+vet+build clean; web 15/15 + tsc + lint baseline + both builds; smoke PASS; live walk reconciles exactly with CH (9 req/0%/$0.0003), badge matrix exact, zero $0.0000; mock intact (64 traces, no badges). Git tree clean, 43 intended entries.
- seam findings: TraceExplorer.tsx imports mock allTraces → "diff vs healthy run" link to a MOCK trace id on the live detail (latent; fires on root-name collision). Only finding.
- fix tasks: F8 (TraceExplorer.tsx + traces/[id]/page.tsx, F6/F7 executor; integrator re-walk is the gate — separate reviewer skipped for a 2-file one-prop change, integrator verification is independent)

## Goal check (Step 7, one entry per iteration)
<!-- Compares delivered work against the goal line at the top of this file, not just the task list. -->
- iteration 0: GOAL MET (advisor ruling with exit-criterion sign-off, 2026-08-15) — no gap. Carry-forward ledger: M2-gate (unpriced-label cardinality cap, prices.json as-of/refresh, GenAI event-form extraction, list-count/pagination, TracesSearch desync); M3-gate (real org/identity/notifications replacing SAMPLE markers, billing surfaces, dup-exposure revisit on design-partner evidence); M4-gate (badge/page build-vs-runtime mode seam, Writer.Close bound with Helm grace periods).
- gap tasks this round: none
- integrator gate: pending

## Retro (filled at end of run, 2026-08-15)
- tasks: 20/20 approved (T1-T12 + F1-F8), deferred: none. Iteration 0/3 (goal met first pass); integrator 1 NO-GO round (F8) then GO.
- breakdown corrections by advisor: D17-D27 + amendments — biggest: pricing split (T11), badge task (T12), registry placement amendment (live-routes.ts), honesty-principle extensions (F6/F7/F8).
- reviewer verdicts: approve 9 / fixed 7 / must-fix 0 (resume-session reviews).
- lesson: no task owned cmd/ingest/main.go's wiring after T2 closed — two tasks (T3, T9) delivered against a binary that listened on nothing; put an explicit integration-wiring task in the graph whenever a skeleton task closes before its consumers land. Second: "mock stays mock" needs an owner per SURFACE, not per file — unlabelled fiction leaked through every file nobody enumerated (overview widgets, shell chrome, trace-detail compare link).
