# Team Plan — obstack M4 sprint 4 — docs, status, changelog, landing: the launch content pass (exit: a quickstart-first docs site builds from the same content source the in-app docs render, /status and /changelog tell the truth by construction, and the landing page carries launch copy with no fiction left — "read the docs, follow them, and everything they say is true")

meta:
- date: 2026-08-30
- repo/branch: `s4.4-launch-content` @ `893db97` (master after PR #23 merged S4.3)
- mode: **manager-directs** (as S4.3: Fable 5 plans/reviews/rules; Opus@high executors one per disjoint file set, parallel where disjoint; batched Opus@xhigh reviewers; advisor + integrator gates)
- team-cap: executors per disjoint file set; stage explicit paths only; 2 batched reviewers; 1 advisor; 1 integrator
- models: executor=opus@high reviewer=opus@xhigh advisor=fable@xhigh integrator=opus@xhigh
- iteration-limit: 3 · iteration: 0/3
- source of truth: `.planning/2026-08-23-s4-m4-phase-plan.md` §3 S4.C3 (run-goal + exit criterion, verbatim) + §9 rulings **D255, D256, D257** (BINDING), D259 (honest absences), D262 (the marketing host IS the mock-mode web image), D261 (no docs CI job — the web build compiles the content), "Verdict and gates" critical item 4; the S4.3 bundle §7 handoffs (D309/D315/D310/D302/D308/D301/D316); the S4.2 bundle §7 (D292/D294/D295, receivers "coming soon"); the S4.1 operator-doc handoffs; **the fence: `.planning/2026-08-30-s4.4-landing-fence.md`** (D257's critical item, 116 rows)
- scope fence (phase plan): no product-feature work, no deployment, no monitor account creation (S5/U12). D13/D21 bind every new surface. AGENTS.md: this Next is not the one in training data — the vendored docs at `node_modules/next/dist/docs/` decide every Next question.
- standing guards inherited: D137/D207 (`e2e` the PR critical path, line HELD, band 5m28–6m02); `stack` on the re-based master-push watch (D318: first master run is the cold seed, the second is the datum); K3 forever-names (no new job — the docs build rides `web`/`images`); `web` band 4m20–4m30 with a 15m cap; the D246 repo-wide sweep (`TourGuide.test.ts`); the S3.4 quickstart byte-check (`Quickstart.test.ts`); D178 (no `@polar-sh/sdk` bump planned); the `sdk-js` `package.test.ts` README↔`package.json` mirror; S2.2 L1 (no activation claims — Vercel/CloudWatch stay "coming soon")

## Measured facts (S2.4 L1 — measured at breakdown, never believed)

Three read-only Opus passes, 2026-08-30, master @ `893db97`. Every claim carries a file:line; **(ruling)** marks a decision the measurement surfaced.

### NX — the mechanism, on the vendored Next 16.3.0 docs (`node_modules/next/dist/docs/01-app/02-guides/mdx.md`, `mdx-components.md`, `output.md`, `cacheComponents.md`)
- **NX1 — version + packages.** `apps/web/package.json:20` `next 16.3.0`, react 19.2.8; `next` hoists to the repo root `node_modules`. **No MDX package in the lockfile** (zero hits for `@next/mdx|@mdx-js|remark|rehype|gray-matter`). `@next/mdx` latest is **16.3.3**, installs beside 16.3.0; the guide's install set is `@next/mdx @mdx-js/loader @mdx-js/react @types/mdx` (`mdx.md:36-40`). CI node 24 (`web.yml:75`); images `node:24.19.0-slim` (`Dockerfile:18,55`).
- **NX2 — the ruled shape is first-party.** `mdx.md:275-325` "Using dynamic imports": `app/blog/[slug]/page.tsx` doing `await import(\`@/content/${slug}.mdx\`)` with `generateStaticParams` and `dynamicParams = false` (404 off-list). `mdx-components.tsx` is **required** for App Router (`:109`) and **its `useMDXComponents` takes no arguments** (`mdx-components.md:36` — differs from pre-16 memory). `pageExtensions` gains `md`/`mdx` (`:61`). Frontmatter: **not supported by default** (`:622`) but `export const metadata = {...}` / any named export works (`:628-643`) — no extra package if the export form is used. Plugins: ESM-only config (`:701`), **Turbopack requires plugin names as strings with serializable options only** (`:728,:760`). The Rust MDX compiler is experimental, "not recommended for production" (`:793`) — not used. `cacheComponents` is off in this repo (`next.config.ts` sets only `turbopack.root` + `output`) and is not needed for SSG.
- **NX3 — proven in a scratch build** (`next@16.3.0` + `@next/mdx@16.3.3`, `output:'standalone'`, `src/content/docs/**.mdx`, `app/docs/[...slug]/page.tsx` with an `fs` walk in `generateStaticParams` + dynamic import, string-form `remark-gfm`/`rehype-slug`): **`next build` 8.99s, both routes `● (SSG)`**, nested slugs work, `export const frontmatter` rendered as a named export, GFM tables and heading ids present. **Gotcha:** an `h1` override in `mdx-components.tsx` that does not spread `...props` silently drops rehype-slug's `id` (measured) — every override spreads props. **Standalone:** the compiled MDX is bundled into `.next/server/**` AND the source `content/**.mdx` was traced into `.next/standalone` with no `outputFileTracingIncludes`; `Dockerfile:61` copies the tree whole → both images carry it. Any route reading content with `fs` at *request* time is outside what was measured — every doc page is SSG, so none does.
- **NX4 — build constraints.** `web.yml:171` is a bare `npm run build` (D261: no docs job — a content failure fails `web` and `images`). Mode stamp (D251/D267): `OBSTACK_DATA_MODE` is a build-time input (`Dockerfile:36-53`), `images.yml:51-52` builds `obstack-web:live` and `:mock` from one source → **the docs/status/changelog renderer must be mode-blind** (no `resolveMode()` branch, no `@/mock/*` edge) so both images serve byte-identical content; `Quickstart.test.ts:217-222`'s "no edge to `@/mock/*`" test is the pattern to pin it.
- **NX5 — the D21 gap: no "product chrome" class.** `apps/web/src/lib/live-routes.ts:11-24` is the whole definition; `:33` `isLiveWiredRoute`. Consumers: `app/app/layout.tsx:147` (`live && <SampleDataBadge/>`), `SampleDataBadge.tsx:16` (null on a wired route), `CommandPalette.tsx:72` (`isLiveWiredRoute ? item.hint : "sample data"`). **`/app/docs` is not listed, so in live mode the real docs would carry "SAMPLE DATA — preview"**; adding it to `liveWiredRoutes` would be semantically false (it reads no workspace data). **(ruling — K3.)**
- **NX6 — two mount points, one auth wall.** `/app/*` is wrapped by `app/app/layout.tsx`, which at `:131` does `if (live && !(session && user)) redirect("/login")`. A public `/docs` is unauthenticated; an in-app `/app/docs` is auth-gated in live mode. One renderer, two mounts, one boundary between them. **(ruling — K2.)** Nav sites that hardcode `/app/docs`: `SideNav.tsx:56`, `TopBar.tsx:138`, `CommandPalette.tsx:31` (and `:33` for `/status`).
- **NX7 — the current in-app docs.** `apps/web/src/mock/docs.ts` (342 lines; `DocBlock`/`DocSection`/`DocArticle` types `:3-24`; 10 fictional Loopwork articles; 7 "Loopwork" occurrences), sole importer `components/docs/DocsLibrary.tsx:6` (client, 116 lines), route `app/app/docs/page.tsx` (19 lines, `:13-15` "Loopwork's authored infrastructure documentation…"). No search/index. **Tests that pin it:** `mock/corpus-honesty.test.ts:47` (bans fabricated hosts/credentials in the docs corpus) and `:62-67` ("every helm command in the runbooks installs a chart that exists in-repo" — `assert.ok(charts.length >= 1)` **fails closed on an empty corpus**). Deleting `docs.ts` breaks both — the helm assertion's real target (`helm install obstack deploy/helm/obstack --timeout 900s --wait`) moves to the MDX corpus, which S4.4 documents anyway.
- **NX8 — `/changelog` + the D246 sweep.** `app/changelog/page.tsx` (109 lines): the four entries at `:17-45` (verbatim in CS below); the `:5-16` D229/D246 docblock is what keeps the file honest in prose. **The sweep is `components/shell/TourGuide.test.ts`** — a repo-wide filesystem walk (`:20` repo root, `:56` skips `.git/.next/node_modules/.planning`, `:58-68` walk), asserting the case-insensitive hit set for "live tail"/"live-tail" (needles built at `:39-40`, never spelled) **deep-equals `ALLOWED = ["apps/web/src/components/marketing/ScreensShowcase.tsx"]`** (`:49`); `:75` `scanned.length > 200`; `:95-100` the case-sensitive set must differ (the fold stays exercised). → content under `apps/web/src/content/**` is swept automatically; the live risks are a content dir under a skipped path, and the `:96` hollowness guard if the one capital-L fixture goes.
- **NX9 — `/status`.** `app/status/page.tsx` (136 lines) imports `mulberry32` from `@/mock/rand` (`:3` — its only consumer outside `src/mock/`; `rand.ts` stays for `pick/between/hexId`). The fiction verbatim: `:5` "Public status page — SLO-fed, incident history included. Demo of Loopwork's page."; `:7-12` four components with `uptime90d` 99.97/99.89/99.99/100%; `:14-38` `UptimeStrip` seeded PRNG (`rng() < 0.02 ? "degraded" : "ok"` over 90 days); `:51-52` "Loopwork status · status.loopwork.ai · powered by obstack SLOs"; `:72-75` "All systems operational" / "recovered from a partial degradation earlier today (13:04–13:26 UTC)"; `:114-126` the INC-42 narrative "published from INC-42 · impact and timeline sourced from obstack automatically"; `:130-132` "demo page — this is what your customers would see, generated from your SLOs and incidents". Inbound: `app/app/slos/page.tsx:19`, `CommandPalette.tsx:33`, `mock/docs.ts:339` (dies with `docs.ts`). No test references it. `mock/incident.ts`'s types serve `/app/incidents` (a separate mock surface) — the curated incident files must NOT reuse them (D125/D208 seam).
- **NX10 — landing structure.** `app/page.tsx` (555 lines): footer `:547`; nav `:55-59` (Product/Connections/Pricing/Changelog — no Docs, no Status) + footer link row `:543-545`; pricing `:397-448` with the D226 comment `:394-404`, Free `$0 · 50k events/mo · 7-day retention · 20 Explain runs/mo`, Pro `$49 /mo + usage · 1M events included · 30-day retention · 200 Explain runs/mo`, Scale/Self-hosted `Planned` with no numbers — **all six numbers match the seed today** (`0005_metering.sql:128-129`, `0006_explain_metering.sql:20`); waitlist at `:124-133` and `:521` via `components/marketing/WaitlistForm.tsx` → `actions.ts` → `server/waitlist.ts:6-19` (Vercel Blob, `BLOB_READ_WRITE_TOKEN` appears **only** at `server/waitlist.ts:7`; `@vercel/blob@2.8.0` at `package.json:14` becomes unused) — inbound `signup/page.tsx:60`, `login/page.tsx:55` (`/#waitlist`) and **`signup/mock-mode.test.ts:94-98,116` asserts `/#waitlist` twice** (D156's shim-of-record); comparison table `:10-24` nine rows. The mirror pattern to copy: `server/ingest-endpoint.test.ts:20` (D266) — read sources as text, `deepEqual` against the single source; the SQL seed is parsed as text the way `Quickstart.test.ts:30` reads `package.json`.
- **NX11 — the quickstart byte-check** (`components/onboarding/Quickstart.test.ts`, 362 lines): reads `Quickstart.tsx` as text (`:27`) + both SDK READMEs + `obstack-js/package.json`; `tabCode()` (`:55-84`) reconstructs each tab resolving `${http}`, `${grpc}`, `${grpcLine}`, `${OBSTACK_JS_VERSION}`, `${key}` and asserts no `${` survives; verbatim comparisons: `pip install './packages/obstack-py[fastapi]'` (`:97-99`), `npm pack ./packages/obstack-js  # -> obstack-js-${version}.tgz` + `npm install ./obstack-js-${version}.tgz` (`:101-105`), `export OTEL_SERVICE_NAME=my-agent`, `export OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer%20` (URL-encoding load-bearing), `export OTEL_EXPORTER_OTLP_ENDPOINT=${OTLP_HTTP_ENDPOINT}` (`:134-148`); zero-arg `init()` + four banned forms (`:121-129`); banned literals `x-obstack-key`, `ingest.obstack.dev`, `obstack.dev`, `ok_live_9f2e` (`:173`); **endpoints interpolated, never spelled** (`:181-182`); the version fences read from `peerDependencies` (`:245-286`, D311). **Hard constraint:** an `.mdx` quickstart cannot spell the endpoint value or hardcode the SDK version — it imports the constants (MDX supports imports, `mdx.md:152`) or the test grows a parallel arm. **(ruling — K4.)**
- **NX12 — budget.** Local `next build` in `apps/web` **35.9s**, 36 static pages; the MDX probe built 2 pages in 9s (a 20–40-page corpus adds single-digit seconds). `web` last three: 4m26s · 4m30s · 4m20s (cap 15m). `images` builds two web images (grows by the MDX compile ×2).

### CS — the corpus sources and the sentences owed (inventory; the page map is in T2)
- **CS1 — there is no `/docs` route and no docs content on master** (`apps/web/src/app/` = `api, app, changelog, invite, login, signup, status`). D255 is literal: the corpus is written from zero; **the PRD requirement is "Quickstart-first docs site"** (`execution-prd.md:146-153` §11 GTM; `:171` Phase 4 deliverable "Self-hosted bundle, docs site, public demo environment"; the build-vision `:62` "docs/status/changelog content builds").
- **CS2 — the 29 handoff sentences owed to S4.4, with ruling ids** (the executor writes them as ruled, never paraphrased into something stronger): (1) D309 ai@7 telemetry on by default, opt-out `telemetry:{isEnabled:false}`; ai@5/6 `experimental_telemetry:{isEnabled:true}` per call (`obstack-js/README.md:203-232`); (2) D315 per-call `telemetry:{integrations:[…]}` replaces the global array — obstack draws nothing for that call (`:234-246`); (3) D310 the `ai` floor is **7.0.0 measured** — never "the whole major" (`:253-262`); (4) D303/D317 ai@7 needs node ≥22, obstack-js stays ≥20 (`:248-251`); (5) D302 register obstack-js OR `@ai-sdk/otel`, not both (`:264-270`); (6) D308 Responses covered from openai 4.87, `parse()` through it; NOT `stream`/`responses.stream()`/`beta.responses`/`compact()`/`retrieve(id,{stream:true})` (`:122,157-165,187-192`); (7) D301 no finish_reason on Responses — obstack records `status` (`:180-185`); (8) D259 streaming not instrumented on any of the three SDKs (`:137-141`; `obstack-py/README.md:109-111`); (9) D259 no JS logging bridge — "no bridge from `console`, `pino` or `winston`" (`:277-280`); (10) D316 fail-open = "a span with request attributes only", never "no span" (`:173-178`); (11) S4.1 gap — `images`/`lint` have no per-check README bullets (`README.md:135-145`); (12) D292/CH3 "gone from every product read the moment the statement returns; disk shrinks in the background — both halves" (`retention.go:8-14`); (13) D292 straddling trace: summaries expire on `max_seen_date`, older spans go first (`:43-46`); (14) D294 the bound is a stated posture: ~100M+ past-cutoff rows in one workspace-table; failure = loud, counted, next workspace, retry tomorrow; consequence = over-retention until the 90-day TTL (`:29-41`); (15) D295 Vercel grouping identity is the **project id**, `projectName` rides as `vercel.project.name`; (16) receivers built + CI-exercised, "coming soon" because the hosted endpoint is S5's — self-hosters can use both today (`connectors.ts:141-148`; `cloudwatch-forwarder/README.md:7-11` verbatim); (17) D251/D265/D267 mode stamp: a live-stamped artifact refuses to serve as mock and vice-versa, naming both values; (18) D265/D272 compose secret: `OBSTACK_BETTER_AUTH_SECRET` → `BETTER_AUTH_SECRET`, no default, refuses to boot with the `openssl rand -base64 32` instruction (`.env.example:18-25`; `compose/README.md:42-48`); (19) D253/D123 chart `existingSecret` is a name never a value; rotation is a documented procedure, never automated, per credential (`helm README:406-455`); (20) D263/D278 grace: chart `terminationGracePeriodSeconds: 45`, compose `stop_grace_period: 45s`, overrun drops counted under `ReasonShutdown`; (21) D253/D284 managed Postgres disables the in-chart one and retargets the pg-migrate Job — the Job failing IS the reachability gate; real-provider TLS/`sslmode` verified at S5 (`helm README:363-388`); (22) D119 an `http://` origin downgrades the session cookie away from `__Secure-` (`.env.example:31-35`); (23) D254 trace context only from measured payload fields — no inferred correlation, ever; (24) D255 corpus list — signup, quickstart (byte-check re-runs), self-hosted bundle, connectors incl. receivers' coming-soon, Explain, billing, D259 absences; (25) D255 the four changelog entries migrate verbatim and the D246 sweep's coverage survives the move; (26) D255 `mock/docs.ts` deleted with its render paths, both modes render the real corpus; (27) D256 status = components (app, ingest, docs) + curated incident files + "external monitoring begins at launch", no fabricated uptime; (28) D257 landing floor (footer, nav, pricing mirror, waitlist out); (29) the idea doc's docs sentence — **stated in the absolute form today's truth allows**: `traceAgent`/`traceTool` capture no arguments and no return values (`obstack-js/README.md:110-112`).
- **CS3 — the only true pricing source** is `0005_metering.sql:121-129` + `0006_explain_quota.sql:21-23`: `free` 50,000 events/mo · 7 days · $0 · 20 Explain runs; `pro` 1,000,000 · 30 · $49 · 200. **No `scale` row, no overage column, no seat column.** The mock `BillingTab` (`SettingsSuite.tsx:813,815`: "1M events/mo included, then $1.50 per 100k", "Unlimited seats") and the mock ingest/audit copy (`:1229-1253`: "90-day audit retention · exportable as JSON on Enterprise") are PRD hypotheses with no catalog column — **none may enter the docs**.
- **CS4 — true sources by page** (T2 writes from these and nothing else): root `README.md` (run mock/real, the two SDKs, CI checks, repro); `obstack-js/README.md` (install-from-source, `init()`, coverage table `:119-126`, floors, the two `ai` mechanisms, fail-open); `obstack-py/README.md` (install, `init()`, coverage `:102-107` — **the Responses API is in Python's Not-covered column, a real asymmetry to state**, D8 table `:122-130`, `OTEL_*` env table `:160-167`); `deploy/compose/README.md` (boot, secrets, collector route, smoke, drive, guard, SDK evidence, keys + `ok_dev_local`, pins); `deploy/helm/obstack/README.md` (D35 boundary, the destructive 0.2.0 upgrade, Jobs, `--wait`, managed Postgres `:363-388`, credentials & rotation `:406-455`, collector contract, kind repro, acceptance); `cloudwatch-forwarder/README.md`; the demo READMEs; `Quickstart.tsx:31-127` (the D78-verbatim three tabs — the Python-only `OTEL_SEMCONV_STABILITY_OPT_IN` asymmetry is deliberate); `connectors.ts` (3 available: OTLP, Kubernetes, Docker; 16 coming soon); `SettingsSuite.tsx:665-760` (the LIVE billing tab formats `plans` columns, defines none); `explain/contract.ts:29-42` (two refusal reasons `not-configured`, `over-quota`), `quota.ts:5-11`; `retention.go:1-46`; `receive/vercel.go:24-41`, `cloudwatch.go:18-33` ("S5 activates it" — carried, not dropped); `.env.example`; `values.yaml` groups (`clickhouse:20`, `postgres:63`, `web:120`, `ingest:176`, `collector:230`, `demo:261`, `utilityImage:282`).
- **CS5 — already-stale sentences on master the docs must not copy** (and the root-README pass fixes): `README.md:5` "This repo contains Phase 0 … and Phase 1" (the repo is at Phase 4); `:135,:213` `images`/`lint` named, never described; `:145` "The real number returns with the first runner" for `sdk-e2e` (it returned: 4m40s); `:140` `stack` "5m58s–7m17s" without the D318 rider; `obstack-js/README.md:20-21` names the tgz version literally (docs must interpolate `OBSTACK_JS_VERSION`).
- **CS6 — the 21 honest absences** (each with its ruling; T2's "what obstack does not do" page): no JS logging bridge (D259); streaming untraced on all three SDKs (D259); Responses edges (D308) + below-4.87 no module; obstack-js OR `@ai-sdk/otel` (D302); the D316 request-attributes-only span; obstack-py covers chat completions + FastAPI only (D87); `traceAgent`/`traceTool` capture no arguments/returns (D78); Vercel/CloudWatch built, CI-exercised, coming soon until the hosted endpoint (D101/D208) — self-hosters can use both today; drain/CloudWatch logs carry no trace context, none invented (D254); no Pub/Sub, Event Hub, Heroku/Fly/Railway drains, syslog, Loki/Elastic compatibility (D264b); no MCP/skill/CLI vehicle (D264a); dashboards/diff/explore/infra/map/services/users/cost persistence are M5 — nothing saved (D264c); no alerts/oncall/SLOs/incidents/issues/changes (M6) — the status page is deliberately not SLO-fed; no Ask/evals/security-redaction/pipelines/SSO/SOC2 (M7); exactly `free` and `pro` — no Scale, no overage, no seats (D226/D163); retention enforced by a daily sweep with a stated bound (D294); deleted data gone from reads immediately, disk at merge (D292); external uptime monitoring begins at launch — no numbers until then (D256); managed Postgres verified in kind only (D284); expired invitations never reaped, grant nothing (D153); nothing tails or polls — refreshing is a button (D273).

### LF — the landing fence (`.planning/2026-08-30-s4.4-landing-fence.md` — the record; summary here)
- **LF1 — 116 claims: 51 T / 40 F / 16 U (14 ‡) / 9 S.** The 40 FALSE resolve to 16 replacement classes (fence §2): "one env var" ×4 → three env vars (`connectors.ts:83`); "live demo" ×4 → "the demo"; "a real failure" → "a sample failure"; connector breadth ×3 → "Kubernetes, Docker and any OpenTelemetry source today — … on the way"; "your own watches" → the live overview's real four numbers; **"Live tail across every pod" → search copy, and the `TourGuide.test.ts:49` allowlist emptied**; "& retries" dropped; "& feature" → "and model"; the K8s-events row/sentence → pod/node/container identity (`0001_spans.sql:34-37`); "one store" → "one telemetry store"; "one container to self-host" → "one compose file"; "Point your OTel at us" → "at your obstack"; the waitlist block ×2 + "private preview" ×2 deleted; the auth pages' `/#waitlist` links replaced; **the four screenshots re-shot** (pixels carry "ingesting", `ziad@loopwork.ai`, "live tail", Vercel/CloudWatch shown CONNECTED, a K8S-EVENTS track — bans the suite enforces in source and cannot see in a PNG); `/status` deleted with its two in-app links.
- **LF2 — the 14 rulings (fence §3):** the "six tabs and a guess" framing (L24-27); "A full observability platform — not just a trace viewer" (L35 — no metrics/alerts/SLOs/dashboards are wired); Alerts/Ask/Evals visible in the marketing screenshots (L44 — M6/M7); "The join is the product" (L49); **every Datadog/Grafana/Langfuse/Helicone cell and bullet — 18 cells + 4 bullets, none measured** (L50/L60/L63/L69); "Priority support" on Scale (L79). Two verifications: the "2-line SDK" claim is true in content but neither package is published (L71 — S5 gate or "install from source"); Pro "+ usage" — the meter exists, the unit price lives in Polar (L77 — S5).
- **LF3 — `:547` is the ONLY honesty label** over the hero trace (`HeroTrace.tsx` imports `mock/stories.ts:251`), the video, and the screenshots (fence §6). Inside the app D21's per-surface grain holds (`DemoFooter`, `SampleDataBadge`, the auth pages' prose); on the landing it does not. **(ruling — K9: per-surface labels before `:547` goes.)**
- **LF4 — nothing tests the landing** (fence §5): `page.tsx` is read by no test; the only indirect coverage is the allowlist that pins the lie and `connectors.test.ts:118`. The fence test S4.4 owes: pricing mirror (+ no Scale numbers), banned-phrase sweep with an empty allowlist, the sample-label invariant (imports `@/mock/*` ⇒ a SAMPLE marker in the file), the connector-breadth mirror (3 available), the link set (every `href` resolves — would have caught `/#waitlist`), an image manifest (each `shots/*.png` by content hash).
- **LF5 — D262 cross-host wiring:** both "Create your workspace" CTAs (`:66`, `:526`) and the auth pages' links must reach the LIVE host from the mock marketing host; the URL is S5's (U10/U11). **(ruling — K13.)**

## Advisor decisions
<!-- Kickoff round 2026-08-30, recorded as D319–D330 continuing the ledger. Citations verified on the tree at b3f6a54; verdict: CLEARED WITH CONDITIONS (D319 tree under src/ + lockstep pin, D320 no runtime fs, D321 DemoFooter, D323 hollowness guard — applied to T1/T3/T5 below). -->
- **D319 (K1) — Mechanism RATIFIED, two corrections.** `@next/mdx` + `@mdx-js/loader` + `@mdx-js/react` + `@types/mdx` per `mdx.md:36-40`, plus `remark-gfm` + `rehype-slug` string-form (`mdx.md:728-760`) — **six** deps, exact pins. **`@next/mdx` pins to `16.3.0`, lockstep with `next`** (NX3's 16.3.3 is a mixed pair nobody ships). T1's first done-check is the scratch build re-run on 16.3.0; if string plugins fail there, bump `next`+`eslint-config-next`+`@next/mdx` to 16.3.3 *together* — never mixed. **Content tree lives at `apps/web/src/content/{docs,changelog,status}/`**, not `apps/web/src/content/`: `tsconfig.json` maps `@/*` → `./src/*`, so the guide's `import(\`@/content/${slug}.mdx\`)` (`mdx.md:295`) resolves only under `src/`; a second alias for one tree is refused. Under `src/` it is still swept (`TourGuide.test.ts:56` skips only `.git/.next/node_modules/.planning`) and never routed (not under `app/`). `pageExtensions` = the guide's list verbatim; `apps/web/src/mdx-components.tsx` zero-arg (`mdx-components.md:36`), every override spreads `...props` (NX3 gotcha); frontmatter = `export const frontmatter` (`mdx.md:628-643`). `dynamicParams=false` + `generateStaticParams` on the public mount; SSG only.
- **D320 (K2) — (b) RATIFIED: one `DocsPage` + one loader, two thin route files. Binding: no `fs` in any runtime module.** `app/app/layout.tsx:110-112` calls `connection()`/`getSessionContext()` in live mode, so `/app/docs/*` renders per request in the live image — a request-time `fs` walk there is exactly what NX3 did not measure. Shape: `src/content/docs/manifest.ts` (ordered slug list with section grouping — nav order IS array order; titles from each MDX's `frontmatter` export via `import()`); `generateStaticParams` returns the manifest; the loader `notFound()`s any slug not in it (both mounts, one place — do not rely on `dynamicParams` under the dynamic layout); `docs.test.ts` walks `src/content/docs/**` with `fs` and `deepEqual`s the file set to the manifest (D206 mirror shape — a page added without a manifest entry is red). Same rule for changelog and incidents (D323/D324). In-app nav stays `/app/docs` (`SideNav.tsx:56`, `TopBar.tsx:138`, `CommandPalette.tsx:31`).
- **D321 (K3) — chrome class RATIFIED, with a third consumer the plan missed.** `live-routes.ts` gains `productChromeRoutes = ["/app/docs/"]` (+ exact `/app/docs`) and `isProductChromeRoute()`; `liveWiredRoutes` untouched. Consumers: `SampleDataBadge` (null on chrome), `CommandPalette.hintFor` (hint `"docs"`), **and `DemoFooter`** — `app/app/layout.tsx:149` renders "every screen here is sample data from a fictional company — nothing is being ingested" on every `/app` route in mock mode, i.e. under the real docs on the D262 marketing host: the inverse lie. `DemoFooter` moves to `components/shell/DemoFooter.tsx` as a client component (the `SampleDataBadge` shape: `usePathname`, null on chrome); `shell-honesty.test.ts:91` retargets to that file, `:92` unchanged.
- **D322 (K4) — (a) RATIFIED; the docs render the D215 constants, not a placeholder.** `snippetsFor`/`sdkTab`/`httpAbsence` (`Quickstart.tsx:59-130`) move verbatim to `components/onboarding/snippets.ts` (pure, no directive, no hooks); `Quickstart.tsx` imports it; `QuickstartSnippets.tsx` (server) renders the three tabs stacked for MDX; `Quickstart.test.ts:27` reads the new module. The docs page interpolates `OTLP_HTTP_ENDPOINT`/`OTLP_GRPC_ENDPOINT` from `@/lib/ingest-endpoint` (compose's published ports — the self-hosted default, `ingest-endpoint.ts:5-9`) with one sentence: "your deployment's own endpoint is on the in-app quickstart". A `<your endpoint>` placeholder is a third definition of nothing and breaks copy-paste — refused; the hosted endpoint stays out of copy until owned (D101). New test arm: `src/content/**` contains none of `Quickstart.test.ts:173`'s banned literals, no unresolved `${`, and no line matching `pip install|npm install ./obstack-js-|OTEL_EXPORTER_OTLP_` — every snippet line comes from the component.
- **D323 (K5) — changelog RATIFIED; date rule made exact.** Files `src/src/content/changelog/YYYY-MM-DD-slug.mdx`, `frontmatter = {date, kind, title}`, manifest per D320, newest-first, bodies byte-pinned. **Date = the master merge date of the PR that made the entry true**, from `git log --merges`. C6 understates: the repo's first commit is `87c60a4` 2026-08-09, so "Jul 28" is impossible, and "Aug 6/8" precede the S2.2/S2.3 merges (`fd3ab6a` Aug 16, `9fabcde` Aug 17); only Explain's Aug 20 (`2443922`) is right. Each file names its merge in a comment. Sweep continuity: a `TourGuide.test.ts` assertion that `scanned` includes `apps/web/src/content/**`. **The `:95-100` hollowness guard must be reshaped when `ALLOWED` empties** — `notDeepEqual([], [])` fails by construction; replace the fixture proof with a synthetic one: `matches(NEEDLES[0].toUpperCase().toLowerCase())` true and `matches(NEEDLES[0].toUpperCase())` false — built from `CLAIM`, never spelled. T3+T5 coordinate.
- **D324 (K6) — status shape RATIFIED; SLO link REMOVED, palette kept.** `app/slos/page.tsx:19` reads "public status page" from an M6 mock surface — the link itself implies the page is SLO-fed (D256's deleted claim); remove. `CommandPalette.tsx:33` (hint "public") stays. Incidents: `src/src/content/status/incidents/` + `README.md` (contract) + empty manifest; "No incidents recorded". Test: no `/\d%/` in the page source.
- **D325 (K7) — THE FENCE. The 16 classes are THE landing copy, with these exact texts; the 14 ‡ ruled.** Class 1 → "three `OTEL_*` variables" (`connectors.ts:83`): L11 "OTel-native · three env vars to try it"; R9 "Three OTEL_* variables to try with existing OTel"; L68 "three env vars to try · one compose file to self-host". Class 4 → "Kubernetes, Docker and any OpenTelemetry source today; the rest of the catalog is listed as coming soon." ("on the way" refused — S2.2 L1.) Headline "If it speaks OTel, it plugs in today." Class 5 → "Requests, errors, latency and LLM cost for your workspace." (`app/app/page.tsx:164` stat row.) Class 6 → the search sentence as proposed; allowlist emptied (D323 guard). Class 9 → **"Pod and container identity on every span and log line"** (`0001_spans.sql:34-37`; `0002_logs.sql:23-25` — logs carry pod/container/namespace, no node: "node" dropped). Classes 2, 3, 7, 8, 10, 11, 12, 13, 15, 16 as proposed. Class 14 → "Open the demo" + `/docs/quickstart`. **L24-27, L49: KEEP** — unnamed problem framing and opinion; the rule: positioning is allowed when it names no vendor and asserts no obstack capability. **L35: CUT** → "One trace across your whole stack — not a trace viewer bolted onto a log store." **L44: uncropped, real sidebar** — the shot is of the demo strangers reach one click away; the DEMO WORKSPACE label is in the pixels, D326's caption covers the rest (the current shots have two different sidebars; the re-shoot from one build ends that). **L50/L60/L63/L69: competitor columns CUT, not hedged.** 18 unmeasured cells about named vendors are believed-not-measured claims to strangers; at least one is false on its face (Datadog LLM Observability shows prompts inline). `comparisonRows` loses `apm`/`llm`, `Mark`, and the L61 legend; the nine rows render as obstack's capability list under the same H2. The assembled-vs-joined block STAYS (L62 product names, L63 bullets are LGTM architecture facts) with two edits: bullet 3 → "correlation holds only while labels match"; L69 parenthetical → "(agent runs, prompts, token cost)". **L79: drop "Priority support"**; Scale items = two. **L71** → "Or add our two-line SDK for Python / TypeScript — installed from this repo today." ("at launch" refused, D101.) **L77** stays; Polar unit price on the S5-GATE. The fence test per LF4 (a)–(f) with `connectors.ts` as the count source for **19 chips / 16 SOON / 3 available** (imprecision 4).
- **D326 (K8+K9) — re-shoot IN; labels one definition; video gets a caption.** All four shots at 1440×900, DPR 1, from the mock build at the T5 tip, sidebar uncropped; manifest pins sha256 + the commit. `SAMPLE_COPY = "sample data from a fictional company"` exported from one module, consumed by `DemoFooter`, the HeroTrace caption (`:152` becomes "a sample failure, joined — {SAMPLE_COPY}: pod OOM-kill → …"), a caption under the `<video>` (`:137-144`; its own `<section>`, so its own label), and the showcase (`ScreensShowcase.tsx:58` above the image). Invariant test: a file under `components/marketing/**` or `app/page.tsx` that imports `@/mock/` **or references `/shots/` or `.mp4`** must import `SAMPLE_COPY` (transitive trigger). Then `:547` → `© 2026 obstack`.
- **D327 (K10) — RATIFIED as written.** `mock-mode.test.ts:97,115` → assert `/app` and `/docs/quickstart`, and assert `/#waitlist` absent. Blob entries: user's, untouched.
- **D328 (K11+K12) — both RATIFIED.** README pass in scope, prose only. obstack-py's Responses absence stated (`README.md:104`), no code.
- **D329 (K13) — (b) RATIFIED in the `OBSTACK_DATA_MODE` shape, scoped to the two landing CTAs.** `/` is SSG, so any env read is baked at prerender — name it honestly: `OBSTACK_APP_ORIGIN` (no `NEXT_PUBLIC_`; server-only module `lib/app-href.ts` with `import "server-only"`), `ARG`/`ENV` beside `OBSTACK_DATA_MODE` in `apps/web/Dockerfile:42-43`; unset = relative. Consumers: `:66`, `:526` only. The auth pages stay same-host: their prose (`signup:50-53`, `login:47-49`) and `:118-121` are true today and false the day S5 deploys — **S5-GATE item: the hosting flip re-fences every "you run"/"don't host" sentence and the banned-phrase list gains them.** T5 owns the Dockerfile lines; `images.yml` unchanged until S5.
- **D330 (K14) — RATIFIED.** Six deps, `web`/`images` first-runner numbers return in the bundle (D137 shape); cap 15m.

**Verdict: CLEARED WITH CONDITIONS** — D319 (tree under `src/`, lockstep pin), D320 (no runtime `fs`), D321 (DemoFooter), D323 (hollowness guard) applied to T1/T3/T5 below.

**Imprecisions the advisor found (eight, none void-making) — corrections applied:** (1) "four MDX deps" → six (the two plugins); 16.3.3 mixed with 16.3.0 refused. (2) `apps/web/src/content/**` unreachable via `@/content` → `src/content/**`. (3) NX5 named two chrome consumers; `DemoFooter` (`layout.tsx:149`) is the third and the one the marketing host shows. (4) Fence L33 "18 chips, 15 SOON" → `connectors.ts` has 19 (3 available + 16 coming-soon), rendered whole. (5) The explain-quota file is `0006_explain_quota.sql`, values at `:20`, block `:19-22`. (6) C6 "unpinned" understates — one date impossible, two precede their merges. (7) *The advisor's own imprecision:* `docs/superpowers/specs/2026-08-09-obstack-execution-prd.md` IS tracked (`git ls-files` — manager-verified); the §11 sentence enters T2 by paste anyway, with D325/L70's "at your obstack" wording, since the PRD's "at us" is the fundraising register. (8) `connectors.ts` is `components/connections/connectors.ts`.

## Kickoff questions for the advisor
- **K1 — the mechanism, as measured (NX1–NX3): ratify.** `@next/mdx@16.3.3` + `@mdx-js/loader` + `@mdx-js/react` + `@types/mdx` (dev + runtime as the guide states); `next.config.ts` gains `pageExtensions` + `withMDX` with string-form `remark-gfm`/`rehype-slug`; `apps/web/src/mdx-components.tsx` (zero-arg `useMDXComponents`, every override spreads props); the content tree at **`apps/web/src/content/{docs,changelog,status}/**.mdx`** (NOT under a sweep-skipped dir — NX8) with `export const frontmatter = {title, order, …}`; one catch-all `app/docs/[[...slug]]/page.tsx` with `generateStaticParams` over an `fs` walk at build time + `dynamicParams = false` + dynamic `import()`; SSG only, no request-time `fs`. Content version pins: exact.
- **K2 — one renderer, two mount points, one auth wall (NX6).** Options: (a) `/app/docs` REDIRECTS to `/docs` — one mount, identity trivially satisfied, in-app "Docs" nav points at the public route (the app shell is not around it; the reader leaves the shell); (b) `/app/docs/[[...slug]]` renders the SAME server component + content loader inside the app shell (auth-gated in live mode, exactly as every `/app` route is), so "in-app docs" is literal. PM lean: **(b)** — amendment 6 says the in-app surface renders the same content source; one `DocsPage` component + one loader, two thin route files; the auth gate is the app's existing posture, not a docs decision.
- **K3 — the D21 third class (NX5).** `live-routes.ts` gains a named `productChromeRoutes` set (`/app/docs` — and any future chrome), consumed by `SampleDataBadge` (null for chrome in either mode) and `CommandPalette` (hint "docs", never "sample data"); `liveWiredRoutes` untouched. Ratify the shape — never smuggle chrome into the live set.
- **K4 — the quickstart in docs (NX11).** Options: (a) the docs quickstart page renders the SAME snippets through a shared server component that `Quickstart.tsx` also uses (one definition of the D78-verbatim strings; the byte-check covers both by construction, plus a "no second copy" test that greps `src/content/**` for the banned literals and for any `npm install ./obstack-js-` / `OTEL_EXPORTER_OTLP_ENDPOINT=` line — every such line must come from the component); (b) duplicate the strings in MDX and grow `Quickstart.test.ts` a parallel arm. PM lean: **(a)** — D215/D206 one-definition. Note the endpoint interpolation: the docs are SSG and mode-blind, so the endpoint the public docs show is the **placeholder form** (`<your ingest endpoint>` / the self-hosted default), while the in-app quickstart interpolates the deployment's real endpoint — state which the docs render and why (the docs are not the onboarding page).
- **K5 — `/changelog` on the pipeline (NX8).** `src/content/changelog/YYYY-MM-DD-slug.mdx` with `export const frontmatter = {date, kind: "new"|"improved", title}`; `app/changelog/page.tsx` lists them from a build-time index (an `fs` walk inside the page's build — it is a static route); the four entries migrate verbatim; the `:5-16` docblock's substance moves to `src/content/changelog/README.md` (swept). Done-check: a test asserting the D246 sweep's `scanned` set includes `apps/web/src/content/**` (so the "coverage survives the move" clause is pinned, not believed) and that the allowlist is **empty** after T5. **Dates:** the four dates are unpinned (C6) — verify against `git log` for the commits that shipped each; if a date is wrong, it is corrected (a changelog date is a claim).
- **K6 — `/status` shape (D256, NX9).** `app/status/page.tsx` rewritten mode-blind: a component list (`app`, `ingest`, `docs`) each stating **what it is** (no state pill — there is no monitor to feed one), a "Monitoring" slot stating plainly that external uptime monitoring begins at launch (no numbers, no strip), and "Incidents" listing `src/content/status/incidents/*.mdx` (`export const frontmatter = {date, title, status: "investigating"|"monitoring"|"resolved", components: [...]}`), rendering "No incidents recorded" when the directory is empty — which it is at launch. The Loopwork page deleted; `mulberry32` import gone; `app/slos/page.tsx:19` link → removed or pointed at the real page? PM lean: pointed at the real `/status` (the SLO page is mock; a link to a true page is fine), `CommandPalette.tsx:33` kept pointing at the real page. Ratify the file shape + the two link dispositions.
- **K7 — the fence's FALSE→TRUE replacements (LF1, fence §2): ratify the 16 classes as the copy.** And **the 14 ‡ rulings (LF2)**: PM leans — keep opinion-class positioning ("six tabs and a guess", "the join is the product") as the voice, not claims; **cut** "A full observability platform — not just a trace viewer" → "One trace across your whole stack — not a trace viewer bolted onto a log store"; **drop** "Priority support" from Scale (no commitment exists); **the comparison table**: keep the nine rows with obstack's three FALSE cells fixed (retries, feature, K8s events), keep the competitor cells **but** add a dated, hedged legend ("as we read their public docs in August 2026 — tell us if we are wrong", with the date a literal the fence test pins) — or cut the competitor columns entirely. The advisor decides which; the PM lean is the dated legend (the table is the page's argument). L71 "2-line SDK" → "install from source today; on npm and PyPI at launch" (true now, S5 flips it). L77 stays with "+ usage" and the S5 Polar verification registered.
- **K8 — the screenshots (LF1 item 15).** Re-shoot the four `shots/*.png` from the CURRENT mock build (the D262 marketing host is the mock image — the screenshots must be of what strangers will see) with the current sidebar; **L44**: the sidebar shows Alerts/Ask/Evals (mock surfaces of M6/M7 products) — options: crop them out, or accept them as the SAMPLE-labelled demo they are (the DEMO WORKSPACE label is baked into every shot). PM lean: shoot the real sidebar uncropped (the demo shows the demo; the label is on it) and let the fence's image manifest pin the hashes. The hero video: keep, add the per-surface sample label (K9). Tooling: the gstack `/browse` headless browser against the local mock build at a fixed viewport; the shots' dimensions recorded. Ratify IN (S1 L2: pixels no sweep reads).
- **K9 — the `:547` replacement (LF3).** Per-surface labels: the hero trace block and the screens showcase each carry a visible "sample data — a fictional AI support-agent company" line (the same wording the app's `DemoFooter` uses, D134/D228), and the fence test's sample-label invariant enforces it (a file importing `@/mock/*` must contain the marker). Then `:547` becomes `© 2026 obstack` + the link row. Ratify.
- **K10 — the waitlist removal (D257, NX10).** Delete `WaitlistForm.tsx`, `marketing/actions.ts`, `server/waitlist.ts`, `lib/waitlist.ts` + `.test.ts`, the two render sites, `@vercel/blob` from `package.json`; `signup:60`/`login:55` → "Open the demo" + the docs quickstart link; `signup/mock-mode.test.ts:94-98,116` reconciled (it is D156's shim-of-record — the assertion moves to the new links, not deleted). `BLOB_READ_WRITE_TOKEN` exits by deletion (it lives only in `server/waitlist.ts:7`). **The collected entries' disposition is the USER's** (asked 2026-08-30; pending — nothing in this sprint touches the Blob store either way). Ratify.
- **K11 — the root README operator pass (CS5) is IN this sprint** — it is product surface a stranger reads first: `:5` phase sentence, the `images`/`lint` bullets (S4.1 gap), `:140/:145` CI numbers with the D318 rider, plus any sentence T2 finds false while sourcing from it. Ratify as in-scope (no code).
- **K12 — the Python asymmetry (CS4):** obstack-py does not cover the Responses API (`README.md:104`); the docs state it as an absence; no code this sprint (M5-planning packet if it matters). Ratify.
- **K13 — D262 cross-host links (LF5).** The mock marketing host's "Create your workspace" and the auth pages' links must reach the live host, whose URL is S5's (U10/U11). Options: (a) leave `/signup` as-is and register the rewire as an S5-GATE item; (b) add now one build-time `NEXT_PUBLIC_OBSTACK_APP_ORIGIN` (default empty = same host) read by a single `appHref(path)` helper used by every cross-host link, so S5 sets one value. PM lean: **(b)** — additive, one definition (D215 shape), the mock host stops sending strangers to "there is no workspace here" the moment S5 sets it; registered on the S5-GATE either way.
- **K14 — budget.** `web` 4m20–4m30 + MDX compile (single-digit s) + four new deps in `npm ci`; `images` ×2; `e2e`/`sdk-*` untouched. No new job (K3). The first-runner numbers return in the bundle; the `web` cap is 15m.

## Review record
- **R1 (docs mechanism + corpus + changelog + status: T1–T4, range `8cf7fe6..1b31cac`; Opus, read-only, archive copy of `1b31cac` with a fresh `npm ci`) — verdict MUST-FIX; mechanism sound, corpus "the best-sourced prose in this repo".** Verified: 63/63 static pages, 28 docs routes `● SSG`, `/changelog` + `/status` prerendered; 468/415/0/53; eslint + `tsc --noEmit` clean (note: `apps/web` has no `typecheck` script); standalone carries 28 docs `.html`, 0 `.mdx` traced. **Falsifications all red as claimed:** unregistered page → red; manifest entry without page → red; mirror 50,000→50,001 → red; `pip install` planted in an `.mdx` → red; the silent-`{}` catch-all behaviour reproduced (35 pages, still `●` green); a missing `frontmatter.title` fails the build loudly (no third silent behaviour). Snippet byte-identity re-measured identical on both routes; `snippets.ts` is the verbatim move (+2 `export`). Corpus sourced page-by-page against `retention.go`, `keystore/store.go:81`, `prompt.ts:26-28`, `0005`/`0006`, `connectors.ts` (19 = 3 + 16), compose/values, both SDK READMEs — **zero banned residue**; all 29 owed sentences placed, 12 spot-checked verbatim-as-ruled; all 21 absences present, none overstating. T3's dates independently re-derived — all four correct and true at their merges (the "Traces carry their logs" suspicion refuted: `b9691ec` is an ancestor of `fd3ab6a`). Lockfile delta exactly the six roots. **MUST-FIX:** (1) `TourGuide.test.ts:131-135` — the reshaped fold guard asserts `matches(shouted.toLowerCase())` (String.prototype), never the sweep's `.toLowerCase()` at `:102`; masked by the Title-Cased `ALLOWED` entry — with `ALLOWED=[]` and the fold deleted, a planted "Live Tail" ships green → `foldedMatches` at both sites (**T5's file — routed to T5**); (2) `status.test.ts:35-40` `SURFACE` excludes incident `.mdx` bodies — a fixture "99.4%" shipped green while the README claims otherwise; (3) `status/page.tsx:66,:108` unhedged "The OTLP endpoint your telemetry is sent to" / "the components obstack runs" while the corpus says the hosted endpoint is not public; (4) `:34-35` "only consumer outside its own directory" false (21 `src/app` files import `@/mock/*`; true form: `@/mock/rand` has none); (5) `:16-19` "never spelled anywhere under `src/`" false (`Loopwork` ×9, `INC-42` ×11 in the demo corpus; the test hunts four files); (6) `content/docs/README.md:11-12` "nothing depends on the mount" — 20 absolute `/docs/…` body links render on `/app/docs/*` and drop a signed-in reader out of the shell (verified in the prerender). **SHOULD-FIX 7–15:** the changelog reversal test is vacuous for slug-ascending input (all 24 permutations); the sort fixture keys on the slug date not `frontmatter.date`; the `merged:` comment's sha is shape-checked, never verified against `git log`; `aws-cloudwatch` "`npm run package`" lacks its `cd`; the D282 gRPC-only sentence renders as a flat false paragraph (quote it) and "works as written" understates the endpoint change; the TS page dropped D310's "of the 80"; the TS sample computes `facts` and never uses it (the `${` ban broke the template literal); `incidents.test.ts:81,:187` hardcode the empty state without saying why; `status.test.ts:149` bans "status page" in comments. **NOTE, manager-ratified:** `what-obstack-does-not-do` says "nothing tails or streams" with the quickstart's 5-second poll as the stated exception — the ruled D273 form "or polls" was FALSE (`Quickstart.tsx:13 POLL_MS`); the docs' form stands, recorded for the advisor. → **must-fix 1 to T5 (owns the file); F1 dispatched for 2–15 in parallel.** **F1 DONE @ `944f30c` (status + incidents) + `d081cd0` (docs links, corpus, changelog tests): 487 / 434 / 0; 63/63 pages; eslint + tsc clean.** (2) `SURFACE` = the four modules + every incident `.mdx` read at test time (code-shaped bans stay on modules, reason stated); fixture "99.4%" → red naming the file. (3) `ingest` → "The OTLP endpoint your self-hosted stack runs today — and the hosted one at launch."; "The components an obstack deployment runs"; pins updated, each old sentence red with its own message. (4) the true narrower claim (`mock/rand` has no consumer outside `src/mock/`) + a tree sweep for an import of it, falsified with a probe file. (5) scoped to `app/status/` + the incident tree with the demo-corpus reason. (6) **`components` prop measured** (`mdx.md:414-435`, the compiler's own `recma-jsx-rewrite.js:52`, a real build): `DocsPage` renders `<Body components={{ a: DocLink }} />` with the pure rule in `lib/docs/doc-href.ts` (`/docs`→base, `/docs/x#y`→`base/x#y`, anchors/external untouched), `MDX_ANCHOR_CLASS` one definition; **rendered proof: without the prop the in-app HTML carries `href="/docs/…"` (red), with it 0 hits and both links present as `/app/docs/…`, public mount unchanged**; new `doc-links.test.ts` (4) pins the rule, the 20 corpus links against the manifest (that mirror did NOT exist before — added), the `DocsPage` line, and both prerenders. (7) all 24 permutations (old assertion passes a null comparator, new fails it). (8) fixture keyed on `frontmatter.date`. (9) `git log -1 --format=%cd` per `merged:` sha, skips stated. (10) `cd deploy/cloudwatch-forwarder`. (11) the D282 absence as a `<blockquote>` + the corrected `# gRPC instead:` sentence. (12) "19 of the 80". (13) concatenation; the `${` ban still green. (14) one launch-state pin with the deliberate-update name; hollowness fixture renamed. (15) the ban narrowed to rendered text; the slos comment reworded. D273 deviation recorded as an MDX comment. F1 also noted F2's in-flight `infra-track.test.ts` cast broke `tsc` for ~20 min — F2 fixed it (`d8d5b41`).
- **R1 re-verify (HEAD `d081cd0`, fresh archive + `npm ci`) — verdict APPROVE WITH SHOULD-FIXES.** 63/63 pages, 487/434/0/53, eslint + tsc clean, mode-blind grep only assertions. **All 15 findings re-falsified fixed:** (1) fold removed at the shared site → red; planted "Live Tail" with `ALLOWED=[]` → red; (2) a fixture "99.4%" notice → red; (3–5) the sentences pinned verbatim; (6) in-app prerender 0 `href="/docs`, the same 13 links as `/app/docs/…`, public unchanged; `doc-links.test.ts` not hollow (dangling `/docs/nope` → red; hard-coded `/app/docs` → red ×2; `<Body/>` without the `a` → red); (7) a `? 0` tie-break → red over 24 permutations; (8) a moved frontmatter date → red ×2; (9) a swapped real sha → red naming the true date; (10–15) as specified; the D273 comment present. Every fix landed as ONE shared mechanism (`foldedMatches`, `SURFACE`, `DocLink`). **SHOULD-FIX (→ F3 with R2's):** (A) two new arms **silently SKIP on CI** — `doc-links.test.ts`'s prerender-link arm reads `.next/server/app` but `web.yml:121` runs `npm test` BEFORE `:171 npm run build`; `changelog.test.ts`'s merge-date arm needs full history but `checkout@v7.0.1` at `web.yml:31` is depth-1 → `fetch-depth: 0` + build-before-test (or fail loudly under `CI`); (B) `doc-links.test.ts` resolves pages but not anchors (`/docs/quickstart#no-such-heading` stays green). NOTE: the lockfile delta since `1b31cac` is +7/−278 (T5's `@vercel/blob` removal — justified; the integrator re-baselines).
- **R2 (landing pass + README: T5 ×4, F2 ×2, T6 ×2 @ `b6c26b6`; Opus, read-only, archive copy, both build modes) — verdict MUST-FIX (2 blockers); "114 of 116 rows land as ruled, all six fence checks falsify red, both builds green."** Verified: every ruled replacement present verbatim in the rendered page; competitor cut complete (`capabilityRows: string[]`, nine rows; the surviving `c.mark` is the connector chip glyph); chips 19/16; `:547` → `© 2026 obstack`; the video as `<figure>` + `<figcaption>` + `aria-label`; auth pages true. **Falsifications all red:** (a) both directions (page 50k→60k; seed 50000→60000), (b) planted phrase in an existing AND a new marketing file, (c) dropped `SAMPLE_COPY` import, (d) a coming-soon name in the breadth sentence, (e) a bad Link href / `appHref` / anchor, (f) one flipped PNG byte; `BANNED` D325-complete, `ALLOWED = []`; `TourGuide` fold guard reaches the sweep's fold (three variants red); waitlist grep only the absence assertions, lockfile −24 all under `@vercel/blob`, an additive `/#waitlist` → red. Screenshots: all four sha256 MATCH, 1440×900, `commit d8d5b41` an ancestor, `capturedAt` 3m51s after it; uncropped, DEMO bar visible, none of the banned pixels. Builds: mock AND live `next build` 63/63 (live needs the Dockerfile's `CLICKHOUSE_URL` placeholder; `/app/docs/[[...slug]]` `ƒ` under the dynamic layout); 487/434/0/53; eslint/tsc clean. **MUST-FIX 1 — `/` renders "infra · pods & k8s events":** `HeroTrace.tsx:60-61` renders the real `Waterfall` on `oomTrace`, which `mock/stories.ts:266` gives `k8sEvents`, so `infraTrackHeading` returns the long form on the marketing page (`grep -o "infra · pods[^<]*" index.html` → the banned phrase) while fence check (b) stays green — its sweep reads `page.tsx` + auth pages + `components/marketing/**`, not `lib/` or `components/trace/`. **MUST-FIX 2 — `connections.png` lists `loopwork-web (production) · Vercel · degraded · 86/min` under CONNECTED · 3** while Vercel is `coming-soon` 350px lower: `mock/connectors.ts:24-30` hardcodes a `connectorSlug: "vercel"` connected row — the third in-code claim (F2 found two). **SHOULD-FIX:** (3) the dashboard shot's setup checklist `○ Create an SLO ○ Route alerts to Slack` (mock-only targets; "Slack" absent from the catalog); (4)/(5) `app-href.ts:26` + `Dockerfile:48` say "two" CTAs (three); (6) README's standalone-mock repro curls a cold container (CI retries 30×). NOTES: README `:145` mixes job and run durations (15m47s is the run); `:150` "~21s locally" unverifiable; the quota banner's "degrades to sampling, never a hard cut" IS true (`metering.go:148`). → **F3 (fresh executor): R2 1–6 + R1 A/B — and check (b) widened to the RENDERED HTML so the next transitively rendered claim cannot hide.** **F3 DONE @ `b22718d` + `3888da5` + `6a2d8e4`: 495 / 442 / 0 (+8); mock AND live `next build` 63/63; eslint/tsc/actionlint clean; fence 8/8.** (R2-1) `HeroTrace` passes `{...oomTrace, k8sEvents: []}` (`mock/stories.ts` untouched); **check (b') reads the rendered `index/login/signup.html`** with one-pass entity decoding, the same `BANNED`, `ALLOWED=[]` — proven red against the pre-fix prerender (`index.html: k8s events`); with `.next` absent: `CI` → FAIL naming the build-order dependency, local → SKIP with the reason; own non-vacuity asserted. (R2-2) `mock/connectors.ts` third source → `docker` (`loopwork-web (docker, staging)`, degraded/86/12 kept); `mock/connectors.test.ts` (3) asserts every connected slug resolves to an `available` connector — red with the vercel row; **scope note:** `mock/inbox.ts:83` carried the same claim in the audit log — changed. (R2-3) checklist rows 4–5 → `Explain a trace` (`/app/traces`), `Search your logs` (`/app/logs`) — NOT "Issue an API key" (an unticked key row beside a ticked "Connect a source" would contradict itself; reasoned in the docblock); a new invariant arm: every row's href must be live-wired (falsified with `/app/slos`). (R2-4/5) "two" → "three" in `app-href.ts` + `Dockerfile`, backed by a mirror test counting the CTAs (3/3) and requiring both prose copies to match. (R2-6) README curl retry loop. (R1-A) `web.yml`: `fetch-depth: 0`; `npm run build` moved AHEAD of `npm test` (compose-up kept before tests); header updated; default build mode confirmed `mock` and it prerenders everything the arms read; both arms `assert.ok(!process.env.CI, why)` before skipping — proven under `CI=1` for missing `.next` and (planted bogus sha) for the shallow-clone path. (R1-B) `doc-links.test.ts` gains `slugifyHeading` + fence-aware `headingsOf`; all 5 anchored links resolve (red with `#no-such-heading`); a second arm re-derives every heading id in the prerender (46+) — **measurement changed the rule: github-slugger deletes the em dash and keeps `degrades--it-does-not-stop`, so hyphen runs are NOT collapsed**. **Re-shoot** at `3888da5`, one pass, each PNG >50 KB (172/153/233/182 KB — none an error page): connections CONNECTED · 3 = Kubernetes, OTLP, Docker, Vercel only under PaaS `SOON`; dashboard `○ Explain a trace ○ Search your logs`; trace `INFRA · PODS`; logs no tail claim; manifest `capturedAt 2026-08-30T18:11:17Z, commit 3888da5`; sha256 all match; check (f) green.
- **R2 re-verify (@ `6a2d8e4`, archive copy) — verdict APPROVE, no new findings.** Rendered `index.html`: `infra · pods`, 0 "k8s events"; **(b') falsified red by forcing the long form in `lib/infra-track.ts`** (a module in neither swept dir) and proven entity-decode-dependent; CI gating measured (no `.next`: local SKIP with the reason / `CI=1` FAIL; present + `CI=1`: 8/0/0). Vercel row closed at `mock/connectors.ts:34` (image read: CONNECTED · 3 = K8s, OTLP, Docker; Vercel `SOON`), guard red with the row restored; `mock/inbox.ts` clean. Checklist in pixels; live-wired invariant red with `/app/slos` (3 fails). "three" mirror red in either file. README loop present. `web.yml` `:46` `fetch-depth: 0`, build `:114` before test with the comment; actionlint clean; both arms fail under `CI` without their precondition. Anchors: red with `#no-such-heading` naming the page's real ids; the no-collapse hyphen rule confirmed as a measurement (`degrades--it-does-not-stop`). Manifest all MATCH, `commit 3888da5` an ancestor, captured 1m51s after. Builds mock + live 63/63. Counts 495/441/0/54 in the archive (no `.git` → the merge-date arm self-skips there with its stated reason; 442/53 with `.git`).

## Critical items
<!-- Phase plan "Verdict and gates" item 4 — these return to the advisor before merge. -->
- **The complete landing-fence enumeration** (`2026-08-30-s4.4-landing-fence.md`) with the 14 ‡ rulings applied and the fence test green with an **empty** allowlist (S1 L2)
- The docs-corpus honesty sweep (D246 repo-wide + the corpus-specific bans from `corpus-honesty.test.ts` re-targeted at `src/content/**`) + **the quickstart byte-check re-run against the docs copies** (K4)
- The `mock/docs.ts` deletion sweep (zero importers, the helm assertion re-homed, no Loopwork string left in `apps/web/src` outside `mock/` demo data)
- The changelog pipeline's D246-sweep continuity, pinned (K5)
- The `/status` replacement with the Loopwork deletion confirmed (zero `mulberry32` importers outside `mock/`, zero "Loopwork status" strings)
- CI first-runner numbers for `web` and `images` (D137 shape)

## Tasks

### T1: the docs mechanism — MDX pipeline, content tree, `/docs` + `/app/docs`, the D21 chrome class, the deletion of `mock/docs.ts`
- status: pending · wave: 1 · depends: — (K1–K4 rulings applied)
- owns: apps/web/package.json (+ the SIX deps, exact pins, `@next/mdx` lockstep 16.3.0 — D319), package-lock.json, apps/web/next.config.ts, apps/web/src/mdx-components.tsx (new), apps/web/src/content/docs/** (new — the TREE + `manifest.ts` (D320) + an `index.mdx` placeholder that T2 replaces; conventions README), apps/web/src/lib/docs/** (new — the loader: manifest-driven, NO runtime `fs` — D320; frontmatter, ordering, slug→import), apps/web/src/app/app/layout.tsx + apps/web/src/components/shell/DemoFooter.tsx (new, client, chrome-aware — D321) + apps/web/src/components/shell/shell-honesty.test.ts (`:91` retarget), apps/web/src/components/docs/** (DocsPage/DocsNav/Prose — replacing DocsLibrary.tsx, which is DELETED), apps/web/src/app/docs/[[...slug]]/page.tsx (new, public), apps/web/src/app/app/docs/[[...slug]]/page.tsx (in-app mount per K2; `app/app/docs/page.tsx` replaced), apps/web/src/lib/live-routes.ts (+ `productChromeRoutes`), apps/web/src/components/shell/{SampleDataBadge,CommandPalette,SideNav,TopBar}.tsx (chrome-class consumers + nav), apps/web/src/mock/docs.ts (DELETED), apps/web/src/mock/corpus-honesty.test.ts (the two docs assertions re-targeted at `src/content/**`), apps/web/src/lib/docs/docs.test.ts (new)
- goal: D319's mechanism exactly (scratch-build re-run on 16.3.0 first; lockstep bump only if string plugins fail there); a mode-blind renderer (no `resolveMode()`, no `@/mock/*` import — pinned by test); **manifest as the source of truth (D320)** — `generateStaticParams` returns it, the loader `notFound()`s off-manifest slugs on BOTH mounts, `docs.test.ts` walks the tree with `fs` at test time and `deepEqual`s it to the manifest; frontmatter via named export; the in-app mount per D320 sharing the same component and loader; `productChromeRoutes` + `isProductChromeRoute()` per D321 consumed by `SampleDataBadge`, `CommandPalette.hintFor` ("docs") AND `DemoFooter` (moved to `components/shell/DemoFooter.tsx`, client, null on chrome) so `/app/docs` carries neither the badge nor the footer in either mode; nav entries kept pointing at `/app/docs` (in-app) and the marketing nav gains `/docs` in T5; `mock/docs.ts` + `DocsLibrary.tsx` deleted with zero remaining importers; `corpus-honesty.test.ts:47/62` re-targeted at `src/content/**` (the helm-command assertion now reads the MDX corpus and still fails closed on an empty corpus); the loader unit-tested (ordering, slug resolution, 404 off-list).
- done-check: `npm run build` in `apps/web` green with every docs route `● (SSG)`; `npm test` root green incl. the re-targeted corpus tests; a test proving the renderer has no `@/mock/*` edge; `grep -rn "mock/docs\|DocsLibrary\|Loopwork" apps/web/src --include='*.ts*' | grep -v "src/mock/"` → empty; `next build` wall-clock recorded vs 35.9s; the standalone output contains the compiled docs (checked in the `images` job's mock image at review)
- result: DONE @ `5587154` (mechanism) + `3edc49b` (chrome class). **The 16.3.0 gate passed** — scratch build with `@next/mdx@16.3.0` + string-form plugins: 2.9s, both routes SSG, rehype-slug ids through props-spreading overrides, GFM table; no lockstep bump needed. Six exact pins (`@next/mdx@16.3.0`, `@mdx-js/loader@3.1.1`, `@mdx-js/react@3.1.1`, `remark-gfm@4.0.1`, `rehype-slug@6.0.0`, `@types/mdx@2.0.14`), lockfile +132/−0, `npm ls` both 16.3.0. Clean build **41.6s** (baseline 35.9s), 39 static pages (was 36); `/docs`, `/docs/quickstart`, `/app/docs`, `/app/docs/quickstart` all `● SSG` in the mock build (the in-app pair renders per request in a live build — the manifest + loader `notFound()` is why). `.next/standalone` carries the four `.html`/`.rsc` + chunks; **no `.mdx` source traced in** (corrects NX3's observation — the no-runtime-`fs` rule made visible). Root `npm test` **423 / 370 pass / 0 fail / 53 DSN skips** (+14: 10 `docs.test.ts`, 3 chrome registry, 1 consumer); eslint + typecheck clean; `mock/docs|DocsLibrary` grep empty; `fs|path` only in `docs.test.ts`; prerender-verified `/app/docs` carries neither the badge nor the footer, `/app` still does. **API for T2–T4** (`src/content/docs/README.md`): a page is `<slug>/index.mdx`; `export const frontmatter = { title, description? }` with `title` a plain string literal (the runner has no MDX loader — read as text); register in `manifest.ts` `{ slug: string[], section }` (nav order = array order); `@/lib/docs/docs` pure (`findDocsEntry`, `docsStaticParams`, `docsNavSections`, `hrefFor`, `contentPathFor`, `docsManifest`), `@/lib/docs/load` Next-compiled (`loadDoc(slug)` → `{entry, frontmatter, Body}`, `notFound()` off-manifest; `loadDocsNav()`); T3/T4 copy the two-module split. T2's marker `<!-- T2: helm command pending -->` in `content/docs/README.md` keeps the corpus helm-assertion from passing vacuously. **Two silent Next 16.3.0 behaviours measured and pinned:** a nested template in the dynamic import compiles then dies at prerender (`Cannot find module` — Turbopack globs the context module from the template's literal parts) → the path is precomputed by `contentPathFor`, one interpolation; **returning `{}` for the optional catch-all's index entry prerenders NOTHING for the whole route while printing `●` green** — caught only by the static page count not moving → `{ slug: undefined }`, documented, asserted by key presence. **Manager-ratified deviations:** `lib/live-routes.test.ts` added (pins the registry beside it); frontmatter read as text, not imported (no MDX loader under `tsx --conditions react-server`); `notFound()` in `load.ts` with the pure resolver returning `null`; docblocks avoid spelling the banned import literal (D246 discipline). `Loopwork` grep: T1's share empty; remaining hits are T4's status page and the demo workspace's NAME (`SettingsSuite.tsx:221`, `SideNav.tsx:102` — D208-allowed demo content, pinned by `shell-honesty.test.ts:73`).

### T2: the corpus — quickstart-first, from the true sources, with every owed sentence
- status: pending · wave: 2 · depends: T1 (the tree + renderer + the shared quickstart component per K4)
- owns: apps/web/src/content/docs/** (all pages), apps/web/src/components/onboarding/Quickstart.tsx (ONLY to extract the snippet strings into a shared server component if K4(a) — the rendered quickstart byte-identical), apps/web/src/components/docs/QuickstartSnippets.tsx (new, per K4), apps/web/src/components/onboarding/Quickstart.test.ts (grows the docs-copy arm), apps/web/src/lib/docs/mirror.test.ts (new — the docs mirror tests), packages/obstack-js/README.md + packages/obstack-py/README.md (only if a sentence is found false while sourcing — report each)
- goal: the page map — `/docs` (Start here; the positioning line from PRD §11 verbatim) · `quickstart/` (sign-up-and-get-a-key · send-your-first-trace = the three D78-verbatim tabs through the shared component · what-you-should-see) · `sdks/` (typescript · python · bring-your-own-otel) · `self-hosting/` (docker-compose · helm-chart) · `connectors/` (overview: 3 available / 16 coming soon · vercel-log-drains · aws-cloudwatch — both carrying the "coming soon until the hosted endpoint" truth and the D254 no-inferred-correlation sentence) · `explain` · `billing-and-plans` (the `plans` seed ONLY — free/pro, no Scale, no overage, no seats) · `retention` (D292/D294 both halves) · `what-obstack-does-not-do` (the 21 absences, each as ruled). Every page written FROM CS4's sources; every one of CS2's 29 sentences placed (a checklist in the task result mapping sentence → page); no sentence from CS3's mock billing copy or CS5's stale list. **Mirror tests** (D206/D266 shape, read sources as text): SDK peer ranges (`package.json` ↔ docs), the compose env var names (`.env.example` ↔ docs), the chart values keys named in docs (`values.yaml` ↔ docs), the plan numbers and Explain quotas (`0005`/`0006` ↔ docs), the connector counts (`connectors.ts` ↔ docs), the receivers' route paths (`/v1/integrations/{vercel,cloudwatch}`), the grace/retention constants (`retention.go`, chart/compose grace). Quickstart page: the byte-check re-runs over it (K4).
- done-check: root `npm test` green with the new mirror + byte-check arms; the D246 sweep green (`TourGuide.test.ts` scans `src/content/**` — asserted); `corpus-honesty` bans hold over `src/content/**`; a manual read-through by the reviewer of every page against its sources (the reviewer's checklist is the fence for docs); zero `${` or unresolved placeholder in rendered docs; the honest-absence page carries all 21 with ruling ids in comments
- result: DONE @ `f7189e6` (D322 snippet split) + `1b31cac` (the corpus). **14 pages** (`/docs`, `quickstart` [three H2 anchors on one page], `sdks/{typescript,python,bring-your-own-otel}`, `self-hosting/{docker-compose,helm-chart}`, `connectors/{overview,vercel-log-drains,aws-cloudwatch}`, `explain`, `retention`, `billing-and-plans`, `what-obstack-does-not-do`), each with its source files listed in the task report; **63 static pages** (39 at T1), all 28 docs routes `● SSG` on both mounts, 14 `.html` per mount on disk. **The 29 sentences placed** (TS page 1–10 + 29; Python 8, 29 + D328; retention 12–14; Vercel 15, 16, 23; CloudWatch 16, 23; compose 17, 18, 20, 22; helm 19–21; overview 16; #24 is the corpus itself; #11 → T6, #25 → T3, #26 → T1, #27 → T4, #28 → T5). **The 21 absences** on one page, each behind a non-rendering `{/* Dxxx */}` comment, grouped SDKs 7 · Connectors 4 · Product 6 · Operations 4. Root `npm test` **468 / 415 / 0** (+19: 3 `Quickstart.test.ts` arms, 17 `mirror.test.ts`); eslint/typecheck clean; the forbidden-string grep over `src/content` empty; the helm assertion on its strict branch, T1's marker removed. **Byte-identity proven two ways:** the 110 moved lines diff clean against `Quickstart.tsx` (whose only other change is the import), and the Python snippet is the same **599 bytes** in `.next/server/app/app/onboarding.html` and `.next/server/app/docs/quickstart.html`. SDK READMEs: no false sentence found, untouched. **Manager-ratified decisions:** `OtelEnvTable` as a second export of `QuickstartSnippets.tsx` (D322's ban on `OTEL_EXPORTER_OTLP_` lines in MDX vs the Python page's env table — resolved the D322 way: the table renders from the component and `mirror.test.ts` compares it to `obstack-py/README.md` row for row); the D282 absence sentence obtained by calling `snippetsFor` with a gRPC-only pair (the component's own text); the `${`/snippet-line bans scoped to `.mdx` pages (T4's `manifest.ts` legitimately carries `${` in a docblock); quickstart as one page with three anchors.

### T3: `/changelog` joins the pipeline
- status: pending · wave: 2 · depends: T1
- owns: apps/web/src/src/content/changelog/** (new — four files + README), apps/web/src/app/changelog/page.tsx (rewritten to list the content dir), apps/web/src/lib/docs/changelog.ts (new loader or a mode of the docs loader), apps/web/src/components/shell/TourGuide.test.ts (the sweep-coverage assertion + the allowlist to empty after T5 — coordinated with T5), apps/web/src/app/changelog/changelog.test.ts (new)
- goal: K5 — the four entries verbatim (NX8) in dated files with frontmatter; dates verified against `git log` (each entry names the commit range that shipped it in a comment; a wrong date is corrected and the correction recorded); the page renders newest-first from the build-time index; the `:5-16` docblock substance → `src/content/changelog/README.md`; a test that the D246 sweep's scanned set includes `apps/web/src/content/**` and that the four entries' bodies are byte-equal to the recorded originals (pinned once, so a later edit is deliberate).
- done-check: `/changelog` `● (SSG)`; tests green; `grep -c "Nothing tails" apps/web/src/content/changelog` = 1
- result: DONE @ `85fe051` (pipeline) + `14bedc8` (sweep). **Dates corrected to the true merge dates (D323):** Connections hub "Jul 28" → **2026-08-20** (`9c2621b`, PR #19); Logs explorer "Aug 8" → **2026-08-17** (`9fabcde`, PR #12); Traces carry their logs "Aug 6" → **2026-08-16** (`fd3ab6a`, PR #11); Explain 2026-08-20 unchanged (`f1bba82`, PR #20) — each found by the commit that made the claim true walked forward to its first master merge; recorded per file as an MDX comment (measured: MDX comments compile through T1's pipeline) with a test asserting comment date = entry date. **Byte pin:** the four bodies extracted mechanically from `git show 3edc49b:…/changelog/page.tsx` into `BODY_PINS` (612/190/167/227 bytes) and asserted equal to each `.mdx` body. Page renders newest-first (`○ Static` — no dynamic segment, same marker as before) with the four titles/kinds/bodies byte-exact; 39 static pages in a clean sandbox. **Hollowness guard reshaped:** a shouted heading built from `CLAIM` at run time is invisible to the raw predicate and visible to the folded one; proven green with today's one-entry `ALLOWED` in the real tree AND with `ALLOWED = []` + the fixture repaired in a `git archive` copy, where the old `notDeepEqual` variant fails — `ALLOWED` itself untouched for T5. **Sweep continuity pinned:** `scanned` must include a path under `src/content/changelog/` and one under `src/content/docs/`. Suite on HEAD+T3: 433/380/0 (+10, exactly `changelog.test.ts`); eslint/typecheck clean. **Manager-ratified decisions:** the page docblock and the test de-spell the banned literals (D246 discipline); the manifest is deliberately oldest-first so a broken comparator cannot pass by accident; total order = date desc, slug desc tie-break (two entries on 2026-08-20 — Explain above Connections hub, the real merge order), reversal-tested; `formatEntryDate` hand-rolled (a `YYYY-MM-DD` has no zone); the test lives at `lib/docs/changelog.test.ts`; the stale `ALLOWED` docblock sentence refreshed.

### T4: `/status` — obstack's own page, the Loopwork fiction deleted
- status: pending · wave: 2 · depends: T1 (frontmatter/loader conventions)
- owns: apps/web/src/app/status/page.tsx (rewritten), apps/web/src/src/content/status/incidents/** (new — README + `.gitkeep`-class empty dir with the frontmatter contract documented), apps/web/src/lib/docs/incidents.ts (new loader), apps/web/src/app/app/slos/page.tsx (`:19` link per K6), apps/web/src/components/shell/CommandPalette.tsx (`:33` per K6), apps/web/src/app/status/status.test.ts (new)
- goal: K6's shape verbatim: components (app, ingest, docs) described, not scored; the monitoring slot's one sentence ("External uptime monitoring begins at launch; this page shows no uptime numbers until then" — D256 wording); incidents from curated files, "No incidents recorded" when empty; mode-blind; no `mulberry32`, no percentages, no strips, no "Loopwork". Test: renders the empty state; a fixture incident file renders in order; the page source contains no digit followed by `%`.
- done-check: `grep -rn "mock/rand" apps/web/src/app` → empty; `grep -rn "Loopwork status\|status.loopwork\|powered by obstack SLOs" apps/web/src` → empty; `grep -rn '"/status"' apps/web/src/app/app/slos` → empty (D324); `● (SSG)`; tests green
- result: DONE @ `f2903ab` (8 files, +924/−114). Renders (from `.next/server/app/status.html`): the three components described not scored (`app` — the web application, the demo today and your workspace once it exists; `ingest` — the OTLP endpoint your telemetry is sent to; `docs` — this site's documentation), the D256 monitoring sentence verbatim, **"No incidents recorded."**; site chrome kept; no pill/dot/percentage. Incidents contract in `content/status/incidents/README.md` + an EMPTY `manifest.ts` (no `.mdx` ships): `YYYY-MM-DD-slug.mdx`, `frontmatter = {date, title, status, components}`, ids from `STATUS_COMPONENT_IDS`, "a curated notice written by a person — nothing generates one"; two-module split (`incidents.ts` pure + `incidents-load.ts`). SLO page `:18-23` link removed with a comment. Proofs: `mock/rand` in `app/` empty; the Loopwork/SLO/uptime needles empty repo-wide; `"/status"` in slos empty, in the palette exactly 1; no `/\d%/` in the status files; `/status` prerendered (`○ Static` — `●` marks only `generateStaticParams` routes; same meaning); 40 static pages; eslint/tsc clean; +15 tests (7 + 8) all green — the 3 root failures at the time were T2's in-flight uncommitted work. **Measured Turbopack fact, recorded:** a computed `import()` specifier over a directory with NO `.mdx` fails the build (`Can't resolve '@/content/status/incidents/' <dynamic> '.mdx'`) — the incident tree is empty exactly when things are fine, so each manifest entry carries its own `import()` literal and an empty array resolves nothing (a third mirror direction: an entry's import names its own file, with a D323-style hollowness proof). `INC-42` survives only in the D125/D208 demo corpus (`mock/incident.ts` etc.), empty under `app/status/` — the satisfiable form.

### T5: the landing launch pass — the fence applied, the labels, the nav, the waitlist out, the fence test, the screenshots
- status: pending · wave: 3 · depends: T1, T3, T4 (nav links target routes that exist — D257 sequencing)
- owns: apps/web/src/app/page.tsx, apps/web/src/components/marketing/** (HeroTrace/ScreensShowcase/labels; WaitlistForm.tsx + actions.ts DELETED), apps/web/src/server/waitlist.ts (DELETED), apps/web/src/lib/waitlist.ts + waitlist.test.ts (DELETED), apps/web/package.json (`@vercel/blob` removed) + package-lock.json, apps/web/public/shots/*.png (re-shot), apps/web/src/app/signup/page.tsx (`:57,:60`), apps/web/src/app/login/page.tsx (`:52,:55`), apps/web/src/app/signup/mock-mode.test.ts (`:94-98,116` reconciled), apps/web/src/lib/app-href.ts (new, `server-only`, D329) + its test, apps/web/Dockerfile (`:42-43` — `OBSTACK_APP_ORIGIN` ARG/ENV beside the mode, D329), apps/web/src/components/marketing/SampleLabel.tsx + the `SAMPLE_COPY` module (D326), apps/web/src/app/landing-fence.test.ts (new), apps/web/src/components/shell/TourGuide.test.ts (allowlist → empty + the D323 hollowness-guard reshape, with T3)
- goal: **D325 verbatim** — the 16 classes with the exact texts ruled, the competitor columns CUT (the nine rows become obstack's capability list; `apm`/`llm`/`Mark`/legend removed; the assembled-vs-joined block stays with its two edits), L35 cut to the ruled sentence, L79 dropped, L71/L77 as ruled (the "dated legend" option is VOID); nav gains Docs (`/docs`) + Status (`/status`) in `:55-59` and the footer row; `:547` → `© 2026 obstack` after the per-surface sample labels land on the hero trace and the screens showcase (K9); the waitlist removed per K10 with the auth-page links and D156's test reconciled; the pricing block stays copy with the **mirror test** against the seed (D257); `appHref()` per K13 (default same-host) on every cross-host CTA; the **fence test** (LF4: pricing mirror + no Scale numbers; banned phrases with an EMPTY allowlist; the sample-label invariant; connector-breadth mirror; the link set; the image manifest by content hash); the four screenshots re-shot from the current mock build per K8 (viewport/dimensions recorded; the manifest pinned).
- done-check: `landing-fence.test.ts` green; `TourGuide.test.ts` green with `ALLOWED = []`; root `npm test` green; `grep -rn "BLOB_READ_WRITE_TOKEN\|@vercel/blob\|waitlist" apps/web package.json package-lock.json` → empty; every `href` on `/`, `/login`, `/signup` resolves (the test); the reviewer reads the rendered landing against the fence row by row
- result: DONE @ `3e37dde` + `8a7ffc6` (the R1 fold-guard fix) + `4c7e582` + `b90ddb1`; **481 / 428 / 0**; eslint + tsc clean; 63 static pages. **The 40 FALSE rows:** 31 replaced per D325 verbatim (before→after table in the task report), the other 9 were T4's `/status` deletion. **‡ rulings applied:** L24-27/L49 kept; L35 cut to the ruled sentence; competitor columns CUT (`apm`/`llm`/`Mark`/legend gone — `comparisonRows` → `capabilityRows`, nine rows as obstack's list); the assembled-vs-joined block's two edits; L79 dropped; L71/L77 as ruled; L44 uncropped; `:547` → `© 2026 obstack` after the labels. **Labels (D326):** `SAMPLE_COPY` in `marketing/sample-copy.ts`, consumed by `DemoFooter` (rendering from the constant, text unchanged), the `HeroTrace` caption (moved INTO `HeroTrace.tsx` so the widget and its label cannot be composed apart), the video caption, the showcase. **Waitlist (D327):** five files deleted, both render sites gone, `@vercel/blob` uninstalled (lockfile delta = that tree only), auth pages → "Open the demo" + "Read the quickstart" (`/docs/quickstart`), `mock-mode.test.ts` reconciled; the token survives only in the untracked gitignored `.env.local` (the user's). **Cross-host (D329):** `lib/app-href.ts` (`server-only`), Dockerfile ARG/ENV, measured in the prerender (unset → `/signup`; set → absolute) — on THREE CTAs (the hero has a third "Create your workspace"; manager-ratified). **The fence test** (`app/landing-fence.test.ts`, 7 tests): pricing parsed to numbers vs the seed + no digit in Scale + no Scale row in the seed; 11 banned needles built from parts over the landing/auth/marketing files with **`ALLOWED = []`**; the sample-label invariant (three files tripped, pinned); connectors 19/3/16 with the breadth sentence naming exactly the three available; every `href` (incl. `appHref`) resolving against discovered routes/anchors, resolver falsified; PNG sha256 + IHDR size + capturedAt + commit both directions — each check proven red individually. **`TourGuide.test.ts`:** `ALLOWED = []`; R1's must-fix 1 applied (`foldedMatches` shared by the sweep and the proof; two-state proven). **Re-shoot (D326):** local mock build + Chrome headless via the drive's binary list, raw CDP at 1440×900 DPR 1, sidebar uncropped, DEMO WORKSPACE bar visible; manifest `capturedAt 2026-08-30T17:27:27Z, commit 4c7e582`; verified: no `ziad@loopwork.ai`, no tail claim, Vercel/CloudWatch SOON. **Two banned claims survive in pixels because they live in CODE outside T5's owns:** `app/app/page.tsx:153` renders "ingesting · loopwork-prod · last 6h" on the mock overview (the literal `shell-honesty.test.ts:32` bans in TopBar — the fence attributed it to the wrong component); `components/trace/Waterfall.tsx:192` heads the infra track "pods & k8s events" unconditionally (no producer sets events — L47 was never a screenshot problem). **→ F2 (owns amendment to T5): fix both at source, re-shoot the two PNGs, regen the manifest, fix the stale `TourGuide.test.ts` title/docblock.** `shell-honesty.test.ts`'s DemoFooter pin now asserts the two halves + the constant and reassembles the sentence (ratified). **F2 DONE @ `d8d5b41` (source + guards) + `b6c26b6` (re-shoot + manifest); 487 / 434 / 0 (+5).** `app/app/page.tsx:153` mock half → "sample data · loopwork-prod · last 6h" via the EXISTING `live ?` seam (the live half untouched — there the row IS ingested telemetry); a `shell-honesty.test.ts` guard reads the mode ternary (both halves non-empty, the mock half must not say "ingesting"). `Waterfall.tsx:192` → `{infraTrackHeading(podTracks)}` with the decision in pure `lib/infra-track.ts` (client-safe; `Waterfall` cannot load under `react-server`), 4 tests incl. a corpus-correspondence test (`pipelineTrace.k8sEvents` empty, `oomTrace` not — neither branch dead; a story growing events flags the screenshot). Red-then-green both guards. All four PNGs re-captured in ONE pass from the rebuilt mock build at `d8d5b41` (`capturedAt 2026-08-30T17:40:50Z`; connections + logs byte-identical to the prior capture — deterministic where the corpus is; dashboard + trace differ per run on relative times); visual: "sample data · loopwark-prod"→ `● sample data · loopwork-prod · last 6h`, `INFRA · PODS`; the OOM story still renders `INFRA · PODS & K8S EVENTS` (a decision, not a deletion). `TourGuide.test.ts` title/docblock now state the empty-allowlist truth. **Process note recorded:** a concurrent `next build` deleted `.next` mid-serve and silently produced two 28 KB error-page "screenshots" — caught on file size, re-shot behind a build-lock wait; any task serving the app while others build needs the same guard.

### T6: the root README operator pass (K11) + the records
- status: pending · wave: 3 · depends: T2 (the docs are the reference the README now points at)
- owns: README.md (the stale sentences in CS5 + the `images`/`lint` bullets + a pointer to `/docs`), this file (results), .planning/2026-08-30-s4.4-exit-evidence.md (new), .ziarmy/retro.md (local)
- goal: CS5 fixed; `images`/`lint` described in the register of the other bullets; the CI numbers carry the D318 rider; nothing else re-flowed. Exit bundle in the S4.3 shape.
- done-check: `git grep -n "Phase 0" README.md` → none; the check-list bullets are ten; `npm test` unaffected
- result: README half DONE @ `6f8c410` (+10/−5, one file). `:5` phase sentence → what the repo holds now (web app incl. the docs corpus path, the Go ingest, the two SDKs, compose + chart, the demo/sample apps) — no phase numbers; `lint` + `images` bullets added in sibling register with their first-runner numbers (47s; 4m37s/4m17s), bullet count 10; `sdk-e2e` "returns with the first runner" → the measured 4m40s (+ 4m29s next run); `stack` gained the D318 rider (master-push watch; 9m37s lockfile PR / 7m23s warm / 15m47s cold seed not a datum); a `/docs` pointer in "Run it" (both modes, written from `src/content/docs/`, quickstart first). **Extra false sentences found and fixed:** `sdk-js` "68 tests" → 70 (measured) and its "returns with the PR" → 1m06s; `:219` the plan pointer → the M4 phase plan. Verified-and-left-true: the nine-check sentence, the three SDK ranges vs `package.json`, the tgz version, the Next floor doc path, the mock trace id, `ok_dev_local`, every repro script path. No test reads the root README (16 hits are package/content READMEs). **Follow-up ratified and dispatched:** `lint`/`images` entries for the "Reproducing each check locally" block.

- **Integrator (rebuilt tree, fresh `npm ci`, foreign 5432 untouched — compose Postgres rebound to 5442 via a scratch override + PATH shim, no repo file touched) — verdict FIXED-GO @ `b7310ef`.** Static clean (actionlint 0, helm 0 failed, compose valid, typecheck/eslint clean, `services/` untouched → go skipped); pins lockstep, `@vercel/blob` empty; build 63/63 with 28 docs routes `● SSG`; tests **495/448/0/47** without DSNs, **495/495/0/0 with both DSNs** (the K4 trap: 53 = 47 PG + 6 CH, explained); the two formerly-skipping arms ran as `ok`; fence 8/8; images: both refusals verbatim (mode-mismatch naming both values; the openssl instruction), standalone mock 6/6 curls 200 with 0 banned hits on served pages, bundle 4/4 healthy, **mock vs live `/docs` byte-identical modulo one build id per page**; guard PASS (node 24), smoke PASS, drive 166/0. **The docs walk (the exit line, literally):** signup → workspace `ws_01a4cde2…` → Issue a key → the docs' endpoint equals the in-app one → `npm pack` exactly as documented (incl. the git-error-128 aside being true to the error code) → install → `init()` before `require("openai")` on 7.8.0 → the three `OTEL_*` exports as written → **a four-layer trace (API 33ms · AGENT 30ms · TOOL 0ms · LLM 29ms, 45 tokens, $0.000014 computed at ingest, in the signup's workspace)**; the self-hosting refusal paragraph reproduced exactly (`Restarting (1)`). **Slips:** FIXED `b7310ef` — the root was the only documented `npm pack` location without `obstack-js-*.tgz` in a `.gitignore`. **Recorded, not fixed (→ the advisor at signature):** (i) the TS tab's two lines cannot both run in one directory (`./packages/obstack-js` is repo-root-relative, the tgz CWD-relative; `README.md:78` chains them with `&&` — run as printed at the root it would install the SDK into the monorepo, dry-run proven) — D322/D78-ruled byte-pinned copy, an advisor call; (ii) the compose page says the refusal "prints the exact openssl command above" while it prints the bare `openssl rand -base64 32` naming the in-container variable (the mapping stated a paragraph earlier). **D13 sweep: no banned phrase on any product surface, source or rendered** (hits classified: absence assertions, a docblock record, the honest "rather than a whole major assumed", the historical fundraising PRD). Note for the local-repro block: the e2e drive rewrites `plans.event_quota` and it persists until `down -v` (CI unaffected — separate runners). Node 24 re-run: all green.

## Exit evidence (to be assembled at close)
- The corpus page list with, per page, its sources and the CS2 sentences it carries; the honest-absence page
- The byte-check + mirror tests green over the docs copies; the D246 sweep scanning `src/content/**` with an empty allowlist
- The fence: 116 rows → 0 FALSE, the 14 rulings applied, the fence test green; the re-shot screenshots' manifest
- The two deletions confirmed (`mock/docs.ts`, the Loopwork status) with zero residue
- `web`/`images` first-runner numbers; the `stack` second-master-push datum if it exists by then (D318)

## Run log
- 2026-08-30 — **PR #24 first runner: 9/10 green — `e2e` FAILED at 2m23s in the drift guard** (run 33329451643, node v24.19.0): `capturing into scratch database …` then ~130ms later `57P01 terminating connection due to administrator command`, a `pg` client dump, exit 1, no "identical" line. **The latent `BoundPool` race carried since S4.3 (CI5's "local-only, node-22" rider) is DISPROVEN — it is a race and PR #24's runner lost it** (PR #23 and this sprint's integrator both won it on node 24). Green elsewhere: `web` 4m25s with the new build-before-test order, `images` 4m33s, `sdk-e2e` 4m50s, `sdk-js` 1m10s, `stack` 9m31s (lockfile PR — the D318-characterized cache miss, expected), `go`/`kind`/`lint`/`sdk-py` nominal. **F4 DONE @ `ce2e61e`** (1 file, +73/−5, pushed — checks re-running). **Root cause, measured:** `Pool.end()` resolves before its sockets close (`pg-pool/index.js:133-186`), the FORCE drop's 57P01 lands on a removed-but-listener-bound client whose `idleListener` re-emits on the **Pool**, which had no `'error'` listener — and the crashing pool was NOT `getPool()`'s: **`authConfig().database !== getPool()`** — under `--conditions react-server` the config's module graph holds a SECOND `@/server/postgres` instance with its own singleton (better-auth hands it to kysely). **Fix:** a no-op error listener on the admin client and BOTH pools (`new Set([getPool(), config.database])`) before any client exists; teardown ordered `end()` → `pg_terminate_backend(...)` awaited → plain `DROP IF EXISTS` (FORCE removed — nothing left to kill); docblock argues both axes. **Proof:** the unmodified script crashes on node 22 verbatim (`Emitted 'error' event on BoundPool … idleListener (pg-pool/index.js:62)`); the forced scenario crashes on node 24 too (2/2 — a race, not a version); fixed script survives the forced scenario on both; **5/5 consecutive real runs on node 22 AND 5/5 on node 24**, zero leftover scratch DBs. No unit test possible (needs a live FORCE-terminated backend; the script self-executes on import) — the 10-run loop + forced red→green is the bundle's proof. Note: the script is NOT in `tsc -p apps/web`'s file set (type-checked via a temp tsconfig).
- 2026-08-30 — breakdown with measurements taken first (NX1–NX12, CS1–CS6, LF1–LF5 + the fence record; three read-only Opus passes); kickoff packet K1–K14 to the advisor.
- 2026-08-30 — advisor kickoff ruled **D319–D330**: mechanism ratified with the tree under `src/content/**` and a lockstep `@next/mdx` pin; manifest-driven, no runtime `fs` (D320); the D21 chrome class incl. `DemoFooter` (D321); the fence's 16 classes are THE copy, competitor columns CUT (D325); re-shoot IN with one `SAMPLE_COPY` definition (D326); `OBSTACK_APP_ORIGIN` for the two CTAs (D329). **CLEARED WITH CONDITIONS**; eight imprecisions corrected (one of the advisor's own). T1 dispatched.
