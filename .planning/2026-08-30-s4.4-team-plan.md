# Team Plan — obstack M4 sprint 4 — docs, status, changelog, landing: the launch content pass (exit: a quickstart-first docs site builds from the same content source the in-app docs render, /status and /changelog tell the truth by construction, and the landing page carries launch copy with no fiction left — "read the docs, follow them, and everything they say is true")

meta:
- date: 2026-08-30
- repo/branch: `s4.4-launch-content` @ `893db97` (master after PR #23 merged S4.3)
- mode: **manager-directs** (as S4.3: Fable 5 plans/reviews/rules; Opus@high executors one per disjoint file set, parallel where disjoint; batched Opus@xhigh reviewers; advisor + integrator gates)
- team-cap: executors per disjoint file set; stage explicit paths only; 2 batched reviewers; 1 advisor; 1 integrator
- models: executor=opus@high reviewer=opus@xhigh advisor=fable@xhigh integrator=opus@xhigh
- iteration-limit: 3 · iteration: 0/3
- source of truth: `.planning/2026-08-23-s4-m4-phase-plan.md` §3 S4.C3 (run-goal + exit criterion, verbatim) + §9 rulings **D255, D256, D257** (BINDING), D259 (honest absences), D262 (the marketing host IS the mock-mode web image), D261 (no docs CI job — the web build compiles the content), "Verdict and gates" critical item 4; the S4.3 bundle §7 handoffs (D309/D315/D310/D302/D308/D301/D316); the S4.2 bundle §7 (D292/D294/D295, receivers "coming soon"); the S4.1 operator-doc handoffs; **the fence: `.planning/2026-08-30-s4.4-landing-fence.md`** (D257's critical item, 116 rows)
- scope fence (phase plan): no product-feature work, no deployment, no monitor account creation (S5/U12). D13/D21 bind every new surface. AGENTS.md: this Next is not the one in training data — the vendored docs at `node_modules/next/dist/docs/` decide every Next question.
- standing guards inherited: D137/D207 (`e2e` the PR critical path, line HELD, band 5m28–6m02); `stack` on the re-based master-push watch (D318: first master run is the cold seed, the second is the datum); K3 forever-names (no new job — the docs build rides `web`/`images`); `web` band 4m20–4m30 with a 15m cap; the D246 repo-wide sweep (`TourGuide.test.ts`); the S3.4 quickstart byte-check (`Quickstart.test.ts`); D178 (no `@polar-sh/sdk` bump planned); the `sdk-js` `package.test.ts` README↔`package.json` mirror; S2.2 L1 (no activation claims — Vercel/CloudWatch stay "coming soon")

## Measured facts (S2.4 L1 — measured at breakdown, never believed)

Three read-only Opus passes, 2026-08-30, master @ `893db97`. Every claim carries a file:line; **(ruling)** marks a decision the measurement surfaced.

### NX — the mechanism, on the vendored Next 16.3.0 docs (`node_modules/next/dist/docs/01-app/02-guides/mdx.md`, `mdx-components.md`, `output.md`, `cacheComponents.md`)
- **NX1 — version + packages.** `apps/web/package.json:20` `next 16.3.0`, react 19.2.8; `next` hoists to the repo root `node_modules`. **No MDX package in the lockfile** (zero hits for `@next/mdx|@mdx-js|remark|rehype|gray-matter`). `@next/mdx` latest is **16.3.3**, installs beside 16.3.0; the guide's install set is `@next/mdx @mdx-js/loader @mdx-js/react @types/mdx` (`mdx.md:36-40`). CI node 24 (`web.yml:75`); images `node:24.19.0-slim` (`Dockerfile:18,55`).
- **NX2 — the ruled shape is first-party.** `mdx.md:275-325` "Using dynamic imports": `app/blog/[slug]/page.tsx` doing `await import(\`@/content/${slug}.mdx\`)` with `generateStaticParams` and `dynamicParams = false` (404 off-list). `mdx-components.tsx` is **required** for App Router (`:109`) and **its `useMDXComponents` takes no arguments** (`mdx-components.md:36` — differs from pre-16 memory). `pageExtensions` gains `md`/`mdx` (`:61`). Frontmatter: **not supported by default** (`:622`) but `export const metadata = {...}` / any named export works (`:628-643`) — no extra package if the export form is used. Plugins: ESM-only config (`:701`), **Turbopack requires plugin names as strings with serializable options only** (`:728,:760`). The Rust MDX compiler is experimental, "not recommended for production" (`:793`) — not used. `cacheComponents` is off in this repo (`next.config.ts` sets only `turbopack.root` + `output`) and is not needed for SSG.
- **NX3 — proven in a scratch build** (`next@16.3.0` + `@next/mdx@16.3.3`, `output:'standalone'`, `content/docs/**.mdx`, `app/docs/[...slug]/page.tsx` with an `fs` walk in `generateStaticParams` + dynamic import, string-form `remark-gfm`/`rehype-slug`): **`next build` 8.99s, both routes `● (SSG)`**, nested slugs work, `export const frontmatter` rendered as a named export, GFM tables and heading ids present. **Gotcha:** an `h1` override in `mdx-components.tsx` that does not spread `...props` silently drops rehype-slug's `id` (measured) — every override spreads props. **Standalone:** the compiled MDX is bundled into `.next/server/**` AND the source `content/**.mdx` was traced into `.next/standalone` with no `outputFileTracingIncludes`; `Dockerfile:61` copies the tree whole → both images carry it. Any route reading content with `fs` at *request* time is outside what was measured — every doc page is SSG, so none does.
- **NX4 — build constraints.** `web.yml:171` is a bare `npm run build` (D261: no docs job — a content failure fails `web` and `images`). Mode stamp (D251/D267): `OBSTACK_DATA_MODE` is a build-time input (`Dockerfile:36-53`), `images.yml:51-52` builds `obstack-web:live` and `:mock` from one source → **the docs/status/changelog renderer must be mode-blind** (no `resolveMode()` branch, no `@/mock/*` edge) so both images serve byte-identical content; `Quickstart.test.ts:217-222`'s "no edge to `@/mock/*`" test is the pattern to pin it.
- **NX5 — the D21 gap: no "product chrome" class.** `apps/web/src/lib/live-routes.ts:11-24` is the whole definition; `:33` `isLiveWiredRoute`. Consumers: `app/app/layout.tsx:147` (`live && <SampleDataBadge/>`), `SampleDataBadge.tsx:16` (null on a wired route), `CommandPalette.tsx:72` (`isLiveWiredRoute ? item.hint : "sample data"`). **`/app/docs` is not listed, so in live mode the real docs would carry "SAMPLE DATA — preview"**; adding it to `liveWiredRoutes` would be semantically false (it reads no workspace data). **(ruling — K3.)**
- **NX6 — two mount points, one auth wall.** `/app/*` is wrapped by `app/app/layout.tsx`, which at `:131` does `if (live && !(session && user)) redirect("/login")`. A public `/docs` is unauthenticated; an in-app `/app/docs` is auth-gated in live mode. One renderer, two mounts, one boundary between them. **(ruling — K2.)** Nav sites that hardcode `/app/docs`: `SideNav.tsx:56`, `TopBar.tsx:138`, `CommandPalette.tsx:31` (and `:33` for `/status`).
- **NX7 — the current in-app docs.** `apps/web/src/mock/docs.ts` (342 lines; `DocBlock`/`DocSection`/`DocArticle` types `:3-24`; 10 fictional Loopwork articles; 7 "Loopwork" occurrences), sole importer `components/docs/DocsLibrary.tsx:6` (client, 116 lines), route `app/app/docs/page.tsx` (19 lines, `:13-15` "Loopwork's authored infrastructure documentation…"). No search/index. **Tests that pin it:** `mock/corpus-honesty.test.ts:47` (bans fabricated hosts/credentials in the docs corpus) and `:62-67` ("every helm command in the runbooks installs a chart that exists in-repo" — `assert.ok(charts.length >= 1)` **fails closed on an empty corpus**). Deleting `docs.ts` breaks both — the helm assertion's real target (`helm install obstack deploy/helm/obstack --timeout 900s --wait`) moves to the MDX corpus, which S4.4 documents anyway.
- **NX8 — `/changelog` + the D246 sweep.** `app/changelog/page.tsx` (109 lines): the four entries at `:17-45` (verbatim in CS below); the `:5-16` D229/D246 docblock is what keeps the file honest in prose. **The sweep is `components/shell/TourGuide.test.ts`** — a repo-wide filesystem walk (`:20` repo root, `:56` skips `.git/.next/node_modules/.planning`, `:58-68` walk), asserting the case-insensitive hit set for "live tail"/"live-tail" (needles built at `:39-40`, never spelled) **deep-equals `ALLOWED = ["apps/web/src/components/marketing/ScreensShowcase.tsx"]`** (`:49`); `:75` `scanned.length > 200`; `:95-100` the case-sensitive set must differ (the fold stays exercised). → content under `apps/web/content/**` is swept automatically; the live risks are a content dir under a skipped path, and the `:96` hollowness guard if the one capital-L fixture goes.
- **NX9 — `/status`.** `app/status/page.tsx` (136 lines) imports `mulberry32` from `@/mock/rand` (`:3` — its only consumer outside `src/mock/`; `rand.ts` stays for `pick/between/hexId`). The fiction verbatim: `:5` "Public status page — SLO-fed, incident history included. Demo of Loopwork's page."; `:7-12` four components with `uptime90d` 99.97/99.89/99.99/100%; `:14-38` `UptimeStrip` seeded PRNG (`rng() < 0.02 ? "degraded" : "ok"` over 90 days); `:51-52` "Loopwork status · status.loopwork.ai · powered by obstack SLOs"; `:72-75` "All systems operational" / "recovered from a partial degradation earlier today (13:04–13:26 UTC)"; `:114-126` the INC-42 narrative "published from INC-42 · impact and timeline sourced from obstack automatically"; `:130-132` "demo page — this is what your customers would see, generated from your SLOs and incidents". Inbound: `app/app/slos/page.tsx:19`, `CommandPalette.tsx:33`, `mock/docs.ts:339` (dies with `docs.ts`). No test references it. `mock/incident.ts`'s types serve `/app/incidents` (a separate mock surface) — the curated incident files must NOT reuse them (D125/D208 seam).
- **NX10 — landing structure.** `app/page.tsx` (555 lines): footer `:547`; nav `:55-59` (Product/Connections/Pricing/Changelog — no Docs, no Status) + footer link row `:543-545`; pricing `:397-448` with the D226 comment `:394-404`, Free `$0 · 50k events/mo · 7-day retention · 20 Explain runs/mo`, Pro `$49 /mo + usage · 1M events included · 30-day retention · 200 Explain runs/mo`, Scale/Self-hosted `Planned` with no numbers — **all six numbers match the seed today** (`0005_metering.sql:128-129`, `0006_explain_metering.sql:20`); waitlist at `:124-133` and `:521` via `components/marketing/WaitlistForm.tsx` → `actions.ts` → `server/waitlist.ts:6-19` (Vercel Blob, `BLOB_READ_WRITE_TOKEN` appears **only** at `server/waitlist.ts:7`; `@vercel/blob@2.8.0` at `package.json:14` becomes unused) — inbound `signup/page.tsx:60`, `login/page.tsx:55` (`/#waitlist`) and **`signup/mock-mode.test.ts:94-98,116` asserts `/#waitlist` twice** (D156's shim-of-record); comparison table `:10-24` nine rows. The mirror pattern to copy: `server/ingest-endpoint.test.ts:20` (D266) — read sources as text, `deepEqual` against the single source; the SQL seed is parsed as text the way `Quickstart.test.ts:30` reads `package.json`.
- **NX11 — the quickstart byte-check** (`components/onboarding/Quickstart.test.ts`, 362 lines): reads `Quickstart.tsx` as text (`:27`) + both SDK READMEs + `obstack-js/package.json`; `tabCode()` (`:55-84`) reconstructs each tab resolving `${http}`, `${grpc}`, `${grpcLine}`, `${OBSTACK_JS_VERSION}`, `${key}` and asserts no `${` survives; verbatim comparisons: `pip install './packages/obstack-py[fastapi]'` (`:97-99`), `npm pack ./packages/obstack-js  # -> obstack-js-${version}.tgz` + `npm install ./obstack-js-${version}.tgz` (`:101-105`), `export OTEL_SERVICE_NAME=my-agent`, `export OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer%20` (URL-encoding load-bearing), `export OTEL_EXPORTER_OTLP_ENDPOINT=${OTLP_HTTP_ENDPOINT}` (`:134-148`); zero-arg `init()` + four banned forms (`:121-129`); banned literals `x-obstack-key`, `ingest.obstack.dev`, `obstack.dev`, `ok_live_9f2e` (`:173`); **endpoints interpolated, never spelled** (`:181-182`); the version fences read from `peerDependencies` (`:245-286`, D311). **Hard constraint:** an `.mdx` quickstart cannot spell the endpoint value or hardcode the SDK version — it imports the constants (MDX supports imports, `mdx.md:152`) or the test grows a parallel arm. **(ruling — K4.)**
- **NX12 — budget.** Local `next build` in `apps/web` **35.9s**, 36 static pages; the MDX probe built 2 pages in 9s (a 20–40-page corpus adds single-digit seconds). `web` last three: 4m26s · 4m30s · 4m20s (cap 15m). `images` builds two web images (grows by the MDX compile ×2).

### CS — the corpus sources and the sentences owed (inventory; the page map is in T2)
- **CS1 — there is no `/docs` route and no docs content on master** (`apps/web/src/app/` = `api, app, changelog, invite, login, signup, status`). D255 is literal: the corpus is written from zero; **the PRD requirement is "Quickstart-first docs site"** (`execution-prd.md:146-153` §11 GTM; `:171` Phase 4 deliverable "Self-hosted bundle, docs site, public demo environment"; the build-vision `:62` "docs/status/changelog content builds").
- **CS2 — the 29 handoff sentences owed to S4.4, with ruling ids** (the executor writes them as ruled, never paraphrased into something stronger): (1) D309 ai@7 telemetry on by default, opt-out `telemetry:{isEnabled:false}`; ai@5/6 `experimental_telemetry:{isEnabled:true}` per call (`obstack-js/README.md:203-232`); (2) D315 per-call `telemetry:{integrations:[…]}` replaces the global array — obstack draws nothing for that call (`:234-246`); (3) D310 the `ai` floor is **7.0.0 measured** — never "the whole major" (`:253-262`); (4) D303/D317 ai@7 needs node ≥22, obstack-js stays ≥20 (`:248-251`); (5) D302 register obstack-js OR `@ai-sdk/otel`, not both (`:264-270`); (6) D308 Responses covered from openai 4.87, `parse()` through it; NOT `stream`/`responses.stream()`/`beta.responses`/`compact()`/`retrieve(id,{stream:true})` (`:122,157-165,187-192`); (7) D301 no finish_reason on Responses — obstack records `status` (`:180-185`); (8) D259 streaming not instrumented on any of the three SDKs (`:137-141`; `obstack-py/README.md:109-111`); (9) D259 no JS logging bridge — "no bridge from `console`, `pino` or `winston`" (`:277-280`); (10) D316 fail-open = "a span with request attributes only", never "no span" (`:173-178`); (11) S4.1 gap — `images`/`lint` have no per-check README bullets (`README.md:135-145`); (12) D292/CH3 "gone from every product read the moment the statement returns; disk shrinks in the background — both halves" (`retention.go:8-14`); (13) D292 straddling trace: summaries expire on `max_seen_date`, older spans go first (`:43-46`); (14) D294 the bound is a stated posture: ~100M+ past-cutoff rows in one workspace-table; failure = loud, counted, next workspace, retry tomorrow; consequence = over-retention until the 90-day TTL (`:29-41`); (15) D295 Vercel grouping identity is the **project id**, `projectName` rides as `vercel.project.name`; (16) receivers built + CI-exercised, "coming soon" because the hosted endpoint is S5's — self-hosters can use both today (`connectors.ts:141-148`; `cloudwatch-forwarder/README.md:7-11` verbatim); (17) D251/D265/D267 mode stamp: a live-stamped artifact refuses to serve as mock and vice-versa, naming both values; (18) D265/D272 compose secret: `OBSTACK_BETTER_AUTH_SECRET` → `BETTER_AUTH_SECRET`, no default, refuses to boot with the `openssl rand -base64 32` instruction (`.env.example:18-25`; `compose/README.md:42-48`); (19) D253/D123 chart `existingSecret` is a name never a value; rotation is a documented procedure, never automated, per credential (`helm README:406-455`); (20) D263/D278 grace: chart `terminationGracePeriodSeconds: 45`, compose `stop_grace_period: 45s`, overrun drops counted under `ReasonShutdown`; (21) D253/D284 managed Postgres disables the in-chart one and retargets the pg-migrate Job — the Job failing IS the reachability gate; real-provider TLS/`sslmode` verified at S5 (`helm README:363-388`); (22) D119 an `http://` origin downgrades the session cookie away from `__Secure-` (`.env.example:31-35`); (23) D254 trace context only from measured payload fields — no inferred correlation, ever; (24) D255 corpus list — signup, quickstart (byte-check re-runs), self-hosted bundle, connectors incl. receivers' coming-soon, Explain, billing, D259 absences; (25) D255 the four changelog entries migrate verbatim and the D246 sweep's coverage survives the move; (26) D255 `mock/docs.ts` deleted with its render paths, both modes render the real corpus; (27) D256 status = components (app, ingest, docs) + curated incident files + "external monitoring begins at launch", no fabricated uptime; (28) D257 landing floor (footer, nav, pricing mirror, waitlist out); (29) the idea doc's docs sentence — **stated in the absolute form today's truth allows**: `traceAgent`/`traceTool` capture no arguments and no return values (`obstack-js/README.md:110-112`).
- **CS3 — the only true pricing source** is `0005_metering.sql:121-129` + `0006_explain_quota.sql:21-23`: `free` 50,000 events/mo · 7 days · $0 · 20 Explain runs; `pro` 1,000,000 · 30 · $49 · 200. **No `scale` row, no overage column, no seat column.** The mock `BillingTab` (`SettingsSuite.tsx:813,815`: "1M events/mo included, then $1.50 per 100k", "Unlimited seats") and the mock ingest/audit copy (`:1229-1253`: "90-day audit retention · exportable as JSON on Enterprise") are PRD hypotheses with no catalog column — **none may enter the docs**.
- **CS4 — true sources by page** (T2 writes from these and nothing else): root `README.md` (run mock/real, the two SDKs, CI checks, repro); `obstack-js/README.md` (install-from-source, `init()`, coverage table `:119-126`, floors, the two `ai` mechanisms, fail-open); `obstack-py/README.md` (install, `init()`, coverage `:102-107` — **the Responses API is in Python's Not-covered column, a real asymmetry to state**, D8 table `:122-130`, `OTEL_*` env table `:160-167`); `deploy/compose/README.md` (boot, secrets, collector route, smoke, drive, guard, SDK evidence, keys + `ok_dev_local`, pins); `deploy/helm/obstack/README.md` (D35 boundary, the destructive 0.2.0 upgrade, Jobs, `--wait`, managed Postgres `:363-388`, credentials & rotation `:406-455`, collector contract, kind repro, acceptance); `cloudwatch-forwarder/README.md`; the demo READMEs; `Quickstart.tsx:31-127` (the D78-verbatim three tabs — the Python-only `OTEL_SEMCONV_STABILITY_OPT_IN` asymmetry is deliberate); `connectors.ts` (3 available: OTLP, Kubernetes, Docker; 16 coming soon); `SettingsSuite.tsx:665-760` (the LIVE billing tab formats `plans` columns, defines none); `explain/contract.ts:29-42` (two refusal reasons `not-configured`, `over-quota`), `quota.ts:5-11`; `retention.go:1-46`; `receive/vercel.go:24-41`, `cloudwatch.go:18-33` ("S5 activates it" — carried, not dropped); `.env.example`; `values.yaml` groups (`clickhouse:20`, `postgres:63`, `web:120`, `ingest:176`, `collector:230`, `demo:261`, `utilityImage:282`).
- **CS5 — already-stale sentences on master the docs must not copy** (and the root-README pass fixes): `README.md:5` "This repo contains Phase 0 … and Phase 1" (the repo is at Phase 4); `:135,:213` `images`/`lint` named, never described; `:145` "The real number returns with the first runner" for `sdk-e2e` (it returned: 4m40s); `:140` `stack` "5m58s–7m17s" without the D318 rider; `obstack-js/README.md:20-21` names the tgz version literally (docs must interpolate `OBSTACK_JS_VERSION`).
- **CS6 — the 21 honest absences** (each with its ruling; T2's "what obstack does not do" page): no JS logging bridge (D259); streaming untraced on all three SDKs (D259); Responses edges (D308) + below-4.87 no module; obstack-js OR `@ai-sdk/otel` (D302); the D316 request-attributes-only span; obstack-py covers chat completions + FastAPI only (D87); `traceAgent`/`traceTool` capture no arguments/returns (D78); Vercel/CloudWatch built, CI-exercised, coming soon until the hosted endpoint (D101/D208) — self-hosters can use both today; drain/CloudWatch logs carry no trace context, none invented (D254); no Pub/Sub, Event Hub, Heroku/Fly/Railway drains, syslog, Loki/Elastic compatibility (D264b); no MCP/skill/CLI vehicle (D264a); dashboards/diff/explore/infra/map/services/users/cost persistence are M5 — nothing saved (D264c); no alerts/oncall/SLOs/incidents/issues/changes (M6) — the status page is deliberately not SLO-fed; no Ask/evals/security-redaction/pipelines/SSO/SOC2 (M7); exactly `free` and `pro` — no Scale, no overage, no seats (D226/D163); retention enforced by a daily sweep with a stated bound (D294); deleted data gone from reads immediately, disk at merge (D292); external uptime monitoring begins at launch — no numbers until then (D256); managed Postgres verified in kind only (D284); expired invitations never reaped, grant nothing (D153); nothing tails or polls — refreshing is a button (D273).

### LF — the landing fence (`.planning/2026-08-30-s4.4-landing-fence.md` — the record; summary here)
- **LF1 — 116 claims: 51 T / 40 F / 16 U (14 ‡) / 9 S.** The 40 FALSE resolve to 16 replacement classes (fence §2): "one env var" ×4 → three env vars (`connectors.ts:83`); "live demo" ×4 → "the demo"; "a real failure" → "a sample failure"; connector breadth ×3 → "Kubernetes, Docker and any OpenTelemetry source today — … on the way"; "your own watches" → the live overview's real four numbers; **"Live tail across every pod" → search copy, and the `TourGuide.test.ts:49` allowlist emptied**; "& retries" dropped; "& feature" → "and model"; the K8s-events row/sentence → pod/node/container identity (`0001_spans.sql:34-37`); "one store" → "one telemetry store"; "one container to self-host" → "one compose file"; "Point your OTel at us" → "at your obstack"; the waitlist block ×2 + "private preview" ×2 deleted; the auth pages' `/#waitlist` links replaced; **the four screenshots re-shot** (pixels carry "ingesting", `ziad@loopwork.ai`, "live tail", Vercel/CloudWatch shown CONNECTED, a K8S-EVENTS track — bans the suite enforces in source and cannot see in a PNG); `/status` deleted with its two in-app links.
- **LF2 — the 14 rulings (fence §3):** the "six tabs and a guess" framing (L24-27); "A full observability platform — not just a trace viewer" (L35 — no metrics/alerts/SLOs/dashboards are wired); Alerts/Ask/Evals visible in the marketing screenshots (L44 — M6/M7); "The join is the product" (L49); **every Datadog/Grafana/Langfuse/Helicone cell and bullet — 18 cells + 4 bullets, none measured** (L50/L60/L63/L69); "Priority support" on Scale (L79). Two verifications: the "2-line SDK" claim is true in content but neither package is published (L71 — S5 gate or "install from source"); Pro "+ usage" — the meter exists, the unit price lives in Polar (L77 — S5).
- **LF3 — `:547` is the ONLY honesty label** over the hero trace (`HeroTrace.tsx` imports `mock/stories.ts:251`), the video, and the screenshots (fence §6). Inside the app D21's per-surface grain holds (`DemoFooter`, `SampleDataBadge`, the auth pages' prose); on the landing it does not. **(ruling — K9: per-surface labels before `:547` goes.)**
- **LF4 — nothing tests the landing** (fence §5): `page.tsx` is read by no test; the only indirect coverage is the allowlist that pins the lie and `connectors.test.ts:118`. The fence test S4.4 owes: pricing mirror (+ no Scale numbers), banned-phrase sweep with an empty allowlist, the sample-label invariant (imports `@/mock/*` ⇒ a SAMPLE marker in the file), the connector-breadth mirror (3 available), the link set (every `href` resolves — would have caught `/#waitlist`), an image manifest (each `shots/*.png` by content hash).
- **LF5 — D262 cross-host wiring:** both "Create your workspace" CTAs (`:66`, `:526`) and the auth pages' links must reach the LIVE host from the mock marketing host; the URL is S5's (U10/U11). **(ruling — K13.)**

## Advisor decisions
<!-- Kickoff round 2026-08-30: to be recorded as D319– continuing the ledger. -->

## Kickoff questions for the advisor
- **K1 — the mechanism, as measured (NX1–NX3): ratify.** `@next/mdx@16.3.3` + `@mdx-js/loader` + `@mdx-js/react` + `@types/mdx` (dev + runtime as the guide states); `next.config.ts` gains `pageExtensions` + `withMDX` with string-form `remark-gfm`/`rehype-slug`; `apps/web/src/mdx-components.tsx` (zero-arg `useMDXComponents`, every override spreads props); the content tree at **`apps/web/content/{docs,changelog,status}/**.mdx`** (NOT under a sweep-skipped dir — NX8) with `export const frontmatter = {title, order, …}`; one catch-all `app/docs/[[...slug]]/page.tsx` with `generateStaticParams` over an `fs` walk at build time + `dynamicParams = false` + dynamic `import()`; SSG only, no request-time `fs`. Content version pins: exact.
- **K2 — one renderer, two mount points, one auth wall (NX6).** Options: (a) `/app/docs` REDIRECTS to `/docs` — one mount, identity trivially satisfied, in-app "Docs" nav points at the public route (the app shell is not around it; the reader leaves the shell); (b) `/app/docs/[[...slug]]` renders the SAME server component + content loader inside the app shell (auth-gated in live mode, exactly as every `/app` route is), so "in-app docs" is literal. PM lean: **(b)** — amendment 6 says the in-app surface renders the same content source; one `DocsPage` component + one loader, two thin route files; the auth gate is the app's existing posture, not a docs decision.
- **K3 — the D21 third class (NX5).** `live-routes.ts` gains a named `productChromeRoutes` set (`/app/docs` — and any future chrome), consumed by `SampleDataBadge` (null for chrome in either mode) and `CommandPalette` (hint "docs", never "sample data"); `liveWiredRoutes` untouched. Ratify the shape — never smuggle chrome into the live set.
- **K4 — the quickstart in docs (NX11).** Options: (a) the docs quickstart page renders the SAME snippets through a shared server component that `Quickstart.tsx` also uses (one definition of the D78-verbatim strings; the byte-check covers both by construction, plus a "no second copy" test that greps `content/**` for the banned literals and for any `npm install ./obstack-js-` / `OTEL_EXPORTER_OTLP_ENDPOINT=` line — every such line must come from the component); (b) duplicate the strings in MDX and grow `Quickstart.test.ts` a parallel arm. PM lean: **(a)** — D215/D206 one-definition. Note the endpoint interpolation: the docs are SSG and mode-blind, so the endpoint the public docs show is the **placeholder form** (`<your ingest endpoint>` / the self-hosted default), while the in-app quickstart interpolates the deployment's real endpoint — state which the docs render and why (the docs are not the onboarding page).
- **K5 — `/changelog` on the pipeline (NX8).** `content/changelog/YYYY-MM-DD-slug.mdx` with `export const frontmatter = {date, kind: "new"|"improved", title}`; `app/changelog/page.tsx` lists them from a build-time index (an `fs` walk inside the page's build — it is a static route); the four entries migrate verbatim; the `:5-16` docblock's substance moves to `content/changelog/README.md` (swept). Done-check: a test asserting the D246 sweep's `scanned` set includes `apps/web/content/**` (so the "coverage survives the move" clause is pinned, not believed) and that the allowlist is **empty** after T5. **Dates:** the four dates are unpinned (C6) — verify against `git log` for the commits that shipped each; if a date is wrong, it is corrected (a changelog date is a claim).
- **K6 — `/status` shape (D256, NX9).** `app/status/page.tsx` rewritten mode-blind: a component list (`app`, `ingest`, `docs`) each stating **what it is** (no state pill — there is no monitor to feed one), a "Monitoring" slot stating plainly that external uptime monitoring begins at launch (no numbers, no strip), and "Incidents" listing `content/status/incidents/*.mdx` (`export const frontmatter = {date, title, status: "investigating"|"monitoring"|"resolved", components: [...]}`), rendering "No incidents recorded" when the directory is empty — which it is at launch. The Loopwork page deleted; `mulberry32` import gone; `app/slos/page.tsx:19` link → removed or pointed at the real page? PM lean: pointed at the real `/status` (the SLO page is mock; a link to a true page is fine), `CommandPalette.tsx:33` kept pointing at the real page. Ratify the file shape + the two link dispositions.
- **K7 — the fence's FALSE→TRUE replacements (LF1, fence §2): ratify the 16 classes as the copy.** And **the 14 ‡ rulings (LF2)**: PM leans — keep opinion-class positioning ("six tabs and a guess", "the join is the product") as the voice, not claims; **cut** "A full observability platform — not just a trace viewer" → "One trace across your whole stack — not a trace viewer bolted onto a log store"; **drop** "Priority support" from Scale (no commitment exists); **the comparison table**: keep the nine rows with obstack's three FALSE cells fixed (retries, feature, K8s events), keep the competitor cells **but** add a dated, hedged legend ("as we read their public docs in August 2026 — tell us if we are wrong", with the date a literal the fence test pins) — or cut the competitor columns entirely. The advisor decides which; the PM lean is the dated legend (the table is the page's argument). L71 "2-line SDK" → "install from source today; on npm and PyPI at launch" (true now, S5 flips it). L77 stays with "+ usage" and the S5 Polar verification registered.
- **K8 — the screenshots (LF1 item 15).** Re-shoot the four `shots/*.png` from the CURRENT mock build (the D262 marketing host is the mock image — the screenshots must be of what strangers will see) with the current sidebar; **L44**: the sidebar shows Alerts/Ask/Evals (mock surfaces of M6/M7 products) — options: crop them out, or accept them as the SAMPLE-labelled demo they are (the DEMO WORKSPACE label is baked into every shot). PM lean: shoot the real sidebar uncropped (the demo shows the demo; the label is on it) and let the fence's image manifest pin the hashes. The hero video: keep, add the per-surface sample label (K9). Tooling: the gstack `/browse` headless browser against the local mock build at a fixed viewport; the shots' dimensions recorded. Ratify IN (S1 L2: pixels no sweep reads).
- **K9 — the `:547` replacement (LF3).** Per-surface labels: the hero trace block and the screens showcase each carry a visible "sample data — a fictional AI support-agent company" line (the same wording the app's `DemoFooter` uses, D134/D228), and the fence test's sample-label invariant enforces it (a file importing `@/mock/*` must contain the marker). Then `:547` becomes `© 2026 obstack` + the link row. Ratify.
- **K10 — the waitlist removal (D257, NX10).** Delete `WaitlistForm.tsx`, `marketing/actions.ts`, `server/waitlist.ts`, `lib/waitlist.ts` + `.test.ts`, the two render sites, `@vercel/blob` from `package.json`; `signup:60`/`login:55` → "Open the demo" + the docs quickstart link; `signup/mock-mode.test.ts:94-98,116` reconciled (it is D156's shim-of-record — the assertion moves to the new links, not deleted). `BLOB_READ_WRITE_TOKEN` exits by deletion (it lives only in `server/waitlist.ts:7`). **The collected entries' disposition is the USER's** (asked 2026-08-30; pending — nothing in this sprint touches the Blob store either way). Ratify.
- **K11 — the root README operator pass (CS5) is IN this sprint** — it is product surface a stranger reads first: `:5` phase sentence, the `images`/`lint` bullets (S4.1 gap), `:140/:145` CI numbers with the D318 rider, plus any sentence T2 finds false while sourcing from it. Ratify as in-scope (no code).
- **K12 — the Python asymmetry (CS4):** obstack-py does not cover the Responses API (`README.md:104`); the docs state it as an absence; no code this sprint (M5-planning packet if it matters). Ratify.
- **K13 — D262 cross-host links (LF5).** The mock marketing host's "Create your workspace" and the auth pages' links must reach the live host, whose URL is S5's (U10/U11). Options: (a) leave `/signup` as-is and register the rewire as an S5-GATE item; (b) add now one build-time `NEXT_PUBLIC_OBSTACK_APP_ORIGIN` (default empty = same host) read by a single `appHref(path)` helper used by every cross-host link, so S5 sets one value. PM lean: **(b)** — additive, one definition (D215 shape), the mock host stops sending strangers to "there is no workspace here" the moment S5 sets it; registered on the S5-GATE either way.
- **K14 — budget.** `web` 4m20–4m30 + MDX compile (single-digit s) + four new deps in `npm ci`; `images` ×2; `e2e`/`sdk-*` untouched. No new job (K3). The first-runner numbers return in the bundle; the `web` cap is 15m.

## Critical items
<!-- Phase plan "Verdict and gates" item 4 — these return to the advisor before merge. -->
- **The complete landing-fence enumeration** (`2026-08-30-s4.4-landing-fence.md`) with the 14 ‡ rulings applied and the fence test green with an **empty** allowlist (S1 L2)
- The docs-corpus honesty sweep (D246 repo-wide + the corpus-specific bans from `corpus-honesty.test.ts` re-targeted at `content/**`) + **the quickstart byte-check re-run against the docs copies** (K4)
- The `mock/docs.ts` deletion sweep (zero importers, the helm assertion re-homed, no Loopwork string left in `apps/web/src` outside `mock/` demo data)
- The changelog pipeline's D246-sweep continuity, pinned (K5)
- The `/status` replacement with the Loopwork deletion confirmed (zero `mulberry32` importers outside `mock/`, zero "Loopwork status" strings)
- CI first-runner numbers for `web` and `images` (D137 shape)

## Tasks

### T1: the docs mechanism — MDX pipeline, content tree, `/docs` + `/app/docs`, the D21 chrome class, the deletion of `mock/docs.ts`
- status: pending · wave: 1 · depends: — (K1–K4 rulings applied)
- owns: apps/web/package.json (+ the four MDX deps), package-lock.json, apps/web/next.config.ts, apps/web/src/mdx-components.tsx (new), apps/web/content/docs/** (new — the TREE + an `index.mdx` placeholder that T2 replaces; conventions README), apps/web/src/lib/docs/** (new — the loader: walk, frontmatter, ordering, slug→file), apps/web/src/components/docs/** (DocsPage/DocsNav/Prose — replacing DocsLibrary.tsx, which is DELETED), apps/web/src/app/docs/[[...slug]]/page.tsx (new, public), apps/web/src/app/app/docs/[[...slug]]/page.tsx (in-app mount per K2; `app/app/docs/page.tsx` replaced), apps/web/src/lib/live-routes.ts (+ `productChromeRoutes`), apps/web/src/components/shell/{SampleDataBadge,CommandPalette,SideNav,TopBar}.tsx (chrome-class consumers + nav), apps/web/src/mock/docs.ts (DELETED), apps/web/src/mock/corpus-honesty.test.ts (the two docs assertions re-targeted at `content/**`), apps/web/src/lib/docs/docs.test.ts (new)
- goal: K1's mechanism exactly; a mode-blind renderer (no `resolveMode()`, no `@/mock/*` import — pinned by test); `generateStaticParams` over the tree, `dynamicParams=false`; frontmatter via named export; the in-app mount per K2 sharing the same component and loader; `productChromeRoutes` per K3 so `/app/docs` carries no SAMPLE badge in either mode and the palette says "docs"; nav entries kept pointing at `/app/docs` (in-app) and the marketing nav gains `/docs` in T5; `mock/docs.ts` + `DocsLibrary.tsx` deleted with zero remaining importers; `corpus-honesty.test.ts:47/62` re-targeted at `content/**` (the helm-command assertion now reads the MDX corpus and still fails closed on an empty corpus); the loader unit-tested (ordering, slug resolution, 404 off-list).
- done-check: `npm run build` in `apps/web` green with every docs route `● (SSG)`; `npm test` root green incl. the re-targeted corpus tests; a test proving the renderer has no `@/mock/*` edge; `grep -rn "mock/docs\|DocsLibrary\|Loopwork" apps/web/src --include='*.ts*' | grep -v "src/mock/"` → empty; `next build` wall-clock recorded vs 35.9s; the standalone output contains the compiled docs (checked in the `images` job's mock image at review)
- result:

### T2: the corpus — quickstart-first, from the true sources, with every owed sentence
- status: pending · wave: 2 · depends: T1 (the tree + renderer + the shared quickstart component per K4)
- owns: apps/web/content/docs/** (all pages), apps/web/src/components/onboarding/Quickstart.tsx (ONLY to extract the snippet strings into a shared server component if K4(a) — the rendered quickstart byte-identical), apps/web/src/components/docs/QuickstartSnippets.tsx (new, per K4), apps/web/src/components/onboarding/Quickstart.test.ts (grows the docs-copy arm), apps/web/src/lib/docs/mirror.test.ts (new — the docs mirror tests), packages/obstack-js/README.md + packages/obstack-py/README.md (only if a sentence is found false while sourcing — report each)
- goal: the page map — `/docs` (Start here; the positioning line from PRD §11 verbatim) · `quickstart/` (sign-up-and-get-a-key · send-your-first-trace = the three D78-verbatim tabs through the shared component · what-you-should-see) · `sdks/` (typescript · python · bring-your-own-otel) · `self-hosting/` (docker-compose · helm-chart) · `connectors/` (overview: 3 available / 16 coming soon · vercel-log-drains · aws-cloudwatch — both carrying the "coming soon until the hosted endpoint" truth and the D254 no-inferred-correlation sentence) · `explain` · `billing-and-plans` (the `plans` seed ONLY — free/pro, no Scale, no overage, no seats) · `retention` (D292/D294 both halves) · `what-obstack-does-not-do` (the 21 absences, each as ruled). Every page written FROM CS4's sources; every one of CS2's 29 sentences placed (a checklist in the task result mapping sentence → page); no sentence from CS3's mock billing copy or CS5's stale list. **Mirror tests** (D206/D266 shape, read sources as text): SDK peer ranges (`package.json` ↔ docs), the compose env var names (`.env.example` ↔ docs), the chart values keys named in docs (`values.yaml` ↔ docs), the plan numbers and Explain quotas (`0005`/`0006` ↔ docs), the connector counts (`connectors.ts` ↔ docs), the receivers' route paths (`/v1/integrations/{vercel,cloudwatch}`), the grace/retention constants (`retention.go`, chart/compose grace). Quickstart page: the byte-check re-runs over it (K4).
- done-check: root `npm test` green with the new mirror + byte-check arms; the D246 sweep green (`TourGuide.test.ts` scans `content/**` — asserted); `corpus-honesty` bans hold over `content/**`; a manual read-through by the reviewer of every page against its sources (the reviewer's checklist is the fence for docs); zero `${` or unresolved placeholder in rendered docs; the honest-absence page carries all 21 with ruling ids in comments
- result:

### T3: `/changelog` joins the pipeline
- status: pending · wave: 2 · depends: T1
- owns: apps/web/content/changelog/** (new — four files + README), apps/web/src/app/changelog/page.tsx (rewritten to list the content dir), apps/web/src/lib/docs/changelog.ts (new loader or a mode of the docs loader), apps/web/src/components/shell/TourGuide.test.ts (the sweep-coverage assertion + the allowlist to empty after T5 — coordinated with T5), apps/web/src/app/changelog/changelog.test.ts (new)
- goal: K5 — the four entries verbatim (NX8) in dated files with frontmatter; dates verified against `git log` (each entry names the commit range that shipped it in a comment; a wrong date is corrected and the correction recorded); the page renders newest-first from the build-time index; the `:5-16` docblock substance → `content/changelog/README.md`; a test that the D246 sweep's scanned set includes `apps/web/content/**` and that the four entries' bodies are byte-equal to the recorded originals (pinned once, so a later edit is deliberate).
- done-check: `/changelog` `● (SSG)`; tests green; `grep -c "Nothing tails" apps/web/content/changelog` = 1
- result:

### T4: `/status` — obstack's own page, the Loopwork fiction deleted
- status: pending · wave: 2 · depends: T1 (frontmatter/loader conventions)
- owns: apps/web/src/app/status/page.tsx (rewritten), apps/web/content/status/incidents/** (new — README + `.gitkeep`-class empty dir with the frontmatter contract documented), apps/web/src/lib/docs/incidents.ts (new loader), apps/web/src/app/app/slos/page.tsx (`:19` link per K6), apps/web/src/components/shell/CommandPalette.tsx (`:33` per K6), apps/web/src/app/status/status.test.ts (new)
- goal: K6's shape verbatim: components (app, ingest, docs) described, not scored; the monitoring slot's one sentence ("External uptime monitoring begins at launch; this page shows no uptime numbers until then" — D256 wording); incidents from curated files, "No incidents recorded" when empty; mode-blind; no `mulberry32`, no percentages, no strips, no "Loopwork". Test: renders the empty state; a fixture incident file renders in order; the page source contains no digit followed by `%`.
- done-check: `grep -rn "mock/rand" apps/web/src/app` → empty; `grep -rn "Loopwork status\|status.loopwork\|powered by obstack SLOs" apps/web/src` → empty; `● (SSG)`; tests green
- result:

### T5: the landing launch pass — the fence applied, the labels, the nav, the waitlist out, the fence test, the screenshots
- status: pending · wave: 3 · depends: T1, T3, T4 (nav links target routes that exist — D257 sequencing)
- owns: apps/web/src/app/page.tsx, apps/web/src/components/marketing/** (HeroTrace/ScreensShowcase/labels; WaitlistForm.tsx + actions.ts DELETED), apps/web/src/server/waitlist.ts (DELETED), apps/web/src/lib/waitlist.ts + waitlist.test.ts (DELETED), apps/web/package.json (`@vercel/blob` removed) + package-lock.json, apps/web/public/shots/*.png (re-shot), apps/web/src/app/signup/page.tsx (`:57,:60`), apps/web/src/app/login/page.tsx (`:52,:55`), apps/web/src/app/signup/mock-mode.test.ts (`:94-98,116` reconciled), apps/web/src/lib/app-href.ts (new, K13) + its test, apps/web/src/app/landing-fence.test.ts (new), apps/web/src/components/shell/TourGuide.test.ts (allowlist → empty, with T3)
- goal: the fence's §2 replacements as ruled in K7 (all 40 FALSE rows resolved; the 14 ‡ per the rulings); nav gains Docs (`/docs`) + Status (`/status`) in `:55-59` and the footer row; `:547` → `© 2026 obstack` after the per-surface sample labels land on the hero trace and the screens showcase (K9); the waitlist removed per K10 with the auth-page links and D156's test reconciled; the pricing block stays copy with the **mirror test** against the seed (D257); `appHref()` per K13 (default same-host) on every cross-host CTA; the **fence test** (LF4: pricing mirror + no Scale numbers; banned phrases with an EMPTY allowlist; the sample-label invariant; connector-breadth mirror; the link set; the image manifest by content hash); the four screenshots re-shot from the current mock build per K8 (viewport/dimensions recorded; the manifest pinned).
- done-check: `landing-fence.test.ts` green; `TourGuide.test.ts` green with `ALLOWED = []`; root `npm test` green; `grep -rn "BLOB_READ_WRITE_TOKEN\|@vercel/blob\|waitlist" apps/web package.json package-lock.json` → empty; every `href` on `/`, `/login`, `/signup` resolves (the test); the reviewer reads the rendered landing against the fence row by row
- result:

### T6: the root README operator pass (K11) + the records
- status: pending · wave: 3 · depends: T2 (the docs are the reference the README now points at)
- owns: README.md (the stale sentences in CS5 + the `images`/`lint` bullets + a pointer to `/docs`), this file (results), .planning/2026-08-30-s4.4-exit-evidence.md (new), .ziarmy/retro.md (local)
- goal: CS5 fixed; `images`/`lint` described in the register of the other bullets; the CI numbers carry the D318 rider; nothing else re-flowed. Exit bundle in the S4.3 shape.
- done-check: `git grep -n "Phase 0" README.md` → none; the check-list bullets are ten; `npm test` unaffected
- result:

## Exit evidence (to be assembled at close)
- The corpus page list with, per page, its sources and the CS2 sentences it carries; the honest-absence page
- The byte-check + mirror tests green over the docs copies; the D246 sweep scanning `content/**` with an empty allowlist
- The fence: 116 rows → 0 FALSE, the 14 rulings applied, the fence test green; the re-shot screenshots' manifest
- The two deletions confirmed (`mock/docs.ts`, the Loopwork status) with zero residue
- `web`/`images` first-runner numbers; the `stack` second-master-push datum if it exists by then (D318)

## Run log
- 2026-08-30 — breakdown with measurements taken first (NX1–NX12, CS1–CS6, LF1–LF5 + the fence record; three read-only Opus passes); kickoff packet K1–K14 to the advisor.
