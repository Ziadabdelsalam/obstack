# S3.3 Exit Evidence — usage metering, Polar billing and quota degradation

- sprint: S3.3 · milestone M3 · exit-blocking per D104 · dependencies S3.1 (CLOSED), S3.2 (CLOSED)
- branch: `s3.3-metering-polar-quota`, 26 commits `ab8d483..940a927`, every commit task-owned or manager-landed-and-ratified
- **SIGNATURE: ADVISOR-SIGNED, effective at tip `940a927` on eight CI checks green** (the S3.2 effective-at-tip-on-green precedent). If any check reds at tip, the signature does not attach and the failure returns to the advisor.
- task record: 16 slots approved — T1–T11 (T11 advisor-added at kickoff, D161), F1–F5 (all advisor-ordered or advisor-ruled riders); integrator GO round 1 + targeted re-verify GO; goal MET 0/3 (Step 7, advisor verdict of record)
- rulings this sprint: **D161–D194**, recorded in `.planning/2026-08-19-s3.3-team-plan.md`
- governing record: M3 phase plan §3 S3.3 (post-U3/D110) + D94–D110; S3.2 exit bundle inheritance list discharged in full (verified below)
- recorded by: manager, verbatim from the advisor's return.

## 1. Exit criterion, clause-by-clause (verbatim clause → evidence)

1. **Event counts meter per workspace from the ingest path into the Postgres ledger of record (D99)** — MET. `usage_ledger` (migration 0005, D162 byte-checked twice: T1 review + F1 amendment re-check), UPSERT-add from the T3 accumulator on a 5s flush, proven correct under two concurrent flushers; sandbox run metered 240 spans + 60 logs **exactly**.
2. **Billing tab shows real usage against its plan from that same ledger — one definition, the same number the banner uses (D110 §2)** — MET. One `getUsage` (D171/D181/D183), banner==tab asserted as raw integers tied to the drive's own ledger SQL (drive 132/0).
3. **Plan change round-trips through Polar sandbox — checkout from the billing tab, state confirmed on return by reading the Polar API, webhooks async reconciler, plan row lands in Postgres and propagates on the cache TTL** — MET. Sandbox evidence run: hosted checkout paid with 4242, poll-on-return reconciled → `?upgraded=1` (D189 PRG), plan row `pro` with the rail's `polar_customer_id`, one-paint Pro/1,000,000/30-day, banner down; rail-side subscription active, product = `POLAR_PRODUCT_PRO`, 4900 USD, `external_id == workspace_id`. Integration drive: pro row lifts sampling within one TTL.
4. **Ingest never calls Polar; no third-party round trip in the hot path** — MET by construction (reporter web-side per D170; module-boundary grep; ingest's only stores are ClickHouse + Postgres).
5. **Crossing quota switches to per-trace head sampling; sampled-out traces drop whole; drops under `obstack_ingest_dropped_total{reason="quota"}`** — MET. D165 contract (FNV-1a 64, keep iff h%10==0, 10%); all-or-nothing proven in Go and content-aware in the drive via the three D186(iii) pinned vectors (values re-derived in JS BigInt, presence content-filtered, absence queried workspace-unfiltered); drops agree at three surfaces (Prometheus=8, health row=8, tab=8).
6. **Banner raises from real Postgres rows; `UsageBanner` leaves `@/mock/workspace`; the layout suppression flips** — MET. Prop-fed from the layout's `getUsage` (T8), suppression flipped, negative control asserted before the raise; mock mode keeps its demo banner (byte-untouched, integrator re-probe).
7. **Per-workspace ingest-error count surfaced from the D100 health rows with staleness stated ("as of …")** — MET. `api_key_health` rows, Data & ingest tab, drive-asserted non-zero via a manufactured receive-path error; "as of" = `max(updated_at)`. Basis stated: receive-path errors (write/panic drops remain ops-only — recorded reading of D100, bundle §5).
8. **Pricing overrides apply on the D9 row shape through the workspace-state cache (D108)** — MET. T11 + D174/D175: longest-prefix both directions, table built once per 30s refresh (pointer-identity red proof), store-level lowercase CHECK, cross-language 4/4 in the integration drive.
9. **Billing & usage tab loses its `SampleMark` (D106)** — MET; Data & ingest likewise per D141 (one touch).
10. **Retention stated as an entitlement whose enforcement lands at M4 — no deletion claim (D105)** — MET, sentence rendered from the `plans` catalog.

## 2. Evidence of record

- **CI @ 148c4b5, 8/8 green:** e2e 5m35s · stack 5m4s · sdk-e2e 4m6s · web 3m43s · go 2m28s · kind 1m41s · sdk-js 54s · sdk-py 31s. F5 tip `940a927` re-run pending at signature time — signature effective on green.
- **Drive:** 132 passed / 0 failed on cycled stacks (sign-off run 102s; post-F5 109s), two consecutive greens at T10 close; S2.3 L4 refusals intact +1 (refuses any `OBSTACK_BILLING_MODE` but fake).
- **Sandbox evidence run: PASS**, 16/17 with the one FAIL resolved by D194/F5 (transcript: integrator's `s3.3-integration-evidence.md` + `sandbox-out/transcript.txt`). Measured facts added to the record: polar-sandbox boot works without `POLAR_WEBHOOK_SECRET` (demanded only at `verifyWebhook`); reporter yields 1 event/cycle, re-send → `{inserted:0, duplicates:1}` — **dedup on our `external_id` proven against the real rail**; no batch-size signal at M3 volume (D184(ii) CLOSED; reopens only on a measured limit).
- **Secret hygiene:** zero `polar_oat_`/secret-value hits across git, app, build and chrome logs. Secrets env-only: `POLAR_ACCESS_TOKEN` (sandbox), `POLAR_WEBHOOK_SECRET`; **no Polar secret exists in CI**.
- **D177 env inventory** (integrator's evidence file, of record): `OBSTACK_BILLING_MODE` (default fake through M3) · `POLAR_ACCESS_TOKEN` · `POLAR_WEBHOOK_SECRET` · `OBSTACK_APP_URL` · `POLAR_PRODUCT_<PLAN>` — the S5-GATE token inventory inherits all five.
- **Mock product byte-untouched** (D125, integrator re-probe).

## 3. Watches

- **D137 (`e2e`):** 5m35s vs S3.2's 4m8s — delta fully accounted (102→132 checks + the ~33s D172 flush+TTL propagation wait). Zero of three trips. **~25s headroom to the ~6-minute line; S3.4 and S3.5 each grow this step — first reclaim candidate on a trip is the propagation wait (drive-time TTL/flush override, decided then on measured numbers).**
- **D41/D93 (`stack`):** 5m4s, inside 4m11s–6m10s. Unremarkable.

## 4. Rulings index (D161–D194, headline only)

D161 breakdown corrections (T11 created; T2 call-site rider; T6 page.tsx seam; waves) · D162/D163 DDL + plan catalog (one quota definition, absent-row=free) · D164 workspace-state cache, Identity seam, one pool, fail-static-auth/fail-open-quota asymmetry, D155 restated · D165 sampling contract + pinned vectors · D166 flush contract · D167 overrides layering (as amended by D174) · D168 billing interface + mode selector · D169 webhook consumed set · D170 reporter (closed-windows/settle/external_id/no-send-log) · D171 one usage definition · D172 e2e seeding · D173 critical items · D174 built-table home, alias deleted · D175 lowercase canon CHECK · D176 checkout hardening ratified · D177 env inventory · D178 SDK-bump enum re-measure (standing) · D179 TZ-proof month SQL · D180 flush-discard recorded absence · D181 injected-QueryRows ratified · D182 reads-are-reads + one vocabulary · D183 `cache()` · D184 barrel/batch/tests · D185 counters placement · D186 riders + vector set of record · D187 SDK import-shape deviation ratified (joins D178) · D188 past_due reading · D189 PRG checkout return · D190 union collapse + loud logs · D191 mock narrowing · D192 injection rider (all refusal branches red-provable) · D193 log levels by anomaly class · D194 subscription_id: fake/drive narrowed to rail truth, column webhook-populated of record, S5-GATE rider.

## 5. Recorded absences and narrowings (stated, not glossed)

- `polar_subscription_id` is **webhook-populated only** (D194); a deployment without a reachable webhook endpoint never populates it — **S5-GATE production-promotion checklist item**.
- Write/panic drops are Prometheus ops-only; the product's ingest-error count is receive-path errors, basis stated on the tab (D162 reading of D100).
- Metering flush discards the whole snapshot on FK violation — unreachable in M3 (no workspace-deletion surface); per-row fallback pre-registered M4, gated on that surface (D180).
- Unknown-but-genuine webhook types 403 at the pinned SDK — unreachable at 0.49.0; any SDK bump re-measures the event enum AND the import/client shape as a named done-check (D178/D187).
- Mock mode leaves `?checkout=` in the URL — live-mode-only contract, D191.
- Quota/overrides fail **open** on Postgres outage; auth stays fail-static (D164(d) — the asymmetry is the design, stated in the package doc).
- The drive claims metering only; issued-key attribution wire step deliberately S3.4's (D115 narrowing, drive header).
- `npm test` requires an un-driven stack (exit-seed's quota-lowering has no undo; `down -v` restores) — compose README operational note.
- Six pre-existing eslint errors in non-S3.3 files — standing, no CI job runs lint.

## 6. S3.4 kickoff MUST inherit (binding)

1. **The e2e headroom fact:** 5m35s real, ~25s under the line, and S3.4's step growth lands inside it — the D137 trip protocol governs; the propagation-wait reclaim is the pre-named candidate.
2. **D115 (standing): the issued-key wire drive step** — S3.4's quickstart/onboarding drive sends on a UI-issued key and proves attribution; the S3.3 drive header names this as deliberately not taken.
3. **Quickstart truth per D101, all four named defects** (package names, `api_key=` arg, fake 5-second arrival, `x-obstack-key` header) — byte-level quickstart-vs-D78 review is a pre-registered critical item.
4. **Connections-hub health reads the D100 `api_key_health` rows** — the mechanism exists and is drive-proven; S3.4 consumes it, never a second counter path (S2.3 L3).
5. **The reconcile-wording↔D132-predicate coupling** (integrator-recorded) — resolved or restated at S3.4 breakdown.
6. **Pattern inheritance standing:** D121/D127/D129 forms, D68 corpora, D130 integration class, D11 posture, D142 evidence bar, D150/D152 mock-mode guard + standing test on every new auth-surface action, D189's reads-are-reads/PRG shape for any new return-URL surface, D193's log-level grading.
7. **One-definition contracts with pinned cross-language vectors** (D139 hashes, D186(iii) sampling vectors): any amendment sweeps every implementer in the same round (the S3.2 retro lesson, now twice-proven).
8. **Plan catalog is the one quota/retention definition** (D163) — S3.4 surfaces read `plans`, never a constant.
9. **M4/S5 pre-registration carry:** D178/D187 SDK-bump done-check; D180 flush fallback (gated); D153/D119/D123/D132 inventory unchanged; S5-GATE = production Polar flip + full D177 inventory + webhook-reachability consequence (D194) + D104 exit-clause re-verification against the production rail.

## 7. Signature

The S3.3 exit criterion is met clause-by-clause on the evidence above; the goal check returned MET 0/3; all sixteen task slots are approved; all critical items (D173 items 1–4) returned and were APPROVED; the sandbox run's single FAIL is resolved by D194/F5 with red proofs both ways. **ADVISOR-SIGNED — effective at `940a927` upon eight checks green.** PR #17 merges on that signature; the planning commit records this bundle verbatim as `.planning/2026-08-19-s3.3-exit-evidence.md`.

— Advisor, S3.3 (rulings D161–D194), 2026-08-19
