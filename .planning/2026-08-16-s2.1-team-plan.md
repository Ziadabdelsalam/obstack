# Team Plan — obstack monorepo restructure — the Next.js app moves to `apps/web` under npm workspaces in one mechanical PR (exit: CI green on the restructure PR, `smoke.sh` green from clean volumes, and the full web suite green from the new root, with no behaviour change)

meta:
- date: 2026-08-16
- sprint: **S2.1** (created by advisor D33 — standalone restructure sprint, between S2.0 and S2.2)
- repo/branch: `master` @ `6077af1` (+ S2.0's CI, which must be merged first)
- mode: workflow
- team-cap: 4
- iteration-limit: 3
- iteration: 0/3
- phase plan: `.planning/2026-08-16-s2-m2-phase-plan.md` (§8 D33 = this sprint's ruling)
- dispatch order: **after S2.0 merges.** This sprint's exit is "CI green on the restructure PR" — without S2.0's checks there is nothing to be green.

## Advisor decisions
<!-- Binding for all agents. Full text: .planning/2026-08-16-s2-m2-phase-plan.md §8 -->
- **D33 (the ruling that created this sprint)**: the restructure **precedes the SDK sprint** and lands as its own sprint directly after S2.0 — CI first, then the mechanical move guarded by the CI it just built. Placed before S2.2/S2.3 so the collector's Helm files and the search work land in the final layout once, at minimum code mass.
- **D33 (scope)**: **purely mechanical** — move existing code, npm workspaces (`apps/web`), tsconfig paths, `smoke.sh`/`smoke.ts` paths, Dockerfile contexts, CI paths. **One PR.**
- **D33 (no scaffolding)**: `packages/obstack-js` is created by **S2.4**, not here. No empty `packages/` directory, no placeholder package.json, no "we'll need it later" stubs.
- **D33 (exit)**: CI green on the restructure PR + `smoke.sh` green from clean volumes + the full web suite green from the new root.
- **D33 (CRITICAL ITEM)**: **the restructure PR returns to the advisor before merge.**
- **D28 (why now)**: S2.2 (collector + Helm) and S2.3 (search + LogsExplorer) both add files under the web app and `deploy/`. Every sprint that lands before the move makes the move bigger; this is the last cheap moment before M2's dev work.

### Kickoff gate rulings (advisor, 2026-08-16 — BINDING; verdict: dispatch wave 1 after S2.0 merges + C1–C3 applied)
- **C1 (T1, next.config.ts)**: `turbopack: { root: path.join(__dirname) }` breaks on the move — `__dirname` becomes `apps/web` while the single lockfile lives at the workspace root. T1 sets `turbopack.root` per the vendored Next 16.3 monorepo guidance (cite the doc in the result line). Acceptance signal: post-move build emits ZERO workspace-root/multiple-lockfile inference warnings.
- **C2 (T1/T4, typecheck)**: bare `npx tsc --noEmit` from root is impossible post-move (no root tsconfig). Root `package.json` gains delegating scripts incl. `typecheck` (`tsc --noEmit -p apps/web`) alongside `test`/`build`/`dev`; done-checks and T4's bundle say `npm run typecheck`. Delegating-script additions are configuration required by the move — inside the fence.
- **C3 (T3/T4, .env.local)**: root `.env.local` is untracked and post-move Next stops reading it — under D13 fail-fast, live mode dies on missing `CLICKHOUSE_URL` and reads as a broken restructure. T3's root-README runbook documents "move `.env.local` → `apps/web/.env.local`"; T4's bundle runs with env in the new location.
- **Lockfile churn note (T1 reviewer)**: npm-workspaces conversion rewrites `package-lock.json` substantially — expected mechanical churn, not a red flag.
- **T2 clarification**: root delegating scripts making the CI diff near-empty is RATIFIED over D33's anticipated one-line working-directory flip (better reading of the same intent). Check names stay `web`/`go`/`kind`; go.yml's `-count=1` line must survive untouched.
- **Root residue — silence CORRECT, explicitly**: `.claude/`, `.superpowers/`, `.ziarmy/`, `.planning/`, `docs/`, `pitchdeck-assets/`, `.playwright-mcp/` don't move (unanchored ignore patterns keep working); root `tsconfig.tsbuildinfo`/`next-env.d.ts` are ignored disk artifacts that regenerate under `apps/web`, no repo handling. Only C3 has behavior consequence.
- **Mechanics RATIFIED + riders**: one branch `s2.1-restructure`; T1 lands the move; wave 2 parallel in worktrees, pull-rebase on push races; T4 on final tip. Riders: (a) NO `push --force` by anyone, ever, on the shared branch; (b) T1 opens the DRAFT PR at end of wave 1 (wave 2 iterates against its checks). Wave 2 stays parallel.
- **Route-table parity spec**: capture full route-table text of all four builds (pre×post, mock×live, clean `.next` each), diff pre-vs-post per mode — BYTE-IDENTICAL is the bar, attached to the evidence bundle. D27a-sensitive rows must hold: live `/app`, `/app/traces`, `/app/traces/[id]` = ƒ; mock exactly as today (incl. T7's accepted mock ƒ). Same capture asserts zero lockfile/workspace-root warnings (C1). Test-count parity 21.
- **Review shape**: per-task reviewers STAY (S2.0 proved their value twice); advisor whole-diff review before merge is ADDITIVE. Review method for T1's reviewer and the advisor's final pass: `git diff -M`, review the NON-RENAME RESIDUE against T1's named-edit list — any residue line not on the list is exactly the "no file's meaning changes" escalation.

### S2.0 merge-condition amendments (advisor, 2026-08-16 — BINDING, from the S2.0 integrator's evidence)
- **A1 (T2)**: web.yml's move edit is **two lines, not one** — the `working-directory` anchor AND an explicit `cache-dependency-path: apps/web/package-lock.json`, because `defaults.run.working-directory` does not apply to `uses:` steps (go.yml models the correct form). Amends the earlier "near-empty diff" expectation; the diff is still enumerated line-by-line per T2's done-check.
- **A2 (T1)**: `.claude/` is untracked and NOT gitignored while holding agent worktrees — a `git add -A` staging hazard. Adding `.claude/` to `.gitignore` is a **named config edit** in T1's list (rides the gitignore-anchoring work T1 already owns; in-fence, not a deviation).

## Critical items
<!-- Must return to the advisor before merge. -->
- **The restructure PR itself (D33)** — advisor reviews the whole diff before merge. A repo-wide move is the one change where "it built fine" is not evidence.
- **Any non-mechanical change escalates immediately** — the contract of this sprint is that *no file's meaning changes*. Renaming a symbol, "while we're here" refactors, dependency bumps, lint-baseline fixes, or new files that are not configuration required by the move are all deviations. Report and stop; do not absorb.
- **T4's external linkage** — the Vercel project's Root Directory setting lives outside this repo. It is flagged, not silently skipped.

## Tasks
<!-- One block per task. Exclusive file ownership holds WITHIN a wave. -->

### T1: Move the app to `apps/web` under npm workspaces
- status: pending
- wave: 1
- depends: — (S2.0 merged)
- owns: the moved tree `apps/web/**` (from `src/`, `public/`, `package.json`, `tsconfig.json`, `next.config.ts`, `eslint.config.mjs`, `postcss.config.mjs`), the new workspace-root `package.json`, `package-lock.json`, `.gitignore`
- precondition: read the relevant guide in `node_modules/next/dist/docs/` before changing Next configuration or project layout.
- goal: the Next.js app lives at `apps/web` as the single member of an npm workspace root, with `@/*` still resolving to the app's `src/`, and **the repo-root commands still working unchanged** — `npm ci`, `npm test`, `npm run build` and `npx tsc --noEmit` all run from the repository root exactly as they do today, because every other task, the CI workflows and every human runbook invoke them that way. Nothing in `services/`, `demo/`, `deploy/`, `docs/` or `.planning/` moves.
- binding detail (this is the trap that pollutes the tree): `.gitignore` is **root-anchored** — `/node_modules`, `/.next/`, `/build`, `/coverage` (`.gitignore:4,17,14,22`). After the move, `apps/web/.next/` and any nested `node_modules/` are **no longer ignored**. Update the patterns as part of this task; a build followed by `git status --porcelain` must show nothing.
- binding detail (workspaces): the workspace list covers `apps/*` only. **No `packages/` directory is created** (D33).
- done-check (amended per kickoff C1/C2): `npm ci` from a clean checkout at the repo root succeeds and produces one root lockfile; `npm test` (21 tests today), `npm run build` and `npm run typecheck` (delegating script per C2 — bare `npx tsc --noEmit` is impossible post-move) all pass **invoked from the repo root**; `turbopack.root` set per C1 with the vendored doc cited, and the post-move build emits zero workspace-root/lockfile warnings; `git diff -M --stat` on the PR shows the moved files as **renames**, not delete+add, and no moved file has content changes beyond the config edits this task names; `npm run build` route table (the ○/ƒ listing) is identical to the pre-move build in both `OBSTACK_DATA_MODE=mock` and `=live` — captured with `rm -rf .next` between runs, because the M1 record (F6-ext) shows the build reuses cached prerenders across mode changes and would otherwise compare a stale tree against itself; `git status --porcelain` is clean after a build; no `packages/` directory exists.

### T2: CI workflow paths follow the move
- status: pending
- wave: 2
- depends: T1
- owns: `.github/workflows/*.yml` (S2.0-owned in the previous sprint; sequencing resolves the re-entry — S2.0 is merged and closed, and D33 names CI paths as part of this move)
- goal: the S2.0 checks pass against the new layout with the **smallest possible diff** — the web job's working directory / cache paths follow `apps/web`, the Go and kind jobs are verified untouched (nothing they reference moved), and no check is weakened, renamed, or made non-required to get green. If the root-level scripts kept working per T1, this diff should be near-empty; if it is not, say why in the result line.
- done-check: every S2.0 check is green on the restructure PR under its **original check name** (renaming a required check silently un-requires it in branch protection); the Go and kind checks still exercise what S2.0 proved — in particular the ClickHouse-backed Go job still runs the three integration test files rather than skipping them (D36); the diff to `.github/` is enumerated line by line in the result.

### T3: Smoke path + runbook paths follow the move
- status: pending
- wave: 2
- depends: T1
- owns: `deploy/compose/smoke.sh`, `deploy/compose/smoke.ts`, `deploy/compose/README.md`, root `README.md`
- goal: the D17 acceptance harness works from the new layout without changing what it proves. `smoke.sh` computes `repo_root` at `deploy/compose/smoke.sh:13-14` and runs `npx tsx --conditions react-server deploy/compose/smoke.ts` from it (`:45-47`); `smoke.ts` imports `@/lib/types`, so tsx must resolve `@/` from the tsconfig that now lives at `apps/web/tsconfig.json`. Fix the invocation (working directory and/or script path) — **the D17 mechanism itself does not change**: real facade + adapter code path, no SQL in `smoke.ts`, no throwaway API route, no running Next server. Both READMEs' path references follow.
- verify-only, expected to be a no-op (state the result either way): compose build contexts point at `../../services/ingest` and `../../demo/agent-app` (`deploy/compose/docker-compose.yml:35,66`) and **neither directory moves**; the repo has no `web` Dockerfile (web is not containerized until M4 per D14). If a Dockerfile context does need editing, that is a surprise worth reporting, not a quiet fix.
- done-check: `bash /Volumes/SSD/Workspace/observer-stack/deploy/compose/smoke.sh` runs green from clean volumes (`docker compose --profile demo down -v` first). **Invoke it through the capital-cased `/Volumes/SSD/Workspace/...` path** — the lowercase path makes Docker deny the bind mounts, which reads as a broken restructure and is not one. Smoke output still shows the four-layer assertion and the correlated-log assertion; `deploy/compose/README.md` and root `README.md` contain no stale paths; per kickoff C3, root `README.md` documents "move `.env.local` → `apps/web/.env.local`" (post-move Next reads env from the app dir; a stale root `.env.local` makes live mode die on D13 fail-fast and read as a broken restructure).

### T4: Agent files, external linkage, and the advisor evidence bundle
- status: pending
- wave: 3
- depends: T1, T2, T3
- owns: `AGENTS.md` / `apps/web/AGENTS.md` and root `CLAUDE.md` (reference only), plus the evidence bundle
- goal: the repo's own tooling tells the truth after the move, external linkage is resolved or escalated, and the advisor gets one complete verification bundle.
  1. **Agent files:** `AGENTS.md` states that its block is re-written by `next dev` from `node_modules/next/dist/server/lib/generate-agent-files.js`, resolved from that file's directory, and warns that in monorepos the `next` package may not be visible from the repo root. Run `next dev` once from `apps/web`, commit whatever it regenerates (committing it is what keeps the tree clean, per the block's own instruction), and make sure root `CLAUDE.md`'s `@AGENTS.md` reference still resolves to a file that exists and is accurate for the new layout.
  2. **Vercel linkage (USER ACTION, flag — do not guess):** `.vercel/` is gitignored (`.gitignore:37`), so the project's **Root Directory** setting lives in the Vercel dashboard, not in this repo. After the move, a project configured to build from the repo root will break. Report the exact setting to change (Root Directory → `apps/web`) and stop; do not deploy anything (deployment is M4/S5). Note for the record: the project's deploys also stall at BLOCKED when `HEAD` is a GitHub-noreply-authored merge commit — relevant to whoever makes the change, not to this sprint.
  3. **Verification bundle (amended per kickoff C2/C3 + route-table spec):** from a clean checkout with `.env.local` at its NEW location (`apps/web/.env.local`) — `npm ci`, `npm test`, `npm run build`, `npm run typecheck` from the repo root; `go test ./... && go vet ./... && gofmt -l .` from `services/ingest/` (untouched by this sprint — a change here is a defect); `smoke.sh` green from clean volumes; all S2.0 checks green on the PR; the four-build route-table capture diffed byte-identical per mode with zero workspace-root warnings.
- done-check: the bundle above is complete and attached to the PR; `git diff -M` over the whole PR contains no content change that is not a path or configuration consequence of the move; the Vercel item is recorded as a user action with the exact setting; **the PR goes to the advisor before merge (D33 critical item)**.

## Scope fence (binding on every task)
- **Mechanical only.** No behaviour change, no refactor, no renamed symbols, no dependency add/remove/bump, no lint-baseline edits, no new tests beyond what a moved path requires.
- **No `packages/`**, no `obstack-js` scaffolding, no SDK work (S2.4).
- No collector, no Helm chart, no K8s manifests (S2.2). No search or LogsExplorer work (S2.3).
- `services/ingest/`, `demo/agent-app/`, `deploy/`, `docs/`, `.planning/` do not move.
- One PR. If the move genuinely cannot land as one PR, stop and escalate — splitting it is a plan change, not an execution detail.

## Escalations log
<!-- Manager appends: {T#} — question → advisor decision (one line each). -->
- T1 reviewer fence catch (no escalation needed, recorded): executor's lockfile regen floated 18 package versions — reviewer redid it as seeded `--package-lock-only`, +11 −0, zero bumps.
- T1 ownership gap (Go-side `layer_contract_test.go:65` hardcodes `src/lib/types.ts`; only red on PR #10) → advisor: joins T1's named-edit list as a path consequence of the move — landed by T1 executor, no new task; "the cross-language contract test failing loudly is the mechanism working as designed."
- A1 wrong for workspaces (no `apps/web/package-lock.json` exists — hoisted root lockfile) → advisor AMENDED A1: web.yml edit collapses to `working-directory` → `apps/web`, cache config UNCHANGED (root lockfile is the correct key). T2's line-by-line enumeration still applies.
- layers.test.ts residue → advisor RATIFIED onto T1's named-edit list (three path literals, zero assertion changes, four break-probes = observed-not-assumed standard).
- Symmetry rider → advisor ACCEPTED: whole-diff pass greps repo-root-relative literals across ALL unmoved trees (`services/`, `deploy/`, `docs/`, `.github/`). Plus: `/out/` at `.gitignore:18` is un-anchored within T1's existing gitignore edit (consistency with its four siblings).
- Wave 2 releases once the T1 amendment lands and `go` is green at the new tip.
- T2 reviewer ESCALATE (cwd flip → root delegators `npm test/build/typecheck` no longer CI-exercised; a broken delegator rides green) → advisor ruled **(a): A1 re-amended to `working-directory: .`** — the original A1 assumed a per-app lockfile that doesn't exist; with cache/tests proven identical either way, the flip's only remaining effect is negative. K1 principle governs: one definition of the suite for humans and CI; CI runs root delegation, covers apps/web transitively; K3 anchor = workspace root. T2 executor re-lands; T4 releases on green.

## Integration
<!-- Filled by the integrator gate. Fix tasks get F# IDs, appended under ## Tasks. -->
- Manager call (2026-08-16): the D33 advisor whole-diff review serves as this sprint's integration gate — a repo-wide move's seams ARE the cumulative diff, which is exactly that review's scope (with the ratified grep rider), and the end-to-end flows were already run independently three times (T4 bundle; T3 reviewer's smoke re-run; T2 reviewer's CI-log verification). A separate integrator spawn would duplicate all of it.
- verdict: **PASS** (advisor whole-diff, 2026-08-16) — 133 renames, 3 adds, 9 modifies; zero unnamed residue; binding grep sweep over all unmoved trees CLEAN (`.planning/` `src/` literals correctly preserved as historical record). Non-blocking notes: workspace member kept package name `obstack` (rename to `web` when S2.4 creates packages/*); root `lint`/`start` delegators dropped (outside C2, not in CI, accepted).

## Retro
- SPRINT CLOSED 2026-08-16: exit SIGNED + GOAL MET; PR #10 merged @ `8e21ef3`; branch deleted, worktrees pruned. **Post-merge user-side actions ALL RESOLVED (2026-08-16): Vercel Root Directory → `apps/web` (flipped via API, verified); `.env.local` moved to `apps/web/.env.local` by the user (verified in place, root copy gone, still ignored); branch protection SKIPPED by user decision — checks advisory, merges gated by the run process (README updated).** Run stats: 4/4 tasks approved (T1 must-fix→resolved, T2 approve after one advisor re-ruling, T3 fixed, T4 must-fix→resolved); advisor ruled 7 times mid-run (contract-test ownership, A1 amended twice — ending back at root cwd, layers.test.ts ratified, /out/, symmetry rider, agent-file notes); reviewer catches: 18-package lockfile float redone at +11/−0, broken docstring command proven-by-running, Go-side contract test (the vocabulary guard doing its job), root-delegator CI coverage gap. Two transient agent API deaths, both resumed without loss.

## Goal check
<!-- Compares delivered work against the goal line at the top of this file. -->
- iteration 0: pending

## Retro
- (filled at end of run)
