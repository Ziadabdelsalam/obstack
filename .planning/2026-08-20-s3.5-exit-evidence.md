# S3.5 Exit Evidence — Explain + the M3 honesty pass

meta: sprint S3.5 · milestone M3 · exit-blocking per D104 · branch `s3.5-explain-honesty` @ `2443922` · PR #20, eight checks green · e2e runner number of record: **5m47s** · rulings of record: **D223–D249** (`.planning/2026-08-20-s3.5-team-plan.md`)

## 1. Exit criterion, clause by clause (verbatim source: phase plan §3 S3.C5)

1. **The `explain` server function assembles a failed trace's spans + correlated logs through the existing query layer and returns the structured summary.** MET. Route `apps/web/src/app/app/traces/[id]/explain/route.ts` reads the trace exclusively through the D96 scoped facade (`dataForSessionContext(session).getTrace`), order of record D240 verbatim; output validated server-side into `Explanation` (D223 shape); one wire frame, one definition (`server/explain/contract.ts`, D227). Proven three ways: 434/434 tests with live DSN (0 skips — D225 atomicity ran real), the drive's Explain step (166/0), and the §2 real-provider evidence run.
2. **The Explain control appears on failed live traces.** MET. Gate is `hasFailure && (explain.live || trace.explanation)` (`TraceExplorer.tsx:172`, D224); the adapter never assigns `explanation` and `adapters.test.ts` pins are byte-untouched vs master (integrator diff check). The RAF fake-stream is DELETED (D102/D230); mock mode renders the three prepared stories statically through the same panel.
3. **Cloud path uses Claude via API; self-hosted takes a configurable model/endpoint (PRD §6.7).** MET. `@anthropic-ai/sdk` behind `OBSTACK_EXPLAIN_MODE` (default `fake`, the D168 pattern); cloud `ANTHROPIC_API_KEY`, self-hosted `OBSTACK_EXPLAIN_BASE_URL`/`OBSTACK_EXPLAIN_MODEL`/`OBSTACK_EXPLAIN_API_KEY` (D102 — multi-provider abstraction refused and not built). Cloud leg evidenced by the §2 real run on `claude-sonnet-4-5-20250929`.
4. **Runs are rate-limited per plan; the hardcoded `:111` line is gone.** MET. `plans.explain_quota` + `explain_runs` in migration 0006 (free=20/pro=200 — D226, catalog of record); enforcement is the D225 single atomic statement; ONE definition (`server/explain/quota.ts`) feeds panel counter, settings meter and route enforcement — the drive asserts panel == meter end-to-end; only hand-written landing copy restates 20/200, inside the D226 fence. The over-quota refusal was walked ON PURPOSE in the drive and read by the live `:2430` predicate on an authenticated path — the drive-level D206 proof.
5. **With no model configured the surface says so rather than fabricating.** MET. Typed `not-configured` refusal, checked BEFORE any spend (D240); wording pinned D206-safe by `explain-wording.test.ts` (mirror asserts the drive still carries the predicate, character for character).
6. **Content items closed:** CommandPalette mock trace links off the live path (D60/D63); "live tail" sweep done — the guard was made non-self-satisfying (D246, red-proven); S1 shell-chrome M3-gate closed — TopBar honest, dead switcher replaced by the honest static label, D134 demo footer on every mock surface (D228). Honesty sweep of record: no surface a signed-up stranger reaches renders unlabelled sample content; D106 landing fence held at exactly 4 hunks; all four honesty guards red-proven by mutation and restored byte-exact. Repo-wide PII removal (D235/D239) — real-person literals survive only inside the two mirrored ban lists.

## 2. The evidence run (U6 — the one real-key run)

Protocol held: `down -v` first (environment-ordering note, §5), key via env at runtime only. Real signup → in-product `ok_live_` key → OTLP failing trace `7d807f489c68184ff2f29ec048e258dc` in `ws_4d249e8eba9ebc8b` → POST explain: HTTP 200, `application/x-ndjson`, `no-store`, 9 deltas + exactly one terminal `result`, wall 8.18s, model `claude-sonnet-4-5-20250929` against Anthropic's endpoint. All 3 evidence ids resolve to the trace's own ids (spans `…0003` payments.charge + `…0001` root; log `<traceid>-1` ERROR record); zero dropped — warn+count line correctly absent. Genuine-reading check: the answer fused the child span's "connection reset after 750ms" with the log's "0 idle of 32" into pool exhaustion — not producible by the fake. Drop path deliberately unexercised here; its evidence is the deterministic rail's. Artifacts: `.planning/2026-08-20-s3.5-explain-evidence-transcript.ndjson` (verbatim) + `-notes.md`. CI never spent (U6): the drive ran fake mode throughout; the fake carries no real credential.

## 3. Budget (D137 watch — DISCHARGED)

Runner number of record **5m47s** ≤ 6m at the effective tip, with the Explain step riding the metering propagation wait (absolute-deadline sleep; ~3–4s of the 33s local window). **D207 reclaim NOT consumed — still available and pre-registered as the first response to future growth.** Local drive 129s mid-band; 166 passed / 0 failed.

## 4. Hygiene and security

`sk-ant` zero hits across every artifact and the repo; the key never entered git, logs or CI (U6/D132). App log holds zero model content — the D248 invariant (**model-supplied content never enters the server log at any level; the panel is where untrusted content is shown, stated as untrusted**) held under the real provider. `EXPLAIN_CREDENTIALS` deleted from served appEnv, swept, non-fake mode refused in the drive. Mock POST to the explain route 404s + tripwire; D150/D152 standing test enumerates the new route.

## 5. Residue pre-registered, not absorbed

- **M4**: D103(a) M4-PLANNING items (JS logging bridge, streaming instrumentation, Responses API); D105 M4-GATE retention TTL + disclaimer removal; D101 `ai@7` owner; D218 windowed-rate revisit; Vercel + CloudWatch receivers (SOON cards flip); D214 K8s external connect; environment-ordering test class — the drive's D172 overrides mutate shared compose Postgres, a DSN-backed suite run AFTER a drive shows 4 pre-existing-class failures, `down -v` restores 434/434 (operational line: `down -v` before DSN-backed suites in any evidence sequence).
- **S5-GATE**: Polar sandbox→production flip; D23 dup-exposure revisit (D103(b)); public reachability of `/signup` (U1=LOCAL); D200 list unchanged.
- **Standing, reopen-on-measurement only**: D243 per-view quota read (D109 pattern); D109 skip-index closure.
- **Pattern additions this sprint**: D225 single-statement spend (no TxQuery where no lock exists); D241 no-allowlist-rider principle (the fake's determinism makes an absent error line a free drive guarantee); D248 log/untrusted-content invariant.

> **Advisor signature — S3.5 exit bundle.** Signed on the record above: all six exit clauses evidence-walked; goal check D249 MET clause-by-clause with 0/3 fix rounds consumed; critical items D233.1–5 discharged on the cited reviews and the §2 evidence run; the D137 budget watch discharged at 5m47s with D207 unconsumed. **Effective at:** branch tip `2443922` plus the planning commit recording rulings D223–D249 and this bundle, eight checks green on PR #20 (confirmed). Rulings of record: D223–D249. — advisor, 2026-08-20
