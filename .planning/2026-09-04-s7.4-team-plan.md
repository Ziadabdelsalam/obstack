# Team Plan — obstack M6 sprint 4 (S7.4): incidents for real — objects CRUD in Postgres, a timeline stitched at read time over the workspace's own alerts, changes and error traces, RCA on the one Explain rail, the net-new list route and the detail view flipped; mock mode byte-identical

meta:
- date: 2026-09-04
- repo/branch: `s7-4` (cut from master @ `c8c262f`, the RULED packet commit)
- mode: **Opus-direct, session-wide** — no subagent spawns for execution; every task T1–T8 is executed in the main session, sequentially in wave order. The per-task `model:` lines record what a routing policy WOULD have chosen, for the retro's calibration only (the S7.3 precedent, which found that all four corrections that mattered came from reading the tree or running the thing, none from reading the plan).
- iteration-limit: 3
- iteration: 0/3
- governing docs (binding, read before executing): `.planning/2026-09-01-s7-m6-ops-suite-phase-plan.md` §5 S7.4 + §7; `.planning/2026-09-04-s7.4-incidents-packet.md` (RULED — implemented VERBATIM, D519–D566; deviations escalate, never smoothed).

## Kickoff notes

- Packet ruled 2026-09-04. The four calls that change what gets built were put individually and ruled as drafted: the mock stays frozen (a), incidents are never swept (c), three timeline legs (e), the RCA is not stored (l). The other nine ratified as drafted. Seams surfaced by the breakdown are numbered from **D567**.
- **§5 (RCA) carries a weaker evidence standard than the rest** — drafted against the tree, not adversarially reviewed. T5 is therefore the task most likely to surface a plan defect, and the A-rule binds hardest there: a defect found mid-task is corrected in the plan and recorded, never smoothed.
- Lessons carried in as plan items: the drive runs in the FOREGROUND, full stop (three occurrences of a harness kill at the build step across S7.2/S7.3); an arm's first green run is not evidence its fixture reads the world right, and the settle condition IS the assertion; a worked example in a packet is a claim to be COMPUTED, not typed (T1, T4 and T8 each recompute theirs before it becomes a pin); the compose `plans` drift has TWO columns and T8 is where it is finally paid off, so the local red on four usage/quota tests before T8 lands is expected and is not an S7.4 failure.
- Sequencing note: there is **no Go product change in this sprint**. `0013` needs no Go registration (the embed is `//go:embed *.sql`), D526 rejected the `alert_events.incident_id` direction so the deliverer is untouched, and retention's `pgDeletes` is unchanged. T1's Go work is three test files and one pinned string.

## Critical items (advisor-return before merge)
- T1 — the DDL and its FK action: the retention non-wedge proven on real Postgres (GREEN and the 23503 RED both recorded), and the double-apply proven raw AND through the runner.
- T3 — the promotion path: Path C's red run (the scoped SELECT defanged, with the FK still satisfied and Postgres raising nothing) run and SEEN, not predicted.
- T4 — the timeline's honesty states: locatable per-leg truncation, the clip note that survives non-empty results, and the zero-read `outsideRetention` wording that never claims a deletion.
- T5 — the Explain seam and the route order: the widening lands with `fake.ts` answering for BOTH subjects, and D225's order proven by counter readings against real Postgres, never by a grep on the route source.
- T6 — the flip: the sha pin taken against the pre-flip commit, the registry pair, BOTH unwired controls moved, the D440 tenancy proof on the store and at the surface.
- T8 — the exit evidence: the drive's incidents arm with its proven-red run, and the independent recomputation matching the card.

## Tasks

### T1: Postgres DDL `0013_incidents.sql`, the migration pin, and the two retention proofs
- status: done (workflow: 3 drafts → gates → 2 adversarial reviews; see result)
- wave: 1 · depends: — · model: Sonnet (DDL in a stated house style; the two Go tests are in named precedents' shapes)
- owns: `services/ingest/pgmigrations/0013_incidents.sql` (new), `services/ingest/internal/pgmigrate/qa_a6_migrations_test.go` (the applied-set literal), `services/ingest/internal/retention/retention_test.go` (a NEW `TestIncidentsAreDeliberatelyNotSwept`, its OWN function), `services/ingest/internal/retention/pg_integration_test.go` (a NEW `TestSweepDeletingAPromotedAlertEventDoesNotWedge`)
- goal: packet D524/D526/D528 VERBATIM. `incidents` with `id TEXT PRIMARY KEY` (`inc_`), workspace FK CASCADE, `title` CHECKed 1–200, `status` CHECK IN ('ongoing','resolved') DEFAULT 'ongoing', `severity` CHECK IN ('critical','warning','info'), `origin` CHECK IN ('manual','alert') DEFAULT 'manual', `impact` ≤500, `summary` ≤2000, `started_at` NOT NULL, `ended_at` NULL, `opened_from_event_id` NULL REFERENCES alert_events ON DELETE **SET NULL**, `created_at`/`updated_at`; table CHECKs `((status='resolved') = (ended_at IS NOT NULL))` and `(ended_at IS NULL OR ended_at >= started_at)`. **NO `rca`/`rca_generated_at` columns (D555), NO `UNIQUE (workspace_id, title)` — both absences stated in the DDL comment with their reason.** Indexes: `(workspace_id, started_at DESC)`, and a partial UNIQUE on `(opened_from_event_id) WHERE opened_from_event_id IS NOT NULL` — **single-column, and the comment says why**: it is also the FK's own SET NULL lookup, and a `workspace_id`-leading index cannot serve it. No `DO $$ … pg_constraint $$` guard is needed or present (0013 adds no constraint to an existing table). Pin gains `,0013_incidents`.
- done-check: `psql -f 0013_incidents.sql` twice against compose — pass 1 creates, pass 2 prints exactly ONE table NOTICE and TWO index NOTICEs (the PK's implicit index is inside the skipped table and emits nothing — **compute this, do not assume it**); `go test ./internal/pgmigrate/ -count=1` green (fresh schema + the re-apply no-op + the pin), RED first by editing the pin before the file exists. `TestIncidentsAreDeliberatelyNotSwept` in its OWN function (`retention_test.go:415`'s `t.Fatalf` makes an assertion added to `TestPostgresStatementsCarryTheirRules` unreachable — verify that by adding a third `pgDeletes` entry locally and confirming the new assertion never runs); non-vacuity proven by adding an `incidents` entry and seeing the new test red, then removing it. `TestSweepDeletingAPromotedAlertEventDoesNotWedge` in `pg_integration_test.go` beside the existing real-Postgres leg (NOT in `retention_test.go`, whose `pgDeletes` tests are stub-closure seam tests with no database): GREEN = the sweep's own statement runs, the event is gone, the incident survives with `opened_from_event_id IS NULL`; RED = the same statement against a scratch schema whose FK is declared NO ACTION → `23503`, both outputs recorded. `go build ./... && go vet ./...` clean.
- result: **done**, four files, exactly T1's `owns` — `0013_incidents.sql` (128 lines, 82 of prose header), the pin, `TestIncidentsAreDeliberatelyNotSwept`, `TestSweepDeletingAPromotedAlertEventDoesNotWedge`. `retention.go` byte-unchanged (`git diff --exit-code` = 0), as D528 requires. **Gates:** `gofmt -l` clean, `go build`/`go vet` clean, `pgmigrate` 13/13 with the pin PASS (not skip), `retention` 15/15, full module green **serially**. **The idempotency claim was COMPUTED, not typed** (the S7.3 lesson applied): on a fresh scratch schema pass 1 = 0 NOTICEs / 3 objects and pass 2 = exactly 3 NOTICEs — one table, two indexes — with `incidents_pkey` confirmed present and silent, so D524's prediction about the PK's implicit index is measured true. **Both REDs observed**, and one of them proved its own ruling's premise: with a third `pgDeletes` entry present, `TestPostgresStatementsCarryTheirRules` reported ONLY its line-416 `t.Fatalf` and its per-statement loop never ran — so an assertion added inside it would indeed have been unreachable in exactly the case it exists to catch, which is why D528(i) demanded a separate function. The non-wedge test runs the sweep's own statement looked up from `pgDeletes` (not retyped), wedges TWICE (the wedge is the retry, not the first failure), and its 23503 is corroborated in the Postgres server log against the real ctid batch.

  **PLAN CORRECTION (A-rule, recorded) — D524's index rationale was right in conclusion and WRONG in mechanism.** The packet says a `(workspace_id, …)` leading index "cannot serve `WHERE opened_from_event_id = $1`" and that retention "would seq-scan". Measured on 50k rows, both halves are false as stated: Postgres DOES use such an index, as an Index Only Scan applying the predicate as a NON-BOUNDARY cond — it walks the whole index rather than seeking. The ruling (single-column) stands and is now quantified: 194 buffers per lookup against the shipped index's 3, paid once per row of a 5000-row batch. The DDL comment carries the corrected mechanism rather than the packet's imprecise sentence. **Second-order correction, from the review:** the 194 figure only reproduces under `SET enable_seqscan = off`; left free the planner ignores that index entirely and seq-scans at 358 buffers — so the comment now states the condition, and notes the real cost is worse than the forced figure, not better. This is the packet's own discipline turned on the packet: a number in a file a later reader will trust has to be reproducible from what the file says.

  **Four review nits fixed before commit:** the absence guard was a bare `strings.Contains(d.sql, "incidents")` where the precedent it cites is word-boundary-guarded — narrowed, and its non-vacuity re-proven independently (fires on the real sweep shape, not on `incidents_archive` or an `incidents_count` column), since narrowing an assertion invalidates the reviewer's proof of the old one; the D525 paragraph gained its decision id; the buffer measurement gained its condition; and the non-wedge test now re-reads the pointer after the wedged attempts, so its final `IS NULL` assertion cannot pass for the wrong reason via a rolled-back SET NULL.

  **Two documented departures from §7.2's worded example, substantively equivalent, recorded rather than smoothed:** the GREEN seeds a 3-day-old event and shortens `retention_days` to 1 (the sibling test's own idiom, and it reads the window back through `s.plans`) rather than a 30-day-old event; and the RED uses a second probe TABLE in the same schema rather than a whole scratch schema, which holds schema/workspace/event/statement fixed and varies only the FK action — the 23503 names the probe's own constraint, so attribution survives.

  **⚠ Finding, not cleared, not caused by T1 — a latent cross-package race on the shared dev ClickHouse.** `TestSweepEnforcesRetentionOnMetricsRollupsAndSeries` (a metrics test T1's diff does not touch) fails under the FULL parallel module run with `code: 62, Cannot parse expression of type Map(LowCardinality(String), String) … While executing WaitForAsyncInsert`. Evidence, all measured: with T1, full parallel red 2/2 but `-p 1` serial **green**, retention alone green 2/2, and green against every pair I bisected (migrate 3/3, cmd/ingest 2/2, write+alerting+mapping 2/2); on master, full parallel green 3/3. So it needs the whole 16-package parallel set — a concurrency threshold, not a specific pair — and T1's only contribution is ~1s of extra Postgres work in the retention package shifting when the metrics seed lands. The error surfacing at `WaitForAsyncInsert` points at ClickHouse async-insert squashing under concurrent load, i.e. a pre-existing environmental fragility of the shared compose ClickHouse that T1's timing exposes. **Not cleared and not called a flake**: it is carried to integration as a watch item, and if it appears in CI (fresh per-job containers, so it may not) it is diagnosed there rather than papered over here.


### T2: the client-safe contract — `lib/incident-types.ts`
- status: done (workflow: draft → gates → 2 adversarial lenses; see result)
- wave: 2 · depends: T1 (the vocabulary pin reads the DDL) · model: Sonnet (a contract module in a stated shape; the formatters carry the arithmetic)
- owns: `apps/web/src/lib/incident-types.ts` (new, ZERO imports), `apps/web/src/lib/incident-types.test.ts` (new)
- goal: packet §0 + D541 VERBATIM. The three vocabularies with their runtime lists; `IncidentTimelineKind = "alert"|"change"|"trace"|"resolved"` with `INCIDENT_TIMELINE_KINDS`; `IncidentRow`, `IncidentTimelineEntry` (carrying `until` and `link.external`), `IncidentTimeline`, `IncidentListPage`, `IncidentSubject`/`IncidentTimelineRow` (the shape T5's prompt reads), and **`PromotableAlertEvent` HERE, not in the store** — its `severity` is `IncidentSeverity`, because a client component cannot import a `server-only` module for a type alone (S1.5/D10) and the fences catch `import type`. The three formatters with `nowMs` REQUIRED and defaultless, clamped at zero, and `formatIncidentWindow` printing the end's full date when it differs from the start's (a window over midnight must not read as going backwards).
- done-check: `assert.equal(/^import /m.test(source), false)` and never `server-only` (the `change-types.test.ts` pin), RED by adding `import type { AlertSeverity } from "./alert-types"`. The severity/status/origin lists deep-equal the vocabularies **parsed out of `0013_incidents.sql`'s CHECK clauses** — the DDL is the single authority, the cross-source idiom, never a restated literal. `INCIDENT_TIMELINE_KINDS` deep-equal with an explicit assertion that `"k8s"` and `"pipeline"` are absent, naming the S4.4 R3 fence and D533. Formatter cases COMPUTED, not typed, including the midnight-crossing window and the zero clamp; RED by giving `nowMs` a `= Date.now()` default. `tsc` + eslint clean.
- result: **done**, two new files, nothing else touched. **Gates:** `tsc` clean, eslint clean, the new file 10/10, full web suite **1018/1018 with `skipped 0`** — the S7.3 baseline of 1008 plus exactly T2's ten, so no pre-existing test was lost or renamed. Both required REDs run and observed: `import type { AlertSeverity }` reds the client-safety pin (test 9), and `nowMs = Date.now()` reds the ongoing-window test on `Function.prototype.length` (3→2). Two further REDs the executor added: narrowing `INCIDENT_SEVERITIES` to the mock's two reds the DDL pin with the expected side read out of `0013`, and pointing the test's DDL constant at `0012_slos.sql` fails on the guard rather than comparing against an empty set.

  **PLAN CORRECTION (A-rule, recorded) — my task brief under-specified `IncidentTimelineRow`.** The brief enumerated five fields (`kind, id, at, title, detail`); RULED D554's "What IS sent" names **seven** — those plus `service` and `severity`. The packet is the higher authority and the shipped type carries all seven. The brief was mine, not the packet's; recorded because a five-field row would have silently starved T5's prompt of two fields D554 requires it to send.

  **D568 (amends §0's wording, not its rule):** §0 says "`nowMs` REQUIRED and defaultless" of all THREE formatters. `formatIncidentClock` renders an ABSOLUTE instant, so a `nowMs` it never reads would be a discipline that pins nothing — it takes only the ISO string, and its arity is now asserted at ONE so a later editor cannot add a defaulted clock to it. The rule binds the two formatters that take a clock.

  **Three review findings fixed, two of which were assertions that could not fail.** (1) The "alert vocabulary verbatim" pin used a non-global `String.match` against `0010_alerts.sql`, which declares `severity` TWICE — so it pinned `alert_rules` (line 42) while D525 names `alert_events` (line 76) as the authority. The reviewer falsified it: narrowing `alert_events.severity` alone left the test GREEN, i.e. the exact drift D525 exists to catch was invisible. Now `matchAll` captures both declarations, asserts there are exactly two, and requires both to agree with ours — and I re-ran the falsification to confirm it now reds. (2) The behavioural half of the `nowMs` pin passed **because one side was garbage**: with the clock withheld the formatter returned `…· NaNd NaNh so far`, so `notEqual` was satisfied without ever asserting the answer was honest. It now also asserts the withheld answer contains no `NaN` and does render the absent instant. (3) Four carry/boundary expectations (`${1}h ${0}m`, `${1}d ${0}h`, `${23}h ${0}m`, `${4}m`) were TYPED in template syntax that signalled a computation that was not happening — exactly the cases the "computed not typed" rule exists for. All four now derive from the same component counts or from the two instants.

  **A real D13 defect found by the arithmetic lens and fixed.** `Math.max(0, NaN)` is NaN, and NaN fails every `<`, so an unparseable instant fell THROUGH the unit ladder into the days bucket and rendered `NaNd NaNh`. Worse — and this is the half that matters — `new Date(null)` is not NaN but the **EPOCH**, so an absent instant rendered `1970-01-01 00:00:00 UTC`: a fully plausible timestamp no reader could distinguish from a real one. The guard is `instantOf()`, which checks the INPUT TYPE first (the load-bearing half; the finite check alone does not catch the epoch case), and every formatter now renders `—` — the register `SlosLive` already uses for a measurement that does not exist. The RED was observed on the way in: the new test failed with the literal `1970-01-01 00:00:00 UTC` before the input guard existed. Sized honestly and recorded: this is the house-wide precedent (`timeAgo("not-an-iso", 0)` → `"NaNh NaNm ago"`, `fmtClock(null)` → `"00:00:00"`), the live path is closed by `tsc` and by the stores' `toISOString()`, and the frozen mock cannot reach these formatters without breaking the D519 sha pin first — so this is a guard against a runtime edge, not a live bug.

  **Two derived helpers recorded rather than smuggled:** `IncidentTimelineLeg = Exclude<IncidentTimelineKind, "resolved">` and `IncidentTimelineLink`. Neither is in §0's enumeration; both are derived from it rather than restating it, which is what stops `omissions[].leg` and D554's three lanes drifting from the four kinds. **Known idiom limit, not a T2 defect:** the `/^import /m` pin does not catch an indented import, `export … from`, or a dynamic `import()` — it is `change-types.test.ts`'s house pin copied verbatim, and T3+ inherit it.


### T3: the store — `server/incidents.ts`, CRUD, promotion, tenancy
- status: done (workflow: draft → gates → packet-fidelity review + a tenancy ATTACK; see result)
- wave: 3 · depends: T1, T2 · model: Opus (the promotion is the one place tenancy can leak past a green FK)  <!-- plan correction: T3 depends on T2, so it cannot share T2's wave; the later waves renumber with it -->
- owns: `apps/web/src/server/incidents.ts` (new), `apps/web/src/server/incidents.test.ts` (new, hermetic recorder), `apps/web/src/server/incidents.integration.test.ts` (new, compose Postgres)
- goal: packet D542–D545, D527, D529 VERBATIM. `IncidentRefusal`; `NO_SUCH_INCIDENT`/`NO_SUCH_ALERT_EVENT` in the D440 shape; `newIncidentId` = `inc_<16 hex>`; shape checks BEFORE `lockWorkspace` (D430) including `checkedInstant` bounded against the SERVER clock at `INSTANT_SKEW = 5 minutes`; `MAX_INCIDENTS_PER_WORKSPACE = 500` counted under the lock before the INSERT on **both** insert paths; `listIncidents` with **no limit and no cursor**, `ORDER BY i.started_at DESC, i.id DESC`, carrying `(count(*) FILTER (WHERE i.status = 'ongoing'))::int`; `getIncident` returning `null` not a throw; `listPromotableAlertEvents`; the six mutations, with `resolveIncident` defaulting `ended_at` to the SERVER's `now()` and `deleteIncident` returning **void**. `promoteAlertEvent` in the pinned order: shape → lock → scoped SELECT → refuse → already-promoted returns the ORIGINAL → cap → INSERT with derived values (`title` clipped at `MAX_INCIDENT_TITLE = 200` with a stated marker, `severity` identity-mapped, `summary` = the event's detail, `impact = ''`, `started_at` = the event's `created_at`, `origin = 'alert'`) → read back. **No `refusingDuplicateName`, no `23505` string anywhere in the module** — stated in the docblock.
- done-check: hermetic — every statement matches `/workspace_id|pg_advisory_xact_lock/` with `params[0]` bound, every mutation's FIRST statement is the lock, a shape refusal issues ZERO statements, the promotion's statement ORDER pinned on `seen.map(s => s.sql)`, the SET lists pinned (`UPDATE` names neither `status` nor `ended_at` nor `opened_from_event_id`), and the module source matches no `/INSERT INTO alert_events|UPDATE alert_events|DELETE FROM alert_events/`. Integration against compose Postgres — disjoint lists, the D440 sentences for BOTH an id that never existed and another tenant's, `promoteAlertEvent(ws_b, aliceEventId)` refused with `incidents` in ws_b left EMPTY (the leak proof), double promotion returning one row, a promoted incident's derived values equal to the event's read back RAW, a 300-char event title promoting to a 200-char incident title, the two table CHECKs refusing raw INSERTs, and `DELETE FROM alert_events` on a promoted event leaving `opened_from_event_id IS NULL`. Tenancy RED per path (D549) — Paths A, B, C, D, E each defanged in turn and the ACTUAL red count RECORDED, never predicted; **Path C must be run and seen**. Full web suite green with the DSN; tsc + eslint clean.
- result: **done**, three new files. **Gates:** tsc clean, eslint clean (CI form), hermetic **27/27**, integration **11/11 against live Postgres — ran, did not skip**, full web suite **1056/1056 with `skipped 0`**, reconciling as 1018 (T2) + 27 + 11. The gate runner measured the 1018 baseline rather than taking it on trust. The DSN guard is proven non-vacuous in all three states the house rule names: a dead port FAILS loudly (10 red), an unmigrated database FAILS, and only an UNSET variable skips. The T1 carry is discharged — the guard test is titled "…it holds the **0013_incidents** migration", the `slos.integration.test.ts:61` idiom.

  **D549 tenancy RED per path — every path defanged, run and SEEN, counts measured never predicted** (each on a clean migrated database, module restored and diffed between runs): A list read 1 red, B detail read 3, **C the promotion's scoped SELECT 3 — including the integration leak proof failing with `Missing expected rejection`, i.e. with the scope gone bob's promotion of alice's event SUCCEEDS and Postgres raises nothing**, D picker 2 (after the fix below), E1–E3 1 each, E4 delete 2, E5 cap 2. **Honest asymmetry recorded rather than smoothed:** E1/E2/E3 red only in the hermetic file, because `updateIncident`/`resolveIncident`/`reopenIncident` each read through the still-scoped `GET_INCIDENT_SQL` first — so the cross-tenant case is already refused by Path B and their own predicate is defence in depth. `deleteIncident` does not pre-read, so E4 reds at the rows level too.

  **The tenancy floor HOLDS under attack.** A reviewer spent the phase trying to get workspace A's data into B rather than reading for correctness: 19 hostile id shapes (SQL metacharacters, a `UNION SELECT`, an object whose `toString` returns A's id, 100k characters, NUL), all five mutations as B against A's incident id with the row's fields **and `xmin` compared before and after** (unchanged — not even a same-value rewrite reached A's row), D440 byte-identity across all five entry points, and a four-way concurrent promotion race. Nothing leaked. The race is held by the **advisory lock, not the unique index** — proven by holding the lock in a side transaction and watching a concurrent promote block on `pg_locks`.

  **THREE defects found and fixed, two of them by both reviewers independently.**
  (1) **D526's summary clip was never implemented.** The ruling says "Title **and summary** are CLIPPED at the copy site"; only the title was. `alert_events.detail` has no length CHECK, `incidents.summary` CHECKs at 2000, and the gap is REACHABLE through the product — `event_text.go` composes detail from a rule name and every `k=v` filter pair, none of which the alert validator bounds (measured: a 2500-char filter value yields a 2625-char detail). Unclipped, a promotion raised a raw `23514` that is **not** an `IncidentRefusal`, so the action could not catch it by identity and the sprint's headline creation path answered 500 — exactly the failure the copy-site clip exists to prevent. Fixed symmetrically with `clippedEventSummary`, with an integration test proven non-vacuous by removing the clip.
  (2) **Path D's row-level tenancy proof was vacuous.** The picker's integration assertions ran under `withWorkspacePair(async (a) => …)` and never seeded an event in `b`, so a defanged picker had nothing to wrongly offer and stayed green while leaking. It appeared to red on the dev database only because that database held 26 unrelated `alert_events` rows — incidental, and gone in CI. Fixed by seeding a foreign event and asserting both its id and its TITLE are absent; re-verified by defanging, which now reds the integration file with the picker returning three events where one is expected.
  (3) **A NUL byte in any client-supplied id or text was a raw `22021` on every entry point** — `.trim()` does not strip `U+0000` — i.e. a client-reachable 500 past a module whose contract is that shape refusals are judged before Postgres and cost no statement (D430). Screened in `checkedEventId`; the case reds when the screen is removed.

  **Two PLAN CORRECTIONS applied to the packet** (both raised independently by both reviewers): **PC-1** D545's quoted fragment omitted `OVER ()`, and as written it is a plain aggregate that Postgres refuses against the row projection (`42803`) — the ruling always meant the window form, and the shipped code is correct. **PC-2** D549's defang recipe, taken literally as `$1 IS NOT NULL`, does not run: with `$1` appearing nowhere else Postgres cannot infer the parameter type and answers `42P18`, so the ritual reds for the wrong reason and **a genuinely leaking build would pass it**. The recipe is `$1::text IS NOT NULL`. Both corrections are in the packet with their evidence.

  **Latent, recorded, not reachable today:** the dedup read is workspace-scoped while the UNIQUE index backing it is global, so a manufactured cross-workspace row makes a workspace's own promotion raise the `23505` the module documents as impossible (unreachable — `promoteAlertEvent` is the column's only writer and is scoped); the promotion's `started_at` bypasses `checkedInstant` (unreachable — both Go producers let `created_at` DEFAULT `now()`); and the picker's limit guard lets `Infinity` through to a `22P02` (T5/T6 must not pass a client number).

  **Repo-wide mechanism finding, no file T3 owns:** node's test runner treats a positional path as a GLOB, so `src/app/app/traces/[id]/...` is read as a character class and the documented single-file idiom returns `# tests 0 # pass 0` — a silent green. CI's quoted-glob form runs them, so nothing is currently missed; worth a note beside the documented idiom.

### T4: the stitched timeline — the three legs, the merge, the honesty states
- status: done (workflow: 2 legs in parallel → stitch → gates → 4 adversarial lenses; see result)
- wave: 3 · depends: T2 · model: Opus (a new SQL shape across two stores, and the sprint's honesty core)
- owns: `apps/web/src/server/queries/incident-errors.ts` (new), `apps/web/src/server/incident-timeline.ts` (new), `apps/web/src/server/incident-timeline.test.ts` (new), `apps/web/src/server/queries/incident-errors.integration.test.ts` (new), `apps/web/src/server/alerts.ts` (+`listAlertEventsInWindow`), `apps/web/src/server/changes.ts` (+`listChangeEventsInWindow`, through `clampLimit`), `apps/web/src/server/changes.test.ts` (the statement-count pin 2→3), `apps/web/src/server/alerts.test.ts`, and the boundary rows in `alerts.integration.test.ts` / `changes.integration.test.ts`
- goal: packet D530–D540 VERBATIM. Three legs; the spans read with both bounds, `trace_id != ''`, `argMin`, `ORDER BY first_seen`, `LIMIT cap+1`, `{workspace_id:String}` literally present; half-open `[start, end)`; ONE clock sampled once in TS and bound to all three legs; the changes lead-in as its OWN `at DESC LIMIT LEAD_IN_CAP+1` read reversed in TS; per-leg locatable `omissions` from the probe row; the `(at, sourceRank, key)` total order; `effectiveStart` clipping the INPUT; the three render states with the clip note in the SHIPPED `· ${retentionDays}d retained on ${planName}` register and `outsideRetention` issuing zero reads and wording itself as a bound on the read. **Never a date, never "the evidence was deleted".**
- done-check: hermetic with a `recordingQuery` and a fake `ScopedClickHouse` — all three legs bind the workspace they were handed and no other, and the CH fake asserts the literal `{workspace_id:` and that no caller-supplied `workspace_id` is passed. HALF-OPEN proven red against a `BETWEEN` implementation on the boundary row, and proven again against REAL Postgres in the two integration files (an event stamped exactly at `end` excluded, one at `start` included). ONE CLOCK proven red by stubbing `Date.now` to a counter and asserting it advanced exactly once with all three upper bounds byte-equal. TOTAL ORDER proven by building the merged array in a DIFFERENT concat order and asserting the identical rendered sequence (**not** by claiming `Array.prototype.sort` is unstable — it is stable, and a pin justified by a false fact is the S7.3 lesson). LEAD-IN CANNOT EAT THE CAP: 60 changes in the lead-in hour and 3 in the window renders all 3 in-window. OMISSIONS LOCATABLE: 51 rows from one leg yields that leg's `omittedAfterIso`, not a bare boolean. CLIP NOTE SURVIVES ROWS — the assertion that matters, since an implementation setting the note only when empty passes everything else. `outsideRetention` issues ZERO statements and the CH fake is never called. ClickHouse integration against the pinned engine: the two-sided predicate includes the row AT `since` and excludes the row AT `until`, a `trace_id = ''` error span is excluded, and `example_trace_id` is the EARLIER of two (argMin, not argMax).
- result: **done**, ten files (four new, six edited), all inside T4's `owns`. **Gates:** tsc clean, eslint clean, full web suite **1107/1107 with `skipped 0`** — 1103 after the legs and the stitch, plus the four correction tests below. The gate runner reconciled the 1103 by diffing test DECLARATIONS across the four edited test files and finding no removed `test(` at all, so the change is purely additive and nothing was renamed or lost. The ClickHouse leg is proven on the wire, not by a skip count: `system.query_log` shows the shipped statement executing three times as `obstack_web` against the real 26.3.17.110 engine, plus one run of the same statement with its tenancy predicate stripped reading 10,819 rows across the whole table — the D549-shaped defang firing against live data. The trace leg's partition prune is measured, not asserted: `EXPLAIN ESTIMATE` gives `parts 1 / rows 8192` bounded versus `parts 2 / rows 16369` unbounded. Seven mutation REDs were run on the ClickHouse leg, each counted; one of them first reported 12/0 because the mutation had landed in a header COMMENT rather than the SQL — re-anchored and it red, and the executor recorded that a green mutation run is exactly what a mutation test looks like when the mutation did not happen.

  **FOUR honesty defects found by the review phase and FIXED, two of them reproduced independently by two reviewers.**
  (1) **D571 — `outsideRetention` was asserted for two windows that are not outside retention.** The guard conflated "the floor swallowed the window" with "the window has no width" and "the window runs backwards". A zero-width incident (legal: `0013` CHECKs `ended_at >= started_at`) that ended an hour ago, and a future-started ongoing one (legal: D527's five-minute skew), were both told their window predates a 7-day floor — the second under a window whose END preceded its START by three minutes. Both live-proven. The guard is now the floor alone, and a degenerate window reads zero-width at the incident's own start rather than inverted.
  (2) **D572 — the trace leg floored both bounds to whole seconds and drew the wrong rows.** Measured against seeded ClickHouse: a span 500 ms BEFORE the window rendered as the incident's first row while the two Postgres legs correctly excluded rows in the same 500 ms; and a span 300 ms inside incident A was rendered by its NEIGHBOUR B, 700 ms before B's own stated start — one span, two incidents, and the one that owned it showed nothing. It also asked below the retention floor by up to 999 ms, against D540's own structural claim. The bound was expressible all along: `fromUnixTimestamp64Milli`, the form `traces.ts` already binds.
  (3) **D573 — the lead-in band read a probe row and threw it away.** A workspace with 40 changes in the hour before its incident and one with 20 returned `JSON.stringify`-identical objects, so the surface could only render a count of the READ as a count of the hour — D536's own stated failure reappearing in the one leg the mechanism did not cover. The contract gained `omittedBeforeIso` + a `band` marker, because the band caps DESCENDING and an "after" instant on it would state the opposite of the truth.
  (4) **D574 — a band clipped by the floor rendered no clip note.** The surface derives the D507 register from `windowStartIso !== startedAt`, which is blind to a floor landing inside the lead-in BAND: every incident opened within an hour of the retention edge read ten minutes of a sixty-minute band and said nothing. `inputClipped` is now reported rather than derived, because deriving it needs a floor and a floor needs a second clock (D534).

  **Recorded, not fixed — the truncation register's precision.** `omittedAfterIso` names the last row KEPT, so rows dropped AT that instant make "after X is not shown" false; on the trace leg, whose `first_seen` is whole seconds by construction, groups routinely share the boundary instant, and a case was demonstrated where all 50 kept rows and the dropped one sit at one instant. `formatIncidentClock` renders seconds, so even an honest boundary can print the same string as the first dropped row. These are real and they are the surface's sentence to get right — handed to **T6** with the evidence rather than papered over here.

  **Verified clean under attack, stated so the report is falsifiable both ways:** the clip note survives non-empty results (on fakes AND live stores); no wording anywhere claims evidence was deleted, names a date, or asserts a past state; `outsideRetention` issues ZERO statements — proven with throwing fakes and with recording wrappers over real Postgres and ClickHouse, not by an empty result; an empty leg is distinguishable from one that went dark; the sweeper premise was checked against `retention.go` rather than trusted; two identical calls are byte-identical and five concurrent calls produce one distinct output; and every store-side read carries a total ORDER BY, so cap membership is never planner-dependent. Blast radius is zero: `changes.ts` stays reads-only with its pin green and no `actions.ts`, the alerts channel-delete sentence and feed projection pins are untouched, nothing reaches a client, the stitcher imports no store, and `WorkspaceData` is unextended.

### T5: the Explain seam, the RCA route, and the live panel
- status: done — reviewed under two adversarial lenses (D579 wire contract / panels; D580 prompt withholding at the route on real stores); see result
- wave: 3 · depends: T2, T4 · model: Opus (a widening of a rail that has shipped since M3, on the packet's least-reviewed section)
- owns: `apps/web/src/server/explain/types.ts`, `engine.ts`, `validate.ts`, `fake.ts`, `index.ts`, `quota.ts` (+`overQuotaDetail`), `subject.ts` (new), `incident-prompt.ts` (new), `response.ts` (new); `apps/web/src/lib/types.ts` (the evidence keys); `apps/web/src/app/app/traces/[id]/explain/route.ts` (the two lifts + the rename), `route.test.ts`, `explain-wording.test.ts`; `apps/web/src/app/app/incidents/[id]/rca/route.ts` (new); `apps/web/src/components/trace/ExplainPanel.tsx` (`runExplain` takes a url) + `ExplainPanel.test.tsx`; `apps/web/src/components/incidents/IncidentRcaPanel.tsx` (new) + its test; `apps/web/src/server/explain/explain.test.ts` (the incident half); `apps/web/src/server/explain/rca.integration.test.ts` (new); `apps/web/src/app/signup/mock-mode.test.ts` (the seventh surface); `apps/web/src/lib/docs/mirror.test.ts` + `content/docs/explain/index.mdx`
- goal: packet D550–D558 VERBATIM. `ExplainSubject`/`EvidenceReference`/`ReferenceIndex`; `explainTrace` RENAMED `explainSubject` (deleted, not wrapped); `parseExplanation(raw, refs)` with `droppedReferenceNote`'s default keeping the trace string byte-identical; the three citable id kinds with the incident's own id and metric rows excluded; `buildIncidentPrompt` with **three lanes** and the four exhaustive withholdings, sending each linked trace as ONE summary line and no span, log line or log body at all; the sibling route in D225's order with the stitch BEFORE the spend and `"no-evidence"` refusing free; the same `explain_runs` counter, no migration; mock mode a 404 + `[rca]` log line.
- done-check: RED first, in order — (1) change the provider's second argument with `fake.ts` untouched and let `tsc` state the seam moved; (2) a fake INCIDENT run through `explainSubject` landing on one result with linked evidence, asserted through the engine and never through a canned document, so a stubbed fake cannot make it green; (3) an id the subject does not hold dropped with the incident-flavoured note; (4) `droppedReferenceNote('"x"')` pinned character-for-character against `e2e-drive.mjs:2261`'s literal — the drive pin PROVEN not to have moved rather than assumed. The withholding proof with five sentinels (`llm.prompt`, `llm.completion`, the channel target, the change event's `key_id`, a log body) — RED against a `JSON.stringify(incident)` prompt. The lane-truncation lines against a 412-alert / 90-change / 44-trace fixture — RED under a flat cap. `rca.integration.test.ts` against real Postgres: the over-quota refusal on a 200 with `used` unmoved; the not-configured refusal with NO `explain_runs` row (RED if steps 4 and 5 are swapped); an empty timeline refusing `"no-evidence"` with no row (RED if step 6 sits after step 7); and a stitch stubbed to throw leaving no row — **the ordering ruling in one assertion**. `sweptPaths` gains the RCA route and the counter/RAF ban lands on `IncidentRcaPanel.tsx` asserting in the same test that `IncidentRca.tsx` DOES animate. `mirror.test.ts`'s `overQuotaDetail` path moved, shown red by moving the function before editing the path. Full Go module untouched; full web suite green; tsc + eslint clean.
- result: **done**, 27 paths (19 edited, 8 new), full web suite **1154/1154 with `skipped 0`** — 1107 + 44 from the seam/route/prompt plus the corrections below. §5 was the section that never had adversarial review at planning time; it got four lenses here and, as predicted, was refuted on something real by each of them.

  **Gates:** tsc clean, eslint clean (CI form), the trace Explain path proven END TO END through the real POST in fake mode (21 events, one terminal result, evidence resolving to span/log ids, `explain_runs` moving by exactly one) — the seam widening did not break the surface that shipped in M3. The drive's pinned string `"is not in this trace"` proven not to have moved: `explain.test.ts` reads the literal out of `e2e-drive.mjs` and asserts it against `droppedReferenceNote`'s trace output. `rca.integration.test.ts` (9 cases) ran against live Postgres AND ClickHouse; a dead port fails it loudly and only an unset DSN skips it.

  **The D225 order proven by COUNTER READINGS, not by a grep** — and then attacked: an over-quota workspace looped 20× left `used` AND `updated_at` unmoved; not-configured left NO row after the stitch ran; a throwing stitch (unreachable ClickHouse, a refused workspace id) left NO row; a provider dying mid-stream after the spend left exactly ONE row and a stream with NO terminal event (the D227 truncated shape); two concurrent POSTs on quota 1 spent exactly once; 25 on quota 1 spent once; GET/HEAD/PUT/DELETE/PATCH 405; mock mode 404 + `[rca]` with `getSessionContext` never called — and with the guard defeated it throws on the missing secret, which is what proves the guard is load-bearing. Another tenant's incident id and a never-existed id produced BYTE-IDENTICAL responses (status, headers, body, statements).

  **D577 CLOSED here, though it was T4's file.** `IncidentTimelineEntry` carries neither `severity` nor `service`, and the route was rebuilding RCA rows FROM the entries — so every alert reached the model with `severity: null` and it could not tell a critical alert from a warning. The stitcher now projects `rows: IncidentTimelineRow[]` from the SAME capped legs the entries came from (the reads that had those columns), the route's `timelineRowOf` is deleted, and case (vi) — which had deliberately pinned the honest ABSENCE so the fix would flip a test rather than arrive silently — flipped, and now asserts `"critical"` and `"checkout"` reach the subject.

  **Two spend defects found by the attack lens and fixed, each pinned with a sabotage-proven RED:** (vii) a NUL byte in the id segment reached Postgres as a raw 22021 — a 500 where an unknown id gets a 404. The route now refuses any id that is not `inc_` + 16 hex before the read, costing no statement (D440: an id that cannot exist answers as one that does not). (viii) **a run was SPENT on an incident whose window held nothing** if one change sat in the lead-in band: the emptiness test counted band rows, which are context (D535), as evidence, so the counter moved while `NO_EVIDENCE_DETAIL` was literally true. Evidence is now in-window rows only.

  **A false sentence in the CI path fixed:** `fakeIncidentDocument` said "the latest change before the window opened" while `find` on ascending rows picked the EARLIEST, and its tally counted band changes as sharing the window. Both corrected; the fake is the model with the provider removed and may not say untrue things.

  **D578 REVERSED by the withholding lens, and the reversal is right.** A first ruling let the allowlist follow the page's rows (up to 170) while the prompt describes ≤120; the review showed a cap-dropped id resolving to a working link the model never read — exactly the property D553 forbids. `cited ⇔ given` now holds both ways and is RED against an index built from the page's rows.

  **The withholdings proven at the ROUTE on the real stores** (D580): every column that could carry a withheld thing seeded with a sentinel — `notification_channels.target`, `change_events.key_id`, `spans.prompt`/`completion`/`attributes['llm.prompt']`, `logs.body`, another workspace's alert in the same minute — plus a 200-span error trace; every sentinel absent and the 200 spans ONE line. Stated so nobody reads it wider: the withholding is about what obstack SELECTS, not what the customer typed — a pasted webhook URL in a change's `detail` reaches the prompt exactly where the customer wrote it.

  **Recorded, not fixed — D581:** `NOT_CONFIGURED_DETAIL` says "nothing analysed **this trace**" on the RCA route. Wording only, no spend involved; `unavailable()` takes no subject, the constant is pinned to its file by `mirror.test.ts` and quoted into `/docs/explain`, and D557 rules that pin MUST NOT MOVE this sprint. A `subject` parameter on `unavailable()` with the trace default byte-identical is the pre-registered fix.

  **Ownership gap, recorded:** `server/explain/contract.ts` is edited (D558's `"no-evidence"` member lives there) but was absent from T5's `owns` line. **Process defect, mine:** two of the four REVIEWERS edited files and the plan concurrently — my brief told them to attack but did not say "edit nothing"; the spend-attack reviewer correctly did not. Their edits were sound (D579's lost trace-URL pin, D578's reversal) but the tree was a three-way merge that had to be re-verified from scratch. Review briefs from T6 on say it explicitly.

### T6: the flip — the pair of routes, six components, the registry, the guards
- status: pending
- wave: 4 · depends: T3, T4, T5 · model: Opus (the D431 shape doubled across a list and a net-new detail, with three control moves)
- owns: `apps/web/src/components/incidents/{IncidentsMock,IncidentsLive,IncidentDetailMock,IncidentDetailLive,IncidentEditor,IncidentNotFound,actions}.tsx|ts` (new), `apps/web/src/app/app/incidents/page.tsx`, `apps/web/src/app/app/incidents/[id]/page.tsx` (new), `apps/web/src/app/app/incidents/page.test.ts` (new), `apps/web/src/lib/live-routes.ts` + `live-routes.test.ts`, `apps/web/src/components/dash/OnboardingChecklist.test.tsx`, `apps/web/src/app/status/status.test.ts` (the NEW guard, D547)
- goal: packet D519–D523, D541–D549 VERBATIM. **Capture the sha pin from the PRE-FLIP commit BEFORE moving anything, and write the regeneration recipe against that commit, not `master`.** `IncidentsMock` = master's body with one line changed; `IncidentDetailMock` the params-promise ROUTER (fixture id → `IncidentsMock`, else `IncidentNotFound`), no sha pin and the test says why; the detail page's shape is `Promise.all([getIncident, getUsage])` → not-found → `await readIncidentTimeline` (**two serialised round trips — a single `Promise.all` is circular**), and a bogus or foreign id issues exactly the `getIncident` statement and ZERO telemetry; `IncidentsLive` with the derived `{ongoing} ongoing · {total} total` header and never `1 in the last 7 days`; `IncidentDetailLive` rendering the impact box only when non-empty, the timeline rail with `id={row.id}` anchors, and the two link shapes discriminated by `external`; the registry PAIR; the control at `live-routes.test.ts` → `/app/pipelines` and the checklist pair → `/app/oncall` + `/app/pipelines`.
- done-check: RED first on the pin, the branch-before-first-await guard on BOTH pages, the registry pair test, both control moves, and the new `/status` guard. `resolvedImports` fences per file — no `mock/` in the live graph, no `@/server/*` in `IncidentsLive`/`IncidentDetailLive`/`IncidentEditor`, `@/server/explain/contract` the ONLY permitted `@/server/` specifier in `IncidentRcaPanel`, `/IncidentRca` banned by name, `/TerraformExport` banned; **no fence spelled `endsWith("/incidents")`**. The D13 absence list (D548) with the structural non-vacuity pins; `data-tour="incident"` asserted in BOTH `IncidentsMock` and `IncidentsLive`; the three external-anchor assertions on `IncidentDetailLive` mirroring `changes/page.test.ts`. `changes/page.test.ts:100-101` stays GREEN untouched — nothing incident-shaped reaches `ChangesLive`. Full web suite green; tsc + eslint clean. Deferred to integration: the two-build DOM md5 of `/app/incidents` in mock mode.
- result: —

### T7: the tour step, the absences page, and the retention docs page
- status: pending
- wave: 4 · depends: T6 · model: Sonnet (copy plus three needle tests, in stated shapes)
- owns: `apps/web/src/components/shell/TourGuide.tsx` + `TourGuide.test.ts`, `apps/web/src/content/docs/what-obstack-does-not-do/index.mdx`, `apps/web/src/content/docs/retention/index.mdx` (+ its `mirror.test.ts` pin)
- goal: packet D523, D528(ii), §7.7 VERBATIM. The tour body rewritten at the INDEX's altitude, true in both modes, naming no source list of its own; its D404-shape needle test banning `not a working incident tool`, `nothing here is generated from your data yet` and `pipelines`, requiring `target: "incident"`, and asserting the anchor in both components. The M6 absences paragraph narrowed to on-call alone under a `{/* M6, as it stands after S7.4 */}` marker, **keeping its second clause about the public status page intact**, stating S7.4's fence (manual + from-alert promotion, no auto-detection, no metrics leg) as absences, and adding NO new link. The retention page gains the sentence that incidents are kept until deleted while the evidence a timeline stitches still ages out — the first authored object the product keeps past the plan window.
- done-check: the three needle tests RED before the copy lands. `doc-links.test.ts`'s `all.length === 21` and `anchored.length === 5` (unconditional) unchanged, and the build-gated `docsManifest.length === 15` / `checkedBodyLinks === 21` / `SURFACES.docs.pages().length === 15` unchanged — no new page, no new link. `mirror.test.ts` pins the retention sentence. The repo-wide `live tail` sweep still green over the new copy. Full web suite green.
- result: —

### T8: the drive's incidents arm, the RED hook, and the two backlogs
- status: pending
- wave: 5 · depends: T6, T7 · model: Opus (the arm's fixture arithmetic and the plans restore both have to be right the first time)
- owns: `deploy/compose/e2e-drive.mjs`, `deploy/compose/README.md`
- goal: packet D559–D566 VERBATIM. No seeder leg — both incidents are created through the real UI. **Two incidents:** a HISTORICAL one, created with an explicit `started_at` and RESOLVED before the traces leg is asserted (only a closed window is a denominator two readers share), carrying the D71(b) recomputation with both instants bound in SQL via `extract(epoch from …)`; and an ONGOING one whose window precedes the changes and alerts arms' posts, carrying the cross-store claim and asserting its trace leg empty **with its own premise measured**. No settle loop. The Explain collision: the over-quota refusal on alice and the answered path on bob — `EVIDENCE_EXPLAIN_QUOTA` stays at 2 and no existing assertion moves. `RED_WITHHOLD_INCIDENTS` guarding the WHOLE interaction block including both RCA presses, every downstream fact captured with a withheld default, zero `must()` after the creation block. The unauth probe gains six entries and a coverage check that reads `live-routes.ts` as text **with `//`-to-end-of-line stripped before extracting the quoted entries**. The plans restore: the drive reads both previous values with its own `pgOne` before each write (`exit-seed.mjs` UNTOUCHED) and restores them at the top of `finally` before `stopAll()`, wrapped so a failure is a `check()` and never an exception out of `finally`.
- done-check: **the drive runs in the FOREGROUND.** GREEN N/0 and RED (N−K)/K stated as an explicit acceptance clause with K = exactly the arm's checks, never a `must()` abort and never the epilogue — and the RED run SHOWN, its failure lines matching the arm's claims one for one. The recomputation's expected values COMPUTED from the seeder's own arithmetic and then asserted against the recomputation, never against a typed number. The probe coverage check shown red by deleting a probed path, and noted as failing TODAY against the four missing routes. The plans restore proven three ways: an in-drive check that the catalog is back at the CAPTURED values and not a number spelled in the file; the local sequence `up -d` → drive → `usage.integration.test.ts` + `explain/quota.integration.test.ts` both green; and the counter-proof that short-circuiting the restore reds those same two files. The arm's wall-time measured before and after and recorded — **if the job lands over the ~6-minute line the ARM is re-cut, not the line** (D207's reclaim is not pre-spent in this budget). A README bullet in the house voice with its `RED_WITHHOLD_INCIDENTS` clause, inserted before the token-hygiene bullet.
- result: —

## Escalations log
- **D567 (T1, amends D524's reasoning, not its ruling)** — a `(workspace_id, …)` leading index CAN serve `WHERE opened_from_event_id = $1`: Postgres applies it as a non-boundary Index Cond and walks the whole index, so the packet's "cannot serve" and "would seq-scan" are both false as mechanisms. The ruling — the partial UNIQUE is SINGLE-COLUMN because it is also the FK's SET NULL lookup — is unchanged and now quantified: 194 buffers per lookup versus 3 when forced to that index, and a seq scan at 358 buffers when the planner is left free (so a leading-column index is not merely slower, it is not chosen at all). The DDL comment states the mechanism and the condition under which each number reproduces.
- **D568 (T2, amends §0's wording, not its rule)** — §0 says `nowMs` is REQUIRED and defaultless on all THREE formatters. It binds the two that TAKE a clock: `formatIncidentClock` renders an absolute instant, and a parameter it never reads is a discipline that pins nothing. Its arity is asserted at one instead, so a later editor cannot add a defaulted clock to it.
- **D569 (T2, a gap §0 did not cover)** — an instant the product cannot read renders as ABSENT (`—`), never as a number and never as a fabricated one. The guard checks the input TYPE, not just finiteness: `new Date(null)` is the epoch, not NaN, so an absent instant would otherwise render `1970-01-01 00:00:00 UTC` — a plausible timestamp indistinguishable from a real one, which is the D13 failure that matters more than the obviously-broken `NaN` one.
- **D570 (T4, amends D531's REASON for `trace_id != ''`; the ruling stands)** — D531 justified the clause with "the summaries MV has no write-side trace-id filter". That sentence is `traces.integration.test.ts`'s, where it is correct because that read is of `trace_summaries`; this leg reads `obstack.spans`, and the MV is a CONSUMER of spans, so what it filters cannot explain what spans holds. The product's own ingest path also cannot produce the shape (`mapping/spans.go` drops a span with an empty trace id before `write.go`). The clause STAYS — anything holding the `obstack_ingest` grant can write such a row and `argMin` would then return `''`, linking at the traces LIST — but on that reason, not the transplanted one.
- **D571 (T4)** — the `outsideRetention` guard is `floorMs >= windowEndMs`. The shipped `effectiveStart >= windowEnd` conflated three states: a zero-width incident (legal under `0013`'s `ended_at >= started_at`) and a future-started ongoing one (legal under D527's skew) were both told their window predates the plan floor, the second under a window whose END preceded its START. Both reproduced against the live stores by two reviewers independently. A degenerate window now reads zero-width at the incident's own start rather than inverted.
- **D572 (T4)** — the trace leg binds MILLISECONDS (`fromUnixTimestamp64Milli`, the `traces.ts` form), not floored seconds. Flooring was live-proven to draw a span 500 ms before the window as the incident's first row, and to hand a span 300 ms inside incident A to its neighbour B, drawn 700 ms before B's own stated start. It also asked below the retention floor by up to 999 ms, against D540's structural claim.
- **D573 (T4)** — `IncidentTimelineOmission` gains `omittedBeforeIso` and a `band` marker. `omittedAfterIso` fits only an ASCENDING cap; the lead-in band caps DESCENDING, so it was reading a probe row and discarding it — 40 changes in the hour before an incident and 20 returned byte-identical objects, and the surface could only render a count of the READ as a count of the hour.
- **D574 (T4)** — the stitcher reports `inputClipped` rather than leaving the surface to derive the clip from `windowStartIso !== startedAt`. That derivation is blind to a floor landing inside the lead-in BAND: an incident opened within an hour of the retention edge read ten minutes of a sixty-minute band and rendered no clip note. Reported rather than derived, because deriving it needs a floor and a floor needs a second clock (D534).
- **Statement-count pin, recorded (T4):** D530 says `changes.test.ts`'s `seen.length` pin "moves to 3". It moves to **4** — D535, later in the same section, split the changes leg into an in-window read AND a separately capped lead-in read, so the module gained two reads, not one. D530's arithmetic was written before D535 and is stale against its own packet; the plan's T4 `owns` line inherited the wrong figure.
- **`clampLimit` (T4):** D530 says both new reads pass through "the existing `clampLimit`" — but that helper is module-private to `changes.ts` and was never importable, so `alerts.ts` carries its own. One definition per module, pinned by a test.
- **D581 (T5, recorded, not fixed)** — `NOT_CONFIGURED_DETAIL` names "this trace" on the incident RCA route. Wording only; the constant is pinned to its source path by `mirror.test.ts` and quoted into `/docs/explain`, and D557 rules that pin unmoved this sprint. Pre-registered fix: `unavailable(subject?)` with the trace default byte-identical.
- **Carried to T3, from the T1 review (not in T1's `owns`, and not in its done-check — flagged so it does not fall through the gap):** D524's closing clause requires the web-side integration test's DSN guard to name `0013_incidents`, the `slos.integration.test.ts` / `alerts.integration.test.ts` idiom. That belongs to T3's `incidents.integration.test.ts`.
- **D575 (T5, amends D554's REASON; the ruling stands)** — D554 justifies per-lane caps with "a flapping rule can produce hundreds of alert events … and would evict every change event". The stitcher already caps every leg at `TIMELINE_LEG_CAP = 50` (D536), so hundreds never reach the prompt: the most a stitched subject carries is 50 alerts, 70 changes (50 in-window + the 20-row lead-in band) and 50 trace groups, and `MAX_ALERTS = 60` cannot bite on that path. What DOES bite is `MAX_CHANGES` (70 → 40) and `MAX_TRACES` (50 → 20), and a flat 120 over 170 time-ordered rows would still cut whichever lane is latest — so the lanes keep their shape and D554's numbers are kept as ruled, as this module's contract on the unbounded array its type accepts. Recorded in `incident-prompt.ts`'s header.
- **D576 (T5, a gap in D554's "what IS sent")** — `IncidentSubject` does not carry the stitcher's per-leg omissions (D536), so a leg the STITCHER capped reads to the model as complete: the prompt's own truncation line fires only on ITS caps. Mitigated in the system prompt ("treat the end of a lane as the end of what you were shown, never as the moment things went quiet"), which is a sentence and not a number. Not closable in T5 — the honest fix is a field on `IncidentSubject` (T2's type) that the route fills from `timeline.omissions`, and a line per omitted leg in `describeIncident`.
- **D577 (T5, D551 and D554 cannot both hold as written)** — D551 builds the subject "from the stitched timeline's entries"; D554 says `service` and `severity` are SENT; `IncidentTimelineEntry` carries neither (kind, at, until, title, detail, link, key — verified against the shipped type). T4's `alertEntry` comment says severity is "deliberately NOT composed into the detail" because "the RCA row has both as separate fields", but the stitch returns entries only, so the row's two fields are never populated from the read that had them (`AlertEventRow.severity`, `ChangeEventRow.service`). The route cannot re-read them (a second window read is a second clock, D534; the same statement twice is the D171 divergence), so `severity` is null on EVERY row and `service` is set only where the trace group key exposes it. Customer consequence: the model cannot tell a critical alert from a warning. `/docs/explain` currently says an incident row enters as "time, kind, id, title and detail", which is what actually reaches the model today. The fix belongs to the stitcher (T4's file, outside T5's `owns`): `StitchedIncidentTimeline` carries `rows: IncidentTimelineRow[]` projected from the SAME capped legs; `timelineRowOf` in the RCA route is the one function to delete when it lands, and the docs sentence gains the two words then. **Escalated to the integrator — and CLOSED at T5:** the stitcher now projects `rows` from the capped legs, `timelineRowOf` is deleted, and case (vi) flipped from asserting the absence to asserting the value.
- **D578 (T5, amends D553's "the SAME rows the prompt described and the page renders") — AMENDED by the prompt-withholding review; the first ruling below is REVERSED.** Those are two sets on a busy incident: the stitch hands the subject up to 170 read rows (T4: 3×50 + the 20-row band; the 171st is the synthesized `resolved` row, which never reaches the prompt) and the prompt describes at most `MAX_TIMELINE_ROWS = 120` — on the live path at most 110, since `MAX_ALERTS = 60` cannot bite on a 50-capped leg. A first pass ruled "the index follows the SUBJECT's rows, the page's set, because every one is a real row and an anchor" and pinned `busyRefs.size === 133`. **Reversed:** the index follows the PROMPT (`incidentReferences` iterates `describedIncidentRows(incident.rows)`, the one exported function both sides read). A citation is the model's claim about a row it READ; an id the model was never shown cannot be a grounded citation even when it names a real row, and following the page would let a cap-dropped id — reachable only by the model inventing a string that collides with a real row — resolve to a working link the analysis never saw. That is precisely the property the review brief names as a defect ("a citation resolves to a row the model never saw"). `cited ⇔ given` is now asserted in both directions in `explain.test.ts` (the 130-alert fixture: the allowlist's key set deep-equals the prompt's shown-id set, and the last flapping alert is on the page, in the subject and citable nowhere) — RED against an index built from `incident.rows` (`the allowlist and the prompt disagree on a busy incident`). `fakeIncidentDocument` answers from the same described rows, so the fake — the model with the provider removed — cannot cite a row the lanes cut and have validation call a real row "not in this incident's timeline". Originally written as a second "D575" in that file; renumbered at the sibling review, then reversed here.
- **D580 (T5 review — the withholdings proven at the ROUTE on the real stores, and what "withheld" actually guarantees)** — `incident-prompt.test.ts`'s sentinel proof is a PROJECTION proof on an object handed to the builder (RED against `JSON.stringify(incident)`: `CUSTOMER-PROMPT-SENTINEL went to a third party`, 12 of 18 red); it states itself that it cannot prove nothing upstream puts a withheld thing INTO one of the seven fields. `rca.integration.test.ts` case (vi) now proves that half: both Postgres legs and the ClickHouse leg seeded with a sentinel in every column that could carry a withheld thing — `notification_channels.target`, `change_events.key_id`/`who`/`ref`/`link_href`, `alert_events.link`/`channel_id`, `spans.prompt`/`completion`/`status_message`/`attributes['llm.prompt']`, `logs.body`, another workspace's alert in the same minute — plus a 200-span error trace; the REAL `readIncidentTimeline` runs through the route's own `forWorkspace`, the prompt is captured at the provider seam, and every sentinel is absent while the seven fields of each row are present. The data-path facts the proof rests on, traced column by column: `LIST_EVENTS_IN_WINDOW_SQL` selects `id, rule_id, rule_name, slo_id, slo_name, severity, title, detail, link, delivery, created_at` (no `channel_id`, no join to `notification_channels`); `changes.ts`'s `COLUMNS` is `id, kind, at, title, detail, who, service, ref, source, link_label, link_href` (no `key_id`); the spans leg selects `service, name, count, min/max(start_time), argMin(trace_id)` and nothing from `prompt`/`completion`/`attributes`/`status_message`, and no leg reads `logs` at all — so a trace enters as ONE `(service, span name)` group line and 200 spans are one line with `200 errors` in it. Every writer of `alert_events.detail` (`evaluator.go`'s `firingText`/`resolutionText`, `slo_text.go`, and `alerts.ts`'s test notification, which interpolates the channel's NAME) composes condition text, never a target. **What withholding (2) does NOT guarantee, stated so nobody reads it wider:** "no other incident, no other workspace" is a statement about what obstack SELECTS, not about what the customer typed — a change event's `detail` is customer text sent verbatim (clipped at 300), and the case seeds it with the incident's own `inc_` id, another incident's id and a pasted webhook URL, all of which reach the prompt exactly where the customer wrote them; the incident's own id appears in the prompt ONLY there (the subject's `id` is never printed) and is excluded from the allowlist, so a model citing it from the customer's sentence is dropped, never linked. Two further findings, fixed: the subset sentence said "the incident page shows all N" where N excluded the synthesized `resolved` row (off by one on every resolved incident) and the stitcher's own truncation notes — reworded to name only this document's two numbers; and D577's honest absence is now PINNED on the live path (`subject.rows.every(severity === null)`, the change row's `service === null`) so the stitcher fix flips a test rather than silently arriving.
- **D579 (T5 review — the wire contract and the two panels)** — `runExplain(url)` moved the trace path's construction from the function (where `ExplainPanel.test.tsx` pinned it via `served.calls`) to the component's call site, where nothing pinned it: the `calls` assertion now proves only that the url handed in is the url fetched. The call site `runExplain(\`/app/traces/${encodeURIComponent(traceId)}/explain\`, …)` is now pinned by a source assertion in the same test, as `rcaUrl` is for the incident panel. Everything else on the lens held: no consumer switches on `ExplainRefusalReason` (`applyExplainEvent` switches on `event.type`, `readExplainStream` parses without inspecting it, the drive names no reason); the lifted `explainResponse`/`oneEvent` differ from `6b20c52` by `export` and one comment word, `overQuotaDetail`'s template is byte-identical; the trace renderer's `spanId ? … : logRef ? … : null` ignores `eventRef`/`traceRef` and the incident renderer links exactly those two; a fake-mode simulation of `e2e-drive.mjs:2200-2290` against `explainSubject` + `explainResponse` reproduced every drive assertion and the `"is not in this trace"` literal; `mirror.test.ts`'s `NOT_CONFIGURED_DETAIL` pin, `OBSTACK_*` list and `corpus.length === 15` are untouched by the diff; `IncidentRcaPanel.tsx` imports nothing from `@/mock/`, `IncidentRca.tsx`, or `@/server/explain`. Minor, unfixed: `explain-wording.test.ts`'s comment says "three known non-error lines" while four exist (the RCA route warns twice); the `>= 3` floor is unaffected.

## Integration
- verdict: —

## Goal check (Step 7, one entry per iteration)
- (filled at integration)

## Retro (filled at end of run)
- (filled at end of run)
