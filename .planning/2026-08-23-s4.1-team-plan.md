# Team Plan — obstack M4 sprint 1 — the distributable artifact (exit: a clean checkout produces the web and ingest images, the compose bundle and the Helm chart both boot the full stack from those same images, a mode-mismatched serve of the web artifact refuses rather than renders, and ingest shuts down cleanly inside the chart's termination grace)

meta:
- date: 2026-08-23
- repo/branch: `m4-planning` @ `a11ae7a`
- mode: manager-executes (user directive 2026-08-23 mid-F-round: no Workflow-tool orchestration — too many tokens, too slow. The manager (Fable 5, full context) implements directly; review gate preserved via background Opus@xhigh reviewers (batched: one per coherent scope, not one per task); advisor + integrator gates unchanged. The stopped F-round's partial executor work remains in the tree and is verified/completed rather than discarded — round-1 cleared work is interleaved in the same uncommitted delta and cannot be separated.)
- team-cap: 10
- models: executor=sonnet@xhigh reviewer=opus@xhigh advisor=fable <!-- user override 2026-08-23 mid-run, supersedes the prior Opus-executor standing rule for this run; first wave's partial Opus work stashed (stash: "s4.1 partial work from stopped opus executors"), rerun from clean a11ae7a -->
- iteration-limit: 3 — fix rounds allowed before escalating to the user
- iteration: 1/3 <!-- round-1 fix round F1–F5 per D271–D280 -->
- source of truth: `.planning/2026-08-23-s4-m4-phase-plan.md` §3 S4.1 + §9 rulings **D251, D253, D261, D262, D263** (BINDING); D250 sequence; phase-plan §4 ledger rows 2–5, 10–13, 17
- standing guards inherited: e2e at ~10s headroom (D207 reclaim consumable per D261, consumption recorded here if fired); `stack` watch; environment-ordering `down -v` before DSN-backed suites; K3 no renames (new forever-names `images`, `lint` are D261-ruled); eight-check every-PR set grows only per D261; S2.3 L1 alias/env-name sweeps; D215 one-definition rule on the ingest endpoint

## Advisor decisions
<!-- Kickoff round 2026-08-23, recorded as D265–D270 continuing the ledger. Phase rulings D251/D253/D261/D262/D263 remain binding. All facts verified on the tree by the advisor before ruling. -->
- **D265 (kickoff a) — e2e/harness reconciliation + secret posture.** (a1) Web stays **default-profile** (D251(c) stands); every telemetry-path harness excludes it with **`--scale web=0` on its existing up line**: `e2e.yml` boot step, `smoke.sh:24`, `sdk-evidence.sh:167` — each with a one-line comment ("web is not this harness's claim; the `images` job and the self-hosted user boot it"); compose README's "The e2e drive" section documents the same line (S2.1 L3 one definition). `stack.yml` needs NO change (its up line names services explicitly, `:124`). The drive is NOT switched to the containerized web (its claim is product flows from checkout; an in-job image build blows the 6-min line). No port conflict remains; the drive keeps :3000. (a2) **Secret mechanism AMENDED — intent unchanged**: `${VAR:?}` fails file-wide at compose parse and would break every non-web harness and M1's signed smoke path. Instead compose passes `OBSTACK_BETTER_AUTH_SECRET` through with an empty default, and **T1's boot check refuses to start a live-stamped artifact with a missing/empty secret, loudly, with the generate instruction** (`openssl rand -base64 32`) — same check family as the mode stamp, one boot-check module. **Mock-stamped artifacts need no secret** (no auth, no Postgres — what makes the D262 demo variant serveable). "Unset secret fails fast" is proven against the web container's exit, not compose parse.
- **D266 (kickoff b) — endpoint config: runtime, server-resolved, two new env names.** **`OBSTACK_PUBLIC_OTLP_HTTP_ENDPOINT`** and **`OBSTACK_PUBLIC_OTLP_GRPC_ENDPOINT`** (grep-verified zero hits repo-wide; "PUBLIC" = what we tell operators, distinct from the collector's internal `OBSTACK_INGEST_ENDPOINT`). Build-time baking and `NEXT_PUBLIC_` inlining REFUSED (one image, many deployments). Shape: `lib/ingest-endpoint.ts` keeps the loopback default constants unchanged (client-safe; mock/demo copy byte-identical); a new **server-only resolver** exports the effective pair = env-override-or-default, each var independent, empty = unset; live surfaces resolve server-side and pass strings to client children as props (two consumers are `"use client"`: `Quickstart.tsx`, `DemoArrival.tsx` — the split-module shape is mandatory). Mirror test extends: resolver-without-env returns the constants byte-identically. Chart join in T5: ingest ingress enabled → values set both vars on the web workload. Quickstart renders HTTP as primary; the gRPC-unexposed display case is a named T7 breakdown check.
- **D267 (kickoff c) — interim join shapes.** Image repo **`obstack-web`**; tags **`:kind`** for chart/kind loads (live-stamped, `imagePullPolicy: Never`, existing README pattern), **`:live` / `:mock`** in the T6 images-job matrix; compose builds live-stamped in place via `build:` args. Mode stamp = **stamp file in the image** (path stated once, in the boot-check module) + **OCI label `io.obstack.data-mode`**. Boot check: **mismatch → refuse; stamp present + runtime unset → refuse** (explicit over implicit at "costs signup entirely" stakes — compose and chart always set it); **stamp file absent → no-op** (checkout serves: dev, the e2e drive, local `next start`). T5 pins `image: obstack-web:kind`, wires `OBSTACK_DATA_MODE=live` + secret refs from T4's Secret.
- **D268 (kickoff d) — chart version: one bump per sprint.** T4 sets **`version: 0.3.0`** with a comment in the existing Chart.yaml style; T5 rides the same 0.3.0 (one unreleased train). `appVersion` stays `0.1.0` — moves at release time (S5).
- **D269 (kickoff e) — T8 mechanism.** `NoSessionError` lives at `data.ts:274-288`; the noise source is the D114 race (`app/app/layout.tsx:120-127` — wired segments throw while the layout redirect wins 307). Fix at the **request-error instrumentation hook** — exact hook name/signature from the **vendored docs** (`node_modules/next/dist/docs/`), cited by file in the report. Expected unauthenticated flow → no error-class log; every other error passes through; D114 redirect behavior byte-unchanged. **If the vendored docs show no such hook in this Next version, T8 ESCALATES before improvising** — no catch-wrappers across segments.
- **D271 (round 1) — D265(a1) mechanism REPLACED: named services on the up lines; `--scale web=0` is out everywhere.** Falsification accepted (image builds anyway; running web torn down). `e2e.yml` → `up -d --wait --wait-timeout 240 clickhouse postgres ingest`; `smoke.sh` → `up -d --build demo`; `sdk-evidence.sh` → `up -d --build --wait --wait-timeout "$HEALTH_TIMEOUT_S" sdk-sample-py sdk-sample-ts` (depends_on pulls the rest). e2e floor comment re-worded true; compose README documents the same line (S2.1 L3); a running web is provably left alone. D265(a1)'s intent unchanged.
- **D272 (round 1) — one in-container secret name: the boot check reads `BETTER_AUTH_SECRET`; `OBSTACK_` prefix exists only at the compose host layer** (the established `OBSTACK_POSTGRES_PASSWORD`→`POSTGRES_PASSWORD` two-layer pattern). Compose maps `BETTER_AUTH_SECRET: ${OBSTACK_BETTER_AUTH_SECRET:-}`; the dual-mapped container line is deleted; `.env.example` documents the host name; T5's chart wires the Secret key to env `BETTER_AUTH_SECRET`. T1's 6-case matrix re-runs under the new name.
- **D273 (round 1) — refusal mechanism SANCTIONED: `console.error` + `process.exit(1)` from the boot check.** A `register()` throw does not stop Next 16.3.0 (fire-and-forget `prepare()`, measured live: container Up, all requests 500) — a throw is a lie-shaped refusal. The restart-loop is the documented operator symptom; `docker compose logs web` carries the instruction.
- **D274 (round 1) — D269 REPLACED: request-path no-session becomes `redirect("/login")`; the onRequestError filter is DELETED.** The hook is a reporting sink (silenceLog unreachable; Next logs after awaiting it). (1) `app/app/page.tsx:115` + `data.ts` request-path no-session branch → `redirect("/login")` (NEXT_REDIRECT short-circuits before logging; client outcome byte-identical 307). (2) `dataForSessionContext(null)` KEEPS throwing `NoSessionError` — D96/D113 contract and `tenancy.test.ts:210` untouched. (3) The onRequestError addition + its self-referential test are deleted. (4) Digest marker conditional: removed if no request-path caller can still throw; else kept with the record stating **N→1 dedup in prod, never N→0, none in dev** — the exit bundle claims zero-by-construction for the expected flow, never "elimination". (5) Proof at server level: production serve, no-cookie `GET /app` → 307 + stderr free of error lines.
- **D275 (round 1) — chart credential contract: passwords leave the DSN strings.** Ingest binary gains two additive optional envs **`OBSTACK_CLICKHOUSE_DSN_PASSWORD` / `OBSTACK_POSTGRES_DSN_PASSWORD`** (injected as the DSN password component; unit-tested); ingest Deployment always `secretKeyRef` (chart Secret or existingSecret) → these envs; hook Jobs branch — `existingSecret` set → `secretKeyRef` (user's Secret pre-exists), default → render-literal values password (today's dev/CI posture); the dead live-Deployment `lookup` helper is deleted + `migrate-job.yaml:7` stale pointer fixed; `stack.yml:104,147` → `statefulset/obstack-postgres`. Rendered manifests carry no password inside any DSN string on the existingSecret path.
- **D276 (round 1) — upgrade refusal guard IN; D270 amended (a refusal is not migration).** Template-level `lookup` of the same-name live Deployment at render → `fail` with the README "no upgrade path from 0.2.0" pointer. Offline `helm template` gets nil → no false failures. No adoption code.
- **D277 (round 1) — D266 extended: resolver moves to `server/ingest-endpoint.ts` (`import "server-only"`); wiring assigned (F4); display rule.** Lib module's whole-file no-`process.env` assertion restored; resolver tests move beside the server module; prose hazard block trims to a pointer. Wiring follows the file's `API_KEY_PLACEHOLDER` pattern — endpoint placeholder substituted at render with server-resolved values passed as props; reads request-scoped/dynamic-safe (vendored env-vars doc cited). **Display rule: any override set → only overridden endpoints render (unconfigured protocol = honest omission, never a loopback beside a public endpoint); none set → both loopback defaults byte-identical; both set → both.**
- **D278 (round 1) — D263 amended: `Close` is best-effort-bounded with a stated worst case; in-flight abort REFUSED pending measured semantics; grace resized to 45s.** The retry ladder gates on the shutdown deadline between attempts — past it, remaining retries abandoned, rows counted `ReasonShutdown`. Worst case on the record: ≤ deadline + one in-flight attempt ≤ 10s+30s = 40s. **Chart `terminationGracePeriodSeconds: 45`, compose `stop_grace_period: 45s` (supersedes D263.3/.4's 30s).** Aborting an in-flight INSERT is refused now — MergeTree partial-application/retry-dup semantics are S2.4 L1 measured questions. T2's two out-of-owns files (metrics.go `ReasonShutdown`, integration_test.go call sites) RATIFIED; record on T2's owns at close-out.
- **D279 (round 1) — `.gitignore` surgical lines move to F2** (`!deploy/compose/.env.example` + `.obstack-mode-stamp`); tree consistent this round; T6 must not duplicate or revert; T6's remaining hygiene unchanged.
- **D280 (round 1) — timing posture: F2 records fresh local boot/smoke/drive timings as the working reference; the deciding numbers remain T6's first-runner return (D137/D261); D207 reclaim rule stands.** No pre-T1 timing carries (standalone +2.5s build, drive builds the app).
- **D281 (round 2) — the connections surface joins the D277 wiring NOW; deferral REFUSED.** T7's ruled goal was always "quickstart AND connect flows" — the modal is the second half of the D214 obligation. Owns assigned: `connectors.ts`, `ConnectModal.tsx`, `ConnectionsHub.tsx`, + the connections page (server-resolves once, passes as props). Mechanism identical to F4: placeholder substitution at render (the `API_KEY_PLACEHOLDER` pattern); `connectors.ts:70,75` become no-override defaults behind placeholders; D277.3 display rule unchanged; mock/demo byte-identical (D208). Done-check mirrors F4's (no-env byte-identical; HTTP-only → no loopback gRPC anywhere in the modal; both → both). Lands before the integrator gate; evidence rides the F-round return.
- **D282 (round 2) — gRPC-only corner: the D277 rule made symmetric — honest absence with the remedy named; empty exports a tested impossibility.** Refusing the configuration (option b) refused: a display resolver must not judge deployment validity. (1) A tab/step/line needing an unpublished protocol renders an honest absence, never an empty value; the HTTP-unset absence line names the remedy (set `OBSTACK_PUBLIC_OTLP_HTTP_ENDPOINT`); LiveArrival renders published endpoint(s), never a bare stub. (2) Class-closing test: no rendered snippet in quickstart or connect flows contains an empty assignment (`export OTEL_EXPORTER_OTLP_ENDPOINT=` shape) under any of the four override corners — the measured silent-localhost SDK fallback is why an empty export is worse than an omission. (3) `Quickstart.tsx:47-55`'s acknowledged-but-unimplemented comment is replaced by the rule + citation.
- **D283 (round 2) — resources: requests everywhere; limits only on the bounded auxiliaries; store limits are a measured S5 item.** ClickHouse/Postgres StatefulSets get conservative stated REQUESTS and deliberately NO limits (a mis-estimated request degrades scheduling; a mis-estimated limit OOM-kills the store; store limits are only honest coordinated with the DB's own memory settings — **reopen-on-measurement, owner: S5's sizing pass**). Collector + demo get small requests AND limits (bounded fixed workloads; a limit on a log-tailer is genuine node protection). Ingest/web keep theirs. Numbers from the tree: at/below observed kind-acceptance footprints (ClickHouse ~1.66Gi peak → 500m/1Gi req; Postgres ~48Mi → 100m/128Mi; demo ~55Mi); the acceptance install completing on the GitHub runner is the scheduling proof.
- **D284 (round 2) — managed-PG NOT discharged as shipped; the gating is owed in S4.1, both halves.** (1) `postgres.managed.enabled=true` stops rendering the in-chart Postgres (StatefulSet, Services — gone, not idle). (2) The pg-migrate Job retargets the managed DSN — same D95(a) one-runner contract, credentials per D275 injection, never a DSN literal. (3) No reachability machinery invented: **the Job failing IS the reachability gate** (`--wait` fails loudly there), stated in the README as designed behavior. (4) Evidence split: kind proves the shape against an out-of-release Postgres (template matrix = zero in-chart Postgres objects under managed; Jobs Complete + schema landing in the external target; unreachable-DSN install failing at the Job); **real-provider verification (TLS/sslmode, provider auth) pre-registered to S5** with U10's instance — recorded, not absorbed. Also of record: the infra reviewer's `beginClose(ctx)` fix ACCEPTED — sequential two-batcher close measured ≈2×writeTimeout past the ≤40s bound; the bound of record becomes deadline + one writeTimeout SHARED across both batchers (deadline published to both before either is waited), restoring the D278 arithmetic the 45s grace was sized for.
- **D270 (kickoff f) — T4 storage posture: breaking change accepted, documented, no migration machinery.** Zero production installs exist (chart header: CI-grade; standing installs are throwaway kind clusters); README pre-committed the replacement. No hostPath-adoption code, no dual storage path. README gains an "upgrading from 0.2.0" note: pre-M4 hostPath releases have no upgrade path — `helm uninstall`, remove node dirs, fresh install. The old hostPath consequences block is deleted with the mechanism it described.

## Critical items
<!-- Per phase plan §9 verdict, these return to the advisor before merge. -->
- T1 — Dockerfile + standalone design **with vendored-doc citations** (`node_modules/next/dist/docs/` — never training-data memory, AGENTS.md rule); the mode-stamp mechanism + red-then-green refusal proof
- T3 — the compose secret posture (`BETTER_AUTH_SECRET` required-no-default line verbatim); the e2e-drive reconciliation with web-in-compose
- T4/T5 — the StatefulSet+PVC migration plan and the values surface against the D253 floor
- T2 — `Writer.Close(ctx)` implementation + both grace numbers (chart 30s / app 10s; compose `stop_grace_period` lands in T3)
- T7 — the `ingest-endpoint.ts` configurability design (D215 one-definition must survive; env pair per D266)
- T6 — `images`/`lint` first real-runner numbers (D137 shape — return to advisor)
- Kickoff amendments: **T3's return includes reconciliation evidence** (post-change local timings for smoke floor + drive; e2e/sdk-e2e envelopes untouched, D207 unconsumed or consumption recorded); **T1's red-then-green matrix covers all four boot-check outcomes** (mismatch-refuse, runtime-unset-refuse, stamp-absent-no-op, live-without-secret-refuse); **T8 joins the critical list** (same instrumentation seam as T1 — vendored-doc hook citation + quiet/loud test pair return with the merge)

## Tasks

### T1: web standalone image + mode stamp
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: 1
- depends: —
- owns: apps/web/next.config.ts, apps/web/Dockerfile (new), apps/web/Dockerfile.dockerignore (new), apps/web/src/instrumentation.ts, apps/web/src/server/mode-stamp.ts (new; name may follow vendored-doc idiom), apps/web/src/server/mode-stamp.test.ts (new)
- goal: `output: "standalone"` in next.config.ts; one multi-stage Dockerfile at apps/web/Dockerfile with build context at the REPO ROOT (D251(a); the demo/sdk-sample-ts root-context pattern is the precedent), base image exact-pinned (D14), Dockerfile.dockerignore scoping the context with measured context size noted; the build accepts `OBSTACK_DATA_MODE` as a build arg and produces the **live-stamped and mock-stamped variants of the one Dockerfile** (D262); the artifact carries a baked mode stamp and a boot-time check (instrumentation hook per the **vendored Next docs** — cite doc files in your report) compares stamp vs runtime `OBSTACK_DATA_MODE` and **refuses to start, loudly, naming both values** (D251(b)). **Kickoff-binding mechanics (D265/D267): the stamp is a file in the image (path stated once, in the boot-check module) + OCI label `io.obstack.data-mode`; boot outcomes: mismatch → refuse; stamp present + runtime unset → refuse; stamp file ABSENT → no-op (checkout serves — dev, the e2e drive, local `next start` — must be untouched); the same boot-check module also refuses a live-stamped artifact with missing/empty `OBSTACK_BETTER_AUTH_SECRET`, loudly, with the `openssl rand -base64 32` generate instruction (D265(a2)); mock-stamped artifacts need no secret.** `instrumentation.ts` EXISTS (extend it — never replace its current registration). Image repo/tags per D267 (`obstack-web`, `:kind`/`:live`/`:mock`). The S1 T12 badge keeps its build-time env read (ruled — no forced-dynamic shell). Constraint of record: `apps/web/package.json:5` test-runner conditions and D156 shim must keep working; `OBSTACK_DATA_MODE` remains a BUILD-time input (`data.ts:71-73` wording stays true).
- done-check: `docker build -f apps/web/Dockerfile --build-arg OBSTACK_DATA_MODE=live .` and `--build-arg OBSTACK_DATA_MODE=mock .` both produce runnable images; **red-then-green matrix over all four boot outcomes** (mismatch-refuse, runtime-unset-refuse, stamp-absent-no-op, live-without-secret-refuse); `npm test` + `npm run build` green from root
- result:

### T2: Writer.Close(ctx) + counted overrun drops
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: 1
- depends: —
- owns: services/ingest/internal/write/write.go, services/ingest/internal/write/batcher.go, services/ingest/internal/write/write_test.go, services/ingest/internal/write/batcher_test.go, services/ingest/cmd/ingest/main.go
- goal: D263 verbatim — `Writer.Close(ctx)`: `main.go:287` passes `shutdownCtx`; each batcher `close()` takes ctx; final flush runs under `min(writeTimeout, ctx deadline)`; the retry loop never sleeps past the deadline (corrected fact of record: today's bound is ~181.5s worst case vs the 10s deadline — coordination defect, not unboundedness). On deadline overrun remaining rows are counted via the existing `countDrops` path under a new stated drop-reason constant (loud log + per-workspace counts, never silent — the D5 posture extends to shutdown). The `main.go:284-293` shutdown ordering is unchanged. Error path `:250-255` passes a ctx too.
- done-check: `go test ./...` green in services/ingest; a shutdown-under-load test showing clean flush inside deadline AND a deadline-overrun test showing the counted drop, red-then-green on the ctx threading
- result:

### T3: compose — web service, secret posture, grace, harness reconciliation
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: 2
- depends: T1, T2
- owns: deploy/compose/docker-compose.yml, deploy/compose/.env.example (new), deploy/compose/README.md, .github/workflows/e2e.yml, deploy/compose/smoke.sh, deploy/compose/sdk-evidence.sh
- goal: `web` joins the compose **default profile** built from T1's Dockerfile (live-stamped, `build:` args per D267) so `docker compose up` boots the full self-hosted stack (D251(c)); the `docker-compose.yml:38-42` rationale is answered in place, not deleted — secrets move to the web container; **secret posture per D265(a2)**: compose passes `OBSTACK_BETTER_AUTH_SECRET` through with an empty default (NO `:?` parse-time syntax — it breaks every non-web harness file-wide), the refusal lives in T1's boot check, `.env.example` ships beside the file; `BETTER_AUTH_URL` defaults to local loopback with the D119 http-downgrade consequence documented at the value; ingest gains `stop_grace_period: 30s` (D263.4 — the sdk-samples precedent at `:202`); **harness reconciliation per D265(a1)**: `--scale web=0` added to the `e2e.yml` boot step, `smoke.sh:24`, and `sdk-evidence.sh:167`, each with the one-line comment; compose README's "The e2e drive" section documents the same line; `stack.yml` untouched (its up line names services explicitly).
- done-check: `docker compose -f deploy/compose/docker-compose.yml up -d --wait` from a clean checkout + exported secret boots clickhouse+postgres+ingest+web healthy; with the secret unset the web container exits fast with the generate instruction (T1's refusal, proven here against the container); `bash deploy/compose/smoke.sh` PASS with timings recorded; the e2e drive runs locally against the reconciled shape with timings recorded (critical-item evidence)
- result:

### T4: chart — StatefulSets+PVCs + Secrets/existingSecret + rotation doc
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: 1
- depends: —
- owns: deploy/helm/obstack/templates/clickhouse/**, deploy/helm/obstack/templates/postgres/**, deploy/helm/obstack/templates/secret.yaml (new), deploy/helm/obstack/values.yaml, deploy/helm/obstack/Chart.yaml, deploy/helm/obstack/README.md
- goal: D253 items 2/3/7 — ClickHouse and Postgres move from Deployment+hostPath to **StatefulSets + PVCs** (the README's own pre-commitment at `:50-53`); the chart renders its own Secret from values by default and **every credential accepts an `existingSecret` name override** (Postgres password, both ClickHouse passwords; `BETTER_AUTH_SECRET`/`OBSTACK_EXPLAIN_API_KEY` slots land here for T5 to consume); **no external-secrets, no Polar values** (refused/out per D253); D123 rotation becomes a documented procedure in the chart README (update Secret → in-DB `ALTER USER` → roll); the migrate Jobs and the D122 ddl-drift step keep working; values.yaml's M2-scope header comment is rewritten to the M4 truth. Storage class/size are values with stated defaults. **Kickoff-binding (D268/D270): `Chart.yaml` version → `0.3.0`** (one bump for the sprint, T5 rides it; `appVersion` stays `0.1.0`); **storage cut is breaking-and-documented, no migration machinery** — README "upgrading from 0.2.0" note (helm uninstall → remove node dirs → fresh install), no hostPath-adoption code, no dual storage path; the old hostPath consequences block is deleted with the mechanism.
- done-check: `helm lint deploy/helm/obstack` clean; `helm template` renders StatefulSets, PVCs, Secret, and honors `existingSecret`; kind: `helm install --wait` from a virgin cluster completes with both migrate Jobs and `deploy/helm/obstack/acceptance.sh` PASS
- result:

### T5: chart — web workload + Ingress (web AND ingest) + managed-Postgres values + resources + grace
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: 2
- depends: F1, F2 (T1/T4 substance merged; consumes F1's secretKeyRef/env contract, D272's `BETTER_AUTH_SECRET` wiring, D278's grace 45 — F1 already sets ingest's `terminationGracePeriodSeconds: 45`, T5 must not duplicate/conflict; D277 display rule noted in values docs)
- owns: deploy/helm/obstack/templates/web/** (new), deploy/helm/obstack/templates/ingress/** (new), deploy/helm/obstack/templates/ingest/deployment.yaml, deploy/helm/obstack/values.yaml, deploy/helm/obstack/README.md
- goal: D253 items 1/4/5/6 — `web` Deployment + Service, image exact-pinned (T1's live-stamped image), mode-stamp env wired, `BETTER_AUTH_URL` a required value with the D119 https posture documented; **optional Ingress templates for web and for ingest OTLP/HTTP** (host + TLS-secret values, annotation pass-through, no controller/cert-manager shipped — ingest's ingress IS the D214 network-reachable surface); resource requests/limits values with conservative stated defaults on every workload; managed-Postgres values (external DSN + `existingSecret`, disabling in-chart Postgres); ingest Deployment gains explicit `terminationGracePeriodSeconds: 30` (D263.3) and consumes T4's secret refs; D221's values comment updated if the key story changes. HA beyond ingest's existing replicas is OUT (D253). **Kickoff-binding (D266/D267/D268): web pins `image: obstack-web:kind` with `imagePullPolicy: Never`, wires `OBSTACK_DATA_MODE=live` + secret refs from T4's Secret; when ingest ingress is enabled, values set `OBSTACK_PUBLIC_OTLP_HTTP_ENDPOINT`/`OBSTACK_PUBLIC_OTLP_GRPC_ENDPOINT` on the web workload (the D266 pair); same 0.3.0 version train as T4.**
- done-check: `helm lint` clean; `helm template` with ingress on/off, managed-PG on/off all render valid; kind: `helm install --wait` boots the full stack including web from the loaded images; acceptance.sh PASS (extended to probe web serving)
- result:

### T6: CI — `images` + `lint` jobs, eslint zero, repo hygiene
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: 3
- depends: F2, F3, T5 (D279: the two surgical .gitignore lines are F2's — do not duplicate or revert; remaining hygiene scope unchanged)
- owns: .github/workflows/images.yml (new), .github/workflows/lint.yml (new), .gitignore, apps/web/src/app/status/page.tsx, apps/web/src/components/dash/OnboardingChecklist.tsx, apps/web/src/components/dash/WatchWidgets.tsx, apps/web/src/components/incidents/IncidentRca.tsx, apps/web/src/server/onboarding.test.ts, (deletions: /.next/, /src/app/, /test-output.log, /tsconfig.tsbuildinfo, /next-env.d.ts, apps/web/.next from tracking)
- goal: D261 — new K3 forever-job **`images`**: builds the web image (live + mock variants) and the ingest image, boots the compose bundle **from those images**, runs healthchecks + a smoke probe + the mode-mismatch refusal check; provisionally every-PR, first real-runner number returns to the advisor (D137 shape). New forever-job **`lint`** running `eslint` over apps/web with the **6 recorded errors fixed in the same change** (locations verified 2026-08-23: `status/page.tsx:121` unescaped entity; `OnboardingChecklist.tsx:52`, `WatchWidgets.tsx:245`, `IncidentRca.tsx:22` setState-in-effect; `IncidentRca.tsx:~61` reassign-after-render; `onboarding.test.ts:131` no-assign-module) — no carve-outs, a lint job born with exceptions is refused by D261. Repo hygiene: stray build artifacts deleted and gitignored (a distributable clean checkout is product surface; `pitchdeck-assets/` is the USER's — untouched pending their move). **Kickoff-binding (D267): image names `obstack-web:live` / `obstack-web:mock` in the job matrix; "boot from those images" is satisfied by compose's own `build:` directives at the same commit — no artifact-passing plumbing.** Workflow-header comment discipline matches the existing eight files.
- done-check: `npx eslint apps/web/src` exits 0; both workflows pass `actionlint` (or dry parse); local `act`-free verification = the images job's compose-from-images boot script runs green locally; `git status` clean on a fresh clone + build
- result:

### T7: ingest-endpoint configurability (D215 survives)
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: 1
- depends: —
- owns: apps/web/src/lib/ingest-endpoint.ts, apps/web/src/lib/ingest-endpoint.test.ts
- goal: the one-definition endpoint module (`:15-31`, hardcoded `http://127.0.0.1:4318/4317` with the M4 comment) becomes deployment-configurable per **D266**: the module keeps its client-safe loopback default constants byte-unchanged; a new **server-only resolver** exports the effective pair = env-override-or-default from `OBSTACK_PUBLIC_OTLP_HTTP_ENDPOINT`/`OBSTACK_PUBLIC_OTLP_GRPC_ENDPOINT` (each independent, empty = unset); live surfaces resolve server-side and pass strings to client children as props (`Quickstart.tsx` and `DemoArrival.tsx` are `"use client"` — the split-module shape is mandatory); the resolver imports the constants (one definition of the default, one of the override rule); no `NEXT_PUBLIC_`, no build-time baking. Mirror test extends: resolver-without-env returns the constants byte-identically. **Named breakdown check: the gRPC-unexposed display case** (HTTP is the primary quickstart render). Mock/demo copy byte-identical.
- done-check: `npm test` green; consumers keep importing this module (grep); with the vars set, resolver returns them (test-asserted); without them, current strings byte-identical
- result:

### T8: D132 — NoSessionError log volume
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: 2
- depends: T1
- owns: apps/web/src/instrumentation.ts (the error-filter addition only — T1's boot-check registration must be preserved), apps/web/src/server/data.ts (only if the error needs a marker property), plus the test file for the filter
- goal: **per D269** — `NoSessionError` (`data.ts:274-288`) noise comes from the D114 race (`app/app/layout.tsx:120-127`: wired segments throw while the layout redirect wins 307; the losing throws get error-logged). Filter expected `NoSessionError` at the **request-error instrumentation hook** — exact hook name/signature from the **vendored docs** (`node_modules/next/dist/docs/`), cited by doc file in the report. Expected unauthenticated flow → no error-class log; every other error passes through untouched; D114 redirect behavior byte-unchanged. **If the vendored docs show no such hook in this Next version: STOP and escalate — no catch-wrappers across segments.**
- done-check: `npm test` green; the quiet/loud test pair (unauthenticated request → no error-level log; a genuine error still logs); vendored-doc citation in the report
- result:

### F1: chart credential refit + upgrade guard + stack.yml (D275, D276; carries D278's 45s)
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: F1 (fix round, wave 1)
- depends: —
- owns: deploy/helm/obstack/templates/_helpers.tpl, deploy/helm/obstack/templates/ingest/migrate-job.yaml, deploy/helm/obstack/templates/ingest/pg-migrate-job.yaml, deploy/helm/obstack/templates/ingest/deployment.yaml, deploy/helm/obstack/templates/clickhouse/statefulset.yaml, deploy/helm/obstack/templates/postgres/statefulset.yaml, deploy/helm/obstack/templates/secret.yaml, deploy/helm/obstack/values.yaml, deploy/helm/obstack/README.md, .github/workflows/stack.yml, services/ingest/cmd/ingest/main.go (or its config module) + its test file
- goal: implement D275 (DSN password-injection envs `OBSTACK_CLICKHOUSE_DSN_PASSWORD`/`OBSTACK_POSTGRES_DSN_PASSWORD` in ingest, additive + unit-tested; ingest Deployment always secretKeyRef; hook Jobs branch on existingSecret; dead lookup helper deleted; migrate-job.yaml:7 pointer fixed; stack.yml → statefulset/obstack-postgres) + D276 (upgrade refusal guard: template lookup of same-name live Deployment → fail with README pointer) + D278's `terminationGracePeriodSeconds: 45` on ingest.
- done-check: `go test ./...` green with injection tests; `helm lint --strict` clean; `helm template` matrix (defaults / all-three existingSecret) renders with zero password substrings inside DSN strings on the existingSecret path; kind ×2 (virgin default + brought-Secret) both `--wait` green with acceptance.sh PASS; upgrade guard measured FAIL from an installed 0.2.0; stack.yml exec line green against the StatefulSet
- result:

### F2: harness boot lines + secret single-name + gitignore + timings (D271, D272, D279, D280; carries D278's 45s)
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: F1
- depends: —
- owns: .github/workflows/e2e.yml, deploy/compose/smoke.sh, deploy/compose/sdk-evidence.sh, deploy/compose/docker-compose.yml, deploy/compose/README.md, deploy/compose/.env.example, .gitignore (the two D279 lines only), apps/web/src/server/mode-stamp.ts, apps/web/src/server/mode-stamp.test.ts
- goal: D271 named-service up lines in all three harnesses (+ true floor comment, README same line); D272 single in-container name `BETTER_AUTH_SECRET` (compose maps from host `OBSTACK_BETTER_AUTH_SECRET`, dual line deleted, .env.example documents host name, mode-stamp.ts reads BETTER_AUTH_SECRET); compose `stop_grace_period: 45s` (D278); D279's two .gitignore lines + `git add -f deploy/compose/.env.example`; fresh local boot/smoke/drive timings recorded (D280).
- done-check: with obstack-web image deleted, the e2e boot line shows NO `obstack-web Building`; full `up -d --wait` (web running) then smoke.sh → web still Up + smoke PASS; sdk-evidence.sh PASS; T1's 6-case docker-run matrix re-passes under BETTER_AUTH_SECRET; `git check-ignore deploy/compose/.env.example` → not ignored; timings in report
- result:

### F3: NoSessionError redirect refit (D274)
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: F1
- depends: —
- owns: apps/web/src/app/app/page.tsx, apps/web/src/server/data.ts, apps/web/src/instrumentation.ts (delete the onRequestError addition ONLY — D273's boot check untouched), apps/web/src/instrumentation.test.ts (delete)
- goal: D274 verbatim — request-path no-session → `redirect("/login")`; `dataForSessionContext(null)` keeps throwing NoSessionError (tenancy.test.ts:210 untouched); onRequestError addition + self-referential test deleted; digest marker kept-or-removed per the post-refit grep with reason stated.
- done-check: `npm test` green incl. tenancy.test.ts:210; server-level proof: production serve, no-cookie GET /app → 307 to /login with no error-level stderr for the request; grep verdict on residual NoSessionError request paths in report
- result:

### F4: endpoint resolver split + wiring + display rule (D277)
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: F1
- depends: —
- owns: apps/web/src/server/ingest-endpoint.ts (new), apps/web/src/server/ingest-endpoint.test.ts (new), apps/web/src/lib/ingest-endpoint.ts, apps/web/src/lib/ingest-endpoint.test.ts, apps/web/src/components/onboarding/Quickstart.tsx, apps/web/src/components/onboarding/Quickstart.test.ts, apps/web/src/components/onboarding/DemoArrival.tsx, apps/web/src/app/app/onboarding/page.tsx, apps/web/src/server/onboarding.ts, apps/web/src/server/onboarding.test.ts, apps/web/src/components/connections/connectors.ts
- goal: D277 verbatim — resolver moves to server/ingest-endpoint.ts with `import "server-only"`; lib module restored to client-safe constants + whole-file no-process.env assertion; wiring via the API_KEY_PLACEHOLDER pattern (server-resolved values as props, request-scoped/dynamic-safe, vendored env-vars doc cited); display rule: any override set → only overridden endpoints render; none → byte-identical defaults; both → both.
- done-check: no env → every rendered string byte-identical (test-asserted); HTTP-only → configured HTTP renders, no loopback gRPC anywhere (test-asserted); both → both; lib no-process.env assertion restored; `npm test` green
- result:

### F5: Close retry-gate + stated bound (D278)
- status: approved (integrator GO 2026-08-23; advisor signature pending)
- wave: F1
- depends: —
- owns: services/ingest/internal/write/batcher.go, services/ingest/internal/write/batcher_test.go
- goal: D278 — retry ladder gates on the shutdown deadline between attempts; past it, remaining retries abandoned and rows counted ReasonShutdown; close() doc states the ≤ deadline + one writeTimeout bound and the in-flight-abort refusal ground.
- done-check: red-then-green — pre-close in-flight ladder abandons remaining retries past deadline, abandoned rows counted ReasonShutdown; `-race -count=3` clean
- result:

## Escalations log
<!-- {T#} — question → advisor decision (one line each). -->
- 2026-08-23 round 1 (executors Sonnet@xhigh, reviewers Opus@xhigh): **T1 FIXED** (reviewer caught `.env.local` secrets entering the build context via `!apps/web` re-admission — fixed with `**/.env*` ignore; context re-measured) · **T2 FIXED** (reviewer caught the D263 defect MOVED not fixed — drain/ticker flushes still ran unbounded ladders; added shutdown channel + sendCtx, red-then-green) · **T4/T7/T3/T8 MUST-FIX** · T5/T6 blocked.
- 2026-08-23 round 2 (manager-executes mode): F1–F5 substance found largely complete in the tree from the stopped executors; manager verified/completed each against D271–D280, then implemented T5 (templates/web/{deployment,service}.yaml, templates/ingress/{web,ingest}.yaml, acceptance.sh web probe + per-run WEB_AUTH_SECRET + web image build/load — acceptance.sh and chart README are out-of-owns additions, disclosed D278.4-class) and T6 (images.yml + lint.yml with `working-directory: apps/web`, eslint 0 errors 0 warnings incl. the costs-page unused import, root strays deleted, .gitignore already carried F2's D279 lines). **Evidence so far**: go test -race green; helm lint --strict clean; existingSecret render matrix = zero password substrings in DSNs; web suite 345/0 + prod build green; refusal matrix on real containers (mock-as-live rc=1 naming both values; live-no-secret rc=1 with the openssl instruction); **cold `down -v` compose boot 32s** to all-healthy incl. web, /login 200; **F3 server proof**: no-cookie GET /app → 307 /login with zero error lines in the web container log; **smoke PASS 47s with web surviving Up** and zero web-build lines (both D271 falsification fixes proven); **drive 166/0 in 134s** after flipping its stale D114 assertion to the D274 truth (e2e-drive.mjs `:2678-2761` — out-of-owns, disclosed: the probe slice must now be error-free entirely; a NoSessionError there is a refit regression). Local→runner projection stays inside the envelope with D207 armed. Port-3000/5432 clash with the user's botly containers resolved by user decision (stop botly for the battery window; restart after). Kind battery running.
- 2026-08-23 round 2, D281/D282 executed by the manager: `connectors.ts` OTLP snippets moved to `OTLP_HTTP_PLACEHOLDER`/`OTLP_GRPC_PLACEHOLDER` (the "Or gRPC on 4317" title/body de-ported on the D208 product-claim ground — the byte-freeze protects demo data, not false claims); `ConnectModal` gained the exported pure `resolveStepSnippet` (substitute-or-honest-absence, remedy var named per protocol) + `endpoints` prop; `ConnectionsHub` threads it with the client-safe constants as the demo fallback; the connections page resolves after `connection()` and passes down. Quickstart: `snippetsFor` gained the D282 absence path (SDK tabs absent with remedy + pointer at the working tab; the OTel tab renders a REAL gRPC snippet in the gRPC-only corner), the render gained the absence branch (nothing to copy), `LiveArrival` renders the published endpoint(s) — never a bare stub. Tests: `connectors.test.ts` re-pinned to the placeholder truth (literal loopback in steps is now an asserted regression); `Quickstart.test.ts` extraction updated for the new shapes + the gRPC-only corner + the four-corner no-empty-export class test; `ConnectModal.test.tsx` gained the three-corner resolver tests. Gates after: suite 392/0, build green, lint 0/0.
- Round-1 escalations routed to advisor in one consolidated packet (13 items): D265(a1) falsified (`--scale web=0` builds the image anyway + tears down running web; service-naming proposed); secret name mismatch (`OBSTACK_BETTER_AUTH_SECRET` vs `BETTER_AUTH_SECRET`); process.exit(1) refusal sanction (register() throw does not stop Next 16.3.0); D269 falsified (onRequestError cannot suppress Next's own log; redirect-on-losing-path alternative; N→1 not N→0 dedup truth); T4 existingSecret scope hole (migrate Jobs/_helpers.tpl unowned; stack.yml `deploy/obstack-postgres` breaks; migrate-password lookup dead); 0.2.0→0.3.0 silent-green-data-loss upgrade guard question; T7 server-only split + zero call sites + gRPC display ruling; T2 in-flight INSERT residual (abort vs best-effort-bounded); standalone +2.5s build cost → T3 timings re-measure; `.env.example` gitignored. Advisor round pending → rulings land below as D271+.

## Integration
- verdict: **GO** (round 1, zero seam fixes; integrator's git-status byte-identical through its run)
- rounds: 1
- e2e: compose all-healthy 4m26s cold rebuild; REAL signup via the product server action (303 + session cookie + workspace); 9 authed surfaces 200; no-cookie /app 307 with 0 error lines; refusal matrix 3× rc=1 on the real image; live display-rule both corners (loopback 0, empty exports 0); drive 166/0 in 151s; fresh kind acceptance PASS 153s @ obstack-0.3.0 (= the D283 scheduling proof, 9/9 PodScheduled); D284 managed matrix + external-schema + unreachable-FAIL legs banked pre-gate
- seam findings: none (4 non-blocking notes recorded: stale-image evidence window pre-14:52Z superseded; two stray files since removed; stamp path spelled twice — mirror-test candidate)
- fix tasks: none

## Goal check
- iteration 1: **MET** — the run-goal's four clauses proven clause-by-clause on the integrator's GO evidence; advisor exit bundle SIGNED 2026-08-23 (`.planning/2026-08-23-s4.1-exit-evidence.md`). One fix round used (F1–F5 per D271–D280). Two post-merge conditions ride the signature: first real-runner numbers (`images`/`lint`/`e2e`) return to the advisor; the D274.4 branch recorded (done: marker removed).
- gap tasks this round: none
- at limit and still unmet: n/a

## Retro (filled at end of run)
- tasks: 13/13 approved (T1–T8 + F1–F5); deferred: none
- breakdown corrections by advisor: kickoff 6 (D265–D270, incl. T8 relocated to T1's instrumentation seam + T3 owns widened) + 2 ruled mechanism REPLACEMENTS on reviewer falsification (D271 `--scale web=0`→named services; D274 onRequestError→redirect) + 2 scope-hole rounds (D281/D282 connections wiring + gRPC corner; D283/D284 resources + managed gating)
- reviewer verdicts (batched, 2 reviewers): 0 approve / 2 fixed-then-cleared (11 self-fixed findings incl. a build-context secret leak, a user-data-loss bug in a lint refit, and a measured grace-bound violation; 2 must-fix items closed by manager)
- lesson: xhigh reviewers falsified two advisor-ruled mechanisms by MEASURING them (compose --scale semantics; Next's onRequestError being a sink) — rulings that name a mechanism need the same proven-by-failure treatment as code; manager-executes mode made each falsification a same-hour re-rule instead of a wave respin
