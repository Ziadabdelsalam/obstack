# Team Plan — obstack M3 sprint 1 — Postgres arrives and the workspace becomes a real identity (exit: the stack boots with Postgres in compose and in the M2 chart, a stranger signs up and lands in their own auto-created workspace, every telemetry surface reads that workspace instead of an env constant, and saved views live in Postgres with the browser store deleted)

meta:
- date: 2026-08-18
- repo/branch: master @ 65aff0b
- mode: waves
- team-cap: 10
- models: executor=opus (standing user rule — Opus 5 executors AND reviewers, never Sonnet; Fable 5 only on explicit per-task user override) reviewer=opus advisor=fable
- iteration-limit: 3 — fix rounds allowed before escalating to the user
- iteration: 0/3
- sprint: S3.1 per `.planning/2026-08-18-s3-m3-phase-plan.md` (§3 S3.C1→S3.1, §9 rulings D94–D110 BINDING)
- **run outcome (2026-08-19): COMPLETE — exit bundle ADVISOR-SIGNED @ `0bea93a`.** 16/16 task-slots approved (T1–T8, F1–F8 incl. reassignments), integrator FIXED-GO round 1, goal **MET 0/3** clause-by-clause (see `## Goal check`). Rulings **D111–D137**. **PR #14 MERGED** from branch `s3.1-postgres-identity` to `master` @ **`9ba55bb`**, **eight checks green at the signed tip**; planning commit @ **`987ea1b`**. Exit bundle recorded verbatim at `.planning/2026-08-19-s3.1-exit-evidence.md`.
- **D137 (2026-08-19) — the D107/D136 trigger ruling, BINDING going forward: `e2e` runs EVERY-PR.** 4m12s real sits inside the standing envelope (35s–5m10s, `stack` at the ceiling), and the job carries both the M1 smoke floor and the tenancy spine — coverage that must not wait for a label. The ~3× local-to-runner gap landed as S2.4 L3 predicted, which is why the decision waited for the real number. **Watch pre-registered in the D41/D93 shape: three consecutive `e2e` runs over the ~6-minute K5 line → the over-branch is decided then, not now.** `stack`'s existing watch continues unchanged (5m10s post-Postgres is inside its recorded range). **K3 stands: both names are forever.** Advisor correction of record: the every-PR set is **eight** checks (M2's seven + `e2e`), not "nine".
- sources: phase plan §9 (D94–D110), vision doc S3 paragraph (amended, user-signed 2026-08-18), `.ziarmy/retro.md` (all lessons binding), `docs/superpowers/specs/2026-08-09-obstack-execution-prd.md` §6

## Binding context (read before executing anything)

- **D95** — Postgres arrival: ingest owns a SECOND tracked migration set (same class as `services/ingest/internal/migrate/` — tracked, idempotent, runner-per-upgrade per PR #4); web access via `pg` (node-postgres) behind a thin `apps/web/src/server/postgres.ts` mirroring `clickhouse.ts`'s shape, parameterized SQL only (D11 posture); auth = **better-auth** (email+password, sessions in Postgres via the same pool, organization plugin for orgs/members/invites), its DDL emitted via the generate path and CAPTURED into the ingest-owned migration set — better-auth never migrates the database itself; identity shape org→workspaces 1:N, signup auto-creates one org + one workspace; compose gains an exact-pinned `postgres` service in the default profile; the M2 Helm chart is extended IN PLACE (D35 — a second chart is drift); `stack` CI grows in place, never renamed (K3).
- **D96** — Tenancy refit: `clickhouse.ts:12`'s exported `workspaceId` + `OBSTACK_WORKSPACE_ID` are DELETED; the raw query client stops being exportable; `forWorkspace(workspaceId)` is the ONLY way to obtain the telemetry query interface; the scoped executor injects the `workspace_id` binding and REFUSES SQL that lacks the workspace placeholder (runtime tripwire, proven red in tests by removing a predicate); standing suite gains a red-then-green cross-tenant probe; the four signed harnesses (`deploy/compose/smoke.ts`, `trace-checks.ts`, `sdk-checks.ts`, `deploy/helm/obstack/acceptance.ts`) pass their workspace explicitly; S2.3 L1 alias-crossing sweep is a named done-check.
- **D97/D30** — Saved views land in Postgres THIS sprint, workspace-scoped; `apps/web/src/lib/saved-views.ts` + the `obstack.saved-views` key are DELETED outright — no dual store, no import path, discard-never-read (D52).
- **D107** — New CI job, stable name `e2e` (named once, never renamed): the promoted CDP drive (`deploy/compose/exit-browser.mjs` lineage; S2.3 L4 binding: fresh throwaway profile, self-started processes only, refuse loudly rather than clean up) against the compose stack; lands this sprint with the signup + tenancy-disjointness drive; grows one step per later sprint — one drive, one definition (S2.3 L3). Trigger policy decided from FIRST REAL-RUNNER numbers, not local ones (D93/S2.4 L3); the numbers return to the advisor.
- **Scope fence (phase plan §3)**: no key issuance UI, no invites UI, no metering, no billing, no connectors, no Explain. Ingest's API-key lookup does NOT move this sprint (env path stays; D98 moves it in S3.2). Mock mode stays unchanged — **except saved views, which become live-only per D125 (forced by D30/D97 + D114; honest-absence menu copy, no SampleMark)**; wired surfaces keep D13/D21 honesty.
- **User round (2026-08-18, signed)**: U1 local-only M3 (no hosted env); U4 password auth, NO email verification, invites are S3.2 in-app links; U9 stranger protocol ratified = the e2e drive's script.
- **Retro lessons binding here**: lockfile changes via seeded `npm install --package-lock-only` and the diff checked for floats (S2.1 L1); every absence/correlation claim carries a falsification probe (S2.2 L1); cross-tree grep for root-relative + alias-crossing literals (S2.1 L2 / S2.3 L1); CI runs the exact commands the runbook tells humans (S2.1 L3); scripted commands state their fixing values (S2.2 L3); an import-time-ordering API must be documented as such (S2.4 L4).
- **Next.js note**: this repo's Next has breaking changes vs training data — read the relevant guide under `<repo-root>/node_modules/next/dist/docs/` before writing App Router / route-handler / middleware code (repo AGENTS.md, managed block).

## Advisor decisions
<!-- Advisor kickoff D111–D117 (2026-08-18). BINDING for all agents. Full text in the escalations record; operative content here. -->
- **D111 (breakdown)**: waves recut — W1 = T1,T2,T3 · W2 = T4,T6 · W3 = T5,T7 · W4 = T8. T1's embed MUST be a `//go:embed *.sql` glob so T3's `0003_auth.sql` joins with zero edits to T1's files; T3 never touches `migrations.go`. T4 absorbs every consumer of the symbol it deletes (S2.3 L1 — `data.ts:46` re-export, `app/app/layout.tsx`, `app/app/page.tsx`, `search-latency.bench.ts`). T7's real paths are `components/shell/` (TopBar.tsx, SideNav.tsx) and it edits `layout.tsx` AFTER T4 (sequential cross-wave ownership). T8 also DELETES `exit-browser.mjs` + `exit-evidence.sh` when its replacement lands — no dual drive definition, no gap (L5 re-run of record = the e2e drive).
- **D112 (plumbing contract)**: Go driver `github.com/jackc/pgx/v5` pinned **v5.10.0** (measured 2026-08-18; lib/pq refused). Env names, one per value, all trees: runtime DSN **`OBSTACK_POSTGRES_DSN`** (ingest + web identically), test DSN **`OBSTACK_TEST_POSTGRES_DSN`**, boot flag **`OBSTACK_PG_MIGRATE_ON_BOOT`**. Dev creds: user `obstack` / password `obstack_postgres_dev` / db `obstack`; compose maps `127.0.0.1:5432`. **`BETTER_AUTH_SECRET` (+ `BETTER_AUTH_URL` if 1.7.1 requires it, dev default `http://localhost:3000`) are web-process env ONLY — never in compose services or the chart** (compose does not run web; chart has no web workload until M4). Postgres image **`postgres:17.11`** exact pin (Debian base), compose and chart. Migrations layout FINAL: `services/ingest/pgmigrations/0001_workspaces.sql`, `0002_saved_views.sql` (T1), `0003_auth.sql` (T3, captured). Tracking table `schema_migrations` in Postgres (no collision — different store; runner log lines name their store). **No cross-set FKs: `workspaces.org_id` is soft `TEXT NOT NULL`** — our DDL never binds to library-shaped tables; integrity is the signup transaction's property (D117).
- **D113 (forWorkspace)**: `clickhouse.ts` exports exactly `forWorkspace(workspaceId: string): ScopedClickHouse` (throws on empty/undefined/whitespace; no default; no env read) and the type `ScopedClickHouse = { queryRows<T>(sql, params?): Promise<T[]> }`. Raw client + `queryRows` module-private; `workspaceId` export and `data.ts:46` re-export die. Tripwire, two clauses, both red-provable: (i) refuse SQL whose text lacks `{workspace_id:`; (ii) always inject `workspace_id` from scope, refuse caller-supplied conflicting binding. Query functions take the scoped client as first parameter — no ambient state, no module-level workspace. `data.ts` exports **`dataForWorkspace(workspaceId)`** (explicit entry: harnesses/tests/bench — literal argument, NO fallback) and **`dataForSession()`** (product entry: D114 helper → throws `NoSessionError` if null → calls `dataForWorkspace`). Both funnel into the single `forWorkspace` call; mock mode short-circuits before either store is touched.
- **D114 (session + guard)**: `session.ts` exports `getSessionContext(): Promise<{userId; orgId; workspaceId} | null>` — pure resolution, no redirect. Active workspace = the org's only workspace (`ORDER BY created_at, id LIMIT 1`); NO active_workspace column, NO switcher (refused as speculative). Guard is layout-level in `app/app/layout.tsx` (T7): live mode + no session ⇒ `redirect("/login")`. Middleware/proxy REFUSED. **Byte-invariance: mock mode short-circuits on the mode check before any session/Postgres touch — the app must run mock with no Postgres present at all; that invariant is a stated test.**
- **D115 (ws_demo + T8 seam)**: NO `ws_demo` row in Postgres, ever, in S3.1 — `workspaces` governs product identity only; ClickHouse write-side authority stays the env key map until S3.2. T8's drive seeds by DIRECT ClickHouse seed via T5's refitted `exit-seed.mjs` (D71(b) precedent), reading each real workspace id from the DOM the product renders (SideNav's mono workspace line — U9-conformant). Restart-ingest-for-wire-path REFUSED; **the issued-key wire path upgrade is S3.4's pre-registered e2e extension.** Disjointness + negative probe run against the two seeded real ids.
- **D116 (saved_views DDL)**: `saved_views(id TEXT PK app-generated, workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE, surface TEXT NOT NULL CHECK (surface IN ('traces','logs')), name TEXT NOT NULL, filters JSONB NOT NULL, created_at/updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), UNIQUE(workspace_id, surface, name))`. D47/D52 identity contract carries verbatim: trimmed name unique per surface; save-over-name = UPSERT on the unique key; `filters` = URL-shape `Record<string,string>` minus the page param. CHECK, never a Postgres enum type. In-table FK to our own `workspaces` is required — D112's no-FK rule bars only cross-set references.
- **D117 (better-auth)**: pins **`better-auth@1.7.1`**, **`pg@8.23.0`** (measured 2026-08-18), added via seeded `npm install --package-lock-only`, lockfile diff checked for floats. Email+password ON, verification OFF (U4), organization plugin ON (invites UI is S3.2), sessions in Postgres through the one pool, DDL captured via generate — better-auth never applies anything. **Signup contract is a property: success ⇒ user + org + workspace all exist; any workspace-creation failure fails the signup visibly — no half-state; proven red-then-green.** Measured at execution, stated in T3's report: exact generate-CLI invocation + output shape at 1.7.1; org-auto-create plugin option vs own post-signup hook (and its transactional boundary — if none, we enforce via our own transaction + compensating failure); whether `BETTER_AUTH_URL` is required; the session-cookie surface the e2e drive must respect. Any measurement that would change the ruled design is an ESCALATION, not an adaptation.

## Critical items
<!-- Task IDs that must return to the advisor before merge (pre-named by D94–D110; kickoff may add). -->
- T1 — the initial Postgres migration set (runner + our DDL)
- T2 — the chart diff returns with the bundle: first execution of D35's "extended in place" on a stateful workload; the pg-migrate Job's hook ordering beside the ClickHouse migrate Job is contract, not plumbing (advisor-added at kickoff)
- T3 — the captured better-auth DDL + org→workspace auto-create shape
- T4 — the `forWorkspace` scoped-factory design + refuses-unscoped-SQL tripwire
- T5 — the four-harness refit + alias sweep
- T8 — the `e2e` job; its first real-runner trigger numbers return to the advisor
- exit — the two-strangers-disjoint evidence incl. the negative probe (advisor signs the bundle)

## Tasks

### T1: Postgres migration runner + obstack DDL in ingest
- status: fixed
- wave: 1
- depends: —
- owns: services/ingest/internal/pgmigrate/** (new package), services/ingest/pgmigrations/** (new: migrations.go with a `//go:embed *.sql` GLOB per D111 + 0001_workspaces.sql + 0002_saved_views.sql; T3 adds 0003_auth.sql later — do NOT create it), services/ingest/cmd/ingest/main.go, services/ingest/cmd/ingest/main_test.go, services/ingest/go.mod, services/ingest/go.sum, .github/workflows/go.yml
- goal: the ingest binary owns a second tracked migration set for Postgres, exactly the `internal/migrate` class: embedded SQL applied in filename order (glob embed, D111), recorded in a Postgres `schema_migrations` table (D112(d) — runner log lines name their store), idempotent second run, one runner per upgrade; `ingest pg-migrate` subcommand + the `OBSTACK_PG_MIGRATE_ON_BOOT` boot-flag path mirroring PR #4's ClickHouse design; driver pgx v5.10.0 (D112(a)); DDL per D112(d)/D116 — `workspaces` with soft `org_id TEXT NOT NULL` (NO cross-set FK), `saved_views` per the D116 shape verbatim; `.github/workflows/go.yml` gains the Postgres service container + `OBSTACK_TEST_POSTGRES_DSN` so CI runs the integration test (D36 skip-trap — unexpected skips fail).
- result: VERDICT fixed. Done-check green (reviewer re-ran, CI-style skips 0). Reviewer fix: go.yml health-cmd → TCP `pg_isready -h 127.0.0.1` (socket-phase false-ready, compose-documented trap). Executor facts of record: `workspaces` has NO name column (id/org_id/created_at — nothing renders a name; additive 0004 if a reader appears); ingest requires `OBSTACK_POSTGRES_DSN` on both `run` and `pg-migrate` (→ escalated, D118); glob embed proven with T3's 0003 mid-task; skip-trap proven both ways; falsification probes red-then-reverted. Cross-file must-fixes routed to T2: chart ingest Deployment env (+DSN, +OBSTACK_PG_MIGRATE_ON_BOOT=false, README §fix) — blocking, stack would fail; root README.md go-repro block (+postgres, +test DSN). Integration note: `pgMigrateOnBoot()` duplicates `config.envBool` — export/move at integration, not a fix round.
- done-check: `cd services/ingest && gofmt -l . && go vet ./... && go test ./...` green including a pgmigrate integration test against a real Postgres started by the test setup's own documented `docker run postgres:17.11` with stated fixing values (NOT T2's compose — same-wave independence, D111) proving fresh-boot-applies / second-boot-no-op / partial-failure-reports; skip-trap pattern in place
- result:

### T2: Postgres in compose + the M2 chart, extended in place
- status: fixed
- result: VERDICT fixed (4 stale helm-README Init-count/pull-list lines corrected). All priority checks pass: D112 exactness (BETTER_AUTH absent from deploy/ except prose declaring absence), D118 rider attributable end-to-end, D35 fidelity line-for-line vs the ClickHouse pair (helper-file split assessed acceptable), hook peers verified deterministic on install AND upgrade (equal-weight tiebreak = name sort; migrate precedes pg-migrate), stack.yml grows in place with the exact runbook line + verified red mechanism, root README char-for-char vs go.yml, compose default-profile + TCP healthcheck + depends_on healthy; no M4 values leakage. Escalations (visibility, no change requested): settable-but-unrotatable postgres.password pre-M4; install-path refusal race now doubled (accepted ClickHouse class extended; full merged-tree helm install deferred to integration gate as planned) → advisor. Compose stack left UP healthy.
- wave: 1
- depends: —
- owns: deploy/compose/docker-compose.yml, deploy/helm/obstack/values.yaml, deploy/helm/obstack/Chart.yaml, deploy/helm/obstack/templates/postgres/** (new), deploy/helm/obstack/templates/ingest/** (pg-migrate Job extension + Deployment env per T1-review must-fix), deploy/helm/obstack/README.md, deploy/compose/README.md, README.md (go-repro block only, T1-review must-fix — manager-added), .github/workflows/stack.yml
- goal: compose gains a `postgres` service (`postgres:17.11` exact pin per D112(c), default profile, healthcheck, named volume, dev creds + `127.0.0.1:5432` mapping per D112(b)) with ingest wired via `OBSTACK_POSTGRES_DSN`; the M2 Helm chart (D35: IN PLACE, CI-grade, same class as its ClickHouse) gains a Postgres workload plus the pg-migrate pre-install/pre-upgrade Job extension alongside the existing ClickHouse migrate Job; `stack` CI covers the Postgres boot path in place (job never renamed, K3; the signup-path check arrives with T8's e2e job, not here). `BETTER_AUTH_*` enters NEITHER compose services NOR the chart (D112(b)). Code against the D112 contract — the subcommand is T1's, same wave.
- done-check: `docker compose -f deploy/compose/docker-compose.yml config -q` clean; `helm template deploy/helm/obstack | grep -c postgres` shows the workload rendered; `helm lint deploy/helm/obstack` clean; compose up from the capital-cased `/Volumes/SSD/Workspace/...` path shows postgres healthy and ingest still healthy (state exact commands + fixing values, S2.2 L3). The `OBSTACK_PG_MIGRATE_ON_BOOT` exercised check is DEFERRED to the integration gate on the merged tree (D111 — T1's subcommand lands in the same wave)
- result:

### T3: better-auth — signup/login, org→workspace auto-create, captured DDL
- status: fixed
- result: VERDICT fixed. Reviewer fix: signup-page copy fiction (wire-path promise D115 doesn't ship) reworded. Verified hard: no-half-state property end-to-end vs real Postgres (compensating delete + real ON DELETE CASCADE); /sign-up/email 404 blocks all 8 path variants, only user-creating door among 52 mounted endpoints; 0003_auth.sql re-captures byte-identically, complete for runtime; pins exact, lockfile float-free; D112(b)/D114(f) verbatim. Correction of record: production session cookie is `__Secure-better-auth.session_token` (T8 keys off the PRODUCTION name). Escalations E1–E3 → advisor (D119, pending): BETTER_AUTH_URL required-vs-Secure-cookie collision; org-plugin mutation surface = live tenancy break (accept-invitation lands on another org's workspace; delete orphans → permanent 500; create makes org-sans-workspace); ?error= reflected-text channel. Finding 4 (no CI guard tying captured DDL to authConfig — silent desync) routed to integration. `.playwright-mcp` parked at /tmp/claude-501-parked-playwright (pre-existing unrelated red, confirmed genuine).
- wave: 1
- depends: —
- owns: apps/web/src/server/postgres.ts (new), apps/web/src/server/auth.ts (new), apps/web/src/server/session.ts (new), apps/web/src/app/api/auth/** (new), apps/web/src/app/signup/** (new), apps/web/src/app/login/** (new), services/ingest/pgmigrations/0003_auth.sql (new — the CAPTURED better-auth generate output; filename FINAL per D112(d); do NOT touch migrations.go — the glob embed picks it up), apps/web/package.json, package.json, package-lock.json
- goal: better-auth@1.7.1 + pg@8.23.0 (exact pins, D117) configured server-side per D117: email+password ON, verification OFF (U4), organization plugin ON, sessions in Postgres through the one `pg` pool in `postgres.ts` (mirrors clickhouse.ts's shape; parameterized SQL only; reads `OBSTACK_POSTGRES_DSN`); DDL captured via the generate path into `0003_auth.sql`, never applied by better-auth; signup auto-creates one org + one workspace (org→workspaces 1:N, soft org_id per D112(d)) — the D117 signup contract is a PROPERTY proven red-then-green: success ⇒ user+org+workspace all exist, any workspace-creation failure fails signup visibly, no half-state; `session.ts` exports `getSessionContext()` per D114(f) verbatim (pure resolution, no redirect; active workspace = the org's only workspace, `ORDER BY created_at, id LIMIT 1`); minimal honest signup/login pages (match app shell styling; no marketing copy). Dependency adds via seeded `npm install --package-lock-only`, lockfile diff checked for floats (S2.1 L1). Read the Next docs under `node_modules/next/dist/docs/` before writing route handlers/pages. D117's measured items (generate-CLI shape, org-auto-create option vs own hook + transactional boundary, BETTER_AUTH_URL requirement, session-cookie surface) go in your report; a measurement that would change the ruled design is an ESCALATION.
- done-check: `npm test` green from repo root including new unit tests for the auto-create shape (org+workspace created transactionally on signup) and session→workspace resolution; `npm run build` green; the captured SQL file applies cleanly through T1's runner once both land (wave-2 reviewer re-verifies; state the local command used)
- result:

### T4: tenancy refit — forWorkspace scoped factory, env constant deleted
- status: approved
- result: VERDICT approve (incl. D126 delta). Reviewer independently proved: tripwire red both clauses (incl. executing an unscoped statement with clause (i) disabled — probe failed on EMPTINESS, cross-tenant rows named); export surface exactly ["forWorkspace"]; facade one-path with mock short-circuit before the session import; result shapes line-for-line vs master; connection() necessity revert-tested. Process disclosure of record: the reviewer's `git checkout --` tripwire-revert caused the earlier interference (~2min, restored md5-identical, zero residue). Clause (i) text-check limitation noted honestly — not reachable (checked-in constants, D11), live probes are the backstop; no gold-plating. Final tree: tsc clean, 179/0 + 6 known DSN skips (F3's), build green. Sanctioned deploy/ survivors enumerated = T5's exact target list.
- wave: 2
- depends: T1, T3
- owns: apps/web/src/server/clickhouse.ts, apps/web/src/server/data.ts, apps/web/src/server/data.test.ts, apps/web/src/server/queries/traces.ts, apps/web/src/server/queries/logs.ts, apps/web/src/server/queries/overview.ts, apps/web/src/server/queries/traces.integration.test.ts, apps/web/src/server/queries/logs.integration.test.ts, apps/web/src/server/queries/search-latency.bench.ts, apps/web/src/app/app/layout.tsx, apps/web/src/app/app/page.tsx, apps/web/src/server/tenancy.test.ts (new), apps/web/src/app/app/traces/page.tsx (D126), apps/web/src/app/app/traces/[id]/page.tsx (D126), apps/web/src/app/app/logs/page.tsx (D126)
- goal: D96 + D113 verbatim — the exported `workspaceId` constant and `OBSTACK_WORKSPACE_ID` are deleted; `clickhouse.ts` exports exactly `forWorkspace(workspaceId: string): ScopedClickHouse` + the type (throws on empty/undefined/whitespace, no default, no env read); raw client + `queryRows` module-private; tripwire per D113's two clauses, proven RED by removing a predicate then green; every query function takes the scoped client as first parameter; `data.ts` exports `dataForWorkspace(workspaceId)` (explicit, no fallback) and `dataForSession()` (via T3's `getSessionContext`, throws `NoSessionError` on null), both funneling into the one `forWorkspace` call; mock mode short-circuits before either store is touched. T4 absorbs the FULL consumer sweep of the deleted symbol (D111): `app/app/layout.tsx` (delete the import, resolve session workspace via T3's helper, thread to SideNav — the prop survives; SideNav/TopBar internals stay T7's), `app/app/page.tsx`, `search-latency.bench.ts` (refit to `forWorkspace` with its own bench constant). Standing suite gains the red-then-green cross-tenant probe (workspace A requesting workspace B's trace id → empty/404). Every existing query keeps its exact result shape — a scoping refit, not a query rewrite.
- done-check: `npm test` + `npm run build` green from root; the tripwire test demonstrably red-then-green (commit the green form, state the red run in the report); integration tests against compose ClickHouse pass with two seeded workspaces returning disjoint results
- result:

### T6: saved views — Postgres rows, browser store deleted outright
- status: fixed
- result: VERDICT fixed. Reviewer fix: non-object `filters` payloads broke total-parse (null threw; "abc" persisted as char-spread) — guarded to `{}`, tested. Verified at three altitudes: real-Postgres probe 15/15 (FK cascade, UPSERT identity); own red run (unscoped LIST_SQL → 7 red, restored sha-identical); hostile POSTs vs production serve with two real signups (cross-list [], cross-delete survives, forged workspaceId lands own-workspace, no-session null with rows 8→8). D130 addendum verified incl. a reviewer-added connectable-but-unmigrated case (loud failure, never skip). Mock-mode copy honest per D125. Seams: stale localStorage claims at deploy/compose/README.md:137 + exit-evidence.sh:237,240 (same class as exit-browser.mjs:287 — T5/T8 deletion must cover all three); F3 merge-order (recorded); stray untracked apps/web/f4probe.mts is F4's — nobody `git add -A`s it.
- note: manager RATIFIED `apps/web/src/components/saved-views/actions.ts` outside owns ("use server" files expose every export as a public POST, so the workspace-id-taking core cannot live there — same split as the ratified signup pattern). Seam findings: `exit-browser.mjs:287` stale ref (T8 deletes wave 4, known); web.yml-has-no-Postgres gap → proposed F3 (advisor, pending); mock-mode-loses-saved-views fence deviation → advisor (pending).
- wave: 2
- depends: T1, T3
- owns: apps/web/src/lib/saved-views.ts (DELETE), apps/web/src/lib/saved-views.test.ts (DELETE), apps/web/src/server/saved-views.ts (new), apps/web/src/server/saved-views.test.ts (new), apps/web/src/components/saved-views/SavedViewsMenu.tsx, apps/web/src/components/traces/TracesSearch.tsx, apps/web/src/components/traces/TracesSearch.test.tsx, apps/web/src/components/logs/LogsExplorer.tsx, apps/web/src/lib/traces-filter.ts, apps/web/src/lib/traces-filter.test.ts, apps/web/src/lib/logs-filter.ts, apps/web/src/lib/logs-filter.test.ts
- goal: saved views are workspace-scoped Postgres rows (T1's saved_views table) read/written through a server module + server actions; `lib/saved-views.ts` and the `obstack.saved-views` localStorage key are DELETED outright — no dual store, no import path, no read-and-migrate (D30/D52: discard, never read); every consumer (SavedViewsMenu, the traces bar, the logs explorer) goes through the server path; UI may now truthfully say views are workspace-scoped; any literal the deleted module exported (e.g. PAGE_PARAM-strip vocabulary) is re-homed with ONE definition, not duplicated (S2.3 L3). Cross-tree grep proves zero surviving references to the deleted module path and the storage key literal (S2.3 L1 — every tree that can resolve the alias, `deploy/` included).
- done-check: `npm test` + `npm run build` green from root; `grep -rn "saved-views\"\|obstack.saved-views" --include="*.ts*"` across apps/, deploy/, packages/ returns only the new server module's own files; a saved view created in one workspace is invisible to another (unit/integration test)
- result:

### T7: live-mode session guard + real identity in the shell chrome
- status: approved
- result: VERDICT approve, zero fixes needed. Reviewer reproduced everything: four routes 307 with zero error content in bodies (grep-proven), forged/unprefixed cookies refused, Cache-Control private/no-store on both 307 and signed-in shell (reviewer-added check), mock invariance with the guard's whole server graph loaded, real-browser signup→identity→sign-out→failure-honesty drive vs Postgres cross-check. T8 corrections of record: only ONE cookie is set at signup (`__Secure-better-auth.session_token`); sign-out 403 MISSING_OR_NULL_ORIGIN only when a session cookie rides (no-cookie Origin-less → 200; no content-type → 415). Escalated: ~4 Postgres tx per signed-in shell render (unauthenticated ≈ 0 — no scanner amplification; fix one level down in session.ts, D114-frozen — advisor); workspace-switcher affordance (chevron button, no handler) above a real id — S3.5 residue family with the footer.
- note: manager RATIFIED `apps/web/src/app/app/layout.test.ts` outside owns (done-check required the invariance test; no other owner — T3/F1 precedent). T8-relevant measured facts: better-auth 1.7.1 sign-out POST needs `Origin: http://localhost:<port>` (curl → 403 MISSING_OR_NULL_ORIGIN; browsers send it) and clears THREE cookies. S3.5 residue noted: SideNav's "demo workspace" footer now contradicts the real id above it (not widened, per fence). Advisor question routed: per-request server-side NoSessionError log line on unauthenticated hits (a Next 16 layout cannot stop nested segments — client gets clean 307; measured 5 requests → 5 log lines).
- wave: 3
- depends: T3, T4
- owns: apps/web/src/app/app/layout.tsx (guard lines only, sequential after T4 per D111), apps/web/src/components/shell/TopBar.tsx, apps/web/src/components/shell/SideNav.tsx (identity/sign-out lines only — enumerate exact lines touched in the report), apps/web/src/lib/live-routes.ts (only if the registry mechanism needs a hook — else untouched)
- goal: D114(g) verbatim — layout-level guard in `app/app/layout.tsx`: live mode + no session ⇒ `redirect("/login")`; middleware/proxy REFUSED; **byte-invariance: mock mode short-circuits on the mode check before any session or Postgres touch — the app must run mock with no Postgres present at all, and that invariant is a stated test**; the TopBar shows the real signed-in user + workspace with sign-out (replacing exactly the "no signed-in operator until M3 auth" placeholder lines, `TopBar.tsx:39,45`; SideNav's workspace line shows the real workspace — the line T8's drive reads); residue sweeps stay S3.5, do not widen. Read the Next docs under `node_modules/next/dist/docs/` before touching the layout. **T4 seam facts (measured, binding):** the layout's live branch needs `await connection()` before session work or a live `next build` fails at export time (T4 added it — keep it, understand it before editing); and `/app/page.tsx` throws `NoSessionError` in live mode with no session — your layout guard must provably win that race (layout and page render concurrently).
- done-check: `npm test` + `npm run build` green from root; the mock-no-Postgres invariance test green; a live-mode request without a session provably redirects INCLUDING the explicit no-cookie live check against a running production serve on ALL FOUR NoSessionError-throwing routes — `/app`, `/app/traces`, `/app/traces/<id>`, `/app/logs` (post-D126 the pages throw too; the layout guard must win each race — T4-review finding 5; state fixing values)
- result:

### T5: signed-harness refit + alias-crossing sweep
- status: fixed
- result: VERDICT fixed. Reviewer fix: exit-seed.mjs stamped tenancy spread-FIRST so a caller's row override could shadow `--workspace` — flipped to stamp-last (the exact bug class this sprint closes, in the ONE seeding definition T8 inherits). Verified: OBSTACK_WORKSPACE_ID has exactly one repo-wide hit (a comment declaring absence); byte-fidelity value-verified (DEMO_WORKSPACE reproduces HEAD's default exactly); exit-evidence.sh refusal honest + attributable (build/serve/browser provably never execute; label-dead ratified — the ~150 dead lines are the written spec T8's drive carries forward); sweep sole survivor = exit-browser.mjs (T8-deleted); probe re-proven red for the right reason. Stack left healthy; all probe residue cleared. Zero escalations.
- wave: 3
- depends: T4
- owns: deploy/compose/smoke.ts, deploy/compose/smoke.sh, deploy/compose/trace-checks.ts, deploy/compose/sdk-checks.ts, deploy/compose/exit-seed.mjs, deploy/compose/exit-evidence.sh, deploy/helm/obstack/acceptance.ts, deploy/helm/obstack/acceptance.sh
- goal: the FIVE signed harnesses (D111 — the sweep found `exit-evidence.sh` setting `OBSTACK_WORKSPACE_ID` at :32,95,128,132) pass their workspace EXPLICITLY through `dataForWorkspace` (D113; they seed it, they know it); `exit-seed.mjs` refits to an explicit `--workspace` argument — ONE seeding definition, reused by T8's drive (D115); the S2.3 L1 sweep is executed and reported: grep every tree that can resolve `@/server/*` or the deleted `workspaceId` symbol (apps/, deploy/, packages/, services/) with the hit list in the report; harness behavior otherwise byte-identical — same assertions, same output lines, same refuse-loudly posture. (`exit-evidence.sh`/`exit-browser.mjs` are DELETED by T8 in wave 4 — refit here only what keeps wave-3 evidence runnable; do not delete.)
- done-check: `bash deploy/compose/smoke.sh` PASS from clean volumes via the capital-cased `/Volumes/SSD/Workspace/...` path (Docker bind-mount casing trap); `npx tsx --conditions react-server deploy/compose/trace-checks.ts` (with its documented env) green; sweep output attached
- result:

### T8: the `e2e` CI job — signup + tenancy-disjointness CDP drive
- status: fixed
- final: T8-reviewer second pass VERDICT fixed. New finding caught by the re-verify itself and fixed: append-mode process logs inherited a previous run's bytes, making the D132 byte-split read old allowlisted lines as unallowed (78/1 false-red reproduced; `openSync "w"` fix; regression-proved by re-running into the same OUT_DIR — the S2.3 L4 leaked-state class extended to logs). Reviewer's own REAL merge red (bob seeded with alice's label, assertions untouched): 75/4 — counts all green, content claims caught it, red attributes to the right tenant. Weave rule verified in ClickHouse (zero labels on identifiers; the empty carrier row still empty; token intact beside label). Byte-parity three ways; D132 correct with smoke in-job; 79/0 final.
- result: REVIEW: two in-scope L5 clause losses FIXED by reviewer (D69 input-state clause on the forward leg; pod-filter clause on the logs back-check — claims now no wider than assertions); independent red by a different mutation (69/3 with true values printed); all four refusal branches re-fired; byte-exact runbook parity; deleted-file grep zero. MUST-FIX routed: root README.md :123/:125-132/:137-190/:193 still enumerate SEVEN checks — e2e needs the enumeration bump, a bullet, and a repro block (T2's file). ESCALATED: E1 content-blind post-seed probe half (per-actor seed suffix would be strictly stronger; needs T5's exit-seed.mjs + D115 one-definition — advisor); E2 smoke.sh no longer has an evidence run of record (no CI job ever ran it — pre-existing; design call). Advisor-file notes: first REAL-runner number still owed (workflow has never run — by design, lands on the PR); exit-seed.mjs:2 stale prose; commit discipline — T8's deletions unstaged vs T4's staged, `git add -A` at commit time or D111's dual definition survives the tip.
- wave: 4
- depends: T2, T4, T5, T7
- owns: .github/workflows/e2e.yml (new), deploy/compose/e2e-drive.mjs (new), deploy/compose/exit-browser.mjs (DELETE, D111), deploy/compose/exit-evidence.sh (DELETE, D111), deploy/compose/README.md (e2e runbook section only — T2 owns the rest of that file in wave 1; sequential ownership is safe across waves), .gitignore (one entry: the CI traps' `test-output.log` + any drive artifacts — F3-review seam)
- goal: D107 verbatim — a new stable CI job `e2e` (named once, never renamed) standing up the compose stack (with Postgres) and running the promoted CDP drive in the `exit-browser.mjs` lineage under the S2.3 L4 rules (fresh throwaway profile per actor, self-started processes only, refuse loudly rather than clean up): TWO fresh profiles, two signups, each lands in its own auto-created workspace; the drive reads each real workspace id from the DOM the product renders (SideNav's mono workspace line — U9-conformant, D115), seeds telemetry for exactly that id via T5's refitted `exit-seed.mjs --workspace` (direct ClickHouse seed, D115 — the issued-key wire path is S3.4's pre-registered e2e extension; T7-measured drive facts (as corrected by T7 review): the ONE session cookie is `__Secure-better-auth.session_token` (D119; key on it alone); a session-carrying sign-out POST needs `Origin: http://localhost:<port>` else 403; SideNav renders `data-workspace-id` for machine reading; D132 RIDER — any drive log-cleanliness assertion allowlists exactly the NoSessionError line, by name, on unauthenticated-probe steps ONLY, exception named inside the assertion), asserts strict disjointness between the two, and runs the negative probe (A requesting B's trace id → empty/404) in-drive. DELETE `exit-browser.mjs` + `exit-evidence.sh` in this same task — the old drive dies the moment the new one exists (no dual definition; the L5 re-run of record IS this drive). This drive IS the ratified U9 stranger protocol and grows one step per later sprint — one drive, one definition. The job runs the exact commands the runbook documents (S2.1 L3). Your README section also gains the one-line runbook command for F2's DDL drift check (today it lives only in `deploy/compose/ddl-drift-check.mts`'s header — S2.1 L3 routing per F2's report), and your README edit CLOSES the stale claims T6/T5 found: `deploy/compose/README.md:112` + `:137` ("saved views survive a reload"/localStorage — that store is deleted) and `:118-130` (still sells `exit-evidence.sh` as the evidence command — your drive replaces it). Record the first real-runner duration numbers in the report — the trigger policy (every-PR vs label) is the ADVISOR's call on those numbers; wire it every-PR provisionally.
- done-check: the workflow passes on the PR (real runner); locally `node deploy/compose/e2e-drive.mjs` green against a fresh compose stack with stated fixing values; deliberately breaking the disjointness assertion turns the job red (proven-by-failure, S2.0 L1 — state the red run); repo-wide grep shows zero references to the deleted files
- result:

### F1: apply D119–D121 (auth allowlist, owner-pin, error codes)
- status: fixed
- result: VERDICT fixed. Reviewer fix: the 52-endpoint fixture's claimed tripwire didn't exist (two literals in one file) — added a better-auth version pin asserted against package.json before the size pin, proven red at 1.8.0. Verified hard: allowlist fail-closed vs ;-params/mount-case///-prefix/malformed; fixture independently re-enumerated (set-diff empty both ways); codes verified against installed 1.7.1 dist (no enumeration oracle — one code for all three login failure sites); owner-pin matches the row signup writes; D68 corpus + Object.hasOwn prototype-walk closure; D119 clean repo-wide. Full suite GREEN at review time (178/178). Escalations → advisor: D121 text amendment (absent ⇒ no alert — literal text would mandate fiction on clean loads); row-delta matrix by proxy (F3's new web.yml Postgres may upgrade it); INVALID_EMAIL/PASSWORD_TOO_LONG vocabulary coverage.
- note: manager RATIFIED two files outside the enumerated owns — `apps/web/src/app/signup/errors.ts`, `apps/web/src/app/login/errors.ts` (inside T3's original tree; `"use server"` forbids non-function exports so the code→copy map cannot live in actions.ts). Cookie record CORROBORATES D119: production build at localhost with BETTER_AUTH_URL unset sets `__Secure-better-auth.session_token`; the earlier unprefixed observation was itself the downgrade D119 corrected.
- wave: 2
- depends: T3
- owns: apps/web/src/app/api/auth/[...all]/route.ts, apps/web/src/server/session.ts, apps/web/src/server/auth.test.ts, apps/web/src/server/session.test.ts, apps/web/src/app/signup/page.tsx, apps/web/src/app/signup/actions.ts, apps/web/src/app/login/page.tsx, apps/web/src/app/login/actions.ts
- goal: D120's allowlist (exactly get-session + sign-out; 404 with pointer message otherwise) + the promoted mutation-matrix standing test (404 + zero row deltas per refused endpoint, red-provable by removing an allowlist guard); D120's owner-pin in ACTIVE_WORKSPACE_SQL (`member.role='owner'`, tie-break created_at, id); D121's closed error-code enum with page mapping and the D68 corpus on ?error= (value never interpolated). getSessionContext signature/semantics UNCHANGED (T4/T6 consume it concurrently).
- done-check: `npm test` green (matrix test's red run stated); `npm run build` green
- result:

### F2: captured-DDL drift guard as a standing `stack` step
- status: fixed
- result: VERDICT fixed (one finding: stale trigger-policy basis sentence in stack.yml — annotated, next real-runner run re-tests the ~6-min line). Reviewer re-proved red independently (mutation → exit 1 naming both sides + recipe; restored md5-identical); failure-path hygiene proven in code (finally drops scratch DB on green/drift-red/throw-red — 0 residue); guard premise falsified properly (dropping the organization plugin shrinks the generated set — tracks authConfig, not library version); reorder cannot false-negative; D119 clean; scope clean. Notes: guard runs after Stack acceptance (ordering preference, not defect); recipe() couples to 0003's header comment convention (known, no fallback).
- wave: 3
- depends: T2, T3
- owns: .github/workflows/stack.yml, deploy/compose/ddl-drift-check.mts (new, ONLY if an inline step is unreasonable — no other files)
- goal: D122 verbatim — after compose is up: create scratch DB on the compose Postgres, run `getMigrations(authConfig()).compileMigrations()` against it (generator needs an empty DB — measured), diff vs `services/ingest/pgmigrations/0003_auth.sql`'s non-comment body, drop the scratch DB; step fails on any diff.
- done-check: `stack` green with the step; proven red against a deliberately mutated authConfig or altered 0003 (state the red run)
- result:

### F3: web.yml DSN at the compose Postgres (D130 shape; was "service in web.yml" per void D124)
- status: fixed
- result: VERDICT fixed (comment-only: the trap's premise sentence generalized falsely — now names both conditions: ClickHouse tests skip on no-answer, Postgres tests skip ONLY on unset DSN and FAIL on dead). Reviewer proved: no services key, one schema path, --wait means healthy AND migrated (ensurePGSchema precedes the /healthz listener in main.go — from code, not comment); 199/199 0 skips green; own red run 11 ::error:: exit 1 (exactly the D130 set); collision note re-proved verbatim; no third DSN consumer. Escalated seam → T8: `test-output.log` written to repo root, uncovered by .gitignore (pre-existing, also true of go.yml).
- merge-ordering fact (T6 delta, binding on integration): with T6's integration test on the tree, `web.yml` goes red on 6 unexpected skips until F3 lands the DSN — **F3 is the closer; the PR tip must carry F3.** (Verified: DSN set ⇒ zero skips, 185/185.)
- result: BLOCKED, zero files changed (correct call). Measured: the three named test files are fake-client unit tests — every pg import is `import type`, nothing reads OBSTACK_TEST_POSTGRES_DSN; live/dead/unset DSN all give identical 26/26 and 178/178 with 0 skips; Postgres-free is a WRITTEN property in each file (comments cite the skip-trap). Real-Postgres runs (T3/T6/F1) were manual. Also measured: a go.yml-shaped `5432:5432` service container collides with compose postgres (web.yml:48 boots ingest → depends_on postgres) — bind failure at step 1. Executor's rider answer if unblocked: NO service container — reuse the compose postgres this job already boots (schema via ingest's boot migrator = the checked-in files, zero second schema path, K1).
- wave: 2
- depends: T1, T3, T6
- owns: .github/workflows/web.yml
- goal: D124 verbatim — web.yml gains the postgres:17.11 service container (D112(c) pin) + `OBSTACK_TEST_POSTGRES_DSN`, T1's go.yml shape exactly (incl. the TCP `pg_isready -h 127.0.0.1` health-cmd — the socket-phase false-ready trap); the zero-skip trap enforces the new Postgres-backed tests run (unexpected skip fails). RIDER: schema source = checked-in `services/ingest/pgmigrations/*.sql` in filename order — never a second schema definition; state whether a psql step or the tests' own setup applies them, and why.
- done-check: workflow YAML parses; locally reproduce the job's env against a fresh postgres:17.11 container and show the previously-unguarded Postgres-backed tests (saved-views tenancy, session/auth) RUN (0 skips) and pass; skip-trap proven by pointing the DSN at a dead port (tests skip → job-fail mechanism fires)
- result:

### F4: real-Postgres integration tests — auth/session + the D120 row-delta matrix (D130)
- status: executing
- wave: 3
- depends: T3, F1, F3-ruling D130
- owns: apps/web/src/server/auth.integration.test.ts (new), apps/web/src/server/session.integration.test.ts (new) — file split at executor's discretion within NEW `apps/web/src/server/*.integration.test.ts` only; touch nothing existing
- goal: D130(2)+(3) — the real-Postgres halves of the auth/session verifications (D117 transaction legs incl. compensating delete; owner-pin behavioral scenario with a seeded member-role row) as D36-class integration tests (self-skip ONLY when OBSTACK_TEST_POSTGRES_DSN unset, documented pattern; genuinely dials — dead port = visible failure); plus the D128-shape row-delta matrix: real session via the signup server action, POST every refused endpoint from the D120 allowlist's complement, assert 404 AND before/after row counts byte-equal across all auth tables + workspaces, red-provable by removing an allowlist guard.
- done-check: with a live DSN: tests RUN (0 skips) and pass; DSN unset: documented self-skip; dead-port DSN: visible failure (state all three runs + fixing values); matrix red run stated
- result: exec done — ONE file (auth.integration.test.ts; node --test parallelism measured — a second Postgres-writing file perturbs the matrix counts). 9/9 live / 5 skips unset / 6 loud dead-port; root suite 196/196 with DSN. Matrix red proven + a PERMANENT falsification test (guard bypassed → 200 + row deltas) keeps the 404s honest. Deviation → advisor: workspaces counted run-scoped not whole-table (T6's integration file writes it concurrently; measured 2 spurious reds in 22 runs, 0 in 15 after). Server action unimportable under react-server (measured) — drives signUpWithWorkspace + mints the cookie via signInEmail asResponse. REVIEW: VERDICT fixed — D131 rider comment written by reviewer; reviewer re-proved all three DSN modes + matrix red + a SECOND induction mechanism (CHECK(false) NOT VALID) showing zero-rows is the code's property; scoping soundness proven from the library itself (no better-auth dist file touches `workspaces`). Measurement-of-record note (manager): the flake numbers of record are the F4 report's (2-in-22 → 0-in-15); the in-file ":110 comment says 1-in-10" — correct that line at the next touch (integration note, not a round). f4probe.mts does not exist (already gone).

### F5: D129 vocabulary — invalid-email, password-long
- status: approved
- result: VERDICT approve, zero findings. Flip verified at dist level (sign-up's INVALID_EMAIL arm shadowed by z.email() body schema → VALIDATION_ERROR; sign-in throws INVALID_EMAIL); 8/128 pins = installed defaults, test-asserted; D68 totality over enlarged vocabularies; no creep (mtime-verified). Advisor file note: signup's INVALID_EMAIL arm is UNREACHABLE at 1.7.1 (documented, kept as defense-in-depth — not live coverage). Escalated: `codeFor` mapping has no standing test ("use server" modules can't export it — closing means moving the mapping into errors.ts; gap pre-exists from F1).
- note: manager RATIFIED `app/login/actions.ts` edit beyond the enumerated owns (F1-owned file; a vocabulary entry without its mapping is dead copy). **Measured flip of record: at 1.7.1 INVALID_EMAIL is raised on SIGN-IN; signup's bad email arrives as VALIDATION_ERROR with a `[body.email]` prefix** — both surfaces mapped, vocabulary still grows by exactly two codes; unattributable VALIDATION_ERROR keeps the generic sentence + log line. Final vocabularies and the 8/128 pins are test-enforced. Suite: 179 pass / 6 skipped (integration files, DSN unset — F3 closes).
- wave: 3
- depends: F1
- owns: apps/web/src/app/signup/errors.ts, apps/web/src/app/signup/actions.ts, apps/web/src/app/login/errors.ts (only if login needs a mapping change — else untouched), apps/web/src/server/auth.test.ts (mapping-test lines only)
- goal: D129 verbatim — add exactly `invalid-email` and `password-long` with fixed copy mirroring `password-short`; map from the measured 1.7.1 codes (INVALID_EMAIL, PASSWORD_TOO_LONG); the D121/D127 property governs (fixed copy only, value never rendered; absent ⇒ no alert). No further widening.
- done-check: scoped auth tests green incl. new mapping cases; `npm run build` green
- result:

### F6: codeFor extraction + standing tests (D133)
- status: fixed
- result: VERDICT fixed. Reviewer fix: totality corpus count pinned at the TRUE 38 (was a >30 floor — the only guard over the 17 unmapped library codes; nine could leave unnoticed, S2.0 L1). Record correction (manager): §F6's "39 entries" was a miscount — 38 is the number of record (the 15th D68 entry is an array the string filter drops). Extraction proven pure/total; console.error split proven sound by construction (generic codes produced only by the fallback arm — invariant survives extension); all 17 unmapped codes verified genuine 1.7.1 base codes; arm-deletion red re-proven independently on a different arm in a different file. 199/199 vs fresh Postgres. Zero escalations — D133's gap is closed.
- wave: 3
- depends: F5
- owns: apps/web/src/app/signup/errors.ts, apps/web/src/app/signup/actions.ts, apps/web/src/app/login/errors.ts, apps/web/src/app/login/actions.ts, apps/web/src/server/auth.test.ts (test lines), apps/web/src/server/auth.integration.test.ts (the :110 flake-comment line ONLY)
- goal: D133 verbatim — move `codeFor` (all arms, both surfaces) into `errors.ts`; actions import it; unit tests assert every arm directly + totality (unknown → generic), red-provable by deleting an arm; correct the F4 in-file flake comment to the record (2-in-22 → 0-in-15). No interface changes.
- done-check: scoped auth tests green incl. the new arm tests + one stated arm-deletion red; `npm run build` + tsc green
- result:

### F7: per-request session memoization (D134)
- status: approved
- result: VERDICT approve, zero fixes. Reviewer's contrast measurement is the proof of record: /app (two readers) and /app/settings (one reader) show IDENTICAL per-render read counts — the second reader costs zero — and the residual is confirmed as layout.tsx:33's profile getSession (T7's file, D134(b) refused widening SessionContext). Cross-request: 20 parallel two-tenant renders → exactly 20 owner-pin reads, 10/10 own + 0/10 other both tenants. Unauthenticated control: 0 store reads. Tripwire falsified against a memoizing stub (fires correctly); its scope honestly stated (asserts installed React's outside-render behavior; in-render isolation covered by the live probe). Pass-through verified in the PRODUCTION react build, not just dev. Correctness bonus on the record: the memo also kills the mid-render session-expiry inconsistency.
- wave: 4
- depends: T7
- owns: apps/web/src/server/session.ts (+ its test file for the isolation proof if needed: apps/web/src/server/session.test.ts)
- goal: D134(1) verbatim — cache()-wrapped per-request resolution; exported signature/semantics byte-unchanged.
- done-check: reviewer's measurement re-run showing ~1 Postgres tx per signed-in /app render (stated fixing values); per-request isolation proven (shared within one request, never across requests); scoped tests + tsc + build green
- result:

### F8: distinct-content seeding + content-aware drive assertions + the smoke floor (D135/D136)
- status: executing
- wave: 4
- depends: T5, T8
- owns: deploy/compose/exit-seed.mjs (sequential after T5's closed cycle), deploy/compose/e2e-drive.mjs, .github/workflows/e2e.yml, deploy/compose/README.md (e2e section lines only, if the documented commands change)
- goal: D135 + D136 verbatim (see Escalations log) — seed parameter, content-aware assertions in the both-populated steady state, simulated-tenant-merge red, stale :2 prose fix; the e2e workflow gains the `bash deploy/compose/smoke.sh` step with executor-arranged, stated sequencing vs the drive's stack.
- status: fixed (reviewed in T8's second pass)
- done-check: full drive green from a cycled stack incl. the new content-aware claims + the smoke step run locally in the job's documented sequence; the tenant-merge red stated; refusal branches still fire; runbook parity byte-exact
- result: exec done — 72→79 claims; composite sequence boot 13s + smoke 17s + drive 54s = 84s, twice. **The tenant-merge red is the finding of record: all four count claims stayed green under the merge (same ids ⇒ every total unmoved); only the content-aware claims caught it** — D135's premise proven in one run. Weave rule verified in ClickHouse (content fields only; never identifiers — exact-match legs depend on them; never empty fields — the D51(e) carrier leg survives). Sequencing stated: boot→smoke→drive, attribution-ordered, nothing destroys volumes; smoke's ws_demo is a third tenant neither stranger sees. Routed: two stale README spots (:129 composite 84s + three lines; :175-179 repro block) → T2's executor, exact wording in scratchpad. Review pending (single pass: F8 diff + both README deltas).

## Escalations log
<!-- Manager appends: {T#} — question → advisor decision (one line each). -->
- kickoff — breakdown + 10 open decisions → advisor D111–D117 (recorded under Advisor decisions; waves recut to 4, T2 added to critical items, fifth harness found, all pins measured)
- T1 — reviewer design fork: must ingest hard-require `OBSTACK_POSTGRES_DSN` on `run`? → **D118 RATIFIED verify-and-refuse** on both `run` and `pg-migrate` (D95(e) pre-answered — no Postgres-less boot exists from S3.1 on; optional-on-run = the ambient-fallback shape D96 refuses; boot verification is `helm install --wait`'s only Postgres gate). Rider binding on T2's re-verify: the refusal is loud and NAMES `OBSTACK_POSTGRES_DSN` (T1's current error text satisfies this).
- T3 — two test files created outside the owns list (`apps/web/src/server/auth.test.ts`, `session.test.ts`) — manager RATIFIED: done-check required them, no other task owns the paths; added to T3 owns of record.
- T3 — reviewer escalations RULED **D119–D122** (recorded here as decisions of record; wave 2 released):
  - **D119 (E1)**: `BETTER_AUTH_URL` NOT set in any local/CI env (D112(b)'s dev-default clause CORRECTED — measured not required; setting http:// downgrades the production cookie). Canonical app origin everywhere: `http://localhost:<port>`, never `127.0.0.1` (contract line, swept like a literal; compose port BINDINGS unaffected). Canonical session cookie for the e2e drive: `__Secure-better-auth.session_token` (drive serves a production build at localhost). M4 pre-registered: hosted env sets BETTER_AUTH_URL to the real https origin.
  - **D120 (E2)**: `route.ts` flips blocklist→**ALLOWLIST: exactly `get-session` + `sign-out`**; every other endpoint 404s. Reviewer's mutation matrix PROMOTED to a standing test (each refused org-plugin endpoint: 404 + zero row deltas; red-provable). D114(f) amended: `ACTIVE_WORKSPACE_SQL` pins to the org where `member.role='owner'` (tie-break created_at, id). `resolveSessionContext` throw on workspace-less user RATIFIED as tripwire (impossible state fails loud). S3.2 widens the allowlist deliberately, door by door — pre-registered breakdown material.
  - **D121 (E3)**: `?error=` becomes a closed code enum (executor states final enum); pages map code→fixed copy, unknown/absent → one generic sentence; the query value is NEVER interpolated. D68 hostile corpus applies to `?error=` on both pages.
  - **D122 (finding 4 + dispatch)**: capture-vs-config drift guard = STANDING `stack` step (scratch DB on compose Postgres → compileMigrations → diff vs 0003's non-comment body → drop) as fix task **F2** after T2 merges. Code changes for D119–D121 = fix task **F1** (T3-owned files; `getSessionContext` interface untouched) — wave 2 dispatches concurrent with F1.
- T6 seams RULED **D124/D125**: F3 ratified (web.yml Postgres service, T1's go.yml shape; RIDER: schema source = the checked-in `services/ingest/pgmigrations/*.sql` applied in filename order — never a second schema definition; psql-step or test-setup application both acceptable, executor states which). Mock-mode honest absence ratified AS-BUILT (D125 — a "saved" view persisting nowhere is worse fiction than absence; no SampleMark needed; fence line amended with the signed exception). (b) mock-only in-memory store REFUSED.
- D126 application note: T4's delta complete (3 pages converted, call-throughs deleted; facade read surface = exactly dataForWorkspace/dataForSession/dataForSessionContext; T4 scope green incl. tenancy 9/9). One un-sanctioned knock-on found: T6's `TracesSearch.test.tsx` imported the deleted `searchTraces` (4 lines) — routed into T6's open cycle (its file). Evidence-hygiene incident of record: a concurrent agent briefly restored pre-T4 `clickhouse.ts` onto disk mid-measurement (14 spurious failures); clean re-run verified — reviewers seeing `forWorkspace is not a function` are looking at interference, not T4.
- T4 call-through question RULED **D126**: option (a) — T4's owns extends to the 3 pages (traces, traces/[id], logs); pages call `dataForSession()` directly; the `getTrace`/`searchTraces`/`searchLogs` call-throughs DELETED, riding T4's open review cycle (D60/D65 small-ordered-edit precedent). Consequences of record: T5's harness refit becomes mandatory-by-compile (trace-checks/sdk-checks stop resolving — the wave-2→3 intermediate red is BY DESIGN, never reaches CI at the PR tip; nobody shims it); the wrappers were latent runtime traps for harnesses anyway (dataForSession outside a request context throws, invisible to npm test). T7 spec insertions RATIFIED (connection() seam fact; guard-beats-NoSessionError as a proven-order done-check).
- F1 escalations RULED **D127–D129**: D121 text AMENDED of record ("any value PRESENT but outside the vocabulary renders one generic sentence; an absent code renders no alert") — implementation was right, sentence was wrong; reviewer's version-pin tripwire ratified. D129: vocabulary grows by exactly `invalid-email` + `password-long` (reachable codes whose generic copy invites impossible retries — D13 in miniature); no speculative widening. **D128 (row-delta matrix upgrade) was ruled on the pre-correction premise ("F3 gave web a CI Postgres" — F3 is in fact BLOCKED); re-ruling in flight — matrix work HELD until it lands.**
- F3-blocked re-ruling **D130**: D127/D129 STAND; D124's mechanism + D128's placement VOID (ruled on my erroneous "F3 landed" report — advisor's own bad-citation rule). e2e-as-only-guard REFUSED (trigger policy undecided until real-runner numbers; browser altitude wrong for SQL-level predicates). Ruling: (1) the unit files' Postgres-free property RATIFIED as written — nobody edits it; (2) real-Postgres halves become `.integration.test.ts` files, D36 class (self-skip only when `OBSTACK_TEST_POSTGRES_DSN` unset; CI sets it + forbids skips — the trap now genuinely dials); (3) T6's disjointness half → `saved-views.integration.test.ts` riding T6's open cycle; auth/session halves + the row-delta matrix (real session via signup action; refused-endpoint POSTs; before/after counts byte-equal across all auth tables + workspaces; red-provable) → **F4** (new files); D129's two codes → **F5** (F1's files, original executor); (4) **F3 UNBLOCKED in the rider shape RATIFIED verbatim** — NO service container; web.yml sets the DSN at the compose Postgres the job already boots; ingest's boot migrator is the ONE schema path (K1); F3 depends on T6's cycle + F4; done-check includes one attributable red run; (5) go.yml service-container asymmetry is correct topology, not drift.
- T8-review escalations RULED **D135–D136**: F8 ordered — `exit-seed.mjs` gains a distinct-content parameter (per-actor suffix woven into content fields; ONE seeding definition with a stated fixing value — D115 satisfied, not bent); the drive's disjointness + negative-probe assertions become content-aware in the both-populated steady state (each actor's results carry only their own suffix — RATIFIED as the assertion shape of record; the outer guard must not share assumptions with the inner tripwire); one attributable red proving the content assertion catches a simulated tenant merge; stale exit-seed.mjs:2 prose corrected same touch. **D136: the smoke floor joins the `e2e` workflow** — a step running `bash deploy/compose/smoke.sh` verbatim (S2.1 L3); manual-only REFUSED (L5 coverage-rot); `stack` stays unpolluted (K5/D41); sequencing executor's to arrange and state. Consequence accepted: the owed first real-runner number prices smoke+drive together and the trigger policy is decided on that composite (S2.4 L3). Commit-discipline check ratified as a stated integration-gate item.
- T7-review escalations RULED **D134**: (1) F7 ordered — `cache()`-memoized per-request resolution INSIDE session.ts (signature/semantics byte-unchanged; one resolution per request, N readers share; kills the amplification CLASS before S3.2 multiplies readers AND the mid-request inconsistent-state hazard); (b) widening SessionContext and (c) deferral both REFUSED. F7 done-check: ~1 tx per signed-in render re-measured + per-request ISOLATION proof (two readers in one request share; two requests never do — a cross-request leak is a tenancy bug, not optional). Only F7's diff reviews. (2) Switcher affordance + demo-footer contradiction PRE-REGISTERED to S3.5's per-surface sweep enumeration by name.
- F5/F4 batch escalation RULED **D133**: F6 ordered — `codeFor` (both surfaces' arms incl. F1's pre-existing ones) moves into `errors.ts` beside the vocabulary (S2.3 L3/D75 class: pure mapping trapped in a "use server" shape that forbids export); actions.ts imports it; unit tests assert every arm + the totality property (unknown code → generic), red-provable by deleting an arm; the shadowed INVALID_EMAIL arm's coverage stops depending on live reachability. Same touch corrects F4's in-file flake comment to the record's numbers ("a comment asserting a wrong measurement is a small lie with a long life"). (b) pre-registration into S3.2 REFUSED (D126 ground — S3.2 inherits a tested mapping, doesn't create one mid-surgery). Only F6's own diff re-reviews.
- T7 escalation RULED **D132**: the per-request server-side `NoSessionError` log line on unauthenticated hits is SPEC, not defect — the D114 tripwire working as designed behind a guard that wins the response (defense-in-depth); (b) guard duplication and (c) a quieter-log seam both REFUSED. **RIDER binding on T8/e2e and any log-grepping harness: allowlist exactly this line — by error name, on unauthenticated-probe steps ONLY, exception named inside the assertion** (D60 precedent: a new error class, or this error on an authenticated path, still goes red). Hosted-volume fix pre-registered as an M4-planning note, not built now.
- F4 deviation RULED **D131**: run-scoped `workspaces` counting RATIFIED as the D128 reading of record (whole-table on a shared parallel DB = false coupling to other tests; measured 2-in-22 flake → 0-in-15; a guard red on unrelated concurrency trains re-running reds). Serialization REFUSED. Composite closes the theoretical hole (org-plugin paths touching workspaces also write org/member rows — whole-table-caught; the falsification test proves live ammunition). RIDER: the file carries a comment stating the two-tier scoping so no future "simplification" in either direction survives review.
- T2 — visibility escalations RULED **D123**: (1) `postgres.password` stays install-time-fixed, no-lookup-bridge RATIFIED (a lookup shim = compat layer for an unclaimed capability); **pre-registered M4-GATE: password rotation story lands with M4's self-hosted values surface**. (2) doubled install-path refusal race ACCEPTED (signed ClickHouse design extended; loser names what it waits on); condition reaffirmed — merged-tree `helm install --wait` at the integration gate must show both Jobs completing and ingest converging on a single install; chart diff returns with the exit bundle.

## Integration
- verdict: **FIXED-GO** (round 1, 2026-08-19)
- rounds: 1
- e2e: npm test no-DSN 189/0/11-documented-skips + with-DSN 204/204/0 · go suites both DSNs 153 PASS/0 SKIP · build+tsc clean · cycled-stack three-line sequence: smoke PASS 18s + drive 79/0 50s (composite 80.1s) · ddl-drift PASS · post-drive Postgres: 2 stranger workspaces, 0 ws_demo rows (D115), 1 saved_views row · **helm/D123: first full-chart merged-tree install — both migrate Jobs Complete 1/1 (65s), both ingest replicas Running after RESTARTS 2 = the accepted refusal race, loser naming its wait verbatim; acceptance PASS 174s**
- seam findings: T2-deferred boot-flag exercised clean on merged tree (refusal names flag+fix; pg-migrate applies all three; `ture` refused; D118 attributable) · F8 weave + T5 stamp = disjoint layers, no shadowing · D126 facade surface conforms everywhere, zero live hits on deleted symbols · D133 split as-designed
- fix tasks: integrator fix 1 (in-charter): `config.EnvBool` exported, `pgMigrateOnBoot` delegates — 11 dup lines gone, semantics byte-identical, proven live
- commit-time record: **foreign out-of-band waitlist feature in tree (mtimes mid-run, no task owns it)** — 6 files EXCLUDED from the sprint commit (waitlist.ts/.test.ts, server/waitlist.ts, WaitlistForm.tsx, marketing/actions.ts, app/page.tsx); `apps/web/package.json` + `package-lock.json` staged WITHOUT the `@vercel/blob` subtrees (hunk-level); T8's two deletions staged; gitignored scratch stays out; parked playwright dir stays parked

## Goal check (Step 7, one entry per iteration)
- iteration 0: **MET (2026-08-19) — advisor clause-by-clause verdict of record; 0/3 iterations used.** Every clause of the run-goal line at the top of this file is met on evidence, no clause carried and none narrowed:
  1. **"the stack boots with Postgres in compose"** — cycled from `down -v`: `up -d --wait` exit 0 (12.2s), postgres healthy **in the default profile**, smoke PASS (18.0s) on that same stack; volumes exactly `clickhouse-data` + `postgres-data`.
  2. **"…and in the M2 chart"** — virgin-kind `helm install --wait`: deployed, **both** migrate Jobs Complete on one install, both ingest replicas converged through the D123-accepted refusal race with D118-conformant naming; acceptance PASS 174s. **D35 held — the chart was extended in place, its first real test**, with `BETTER_AUTH_*` absent from `deploy/` by design (D112(b)).
  3. **"a stranger signs up and lands in their own auto-created workspace"** — 79/79 drive claims across **two fresh CDP profiles, two signups**; post-drive Postgres shows exactly 2 `workspaces` rows under 2 distinct orgs and **zero `ws_demo` rows** (D115); the D117 no-half-state property proven **red-then-green end to end**.
  4. **"every telemetry surface reads that workspace instead of an env constant"** — `clickhouse.ts` exports exactly `forWorkspace` + `ScopedClickHouse`; four product pages run on `dataForSession()`; four harnesses + the bench run on explicit-workspace entries with **one** `DEMO_WORKSPACE` definition; **`OBSTACK_WORKSPACE_ID` → 0 hits repo-wide**; the unscoped-SQL tripwire proven **red four ways**; content-aware disjointness plus the in-drive negative probe (D135) green in the both-populated steady state.
  5. **"and saved views live in Postgres with the browser store deleted"** — a real `saved_views` row written **through the product** by the drive; `lib/saved-views.ts` + its test deleted at the tip (verified staged); the module path and the `obstack.saved-views` key survive only in prose declaring the deletion; **D30/D52 discard-never-read held**; mock-mode absence is the ruled D125 exception.
- **ADVISOR SIGNATURE: S3.1 exit bundle SIGNED at `0bea93a` (2026-08-19).** Eight checks green at the signed tip — web 3m16s · go 2m34s · kind 1m39s · stack 5m10s · **e2e 4m12s** · sdk-py 35s · sdk-js 52s · sdk-e2e 3m46s. Commit hygiene verified: sprint pins only, float-free lockfile, `@vercel/blob` absent, T8's two deletions in the commit. Advisor correction of record: **the every-PR set is eight checks** (M2's seven + `e2e`), not the "nine" echoed in a condition line. **PR #14 MERGED to `master` @ `9ba55bb`; planning commit @ `987ea1b`.** Bundle recorded verbatim at `.planning/2026-08-19-s3.1-exit-evidence.md`. *(The commit-time exclusion recorded under `## Integration` was resolved by the user landing that work as its own reviewed PR #15, `master` @ `73798f6` — see the phase-plan and vision run logs; it was never sprint scope.)*
- **Standing guards left armed by this sprint** (they outlive it and bind later M3 runs): the unscoped-SQL tripwire; the cross-tenant probe; the **D120 allowlist matrix with its live-ammunition falsifier**; the **D122 ddl-drift `stack` step**; the D68 corpus; and the **smoke floor + drive inside `e2e`, every-PR per D137**.
- **Handed to S3.2's kickoff as a binding inheritance list** (advisor's wording, in the bundle): **D29 cap + `as_of` — BLOCKING, merges inside S3.2 before any second workspace's data can arrive** (D97 placement; the one hard ordering constraint); **the D98 key move whole** — prefix + deterministic SHA-256 storage, shown-once, ingest read-through cache with bounded TTL and fail-static behaviour, revocation ≤TTL, **`OBSTACK_API_KEYS` DELETED with no dual lookup path**, the `ok_dev_local:ws_demo` continuity row seeded through the migration/seed path (compose `:48`, chart `values.yaml` apiKeys + collector apiKey literals all touched; the S2.3 L1 sweep is a named done-check); **D120 widens door-by-door**, each new door carrying its stated transactional property, owner-pinned resolution inherited — accepting an invitation must be **additive, never a re-home** (the measured tenancy break stays impossible), invites being U4 in-app copyable links with the unused `invitation` table as schema of record; **D106** — `/signup` landing CTA goes live and settings registers under the per-section badge rule (General/Members/API keys real; Billing & usage `SampleMark`'d until S3.3; Audit log/Compliance through M3); **the `e2e` drive grows its S3.2 step in place** (one drive, one definition, D107), with the full issued-key wire path still pre-registered to S3.4 (D115); and **pattern inheritance** — the D121/D127/D129 error-code vocabulary for every new form, the D68 corpus on every new contract module's parse (D65 standing clause), and the ddl-drift guard catching any `authConfig()` change S3.2 makes.
- **Residue pre-registered rather than absorbed (NOT S3.2 work):** **S3.5** — switcher affordance + demo-workspace footer (D134), palette/changelog (D60/D63), TopBar residue. **M4** — `BETTER_AUTH_URL` in the hosted env inventory (D119), the `postgres.password` rotation story (D123), the NoSessionError log-volume note (D132).

## Retro (filled at end of run)
- tasks: 16/16 approved (T1–T8, F1–F8), deferred: none
- breakdown corrections by advisor: kickoff D111–D117 (T4/T7 recut, wave added, fifth harness found, two plan defects caught pre-code) + 20 mid-run rulings D118–D136 + D137 at exit; one advisor self-void (D124/D128 on the manager's premature "F3 landed" report — corrected same round)
- reviewer verdicts: 3 approve / 8 fixed / 1 must-fix (cross-file, routed and closed)
- lesson: count-based tenancy assertions are blind to a tenant merge — content-aware claims are the last line's only honest ammunition (F8 red: all counts green under a simulated merge); and a mounted library surface is a liability inventory — allowlist fail-closed, promote the discovering probe to a standing matrix.
