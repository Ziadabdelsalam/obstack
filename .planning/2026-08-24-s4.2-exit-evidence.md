# S4.2 — exit evidence — obstack M4 sprint 2: retention enforcement and the launch receivers

meta:
- date: 2026-08-24
- branch: `s4.2-ingest-launch-hardening`, base `master` @ `76c1121` (S4.1, PR #21)
- commits: `690cc77` (T1–T6) · `9316256` (T7) · `b56f0a9` (planning) · `7f01230` (review round 1) · `bf4ecbb` (panic dialect) · `79d16ed` (escalation deltas D293–D297) · `1ad55d4` (integrator gate) · this bundle
- diff: 51 files, +3539 / −85
- mode: manager-executes; three batched Opus reviewers (retention / receivers / windowed-rate), one advisor kickoff round + one escalation round, one integrator gate
- rulings applied: **D252, D254, D260** (phase plan, binding) · **D285–D292** (kickoff) · **D293–D297** (escalation)

---

## 1. The exit criterion, clause by clause

> *"retention-tier TTLs are enforced on the D7 tables per workspace plan and the settings disclaimer is removed in the same change, and the Vercel log-drain receiver and CloudWatch forwarder artifact exist, CI-exercised against local requests, still honestly 'Coming soon' in the hub until S5 activates them."*

| clause | state | proof |
|---|---|---|
| tier TTLs enforced on the D7 tables per workspace plan | **MET** | `internal/retention` sweep; `TestSweepEnforcesBothDirectionsOnTwoTiers` passes against real ClickHouse + Postgres — free (7d, resolved through D163's absent-row rule) keeps 1 of 3 seeded rows, pro (30d) keeps 2 of 3, on `spans`, `logs` **and** MV-populated `trace_summaries`; a live sweep on the compose stack logged `failed_statements:0` |
| …in the same change as the disclaimer removal | **MET** | `SettingsSuite.tsx`'s "TTL enforcement lands at M4…" sentence deleted in commit `690cc77`, the same commit as the sweep. Grep: zero occurrences repo-wide. The orphaned second site (`0005_metering.sql`'s catalog comment, named by phase-plan ledger row 1) corrected in `7f01230` |
| Vercel log-drain receiver exists | **MET** | `POST /v1/integrations/vercel`; JSON array, vendor object-sequence, NDJSON, gzip; optional HMAC-SHA1 signature over raw pre-gunzip bytes |
| CloudWatch forwarder artifact exists | **MET** | `deploy/cloudwatch-forwarder/` — dependency-free single-file Lambda relay + `dist/cloudwatch-forwarder.zip` built by `npm run package`, handler at zip root (extracted and imported by the integrator) |
| CI-exercised **against local requests** | **MET** | `internal/receive/integration_test.go` POSTs the captured fixtures over real HTTP into a receiver wired to the **real batch writer**, asserting rows in `obstack.logs`; runs in the `go` job under the K4 skip trap (**0 skips**). The forwarder's live leg POSTs through the deployed route in the `web` job |
| still "Coming soon" in the hub | **MET** | `connectors.ts` both cards `status: "coming-soon"`; zero diff vs master on the status fields. The stale "no receiving component yet" comment was corrected to state why they stay unflipped (D101/D208: the hosted endpoint is S5's) |

---

## 2. Measured facts (S2.4 L1 — measured at breakdown, never believed)

Against the compose-pinned `clickhouse/clickhouse-server:26.3.17.110`, the three real migrations, ~2.1M seeded span rows + ~1.07M log rows over 2 workspaces × 89 days.

- **CH1** lightweight `DELETE FROM` works on all three engines, per-workspace, immediately visible: spans 996,000 rows masked in **8.37s**; logs (with its bloom-filter skip index) **4.99s**; `trace_summaries` (**AggregatingMergeTree** — the phase plan's flagged unknown) **0.075s**. Other workspace untouched.
- **CH2** `ALTER … DELETE` also works (6.72s/708k, `mutations_sync=2`), heavier — part rewrite vs mask. Documented fallback, **not** implemented (D294).
- **CH3** disk reclaim is merge-time; visibility is immediate (parts held 1,416,000 rows vs 420,000 visible). → S4.4 operator-docs handoff (D292).
- **CH4** **TTL is enforced at insert-time part formation** — 40-day-old rows into a 30-day-TTL table land as **0 rows**, before any merge. This is why 0004 lands before the receivers, and it bit for real: the vendors' 2019-dated fixtures silently failed to land until the integration tests re-stamped the instant (only the instant).
- **CH5** `MODIFY TTL` alone does not retro-apply at merge; the auto `MATERIALIZE TTL` mutation does. Widening 30→90 with all live data ≤30d: nothing eligible, safe.
- **CH6** `obstack_ingest` can lightweight-DELETE but has **no system-table SELECT** (measured ACCESS_DENIED) — the sweep reads none.
- **CH7 (review round)** the deployed `obstack_ingest` profile sets `max_execution_time=60`, so the sweep's stated 15m bound was **unreachable**; it is a default, not a `<constraints>` block, so the sweep sends its own per-statement value (D293) — transmission proved red-then-green by probing with a bogus setting name.
- **VD / CW** the two vendor wire contracts captured from the vendors' own references (schemas, encodings, signature algorithm, `awslogs` envelope, `CONTROL_MESSAGE` semantics), with `PROVENANCE.md` recording retrieval dates — including the vendor's own **internal contradiction** about whether the JSON format is a bracketed array (the receiver takes both; neither reading is asserted as truth until a real delivery settles it, D285).

---

## 3. Seam evidence (integrator, on a rebuilt image from this tree)

- **Receivers → metering → windowed rate:** accepted 21→33 (+12) with the window bucket at exactly 12 — the number of records POSTed. One `apply` closure feeds both accumulators, so the cumulative row and the rate bucket cannot count different events; **no double count**. Window read: 2.40/min over 5 complete minutes.
- **Receivers → retention:** both agree on `workspace_id`, on UTC instants, and on the 90-day outer bound (max tier 30d — no gap).
- **Rows landed, per receiver:** `service=<projectId>` n=6 traced=3 with `vercel.project.name` as an attribute (D295); `service=CloudTrail` n=6 traced=0 with `aws.cloudwatch.log_group/stream` — the CloudWatch envelope carries no trace context and none is invented.
- **Signature posture, live:** restarted with `OBSTACK_VERCEL_DRAIN_SECRET` → boot log flips `enabled:true`; valid HMAC 200, missing/wrong 401, and **gzip signed over the raw pre-gunzip bytes → 200** (D287 read literally).
- **Shutdown:** SIGTERM → exit in **0.87s**; a mid-DELETE cancel returns in ~1ms (probed with the driver under the sweep's exact context and settings), so the sweeper adds nothing to D278's 45s grace.
- **Deployment reality:** chart renders correctly all four ways (no env var by default; chart-owned key when set; reference-without-write when a Secret is brought — the last needing `optional: true`, which the integrator added); `docker compose config` valid.

---

## 4. Review record

Three batched Opus reviewers, all returning **must-fix**, all findings resolved. The three that mattered:

1. **The signature check could not be enabled in any deployment.** The secret was read by `os.Getenv` inside the handler and existed in no chart, no compose file and no config — the feature would have shipped looking complete and been unreachable. Now resolved at boot into `receive.Config`, with a chart value + `existingSecret` override, a compose passthrough, a documented `.env` line, and a boot log stating the posture.
2. **The forwarder's README claimed CI-exercised while every test stubbed `fetch`** — the two halves of that connector were only ever tested apart. Now an env-gated live leg, verified end to end (handler → HTTP → ingest → 3 rows in ClickHouse) and wired into the `web` job.
3. **Nothing read the window rows back from Postgres**, so an inverted trim or a SET-instead-of-add would have shipped green, surfacing as a rate reading "—" forever — indistinguishable from no data. Now asserted (bucket, retention delete, two-flusher sum) and **falsified red** by inverting the predicate before restoring.

Plus: the unreachable statement bound (CH7), a vacuously-passing half of the retention proof, a sweep that swept `ws_demo` on a shared dev stack, a cancel checked only between workspaces, a timezone-dependent summaries cutoff, three comments the sprint had falsified, and a hydration-unsafe `toLocaleString`.

## 5. Escalation record (D293–D297)

- **D293** the per-statement `max_execution_time` override stands — a hot-path cap must not size the cold path.
- **D294** no chunking, no mutation fallback: the bound is the **stated posture** (~100M+ overdue rows per workspace-table before a statement cannot finish; failure is loud, benign, and capped by the 90-day TTL). Day-partition chunking is the named mechanism with a registered trigger.
- **D295** Vercel grouping identity is the **project id** — `projectName` is per-entry optional and absent on the vendor's own runtime example, so grouping by it split one project into two services. The name rides as an attribute. **S4.4 product fact.**
- **D296** **D288 amended: window retention 65m → 15m** (the advisor retracted his own number — nothing renders an hour of history), trim fires only when the cutoff minute advances, no `bucket_start` index.
- **D297** the `—` / `0/min` asymmetry stands — different operator facts — and the caveat gains one clause saying so.

---

## 6. Verification at the signed tip

| check | result |
|---|---|
| `go test -count=1 ./...` (both DSNs) | **PASS**, all packages, **0 skips** (K4 trap safe) |
| `npm test` | **PASS** — 397 pass / 0 fail / 47 skipped (all pre-existing DSN-gated) |
| `tsc --noEmit` | clean |
| `eslint src --max-warnings 0` | clean |
| `gofmt -l` / `go vet ./...` | clean |
| `helm lint` + `helm template` ×4 paths | clean |
| `actionlint` | clean |
| forwarder `node --test` | 6/6 incl. the live leg |
| `docker compose config` | valid |

### First-runner CI numbers (the D137/D280 return)

Measured on PR #22's real runner, after the drive fix:

| job | wall-clock | note |
|---|---|---|
| `e2e` | **5m33s** | 166 assertions, 0 failed. **Faster than the M3 tip's 5m47–50s** — the ~10s of headroom the sprint inherited is now ~27s. The D137 watch does not fire and **the D207 reclaim stays unconsumed** |
| `images` | 4m28s | S4.1's job, unaffected |
| `web` | 4m25s | includes S4.2's forwarder harness (with its live leg) and the zip build |
| `go` | 1m16s | includes the retention both-directions proof and both receivers' end-to-end legs — **0 skips**, K4 trap satisfied |
| `lint` | 48s | |
| `stack` | **7m06s** | **over the ~6-minute K5 line — see the watch note below** |

S4.2 added no job (K3: no new names, no renames); the receiver and retention proofs grew the existing `go` job, the forwarder grew `web`.

### `stack` is over the K5 line, and it did not start here — RAISED, not absorbed

`stack`'s successful runs since S4.1 landed: **7m17s · 5m58s · 6m45s** (S4.1) → **7m06s** (S4.2, this PR). The phase plan's inherited number was 5m04–5m12s at the M3 tip, and S4.1's own bundle recorded 5m54s for its post-fix run — the best of its runs, not its range.

**S4.2 did not touch `stack.yml`, nor anything the job builds**: the growth is S4.1's web-image pre-build, and it was already over the line before this sprint opened. What S4.2 adds to that job is one ClickHouse migration (three `ALTER … MODIFY TTL` statements) and a conditional Secret key in the chart.

**RULED — D298 (2026-08-24): the K5 over-branch FIRES for `stack`.** The advisor verified the numbers on the tree and declined to re-base the watch: S4.1's premise that the cached steady state collapses to seconds is falsified — the cache restore plus miss variance IS the structural cost, so the job's floor sits at the line and its typical run above it, and re-basing to ~7m would delete the K5 line rather than enforce it ("watches absorb variance, never structural breaches"). New shape, binding, **implemented in S4.3**: `stack` drops from the required set (never required-but-conditional); `pull_request` becomes **path-filtered** to what `acceptance.sh` actually builds (`stack.yml`, `deploy/**`, `services/ingest/**`, `demo/agent-app/**`, `apps/web/Dockerfile*`); `workflow_dispatch` for the rare force; `push` to master always. The pre-committed `ci:stack` label is **superseded** by the path filter — one forcing mechanism is enough. D41's rider (a red `stack` on master is stop-the-line) is retained as the primary backstop and is what buys the reclaim: the PR critical path drops from `stack`'s ~7m to `e2e`'s 5m33s. **One carve-out, load-bearing:** the D122 captured-DDL drift guard MOVES from `stack` to `e2e`, because its failure source is app code (`authConfig()` in `apps/web/src/server/auth.ts`) that ordinary web PRs touch — leaving it on master-push would convert its exact designed catch into a post-merge stop-the-line. The chart-Postgres schema step stays in `stack` (it is a chart assertion and needs the cluster). Watches re-based: `stack` measured on master pushes henceforth, recorded band 5m58–7m17; `e2e` becomes the PR critical path and re-measures with the guard aboard, its own pre-committed ~6m over-branch rule standing as written. **D207's reclaim stays unconsumed.**

This was a **D137/K5 watch condition on a job whose trigger policy is the advisor's to set** (the K5 branch is: label + `workflow_dispatch` + always-on-push-to-master, and drop it from the required set — never required-but-conditional). It is **raised to the advisor for S4.3 planning rather than decided here**, and deliberately not absorbed into this sprint: nothing about it makes S4.2's evidence weaker, and quietly noting a number that crossed a stated line is how a watch stops working. **D207's reclaim is `e2e`'s and stays unconsumed** — it does not apply to `stack`.

Chart bumped **0.3.0 → 0.4.0** (its rendered manifests changed observably; D268's one-bump-per-sprint).

---

## 7. Carried forward

- **To S4.4 (docs):** CH3's merge-time-reclaim sentence and the straddling-trace retained semantic (D292); D294's stated retention bound and its failure signature; D295's grouping fact; the two receivers' honest "coming soon at launch" state.
- **To S5:** D285's fixture re-validation against the first real delivery **before any SOON card flips** (divergence = fix-before-flip); receiver activation + the card flip itself (D101/D208); real-provider verification of managed Postgres.
- **Registered trigger (D294):** sweep failures repeating for the same workspace across consecutive sweeps → day-partition chunking, owner = next sprint's manager.
- **Non-blocking observations from the integrator** (recorded, not fixed): the window-discard path has no counter while the package doc's stated posture is "loud in the logs, counted ops-side"; `lastTrimmed` advances before commit (a rolled-back flush skips one trim cycle — self-correcting); the trim is idle while the meter is idle (bounded, unreadable rows); a >90-day-late delivery 200s and meters while the outer TTL drops the row.

## Run log

- 2026-08-23 — breakdown with measurements taken first (CH1–CH6, VD, CW); advisor kickoff **D285–D292**, cleared for implementation; T1–T7 implemented and committed.
- 2026-08-24 — three batched Opus reviewers returned must-fix; all findings fixed (`7f01230`, `bf4ecbb`). Advisor escalation round ruled **D293–D297**, deltas landed (`79d16ed`). Integrator drove the whole thing on a rebuilt image and returned **FIXED-GO**, fixing three chart-contract slips (`1ad55d4`). Suites green at the tip.
