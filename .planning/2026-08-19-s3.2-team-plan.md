# Team Plan — obstack M3 sprint 2 — DB-backed API keys, invites and a real settings surface (exit: a signed-up user issues and revokes API keys in settings, invites a teammate into the workspace, and telemetry sent with an issued key lands in that workspace and no other — the same wire format the M1 dev keys used)

meta:
- date: 2026-08-19
- repo/branch: master @ a67ceea (branch `s3.2-keys-invites-settings` to be cut before wave 1)
- mode: waves
- team-cap: 10
- models: executor=opus (standing user rule — Opus 5 executors AND reviewers, never Sonnet; Fable 5 only on explicit per-task user override) reviewer=opus advisor=fable
- iteration-limit: 3 — fix rounds allowed before escalating to the user
- iteration: 0/3
- **run outcome (2026-08-19): COMPLETE — exit bundle ADVISOR-SIGNED, effective at `8aaa15e` with eight checks green** (e2e 4m8s · go 2m29s · kind 1m48s · sdk-e2e 4m4s · sdk-js 59s · sdk-py 34s · stack 5m2s · web 3m6s). 10/10 task-slots approved (T1–T8, F1, F2), integrator FIXED-GO round 1, goal **MET 0/3** clause-by-clause. Rulings **D138–D160**. **PR #16 MERGED to `master` @ `d1e77f3`.** Bundle verbatim at `.planning/2026-08-19-s3.2-exit-evidence.md`. D137 watch: 4m8s real (vs S3.1's 4m12s — the S3.2 step grew coverage at zero real-runner cost), zero of three trips; `stack` 5m2s in range. Standing guards armed leaving the sprint: D120 matrix incl. named stay-closed invitation entries, D122 ddl-drift, D146 keystore red set, D160 prefix guard, D150/D152 mock-mode action guard + standing test, smoke floor + 102-claim drive in `e2e` every-PR.
- sprint: S3.2 per `.planning/2026-08-18-s3-m3-phase-plan.md` (§3 S3.C2, §9 rulings D94–D110 + D137 BINDING); S3.1 inheritance list in `.planning/2026-08-19-s3.1-exit-evidence.md` (BINDING, verbatim)
- sources: phase plan §9, S3.1 team plan `.planning/2026-08-18-s3.1-team-plan.md` (D111–D137), `.ziarmy/retro.md` (all lessons binding), PRD §6/§8

## Binding context (read before executing anything)

- **D98 (the key move, whole)** — token `ok_live_<high-entropy-random>`; store key id + display prefix + **SHA-256 of the full token** (no per-row salt — deterministic hash on a high-entropy token; a KDF would burn the hot path); lookup by hash in one indexed query. Verbatim storage REFUSED — a DB dump must not be a credential dump; "shown once" must be TRUE. **Wire format = the HTTP contract** (`Authorization: Bearer <token>`, exact-string resolution, D6 error posture — one indistinguishable `ErrUnauthorized`), NOT the token's internal shape. Ingest: **read-through in-memory cache, bounded TTL (order 30 s, a stated constant), negative caching, fail-static** (Postgres down ⇒ cached entries served stale; unknown keys 401); revocation honored ≤TTL and documented as such; per-request Postgres round-trips REFUSED (ingest is never the customer's outage, PRD §9). **`OBSTACK_API_KEYS` DELETED — no dual lookup path**; the `ok_dev_local:ws_demo` literal becomes a **seeded dev row through the migration/seed path**; collector + every signed harness keep working against that row. The S2.3 L1 cross-tree sweep is a **named done-check**.
- **D29 (BLOCKING — merges inside this sprint, before any second workspace's data can arrive)** — `obstack_ingest_unpriced_models_total{model}` gains a cardinality cap/allowlist (`metrics.go:67-70`, one increment site `internal/pricing/pricing.go:118`); `prices.json` gains the **`{as_of, prices:[…]}` envelope** (D108 wording) — bare array today, parse at `internal/pricing/pricing.go:49`, panic-on-boot posture stands.
- **D120 (allowlist widens door-by-door)** — `route.ts:16` holds exactly `get-session` + `sign-out`; every door S3.2 opens is named, carries its **stated transactional property**, and joins the standing matrix (unit + live-ammunition halves). **Owner-pinned resolution inherited: accepting an invitation is ADDITIVE, never a re-home** — the measured tenancy break stays impossible. Invites are U4 **in-app copyable links** (no email); the unused `invitation` table (`0003_auth.sql:48`) is schema of record. **`0003_auth.sql` is captured library DDL — never hand-edited**; any `authConfig()` change re-captures and the D122 ddl-drift guard catches drift.
- **D106** — landing gains `/signup` as the **primary CTA target** (route already real: `apps/web/src/app/signup/`); "Open the live demo" survives as secondary; the `page.tsx` "no signup, no setup" sentence is **S3.5's edit, not ours**. Settings registers in `live-routes.ts` under the **per-section badge rule**: General / Members / API keys real; **Billing & usage `SampleMark`'d until S3.3; Audit log + Compliance `SampleMark`'d through M3** (owners M6/M7). The waitlist block (PR #15) coexists on the landing page — the CTA work states their relationship (phase-plan run log 2026-08-19).
- **D107/D137** — the `e2e` drive (`deploy/compose/e2e-drive.mjs`, every-PR) **grows its S3.2 step in place** — one drive, one definition; S2.3 L4 binding (fresh profiles, self-started processes only, refuse loudly); D132 log-allowlist shape binding. **The issued-key WIRE path is pre-registered to S3.4's drive extension (D115)** — this sprint's wire evidence lands elsewhere (see kickoff Q5).
- **Pattern inheritance** — D121/D127/D129 error-code vocabulary for every new form (template: `apps/web/src/app/signup/{errors,actions}.ts` — closed code enum, reader/writer split, `Object.hasOwn`, code never interpolated); D68 corpus on every new contract module's parse (`apps/web/src/lib/hostile-url-values.ts`, by rule not by copy); D130 `.integration.test.ts` class (self-skip only when `OBSTACK_TEST_POSTGRES_DSN` unset; CI sets it); D112 env names; D11 parameterized SQL only; D13/D21 honesty on every wired surface.
- **Retro lessons binding**: content-aware tenancy assertions (S3.1 L1); allowlist fail-closed + promote discovering probes to standing guards (S3.1 L2); manager reports only notified results (S3.1 L3); lockfile via seeded `--package-lock-only`, diff checked for floats (S2.1 L1); cross-tree grep for alias-crossing/root-relative literals (S2.1 L2/S2.3 L1); CI runs the exact runbook commands (S2.1 L3); falsification probes on absence claims (S2.2 L1); one-definition rulings over review (S2.3 L3); measured version facts, not believed (S2.4 L1).
- **Scope fence (phase plan §3 S3.C2)**: no metering numbers, no Polar/billing, no connectors, no Explain, no quickstart/onboarding rework (S3.4), no switcher affordance (S3.5, D134), no retention anything. Settings tabs this sprint does not wire keep SAMPLE marking rather than being half-wired.
- **Next.js note**: this repo's Next has breaking changes vs training data — read `node_modules/next/dist/docs/` guides before App Router / route-handler code (repo AGENTS.md managed block).

## Advisor decisions
<!-- Kickoff D138–D149 (2026-08-19). BINDING for all agents. Verbatim return in the escalations record; operative content here. Facts marked MEASURED were measured against the installed better-auth 1.7.1 dist. -->
- **D138 (api_keys DDL + continuity row)**: real FK — `workspace_id TEXT NOT NULL REFERENCES workspaces(id) ON DELETE CASCADE` (D112 bars only cross-set refs; D116 precedent). DDL of record: `api_keys(id TEXT PK app-generated, workspace_id FK, name TEXT NOT NULL (trimmed 1–100 app-side), prefix TEXT NOT NULL (first 12 chars, display only), token_hash TEXT NOT NULL UNIQUE (lowercase hex SHA-256 of full token), created_at TIMESTAMPTZ DEFAULT now(), revoked_at TIMESTAMPTZ)` + `CREATE INDEX api_keys_workspace_id_idx ON api_keys (workspace_id)`. The UNIQUE on token_hash IS the lookup index. **NO `last_used_at`** — D100 health rows (S3.3) own liveness; REFUSED. Continuity seed in 0004, plain INSERTs: `workspaces(id='ws_demo', org_id='org_demo')` + `api_keys(id='key_dev_local', workspace_id='ws_demo', name='dev local', prefix='ok_dev_local', token_hash=<D139 vector>)`, comment stating the preimage. **D115 SUPERSESSION on the record**: Postgres is now the ONE key authority, so the continuity key's workspace must exist as a row; `ws_demo` stays product-invisible by construction (session resolution joins `member`; no member row ever references `org_demo`). MEASURED: `workspaces` has no name column — seed needs nothing more.
- **D139 (token + hash contract, one definition)**: issued token = `ok_live_` + 64 lowercase hex chars from 32 CSPRNG bytes (length 72, 256-bit). Hash contract both languages: **SHA-256 over UTF-8 bytes of the exact full token string, lowercase hex**. Pinned cross-language test vector asserted in BOTH T1 Go and T3 TS suites: `SHA-256("ok_dev_local") = 45880674fdc48bbcd49721bf6ac190e804836ca4fcd54c736c604153f4947e20`. Prefix rule for both shapes: `prefix = token[0:12]` (dev literal = whole token, honest — it is a public dev credential by design). **Ingest never validates token shape** — exact-string hash lookup only; a keystore rejecting non-`ok_live_` shapes breaks the dev row and is a defect.
- **D140 (invited member's view)**: the exit clause is satisfied by MEMBERSHIP — Bob's session stays owner-pinned to his own workspace; the viewing path is S3.5's switcher (D134). Copy on Members tab + accept-success surface: **present-tense truth only, no promised features** ("You've joined <org> as a member. You're viewing your own workspace."), never "you'll be able to switch soon".
- **D141 (Data & ingest tab)**: `SampleMark`'d in S3.2; `as_of` display lands S3.3 with overrides (one touch, not two). D106 real set stays exactly General / Members / API keys.
- **D142 (wire evidence, three layers, NO drive step — D115 stands)**: (1) T1 integration test: issued-format token hashed+inserted resolves via keystore; revoked/unknown/fail-static cases. (2) T7 cycled compose: collector + sdk profiles ingesting under `ok_dev_local` IS live wire evidence through the Postgres path (no other path exists). (3) **T8 owns the issued-key end-to-end line at the integration gate as recorded runbook commands, NOT a committed harness, NOT a drive step**: issue key in UI for a drive-created workspace → curl one trace `Authorization: Bearer <token>` (M1 smoke curl shape) → ClickHouse query proves attribution to that workspace and the other workspace's counts unchanged → revoke in UI → wait >TTL (30s) → resend → 401. Commands verbatim with fixing values in the exit bundle. Standing CI coverage of this path = S3.4's pre-registered drive extension.
- **D143 (doors — ZERO open)**: **`ALLOWED_ENDPOINTS` stays byte-identical `{get-session, sign-out}`.** All invite ops are server actions calling `auth.api.*` server-side with request headers (the signup pattern; our actions own the invariants — org id always from `SessionContext.orgId`, never caller-supplied). MEASURED endpoint set the actions use at 1.7.1: `auth.api.createInvitation / cancelInvitation / acceptInvitation / getInvitation / listInvitations` (HTTP paths `/organization/invite-member`, `/cancel-invitation`, `/accept-invitation`, `/get-invitation`, `/list-invitations` join the matrix as named STAY-CLOSED entries with live-ammunition row-delta assertions). Copyable link = **`/invite/<invitation.id>`**, id treated as opaque token (hostile corpus on the parse); own token table REFUSED. MEASURED: accept requires signed-in user's email == invitation email case-insensitive (crud-invites.mjs:269) — invite creation asks for the teammate's email and the copy states the link works only for an account with that email. Default expiry 48h (MEASURED :137); `sendInvitationEmail` optional (MEASURED :150) — U4 satisfied with zero `authConfig()` change expected. **MEASURED, load-bearing: `acceptInvitation` mutates the session row's `activeOrganizationId` to the inviting org (crud-invites.mjs:330); our owner-pin resolution (session.ts:26-32) ignores that column — exactly why acceptance cannot re-home.** T4's red-provable test: post-accept, `activeOrganizationId` = inviting org AND resolution still returns the invitee's own workspace; honoring `activeOrganizationId` (or dropping the role pin) → red. **ESCALATIONS (not adaptations)**: any invite op not performable via server-side `auth.api.*`; the emailVerified guard (crud-invites.mjs:274) firing with verification OFF; `acceptInvitation` writing anything beyond member row + invitation.status + session activeOrganizationId.
- **D144 (mock strings)**: reconciled in S3.2 by T5 — mock keys become format-true `ok_live_…`; the `ob_read_` row DELETED (one key class exists; mock must not exhibit a class that doesn't). T5 greps `ob_live_`/`ob_read_` repo-wide and corrects every literal (quickstart's fabricated string = one-line copy-truth fix, NOT the S3.4 rework; fence intact).
- **D145 (breakdown)**: waves RATIFIED — W1 = T1,T2,T4,T6 · W2 = T3,T7 · W3 = T5 · W4 = T8. T7 stays wave 2. T1 receive-conditional ownership correct (expect untouched). T4-owns-auth.ts correct; with D143 expect zero authConfig diff; ddl-drift arbitrates if any.
- **D146 (keystore constants/semantics)**: lookup `SELECT workspace_id FROM api_keys WHERE token_hash = $1 AND revoked_at IS NULL`; no-row → negative entry; D6 holds (one indistinguishable ErrUnauthorized for missing/unknown/revoked). `keyCacheTTL = 30 * time.Second` — one constant, positive AND negative entries, no refresh-extend on hit; revocation ≤30s documented at the constant. `keyCacheMaxEntries = 10_000` total; positive entries always stored (evict an arbitrary negative if full); at cap new NEGATIVE entries not stored. Fail-static: on Postgres query error an expired POSITIVE entry serves stale (never evict on error); no entry → 401; package doc states "Postgres down + cold cache ⇒ valid keys 401 until Postgres returns." No metrics, no workspace-state fields (D99 widening is S3.3's) — build nothing speculative.
- **D147 (D29 mechanism)**: CAP, not allowlist (the metric exists to surface unknown models). First-come cap `maxUnpricedModelLabels = 100` distinct per process lifetime; beyond → fixed bucket `model="_overflow"`. Red-provable by >100 distinct hostile strings. Envelope `{"as_of":"YYYY-MM-DD","prices":[...]}`; `Load()` refuses a bare array loudly (panic-on-boot stands), validates as_of as a real date, exposes `AsOf`.
- **D148 (authorization + settings scope)**: authorization IS the owner-pin — every key/invite action operates on `SessionContext.orgId`/`workspaceId` only, resolved server-side; no role-check machinery beyond it. Members real surface = list members + pending invites + create invite/copy link + **cancel invitation**; **NO member-removal UI** (endpoint stays matrix-refused 404). **General tab read-only real data** (org name from `organization` row, workspace id; Loopwork/eu-central literals die; no region displayed — none exists). Rename/mutation not this sprint.
- **D149 (invite accept page posture)**: `/invite/<id>` with no session links to `/login` + `/signup` ("sign in, then reopen your invite link"). **NO `?next=`/redirect-back parameter** (open-redirect channel of the D68 class). Accept surface gets its own errors.ts closed enum mapping the measured better-auth codes (not-recipient, expired, already-accepted/cancelled → fixed copy; unknown → generic).

## Critical items
<!-- Pre-named by the phase plan §9 "critical items" list; kickoff may add. -->
- T1 — DDL vs D138 byte-check; cache constants/semantics vs D146; fail-static proof; boot-failure posture
- T2 — the BLOCKING gate of record; D147 mechanism + red proofs
- T3 — (advisor-added) the cross-language hash contract (D139 vector in both suites) + the shown-once proof — the seam two languages implement (S2.3 L3 class)
- T4 — the D143 zero-doors verdict held (route.ts byte-identical); measured 1.7.1 facts vs the advisor's MEASURED list; additive-never-re-home red-then-green
- T5/T6 — D106 badge rule + D141/D144/D148 conformance; landing CTA honesty
- T7 — the sweep grep output, verbatim
- T8 — the drive's S3.2 step + the D142 wire evidence line + the ws_demo-row expectation change + the composite runner number (D137 watch goes live with the step growth)
- exit — the D142 evidence commands appear verbatim; advisor signs the bundle

## Tasks

### T1: api_keys migration + ingest key lookup moves env→Postgres
- status: approved
- wave: 1
- depends: —
- owns: services/ingest/pgmigrations/0004_api_keys.sql, services/ingest/internal/keystore/ (new package: store.go, store_test.go, integration test), services/ingest/internal/auth/auth.go, services/ingest/internal/auth/auth_test.go, services/ingest/internal/config/config.go, services/ingest/internal/config/config_test.go, services/ingest/cmd/ingest/main.go, services/ingest/cmd/ingest/main_test.go, services/ingest/internal/receive/receive.go, services/ingest/internal/receive/http.go, services/ingest/internal/receive/grpc.go (receive files only if the Authenticator seam requires it — else untouched)
- result: reviewer APPROVE, 0 findings in scope, ADVISOR-SIGNED. D138 DDL byte-conformant; D146 lookupSQL byte-identical; 9 red proofs re-fired by reviewer (fail-static, refresh-extend, revoked_at predicate, shape gate, negatives-at-cap, seed hash, UNIQUE, FK CASCADE, boot refusal); -race clean under clock-accelerated expiry; migration proven as upgrade + idempotent; D6 one-string-one-status through the Resolver seam; both DSN modes green ×2 runners. D151 rider (Init deletion) clean, doc half completed by T2's executor. Evidence: scratchpad/t1-review-evidence.md.
- goal: `0004_api_keys.sql` creates `api_keys` with the **D138 DDL verbatim** (byte-check at review) + the continuity seed rows (`workspaces('ws_demo','org_demo')` + the dev key row with the D139 vector hash, comment stating the preimage AND the D115 supersession), picked up by the `//go:embed *.sql` glob with zero edits to `pgmigrations/migrations.go`. Ingest gains a serving-path Postgres pool + a keystore per **D146 verbatim** (30s TTL constant, 10k entry bound, negative caching, fail-static, no refresh-extend, one indistinguishable `ErrUnauthorized`); `Authenticator` seam signature unchanged (receive files expected untouched); **boot posture: with the env map deleted, `OBSTACK_POSTGRES_DSN` unset/unreachable at boot = loud boot failure (D95(e) — no Postgres-less serve exists)**; keystore never validates token shape (D139). `OBSTACK_API_KEYS` parsing (`config.go:55,104-121`) DELETED; wire format byte-unchanged; `main.go:114-119`'s "serves nothing out of Postgres" comment updated truthfully. The D139 cross-language vector asserted in the Go suite.
- done-check: `cd services/ingest && go test ./...` green with and without `OBSTACK_TEST_POSTGRES_DSN`; keystore integration test proves: issued-format key resolves to its workspace, revoked key 401s after cache expiry, unknown key 401s, Postgres-down serves cached entries stale (fail-static, proven red-then-green); `grep -rn OBSTACK_API_KEYS services/` → only prose declaring the deletion

### T2: D29 — unpriced-model cardinality cap + prices.json as_of envelope
- status: approved
- wave: 1
- depends: —
- owns: services/ingest/internal/metrics/metrics.go, services/ingest/internal/pricing/pricing.go, services/ingest/internal/pricing/pricing_test.go, services/ingest/pricing/prices.json, services/ingest/pricing/pricing.go, services/ingest/internal/mapping/mapping_test.go, services/ingest/internal/write/integration_test.go
- goal: the `model` label on `obstack_ingest_unpriced_models_total` is bounded (cap/allowlist mechanism per kickoff ruling; overflow collapses to a fixed bucket, red-provable by exceeding the cap); `prices.json` becomes `{as_of: "YYYY-MM-DD", prices: [...]}`; `Load()` (`internal/pricing/pricing.go:47-81`) parses the envelope, exposes `AsOf`, keeps every existing validation + longest-prefix sort + panic-on-malformed boot posture; embed doc comments updated.
- done-check: `cd services/ingest && go test ./...` green; a test proves the cap holds under >cap distinct hostile model strings (red-provable by removing the cap); a test proves a bare-array prices.json is refused loudly
- result: reviewer APPROVE, 0 findings. D147 byte-conformant (cap 100 first-come under mutex, `_overflow`, envelope refusal names both keys, time.DateOnly validation, boot-panic posture intact, AsOf exported). Reviewer re-ran all red proofs + go vet + -race + a 32×500-name concurrency probe (series ≤101, zero dropped counts). Counter vec unexported — one door `metrics.CountUnpricedModel`, Collect-based read. Repo-wide: zero stale UnpricedModels refs. prices.json rows byte-identical in value; as_of=2026-08-15 (last content-change commit). Record notes: internal/metrics has no own test file (cap exercised via pricing suite); cap is per-process — later tests expecting fresh own-series would land in _overflow (source order currently safe).

### T3: web key management server layer (issue / list / revoke, shown-once)
- status: approved
- wave: 2
- depends: T1
- owns: apps/web/src/server/api-keys.ts (new), apps/web/src/server/api-keys.test.ts (new), apps/web/src/server/api-keys.integration.test.ts (new), apps/web/src/app/app/settings/actions.ts (new), apps/web/src/app/app/settings/errors.ts (new)
- goal: token generation (`ok_live_<high-entropy-random>` per D98/kickoff), SHA-256 at issue time, Postgres rows via `getPool()`/parameterized SQL only, scoped to the session's workspace (dataForSession posture — no caller-supplied workspace ids); create returns the full token exactly once and never persists it; list returns prefix/name/created/revoked; revoke sets revoked_at. Server actions follow the D121/D127/D129 vocabulary (closed enum in errors.ts, reader/writer split); any new parse joins the D68 corpus totality test.
- done-check: `cd apps/web && npm test` green both with and without `OBSTACK_TEST_POSTGRES_DSN`; integration test proves shown-once (the stored row cannot reproduce the token), cross-workspace revoke refused, and the hash in Postgres matches ingest's lookup contract (same SHA-256 of the same token string)
- result: reviewer MUST-FIX (1 item, outside T3's files — settings actions carry the D152 guard but nothing tests it; test of record is F1's mock-mode.test.ts, routed to the rider executor) + 1 finding FIXED in place (false coverage-claim comment corrected to the real dist-transcription basis). T3's own tree approve-quality: both D139 vectors independently recomputed (latin1/utf16le decoys confirm the UTF-8 vector is load-bearing); shown-once falsified via to_jsonb LIKE probe; D148 content-aware cross-workspace refusal; D11 all $-bound; (code)=>never sound; red proofs re-fired ×3 classes. 234/212/0/22 + 253/253 + build green after fix. Must-fix DISCHARGED: per-action D152 case landed in mock-mode.test.ts (red-proved twice — executor and reviewer independently), settings/actions.ts + api-keys.ts sha-verified byte-identical. Re-verify APPROVE — T3 CLOSED.

### T4: invites — ZERO doors (D143), server actions over auth.api.*, acceptance additive-never-re-home
- status: approved
- wave: 1
- depends: —
- owns: apps/web/src/app/api/auth/[...all]/route.ts (comment/matrix references only — ALLOWED_ENDPOINTS stays byte-identical), apps/web/src/server/auth.ts, apps/web/src/server/auth.test.ts, apps/web/src/server/auth.integration.test.ts, apps/web/src/server/invites.ts (new), apps/web/src/server/invites.integration.test.ts (new), apps/web/src/app/invite/ (new accept surface: page.tsx, actions.ts, errors.ts)
- goal: **D143 verbatim** — `ALLOWED_ENDPOINTS` stays exactly `{get-session, sign-out}`; invite ops are server actions calling `auth.api.createInvitation/cancelInvitation/acceptInvitation/getInvitation/listInvitations` server-side with request headers (signup pattern; org id always from `SessionContext.orgId`); the five invitation HTTP paths join the matrix as named STAY-CLOSED entries with live-ammunition row-delta assertions. Copyable link `/invite/<invitation.id>` (opaque token, hostile corpus on the parse); accept page posture per **D149** (no session → links to /login + /signup, NO ?next= param; own errors.ts closed enum on measured better-auth codes). Create-invite asks for the teammate's email; copy states the link works only for an account with that email (measured :269 guard) and follows **D140** present-tense truth. **Additive-never-re-home red-provable test per D143**: post-accept, session row's `activeOrganizationId` = inviting org AND resolution still returns the invitee's own workspace; honoring that column (or dropping the role pin) → red. `0003_auth.sql` untouched; zero `authConfig()` diff expected (ddl-drift arbitrates). ESCALATE (never adapt): any invite op not performable via server-side `auth.api.*`; the emailVerified guard firing with verification OFF; acceptance writing beyond member row + invitation.status + session activeOrganizationId.
- done-check: `cd apps/web && npm test` green both DSN modes; the widened matrix red-provable; additive-never-re-home proven red-then-green (content-aware, S3.1 L1); `git diff` on route.ts shows ALLOWED_ENDPOINTS byte-identical
- result: reviewer FIXED (1 finding): the %inv-it-% sibling-literal exclusion in ROW_COUNTS_SQL made an ASSERTED property (fixtures + leak bodies proven literal-free, red-proved) per the D131 rider. Held: route.ts byte-identical; auth.ts comments-only; additive-never-re-home content-aware, all red proofs re-run by reviewer; all MEASURED 1.7.1 facts re-verified; D148/D149/D140 conformant; enum refinement (3 members, both not-found spellings) within D149. Suites 197/0/15 + 231/0/0; build green, /invite/[id] dynamic (ƒ). Exports for T3/T5 incl. PendingInvite.expiresAt (the honest 48h surface for Members tab). Two escalations sent to advisor (mock-mode POST 500 on invite action; expired-invite cancel gap) — rulings pending (D152+). ADVISOR-SIGNED as fixed; E1 → D152 (invite action joins D150 via F1's extended owns + action-level standing test + 'every future auth-surface server action joins this guard' standing property); E2 → D153 (recorded absence — expired-invitation lifecycle pre-registered to the M4 inventory beside D119/D123/D132). APPROVED.

### T5: settings surface real — General / Members / API keys; per-section badges; live-routes
- status: approved
- wave: 3
- depends: T3, T4
- owns: apps/web/src/components/settings/SettingsSuite.tsx, apps/web/src/app/app/settings/page.tsx, apps/web/src/lib/live-routes.ts, apps/web/src/lib/live-routes.test.ts, apps/web/src/mock/workspace.ts, apps/web/src/components/onboarding/Quickstart.tsx (D144 one-line copy-truth fix only)
- goal: `page.tsx` fetches real data via `dataForSession()`/session context + the T3/T4 server layer; General renders **read-only** real data per D148 (org name from the `organization` row, workspace id; "Loopwork"/"eu-central" literals die; no region displayed — none exists); Members renders real members + pending invites + create-invite/copy-link + cancel-invitation (D148 — NO member-removal UI); API keys renders real issue/shown-once/revoke against T3's actions (reusing the shown-once UX shape at `SettingsSuite.tsx:174-215`, real data); **SampleMark per D106+D141 on exactly: Billing & usage (until S3.3), Data & ingest (until S3.3), Audit log, Compliance (through M3)**; `/app/settings` registers in `live-routes.ts`; mock mode keeps rendering the mock suite untouched (D125 byte-invariance — no Postgres touch in mock). **D144**: mock keys become format-true `ok_live_…`, the `ob_read_` row DELETED, repo-wide `ob_live_`/`ob_read_` grep with every literal corrected (Quickstart's fabricated string = one-line copy-truth fix; the S3.4 fence intact).
- done-check: (as written)
- result: reviewer APPROVE, no fixes. Badge rule measured from live DOM (SAMPLE on exactly the 4 ruled tabs, honest titles, marked tabs render untouched mock components); D125/D114 byte-invariance proven both build modes (no-DSN mock build ○ Static with fixtures; live-env ƒ Dynamic; lazy live-path imports); D148 exact; shown-once DOM lifecycle probed (token gone after dismiss + across navigations; Postgres prefix+hash only); D144 discharged repo-wide; reviewer independently re-drove the browser evidence (own build, fresh profile, all claims green) and corrected an evidence-quality race in the scratchpad drive (revoke gate vs standing copy → polled revoked_at); host cleaned (3 orphaned next-servers killed — S2.3 L4). 236/214/0/22 + 255/255 + both builds green. 3 escalations → advisor (ORG_NAME_SQL placement; mock lastUsed/scope fields vs D144; ob_ literal standing guard) — D158+ pending. APPROVED.

### T6: landing CTA → /signup; waitlist relationship stated
- status: approved
- wave: 1
- depends: —
- owns: apps/web/src/app/page.tsx, apps/web/src/app/signup/page.tsx
- goal: the primary CTA (nav `:56-63` + hero `:86-93`) targets `/signup`; "Open the live demo" survives as the secondary CTA; the footer CTA block gains the signup target per kickoff ruling; the waitlist block stays and the page states the CTA↔waitlist relationship honestly (signup = self-hosted/local product today, waitlist = cloud preview — exact copy per kickoff); the "no signup, no setup" demo sentence is NOT edited (S3.5's, D106); `signup/page.tsx:29-32`'s stale "key still maps by env" comment updated once T1's reality lands.
- done-check: `cd apps/web && npm test && npm run build` green; every landing CTA href verified; no D13 claim the product can't back
- result: reviewer FIXED — (1) hero "or your cluster" claim dropped (no web image until M4, values.yaml:4; D13); (2) WATCH ITEM: the "issue a key… lands in that workspace and no other" copy (page.tsx + signup/page.tsx) is true only once T1+T3+T5 land — if either slips, both sentences + the signup comment revert in the same round (integration-gate item); (3) sub-640px nav shows signup only — hero/footer keep the demo secondary at every width, no round. "no signup, no setup" byte-untouched; both WaitlistForm mounts intact; tests 189/0/11, build green after fix. ESCALATED → RULED D150: honest mock-mode surface, owned by F1; T6's verdict stands, CTA unchanged. APPROVED.

### T7: the deletion sweep — compose/chart/collector/harnesses/docs off the env key path
- status: approved
- wave: 2
- depends: T1
- owns: deploy/compose/docker-compose.yml, deploy/helm/obstack/values.yaml, deploy/helm/obstack/templates/ingest/deployment.yaml, deploy/helm/obstack/templates/ingest/migrate-job.yaml, deploy/helm/obstack/templates/ingest/pg-migrate-job.yaml, deploy/collector/config.compose.yaml, deploy/collector/README.md, deploy/compose/trace-checks.ts, deploy/compose/sdk-checks.ts, deploy/compose/README.md, deploy/helm/obstack/acceptance.ts, README.md, demo/agent-app/README.md, demo/sdk-sample-ts/README.md, demo/sdk-sample-py/README.md, packages/obstack-py/README.md, packages/obstack-js/README.md
- goal: `OBSTACK_API_KEYS` leaves compose (`docker-compose.yml:97`) and the chart (`deployment.yaml:66-67`, `values.yaml:55`); the collector's pinned `ok_dev_local` bearer keeps authenticating against T1's seeded continuity row; every README/harness line that teaches or asserts the env path teaches the seeded-row reality instead; comments referencing the env path corrected. The S2.3 L1 sweep is the done-check: grep EVERY tree.
- done-check: `grep -rn "OBSTACK_API_KEYS" --exclude-dir=.planning --exclude-dir=node_modules --exclude-dir=.git .` → only prose describing the deletion/history; cycled compose stack from `down -v`: `up -d --wait` + `bash deploy/compose/smoke.sh` PASS; collector + sdk profiles still ingest under `ok_dev_local`
- result: reviewer FIXED (1 finding: compose-README Postgres enumeration omitted api_keys — fixed with anchor) → ADVISOR-SIGNED. Final greps of record: OBSTACK_API_KEYS → 4 hits all deletion prose; ok_dev_local → 54 hits all client-credential teaching. All doc claims verified against landed source; chart ingest env exactly the 4 DSN/flag vars; .Values.ingest.apiKeys gone repo-wide; harness diffs comment-only, D96 posture intact; 5 demo/package READMEs correctly untouched. Wire evidence (D142(2)) reproduced independently by the reviewer from three fresh down -v cycles (smoke PASS, seed row content-verified incl. D139 hash, sdk-evidence 28/0, collector as ws_demo zero 401s). Owns extended twice, both ratified: helm README:262-273 (second executor), D154 lazy-series sentence + S3.2 drive bullet in compose README (T7-rider). APPROVED; original executor stalled 2× — riders finished by the fresh executor.

### T8: e2e drive grows its S3.2 step; CI in place
- status: approved
- wave: 4
- depends: T5, T4, T7
- owns: deploy/compose/e2e-drive.mjs, deploy/compose/exit-seed.mjs (only if the step needs it), .github/workflows/e2e.yml (only if env/setup changes), .github/workflows/stack.yml (only if a pg-key guard step is ruled)
- goal: the drive grows the S3.2 step IN PLACE (one drive, one definition): alice opens settings → real workspace name asserted (content-aware) → issues a key → shown-once banner asserted → key listed by prefix → creates an invite link (bob's email) → bob (already signed up in the drive) accepts at `/invite/<id>` → alice's Members shows bob → **bob's session provably NOT re-homed: content-aware disjointness re-asserted post-acceptance** → alice revokes the key → revoked state asserted. The issued-key WIRE step stays out of the drive (D115 — S3.4); **T8 instead owns the D142(3) end-to-end line at the integration gate as recorded runbook commands with fixing values** (issue in UI → curl with Bearer token → ClickHouse attribution proof + other workspace unchanged → revoke → >30s → 401), verbatim in the exit bundle. **Integration-gate Postgres expectation updated per kickoff: exactly one `ws_demo` row (the 0004 seed, org_id `org_demo`, zero `member` rows referencing it) beside the two stranger workspaces** — S3.1's "zero ws_demo rows" line is superseded (D138). D132 log-allowlist discipline holds.
- done-check: local composite run: smoke PASS + drive all-claims-green from a cycled stack; the new step's claims red-provable (simulated re-home caught); composite runner number reported to the advisor (D137 watch live)
- result: reviewer APPROVE, 0 fixes in scope. Composite of record 88s (reviewer's own cold cycle: 13+18+57; drive 102/0; executor's 89s within noise); claim delta fully accounted; owner-pin red run reproduced (exactly 6 post-acceptance fails, precondition green — outer guard independent of inner tripwire); D139 end-to-end on the real row; revoked_at polled row-specific; D115 grep-proven; D160 guard red-proved twice, every-PR, stack untouched. Routed: compose-README S3.2 drive bullet (T7-rider executor). D142(3) wire line → integrator performs, bundle records (advisor confirmation pending). APPROVED.

### F1: honest mock-mode /signup and /login (D150)
- status: approved
- wave: 2
- depends: T6 (merged in tree — sequential ownership, D111 precedent)
- owns: apps/web/src/app/signup/page.tsx, apps/web/src/app/signup/actions.ts, apps/web/src/app/login/page.tsx, apps/web/src/app/login/actions.ts, and their tests (new test files as needed); extended by ruling: apps/web/src/app/invite/[id]/actions.ts (D152), apps/web/package.json line 5 + apps/web/src/server/data.ts + root README.md (D156/D157 riders)
- goal: D150 verbatim — in mock mode both pages render NO form: an honest static state (this deployment is the fictional-data prototype; signup runs in a self-hosted obstack) with two real pointers — the live demo (/app) and the cloud-preview waitlist on the landing page. Mode check short-circuits BEFORE getAuth()/authConfig() using the ONE existing data-mode predicate (no second mode definition, S2.3 L3); both server actions also short-circuit in mock (tripwire branch — no new error-vocabulary code, D129); copy per D140 present-tense truth.
- done-check: `cd apps/web && npm test && npm run build` green; unit test proves mock renders the honest no-form state with BETTER_AUTH_SECRET unset (no-env-touch property), red-provable by re-introducing the getAuth() call on the mock path
- result: reviewer FIXED (1 finding): executor's D152 invite test fixture "inv_mockmode" was refused by the INVITATION_ID alphabet and exited at notFound() above the guard — suite red at review; fixed to invmockmode, guard-disable red proof verified. D150 mechanics held (mode check first statement everywhere, one dataMode predicate, no vocab changes, T6 untouched); 198/0/15 + build green; mock build → both pages ○ Static (correct — mock branch precedes searchParams await), live/e2e unaffected. Two escalations → advisor (createContext shim vs package.json:5 constraint of record — shim measured safe, per-file child processes; build-time mode baking now costs signup, S2.3 L6 sharpened) — RULED: **D156** shim RATIFIED + package.json:5 sentence AMENDED (real component modules importable ONLY by tests walking returned element trees without rendering, per-file process isolation, shim named; runner-flag comment-rider ON the shim: --experimental-test-isolation=none would make the leak real); **D157** documentation now, enforcement stays M4 (one sentence at the dataMode definition + human-facing mode doc if any: OBSTACK_DATA_MODE is a BUILD-time input, serve only in the built mode — mock artifact cannot sign anyone up); the S2.3 L6 M4 gate is AMENDED on the record from "costs a missing badge" to "costs signup entirely". Riders applied by a fresh executor (original F1 executor stalled 3× — abandoned mid-report; reviewer's record + manager tree verification stand as F1's report of record). Rider re-verify: APPROVE — all four D156 elements in package.json:5 (JSON valid), shim rider matches the reviewer's own isolation measurement, D157 sentence verbatim + the sourced S2.3-L6 tail kept (verified against s2.3-team-plan.md:327), README paragraph true. **F1 CLOSED approved; both escalations discharged.**

### F2: D158 SQL placement + D159 mock-field deletions
- status: approved
- wave: 4
- depends: T4, T5 (closed — D126 small-ordered-edit class, neither cycle reopens)
- owns: apps/web/src/server/invites.ts, apps/web/src/app/app/settings/page.tsx, apps/web/src/mock/workspace.ts, apps/web/src/components/settings/SettingsSuite.tsx
- goal: two ordered diffs. (1) D158: ORG_NAME_SQL + a getOrgName(orgId) reader move from settings/page.tsx into src/server/invites.ts beside listOrgMembers (the existing org read surface); page consumes the reader; no new module, no auth.api call (D143 fence). (2) D159: mock/workspace.ts loses lastUsed and the scope union; the mock suite component loses the chip/column render — mock renders the REAL product's shape with fictional values (lastUsed exhibits the liveness column D138 refused; scope exhibits a key-class axis the product doesn't have).
- done-check: `cd apps/web && npm test` both DSN modes + `npm run build` green (both build modes); mock settings still renders truthfully (no dangling references)
- result: reviewer APPROVE, 0 fixes. Page holds zero SQL; getOrgName module-style with a live injection probe proving D11 (bound not interpolated); D159 fields deleted as whole elements, one explanatory-comment grep hit repo-wide; mtime sweep proves exactly the 4 owned files; T4 surface undisturbed (exports = prior 13 + getOrgName). Non-blocking coverage note discharged: two getOrgName asserts added to invites.integration.test.ts (manager-ratified one-file extension), 255/255 with-DSN. APPROVED.

## Escalations log
<!-- Manager appends: {T#} — question → advisor decision (one line each). -->
- T4 — advisor SIGNED as fixed. **D152**: /invite action joins the D150 guard — F1's owns extended by exactly apps/web/src/app/invite/[id]/actions.ts + an action-level standing-test case; standing property: every future auth-surface server action joins this guard + test at creation. **D153**: expired-invitation lifecycle = recorded absence, pre-registered to the **M4 inventory** (beside D119 BETTER_AUTH_URL, D123 password rotation, D132 log-volume) — no reaper, no cancel widening this sprint.
- T4 (open seam, pre-registered for F1/integration): /invite/[id] follows the D150 mock-mode posture but F1's signup/mock-mode.test.ts does not cover it — the D150 standing test's owner extends it (route to F1 when it reports, else integration).
- T8 — critical return → **ADVISOR-SIGNED**; D142(3) ownership CONFIRMED: integrator performs the wire line at the gate, bundle records commands verbatim, with two restated properties — content-aware attribution (never counts, S3.1 L1) and the 401-after-revoke wait named against keyCacheTTL (the property is revocation ≤TTL, D146). Compose-README S3.2 drive bullet landed (T7-rider). D137: real-runner composite reported with the exit bundle.
- T5 — critical return → **ADVISOR-SIGNED**. **D158**: ORG_NAME_SQL moves into invites.ts as getOrgName (SQL lives in src/server — one-definition layering rule; first counter-example refused before it breeds). **D159**: D144 reaches the mock key's lastUsed + scope fields — both DELETED (mock renders the real shape with fictional values; D13 fiction on the prototype deploy). D158+D159 = fix task **F2**, one cycle. **D160**: standing guard ordered per S3.1 L2 — web.yml grep step `git grep -nE "ob_(live|read)_" -- ':!.planning'` zero-hit with the D144/D159 pointer message, red-provable by planting a literal; owner T8 (open workflows ownership); stack unpolluted.
- T5 — D144's repo-wide grep reached mock/security.ts + mock/connectors.ts outside T5's owns → manager-RATIFIED extension (D144 orders the repo-wide correction; no other owner); applied — repo-wide ob_live_/ob_read_ now zero outside .planning.
- T3 — critical return → **ADVISOR-SIGNED**; **D139 AMENDED of record: the hash contract carries BOTH vectors** (ok_dev_local ASCII + ok_live_é non-ASCII = e9d4d753fa6e21f9da99b36f719d76ad9d01526bf9b47fcf2c5b1925a0b458db) — the ASCII vector alone cannot catch an encoding swap; any future implementation meets both or fails.
- T7 — critical return → **ADVISOR-SIGNED** (fixed→closed; wire evidence independently reproduced by reviewer).
- T7 — helm chart README:264-266 stale env-map paragraph found outside T7's owns → manager-RATIFIED one-file extension (same sweep class, no other owner), routed to T7's executor.
- F1 — original executor stalled 3× (watchdog; last stall on a report-only ask) — ABANDONED; the reviewer's FIXED record + the manager's tree verification are F1's report of record; D156/D157 riders → fresh executor.
- T1 — critical return → **ADVISOR-SIGNED** on the approve. **D154**: lazy-series operator note = one present-tense sentence riding T7's README sweep (missing series means no events, not a broken pipeline). **D155**: the 10k cache bound bounds NEGATIVES (attacker-controlled dimension); positives bounded by real api_keys row count — as designed; pre-registered to S3.3's cache-widening breakdown (memory story restated for widened entries at large-tenant row counts).
- T1 — out-of-scope edits manager-RATIFIED (S3.1 T3 precedent; advisor notified): go.mod (puddle/x-sync indirect for pgxpool), receive/{codec_test,receive_test}.go (auth.New map→Resolver testResolver swap). Cross-task seam to advisor: metrics.Init now uncalled (T1 deleted its only caller; boot-time per-tenant series pre-materialization refused by executor as against D29's grain) — RULED **D151**: DELETE metrics.Init outright riding T1's open cycle (bounded seed refused as arbitrary special case; behavior change accepted and NAMED — per-workspace series appear on first event, standard Prometheus posture; harness grep for pre-traffic zero-value assertions escalates on hit, never adapted). T2's signature undisturbed by design. Both T1 ratifications CONFIRMED by advisor.
- T2 — critical-item return → **ADVISOR-SIGNED**; D29 BLOCKING gate DISCHARGED. No metrics_test.go this sprint (pricing-suite coverage IS the contract test — one increment site, one door; a direct file = second definition, S2.3 L3). Rider: source-order-is-load-bearing comment on the cap test (D131 precedent), riding T2's files — APPLIED by T2's executor (gofmt clean, pricing suite green).
- T6 — mock-mode /signup always fails on the prototype deploy → **D150**: honest mock-mode signup/login surface (no form in mock, mode check BEFORE getAuth() — mock never reads BETTER_AUTH_SECRET/touches Postgres; actions short-circuit as tripwire, no new error vocabulary; /login identical treatment; unit test with secret unset, red-provable by re-introducing getAuth on the mock path; permanent behavior, not a stopgap, no SampleMark — D125 precedent), NEW FIX TASK F1, CTA unchanged; (a) CTA gating and (b) live deploy REFUSED
- kickoff — breakdown + Q1–Q8 → advisor **D138–D149** (recorded under Advisor decisions): T4 boundary corrected to ZERO doors (D143); T1 seed supersedes D115 for ws_demo with the supersession stated (D138); T5 gains D141/D144; T8 gains the D142(3) runbook line + the ws_demo-row expectation; T3 added to critical items (cross-language hash seam); waves ratified; all eight questions closed — no open kickoff item.

## Integration
- verdict: **FIXED-GO** (round 1, 2026-08-19)
- rounds: 1
- e2e: cycled compose from `--profile '*' down -v`: up 12s · smoke PASS 19s · drive **102/0** 58s (composite 89s) · ddl-drift PASS (15 stmts identical) · post-drive Postgres per D138 supersession: one ws_demo/org_demo row with 0 member rows + two stranger workspaces; api_keys = key_dev_local (live) + drive's ok_live_13b7 (revoked) · collector+sdk profiles into ws_demo under ok_dev_local, zero 401s · go test ./... both DSN modes ×2 · npm 236/214/0/22 + 255/255/0 · builds mock ○ / live ƒ · tsc clean · helm lint 0 failed · go mod tidy clean · **virgin-kind acceptance PASS 182s** (both migrate Jobs Complete, ingest 2/2, D37/D43/D38(e) PASS, chart Postgres carries the seed row) · sweeps clean (OBSTACK_API_KEYS 4 prose lines; metrics.Init gone; ob_(live|read)_ zero hits) · eslint 6 pre-existing errors in non-S3.2 files (no CI job runs lint — recorded, not S3.2's)
- seam findings: **D142(3) wire evidence PERFORMED by the integrator, commands verbatim with fixing values in the evidence file** (workspace ws_db11d22d30ec8e1c, key ok_live_6d5c…fae2, content token zzwiremszyuqsb6opr): curl with issued Bearer → 200; ClickHouse content-aware attribution (1 row in the key's workspace, 0 in every other incl. ws_demo); revoke in UI → in-TTL resend 200 **stated as the D146 cost** (revocation ≤ keyCacheTTL, not instant); sleep 35s past keyCacheTTL → 401. Evidence: scratchpad/s3.2-integration-evidence.txt (the transcript's one FAIL line is the runner's own baseline arithmetic, re-measured clean — 2 spans = 2 accepted sends, the 401 wrote none).
- fix tasks: integrator fix 1 (in-charter): the D139 amendment (both vectors) postdated T1's close, so the Go suite lacked the non-ASCII vector — `hashToken("ok_live_é")` assertion added to keystore/store_test.go beside the ASCII one (values re-verified via shasum outside both processes; latin1 decoy proves it load-bearing); gofmt/vet clean, full Go suite re-run both DSN modes.
- commit discipline: 63 entries, every one maps to an owned list or a recorded ratification; lockfiles untouched (go.mod tidy-clean); nothing foreign; .planning + scratchpad out of the sprint commit's file set. data.ts's diff is exactly D157's ordered sentence (owns list updated).

## Goal check (Step 7, one entry per iteration)
- iteration 0: **MET (2026-08-19) — advisor clause-by-clause verdict of record; 0/3 iterations used.** All four run-goal clauses on evidence (issue/revoke shown-once with the dual-vector hash contract; additive-never-re-home invites at zero opened doors; D142(3) content-aware wire attribution with the in-TTL 200 stated as the D146 cost; wire format byte-unchanged with the continuity row). Inheritance list discharged incl. the D29 BLOCKING pair. **Exit bundle SIGNED, effective at the PR tip on eight green checks** — recorded verbatim at `.planning/2026-08-19-s3.2-exit-evidence.md` (with the D142(3) command block embedded as the durable copy). D137: real-runner composite returns to the advisor from the PR run.

## Retro (filled at end of run)
- tasks: 10/10 approved (T1–T8, F1, F2), deferred: none
- breakdown corrections by advisor: kickoff D138–D149 (T4 recut to zero doors — the largest correction; D115 supersession stated; one-definition contracts pinned incl. the dual-vector hash) + mid-run D150–D160 (mock-mode surface class D150/D152/D156/D157; layering + mock-truth D158/D159; standing guard D160; D139 amended on the reviewer's decoy analysis)
- reviewer verdicts: 5 approve / 4 fixed / 1 must-fix (T3's — a coverage gap in another task's file, routed and closed same round)
- lesson: a ruling amended after a task closes leaves the closed task's suite asserting the OLD contract — the integrator caught the Go suite missing the D139 second vector because the amendment postdated T1; when a one-definition contract is amended, sweep every already-closed implementer of it in the same round. Also: repeated 600s watchdog stalls are infrastructure, not task difficulty — resume-with-context first, fresh minimal executor for small remainders; three stalls on one agent means abandon it.
