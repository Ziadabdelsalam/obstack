apiVersion: v2
name: obstack
description: >-
  The obstack stack on Kubernetes — CI-grade ClickHouse, the ingest
  Deployment plus its migrate Job, the obstack-collector DaemonSet, and the
  demo app. M2 scope only (D35); M4 extends this chart in place rather than
  replacing it — see README.md's scope boundary.
type: application
version: 0.1.0
appVersion: "0.1.0"
